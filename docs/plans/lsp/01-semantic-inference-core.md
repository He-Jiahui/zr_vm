---
doc_type: plan-detail
related_code:
  - zr_vm_parser/include/zr_vm_parser/cfg.h
  - zr_vm_parser/include/zr_vm_parser/semantic.h
  - zr_vm_parser/include/zr_vm_parser/semantic_query.h
  - zr_vm_parser/include/zr_vm_parser/semantic_facts.h
  - zr_vm_parser/include/zr_vm_parser/type_inference.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_control_flow.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_catch.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_constants.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_throw_bindings.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_throw_branch_bindings.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_throw_loop_bindings.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_throw_profile.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_throw_switch_bindings.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/cfg_internal.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/dataflow.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/dataflow.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_definite_assignment.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_definite_assignment.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/semantic_definite_assignment.c
  - zr_vm_parser/src/zr_vm_parser/semantic.c
  - zr_vm_parser/src/zr_vm_parser/semantic/semantic_query.c
  - zr_vm_parser/src/zr_vm_parser/type_inference.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_supported_count.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_supported_count_wrapped.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_supported_count_wrapped.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_direct_range.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_direct_range.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_bitwise_identity_bitwise_not_range.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_core.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_native.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_interval_logic.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_numeric_range.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_numeric_range.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_branch_refinement.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_branch_assignment_join.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_branch_assignment_segments.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_branch_assignment_segments.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_entry.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_join.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_join_internal.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_replay.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_sequence.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_sequence.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_scope.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_syntax.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_syntax.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_nested_if.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_nested_if.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_nested_loop.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_nested_loop.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_self_dependency.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_self_dependency.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_target_zero.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_loop_assignment_target_zero.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_semantic_facts.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/lsp_semantic_definition_query.h
  - zr_vm_language_server/src/zr_vm_language_server/semantic/lsp_semantic_definition_query.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/lsp_semantic_query.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/semantic_analyzer_constant_condition.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/semantic_analyzer_query_diagnostics.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/semantic_analyzer_typecheck.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_semantic_query_diagnostics.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_internal.h
  - zr_vm_parser/src/zr_vm_parser/compiler.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_statement.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_statement_flow.c
  - zr_vm_core/include/zr_vm_core/ownership.h
---

# 01 · 语义推断核心：表达式 / 数值 / 引用 / 所有权 / 控制流

目标：把今天「表达式级、零散」的推断，升级为「以控制流图（CFG）为骨架、数据流为引擎、语义事实为产物」的统一语义层，让编译器、LSP、debug、REPL 都消费同一份事实。

设计原则：
1. **一份事实，多处消费**。所有推断结果落到 `semantic_facts.h` 的六类事实里，新增 CFG/dataflow 也只是「事实的生产者」。
2. **可局部、可降级**。每个分析既能整模块跑，也能限定到单函数 / 单作用域跑（供 LSP）；任何一步失败都产出「未知」而不是崩溃或全盘放弃。
3. **不改运行时表面**。所有权运行时（`ownership.h`）不动，只增强编译期推断与诊断。

## 状态与产出记录

| 阶段 | 状态 | 完成时间 | 完成项目 | 验证 |
| --- | --- | --- | --- | --- |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Counterpart Deep Chain Module Split | 已完成 | 2026-07-06 07:23 +08:00 | 按 06:34 记录要求，在继续新增 deep counterpart helper 前先做行为保持拆分：新增内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart_deep.c/.h`，将 zero-minus / unary-minus zero-minus bitwise-not OR counterpart 深层 helper 链从 925 行主 counterpart module 抽出；原 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c` 保留 safe negation、unary-minus direct leaf、zero-wrapper skipper 和 public dispatch，并通过新的 `type_inference_bitwise_identity_expression_bitwise_not_or_deep_counterpart_leaf_int64_range` 聚合入口按原顺序调用深层链。主 module 降至 141 行，deep module 803 行，deep header 11 行。该切片不新增语义覆盖、不改变 parser/LSP case 数，不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary subtraction、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | Behavior-preserving GREEN：拆分前 WSL gcc focused baseline 通过 private parser 260 tests/0 failures + private-mask LSP PASS；拆分后独立 WSL gcc Ninja (`build-wsl-gcc-lsp-counterpart-deep`) 同组 260/0 + LSP PASS，独立 WSL clang Ninja (`build-wsl-clang-lsp-counterpart-deep`) 同组 260/0 + LSP PASS，Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run 260/0 + LSP PASS，且 MSVC/GCC/Clang 构建日志确认 `type_inference_bitwise_identity_bitwise_not_or_counterpart_deep.c` 编译/链接。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 06:34 +08:00 | 在 06:09 counterpart 基础上继续按阶段 2 数值区间/语义事实路线推进：新增外层 exact-zero-left `zero -` 包住的 deep unary-minus counterpart reader，使 `zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour))))))))))))))))` 这类 one-sided all-ones OR counterpart 可进入 private bitwise-not signed all-ones mask operand；outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现限定在 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`，新增私有 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用 06:09 的 unary-minus reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。parser private target 从 252 扩到 260 cases，private-mask LSP target 从 216 扩到 224 queries；新增 8 个 parser cases 继续放在 deep cases 文件，新增 8 个 LSP queries 继续放在 deep query helper，deep parser 注册数到 72，deep LSP query 数到 80。当前相关行数：bitwise-not range source 591，counterpart module 925，counterpart header 16，private parser main 696，OR-leaf cases 1016，parser deep cases/header 656/6，private-mask LSP entry 96，OR counterpart query source/header 963/10，deep query source/header 575/10，main supported-count LSP query 973，`tests/CMakeLists.txt` 6940。counterpart module 这次跨过 900 行，但新增代码仍只属于同一条 private deep counterpart chain；本阶段不拆出新模块以避免打断静态 helper 顺序，若后续继续新增该链 helper，应先抽出 deep alternating counterpart chain。scope 继续保持 private/local：不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、unsupported shift-count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused build 通过，private parser target 扩到 260 tests 后新增 8 semantic failures（旧 `0` range），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 260 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 260 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `buildParser=0 buildLsp=0 parser=0 lsp=0`，且构建日志确认 production counterpart source、parser deep cases 与 deep query source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 06:09 +08:00 | 在 05:39 counterpart 基础上继续按阶段 2 数值区间/语义事实路线推进：新增外层 unary `-` 包住的 deep zero-minus counterpart reader，使 `-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour)))))))))))))))` 这类 one-sided all-ones OR counterpart 可进入 private bitwise-not signed all-ones mask operand；outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现限定在 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`，新增私有 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，operand 复用 05:39 的 zero-minus reader，并复用 safe negation guard；调度点在该 zero-minus reader 后、same-identifier fallback 前。parser private target 从 244 扩到 252 cases，private-mask LSP target 从 208 扩到 216 queries；新增 8 个 parser cases 继续放在 deep cases 文件，新增 8 个 LSP queries 继续放在 deep query helper，deep parser 注册数到 64，deep LSP query 数到 72。当前相关行数：bitwise-not range source 591，counterpart module 877，counterpart header 16，private parser main 696，OR-leaf cases 1016，parser deep cases/header 584/6，private-mask LSP entry 96，OR counterpart query source/header 963/10，deep query source/header 518/10，main supported-count LSP query 973，`tests/CMakeLists.txt` 6940。scope 继续保持 private/local：不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、unsupported shift-count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：先修正新增 parser RUN_TEST 注册名，使 8 个新增函数实际进入 runner；随后 WSL gcc focused build 通过，private parser target 扩到 252 tests 后新增 8 semantic failures（旧 `0` range），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 252 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 252 tests/0 failures + LSP PASS；Windows MSVC (`build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，且构建日志确认 production counterpart source、parser deep cases 与 deep query source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 05:39 +08:00 | 在 05:13 counterpart 基础上继续按阶段 2 数值区间/语义事实路线推进：新增外层 exact-zero-left `zero -` 包住的 deep unary-minus counterpart reader，使 `zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour))))))))))))))` 这类 one-sided all-ones OR counterpart 可进入 private bitwise-not signed all-ones mask operand；outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现限定在 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`，新增私有 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用 05:13 的 unary-minus reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。parser private target 从 236 扩到 244 cases，private-mask LSP target 从 200 扩到 208 queries；新增 8 个 parser cases 继续放在 deep cases 文件，新增 8 个 LSP queries 继续放在 deep query helper，deep LSP query 数到 64。当前相关行数：bitwise-not range source 591，counterpart module 832，counterpart header 16，private parser main 696，OR-leaf cases 1016，parser deep cases/header 512/6，private-mask LSP entry 96，OR counterpart query source/header 963/10，deep query source/header 461/10，main supported-count LSP query 973，`tests/CMakeLists.txt` 6940。scope 继续保持 private/local：不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、unsupported shift-count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused build 通过，private parser target 扩到 244 tests 后新增 8 semantic failures（旧 `0` range），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 244 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 244 tests/0 failures + LSP PASS；Windows MSVC (`build-msvc-lsp-bitwise-zero`) focused build/run parser=0、LSP=0，且构建日志确认 production counterpart source、parser deep cases 与 deep query source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 05:13 +08:00 | 在 04:49 counterpart 基础上继续按阶段 2 数值区间/语义事实路线推进：新增外层 unary `-` 包住的 exact-zero-left `zero -` deep counterpart reader，使 `-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour)))))))))))))` 这类 one-sided all-ones OR counterpart 可进入 private bitwise-not signed all-ones mask operand；outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现限定在 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`，新增私有 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，operand 复用 04:49 的 zero-minus reader，并复用 safe negation guard；调度点在该 zero-minus reader 后、same-identifier fallback 前。parser private target 从 228 扩到 236 cases，private-mask LSP target 从 192 扩到 200 queries；新增 8 个 parser cases 继续放在 deep cases 文件，新增 8 个 LSP queries 继续放在 deep query helper。当前相关行数：bitwise-not range source 591，counterpart module 784，counterpart header 16，private parser main 696，OR-leaf cases 1016，parser deep cases/header 440/6，private-mask LSP entry 96，OR counterpart query source/header 963/10，deep query source/header 404/10，main supported-count LSP query 973，`tests/CMakeLists.txt` 6940。scope 继续保持 private/local：不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、unsupported shift-count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused build 通过，private parser target 扩到 236 tests 后新增 8 semantic failures（旧 `0` range），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 236 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 236 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 04:49 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 04:24 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 exact-zero-left `zero -` 包住的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct leaf 形态，让 `zero - (-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour))))))))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用已落地的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 220 cases 扩到 228 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 184 queries 扩到 192 queries。8 个新增 parser cases 继续放入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_deep_leaf_cases.c/.h`，未继续追加到 1016 行的 OR-leaf cases 文件；8 个新增 LSP queries 继续放在 deep query helper。当前 bitwise-not range source 591 行，counterpart module 739 行，counterpart header 16 行，private parser 主文件 696 行，既有 OR-leaf cases 文件 1016 行，parser deep cases/header 368/6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，deep query source/header 347/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6940 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 228 tests 后新增 8 semantic failures（旧 `0` range），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 228 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 228 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认 parser deep cases、deep query source 与 production counterpart source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 04:24 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 04:07 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 unary `-` 包住的 exact-zero-left `zero -` deep counterpart 形态，让 `-(zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour)))))))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，其 operand 复用已落地的 zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 zero-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 212 cases 扩到 220 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 176 queries 扩到 184 queries。8 个新增 parser cases 继续放入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_deep_leaf_cases.c/.h`，未继续追加到 1016 行的 OR-leaf cases 文件；8 个新增 LSP queries 继续放在 deep query helper。当前 bitwise-not range source 591 行，counterpart module 691 行，counterpart header 16 行，private parser 主文件 696 行，既有 OR-leaf cases 文件 1016 行，parser deep cases/header 296/6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，deep query source/header 291/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6940 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 220 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 220 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 220 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认 parser deep cases、deep query source 与 production counterpart source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 04:07 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 03:51 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 exact-zero-left `zero -` 包住的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct leaf 形态，让 `zero - (-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour))))))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用已落地的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 204 cases 扩到 212 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 168 queries 扩到 176 queries。8 个新增 parser cases 继续放入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_deep_leaf_cases.c/.h`，未继续追加到 1016 行的 OR-leaf cases 文件；8 个新增 LSP queries 继续放在 deep query helper。当前 bitwise-not range source 591 行，counterpart module 646 行，counterpart header 16 行，private parser 主文件 696 行，既有 OR-leaf cases 文件 1016 行，parser deep cases/header 224/6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，deep query source/header 235/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6940 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 212 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 212 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 212 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认 parser deep cases、deep query source 与 production counterpart source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 03:51 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 03:20 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 unary `-` 包住的 exact-zero-left `zero -` deep counterpart 形态，让 `-(zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour)))))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，其 operand 复用已落地的 zero-minus unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 zero-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 196 cases 扩到 204 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 160 queries 扩到 168 queries。8 个新增 parser cases 继续放入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_deep_leaf_cases.c/.h`，未继续追加到 1016 行的 OR-leaf cases 文件；8 个新增 LSP queries 继续放在 deep query helper。当前 bitwise-not range source 591 行，counterpart module 598 行，counterpart header 16 行，private parser 主文件 696 行，既有 OR-leaf cases 文件 1016 行，parser deep cases/header 152/6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，deep query source/header 179/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6940 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 204 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 204 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 204 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认 parser deep cases、deep query source 与 production counterpart source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 03:20 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 03:01 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 exact-zero-left `zero -` 包住的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct leaf 形态，让 `zero - (-(zero - (-(zero - (-(zero - (zero - (~negativeFour))))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用已落地的 unary-minus zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 188 cases 扩到 196 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 152 queries 扩到 160 queries。为遵守模块化约束，新增 8 个 parser cases 放入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_deep_leaf_cases.c/.h`，未继续追加到 1016 行的 OR-leaf cases 文件；8 个新增 LSP queries 继续放在 deep query helper。当前 bitwise-not range source 591 行，counterpart module 553 行，counterpart header 16 行，private parser 主文件 696 行，既有 OR-leaf cases 文件 1016 行，新增 parser deep cases/header 80/6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，deep query source/header 123/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6940 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 196 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 196 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 196 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认 new parser deep cases、deep query source 与 production counterpart source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 03:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 02:35 LSP query modularization 后继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 unary `-` 包住的 exact-zero-left `zero - (-(zero - (-(zero - (zero - (~direct))))))` 形态，让 `-(zero - (-(zero - (-(zero - (zero - (~negativeFour)))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，其 operand 复用已落地的 zero-minus unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 zero-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 180 cases 扩到 188 cases；private-mask LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 144 queries 扩到 152 queries，并将 8 个新增 deep counterpart queries 放入 `tests/language_server/test_lsp_bzms_private_not_mask_or_counterpart_deep_query_cases.c/.h`，入口文件只新增 deep helper 调用。当前 bitwise-not range source 591 行，counterpart module 505 行，counterpart header 16 行，private parser 主文件 694 行，拆分 OR-leaf cases 文件 1016 行，header 6 行，private-mask LSP entry 96 行，既有 OR counterpart query source/header 963/10 行，新增 deep query source/header 67/10 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6939 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、different-identifier structural equality、arbitrary non-all-ones operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 188 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 188 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 188 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，并确认新 production counterpart source 与 deep query source 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Private Bitwise-Not Mask OR Counterpart LSP Query Modularization Maintenance | 已完成 | 2026-07-06 02:35 +08:00 | 按阶段 2 的数值区间/语义事实维护路线，在继续追加 private-mask OR counterpart LSP 查询前完成 behavior-preserving 查询承载拆分：将 `tests/language_server/test_lsp_bzms_private_not_mask_count_query.c` 中的 136 个 one-sided all-ones OR counterpart query 移入 `tests/language_server/test_lsp_bzms_private_not_mask_or_counterpart_query_cases.c/.h`，入口文件保留状态初始化、8 个 wrapped/private count-side smoke 查询、统一 PASS/FAIL 和对新 helper 的一次调用；入口文件降到 87 行，新 query source 为 959 行，header 为 7 行，`tests/CMakeLists.txt` 将新 source 接入原 `zr_vm_lsp_bzms_private_not_mask_query_test` target。该维护切片不新增或删除 semantic fact 形态、production inference、parser cases、LSP query cases、public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、OR counterpart 语义范围、arbitrary normalization、fixed point 或 CFG-wide loop dataflow；private parser 仍为 180 cases，private-mask LSP query 仍为 144 queries。 | Behavior-preserving baseline/GREEN：拆分前 WSL gcc private-mask LSP PASS；拆分后 WSL gcc private parser 180 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 180 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，构建日志确认新 `test_lsp_bzms_private_not_mask_or_counterpart_query_cases.c` 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 02:14 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 01:54 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 exact-zero-left `zero -` 包住的 unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct leaf 形态，让 `zero - (-(zero - (-(zero - (zero - (~negativeFour))))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用已落地的 unary-minus zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在该 unary-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 172 cases 扩到 180 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 136 queries 扩到 144 queries。当前 bitwise-not range source 保持 591 行，counterpart module 460 行，counterpart header 16 行，private parser 主文件 694 行，拆分 OR-leaf cases 文件 944 行，header 6 行，private-mask LSP query 1044 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 180 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact；初始 LSP RED 暴露新增 query 名过长导致 URI 格式化失败，缩短 8 个新 query 名后 RED 稳定为语义旧范围。GREEN focused：WSL gcc private parser 180 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 180 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 01:54 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 01:35 counterpart 基础上继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 unary `-` 包住的 exact-zero-left `zero - (-(zero - (zero - (~direct))))` 形态，让 `-(zero - (-(zero - (zero - (~negativeFour)))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 unary `-`，其 operand 复用已落地的 zero-minus unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在 zero-minus unary-minus zero-minus zero-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 164 cases 扩到 172 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 128 queries 扩到 136 queries。当前 bitwise-not range source 保持 591 行，counterpart module 412 行，counterpart header 16 行，private parser 主文件 694 行，拆分 OR-leaf cases 文件 872 行，header 6 行，private-mask LSP query 988 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 172 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 172 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 172 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 01:35 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 01:15 counterpart reader 拆分后继续用 TDD RED/GREEN 收窄扩展 private bitwise-not signed all-ones `&` mask 的 one-sided all-ones OR counterpart：现在可识别外层 exact-zero-left `zero -` 包住的 unary-minus zero-minus zero-minus bitwise-not direct leaf 形态，让 `zero - (-(zero - (zero - (~negativeFour))))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现位于拆分后的内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c`：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，要求外层 binary `-` 左侧递归证明 exact zero，右侧复用已落地的 unary-minus zero-minus zero-minus bitwise-not direct reader，并复用 safe negation guard；调度点在 unary-minus zero-minus zero-minus reader 后、same-identifier fallback 前。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 156 cases 扩到 164 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 120 queries 扩到 128 queries。当前 bitwise-not range source 保持 591 行，counterpart module 367 行，counterpart header 16 行，private parser 主文件 694 行，拆分 OR-leaf cases 文件 800 行，header 6 行，private-mask LSP query 932 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct/zero-minus-unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 164 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0` range fact。GREEN focused：WSL gcc private parser 164 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 164 tests/0 failures + LSP PASS；Windows MSVC (`build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Private Bitwise-Not OR Counterpart Reader Modularization Maintenance | 已完成 | 2026-07-06 01:15 +08:00 | 按阶段 2 的数值区间/语义事实维护路线，完成 behavior-preserving private bitwise-not OR counterpart reader 模块化拆分：把 safe negation guard、zero bitwise-identity wrapper skipper、private OR counterpart leaf reader 调度和 direct / zero-wrapped / bitwise-not / unary-minus / zero-minus / unary-minus zero-minus / zero-minus zero-minus / unary-minus zero-minus zero-minus counterpart readers 从 `type_inference_bitwise_identity_bitwise_not_range.c` 抽到内部 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c/.h`。原 bitwise-not range source 从 913 行降到 591 行，新内部 `.c` 为 319 行、`.h` 为 16 行；CMake file glob 在 WSL clang 与 Windows MSVC focused build 中重新配置并编译新源。该维护切片不新增 semantic fact 形态、public header surface、parser/LSP 用例、ordinary supported-count target、private mask count-side reader、wrapped supported-count readers、OR counterpart 语义范围、arbitrary normalization、fixed point 或 CFG-wide loop dataflow；private parser 仍为 156 cases，private-mask LSP query 仍为 120 queries。 | Behavior-preserving focused GREEN：WSL gcc private parser 156 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 156 tests/0 failures + LSP PASS；Windows MSVC (`build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`，且构建日志确认新 `type_inference_bitwise_identity_bitwise_not_or_counterpart.c` 已编译。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 00:53 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可识别外层 unary `-` 包住的 exact-zero-left `zero - (zero - (~direct))` 形态，让 `-(zero - (zero - (~negativeFour)))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，在既有 direct、zero-wrapped bitwise-not direct、unary-minus bitwise-not direct、zero-minus bitwise-not direct、unary-minus zero-minus bitwise-not direct、zero-minus zero-minus bitwise-not direct readers 后、same-identifier fallback 前使用，并复用刚落地的 zero-minus zero-minus bitwise-not direct reader 与现有 safe negation guard。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 148 cases 扩到 156 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 112 queries 扩到 120 queries。当前 bitwise-not range source 913 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 728 行，header 6 行，private-mask LSP query 876 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行；因 bitwise-not range source 已进入 900-1100 行拆分预警区，下一次继续增加 counterpart reader 前应优先把 private OR counterpart leaf readers 抽成更窄模块。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct/unary-minus-zero-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 156 tests 后新增 8 failures（旧 `0..0` / missing fact），private-mask LSP target 新增 8 queries 返回旧 `0..0` 或缺失 range fact。GREEN focused：WSL gcc private parser 156 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 156 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 00:35 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可识别外层 exact-zero-left `zero -` 包住的既有 exact-zero-left `zero - (~direct)` 形态，让 `zero - (zero - (~negativeFour))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，在既有 direct、zero-wrapped bitwise-not direct、unary-minus bitwise-not direct、zero-minus bitwise-not direct、unary-minus zero-minus bitwise-not direct readers 后、same-identifier fallback 前使用，并要求外层 left 递归证明 exact zero、right 由既有 zero-minus bitwise-not direct reader 证明，同时复用现有 safe negation guard。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 140 cases 扩到 148 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 104 queries 扩到 112 queries。当前 bitwise-not range source 868 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 656 行，header 6 行，private-mask LSP query 820 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct/zero-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 148 tests 后新增 8 failures（旧 `0..0` / missing fact），private-mask LSP target 新增 8 queries 返回旧 `0..0` 或缺失 range fact。GREEN focused：WSL gcc private parser 148 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 148 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-06 00:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可识别外层 unary `-` 包住的 exact-zero-left `zero - (~direct)` 形态，让 `-(zero - (~negativeFour))` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_bitwise_not_direct_leaf_int64_range`，在既有 direct、zero-wrapped bitwise-not direct、unary-minus bitwise-not direct、zero-minus bitwise-not direct readers 后、same-identifier fallback 前使用，并复用现有 safe negation guard。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 132 cases 扩到 140 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 96 queries 扩到 104 queries。当前 bitwise-not range source 820 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 584 行，header 6 行，private-mask LSP query 764 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct/unary-minus-zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 140 tests 后新增 8 failures（旧 `0..0` / missing fact），private-mask LSP target 新增 8 queries 返回旧 `0..0` 或缺失 range fact。GREEN focused：WSL gcc private parser 140 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 140 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Zero-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 23:53 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可识别 exact-zero-left `zero - (~direct)` 形态，让 `zero - (~negativeFour)` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_zero_minus_bitwise_not_direct_leaf_int64_range`，在既有 direct、zero-wrapped bitwise-not direct、unary-minus bitwise-not direct readers 后、same-identifier fallback 前使用，并要求 left 递归证明 exact zero、right 由 zero-wrapped bitwise-not direct reader 证明，同时复用现有安全取负 guard。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 124 cases 扩到 132 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 88 queries 扩到 96 queries。当前 bitwise-not range source 775 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 512 行，header 6 行，private-mask LSP query 708 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct/zero-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 132 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 132 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 132 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Unary-Minus Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 23:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可识别外层 unary `-` 包住的 bitwise-not direct leaf，让 `-(~negativeFour)` 这类有界 counterpart 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_unary_minus_bitwise_not_direct_leaf_int64_range`，在既有 direct reader 与 zero-wrapped bitwise-not direct reader 后、same-identifier fallback 前使用，并复用现有安全取负 guard 防止 `INT64_MIN` 取负。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 116 cases 扩到 124 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 80 queries 扩到 88 queries。当前 bitwise-not range source 727 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 440 行，header 6 行，private-mask LSP query 652 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct/unary-minus-bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 124 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 124 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 124 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask One-Sided All-Ones OR Bitwise-Not Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 23:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones OR leaf reader 上继续收窄扩展：one-sided all-ones OR counterpart 现在可复用既有 `type_inference_bitwise_identity_expression_zero_wrapped_bitwise_not_direct_int64_range`，让 `~negativeFour` 这类 bitwise-not direct leaf 与递归证明的 exact signed all-ones leaf 组合后进入 outer `~(...)` 的 private signed all-ones mask operand，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现仍限于 bitwise-not range module：`type_inference_bitwise_identity_expression_bitwise_not_or_counterpart_leaf_int64_range` 在既有 direct reader 后、same-identifier fallback 前增加 zero-wrapped bitwise-not direct reader；exact all-ones 一侧仍必须递归证明 exact `-1..-1`。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 108 cases 扩到 116 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 72 queries 扩到 80 queries。当前 bitwise-not range source 682 行，private parser 主文件保持 694 行，拆分 OR-leaf cases 文件 368 行，private-mask LSP query 596 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 保持 6937 行。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier/bitwise-not-direct leaves、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 116 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 116 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 116 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask OR Leaf Parser Harness Split | 已完成 | 2026-07-05 23:07 +08:00 | 按阶段 2 的数值区间/语义事实路线，在继续追加 private bitwise-not mask parser 覆盖前先完成测试边界拆分：将 32 个 one-sided all-ones OR leaf parser cases 移入 `tests/parser/bitwise_zero_minus_shift_supported_count_private_not_mask_or_leaf_cases.c` 与 6 行 header，并由原主文件注册；主 private parser 文件从 980 行降到 694 行，新 cases 文件 296 行，`tests/CMakeLists.txt` 增至 6937 行。parser 总覆盖保持 108 cases 不变，主文件 `RUN_TEST` 76 个加拆分文件 32 个；private-mask LSP query 保持 72 queries/540 行，bitwise-not range source 保持 674 行，main supported-count LSP query 保持 973 行。该阶段只拆分测试承载边界，不改变语义事实、production inference、LSP 查询行为、public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart scope、different-identifier structural equality、arbitrary non-all-ones operands、negative/out-of-range shift counts、unsupported count algebra、fixed point 或 CFG-wide loop dataflow。 | Behavior-preserving GREEN：WSL gcc focused private parser 108 tests/0 failures + private-mask LSP PASS；WSL clang focused private parser 108 tests/0 failures + private-mask LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not One-Sided All-Ones OR Same-Identifier Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 22:48 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) \| (span & span)))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (((span \| (span - zero)) \| (~(unit - unit)))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_bitwise_not_or_counterpart_leaf_int64_range`，让既有 one-sided all-ones OR helper 在 exact all-ones 一侧递归证明 exact `-1..-1` 后，另一侧先复用 existing direct int64 range reader，再回退既有 `type_inference_bitwise_identity_expression_signed_same_identifier_range` 读取 same-identifier leaf。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 100 cases 扩到 108 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 64 queries 扩到 72 queries。当前 bitwise-not range source 674 行，private parser 980 行，private-mask LSP query 540 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6936 行；private parser 仍是同一职责覆盖文件，但下一段继续追加前应优先拆分测试边界。该切片不扩展 public headers、ordinary supported-count target files、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、OR counterpart beyond existing direct/local exact-zero-wrapper/same-identifier leaf、different-identifier structural equality、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 108 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 108 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 108 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not One-Sided All-Ones OR Exact-Zero Bitwise Identity Wrapped Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 22:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) \| (unit \| (span - span))))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (((unit ^ (unit - unit)) \| (~(unit - unit)))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：`type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_or_direct_leaf_int64_range` 在读取 direct counterpart 前复用本文件既有 `type_inference_bitwise_identity_bitwise_not_signed_all_ones_operand_skip_zero_bitwise_identity_wrappers`，只剥离 direct counterpart 上的局部 exact-zero `\|` / `^` identity wrapper，再交给既有 direct int64 range reader；exact all-ones 一侧仍必须递归证明 exact `-1..-1`。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 92 cases 扩到 100 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 56 queries 扩到 64 queries。当前 bitwise-not range source 664 行，private parser 908 行，private-mask LSP query 484 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、OR counterpart beyond the existing direct range reader plus local exact-zero bitwise identity wrappers、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 100 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 100 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 100 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not One-Sided All-Ones OR Direct Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 22:09 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) \| unit))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (unit \| (~(unit - unit))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_or_direct_leaf_int64_range`，并只在 `type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_leaf_int64_range` 遇到 binary `\|` 时调用；该 helper 要求一侧递归证明 exact signed all-ones，另一侧仅由既有 `type_inference_bitwise_identity_expression_zero_wrapped_or_unary_minus_zero_wrapped_or_basic_zero_minus_direct_int64_range` 证明为 direct int64 range 后发布 exact `-1..-1`。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 84 cases 扩到 92 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 48 queries 扩到 56 queries。当前 bitwise-not range source 657 行，private parser 836 行，private-mask LSP query 428 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、OR counterpart beyond the existing direct range reader、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 92 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 92 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 92 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not All-Ones OR Identity Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 21:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) \| allOnes))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (allOnes \| (~(unit - unit))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：`type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_leaf_int64_range` 的递归 leaf identity branch 现在接受 binary `&` 或 binary `\|`，但仍要求左右两侧递归证明 exact signed all-ones 后才发布 `-1..-1`；这样 `(~zero) \| allOnes` / `allOnes \| (~zero)` 这类 exact all-ones OR leaf 可以进入既有 zero-minus / unary-minus zero-minus bitwise-not exact-all-ones operand chain。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 76 cases 扩到 84 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 40 queries 扩到 48 queries。当前 bitwise-not range source 591 行，private parser 764 行，private-mask LSP query 372 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、OR with one exact all-ones side and arbitrary counterpart、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 84 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 84 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 84 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not Unary-Minus Zero-Minus All-Ones Mask Identity Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 21:35 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((-(zero - (~(span - span)))) & allOnes))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (allOnes & (-(zero - (~(unit - unit))))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：`type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_leaf_int64_range` 在递归识别 `&` leaf identity 前，先对形状为 unary `-` 的候选复用既有 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_bitwise_not_exact_signed_all_ones_operand_int64_range` 并重新要求 exact signed all-ones；这样 `& allOnes` 的子叶可以是既有 unary-minus zero-minus bitwise-not exact-all-ones 形态，而不是只接受 direct `~zero` 或 binary zero-minus child。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 68 cases 扩到 76 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 32 queries 扩到 40 queries。当前 bitwise-not range source 590 行，private parser 692 行，private-mask LSP query 316 行，main supported-count LSP query 文件保持 973 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、odd-depth zero-minus all-ones operands、arbitrary unary-minus all-ones operands beyond this zero-minus bitwise-not exact-all-ones shape、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 76 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 76 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 76 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not Zero-Minus All-Ones Mask Identity Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 21:18 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((zero - (zero - (~(span - span)))) & allOnes))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (allOnes & (zero - (zero - (~(unit - unit))))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：`type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_leaf_int64_range` 在递归识别 `&` leaf identity 前，先对形状为 binary `-` 的候选复用既有 `type_inference_bitwise_identity_expression_zero_minus_bitwise_not_exact_signed_all_ones_operand_int64_range` 并重新要求 exact signed all-ones；这样 `& allOnes` 的子叶可以是既有 even zero-minus bitwise-not exact-all-ones 形态，而不是只接受 direct `~zero`。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 60 cases 扩到 68 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 24 queries 扩到 32 queries。当前 bitwise-not range source 509 行，private parser 550 行，private-mask LSP query 253 行，`tests/CMakeLists.txt` 6570 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、odd-depth zero-minus all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 68 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 68 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 68 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not All-Ones Mask Identity Leaf Operand Publication and LSP Local Query | 已完成 | 2026-07-05 20:52 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones leaf reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) & allOnes))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (allOnes & (~(unit - unit))))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_bitwise_not_exact_signed_all_ones_leaf_int64_range`，让 evaluated zero-minus bitwise-not exact-all-ones operand reader 不再只接受 direct `~zero` leaf，也能递归识别两侧都为 exact signed all-ones 的 `&` mask identity leaf；`type_inference_bitwise_identity_expression_signed_all_ones_operand_int64_range` 先复用该 helper，再回退既有 zero-wrapped / unary-minus / zero-minus / basic-zero-minus paths。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 52 cases 扩到 60 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 16 queries 扩到 24 queries。当前 bitwise-not range source 488 行，private parser 486 行，private-mask LSP query 197 行，`tests/CMakeLists.txt` 6570 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary non-all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 60 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 60 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 60 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not Exact-Zero Bitwise Identity Wrapped Leaf All-Ones Operand Publication and LSP Local Query | 已完成 | 2026-07-05 20:32 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 zero-minus / unary-minus zero-minus bitwise-not exact signed all-ones operand reader 上继续收窄扩展：`~((zero - (zero - ((~(span - span)) \| (unit - unit)))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - ((~(unit - unit)) ^ (span - span)))))` 现在可作为 outer `~(...)` 的 private signed all-ones mask operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：给既有 `type_inference_bitwise_identity_bitwise_not_signed_all_ones_operand_skip_zero_bitwise_identity_wrappers` 增加前置声明，并让 `type_inference_bitwise_identity_expression_zero_minus_bitwise_not_exact_signed_all_ones_operand_evaluated_int64_range` 的 leaf candidate 复用该 exact-zero `\|`/`^` identity wrapper 剥离器，而不是只剥离加法/减零 wrapper；zero-minus 和 unary-minus zero-minus exact-all-ones operand paths 因共用 evaluated reader 一起受益。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 44 cases 扩到 52 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 8 queries 扩到 16 queries。当前 bitwise-not range source 492 行，private parser 476 行，private-mask LSP query 148 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、private mask count-side reader、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 52 tests 后新增 8 failures（旧 `0..0`），private-mask LSP target 新增 8 queries 返回旧 `0..0`。GREEN focused：WSL gcc private parser 52 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 52 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Count-Side Exact-Zero Bitwise Identity Wrapped Count Operand Publication and LSP Local Query | 已完成 | 2026-07-05 19:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones `&` mask 的 count-side reader 上继续收窄扩展：`~((~(span - span)) & (negativeFour \| (unit - unit)))` 与 `~(((negativeFour ^ (span - span)) & (~(unit - unit))))` 现在可作为 private bitwise-not mask 的 count operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。实现继续放在 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_expression_bitwise_not_signed_all_ones_mask_count_side_int64_range`，并让 `type_inference_bitwise_identity_expression_bitwise_not_signed_all_ones_mask_range` 先证明一侧 exact signed all-ones，再只对另一侧应用 count-side exact-zero `\|`/`^` identity wrapper 剥离和既有 direct/basic-zero-minus range reader；fallback 仍走既有 signed-all-ones operand reader。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 40 cases 扩到 44 cases；短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` 从 4 queries 扩到 8 queries，继续避免推高主 supported-count LSP query 文件。当前 bitwise-not range source 486 行，private parser 404 行，private-mask LSP query 92 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 public all-ones side helpers、ordinary supported-count all-ones side、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc private parser target 扩到 44 tests 后新增 4 failures（旧 `0..0`），private-mask LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc private parser 44 tests/0 failures + private-mask LSP PASS；WSL clang 同组 focused build/run 通过 44 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Left All-Ones Operand Exact-Zero Bitwise Identity Wrapped Bitwise-Not All-Ones Operand Discovery Coverage and LSP Query Split | 已完成 | 2026-07-05 19:33 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 固化上一切片 helper 已覆盖但未记录的 private bitwise-not signed all-ones mask left-side 形态：`((span - span) ^ (~(span - span))) & negativeFour` 与 `((unit - unit) \| (~(unit - unit))) & negativeFour` 现在作为 outer `~(...)` 的 private signed all-ones `&` mask left-side all-ones operand 参与 full zero-minus shift tests，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有生产代码改动；19:11 新增的 `type_inference_bitwise_identity_bitwise_not_signed_all_ones_operand_skip_zero_bitwise_identity_wrappers` 和既有 symmetric `&` mask reader 已覆盖该 left-side 位置。既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 36 cases 扩到 40 cases。为避免继续推高 973 行的主 supported-count LSP query 文件，新增短 LSP target `zr_vm_lsp_bzms_private_not_mask_query_test` / `zr_vm_lsp_bzms_private_not_mask_query_test`，包含 4 个 local query，并注册到 language-server 聚合；主 supported-count LSP query 文件保持 973 行不增长。当前 private parser 368 行，新 LSP private mask query 64 行，`tests/CMakeLists.txt` 6936 行。该切片不扩展 production inference、private mask variable-zero exact wrapper recognition、public all-ones side helpers、ordinary supported-count all-ones side、count-side readers、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | Discovery/GREEN：新增 parser/LSP 覆盖在当前实现下直接通过。WSL gcc focused build/run 通过 private parser 40 tests/0 failures + new private-mask LSP PASS；WSL clang 同组 focused build/run 通过 40 tests/0 failures + LSP PASS；Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused build/run `build=0 parser=0 lsp=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Private Bitwise-Not Mask Right All-Ones Operand Exact-Zero Bitwise Identity Wrapped Bitwise-Not All-Ones Operand Publication and LSP Local Query | 已完成 | 2026-07-05 19:11 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在 private bitwise-not signed all-ones operand reader 上继续收窄扩展：`negativeFour & ((span - span) ^ (~(span - span)))` 与 `negativeFour & ((unit - unit) \| (~(unit - unit)))` 现在可作为 outer `~(...)` 的 private signed all-ones `&` mask right-side all-ones operand 参与 supported-nonnegative count，full zero-minus shift tests 中 outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` / `zr_vm_bzms_private_not_mask_range_test` 从 32 cases 扩到 36 cases，并把既有 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 128 queries 扩到 132 queries。实现继续放在 bitwise-not range module：新增私有本地 `type_inference_bitwise_identity_bitwise_not_signed_all_ones_operand_skip_zero_bitwise_identity_wrappers`，并让 `type_inference_bitwise_identity_expression_signed_all_ones_operand_int64_range` 先尝试 exact-zero `\|`/`^` wrapped direct bitwise-not exact signed all-ones，再回退到既有 zero-wrapped/unary-minus-zero-wrapped/basic-zero-minus direct-range path；每次仍要求 direct bitwise-not result exact `-1..-1`。当前 bitwise-not range source 454 行，private parser 332 行，main LSP supported-count query 973 行，`tests/CMakeLists.txt` 6918 行；LSP 文件虽接近 1000 行，但本轮只追加同一 supported-count query target 的同类 cases，未新增 helper 或职责，暂不拆分。该切片不扩展 private mask left-side wrapped all-ones operand、variable-zero exact wrapper recognition、public all-ones side helper、ordinary supported-count all-ones side、count-side readers、wrapped supported-count readers、global zero-wrapper skipper、non-exact bitwise-not operands、arbitrary all-ones operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 最终 right-side wrapped all-ones fixtures 下 private parser target 36 tests 中新增 4 failures（旧 `0..0`），主 supported-count LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc private parser 36 tests/0 failures + supported-count LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Side Exact-Zero Bitwise Identity Wrapped Bitwise-Not All-Ones Side Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 18:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 signed all-ones side reader 上继续收窄扩展：`(zero \| (~zero)) & (unit + unit)` 与 `(unit + unit) & (zero ^ (~zero))` 现在可作为 ordinary `&` all-ones mask 的 signed all-ones side wrapped count expression；full zero-minus shift tests 中 outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_all_ones_count_range_inference_test` / `zr_vm_bzms_all_ones_count_range_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_all_ones_query_test` 从 4 queries 扩到 8 queries。实现继续放在 supported-count all-ones-side module：`type_inference_bitwise_identity_expression_supported_count_all_ones_side_range` 的入口改用私有本地 `type_inference_bitwise_identity_supported_count_all_ones_side_skip_zero_bitwise_identity_wrappers`，只在 signed all-ones side reader 入口剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；count-side、wrapped bitwise-not readers、global zero-wrapper skipper 和 exact-zero predicate 保持既有路径。当前 all-ones side source/header 185/12 行，all-ones parser/LSP files 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 arbitrary all-ones operand support、signed all-ones `\|` ordinary count support、non-exact bitwise-not operands、nonzero-left zero-minus、arbitrary subtraction、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 扩展 all-ones parser target 到 8 tests 后新增 4 failures（旧 `0..0`），既有 all-ones LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`；其中探索中两个 `zero - unit` XOR all-ones fixtures 被发现不满足 exact all-ones（`unit=2..3`）并在 GREEN 前收敛为 `zero ^ (~zero)`。GREEN focused：WSL gcc all-ones parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not Exact-Zero Bitwise Identity Wrapped RHS Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 18:03 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 `zero - (~count)` wrapped bitwise-not reader 上继续收窄扩展：`zero - (zero \| (~(zero - (~(~negativeFour)))))` 与 `zero - ((~(zero - (~(zero - (zero - (~negativeFour)))))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 36 cases 扩到 40 cases，并把既有 LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 44 queries 扩到 48 queries。实现继续放在拆分后的 wrapped supported-count module：`type_inference_bitwise_identity_expression_zero_minus_bitwise_not_wrapped_direct_supported_count_range` 的 RHS path 改用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `zero - (~count)` reader 的 RHS unary `~` path 剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 direct bitwise-not leaf/evaluated zero-minus chain、all-ones mask count-side、direct zero-minus、unary-minus zero-minus、bitwise-not zero-minus readers 保持既有局部路径，其他 wrapper reader 不扩展。当前 wrapped source/header 779/67 行，nested-not parser/LSP files 368/372 行，`tests/CMakeLists.txt` 6918 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、non-exact bitwise-not operands、arbitrary nested zero-minus beyond accepted readers、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：修正两个新增 XOR 测试表达式括号后，WSL gcc 既有 nested-not parser target 扩到 40 tests 后新增 4 failures（旧 `0..0`），既有 wrapped-not LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc nested-not parser 40 tests/0 failures + wrapped-not LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Zero-Minus Exact-Zero Bitwise Identity Wrapped Inner Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 17:42 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 unary-minus zero-minus supported-count reader 上继续收窄扩展：`-(zero \| (zero - (unit + unit)))` 与 `-(((span - span) - ((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，按 `-(zero - count)` 发布 delegated count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_unary_zm_count_range_test` / `zr_vm_bzms_unary_zm_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_unary_zm_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：`type_inference_bitwise_identity_expression_unary_minus_zero_minus_supported_nonnegative_range` 的 argument / inner zero-minus operand path 改用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `-(zero - count)` reader 的 argument / inner zero-minus operand path 剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 bitwise-not zero-minus、double zero-minus、zero-minus unary-minus、double unary-minus/bitwise-not 路径保持既有局部路径，其他 wrapper reader 不扩展。当前 supported-count 主模块 722 行，wrapped source/header 779/67 行，parser/LSP unary-zm 文件 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、arbitrary nested zero-minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 unary-zm parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc unary-zm parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Zero-Minus Exact-Zero Bitwise Identity Wrapped Inner Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 17:20 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 bitwise-not zero-minus supported-count reader 上继续收窄扩展：`~(zero \| (zero - (unit + unit)))` 与 `~(((span - span) - ((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_not_zm_count_range_test` / `zr_vm_bzms_not_zm_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_not_zm_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：`type_inference_bitwise_identity_expression_bitwise_not_zero_minus_supported_nonnegative_range` 的 inner zero-minus operand path 改用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `~(zero - count)` reader 的 argument / inner zero-minus operand path 剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 unary-minus zero-minus、double zero-minus、zero-minus unary-minus、double unary-minus/bitwise-not 路径保持既有局部路径，其他 wrapper reader 不扩展。当前 supported-count 主模块 722 行，wrapped source/header 779/67 行，parser/LSP not-zm 文件 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、arbitrary nested zero-minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 not-zm parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc not-zm parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Zero-Minus Exact-Zero Bitwise Identity Wrapped Inner Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 16:56 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 double zero-minus supported-count reader 上继续收窄扩展：`zero - (zero \| (zero - (unit + unit)))` 与 `((span - span) - (((span - span) - ((unit + unit) \| (span - span))) ^ (span - span)))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_double_zm_count_range_test` / `zr_vm_bzms_double_zm_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_double_zm_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：`type_inference_bitwise_identity_expression_double_zero_minus_supported_nonnegative_range` 的 inner zero-minus operand path 改用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 exact-zero-left `zero - (zero - count)` reader 的 inner zero-minus operand path 剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 double unary-minus、double bitwise-not、zero-minus unary-minus、bitwise-not/unary-minus wrapper 路径保持既有局部路径，其他 wrapper reader 不扩展。当前 supported-count 主模块 722 行，wrapped source/header 779/67 行，parser/LSP double-zm 文件 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、arbitrary nested zero-minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 double-zm parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc double-zm parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 28 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Unary-Minus Exact-Zero Bitwise Identity Wrapped Inner Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 16:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 zero-minus unary-minus supported-count reader 上继续收窄扩展：`zero - (zero \| (-(unit + unit)))` 与 `((span - span) - (((-((unit + unit) \| (span - span))) ^ (span - span))))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_zm_unary_count_range_test` / `zr_vm_bzms_zm_unary_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_zm_unary_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：`type_inference_bitwise_identity_expression_zero_minus_unary_minus_supported_nonnegative_range` 的 RHS path 改用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 outer exact-zero-left `zero - (-(count))` reader 的 inner unary-minus operand path 剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 double unary-minus、bitwise-not unary-minus、unary-minus bitwise-not、double bitwise-not 路径保持既有局部路径，其他 wrapper reader 不扩展。当前 supported-count 主模块 722 行，wrapped source/header 779/67 行，parser/LSP zm-unary 文件 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、arbitrary unary minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 zm-unary parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc zm-unary parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 28 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Bitwise-Not Exact-Zero Bitwise Identity Wrapped Inner Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 16:19 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 unary-minus bitwise-not supported-count reader 上继续收窄扩展：`-(zero \| (~(unit + unit)))` 与 `-((~((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_neg_not_count_range_test` / `zr_vm_bzms_neg_not_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_neg_not_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：复用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `type_inference_bitwise_identity_expression_unary_minus_bitwise_not_supported_nonnegative_range` 的 inner expression 路径剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 bitwise-not unary-minus、double unary-minus、double bitwise-not 路径保持既有局部路径，其他 wrapper reader 不扩展。当前 supported-count 主模块 684 行，wrapped source/header 704/64 行，parser/LSP neg-not 文件 70/85 行，`tests/CMakeLists.txt` 6552 行；不扩展 arbitrary unary-minus、arbitrary bitwise-not、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 neg-not parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc neg-not parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 28 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Unary-Minus Exact-Zero Bitwise Identity Wrapped Inner Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 15:58 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 bitwise-not unary-minus supported-count reader 上继续收窄扩展：`~(zero \| (-(unit + unit)))` 与 `~((-((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_not_unary_count_range_test` / `zr_vm_bzms_not_unary_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_not_unary_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：复用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `type_inference_bitwise_identity_expression_bitwise_not_unary_minus_supported_nonnegative_range` 的 inner expression 路径剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 double unary-minus / double bitwise-not 路径保持既有局部剥离，其他 wrapper reader 不扩展。当前 supported-count 主模块 684 行，wrapped source/header 705/64 行，parser/LSP not-unary 文件 70/85 行，`tests/CMakeLists.txt` 6552 行；不扩展 arbitrary bitwise-not、arbitrary unary-minus、arbitrary negative operands、zero/negative counts under `~(-count)`、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 not-unary parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc not-unary parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 28 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Bitwise-Not Exact-Zero Bitwise Identity Wrapped Inner Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 08:53 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 double bitwise-not supported-count reader 上继续收窄扩展：`~(zero \| (~(unit + unit)))` 与 `~((~((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_double_not_count_range_test` / `zr_vm_bzms_double_not_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_double_not_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：复用私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `type_inference_bitwise_identity_expression_double_bitwise_not_supported_nonnegative_range` 的 inner expression 路径剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；相邻 `bitwise_not_unary_minus` helper 保持原 zero-identity/unary-plus 剥离路径。当前 supported-count 主模块 684 行，wrapped source/header 706/64 行，parser/LSP double-not 文件 70/85 行，`tests/CMakeLists.txt` 6552 行；不扩展 arbitrary bitwise-not、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：修正两个测试表达式括号后，WSL gcc 既有 double-not parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc double-not parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Unary-Minus Exact-Zero Bitwise Identity Wrapped Inner Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 08:21 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 在既有 double unary-minus supported-count reader 上继续收窄扩展：`-(zero \| (-(unit + unit)))` 与 `-((-((unit + unit) \| (span - span))) ^ (span - span))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。本轮没有新增 CMake target，而是把既有 parser target `zr_vm_bzms_double_neg_count_range_test` / `zr_vm_bzms_double_neg_count_test` 从 4 cases 扩到 8 cases，并把既有 LSP target `zr_vm_lsp_bzms_double_neg_query_test` 从 4 queries 扩到 8 queries。实现继续放在拆分后的 wrapped supported-count module：新增私有本地 `type_inference_bitwise_identity_supported_count_skip_zero_bitwise_identity_wrappers`，只在 `type_inference_bitwise_identity_expression_double_unary_minus_supported_nonnegative_range` 的 inner expression 路径剥离 exact-zero `+`/RHS `-0`、unary `+` 以及 exact-zero `\|`/`^` identity wrapper；不扩展全局 zero-wrapper skipper。当前 supported-count 主模块 722 行，wrapped source/header 783/67 行，parser/LSP double-neg 文件 80/92 行，`tests/CMakeLists.txt` 6918 行；不扩展 arbitrary unary minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 既有 double-neg parser target 扩到 8 tests 后新增 4 failures（旧 `0..0`），既有 LSP target 新增 4 queries 返回旧 `0..0`，`parser_status=4 lsp_status=1`。GREEN focused：WSL gcc double-neg parser 8 tests/0 failures + LSP PASS。Adjacent：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `cl.exe` toolset path 14.44.35207, `build-msvc-lsp-bitwise-zero`) 均通过 31 个 focused/adjacent executable build/run，`FAILURES=0` 且 `MISSING=0`。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 07:54 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `-` over unary `-` 的 supported-count wrapper 推进到 already-supported nonnegative count operand：`-(-(unit + unit))` 与 `-(-((unit + unit) \| (span - span)))` 现在可作为 left/right shift supported-nonnegative count，按 `-(-(count))` 发布 delegated `count`，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_double_neg_count_range_test` / `zr_vm_bzms_double_neg_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_double_neg_query_test`（4 queries）。实现继续放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_double_unary_minus_supported_nonnegative_range`，只识别 outer unary `-` whose argument is inner unary `-`，且 inner argument 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；顶层 unary `-` supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 684 行，wrapped source/header 674/64 行，新 parser/LSP 文件 38/57 行，`tests/CMakeLists.txt` 6552 行；不扩展 arbitrary unary minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, MSVC compiler 19.44.35228.0, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new double unary-minus parser 4 tests/0 failures + LSP PASS、double bitwise-not parser 4 tests/0 failures + LSP PASS、zero-minus unary-minus parser 4 tests/0 failures + LSP PASS、bitwise-not unary-minus parser 4 tests/0 failures + LSP PASS、unary-minus bitwise-not parser 4 tests/0 failures + LSP PASS、double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 07:34 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `~` over unary `~` 的 supported-count wrapper 推进到 already-supported nonnegative count operand：`~(~(unit + unit))` 与 `~(~((unit + unit) \| (span - span)))` 现在可作为 left/right shift supported-nonnegative count，按 `~(~(count))` 发布 delegated `count`，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_double_not_count_range_test` / `zr_vm_bzms_double_not_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_double_not_query_test`（4 queries）。实现继续放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_double_bitwise_not_supported_nonnegative_range`，只识别 outer unary `~` whose argument is inner unary `~`，且 inner argument 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；顶层 supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 669 行，wrapped source/header 631/59 行，新 parser/LSP 文件 38/57 行，`tests/CMakeLists.txt` 6519 行；不扩展 arbitrary bitwise-not、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, MSVC compiler 19.44.35228.0, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new double bitwise-not parser 4 tests/0 failures + LSP PASS、zero-minus unary-minus parser 4 tests/0 failures + LSP PASS、bitwise-not unary-minus parser 4 tests/0 failures + LSP PASS、unary-minus bitwise-not parser 4 tests/0 failures + LSP PASS、double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 07:10 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left outer `zero - (...)` over unary `-` 的 supported-count wrapper 推进到 already-supported nonnegative count operand：`zero - (-(unit + unit))` 与 `((span - span) - (-((unit + unit) \| (span - span))))` 现在可作为 left/right shift supported-nonnegative count，按 `zero - (-(count))` 发布 `count`，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_zm_unary_count_range_test` / `zr_vm_bzms_zm_unary_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_zm_unary_query_test`（4 queries）。实现放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_zero_minus_unary_minus_supported_nonnegative_range`，只识别 outer exact-zero-left binary `-` whose RHS is unary `-`，且 unary operand 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；顶层 supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 654 行，wrapped source/header 588/54 行，新 parser/LSP 文件 38/57 行，`tests/CMakeLists.txt` 6486 行；不扩展 nonzero-left zero-minus、arbitrary subtraction、arbitrary unary minus、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, MSVC compiler 19.44.35228.0, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new zero-minus unary-minus parser 4 tests/0 failures + LSP PASS、bitwise-not unary-minus parser 4 tests/0 failures + LSP PASS、unary-minus bitwise-not parser 4 tests/0 failures + LSP PASS、double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Unary-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 06:41 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `~` over unary `-` 的 supported-count wrapper 推进到 already-supported nonnegative count operand：`~(-(unit + unit))` 与 `~(-((unit + unit) \| (span - span)))` 现在可作为 left/right shift supported-nonnegative count，按 `~(-count)` 发布 `count - 1`，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_not_unary_count_range_test` / `zr_vm_bzms_not_unary_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_not_unary_query_test`（4 queries）。实现放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_bitwise_not_unary_minus_supported_nonnegative_range`，只识别 outer unary `~` over inner unary `-`，且 inner operand 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明，同时要求 count min > 0 以保证 `count - 1` 非负；顶层 supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 678 行，wrapped source/header 599/52 行，新 parser/LSP 文件 44/64 行，`tests/CMakeLists.txt` 6816 行；不扩展 arbitrary bitwise-not、arbitrary unary minus、arbitrary negative operands、zero/negative count under `~(-count)`、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, MSVC compiler 19.44.35228.0, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new bitwise-not unary-minus parser 4 tests/0 failures + LSP PASS、unary-minus bitwise-not parser 4 tests/0 failures + LSP PASS、double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 06:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `-` over unary `~` 的 supported-count wrapper 推进到 already-supported nonnegative count operand：`-(~(unit + unit))` 与 `-(~((unit + unit) \| (span - span)))` 现在可作为 left/right shift supported-nonnegative count，按 `-(~count)` 发布 `count + 1`，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_neg_not_count_range_test` / `zr_vm_bzms_neg_not_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_neg_not_query_test`（4 queries）。实现放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_unary_minus_bitwise_not_supported_nonnegative_range`，只识别 outer unary `-` over inner unary `~`，且 inner operand 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；顶层 supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 626 行，wrapped source/header 498/44 行，新 parser/LSP 文件 38/57 行；不扩展 arbitrary unary minus、arbitrary bitwise-not、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, MSVC compiler 19.44.35228.0, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new unary-minus bitwise-not parser 4 tests/0 failures + LSP PASS、double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Double Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 05:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left outer `zero - (...)` over inner exact-zero-left `zero - supportedCount` 从 direct zero-minus chain leaf 推进到 RHS already-supported nonnegative count：`zero - (zero - (unit + unit))` 与 `((span - span) - ((span - span) - ((unit + unit) \| (span - span))))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_double_zm_count_range_test` / `zr_vm_bzms_double_zm_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_double_zm_query_test`（4 queries）。实现放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_double_zero_minus_supported_nonnegative_range`，只识别 outer/inner 都是 exact-zero-left binary `-`，且 inner RHS 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；顶层 supported-nonnegative reader 只在该 helper 产出范围时发布结果。当前 supported-count 主模块 611 行，wrapped source/header 454/39 行，新 parser/LSP 文件 38/57 行；不扩展 nonzero-left zero-minus、arbitrary `(a-b)`、odd-depth zero-minus chains、arbitrary negative operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new double zero-minus parser 4 tests/0 failures + LSP PASS、bitwise-not zero-minus parser 4 tests/0 failures + LSP PASS、unary zero-minus parser 4 tests/0 failures + LSP PASS、all-ones parser 4 tests/0 failures + LSP PASS、nested-not parser 36 tests/0 failures + wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures + supported-count LSP PASS、private parser 32 tests/0 failures、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 04:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `~` over exact-zero-left zero-minus 的 supported-count wrapper 从 direct bitwise-not operand path 推进到 already-supported nonnegative count RHS：`~(zero - (unit + unit))` 与 `~((span - span) - ((unit + unit) \| (span - span)))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免继续膨胀主 supported-count parser/LSP 文件，本轮新增短 parser target `zr_vm_bzms_not_zm_count_range_test` / `zr_vm_bzms_not_zm_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_not_zm_query_test` / `zr_vm_lsp_bzms_not_zm_query_test`（4 queries）。实现放在拆分后的 wrapped supported-count module：新增 `type_inference_bitwise_identity_expression_bitwise_not_zero_minus_supported_nonnegative_range`，只识别 unary `~` 的 operand 是 exact-zero-left binary `-` 且 RHS 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明，再按 `~(-count)` 发布 `count-1` range，并在顶层 supported-nonnegative unary `~` 分支接入；主 supported-count module 当前 634 行，wrapped source/header 当前 449/37 行。该切片不扩展 nonzero-left zero-minus、arbitrary `~(a-b)`、非 exact-zero-left 的 zero-minus、任意负操作数、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, MSBuild 17.14.40, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new bitwise-not zero-minus parser 4 tests/0 failures、new bitwise-not zero-minus LSP PASS、new unary zero-minus parser 4 tests/0 failures、new unary zero-minus LSP PASS、new all-ones parser 4 tests/0 failures、new all-ones LSP PASS、nested-not parser 36 tests/0 failures、wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Supported-Count Wrapped Reader Modularization Maintenance | 已完成 | 2026-07-05 04:08 +08:00 | 按阶段 2 的数值区间/语义事实维护线，在 `type_inference_bitwise_identity_supported_count.c` 达到 934 行、继续扩展会重新逼近 1000 行门槛前，先做行为保持拆分：把 zero-minus / bitwise-not / unary-minus zero-minus supported-count wrapper readers 移到私有子模块 `type_inference_bitwise_identity_supported_count_wrapped.c/.h`，主 supported-count module 降到 583 行，新 source 350 行、header 29 行；现有 CMake glob 自动编译新 source，未新增语义形态、未新增 parser/LSP query case。拆分只保持既有 Stage 2 supported-count fact surface：unary-minus zero-minus already-supported count、ordinary all-ones mask count side、outer zero-minus bitwise-not wrapped count、direct bitwise-not count、private all-ones side proof 与顶层 supported-nonnegative reader 的行为不变；不扩展 nonzero-left zero-minus、arbitrary `-(a-b)`、non-exact bitwise-not operands、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。 | 行为保持 refactor，基于 03:40 语义切片 GREEN 后执行，未伪造 RED。拆分后 WSL gcc、WSL clang、Windows MSVC focused/adjacent build/run 全通过：new unary zero-minus parser 4 tests/0 failures、new unary zero-minus LSP PASS、new all-ones parser 4 tests/0 failures、new all-ones LSP PASS、nested-not parser 36 tests/0 failures、wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。WSL gcc 首次 build 命令在工具超时边界被截断但日志已到 63/63，随后同目标 rebuild `ninja: no work to do` 退出 0；Windows MSVC configure/build 使用 `VSCMD_VER=17.14.34`、MSBuild 17.14.40，运行矩阵退出 0。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Zero-Minus Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-05 03:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `-` over exact-zero-left zero-minus 的 supported-count wrapper 从 direct/unary-minus direct count 推进到 already-supported nonnegative count operand：`-(zero - (unit + unit))` 与 `-((span - span) - ((unit + unit) | (span - span)))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免已拆出的 supported-count parser/LSP 主目标继续膨胀，本轮新增短 parser target `zr_vm_bzms_unary_zm_count_range_test` / `zr_vm_bzms_unary_zm_count_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_unary_zm_query_test` / `zr_vm_lsp_bzms_unary_zm_query_test`（4 queries），并把 CMake target/file names 保持短名以兼容 MSVC 生成对象路径；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases，wrapped-not LSP target 保持 44 queries。实现仅在 `type_inference_bitwise_identity_supported_count.c` 新增受限 unary-minus zero-minus supported-count helper：只识别 unary `-` 的 operand 是 exact-zero-left binary `-`，且其 RHS 已由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明；然后把 RHS min/max 作为 unary-minus zero-minus supported count 发布；不扩展 nonzero-left zero-minus、arbitrary `-(a-b)`、negative/out-of-range shift counts、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 934 行，接近 1000 行，下一轮继续增加职责前应优先评估拆分。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new unary zero-minus parser 4 tests/0 failures、new unary zero-minus LSP PASS、new all-ones parser 4 tests/0 failures、new all-ones LSP PASS、nested-not parser 36 tests/0 failures、wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-04 18:39 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary signed all-ones `&` mask 的 count side 从少数 direct/zero-minus bitwise-not supported-count readers 扩到 already-supported nonnegative count operand：`allOnes & (unit + unit)` 与 `((unit + unit) | (span - span)) & allOnes` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。为避免已拆出的 supported-count parser/LSP 主目标继续膨胀，本轮新增短 parser target `zr_vm_bzms_all_ones_count_range_inference_test` / `zr_vm_bzms_all_ones_count_range_test`（4 cases）与短 LSP target `zr_vm_lsp_bzms_all_ones_query_test` / `zr_vm_lsp_bzms_all_ones_query_test`（4 queries），并把 CMake target/file names 保持短名以兼容 MSVC 生成对象路径；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases，wrapped-not LSP target 保持 44 queries。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 all-ones mask count-side reader 中复用 `type_inference_bitwise_identity_expression_supported_nonnegative_range`，并在 supported-nonnegative 顶层 `&` 分支先要求一侧已由 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range` 证明为 signed all-ones，再发布另一侧 count range；不扩展 arbitrary `&`、non-all-ones masks、negative/out-of-range shift counts、nonzero-left shift、unsupported count algebra、arbitrary normalization、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 875 行，低于本轮拆分门槛。 | TDD RED：WSL gcc 新 parser target 4 tests 输出 4 failures（旧 `0..0`），新 LSP target 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new all-ones parser 4 tests/0 failures、new all-ones LSP PASS、nested-not parser 36 tests/0 failures、wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not Already-Supported Count Publication and LSP Local Query | 已完成 | 2026-07-04 17:45 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 direct nested wrapped supported-count 再收窄扩到 already-supported nonnegative count operand：exact-zero OR-identity around wrapped count 与 exact-zero AND-annihilator around wrapped count 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 32 扩到 36 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 40 扩到 44 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 outer zero-minus bitwise-not wrapped supported-count reader 中让 unary `~` 的 structurally smaller operand 复用 `type_inference_bitwise_identity_expression_supported_nonnegative_range`，让外层 `zero - (~count)` 消费 already-supported nonnegative count facts；仍沿用 exact-zero-left、unary `~`、supported nonnegative、safe range-negation 和 shift-site upper-bound guards；不扩展 non-exact bitwise-not operands、nonzero-left shift、negative/out-of-range shift counts、unsupported count algebra、arbitrary expression normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 897 行，低于本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 36 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 36 tests/0 failures、wrapped-not LSP PASS（44 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Nested Zero-Minus Bitwise-Not Wrapped Supported-Count Publication and LSP Local Query | 已完成 | 2026-07-04 17:08 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 already-supported wrapped supported-count 再收窄扩到 direct nested wrapped supported-count：`zero - (~(zero - (~(~negativeFour))))` 与 `zero - (~(zero - (~(zero - (zero - (~negativeFour))))))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 28 扩到 32 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 36 扩到 40 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 outer zero-minus bitwise-not wrapped supported-count reader 中复用同一 reader 读取 structurally smaller AST operand，让 nested exact-zero-left `zero - (~(...))` wrapper 能消费 already-supported wrapped supported-count；仍沿用 exact-zero-left、unary `~`、supported nonnegative 和 safe range-negation guard；不扩展 non-exact bitwise-not operands、ordinary all-ones side/OR accepted forms、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 892 行，低于本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 32 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 32 tests/0 failures、wrapped-not LSP PASS（40 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not All-Ones Mask Nested Wrapped Supported-Count Publication and LSP Local Query | 已完成 | 2026-07-04 16:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 ordinary all-ones `&` mask 包住 already-supported zero-minus / unary-minus zero-minus bitwise-not positive count，再收窄扩到 ordinary all-ones `&` mask 包住 already-supported wrapped supported-count：`zero - (~(allOnes & (zero - (~(~negativeFour)))))` 与 `zero - (~((zero - (~(zero - (zero - (~negativeFour))))) & allOnes))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 24 扩到 28 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 32 扩到 36 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 all-ones mask count-side reader 中复用 existing outer zero-minus bitwise-not wrapped supported-count reader，并通过前向声明解决局部 helper 顺序；该路径仍要求 binary `&`、一侧 existing ordinary all-ones proof、另一侧 already-supported wrapped supported-count，并沿用 outer reader 的 exact-zero-left、unary `~` 与 safe range-negation guard；不扩展 non-exact bitwise-not operands、ordinary all-ones side/OR accepted forms、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 887 行，低于本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 28 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 28 tests/0 failures、wrapped-not LSP PASS（36 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not All-Ones Mask Wrapped Supported-Count Publication and LSP Local Query | 已完成 | 2026-07-04 16:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 ordinary all-ones `&` mask 包住 direct positive bitwise-not count，进一步收窄扩到 ordinary all-ones `&` mask 包住 already-supported zero-minus / unary-minus zero-minus bitwise-not positive count：`zero - (~(allOnes & (zero - (zero - (~negativeFour)))))` 与 `zero - (~((-(zero - (~negativeFour))) & allOnes))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 20 扩到 24 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 28 扩到 32 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 all-ones mask count-side reader 中新增 `type_inference_bitwise_identity_expression_supported_count_all_ones_mask_count_side_range` 并把外层 helper 改名为 supported-count 语义：该 helper 只复用 existing zero-wrapped direct bitwise-not、zero-minus zero-minus bitwise-not、unary-minus zero-minus bitwise-not positive count readers，并沿用 safe range-negation guard；不扩展 non-exact bitwise-not operands、ordinary all-ones side/OR accepted forms、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 812 行，低于本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 24 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 24 tests/0 failures、wrapped-not LSP PASS（32 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。WSL clang 首次一体化 build/run 因 300s 工具超时无结果，随后拆成 build 与 run 两步通过；Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Supported-Count All-Ones Side Reader Modularization Maintenance | 已完成 | 2026-07-04 15:24 +08:00 | 按阶段 2 的数值区间/语义事实维护线，在 supported-count module 达到 982 行、继续扩展会越过本轮大文件门槛前，先做行为保持拆分：把 `type_inference_bitwise_identity_supported_count.c` 中的 ordinary all-ones side proof reader 移到私有子模块 `type_inference_bitwise_identity_supported_count_all_ones_side.c/.h`，主 supported-count module 降到 842 行，新 source 144 行、header 12 行；现有 CMake glob 自动编译新 source，未新增语义形态、未新增 parser/LSP query case。拆分只保持既有 Stage 2 supported-count fact surface；不扩展 non-exact bitwise-not operands、ordinary all-ones side/OR accepted forms、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | 行为保持 refactor，基于 15:01 语义切片 GREEN 后执行，未伪造 RED。拆分后 WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 20 tests/0 failures、wrapped-not LSP PASS（28 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not All-Ones Mask Direct Bitwise-Not Count Publication and LSP Local Query | 已完成 | 2026-07-04 15:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 exact-zero identity-wrapped direct positive bitwise-not count 进一步收窄扩到 ordinary all-ones `&` mask 包住 direct positive bitwise-not count：`zero - (~(allOnes & (~negativeFour)))` 与 `zero - (~((~(zero - unit)) & allOnes))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 16 扩到 20 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 24 扩到 28 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 outer zero-minus bitwise-not wrapped supported-count reader 内新增 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_bitwise_not_direct_range`：该 helper 只接受 binary `&`、一侧 existing ordinary all-ones proof、另一侧 existing zero-wrapped direct bitwise-not positive count，并沿用 safe range-negation guard；不扩展 non-exact bitwise-not operands、ordinary all-ones side/OR helper accepted forms、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 982 行，低于本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 20 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 20 tests/0 failures、wrapped-not LSP PASS（28 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not Zero-Identity Direct Bitwise-Not Count Publication and LSP Local Query | 已完成 | 2026-07-04 14:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 direct positive bitwise-not count 进一步收窄扩到 exact-zero identity-wrapped direct positive bitwise-not count：`zero - (~((~negativeFour) + (span - span)))` 与 `zero - (~((~negativeFour) - (unit - unit)))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 12 扩到 16 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 20 扩到 24 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 outer zero-minus bitwise-not wrapped supported-count reader 中把 direct positive bitwise-not count 分支改为复用 existing zero-wrapped bitwise-not direct reader，并沿用 safe range-negation guard；不扩展 non-exact bitwise-not operands、all-ones side mask/OR helper、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 923 行，未触发本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 16 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 16 tests/0 failures、wrapped-not LSP PASS（24 queries）、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Bitwise-Not Direct Bitwise-Not Count Publication and LSP Local Query | 已完成 | 2026-07-04 14:15 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 outer exact-zero-left zero-minus over bitwise-not 的 supported-count wrapper 从 zero-minus / unary-minus zero-minus bitwise-not direct count 进一步收窄扩到 direct positive bitwise-not count：`zero - (~(~negativeFour))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 从 8 扩到 12 cases；wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 16 扩到 20 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 的受限 outer zero-minus bitwise-not wrapped supported-count reader 中补 direct positive bitwise-not count 分支，并沿用 safe range-negation guard；不扩展 non-exact bitwise-not operands、all-ones side mask/OR helper、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 856 行，未触发本轮拆分门槛。 | TDD RED：WSL gcc nested-not parser target 扩到 12 tests 后新增 4 failures（旧 `0..0`），wrapped-not LSP target 新 4 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：nested-not parser 12 tests/0 failures、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、wrapped-not LSP PASS（20 queries）、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Nested Zero-Minus Bitwise-Not Wrapped Direct Count Publication and LSP Local Query | 已完成 | 2026-07-04 13:57 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 supported-count expression 自身从 zero-minus / unary-minus zero-minus bitwise-not direct wrapper 再推进到外层 exact-zero-left zero-minus over bitwise-not：`zero - (~(zero - (zero - (~negativeFour))))` 与 `zero - (~(-(zero - (~negativeFour))))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。新增短 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_nested_bitwise_not_range_inference_test` / `zr_vm_bzms_nested_not_range_test` 覆盖 8 cases；已有 wrapped-not LSP target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 从 8 扩到 16 query cases；主 supported-count parser target 保持 104 cases，private parser 保持 32 cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 新增受限 outer zero-minus bitwise-not wrapped supported-count reader，并在 supported-nonnegative count 入口消费；不扩展 non-exact bitwise-not operands、all-ones side mask/OR helper、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。supported-count module 当前 850 行，未触发本轮拆分门槛。 | TDD RED：WSL gcc 新 nested-not parser target 8 tests/8 failures（旧 `0..0`），wrapped-not LSP target 新 8 queries 返回旧 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：new nested-not parser 8 tests/0 failures、supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、wrapped-not LSP PASS（16 queries）、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not Direct Count Publication and LSP Local Query | 已完成 | 2026-07-04 13:29 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 supported-count expression 自身从 direct bitwise-not positive count 推进到 zero-minus / unary-minus zero-minus wrapper：`zero - (zero - (~negativeFour))` 与 `-(zero - (~negativeFour))` 现在可作为 left/right shift supported-nonnegative count，outer zero-minus RHS 发布 `2..3`，zero-minus unary-RHS 发布 `-3..-2`。主 supported-count parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 从 96 扩到 104 cases；LSP 侧抽出 `lsp_bitwise_zero_minus_shift_supported_count_range_query_test_support.c/.h` 共享 fixture，保留旧 supported-count query target PASS，并新增短 target `zr_vm_lsp_bzms_supported_count_wrapped_not_query_test` / `zr_vm_lsp_bzms_count_wrap_not_query_test` 覆盖 8 个 wrapped bitwise-not count local-query cases。实现仅在 `type_inference_bitwise_identity_supported_count.c` 新增受限 zero-minus / unary-minus zero-minus bitwise-not direct supported-count range readers 与安全取负 guard，并在 supported-nonnegative count 入口消费；不扩展 non-exact bitwise-not operands、all-ones side mask/OR helper、nonzero-left shift、negative/out-of-range shift counts、general bitwise/shift algebra、arbitrary normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 104 tests 中新增 8 failures，new wrapped-not LSP target 8 queries 返回旧 `0..0`，旧 supported-count LSP target 在 helper 拆分后 PASS。GREEN：WSL gcc、WSL clang、Windows MSVC (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) focused/adjacent build/run 全通过：supported-count parser 104 tests/0 failures、private parser 32 tests/0 failures、new wrapped-not LSP PASS、旧 supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus and Unary-Minus Zero-Minus Bitwise-Not Exact-Zero All-Ones Side Mask Publication and LSP Local Query | 已完成 | 2026-07-04 12:58 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary supported-count `&` mask 的 all-ones side 从 direct bitwise-not exact-zero all-ones 推进到 zero-minus / unary-minus zero-minus bitwise-not exact-zero all-ones wrapper：`zero - (zero - (~(span - span)))`、`zero - (zero - (~(unit - unit)))`、`-(zero - (~(span - span)))` 与 `-(zero - (~(unit - unit)))` 都能作为 ordinary all-ones side 保留 exact signed all-ones。新增覆盖 `((zero - (zero - (~(span - span)))) & (~negativeFour))`、`((~(zero - unit)) & (zero - (zero - (~(unit - unit)))))` 及 unary-minus zero-minus 同形，outer supported-count shift 在 zero-minus RHS 下发布 `2..3`，在 zero-minus unary-RHS 下发布 `-3..-2`；主 supported-count parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 从 88 cases 扩到 96 cases，private parser target 保持 32 cases，LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 120 扩到 128 query cases。实现只把已经受限且会重新检查 exact `-1..-1` 的 zero-minus / unary-minus zero-minus bitwise-not exact-all-ones operand readers 暴露给 supported-count all-ones-side reader，并在 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range` 中复用；不扩展 non-exact bitwise-not operands、arbitrary all-ones operands、odd-depth zero-minus all-ones、signed all-ones `|` beyond existing supported forms、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、arbitrary expression normalization、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。LSP query 文件当前 990 行，低于本轮拆分门槛。 | TDD RED：WSL gcc supported-count parser 96 tests 中新增 8 failures，LSP target 同 8 个 zero-minus / unary-minus zero-minus bitwise-not exact-zero all-ones side mask supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 96 tests/0 failures、private parser 32 tests/0 failures、supported-count LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused/adjacent compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过同一 parser/LSP suite。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Identity Leaf Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 12:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从外层 zero-identity wrapped zero-minus/unary-minus zero-minus wrapper 推进到 leaf bitwise-not exact-zero all-ones expression 自身被 exact-zero `+`/`-` identity wrapper 包裹的形态：`zero - (zero - ((~(span - span)) + (unit - unit)))`、`zero - (zero - ((~(unit - unit)) - (span - span)))`、`-(zero - ((~(span - span)) + (unit - unit)))` 与 `-(zero - ((~(unit - unit)) - (span - span)))` 都能在 signed all-ones `&` mask operand 内证明 exact signed all-ones。新增覆盖 `~((zero - (zero - ((~(span - span)) + (unit - unit)))) & negativeUnit)`、`~(negativeUnit & (zero - (zero - ((~(unit - unit)) - (span - span)))))` 及 unary-minus zero-minus 同形，signed all-ones `&` mask 保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；private parser target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` 从 24 cases 扩到 32 cases，主 supported-count parser target 保持 88 cases，LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 112 扩到 120 query cases。实现只在 private evaluated zero-minus bitwise-not exact-all-ones operand reader 的 leaf direct bitwise-not 检查前调用 `type_inference_bitwise_identity_skip_zero_identity_wrappers`，若 leaf 检查不成立仍沿原 zero-minus chain 继续，不把剥离后的 expression 当作新的 chain；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、odd-depth zero-minus all-ones、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。LSP query 文件当前 934 行，低于本轮拆分门槛。 | TDD RED：WSL gcc 新 private parser target 32 tests 中新增 8 failures，LSP target 同 8 个 zero-identity leaf exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，主 supported-count parser 88 tests/0 failures、private parser 32 tests/0 failures、supported-count LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused/adjacent compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过主 supported-count parser 88 tests/0 failures、private parser 32 tests/0 failures、supported-count LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS，并补跑 zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Identity Wrapped Zero-Minus Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 12:17 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从 zero-minus bitwise-not exact-all-ones wrapper 推进到 exact-zero identity-wrapped zero-minus bitwise-not wrapper：`(zero - (zero - (~(span - span)))) + (unit - unit)` 与 `(zero - (zero - (~(unit - unit)))) - (span - span)` 可以先剥离 exact-zero identity wrapper，再由 existing even-depth zero-minus bitwise-not chain 证明 exact signed all-ones。新增覆盖 `~(((zero - (zero - (~(span - span)))) + (unit - unit)) & negativeUnit)` 与 `~(negativeUnit & ((zero - (zero - (~(unit - unit)))) - (span - span)))`，signed all-ones `&` mask 保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；parser supported-count surface 现在拆为主 target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 88 cases 与新 private mask-operand target `zr_vm_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference_test` 24 cases，LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 覆盖 112 query cases。为守住大文件门槛，同时把 private bitwise-not mask operand parser 组拆到 `tests/parser/test_bitwise_zero_minus_shift_supported_count_private_bitwise_not_mask_operand_range_inference.c`，并给该 MSVC executable 设置短输出名 `zr_vm_bzms_private_not_mask_range_test` 规避 Windows 链接输出路径限制。实现只让 private zero-minus bitwise-not exact-all-ones operand reader 在 evaluated zero-minus chain 前复用 `type_inference_bitwise_identity_skip_zero_identity_wrappers`，随后仍要求最终 exact `-1..-1`；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、odd-depth zero-minus all-ones、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：拆分后 WSL gcc 新 private parser target 24 tests 中新增 4 failures，LSP target 同 4 个 zero-identity wrapped zero-minus bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`，主 supported-count parser target 保持 88 tests/0 failures。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，主 supported-count parser 88 tests/0 failures、新 private parser 24 tests/0 failures、supported-count LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过主 supported-count parser 88 tests/0 failures、新 private parser 24 tests/0 failures、supported-count LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS；MSVC 初次暴露新目标可执行文件名过长的 `LNK1104`，仅缩短输出名后通过。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Identity Wrapped Unary-Minus Zero-Minus Wrapped Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 11:48 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从受限 unary-minus zero-minus wrapper 推进到 exact-zero identity-wrapped unary-minus zero-minus wrapper：`(-(zero - (~(span - span)))) + (unit - unit)` 与 `(-(zero - (~(unit - unit)))) - (span - span)` 可以先剥离 exact-zero identity wrapper，再由 `-(zero - (~0))` 证明 exact signed all-ones。新增覆盖 `~(((-(zero - (~(span - span)))) + (unit - unit)) & negativeUnit)` 与 `~(negativeUnit & ((-(zero - (~(unit - unit)))) - (span - span)))`，signed all-ones `&` mask 保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 104 cases 扩到 108 cases，新增上述 count forms under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只让 private unary-minus zero-minus bitwise-not exact-all-ones operand reader 在检查 unary `-` 前复用 `type_inference_bitwise_identity_skip_zero_identity_wrappers`，随后仍要求 evaluated zero-minus bitwise-not range 可安全取负且最终 exact `-1..-1`；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、arbitrary unary-minus all-ones operands、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 108 tests 中新增 4 failures，新 LSP target 同 4 个 zero-identity wrapped unary-minus zero-minus bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 108 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 108 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Zero-Minus Wrapped Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 11:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从 even-depth zero-minus direct-chain wrapper 推进到受限 unary-minus zero-minus wrapper：`-(zero - (~(span - span)))` 与 `-(zero - (~(unit - unit)))` 可以先让 leaf bitwise-not 发布 exact signed all-ones `-1..-1`，再由 exact-zero-left zero-minus 映射为 `1..1`，最后由 unary `-` 映射回 exact signed all-ones。新增覆盖 `~((-(zero - (~(span - span)))) & negativeUnit)` 与 `~(negativeUnit & (-(zero - (~(unit - unit)))))`，signed all-ones `&` mask 保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 100 cases 扩到 104 cases，新增上述 count forms under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现把既有 private zero-minus bitwise-not exact-all-ones operand reader 拆为一个内部 evaluated reader 和一个保留 exact-all-ones recheck 的原入口，再新增 private unary-minus zero-minus bitwise-not exact-all-ones operand reader：只消费该内部 evaluated range，复用现有安全取负 guard，并重新要求最终 exact `-1..-1`；调用点仍限 `type_inference_bitwise_identity_expression_signed_all_ones_operand_int64_range`。不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、arbitrary unary-minus all-ones operands、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 104 tests 中新增 4 failures，新 LSP target 同 4 个 unary-minus zero-minus wrapped bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 104 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 104 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Wrapped Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 11:03 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从 exact-zero identity wrapper 推进到受限 zero-minus direct-chain wrapper：`zero - (zero - (~(span - span)))` 与 `zero - (zero - (~(unit - unit)))` 可以在偶数层 zero-minus direct-chain 后仍作为 exact signed all-ones side。新增覆盖 `~((zero - (zero - (~(span - span)))) & negativeUnit)` 与 `~(negativeUnit & (zero - (zero - (~(unit - unit)))))`，其中内层 bitwise-not 先发布 exact signed all-ones `-1..-1`，偶数层 zero-minus chain 保持 exact all-ones，signed all-ones `&` mask 再保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 96 cases 扩到 100 cases，新增上述 count forms under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只新增 private zero-minus bitwise-not exact-all-ones operand reader，要求每层 left 为 exact zero、leaf 为 bitwise-not direct exact signed all-ones，并在每次 negation 后重新要求最终 still exact signed all-ones；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、odd-depth zero-minus all-ones operand、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 100 tests 中新增 4 failures，新 LSP target 同 4 个 zero-minus wrapped bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 100 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`build-msvc-lsp-bitwise-zero`, MSBuild 17.14.40) 通过 supported-count parser 100 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Identity Wrapped Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 10:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 private bitwise-not signed all-ones `&` mask operand 从 direct unary `~` over exact-zero 推进到 exact-zero identity-wrapped bitwise-not all-ones operand：`(~(span - span)) + (unit - unit)` 与 `(~(unit - unit)) - (span - span)` 可以先剥离 exact-zero identity wrapper，再作为 exact signed all-ones side。新增覆盖 `~(((~(span - span)) + (unit - unit)) & negativeUnit)` 与 `~(negativeUnit & ((~(unit - unit)) - (span - span)))`，其中内层 bitwise-not 先发布 exact signed all-ones `-1..-1`，wrapper 保持 exact all-ones，signed all-ones `&` mask 再保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 92 cases 扩到 96 cases，新增上述 count forms under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_signed_all_ones_operand_int64_range` 的 bitwise-not direct exact-all-ones branch 前剥离 exact-zero identity wrappers，并仍要求剥离后的 bitwise-not direct result 为 exact signed all-ones；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 96 tests 中新增 4 failures，新 LSP target 同 4 个 zero-identity wrapped bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 96 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`build-msvc-lsp-bitwise-zero`, MSBuild 17.14.40) 通过 supported-count parser 96 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Count Parser Test Support Modularization and LSP Query Regression Guard | 已完成 | 2026-07-04 10:36 +08:00 | 按阶段 2 的数值区间/语义事实维护线，在继续增加 supported-count 覆盖前先处理测试文件模块化门槛：把 `tests/parser/test_bitwise_zero_minus_shift_supported_count_range_inference.c` 中的 compiler state、fixture variable registration、AST first-expression lookup、semantic numeric fact assertion 抽到 `tests/parser/bitwise_zero_minus_shift_supported_count_range_test_support.c/.h`，并让原 parser target 通过 `tests/CMakeLists.txt` 同编译该 support source。主 parser supported-count 测试文件从 963 行降到 836 行，新 support source 130 行、header 11 行；未新增语义形态、未新增 parser/LSP query case、未修改 production inference。 | 行为保持 refactor，基于上一语义切片 GREEN 后执行，未伪造 RED。WSL gcc 与 WSL clang 常规 focused build/run 通过：supported-count parser 92 tests/0 failures，supported-count LSP PASS。Windows MSVC focused compatibility (`build-msvc-lsp-bitwise-zero`, MSBuild 17.14.40) 通过 supported-count parser 92 tests/0 failures，并通过 supported-count LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Exact-Zero All-Ones Mask Operand Publication and LSP Local Query | 已完成 | 2026-07-04 10:19 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 bitwise-not operand reader 内部的 signed all-ones `&` mask operand 从 direct/unary-minus/basic-zero-minus proof 推进到 unary `~` over exact-zero expression：`~(span - span)` 与 `~(unit - unit)` 可以作为 private bitwise-not signed-all-ones mask 的 exact all-ones side。新增覆盖 `~((~(span - span)) & negativeUnit)` 与 `~(negativeUnit & (~(unit - unit)))`，其中内层 exact-zero bitwise-not 先发布 exact signed all-ones `-1..-1`，signed all-ones `&` mask 再保留 `negativeUnit=-3..-2`，外层 bitwise-not 发布 `2..3` 作为 supported-nonnegative shift count；现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 88 cases 扩到 92 cases，新增上述 count forms under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_signed_all_ones_operand_int64_range` 消费 bitwise-not direct result 时要求 exact signed all-ones；不扩展公共 signed all-ones mask/OR helper、non-exact bitwise-not operands、arbitrary all-ones operands、signed all-ones `|` as ordinary supported count、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 92 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not exact-zero all-ones mask operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 92 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`build-msvc-lsp-bitwise-zero`, MSBuild 17.14.40) 通过 supported-count parser 92 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Exact-Zero All-Ones Side Mask Publication and LSP Local Query | 已完成 | 2026-07-04 09:51 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary supported-count `&` 的 exact signed-all-ones side 从 direct/unary-minus direct/zero-minus direct/signed all-ones `|` proof 推进到 unary `~` over exact-zero expression：`~(span - span)` 与 `~(unit - unit)`。新增覆盖 `((~(span - span)) & (~negativeFour))` 与 `((~(zero - unit)) & (~(unit - unit)))`，其中 exact-zero predicate 先证明 bitwise-not operand 为 `0..0`，bitwise-not reader 再发布 exact signed all-ones `-1..-1` 供 ordinary supported-count `&` 的 all-ones side 消费；另一侧继续只由 existing bitwise-not direct/zero-minus reader 发布 `2..3` 或 `1..2`，最终在 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 84 cases 扩到 88 cases，新增上述 bitwise-not exact-zero all-ones side + bitwise-not operand count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range` 接受 exact-zero operand 子集，并让 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range` 消费 bitwise-not direct result 时仍要求 exact signed all-ones；不扩展 signed all-ones `|` as ordinary supported count、nonzero bitwise-not operands beyond existing readers、arbitrary all-ones operands、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 88 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not exact-zero all-ones side supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 88 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 88 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Identity Wrapped Signed All-Ones OR Side Bitwise-Not Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 09:32 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary supported-count `&` 的 exact signed-all-ones side 从 direct/unary-minus direct/signed all-ones `|`/zero-minus direct-chain proof 推进到先剥离 exact-zero identity wrapper 后再证明 signed all-ones `|`：`(allOnes | unit) + (span - span)` 与 `(unit | allOnes) - (span - span)`。新增覆盖 `(((allOnes | unit) + (span - span)) & (~negativeFour))` 与 `((~(zero - unit)) & ((unit | allOnes) - (span - span)))`，其中 `span - span` 继续由 exact-zero predicate 证明为 `0..0`，all-ones side 剥离 wrapper 后由既有 signed all-ones `|` helper 发布 exact `-1..-1`；另一侧继续只由 existing zero-wrapped bitwise-not direct/zero-minus reader 发布 `2..3` 或 `1..2`，最终在 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 80 cases 扩到 84 cases，新增上述 zero-identity wrapped signed all-ones OR side + bitwise-not operand count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range` 入口复用 `type_inference_bitwise_identity_skip_zero_identity_wrappers`，让该 helper 的既有 exact all-ones proofs 在 exact-zero wrapper 下透明，但仍要求剥离结果 exact signed all-ones 且消费点限 ordinary supported-count `&`；不扩展 signed all-ones `|` as ordinary supported count、arbitrary all-ones operands、bitwise-not operand reader、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 84 tests 中新增 4 failures，新 LSP target 同 4 个 zero-identity wrapped signed all-ones OR side bitwise-not operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 84 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 84 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Signed All-Ones OR Side Bitwise-Not Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 09:11 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary supported-count `&` 的 exact signed-all-ones side 从 direct/unary-minus direct/signed all-ones `|` proof 推进到 existing zero-minus direct-range chain proof：`zero - (zero - (allOnes | unit))` 与 `zero - (zero - (unit | allOnes))`。新增覆盖 `((zero - (zero - (allOnes | unit))) & (~negativeFour))` 与 `((~(zero - unit)) & (zero - (zero - (unit | allOnes))))`，其中 all-ones side 先由既有 signed all-ones `|` helper 发布 exact `-1..-1`，再通过既有 zero-minus direct-chain reader 回到 exact `-1..-1`；另一侧继续只由 existing zero-wrapped bitwise-not direct/zero-minus reader 发布 `2..3` 或 `1..2`，最终在 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 76 cases 扩到 80 cases，新增上述 zero-minus signed all-ones OR side + bitwise-not operand count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只扩展 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range`：复用 existing `type_inference_bitwise_identity_expression_zero_minus_direct_int64_range`，并再次要求 exact signed all-ones；不扩展 signed all-ones `|` as ordinary supported count、arbitrary all-ones operands、bitwise-not operand reader、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 80 tests 中新增 4 failures，新 LSP target 同 4 个 zero-minus signed all-ones OR side bitwise-not operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 80 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 80 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones OR Side Bitwise-Not Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 09:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary supported-count `&` 的 exact signed-all-ones side 从 zero-wrapped / unary-minus direct all-ones proof 推进到 existing signed all-ones `|` annihilator proof：`allOnes | unit` 与 `unit | allOnes`。新增覆盖 `((allOnes | unit) & (~negativeFour))` 与 `((~(zero - unit)) & (unit | allOnes))`，其中 all-ones side 通过既有 signed all-ones `|` helper 发布 exact `-1..-1`，另一侧继续只由 existing zero-wrapped bitwise-not direct/zero-minus reader 发布 `2..3` 或 `1..2`，最终在 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 72 cases 扩到 76 cases，新增上述 signed all-ones OR side + bitwise-not operand count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只扩展 `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range`：在确认 binary `|` 后复用 existing `type_inference_bitwise_identity_expression_signed_all_ones_or_range`，并再次要求 exact signed all-ones；不扩展 signed all-ones `|` as ordinary supported count、arbitrary all-ones operands、bitwise-not operand reader、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 76 tests 中新增 4 failures，新 LSP target 同 4 个 signed all-ones OR side bitwise-not operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 76 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 76 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Identity Supported-Count Reader Module Split and LSP Local Query Regression Guard | 已完成 | 2026-07-04 08:42 +08:00 | 按阶段 2 的数值区间/语义事实维护线，在上一轮 supported-count GREEN 后先执行模块化门禁：把 `type_inference_bitwise_identity.c` 中的 supported-nonnegative count reader、ordinary exact all-ones side helper、以及 all-ones mask-range helper 拆到新模块 `type_inference_bitwise_identity_supported_count.c`。主 `type_inference_bitwise_identity.c` 保留 exact-zero、zero-identity range consumer 和 apply orchestration；同目录 source-local header 暴露 supported-nonnegative 与 mask-range 两个窄接口供 existing exact-zero / zero-identity / apply consumers 复用。CMake 通过现有 source glob 自动纳入新文件，无新增语义形态、无新增 parser/LSP query case。line counts：bitwise identity 主文件从 978 行降到 408 行，新 supported-count 模块 563 行，direct-range 761 行，bitwise-not range 214 行，header 113 行。 | 行为保持 refactor，基于前一语义切片 GREEN 后执行，未伪造 RED。拆分后 WSL gcc 与 WSL clang focused/adjacent target build/run 通过：supported-count parser 72 tests/0 failures + LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 72 tests/0 failures + LSP PASS 和 zero-wrapper parser 92 tests/0 failures + LSP PASS。CMake glob reconfigure 已编译新模块；输出中的 GLOB mismatch 来自既有 dirty/untracked source glob，不作为测试失败。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Zero-Minus Signed All-Ones Mask Side Bitwise-Not Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 08:24 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary signed all-ones `&` mask supported-count 从 exact signed-all-ones side 只能由 zero-wrapped direct `allOnes` 证明，推进到还能由 unary-minus zero-minus direct 形态证明：`-(zero - allOnes)`。新增覆盖 `(-(zero - allOnes)) & (~negativeFour)` 与 `(~(zero - unit)) & (-(zero - allOnes))`，其中 all-ones side 发布 exact `-1..-1`，另一侧继续由既有 bitwise-not direct/zero-minus reader 发布 `2..3` 或 `1..2`，最终在 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 68 cases 扩到 72 cases，新增上述 unary-minus zero-minus all-ones side + bitwise-not operand count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现新增窄 helper `type_inference_bitwise_identity_expression_supported_count_all_ones_side_range`，只供 ordinary supported-count `&` 分支判断 exact all-ones side；公共 signed all-ones mask/OR helper、bitwise-not operand reader、signed all-ones `|` as ordinary supported count、arbitrary all-ones operands、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 和 CFG-wide loop dataflow 均不扩展。`type_inference_bitwise_identity.c` 当前 978 行，下一次继续增加 supported-count 责任前应拆出 supported-count reader 模块。 | TDD RED：WSL gcc supported-count parser 72 tests 中新增 4 failures，新 LSP target 同 4 个 unary-minus zero-minus all-ones side bitwise-not operand supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 72 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 72 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Identity Bitwise-Not Direct-Range Module Split and LSP Local Query Regression Guard | 已完成 | 2026-07-04 08:09 +08:00 | 按阶段 2 的数值区间/语义事实维护线，在上一轮 bitwise-not zero-identity wrapper GREEN 后先执行模块化门禁：把接近 1000 行的 `type_inference_bitwise_identity_direct_range.c` 中的 bitwise-not direct-range reader、bitwise-not operand reader、以及仅供 bitwise-not 使用的 signed all-ones `&`/`|` operand helpers 拆到新模块 `type_inference_bitwise_identity_bitwise_not_range.c`。direct-range 主文件继续保留 direct/zero-minus/unary-minus/same-identifier/all-ones general helper，两个既有 direct-range helper 通过同目录 private header 暴露给新模块复用；CMake 通过现有 source glob 自动纳入新文件，无新增语义形态、无新增 LSP query case。direct-range 主文件从 974 行降到 761 行，新 bitwise-not range 模块 214 行，头文件 101 行。 | 行为保持 refactor，基于前一语义切片 GREEN 后执行，未伪造 RED。拆分后 WSL gcc 与 WSL clang focused/adjacent target build/run 通过：supported-count parser 68 tests/0 failures + LSP PASS、shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 68 tests/0 failures + LSP PASS 和 zero-wrapper parser 92 tests/0 failures + LSP PASS。CMake glob reconfigure 已编译新模块；输出中的 GLOB mismatch 来自既有 dirty/untracked source glob，不作为测试失败。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Bitwise-Not Zero-Identity Wrapper Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 07:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 ordinary signed all-ones `&` mask bitwise-not direct/zero-minus operand 推进到该 bitwise-not operand 被 exact-zero identity wrapper 包裹的形态：`allOnes & ((~negativeFour) + (span - span))` 与 `((~(zero - unit)) - (span - span)) & allOnes`。`span - span` 继续由 exact-zero predicate 证明为 `0..0`，再剥离到既有 bitwise-not direct/zero-minus range reader，分别发布 `2..3` 与 `1..2`，最后在 signed all-ones `&` identity 与 supported-count 非负 gate 下作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 64 cases 扩到 68 cases，新增上述 bitwise-not all-ones mask zero-identity wrapper count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现新增 direct-range helper `type_inference_bitwise_identity_expression_zero_wrapped_bitwise_not_direct_int64_range`，只先剥离 zero identity wrapper 再委托既有 bitwise-not direct reader，并只由 ordinary supported-count `&` exact signed-all-ones side 分支消费；公共 signed all-ones mask/OR helper、原 bitwise-not direct reader、bitwise-not operand reader、signed all-ones `|` as ordinary supported count、arbitrary all-ones mask operands、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 和 CFG-wide loop dataflow 均不扩展。 | TDD RED：WSL gcc supported-count parser 68 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not all-ones mask zero-identity wrapper supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 68 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 68 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Bitwise-Not Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 07:32 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 ordinary signed all-ones `&` mask unary-minus zero-minus same-identifier operand 推进到 ordinary signed all-ones `&` mask whose other side is bitwise-not direct/zero-minus operand：`allOnes & (~negativeFour)` 与 `(~(zero - unit)) & allOnes`。既有 bitwise-not direct reader 将 `negativeFour=-4..-3` 发布为 `2..3`，并将 `zero - unit=-3..-2` 发布为 `1..2`；在 signed all-ones `&` identity 与 supported-count 非负 gate 下可作为合法移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 60 cases 扩到 64 cases，新增上述 bitwise-not all-ones mask count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把既有 `type_inference_bitwise_identity_expression_bitwise_not_direct_int64_range` 加入 ordinary supported-count `&` exact signed-all-ones side 分支；公共 signed all-ones mask/OR helper、bitwise-not operand reader、signed all-ones `|` as ordinary supported count、arbitrary all-ones mask operands、nonzero-left shift、negative/out-of-range shift count、general bitwise/shift algebra、broader self-dependent replay、fixed point 和 CFG-wide loop dataflow 均不扩展。 | TDD RED：WSL gcc supported-count parser 64 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not all-ones mask supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 64 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 64 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Unary-Minus Zero-Minus Same-Identifier Idempotent Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 07:16 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 ordinary signed all-ones `&` mask exact-zero-left `zero - (...)` same-identifier operand 推进到 unary `-` 包裹该 zero-minus same-identifier operand：`allOnes & (-(zero - (unit & unit)))` 与 `(-(zero - (unit | (unit - zero)))) & allOnes` 先由 same-identifier idempotent 保留 `unit=2..3`，经 exact-zero-left `zero - (...)` 映射为 `-3..-2`，再由 unary `-` 映射回 `2..3`，最后在 signed all-ones mask identity 下被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 56 cases 扩到 60 cases，新增上述 unary-minus zero-minus same-identifier all-ones mask count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现将 `type_inference_bitwise_identity_expression_unary_minus_zero_minus_signed_same_identifier_range` 放在 direct-range 模块，并只由 ordinary supported-count `&` mask 分支在 exact signed-all-ones side 场景消费；该 reader 要求 unary `-` operand 先通过 exact-zero-left zero-minus same-identifier reader，再执行安全取负并避开 `INT64_MIN`。原 zero-minus same-identifier reader、bitwise-not supported-count 分支、signed all-ones public mask/OR helper、supported-count 非负 gate、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 signed all-ones `|` as ordinary supported count、arbitrary all-ones mask operands、nonzero-left zero-minus、different-identifier structural equality、negative/sign-crossing mask result、negative/out-of-range shift count、nonzero-left shift、general bitwise/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 60 tests 中新增 4 failures，新 LSP target 同 4 个 unary-minus zero-minus same-identifier all-ones mask supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 60 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 60 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Zero-Minus Same-Identifier Idempotent Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 06:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 ordinary signed all-ones `&` mask same-identifier idempotent operand 推进到 exact-zero-left `zero - (...)` 包裹的 same-identifier `&`/`|` idempotent operand：`allOnes & (zero - (negativeUnit & negativeUnit))` 与 `(zero - (negativeUnit | (negativeUnit - zero))) & allOnes` 先由 same-identifier idempotent 保留 `negativeUnit=-3..-2`，再经 exact-zero-left `zero - (...)` 映射为 `2..3`，最后在 signed all-ones mask identity 下被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 52 cases 扩到 56 cases，新增上述 zero-minus same-identifier all-ones mask count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现将 exact-zero-left zero-minus same-identifier range reader 放在 direct-range 模块的 `type_inference_bitwise_identity_expression_zero_minus_signed_same_identifier_range` 中，并只由 ordinary supported-count `&` mask 分支在 exact signed-all-ones side 场景消费；该 reader 要求左侧递归证明 exact zero、右侧为 signed same-identifier idempotent range、且避开 `INT64_MIN` negation；公共 signed all-ones mask/OR helper 仍保持 narrow，bitwise-not private all-ones helper 不扩大。原 bitwise-not supported-count 分支、`~max..~min` 发布、supported-count 非负 gate、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 arbitrary all-ones mask operands、signed all-ones `|` as ordinary supported count、nonzero-left zero-minus、different-identifier structural equality、negative/sign-crossing mask result、negative/out-of-range shift count、nonzero-left shift、general bitwise/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 56 tests 中新增 4 failures，新 LSP target 同 4 个 zero-minus same-identifier all-ones mask supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 56 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 56 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Same-Identifier Idempotent Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 06:31 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 direct signed all-ones `&` mask operand 推进到 ordinary signed all-ones `&` mask whose other side is a same-identifier `&`/`|` idempotent nonnegative leaf：`allOnes & (unit & unit)` 与 `(unit | (unit - zero)) & allOnes` 在 signed all-ones mask identity 先保留 `unit=2..3` 后，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 48 cases 扩到 52 cases，新增上述 same-identifier all-ones mask count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 的 ordinary binary `&` 分支增加 exact signed all-ones side + signed same-identifier nonnegative side 的受限读取；公共 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range` 与 signed all-ones OR helper 保持 narrow，bitwise-not private all-ones operand helper 仅重命名为 generic private operand reader 并仍只供 bitwise-not private mask/OR 使用。原 bitwise-not supported-count 分支、`~max..~min` 发布、supported-count 非负 gate、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 arbitrary all-ones mask operands、signed all-ones `|` as ordinary supported count、different-identifier structural equality、negative/sign-crossing mask result、negative/out-of-range shift count、nonzero-left shift、general bitwise/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 52 tests 中新增 4 failures，新 LSP target 同 4 个 same-identifier all-ones mask supported-count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 52 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 52 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Signed All-Ones Same-Identifier Idempotent Operand Range Publication and LSP Local Query | 已完成 | 2026-07-04 06:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not unary-minus zero-minus same-identifier `&`/`|` idempotent operand 推进到 bitwise-not signed all-ones `&` mask / `|` annihilator 中嵌套 same-identifier `&`/`|` idempotent operand：`~(allOnes & (negativeFour & negativeFour))` 与 `~(allOnes | (negativeFour | (negativeFour - zero)))` 在 same-identifier idempotent 先保留负区间 `-4..-3`，signed all-ones mask 保留该负区间或 OR annihilator 发布 exact `-1..-1`，再由 unary `~` 证明为 `2..3` 或 exact `0..0` 时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 44 cases 扩到 48 cases，新增上述 bitwise-not all-ones count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现新增仅供 bitwise-not all-ones operand 使用的私有 reader：先复用既有 zero-wrapped/unary-minus/basic-zero-minus direct operand reader，再回退到既有 basic zero-minus chain leaf reader 以读取 same-identifier `&`/`|` idempotent leaf；`type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range` 的 `&`/`|` all-ones 分支改用新的私有 mask/OR helper，公共 signed all-ones mask/OR helper 不扩大。原 signed same-identifier bitwise-not 分支、`~max..~min` 发布、supported-count 非负 gate、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 arbitrary all-ones operands beyond direct/unary-minus/zero-minus-chain/same-identifier leaf、different-identifier structural equality、`^` under unary `~`、positive operand 后的 bitwise-not negative result、sign-crossing bitwise-not result、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 48 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not signed all-ones same-identifier operand count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 48 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 48 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Unary-Minus Zero-Minus Same-Identifier Idempotent Range Publication and LSP Local Query | 已完成 | 2026-07-04 05:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not signed same-identifier `&`/`|` idempotent operand 推进到 bitwise-not unary-minus zero-minus same-identifier `&`/`|` idempotent operand：`~(-(zero - (negativeFour & negativeFour)))` 与 `~(-(zero - (negativeFour | (negativeFour - zero))))` 在 same-identifier idempotent 先保留负区间 `-4..-3`，经 exact-zero-left `zero - (...)` 映射为 `3..4`，再经 unary `-` 回到 `-4..-3`，最后由 unary `~` 证明为 `2..3` 时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 40 cases 扩到 44 cases，新增上述 bitwise-not unary-minus zero-minus same-identifier count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把私有 `type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range` 的首个 operand reader 从较浅的 zero-wrapped/unary-minus/zero-minus direct reader 替换为既有 `type_inference_bitwise_identity_expression_zero_wrapped_or_unary_minus_zero_wrapped_or_basic_zero_minus_direct_int64_range`，从而复用已经受限的 basic zero-minus chain leaf reader；same-identifier `&`/`|`、signed all-ones `&` mask 与 `|` annihilator operand 分支、`~max..~min` 发布、supported-count 非负 gate、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 nonzero-left zero-minus、different-identifier structural equality、`^` under unary `~`、positive operand 后的 bitwise-not negative result、sign-crossing bitwise-not result、任意非 direct/unary-minus direct/zero-minus chain/signed all-ones/same-identifier idempotent operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 44 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not unary-minus zero-minus same-identifier count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 44 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 44 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Signed Same-Identifier Idempotent Range Publication and LSP Local Query | 已完成 | 2026-07-04 05:10 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not signed all-ones `|` annihilator operand 推进到 bitwise-not signed same-identifier `&`/`|` idempotent operand：`~(negativeFour & negativeFour)` 与 `~(negativeFour | (negativeFour - zero))` 在 same-identifier idempotent 先保留负区间 `-4..-3`、再经 unary `~` 证明为 `2..3` 时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 36 cases 扩到 40 cases，新增上述 bitwise-not same-identifier count under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把私有 `type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range` 的 binary operand 分支追加受限 same-identifier `&`/`|` idempotent 读取：仍复用现有 `type_inference_bitwise_identity_expression_signed_same_identifier_range`，并继续以 `~max..~min` 发布 bitwise-not 结果；`type_inference_bitwise_identity_expression_supported_nonnegative_range` 仍只在 unary `~` 结果下界 `>= 0` 时接受为 supported-nonnegative count。原 zero-wrapped/unary-minus/zero-minus direct operand reader、signed all-ones `&` mask 与 `|` annihilator operand reader、unary `+`/`-` supported-count 分支、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier direct `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 different-identifier structural equality、`^` under unary `~`、positive operand 后的 bitwise-not negative result、sign-crossing bitwise-not result、任意非 direct/unary-minus direct/zero-minus direct/signed all-ones/same-identifier idempotent operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 40 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not signed same-identifier count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 40 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 40 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Signed All-Ones OR Annihilator Range Publication and LSP Local Query | 已完成 | 2026-07-04 04:51 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not signed all-ones `&` mask operand 推进到 bitwise-not signed all-ones `|` annihilator operand：`~(allOnes | unit)` 与 `~(unit | allOnes)` 在 signed all-ones OR annihilator 先证明为 exact `-1..-1`、再经 unary `~` 证明为 exact `0..0` 时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 32 cases 扩到 36 cases，新增 `zero << (~(allOnes | unit))` 与 `zero >> (~(unit | allOnes))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把私有 `type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range` 的 binary operand 分支从 `&` mask 追加到 `|` annihilator：`&` 仍复用 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range`，`|` 复用现有 `type_inference_bitwise_identity_expression_signed_all_ones_or_range`，并继续以 `~max..~min` 发布 bitwise-not 结果；`type_inference_bitwise_identity_expression_supported_nonnegative_range` 仍只在 unary `~` 结果下界 `>= 0` 时接受为 supported-nonnegative count。原 zero-wrapped/unary-minus/zero-minus direct operand reader、unary `+`/`-` supported-count 分支、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 arbitrary `|` operand under `~`、非 all-ones OR、positive operand 后的 bitwise-not negative result、sign-crossing bitwise-not result、任意非 direct/unary-minus direct/zero-minus direct/signed all-ones `&`/`|` operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 36 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not signed all-ones OR count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 36 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 36 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Signed All-Ones Mask Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 04:34 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not unary-minus direct-range operand 推进到 bitwise-not signed all-ones `&` mask operand：`~(allOnes & negativeUnit)` 与 `~((zero - unit) & allOnes)` 在 signed all-ones mask identity 先证明为负区间、再经 unary `~` 证明为非负时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 28 cases 扩到 32 cases，新增 `zero << (~(allOnes & negativeUnit))` 与 `zero >> (~((zero - unit) & allOnes))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现新增私有 `type_inference_bitwise_identity_expression_bitwise_not_operand_int64_range`，先保留既有 zero-wrapped/unary-minus/zero-minus direct operand reader，失败后才对 binary `&` 复用现有 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range`，并继续以 `~max..~min` 发布 bitwise-not 结果；`type_inference_bitwise_identity_expression_supported_nonnegative_range` 仍只在 unary `~` 结果下界 `>= 0` 时接受为 supported-nonnegative count。原 unary `+`/`-` supported-count 分支、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、signed all-ones OR annihilator、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 signed all-ones `|`/OR operand under `~`、positive operand 后的 bitwise-not negative result、sign-crossing bitwise-not result、任意非 direct/unary-minus direct/zero-minus direct/signed all-ones `&` mask operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 32 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not signed all-ones mask count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 32 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 32 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Unary-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 04:10 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 bitwise-not direct/zero-minus direct operand 推进到 bitwise-not unary-minus direct-range operand：`~(-unit)` 与 `~(-(unit + zero))` 在结果范围被证明为非负时，被作为合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 24 cases 扩到 28 cases，新增 `zero << (~(-unit))` 与 `zero >> (~(-(unit + zero)))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把 `type_inference_bitwise_identity_expression_bitwise_not_direct_int64_range` 的 operand reader 从 direct/zero-minus direct 扩为复用现有 `type_inference_bitwise_identity_expression_zero_wrapped_or_unary_minus_direct_int64_range`，因此可读取 zero-wrapped direct、unary-minus direct 或 zero-minus direct int64 operand，并继续以 `~max..~min` 发布 bitwise-not 结果；`type_inference_bitwise_identity_expression_supported_nonnegative_range` 仍只在 unary `~` 结果下界 `>= 0` 时接受为 supported-nonnegative count。原 unary `+`/`-` supported-count 分支、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 bitwise-not 正数 operand 后的负结果、sign-crossing bitwise-not result、任意非 direct/unary-minus direct/zero-minus direct operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 28 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not unary-minus count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 28 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 28 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Bitwise-Not Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 03:46 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 unary-minus direct-range count 推进到 bitwise-not direct-range count：`~negativeFour` 与 `~(zero - unit)` 在结果范围被证明为非负时，被作为 `unit` 的合法非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 20 cases 扩到 24 cases，新增 `zero << (~negativeFour)` 与 `zero >> (~(zero - unit))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现新增 `type_inference_bitwise_identity_expression_bitwise_not_direct_int64_range`，只读取 zero-wrapped direct 或 zero-minus direct int64 operand，并以 `~max..~min` 发布 bitwise-not 结果；`type_inference_bitwise_identity_expression_supported_nonnegative_range` 只在 unary `~` 结果下界 `>= 0` 时接受为 supported-nonnegative count。原 unary `+`/`-`、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 bitwise-not 正数 operand 后的负结果、sign-crossing bitwise-not result、任意非 direct operand、negative/out-of-range shift count、nonzero-left shift、general bitwise-not/shift algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 24 tests 中新增 4 failures，新 LSP target 同 4 个 bitwise-not count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused/adjacent target build/run 通过，supported-count parser 24 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 24 tests/0 failures + LSP PASS，并通过 zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Unary-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 03:23 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 zero-minus direct-range chain count 推进到 unary-minus direct-range count：`-negativeUnit` 与 `-(zero - (unit + zero))` 在结果范围被证明为非负时，被作为 `unit` 的 `2..3` 非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 16 cases 扩到 20 cases，新增 `zero << (-negativeUnit)` 与 `zero >> (-(zero - (unit + zero)))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 的 unary `-` 分支复用现有 `type_inference_bitwise_identity_expression_unary_minus_direct_int64_range`，且仅当返回范围下界 `>= 0` 时发布到 supported-nonnegative reader；原 unary `+`、zero-minus direct-range reader、exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 unary-minus sign-crossing result、negative unary-minus result、negative/out-of-range shift count、arbitrary signed direct range as supported count、nonzero-left shift、general shift algebra/range publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 20 tests 中新增 4 failures，新 LSP target 同 4 个 unary-minus count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，supported-count parser 20 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 20 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Zero-Minus Direct-Range Chain Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 03:00 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 signed all-ones mask identity count 推进到已由 zero-minus direct-range reader 证明为非负的本地 zero-minus chain count：`zero - (zero - unit)` 与 `zero - (zero - (unit + zero))` 被作为 `unit` 的 `2..3` 非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 12 cases 扩到 16 cases，新增 `zero << (zero - (zero - unit))` 与 `zero >> (zero - (zero - (unit + zero)))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 的入口复用现有 `type_inference_bitwise_identity_expression_zero_minus_direct_int64_range`，且仅当返回范围下界 `>= 0` 时发布到 supported-nonnegative reader；原 exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、signed all-ones mask count、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 negative/sign-crossing zero-minus direct-range count、nonzero-left shift、negative/out-of-range shift count、arbitrary signed direct range as supported count、general shift algebra/range publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 16 tests 中新增 4 failures，新 LSP target 同 4 个 zero-minus chain count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，supported-count parser 16 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 16 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Signed All-Ones Mask Identity Range Publication and LSP Local Query | 已完成 | 2026-07-04 02:43 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从 exact-zero identity wrapper count 推进到 signed all-ones mask identity count：`allOnes & unit` 与 `unit & allOnes` 在 `allOnes=-1..-1` 且 identity result 证明为非负时，被作为 `unit` 的 `2..3` 非负移位计数消费。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 8 cases 扩到 12 cases，新增 `zero << (allOnes & unit)` 与 `zero >> (unit & allOnes)` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 的 binary `&` 路径复用现有 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range`，且仅当返回的 identity range 下界 `>= 0` 时发布到 supported-nonnegative reader；原 exact-zero count alternative、zero-identity wrapper stripping、非负算术/shift reader、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 signed all-ones OR as count、negative/sign-crossing mask count、partial signed mask reasoning、nonzero-left shift、negative/out-of-range shift count、general bitwise algebra/range publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 12 tests 中新增 4 failures，新 LSP target 同 4 个 all-ones mask count query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，supported-count parser 12 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 12 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Exact-Zero Identity Wrapper Range Publication and LSP Local Query | 已完成 | 2026-07-04 02:24 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift 的 bounded supported-nonnegative count 从直接非负算术推进到 exact-zero identity wrapper count：`unit + (span - span)` 与 `unit - (span - span)` 在 `span - span` 已由 exact-zero predicate 证明为 exact zero 时，被归一化为 `unit` 的非负区间。现有 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test` 从 4 cases 扩到 8 cases，新增 `zero << (unit + (span - span))` 与 `zero >> (unit - (span - span))` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 入口复用 `type_inference_bitwise_identity_skip_zero_identity_wrappers`，让 supported count reader 剥离已证明 exact-zero 的 `+`/`-` identity wrapper；原 exact-zero count alternative、非负 `+`/`-`/`*`/`/`/`%`/shift reader、same-identifier `^`/`&`/`|`、exact-zero `&` annihilator、zero-left shift 左侧 exact-zero guard、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 negative/out-of-range shift count、nonzero-left shift、unsupported signed/nonnegative proof、nonzero identity wrapper、general shift algebra/range publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc supported-count parser 8 tests 中新增 4 failures，新 LSP target 同 4 个 query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，supported-count parser 8 tests/0 failures + LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过 supported-count parser 8 tests/0 failures + LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Supported-Nonnegative Count Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 02:09 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift exact-zero-left 从 direct/exact-zero shift-count 推进到 bounded supported-nonnegative shift-count：当 binary `<<`/`>>` 左侧递归证明 exact zero，右侧由 `type_inference_bitwise_identity_expression_supported_nonnegative_range` 证明非负且 `max < 63` 时，将该 shift 表达式视作 exact zero。新增独立小型 parser target `zr_vm_bitwise_zero_minus_shift_supported_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_supported_count_range_query_test`，覆盖 `zero << (unit + unit)` 与 `zero >> (unit + unit)` under zero-minus RHS 的 `2..3`，以及同形 zero-minus unary-RHS 的 `-3..-2`。实现只把 `type_inference_bitwise_identity_expression_is_exact_zero_value` 中 `<<`/`>>` zero-left branch 的 right guard 从 zero-wrapper/direct range 换为 supported-nonnegative range，同时保留 right exact-zero alternative；exact-zero count、same-identifier `^`/`-`/`%`、both-exact `&`/`*`/OR/XOR、zero-left `/`/`%`、direct/unary-direct leaf、signed all-ones `&`/`|`、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 negative/out-of-range shift count、nonzero-left shift、right-exact-zero/nonnegative count with nonzero left、unsupported/unknown shift-count、general shift algebra/range publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 中 4 failures，新 LSP target 同 4 个 query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，新 parser 4 tests/0 failures + 新 LSP PASS，同时相邻 shift exact-zero-count parser 4 tests/0 failures + LSP PASS、zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过新 parser 4 tests/0 failures + 新 LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Shift Exact-Zero Count Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 01:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-left shift exact-zero-left 从“shift-count 必须是 zero-wrapper/direct int64 非负小于 63 range”推进到“shift-count 也可由当前 exact-zero predicate 证明为 exact zero”。新增独立小型 parser target `zr_vm_bitwise_zero_minus_shift_exact_zero_count_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_minus_shift_exact_zero_count_range_query_test`，避免继续扩充接近 1000 行的既有 unary-RHS fixture；覆盖 `zero << (span - span)` 与 `zero >> ((span - span) | (unit - unit))` under zero-minus RHS 的 `2..3`，以及 `zero << (span - span)` 与 `zero >> ((span - span) ^ (unit - unit))` under zero-minus unary-RHS 的 `-3..-2`。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 的 `<<`/`>>` zero-left branch 中增加 right exact-zero alternative；原 direct nonnegative shift-count guard、same-identifier `^`/`-`/`%`、both-exact `&`/`*`/OR/XOR、zero-left `/`/`%`、direct/unary-direct leaf、signed all-ones `&`/`|`、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 negative/out-of-range shift count、nonzero-left shift、right-exact-zero with nonzero left、nonzero shift-count algebra、general shift range publication、unknown/non-range operands、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 4 tests 中 4 failures，新 LSP target 同 4 个 query 返回旧 `0..0`。GREEN：WSL gcc 与 WSL clang focused target build/run 通过，新 parser 4 tests/0 failures + 新 LSP PASS，同时相邻 zero-minus RHS parser 88 tests/0 failures + LSP PASS、zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。Windows MSVC focused compatibility (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`) 通过新 parser 4 tests/0 failures + 新 LSP PASS。本切片未运行 full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Two Exact-Zero Bitwise-OR/XOR Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 01:27 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 two exact-zero bitwise-AND/product annihilator 表达式再推进到受限 two exact-zero bitwise-OR/XOR 表达式：当 binary OR 或 XOR 的左右两侧都已由当前 exact-zero predicate 递归证明为 exact zero 时，把该表达式视作 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 86 扩到 88 cases：新增 `((span - span) | (unit - unit)) - (zero - unit)` 与 `((span - span) ^ (unit - unit)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 93 扩到 95 cases：新增 `((span - span) | (unit - unit)) - (-(zero - unit))` 与 `((span - span) ^ (unit - unit)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 增加 both-sides-exact-zero branch for binary OR/XOR，仍保留同名 XOR exact-zero 分支、both-exact `&`/`*`、one-exact-side-plus-direct-range `&`/`*`、zero-left `/`/`%`/shift、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护。该切片不扩展 one exact side plus arbitrary expression、nonzero OR/XOR、XOR equality beyond same-identifier、general bitwise algebra、general structural equality、unknown/non-range operands、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 88 tests 中 2 failures，focused LSP mirror 同 2 个 OR/XOR double-exact query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 95 tests 中 2 failures，focused LSP mirror 同 2 个 OR/XOR double-exact query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 88 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 95 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Two Exact-Zero Bitwise-AND/Product Annihilator Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 01:11 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 zero-left shift exact-zero 表达式再推进到受限 two exact-zero bitwise-AND/product annihilator 表达式：当 binary `&` 或 `*` 的左右两侧都已由当前 exact-zero predicate 递归证明为 exact zero 时，把该表达式视作 zero-minus 左侧 exact zero，即使两侧都不是 direct/zero-wrapper direct range。zero-minus RHS parser/LSP focused fixture 从 82 扩到 86 cases：新增 discovery/GREEN 覆盖 `zero - (zero - (zero << unit))` 与 `zero - (zero - (zero >> (unit - zero)))` under `(zero + zero) | ...` 的 exact `0..0`，并新增 RED/GREEN 覆盖 `((span - span) * (unit - unit)) - (zero - unit)` 与 `((span - span) & (unit - unit)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 89 扩到 93 cases：新增 discovery/GREEN 覆盖 `zero - (-(zero - (zero << unit)))` 与 `zero - (-(zero - (zero >> (unit - zero))))` under `(zero + zero) | ...` 的 exact `0..0`，并新增 RED/GREEN 覆盖 `((span - span) * (unit - unit)) - (-(zero - unit))` 与 `((span - span) & (unit - unit)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 的 `&`/`*` 区域增加 both-sides-exact-zero branch，仍复用现有 exact-zero predicate 的 restricted `&`/`*` annihilator、zero-left `/`/`%`/`<<`/`>>`、same-identifier `^`/`-`/`%` 与 zero-wrapper/direct guards；原 one-exact-side-plus-direct-range `&`/`*` 分支、zero-left `/`/`%`/shift、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 exact-zero `&`/`*` with unknown/non-range opposite side、one exact side plus arbitrary expression、division/modulo/shift both-exact variants、general product/bitwise algebra、general exact-zero predicate beyond the existing restricted predicate plus both-exact `&`/`*`、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | Discovery/GREEN：WSL gcc 中四个 exact-zero shift leaf parser/LSP 用例在生产改动前已通过。TDD RED：WSL gcc zero-minus RHS 新 focused parser 86 tests 中 2 failures，focused LSP mirror 同 2 个 double-exact query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 93 tests 中 2 failures，focused LSP mirror 同 2 个 double-exact query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 86 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 93 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Nonnegative Zero-Left Shift Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 00:57 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 zero-left modulo exact-zero 表达式再推进到受限 nonnegative shift-count zero-left shift exact-zero 表达式：当 binary `<<`/`>>` 的左侧已递归证明 exact zero，右侧具备 zero-wrapper/direct int64 range 且 shift count 区间满足 `min >= 0 && max < 63` 时，把 `zero << nonnegative-shift` / `zero >> nonnegative-shift` 视作 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 80 扩到 82 cases，新增 `(zero << unit) - (zero - unit)` 与 `(zero >> (unit - zero)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 87 扩到 89 cases，新增 `(zero << unit) - (-(zero - unit))` 与 `(zero >> (unit - zero)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 增加 binary `<<`/`>>` 的 zero-left/nonnegative-shift branch，仍要求左侧递归证明 exact zero，右侧通过 zero-wrapper/direct int64 range reader 且 shift count 不为负并小于 int64 sign-bit shift count；原同名 `%` exact-zero 分支、同名 `^`/`-` exact-zero、受限 `&`/`*` exact-zero-left、zero-left `/`/`%` exact-zero-left、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 negative/out-of-range shift count、nonzero-left shift、right-exact-zero shift、general shift algebra、shift overflow/nonzero-left range publication、general exact-zero predicate beyond restricted `&`/`*` annihilator, zero-left `/`/`%`/`<<`/`>>`, and same-identifier `^`/`-`/`%`、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 82 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 89 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 82 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 89 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Nonzero-Divisor Zero-Left Modulo Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 00:42 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 zero-left division exact-zero 表达式再推进到受限 zero-left modulo exact-zero 表达式：当 binary `%` 的左侧已递归证明 exact zero，右侧具备 zero-wrapper/direct int64 range 且区间排除 0 时，把 `zero % nonzero-range` 视作 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 78 扩到 80 cases，新增 `(zero % unit) - (zero - unit)` 与 `(zero % (unit - zero)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 85 扩到 87 cases，新增 `(zero % unit) - (-(zero - unit))` 与 `(zero % (unit - zero)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只把 `type_inference_bitwise_identity_expression_is_exact_zero_value` 中 zero-left/nonzero-divisor 分支从 binary `/` 扩到 binary `%`，仍要求左侧递归证明 exact zero，右侧通过 zero-wrapper/direct int64 range reader 且 `min > 0 || max < 0`；原同名 `%` exact-zero 分支仍只接受 same-identifier 且 shared binding range 排除 0，原同名 `^`/`-` exact-zero、受限 `&`/`*` exact-zero-left、zero-left `/` exact-zero-left、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 zero-inclusive divisor modulo、nonzero-left modulo、right-exact-zero modulo、general modulo algebra、general exact-zero predicate beyond restricted `&`/`*` annihilator, zero-left `/`/`%`, and same-identifier `^`/`-`/`%`、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 80 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 87 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 80 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 87 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS；clang build 期间仅出现 unrelated AOT unused-parameter warnings。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Nonzero-Divisor Zero-Left Division Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 00:32 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 product annihilator exact-zero 表达式再推进到受限 zero-left division exact-zero 表达式：当 binary `/` 的左侧已递归证明 exact zero，右侧具备 zero-wrapper/direct int64 range 且区间排除 0 时，把 `zero / nonzero-range` 视作 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 76 扩到 78 cases，新增 `(zero / unit) - (zero - unit)` 与 `(zero / (unit - zero)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 83 扩到 85 cases，新增 `(zero / unit) - (-(zero - unit))` 与 `(zero / (unit - zero)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 增加 binary `/` 的 zero-left/nonzero-divisor 分支，仍要求左侧递归证明 exact zero，右侧通过 zero-wrapper/direct int64 range reader 且 `min > 0 || max < 0`；原同名 `^`/`-`/`%` exact-zero、受限 `&`/`*` exact-zero-left、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 zero-inclusive divisor division、nonzero-left division、right-exact-zero division、general division algebra、general exact-zero predicate beyond restricted `&`/`*` annihilator, zero-left `/`, and same-identifier `^`/`-`/`%`、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 78 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 85 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 78 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 85 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Product Exact-Zero-Left Annihilator Range Publication and LSP Local Query | 已完成 | 2026-07-04 00:19 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从非零同名 modulo exact-zero 表达式再推进到受限 product annihilator exact-zero 表达式：当 `span * zero` 或 `zero * span` 的一侧已递归证明 exact zero，另一侧具备 zero-wrapper/direct int64 range 时，把该 `*` 表达式视作 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 74 扩到 76 cases，新增 `(span * zero) - (zero - unit)` 与 `(zero * span) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 81 扩到 83 cases，新增 `(span * zero) - (-(zero - unit))` 与 `(zero * span) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只把 `type_inference_bitwise_identity_expression_is_exact_zero_value` 中既有受限 annihilator 分支从 binary `&` 扩到 binary `*`，仍要求一侧递归证明 exact zero，另一侧通过 zero-wrapper/direct int64 range reader；原同名 `^`/`-`/`%` exact-zero、受限 `&` exact-zero-left、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 non-exact-zero-left zero-minus、unknown/non-range product operand、two-nonzero-factor product、general multiplication algebra、general exact-zero predicate beyond restricted `&`/`*` annihilator and same-identifier `^`/`-`/`%`、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 76 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 83 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 76 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 83 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Nonzero Same-Identifier Modulo Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-04 00:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从同名差值 `-` 再推进到非零同名 modulo exact-zero 表达式：`unit % unit` 以及 identity-wrapped `unit % (unit - zero)` 可作为 zero-minus 左侧 exact zero，但同一 binding range 必须排除 0。zero-minus RHS parser/LSP focused fixture 从 72 扩到 74 cases，新增 `(unit % unit) - (zero - unit)` 与 `(unit % (unit - zero)) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 79 扩到 81 cases，新增 `(unit % unit) - (-(zero - unit))` 与 `(unit % (unit - zero)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 的同名 exact-zero 分支中允许 binary `%`，复用 identity-wrapped identifier 和 int64 binding range 校验，并新增 modulo 专属 nonzero guard：`min > 0 || max < 0`；原同名 `^`/`-` exact-zero、受限 `&` exact-zero-left、direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 zero-inclusive divisor modulo、different-identifier modulo、non-exact-zero-left zero-minus、general modulo/subtraction algebra、general exact-zero predicate beyond same-identifier `%`/`-` and restricted `&` annihilator、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 74 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 81 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 74 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 81 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Same-Identifier Difference Exact-Zero-Left Range Publication and LSP Local Query | 已完成 | 2026-07-03 23:57 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从受限 bitwise `&` annihilator 再推进到同名差值 exact-zero 表达式：`span - span` 以及 identity-wrapped 同名差值可作为 zero-minus 左侧 exact zero。zero-minus RHS parser/LSP focused fixture 从 70 扩到 72 cases，新增 `(span - span) - (zero - unit)` 与 `((span + zero) - span) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 77 扩到 79 cases，新增 `(span - span) - (-(zero - unit))` 与 `(span - (span - zero)) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 的同名 exact-zero 分支中允许 binary `-` 与既有 binary `^` 共享 `type_inference_bitwise_identity_expressions_are_same_identity_wrapped_identifier` 和 int64 range binding 校验；原 direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|` leaf、受限 `&` exact-zero-left、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 non-same-identifier subtraction、non-exact-zero-left zero-minus、general subtraction algebra、general exact-zero predicate beyond same-identifier `-` and restricted `&` annihilator、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 72 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 79 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 72 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 79 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Bitwise-AND Exact-Zero-Left Annihilator Range Publication and LSP Local Query | 已完成 | 2026-07-03 23:44 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain 的 accepted exact-zero-left 从 zero-wrapper/direct/unary-direct 与 bitwise identity leaf 推进到受限 bitwise `&` annihilator 左表达式：当 `span & zero` 或 `zero & span` 的一侧已递归证明 exact zero，另一侧具备 zero-wrapper/direct int64 range 时，把该 `&` 表达式视作 exact-zero left。zero-minus RHS parser/LSP focused fixture 从 68 扩到 70 cases，新增 `(span & zero) - (zero - unit)` 与 `(zero & span) - (zero - unit)` under `(zero + zero) | ...` 的 `2..3` query；zero-minus unary-RHS parser/LSP focused fixture 从 75 扩到 77 cases，新增 `(span & zero) - (-(zero - unit))` 与 `(zero & span) - (-(zero - unit))` under `(zero + zero) | ...` 的 `-3..-2` query。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 增加受限 `&` 分支，并要求非零侧通过 `type_inference_bitwise_identity_expression_zero_wrapped_direct_int64_range`；原 direct/unary-direct leaf、signed all-ones `&`/`|`、same-identifier `&`/`|`、每层 exact-zero-left、局部 AST RHS 迭代和 `INT64_MIN` 保护保持不变。该切片不扩展 arbitrary bitwise algebra、non-exact-zero-left zero-minus、general exact-zero predicate beyond restricted `&` annihilator、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 70 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 77 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 70 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 77 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Signed All-Ones OR Leaf Range Publication and LSP Local Query | 已完成 | 2026-07-03 23:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain leaf 的既有安全 bitwise identity leaf 从 signed all-ones `&` mask 和 same-identifier `&`/`|` 再推进到 signed all-ones `|` annihilator。zero-minus RHS parser/LSP focused fixture 从 66 扩到 68 cases，新增 `zero - (zero - (allOnes | span))` 与 `zero - (zero - (span | allOnes))` 的 exact all-ones query；zero-minus unary-RHS parser/LSP focused fixture 从 73 扩到 75 cases，新增 `zero - (-(zero - (allOnes | span)))` 与 `zero - (-(zero - (span | allOnes)))` 的 positive-one query。zero-minus RHS signed all-ones `|` leaf group 保留 parser/LSP `-1..-1`，zero-minus unary-RHS signed all-ones `|` leaf group 保留 parser/LSP `1..1`。实现只在私有 `type_inference_bitwise_identity_expression_basic_zero_minus_chain_leaf_int64_range` 中新增 `|` 分支并复用既有 `type_inference_bitwise_identity_expression_signed_all_ones_or_range`；原 direct/unary-direct leaf、signed all-ones `&` mask、same-identifier `&`/`|`、每层 exact-zero-left、局部 AST RHS 迭代和每次取负的 `INT64_MIN` 保护保持不变。该切片不扩展 non-exact-zero-left zero-minus、其他 non-direct leaf、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 68 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`；zero-minus unary-RHS 新 focused parser 75 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧 `0..0`。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 68 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 75 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Basic Zero-Minus Signed All-Ones Mask and Same-Identifier Leaf Range Publication and LSP Local Query | 已完成 | 2026-07-03 23:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 basic zero-minus chain leaf 从 zero-wrapper/direct 或 unary-direct 推进到既有安全 bitwise identity leaf：signed all-ones `&` mask 和 same-identifier `&`/`|`。zero-minus RHS parser/LSP focused fixture 从 64 扩到 66 cases，新增 `zero - (zero - (allOnes & (span + zero)))` 与 `zero - (zero - (span | (span - zero)))` 的 zero-identity query；zero-minus unary-RHS parser/LSP focused fixture 从 71 扩到 73 cases，新增 `zero - (-(zero - (allOnes & (span + zero))))` 与 `zero - (-(zero - (span | (span - zero))))` 的 zero-identity query。zero-minus RHS leaf group 保留 parser `-2..3` / LSP `-128..127`，zero-minus unary-RHS leaf group 保留 parser `-3..2` / LSP `-127..128`。实现新增私有 `type_inference_bitwise_identity_expression_basic_zero_minus_chain_leaf_int64_range`，只供 `type_inference_bitwise_identity_expression_basic_zero_minus_chain_direct_int64_range` 的 leaf 判定使用；原 direct/unary-direct leaf、每层 exact-zero-left、局部 AST RHS 迭代和每次取负的 `INT64_MIN` 保护保持不变。该切片不扩展 signed all-ones `|` leaf、non-exact-zero-left zero-minus、non-direct leaf beyond signed all-ones `&` mask and same-identifier `&`/`|`、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 66 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧范围；zero-minus unary-RHS 新 focused parser 73 tests 中 2 failures，focused LSP mirror 同 2 个 query 返回旧范围。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 66 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 73 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Sextuple Nested Basic Zero-Minus Direct/Unary Direct-Range Iterative Chain Reader and LSP Local Query | 已完成 | 2026-07-03 22:52 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 22:39 的固定 `ZR_BITWISE_IDENTITY_ZERO_MINUS_CHAIN_MAX_DEPTH=5` 局部 reader 改为沿 AST RHS 迭代到 zero-wrapper/direct 或 unary-direct leaf 的本地 exact-zero-left zero-minus chain reader，并用 sextuple nested basic zero-minus 覆盖验证不再只是把上限从 5 堆到 6。zero-minus RHS parser/LSP focused fixture 从 60 扩到 64 cases，新增 `zero - (zero - (zero - (zero - (zero - (zero - (zero - direct-range))))))` 与 `zero - (zero - (zero - (zero - (zero - (zero - (zero - (-direct-range)))))))` 的 unit/sign-crossing query；zero-minus unary-RHS parser/LSP focused fixture 从 67 扩到 71 cases，新增 `zero - (-(zero - (zero - (zero - (zero - (zero - (zero - direct-range)))))))` 与 `zero - (-(zero - (zero - (zero - (zero - (zero - (zero - (-direct-range))))))))` 的 unit/sign-crossing query。zero-minus RHS sextuple direct group 保留 parser `-3..-2`、sign-crossing `-3..2` / LSP `-127..128`，sextuple unary group 保留 parser `2..3`、sign-crossing `-2..3` / LSP `-128..127`；zero-minus unary-RHS sextuple direct group 保留 parser `2..3`、sign-crossing `-2..3` / LSP `-128..127`，sextuple unary group 保留 parser `-3..-2`、sign-crossing `-3..2` / LSP `-127..128`。实现保留 `type_inference_bitwise_identity_negate_range_in_place` 与 `type_inference_bitwise_identity_expression_basic_zero_minus_chain_direct_int64_range`，移除固定深度宏，仍只供 zero-minus RHS reader 和 zero-minus RHS unary-minus operand reader 消费；每层仍要求 exact-zero-left，leaf 仍必须是 zero-wrapper/direct 或 unary-direct，每次取负仍保留 `INT64_MIN` 保护。该切片不扩展 non-exact-zero-left zero-minus、non-direct/unary-direct leaf、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 64 tests 中 4 failures，focused LSP mirror 同 4 个 query 返回旧范围；zero-minus unary-RHS 新 focused parser 71 tests 中 4 failures，focused LSP mirror 同 4 个 query 返回旧范围。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 64 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 71 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Quintuple Nested Basic Zero-Minus Direct/Unary Direct-Range Chain Reader and LSP Local Query | 已完成 | 2026-07-03 22:39 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left zero-minus chain operand 从受限 quadruple nested basic zero-minus 推进到受限 quintuple nested basic zero-minus，并把手写逐层 helper 收敛为单个有上限的局部 chain reader。zero-minus RHS parser/LSP focused fixture 从 56 扩到 60 cases，新增 `zero - (zero - (zero - (zero - (zero - (zero - direct-range)))))` 与 `zero - (zero - (zero - (zero - (zero - (zero - (-direct-range))))))` 的 unit/sign-crossing query；zero-minus unary-RHS parser/LSP focused fixture 从 63 扩到 67 cases，新增 `zero - (-(zero - (zero - (zero - (zero - (zero - direct-range))))))` 与 `zero - (-(zero - (zero - (zero - (zero - (zero - (-direct-range)))))))` 的 unit/sign-crossing query。zero-minus RHS quintuple direct group 保留 parser `2..3`、sign-crossing `-2..3` / LSP `-128..127`，quintuple unary group 保留 parser `-3..-2`、sign-crossing `-3..2` / LSP `-127..128`；zero-minus unary-RHS quintuple direct group 保留 parser `-3..-2`、sign-crossing `-3..2` / LSP `-127..128`，quintuple unary group 保留 parser `2..3`、sign-crossing `-2..3` / LSP `-128..127`。实现新增 `ZR_BITWISE_IDENTITY_ZERO_MINUS_CHAIN_MAX_DEPTH=5`、`type_inference_bitwise_identity_negate_range_in_place`、`type_inference_bitwise_identity_expression_basic_zero_minus_chain_direct_int64_range` 与 `type_inference_bitwise_identity_expression_zero_wrapped_or_basic_zero_minus_chain_direct_int64_range`，只供 zero-minus RHS reader 和 zero-minus RHS unary-minus operand reader 消费；仍要求每层 exact-zero-left、zero-wrapper/direct/unary-direct leaf，并保留每次取负的 `INT64_MIN` 保护。该切片不扩展六层及以上 nested basic RHS chain、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 60 tests 中 4 failures，focused LSP mirror 同 4 个 query 返回旧范围；zero-minus unary-RHS 新 focused parser 67 tests 中 4 failures，focused LSP mirror 同 4 个 query 返回旧范围。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 60 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 67 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Quadruple Nested Basic Zero-Minus Direct/Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 22:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left zero-minus chain operand 再推进一层到受限 quadruple nested basic zero-minus：zero-minus RHS parser/LSP focused fixture 从 42 扩到 56 cases，新增 `zero - (zero - (zero - (zero - (zero - direct-range))))` 与 `zero - (zero - (zero - (zero - (zero - (-direct-range)))))` 两组各 7 个 query；zero-minus unary-RHS parser/LSP focused fixture 从 49 扩到 63 cases，新增 `zero - (-(zero - (zero - (zero - (zero - direct-range)))))` 与 `zero - (-(zero - (zero - (zero - (zero - (-direct-range))))))` 两组各 7 个 query。zero-minus RHS quadruple direct group 保留 parser `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`，quadruple unary group 保留 parser `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`；LSP 侧对应 `span=seed-128` 保留 quadruple direct `-127..128`、quadruple unary `-128..127`。zero-minus unary-RHS quadruple direct group 保留 parser `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`，quadruple unary group 保留 parser `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`；LSP 侧对应 quadruple direct `-128..127`、quadruple unary `-127..128`。实现新增受限 `basic_zero_minus_with_triple_nested_basic_rhs_direct_int64_range` 与统一 `zero_wrapped_or_basic_zero_minus_or_quadruple_nested_basic_zero_minus_direct_int64_range` operand reader，只供 zero-minus RHS reader 和 zero-minus RHS unary-minus operand reader 消费；仍要求每层 exact-zero-left、保留 `INT64_MIN` 取负保护。该切片不扩展五层及以上 nested basic RHS helper、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 56 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；zero-minus unary-RHS 新 focused parser 63 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；四个 quadruple nested OR all-ones annihilator cases 在生产改动前 discovery/GREEN。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 56 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 63 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS and Unary-Minus RHS Triple Nested Basic Zero-Minus Direct/Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 22:03 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left zero-minus chain operand 再推进一层到受限 triple nested basic zero-minus：zero-minus RHS parser/LSP focused fixture 从 28 扩到 42 cases，新增 `zero - (zero - (zero - (zero - direct-range)))` 与 `zero - (zero - (zero - (zero - (-direct-range))))` 两组各 7 个 query；zero-minus unary-RHS parser/LSP focused fixture 从 35 扩到 49 cases，新增 `zero - (-(zero - (zero - (zero - direct-range))))` 与 `zero - (-(zero - (zero - (zero - (-direct-range)))))` 两组各 7 个 query。zero-minus RHS triple direct group 保留 parser `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`，triple unary group 保留 parser `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`；LSP 侧对应 `span=seed-128` 保留 triple direct `-128..127`、triple unary `-127..128`。zero-minus unary-RHS triple direct group 保留 parser `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`，triple unary group 保留 parser `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`；LSP 侧对应 triple direct `-127..128`、triple unary `-128..127`。实现新增受限 `basic_zero_minus_with_nested_basic_rhs_direct_int64_range` 与统一 `zero_wrapped_or_basic_zero_minus_or_nested_basic_zero_minus_direct_int64_range` operand reader，只供 zero-minus RHS reader 和 zero-minus RHS unary-minus operand reader 消费；仍要求每层 exact-zero-left、保留 `INT64_MIN` 取负保护。该切片不扩展五层及以上 zero-minus chain、`zero - (-(zero - (zero - (zero - (zero - unit)))))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc zero-minus RHS 新 focused parser 42 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；zero-minus unary-RHS 新 focused parser 49 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；四个 triple nested OR all-ones annihilator cases 在生产改动前 discovery/GREEN。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，zero-minus RHS parser 42 tests/0 failures + LSP PASS，zero-minus unary-RHS parser 49 tests/0 failures + LSP PASS，zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Unary-Minus RHS Nested Basic Zero-Minus Direct/Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 21:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus RHS unary-minus operand reader 再推进到受限 nested basic zero-minus：zero-minus unary-RHS parser/LSP focused fixture 扩到 35 cases，新增 `zero - (-(zero - (zero - direct-range)))` 与 `zero - (-(zero - (zero - (-direct-range))))` 两组各 7 个 query。parser 侧 nested direct group 保留 `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`；nested unary group 保留 `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`。LSP 侧 `span=seed-128` 对应保留 nested direct `-128..127`、nested unary `-127..128`。实现新增受限 zero-wrapper/basic/basic-with-basic operand reader，并只把它用于 `unary_minus_zero_wrapped_or_basic_zero_minus_direct_int64_range`；仍要求 exact-zero-left、保留 `INT64_MIN` 取负保护。该切片不扩展 `zero - (-(zero - (zero - (zero - unit))))`、`zero - (zero - (zero - (zero - unit)))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 35 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；两个 nested OR all-ones annihilator cases 在生产改动前 discovery/GREEN。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，新 zero-minus unary-RHS parser 35 tests/0 failures + LSP PASS；同组还通过 zero-minus RHS parser 28 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS Nested Basic Zero-Minus Direct/Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 21:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 21:15 的 `zero - (zero - (-direct-range))` 再推进到受限 nested basic zero-minus RHS：同一个 parser/LSP focused fixture 扩到 28 cases，新增 `zero - (zero - (zero - direct-range))` 与 `zero - (zero - (zero - (-direct-range)))` 两组各 7 个 query。parser 侧覆盖 nested direct forms 保留 `-3..-2`、exact `0..0`、sign-crossing `-3..2`、OR all-ones `-1..-1`；nested unary forms 保留 `2..3`、exact `0..0`、sign-crossing `-2..3`、OR all-ones `-1..-1`。LSP 侧 `span=seed-128` 对应保留 nested direct `-127..128`、nested unary `-128..127`。实现新增受限 `basic_zero_minus_with_basic_rhs_direct_int64_range` 路径，让 zero-minus RHS reader 可接受一层 basic zero-minus whose RHS is another bounded basic zero-minus direct/unary-direct operand；仍要求每层 exact-zero-left，并保留 `INT64_MIN` 取负保护。该切片不扩展 `zero - (zero - (zero - (zero - unit)))`、`zero - (zero - (-(zero - (zero - unit))))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 28 tests 中 12 failures，focused LSP mirror 同 12 个 query 返回旧范围；两个 nested OR all-ones annihilator cases 在生产改动前 discovery/GREEN。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，新 parser 28 tests/0 failures + 新 LSP PASS；同组还通过前序 zero-minus unary-RHS parser 21 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS Basic Zero-Minus Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 21:15 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 21:01 的 exact-zero-left `zero - (zero - direct-range)` RHS reader 再推进到一层受限 basic zero-minus whose RHS is unary `-` over direct/zero-wrapper direct：parser/LSP focused fixture 扩到 14 cases，新增覆盖 `(zero + zero) | (zero - (zero - (-unit)))`、`(zero - zero) ^ (zero - (zero - (-(unit + zero))))` 保留 `-3..-2`，`(zero - (zero - (-(unit - zero)))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (zero - (zero - (-span)))` 保留 parser `-3..2` / LSP `-127..128`，`allOnes & (zero - (zero - (-unit)))` 与 `(zero + zero) | (allOnes & (zero - (zero - (-(unit + zero)))))` 保留 `-3..-2`，以及 `allOnes | (zero - (zero - (-(unit - zero))))` 发布 exact `-1..-1`。实现移除更窄的 zero-wrapped-only basic-zero-minus branch，并让 zero-minus RHS reader 复用受限 `basic_zero_minus_direct_int64_range`；仍要求 exact-zero-left、保留 `INT64_MIN` 取负保护。该切片不扩展 `zero - (zero - (zero - unit))`、`zero - (zero - (-(zero - unit)))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 14 tests 中 6 failures，覆盖新增 `zero - (zero - (-direct-range))` 六个旧范围；focused LSP mirror 在生产改动前已 PASS，作为 discovery/GREEN 记录而非 RED。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，新 parser 14 tests/0 failures + 新 LSP PASS；同组还通过前序 zero-minus unary-RHS parser 21 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus RHS Basic Zero-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 21:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left `zero - direct-range` helper 的 RHS reader 再推进到一层受限 basic zero-minus direct operand：parser 侧新增独立 `test_bitwise_zero_minus_rhs_range_inference.c`，LSP 侧新增独立 `test_lsp_bitwise_zero_minus_rhs_range_query.c`，避免继续扩展既有 321 行 zero-minus unary-RHS fixture。parser 侧 `zero=0..0; unit=2..3; span=-2..3; allOnes=-1..-1` 覆盖 `(zero + zero) | (zero - (zero - unit))`、`(zero - zero) ^ (zero - (zero - (unit + zero)))` 保留 `2..3`，`(zero - (zero - (unit - zero))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (zero - (zero - span))` 保留 `-2..3`，`allOnes & (zero - (zero - unit))` 与 `(zero + zero) | (allOnes & (zero - (zero - (unit + zero))))` 保留 `2..3`，以及 `allOnes | (zero - (zero - (unit - zero)))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `2..3`、`0..0`、`-128..127` 或 `-1..-1`。实现新增受限 basic zero-minus-with-RHS-reader helper，并让 zero-minus RHS reader 在 direct 与 unary-minus 路径之外接受一层 `zero - direct/zero-wrapper direct` operand，保留 exact-zero-left 与 `INT64_MIN` 取负保护。该切片不扩展 `zero - (zero - (-unit))`、`zero - (zero - (zero - unit))`、`zero - (zero - (-(zero - unit)))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 7 tests 中 6 failures，覆盖新增 basic zero-minus RHS operand 六个旧范围；focused LSP 同 6 个 query 返回旧范围。Discovery/GREEN：`allOnes | (zero - (zero - (unit - zero)))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 与 `build-wsl-clang-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，新 parser 7 tests/0 failures + 新 LSP PASS；同组还通过前序 zero-minus unary-RHS parser 21 tests/0 failures + LSP PASS、zero-wrapper parser 92 tests/0 failures + LSP PASS。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Unary-Minus RHS Basic Zero-Minus Unary Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 20:33 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left `zero - direct-range` helper 的 RHS unary-minus operand 路径再推进一层：basic zero-minus reader 的 RHS 从 direct/zero-wrapper direct int64 range 放开到受限 unary `-` over direct/zero-wrapper direct int64 range。parser 侧 `zero=0..0; unit=2..3; span=-2..3; allOnes=-1..-1` 在既有 14 个 zero-minus unary-RHS 覆盖外，新增 `(zero + zero) | (zero - (-(zero - (-unit))))`、`(zero - zero) ^ (zero - (-(zero - (-(unit + zero)))))` 保留 `2..3`，`(zero - (-(zero - (-(unit - zero))))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (zero - (-(zero - (-span))))` 保留 `-2..3`，`allOnes & (zero - (-(zero - (-unit))))` 与 `(zero + zero) | (allOnes & (zero - (-(zero - (-(unit + zero))))))` 保留 `2..3`，以及 `allOnes | (zero - (-(zero - (-(unit - zero)))))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `2..3`、`0..0`、`-128..127` 或 `-1..-1`。实现恢复受限 private unary-minus-over-zero-wrapper direct reader，并把 private basic zero-minus reader 的 RHS 切到 zero-wrapper-or-unary-minus-zero-wrapper direct reader；zero-minus RHS unary-minus 仍只接受 direct/zero-wrapper direct 或一层 basic zero-minus whose RHS is unary-minus-over-direct/zero-wrapper-direct，保留 exact-zero-left 与 `INT64_MIN` 取负保护。该切片不扩展 `zero - (zero - unit)`、`zero - (zero - (-unit))`、`zero - (-(zero - (-(zero - unit))))`、任意递归 zero-minus RHS 链、exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 21 tests 中 6 failures，覆盖新增 basic zero-minus unary-RHS operand 六个旧范围；focused LSP 同 6 个 query 返回旧范围。Discovery/GREEN：`allOnes | (zero - (-(zero - (-(unit - zero)))))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 常规 focused target build/run 通过，新 parser 21 tests/0 failures + 新 LSP PASS，前序 zero-wrapper parser 92 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 同组 focused target build/run 全部通过。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Unary-Minus RHS Basic Zero-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 20:17 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left `zero - direct-range` helper 的 RHS unary-minus operand 从 direct/zero-wrapper direct int64 range 推进到一层受限基础 `zero - direct-range`：parser 侧 `zero=0..0; unit=2..3; span=-2..3; allOnes=-1..-1` 在既有 7 个 zero-minus unary-RHS 覆盖外，新增 `(zero + zero) | (zero - (-(zero - unit)))`、`(zero - zero) ^ (zero - (-(zero - (unit + zero))))` 保留 `-3..-2`，`(zero - (-(zero - (unit - zero)))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (zero - (-(zero - span)))` 保留 `-3..2`，`allOnes & (zero - (-(zero - unit)))` 与 `(zero + zero) | (allOnes & (zero - (-(zero - (unit + zero)))))` 保留 `-3..-2`，以及 `allOnes | (zero - (-(zero - (unit - zero))))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `-3..-2`、`0..0`、`-127..128` 或 `-1..-1`。实现新增 private `type_inference_bitwise_identity_expression_basic_zero_minus_direct_int64_range` 和 zero-wrapper-or-basic-zero-minus reader，让 zero-minus RHS unary-minus 只接受 direct/zero-wrapper direct 或一层基础 zero-minus direct operand；不接受 `zero - (zero - unit)`、`zero - (-(zero - (-unit)))`、任意递归 zero-minus RHS 链，仍保留 exact-zero-left 与 `INT64_MIN` 取负保护。该切片不扩展 exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 14 tests 中 6 failures，覆盖新增基础 zero-minus operand 六个旧范围；focused LSP 同 6 个 query 返回旧范围。Discovery/GREEN：`allOnes | (zero - (-(zero - (unit - zero))))` 在生产改动前已通过。GREEN：gcc 常规 focused build 被共享工作区无关 AOT `backend_aot_c_zrp_metadata_remap.c` 调用/声明不一致阻塞，但 touched direct-range/test objects 已编译；用 recorded Ninja link command 重连 parser shared lib 与 focused executables 后，WSL gcc 新 parser 14 tests/0 failures + 新 LSP PASS，前序 zero-wrapper parser 92 tests/0 failures + LSP PASS。WSL clang 常规 focused target build/run 同组全部通过。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Unary-Minus RHS Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 20:04 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 exact-zero-left `zero - direct-range` helper 的 RHS reader 从 zero-wrapper direct int64 range 推进到受限 unary `-` over zero-wrapper direct int64 range：新增独立 parser/LSP focused targets，避免继续扩展 960 行的 zero-wrapper fixture。parser 侧 `zero=0..0; unit=2..3; span=-2..3; allOnes=-1..-1` 覆盖 `(zero + zero) | (zero - (-unit))`、`(zero - zero) ^ (zero - (-(unit + zero)))` 保留 `2..3`，`(zero - (-(unit - zero))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (zero - (-span))` 保留 `-2..3`，`allOnes & (zero - (-unit))` 与 `(zero + zero) | (allOnes & (zero - (-(unit + zero))))` 保留 `2..3`，以及 `allOnes | (zero - (-(unit - zero)))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `2..3`、`0..0`、`-128..127` 或 `-1..-1`。实现只新增 private `type_inference_bitwise_identity_expression_unary_minus_zero_wrapped_direct_int64_range` 和 `type_inference_bitwise_identity_expression_zero_wrapped_or_unary_minus_zero_wrapped_direct_int64_range`，并把 `type_inference_bitwise_identity_expression_zero_minus_direct_int64_range` 的 RHS reader 切换到该受限 reader；RHS unary-minus 只接受 direct/zero-wrapper direct int64 operand，仍保留 `INT64_MIN` 取负保护。该切片不扩展 exact-zero predicate、`zero - (-(zero - unit))` / zero-minus RHS zero-minus、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 focused parser 7 tests 中 6 failures，覆盖 `(zero + zero) | (zero - (-unit))`、`(zero - zero) ^ (zero - (-(unit + zero)))`、`(zero - (-(unit - zero))) & (+zero)`、`(zero + zero) | (zero - (-span))`、`allOnes & (zero - (-unit))`、`(zero + zero) | (allOnes & (zero - (-(unit + zero))))` 的旧范围；focused LSP 同 6 个 query 返回旧范围。Discovery/GREEN：`allOnes | (zero - (-(unit - zero)))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 常规 focused target build 通过，新 parser 7 tests/0 failures + 新 LSP PASS，前序 zero-wrapper parser 92 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 同组 focused target build/run 全部通过。本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Unary-Minus Zero-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 19:46 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `-` 的 operand 从 zero-wrapper direct int64 range 推进到受限 `zero - direct-range` 形态：parser 侧 `zero=0..0; unit=2..3; span=-2..3; allOnes=-1..-1` 覆盖 `(zero + zero) | (-(zero - unit))`、`(zero - zero) ^ (-(zero - (unit + zero)))` 保留 `2..3`，`(-(zero - (unit - zero))) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (-(zero - span))` 保留 `-2..3`，`allOnes & (-(zero - unit))` 与 `(zero + zero) | (allOnes & (-(zero - (unit + zero))))` 保留 `2..3`，以及 `allOnes | (-(zero - (unit - zero)))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `2..3`、`0..0`、`-128..127` 或 `-1..-1`。实现只新增 private `type_inference_bitwise_identity_expression_zero_wrapped_or_zero_minus_direct_int64_range`，并把 unary-minus direct-range helper 的 operand reader 切换到该受限 reader；`zero-minus` helper 仍要求 binary `-` 左侧 proven exact zero、右侧经 zero-wrapper direct int64 reader 后有效且不触发 `INT64_MIN` 取负保护。该切片不扩展 exact-zero predicate、nonzero unary-minus exact-zero、`unit - nonzero`、nonzero RHS subtract wrapper、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 92 tests 中 6 failures，覆盖 `(zero + zero) | (-(zero - unit))`、`(zero - zero) ^ (-(zero - (unit + zero)))`、`(-(zero - (unit - zero))) & (+zero)`、`(zero + zero) | (-(zero - span))`、`allOnes & (-(zero - unit))`、`(zero + zero) | (allOnes & (-(zero - (unit + zero))))` 的旧范围；focused LSP 同 6 个 query 返回旧范围。Discovery/GREEN：`allOnes | (-(zero - (unit - zero)))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 用 fresh bitwise helper/test objects 手动重连 focused parser/LSP 后通过 parser 92 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 同样通过 parser 92 tests/0 failures + LSP PASS。常规 target rebuild 当前被共享工作区无关 AOT `backend_aot_c_zrp_metadata_remap*` 头/实现签名不一致阻塞；本切片不声明 MSVC/full repository GREEN。 |
| 阶段 2 / Numeric Bitwise Zero-Minus Direct-Range Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 19:27 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary bitwise zero-identity 与 signed all-ones helper 推进到受限 `zero - direct-range` 形态：parser 侧 `zero=0..0; unit=2..3; allOnes=-1..-1` 覆盖 `(zero + zero) | (zero - unit)`、`(zero - zero) ^ (zero - (unit + zero))` 保留 `-3..-2`，`((+zero) - (unit - zero)) & (+zero)` 发布 exact `0..0`，`allOnes & (zero - unit)` 与 `(zero + zero) | (allOnes & (zero - (unit + zero)))` 保留 `-3..-2`，以及 `allOnes | (zero - (unit - zero))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2` 保留同形 query 的 `-3..-2`、`0..0` 或 `-1..-1`。实现新增 `type_inference_bitwise_identity_expression_zero_minus_direct_int64_range`，只接受 binary `-` 左侧已证明 exact zero、右侧经现有 zero-wrapper direct int64 reader 后有有效范围，并复用 `INT64_MIN` 取负保护；该 helper 只接入 supported zero-identity `&`/`|`/`^` range consumer 与 signed all-ones direct-range reader，不扩展 exact-zero predicate、nonzero RHS subtract wrapper、`unit - nonzero`、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 85 tests 中 5 failures，覆盖 `(zero + zero) | (zero - unit)`、`(zero - zero) ^ (zero - (unit + zero))`、`((+zero) - (unit - zero)) & (+zero)`、`allOnes & (zero - unit)`、`(zero + zero) | (allOnes & (zero - (unit + zero)))` 旧范围；focused LSP 同 5 个 query 返回旧范围。Discovery/GREEN：`allOnes | (zero - (unit - zero))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 通过 focused parser 85 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 通过 focused parser 85 tests/0 failures + LSP PASS。该 focused target 保留并通过前序 ordinary bitwise zero-wrapper / unary-minus / signed all-ones 相邻覆盖；本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Unary-Minus Zero-Identity Wrapper Operand Range Publication and LSP Local Query | 已完成 | 2026-07-03 19:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 unary `-` 的 operand 从 direct int64 range 推进到 proven exact-zero identity wrapper direct int64 range：parser 侧 `zero=0..0; unit=2..3; allOnes=-1..-1` 覆盖 `(zero + zero) | (-(unit + zero))`、`(zero - zero) ^ (-(unit - zero))`、`(-(zero + unit)) | (zero + zero)` 保留 `-3..-2`，`allOnes & (-(unit + zero))` 与 `(zero + zero) | (allOnes & (-(unit - zero)))` 保留 `-3..-2`，以及 `allOnes | (-(zero + unit))` 发布 exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2` 保留同形 query 的 `-3..-2` 或 `-1..-1`。实现只把 `type_inference_bitwise_identity_expression_unary_minus_direct_int64_range` 的 operand reader 从 ordinary direct int64 range 切换为现有 `type_inference_bitwise_identity_expression_zero_wrapped_direct_int64_range`，继续复用 `INT64_MIN` 取负保护，并只经由已有 supported zero-identity `&`/`|`/`^` 和 signed all-ones mask/OR helper 消费；不扩展 exact-zero predicate、nonzero unary-minus exact-zero、`zero - unit` / `zero - span`、nonzero RHS subtract wrappers、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 79 tests 中 4 failures，分别覆盖 `(zero + zero) | (-(unit + zero))`、`(zero - zero) ^ (-(unit - zero))`、`allOnes & (-(unit + zero))`、`(zero + zero) | (allOnes & (-(unit - zero)))` 旧范围；focused LSP 同 4 个 query 返回旧范围。Discovery/GREEN：`(-(zero + unit)) | (zero + zero)` 与 `allOnes | (-(zero + unit))` 在生产改动前已通过。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 通过 focused parser 79 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 通过 focused parser 79 tests/0 failures + LSP PASS。该 focused target 保留并通过前序 ordinary bitwise zero-wrapper / unary-minus direct-range / signed all-ones 相邻覆盖；本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Unary-Minus Direct-Range Signed All-Ones Mask/OR Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 18:58 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 ordinary bitwise signed all-ones mask identity 与 OR annihilator 推进到 unary `-` over direct int64 range operand：parser 侧 `allOnes=-1..-1; zero=0..0; unit=2..3` 覆盖 `allOnes & (-unit)` 保留 `-3..-2`，`((-unit) & allOnes) & (+zero)` 发布 exact `0..0`，`(zero + zero) | (allOnes & (-unit))` 保留 `-3..-2`，以及 `allOnes | (-unit)`、`((allOnes | (-unit)) & (+zero))`、`(zero + zero) | ((-unit) | allOnes)` 分别发布 exact `-1..-1`、exact `0..0`、exact `-1..-1`；LSP 侧 `unit=(seed % 2)+2` 保留同形 query 的 `-3..-2`、`0..0` 或 `-1..-1`。实现只在 direct-range helper 模块内新增 signed all-ones 专用的 zero-wrapper-or-unary-minus direct int64 range reader，并仅供 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range` 与 `type_inference_bitwise_identity_expression_signed_all_ones_or_range` 消费；普通 direct int64 range helper、exact-zero predicate、nonzero identity wrappers、`zero - unit` / `zero - span`、nonzero RHS subtract wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 与 CFG-wide loop dataflow 均不扩展。 | TDD RED：WSL gcc focused parser 最终 73 tests 中 5 failures，覆盖 3 个 signed all-ones mask unary-minus operand 旧范围和 2 个 OR annihilator nested consumer 旧范围；focused LSP 同 5 个 query 返回旧范围。GREEN：`build-wsl-gcc-lsp-bitwise-unary-minus` 通过 focused parser 73 tests/0 failures + LSP PASS；`build-wsl-clang-lsp-bitwise-unary-minus` 通过 focused parser 73 tests/0 failures + LSP PASS。该 focused target 保留并通过前序 ordinary bitwise zero-wrapper / signed all-ones 相邻覆盖；本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Unary-Minus Direct-Range Zero-Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 18:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise zero-identity range consumer 推进到 unary `-` over direct int64 range：parser 侧 `zero=0..0; unit=2..3` 覆盖 `(-unit) & (+zero)` 发布 exact `0..0`，以及 `(zero + zero) | (-unit)`、`(zero - zero) ^ (-unit)` 保留 `-3..-2`；LSP 侧 `unit=(seed % 2)+2` 保留同形 query 的 `0..0` 或 `-3..-2`。实现先把 oversized `type_inference_bitwise_identity.c` 的 direct-range consumer helper 拆到 `type_inference_bitwise_identity_direct_range.c/.h`，再新增 `type_inference_bitwise_identity_expression_unary_minus_direct_int64_range`，仅在 supported zero-identity `&`/`|`/`^` consumer 内消费 unary `-` 的 direct int64 operand，并拒绝 `INT64_MIN` 取负溢出；不扩展 exact-zero predicate、nonzero unary-minus exact-zero、`zero - span`、nonzero RHS subtract wrappers、nonzero identity wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。拆分后 `type_inference_bitwise_identity.c` 约 702 行，新 direct-range `.c` 约 341 行、`.h` 约 66 行。 | TDD RED：WSL gcc focused parser 67 tests 中 3 failures，分别是 `(-unit) & (+zero)` 旧范围 `-3..-2`、`(zero + zero) | (-unit)` 旧范围 `0..0`、`(zero - zero) ^ (-unit)` 旧范围 `0..0`；focused LSP 同 3 个 query 返回旧范围。GREEN：隔离 `build-wsl-gcc-lsp-bitwise-unary-minus` 通过 focused parser 67 tests/0 failures + LSP PASS；隔离 `build-wsl-clang-lsp-bitwise-unary-minus` 通过 focused parser 67 tests/0 failures + LSP PASS。该 focused target 保留并通过前序 ordinary bitwise zero-wrapper 相邻覆盖；本切片未运行 MSVC/full repository matrix，full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Nested Exact-Zero Zero-Identity Range Publication and LSP Local Query | 已完成 | 2026-07-03 06:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero range consumer 推进到 nested zero-identity range publication：parser 侧 `zero=0..0; span=-2..3` 覆盖 `((-zero) | span) & (+zero)`、`(zero + zero) | (span & (-zero))`、`(zero - zero) ^ ((-zero) | span)`、`((span ^ (-zero)) & (+zero)) | (zero + zero)`，分别发布 exact `0..0`、exact `0..0`、`-2..3`、exact `0..0`；LSP 侧 `span=seed-128` 保留同形 query 的 exact `0..0` 或 `-128..127`。实现只在 `type_inference_bitwise_identity_expression_supported_zero_identity_range` 中递归消费 nested `&` exact-zero annihilator 与 `|`/`^` exact-zero identity，且另一侧必须已由当前 supported zero-identity range consumer 证明；不改 exact-zero predicate、supported nonnegative publisher、direct signed range helper、all-ones helpers、nonzero identity wrappers、nonzero unary minus、`zero - span`、nonzero RHS subtract wrappers、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。`type_inference_bitwise_identity.c` 最终 1000 行；本切片未新增 helper/subsystem，仍保持同一职责，后续若继续扩展 nested bitwise algebra 应先拆出 coherent consumer 模块。 | TDD RED：WSL gcc 新 parser 64 tests 中 3 failures，集中在 `((-zero) | span) & (+zero)` 旧范围 `-2..3`、`(zero - zero) ^ ((-zero) | span)` 旧范围 `0..0`、`((span ^ (-zero)) & (+zero)) | (zero + zero)` 旧范围 `-2..3`；`(zero + zero) | (span & (-zero))` 为既有 discovery/GREEN 覆盖。GREEN：当前最终代码 WSL gcc 9-target matrix 通过，包含 bitwise focused parser 64 tests/0 failures + LSP PASS、numeric-range parser 10 tests/0 failures、bitwise-offset parser 10 tests/0 failures + LSP PASS、idempotent-wrapper parser 8 tests/0 failures + LSP PASS、same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。当前最终代码 WSL clang focused parser 64 tests/0 failures + LSP PASS；clang adjacent 重建被并行 AOT 脏改动阻塞，错误集中在 `backend_aot_c_zrp_metadata_type_spec*` 相关函数声明/调用参数数目不一致。MSVC Debug 在最终行数收紧前的同一 9-target matrix 已通过，`VSCMD_VER=17.14.34`；当前最终代码的 MSVC 重建同样被该 AOT signature mismatch 阻塞，失败发生在 AOT metadata remap 源文件，且触碰的 bitwise helper 已进入 MSVC 编译阶段。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Exact-Zero Unary-Minus Wrapper Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 05:56 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero 判定推进到 unary `-` exact-zero wrapper：parser 侧 `zero=0..0; unit=2..3; span=-2..3` 覆盖 `(-zero) & unit`、`span & (-zero)` 发布 `0..0`，以及 `(-zero) | span`、`span ^ (-zero)`、`(-(zero + zero)) | span` 保留 `-2..3`；LSP 侧 `zero=0`、`unit=(seed % 2)+2`、`span=seed-128` 保留同形 query 的 `0..0` 或 `-128..127`。实现只在 `type_inference_bitwise_identity_expression_is_exact_zero_value` 中接受 unary `-`，且仅当 operand 已经递归证明 exact `[0,0]`；不扩展 supported nonnegative publisher、direct signed range helper、nested exact-zero range publication beyond existing consumers、nonzero unary minus、`zero - span`、nonzero RHS subtract wrappers、nonzero identity wrappers、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。触碰的 `type_inference_bitwise_identity.c` 仍为单一职责文件，最终 957 行；本切片未新增 helper/subsystem，未做模块拆分。 | TDD RED：WSL gcc 初始 parser 运行包含一个后续收窄掉的 nested candidate，共 61 tests/3 failures；失败集中在最终保留的 `span & (-zero)`、`(-zero) | span`、`(-(zero + zero)) | span`，focused LSP 同三类 query 返回旧范围；`(-zero) & unit` 与 `span ^ (-zero)` 为既有 discovery/GREEN 覆盖。GREEN：WSL gcc final focused parser 60 tests/0 failures + LSP PASS；同一 9-target matrix 在 WSL gcc、WSL clang、Windows MSVC Debug 均通过，包含 numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。WSL clang 第一次与 gcc 并行构建时出现 parser symbols link 中间状态，串行重建/执行通过；MSVC 使用 `VSCMD_VER=17.14.34` 与 `build-msvc-lsp-bitwise-zero`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Signed All-Ones OR Annihilator Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 05:27 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise signed all-ones OR annihilator 推进到 exact `-1..-1` 发布：parser 侧 `allOnes=-1..-1; zero=0..0; span=-2..3` 覆盖 `allOnes | span`、`span | allOnes`、`(allOnes + zero) | span`、`((allOnes | span) & (+zero))`、`(zero + zero) | (span | (zero + allOnes))`，其中 OR annihilator 结果发布 `-1..-1`，exact-zero top-level consumer 发布 `0..0`；LSP 侧 `allOnes=0-1`、`span=seed-128` 保留同样的 exact `-1..-1` / `0..0` 查询结果。同步保留 discovery/GREEN 覆盖：direct signed same-identifier `span & span`、`(span + zero) & span`、`span | span`、`span | (span - zero)` 在当前实现中已直接发布操作数范围。新增 `type_inference_bitwise_identity_expression_signed_all_ones_or_range`，仅在 binary `|` 中、经过已支持 exact-zero wrapper 归一化后，一侧 direct int64 exact `-1..-1` 时发布 `-1..-1`，并接入普通 bitwise range publication 与 nested supported-zero-identity consumer；仍不扩展非 `-1` 负 OR mask、signed partial masks、`^` complement / bitwise NOT、nonzero identity wrappers、nonzero RHS subtract wrappers、unary `-`、`zero - span`、different-identifier structural equality、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：direct same-identifier parser 50 tests/0 failures + LSP PASS 在生产改动前已通过。TDD RED：WSL gcc parser 55 tests 中 3 failures，LSP 同 3 个 query 返回旧范围，集中在 `span | allOnes`、`((allOnes | span) & (+zero))`、`(zero + zero) | (span | (zero + allOnes))`；`allOnes | span` 与 `(allOnes + zero) | span` 作为 left all-ones/wrapper coverage 先已通过。GREEN：WSL gcc focused parser 55 tests/0 failures + LSP PASS；同一 9-target matrix 在 WSL gcc、WSL clang、Windows MSVC Debug 均通过，包含 numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS；WSL gcc/clang build 命令在目标产出后超时，但确认无残留构建进程且直接执行新鲜目标全部通过；MSVC 使用 `VSCMD_VER=17.14.34` 与 `build-msvc-lsp-bitwise-zero`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Signed All-Ones Mask Zero-Identity Wrapper Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 04:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise signed all-ones mask identity 从 direct `-1` mask 推进到 mask 与 masked operand 上的 exact-zero identity wrapper：parser 侧 `allOnes=-1..-1; zero=0..0; span=-2..3` 覆盖 `(allOnes + zero) & span`、`span & (allOnes - zero)`、`allOnes & (span + zero)`、`(zero + zero) | (allOnes & (zero + span))`、`(zero - zero) ^ ((span - zero) & allOnes)` 等保持 `-2..3`，以及 `((zero + allOnes) & span) & (+zero)`、`((span - zero) & allOnes) & (+zero)` 发布 `0..0`；LSP 侧 `allOnes=0-1`、`span=seed-128` 保留 `-128..127` 或 exact `0..0`。新增 `type_inference_bitwise_identity_expression_zero_wrapped_direct_int64_range`，只供 `type_inference_bitwise_identity_expression_signed_all_ones_mask_range` 在 binary `&` 的 exact `-1..-1` all-ones mask identity 中剥离已证明 exact-zero 的 `+`/`-` wrapper；普通 direct int64 range helper 仍保持 unary-plus-only，避免全局扩大 direct signed range consumption。仍不扩展非 `-1` 负掩码、signed partial masks、mask/shift broadening、different-identifier structural equality、nonzero identity wrappers、nonzero RHS subtract wrappers、unary `-`、`zero - span`、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc parser 46 tests 中 8 failures，LSP 8 个 wrapper query 返回旧范围；`span & (allOnes - zero)` 作为既有 right-mask direct wrapper discovery 已先通过。GREEN：WSL gcc focused parser 46 tests/0 failures + LSP PASS；同一 9-target matrix 在 WSL gcc、WSL clang、Windows MSVC Debug 均通过，包含 numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS；MSVC 使用 `VSCMD_VER=17.14.34` 与 `build-msvc-lsp-bitwise-zero`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Signed All-Ones Mask Identity Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 04:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise identity range fact 从 signed same-identifier exact-zero/nested identity 推进到 exact signed all-ones mask identity：parser 侧 `allOnes=-1..-1; span=-2..3` 覆盖 `allOnes & span` 保留 `-2..3`，`(allOnes & span) & (+zero)` 与 `(span & allOnes) & (+zero)` 发布 `0..0`，以及 `(zero + zero) | (allOnes & span)`、`(zero - zero) ^ (allOnes & span)`、`(zero + zero) | (span & allOnes)`、`(zero - zero) ^ (span & allOnes)` 保留 `-2..3`；LSP 侧 `allOnes=0-1`、`span=seed-128` 保留 `-128..127` 或 exact `0..0`。`type_inference_bitwise_identity_expression_signed_all_ones_mask_range` 只在 binary `&` 中接受一侧 direct int64 exact `-1..-1`、另一侧 valid direct int64 range，并让 `type_inference_apply_bitwise_identity_range` 与 top-level exact-zero/zero-identity consumer 共用该范围；不扩展 nonnegative publisher、非 `-1` 负掩码、signed partial masks、mask/shift、different-identifier structural equality、nonzero identity wrappers、unary `-`、`zero - span`、nonzero RHS subtract wrappers、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 初始新增 left-mask cases 使 parser 34 tests 中 4 failures，LSP 同 4 个 query 返回旧范围；随后临时收窄为 left-mask-only 后新增 right-mask outer-consumer cases，parser 37 tests 中 3 failures，LSP 同 3 个 query 返回旧范围。GREEN：WSL gcc focused parser 37 tests/0 failures + LSP PASS；WSL clang focused parser 37 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 37 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`, `build-msvc-lsp-bitwise-zero`)。相邻回归在 WSL gcc、WSL clang、Windows MSVC Debug 均通过：numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Exact-Zero Signed Same-Identifier Nested XOR Exact-Zero Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 03:46 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero wrapper range fact 从 nested signed same-identifier `&`/`|` identity expression 推进到 nested signed same-identifier `^` exact-zero expression：parser `span=-2..3` 覆盖 `(span ^ span) & span`、`span & (span ^ (span + zero))`、`(zero + zero) | (span ^ (span - zero))`、`(span ^ span) ^ (span ^ (span + zero))` 发布 `0..0`，以及 `(span ^ (span - zero)) | span`、`span ^ ((span + zero) ^ span)` 保留 `-2..3`；LSP 侧 `span = seed - 128` 保留 `-128..127` 或 exact `0..0`。`type_inference_bitwise_identity_expression_is_exact_zero_value` 现在仅对 same-identifier `^` 在 zero-identity wrapper 归一化后接受 valid direct int64 binding 作为 exact-zero operand，让外层 exact-zero `&`/`|`/`^` 分支消费；不扩展 supported nonnegative publisher、mask/shift、signed same-identifier `&`/`|` range helper、different-identifier structural equality 或 general signed bitwise algebra。仍不声明 unary `-`、`zero - span`、nonzero RHS subtract wrappers、nonzero identity wrappers、different identifiers、signed mask range、general signed bitwise algebra、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser 30 tests 中新增 2 failures；LSP 同两类 nested XOR exact-zero query 返回旧范围，其余新增 discovery cases 在既有实现中已通过。GREEN：WSL gcc focused parser 30 tests/0 failures + LSP PASS；WSL clang focused parser 30 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 30 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`)。第一次 MSVC 共享 `build-msvc` 构建被并发进程锁住 `ZERO_CHECK.tlog`，未作为代码失败；随后用独立 `build-msvc-lsp-bitwise-zero` 目录完成验证。相邻回归：WSL gcc numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Exact-Zero Signed Same-Identifier Nested Identity Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 03:20 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero wrapper range fact 从 direct sign-crossing operand 推进到 exact-zero `&`/`|`/`^` 顶层分支可消费的 nested signed same-identifier `&`/`|` identity expression：parser `span=-2..3` 覆盖 `(span & span) & (+zero)`、`((span + zero) & span) & (+zero)`、`(span | (span - zero)) & (+zero)` 发布 `0..0`，以及 `(zero + zero) | (span & span)`、`(+zero) | ((span + zero) & span)`、`(zero - zero) ^ (span | (span - zero))` 保留 `-2..3`；LSP 侧 `span = seed - 128` 保留 `-128..127` 或 exact `0..0`，并新增 query helper offset 以稳定定位 nested 表达式里的外层 bitwise operator。`type_inference_bitwise_identity_expression_supported_zero_identity_range` 现在只在顶层 exact-zero identity 分支额外接受 direct int64 range 与 signed same-identifier `&`/`|` range；递归 nonnegative publisher、mask/shift、top-level general signed same-id algebra、nested arbitrary signed algebra 仍不扩展。仍不声明 unary `-`、`zero - unit`、nonzero RHS subtract wrappers、nonzero identity wrappers、signed mask range、general signed bitwise algebra、nested signed identity publication beyond same-identifier `&`/`|` consumed by top-level exact-zero identity、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser 24 tests 中新增 6 failures；LSP RED 暴露同组 nested exact-zero query 旧范围，两个 nested `&` query 的 outer-operator 定位在 GREEN 前用 helper offset 校正。GREEN：WSL gcc focused parser 24 tests/0 failures + LSP PASS；WSL clang focused parser 24 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 24 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`)。相邻回归：WSL gcc numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Exact-Zero Sign-Crossing Operand Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 02:45 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero wrapper range fact 从非负操作数推进到 direct int64 sign-crossing 操作数：扩展 parser target `zr_vm_bitwise_zero_wrapper_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_wrapper_range_query_test`，覆盖 `span=-2..3` 下 `span & (+zero)`、`span & (zero + zero)`、`span & (zero - zero)` 发布 `0..0`，以及 `(+zero) | span`、`(zero + zero) | span`、`(zero - zero) | span`、`(+zero) ^ span`、`(zero + zero) ^ span`、`(zero - zero) ^ span` 保留 `-2..3`；LSP 侧 `span = seed - 128` 保留 `-128..127`。`type_inference_bitwise_identity.c` 新增 exact-zero identity 专用的 direct int64 range helper，并仅把顶层 exact-zero `&`/`|`/`^` 分支切到该 helper；递归 nonnegative publisher、mask/shift、same-identifier idempotent 和普通 signed bitwise algebra 仍不扩展。仍不声明 unary `-`、`zero - unit`、nonzero RHS subtract wrappers、nonzero identity wrappers、signed same-identifier idempotent/mask/general bitwise algebra、nested signed identity publication、任意 expression/offset normalizer、broader self-dependent replay、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser 18 tests 中 9 failures，LSP 9 个 sign-crossing query 仍返回旧范围。GREEN：WSL gcc focused parser 18 tests/0 failures + LSP PASS；WSL clang focused parser 18 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 18 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`)。相邻回归：WSL gcc numeric-range parser 10 tests/0 failures，bitwise-offset parser 10 tests/0 failures + LSP PASS，idempotent-wrapper parser 8 tests/0 failures + LSP PASS，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric Bitwise Exact-Zero Wrapper Expression Range Fact and LSP Local Query | 已完成 | 2026-07-03 02:04 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把普通表达式级 bitwise exact-zero wrapper range fact 与 LSP local expression query 补齐：新增短 parser target `zr_vm_bitwise_zero_wrapper_range_inference_test` 与 LSP target `zr_vm_lsp_bitwise_zero_wrapper_range_query_test`，覆盖 `(+zero) & unit`、`unit & (zero + zero)`、`(zero - zero) & unit`、`(+zero) | unit`、`unit | (zero + zero)`、`(zero - zero) | unit`、`unit ^ (+zero)`、`(zero + zero) ^ unit`、`unit ^ (zero - zero)`，其中 `zero=0..0`、`unit=2..3`。普通 bitwise identity exact-zero 判定现在会在 literal/binding zero 检查前剥离 unary `+`、可证明 `[0,0]` 的 binary `+` identity wrapper，以及 RHS 可证明 exact-zero 的 binary `-` wrapper；`&` 的 zero annihilator 与 `|`/`^` 的 zero identity 因此能发布对应表达式 range。仍不声明 unary `-`、`zero - unit`、nonzero RHS subtract wrappers、nonzero identity wrappers、negative/sign-crossing operands、general bitwise algebra、任意 expression/offset normalizer、self-dependent replay beyond existing separate targets、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 9 tests 中 4 failures，LSP 新 target 4 个 query 仍返回旧范围。GREEN：WSL gcc focused parser 9 tests/0 failures + LSP PASS；WSL clang focused parser 9 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 9 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`)。相邻回归：WSL gcc bitwise offset parser 10 tests/0 failures + LSP PASS，idempotent wrapper parser 8 tests/0 failures + LSP PASS，numeric-range parser 10 tests/0 failures，same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Additive/Subtract-Zero Same-Identifier Bitwise AND/OR Idempotent Range-Offset Conditional Delta Replay | 已完成 | 2026-07-03 01:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 pre-sequence direct target-reader 的 bitwise AND/OR idempotent positive range-offset 从裸 same identifier 推进到 additive-zero/right-zero subtract identity wrapper：新增短 parser target `zr_vm_selfdep_bitwise_idempotent_wrapper_offset_test` 与 LSP target `zr_vm_lsp_selfdep_bitwise_idempotent_wrapper_offset_test`，覆盖 `(unit + zero) & unit`、`unit & (zero + unit)`、`(unit - zero) & unit`、`unit & (unit - zero)`、`(zero + unit) | unit`、`unit | (unit + zero)`、`(unit - zero) | unit`、`unit | (unit - zero)`。普通 bitwise identity 推断和 prefix supported bounded offset 的 `&`/`|` idempotent path 现在会在 same-identifier 比较前剥离 unary `+`、可证明 `[0,0]` 的 binary `+` identity wrapper，以及 binary `-` 的 RHS exact-zero identity wrapper；positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX-2`。仍不声明 nonzero identity wrappers、`zero - unit`、nonzero RHS subtract wrappers、unary `-` over identifiers、unsupported/unbounded same-identifier bitwise facts、general bitwise algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser target 8 failures，LSP 首个 AND add-zero wrapper query 仍保持旧 `5..5` / `0..0` 范围。GREEN：WSL gcc focused parser 8 tests/0 failures + LSP PASS；WSL clang focused parser 8 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 8 tests/0 failures + LSP PASS (`VSCMD_VER=17.14.34`)。相邻回归：WSL gcc bitwise offset parser 10 tests/0 failures + LSP PASS；WSL gcc same-diff/modulo/xor parser 82 tests/0 failures + LSP PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Additive/Subtract-Zero Same-Identifier XOR Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-03 00:51 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus wrapper 下 sign-crossing exact-zero product 的 same-identifier bitwise XOR exact-zero factor 从裸 `^` 推进到 additive-zero/right-zero subtract identity wrapper：扩展 parser target `zr_vm_selfdep_neg_zero_product_unary_same_diff_test` 与 LSP target `zr_vm_lsp_selfdep_neg_zero_product_unary_same_diff_test`，覆盖 `span * ((factor + zero) ^ factor)`、`span * (factor ^ (factor + zero))`、`((zero + factor) ^ factor) * span`、`(factor ^ (zero + factor)) * span`、`span * ((factor - zero) ^ factor)`、`span * (factor ^ (factor - zero))`、`((factor - zero) ^ factor) * span`、`(factor ^ (factor - zero)) * span` 的 direct exact-zero 与 positive/negative delta replay。普通 bitwise identity 推断、prefix exact-zero validation 与 prefix supported bounded offset 的 `^` no-op path 现在会在 same-identifier xor exact-zero 比较前剥离 unary `+`、可证明 `[0,0]` 的 binary `+` identity wrapper，以及 binary `-` 的 RHS exact-zero identity wrapper；positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，negative-delta replay 给 target/reader/mirror `INT64_MIN..5`。仍不声明 `zero - factor`、nonzero RHS、`factor - nonzero`、unary `-` over identifiers、unsupported/unbounded same-identifier xor/difference/modulo、nonzero product/sign-crossing nonzero product、general bitwise xor/multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser/LSP xor wrapper focused cases 在生产改动前失败，parser 为 24 failures，LSP 首个 add-zero-left xor query 仍保持旧 `5..5` / `0..0` 范围。GREEN：WSL gcc focused parser 82 tests/0 failures + LSP PASS；WSL clang focused parser 82 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 82 tests/0 failures + LSP PASS。相邻回归：WSL gcc exact-zero selfdep 子集通过 zero-inclusive-product parser 52 tests/0 failures、zero-inclusive-difference parser 8 tests/0 failures、same-diff/modulo/xor parser 82 tests/0 failures，且三组 LSP product/difference/same-diff-modulo-xor query 均 PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Additive/Subtract-Zero Same-Identifier Modulo Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-03 00:33 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus wrapper 下 sign-crossing exact-zero product 的 same-identifier modulo exact-zero factor 从裸 `%` 推进到 additive-zero/right-zero subtract identity wrapper：扩展 parser target `zr_vm_selfdep_neg_zero_product_unary_same_diff_test` 与 LSP target `zr_vm_lsp_selfdep_neg_zero_product_unary_same_diff_test`，覆盖 `span * ((factor + zero) % factor)`、`span * (factor % (factor + zero))`、`((zero + factor) % factor) * span`、`(factor % (zero + factor)) * span`、`span * ((factor - zero) % factor)`、`span * (factor % (factor - zero))`、`((factor - zero) % factor) * span`、`(factor % (factor - zero)) * span` 的 direct exact-zero 与 positive/negative delta replay。普通表达式推断已能在 same-identifier modulo exact-zero 比较前剥离 unary `+`、可证明 `[0,0]` 的 binary `+` identity wrapper，以及 binary `-` 的 RHS exact-zero identity wrapper；prefix exact-zero validation 现在对 `%` 复用同一窄归一化，并保留 strict-positive divisor guard (`min > 0`)。positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，negative-delta replay 给 target/reader/mirror `INT64_MIN..5`。仍不声明 zero-inclusive divisor、`zero - factor`、nonzero RHS、`factor - nonzero`、unary `-` over identifiers、unsupported/unbounded same-identifier modulo/difference、nonzero product/sign-crossing nonzero product、general modulo/multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser/LSP modulo wrapper focused cases 在生产改动前失败，parser 为 16 failures，LSP 首个 add-zero-left modulo query 仍保持旧 `5..5` / `0..0` 范围。GREEN：WSL gcc focused parser 58 tests/0 failures + LSP PASS；WSL clang focused parser 58 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 58 tests/0 failures + LSP PASS。相邻回归：WSL gcc exact-zero selfdep 子集通过 zero-inclusive-product parser 52 tests/0 failures、zero-inclusive-difference parser 8 tests/0 failures、same-diff/modulo parser 58 tests/0 failures，且三组 LSP product/difference/same-diff-modulo query 均 PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Subtract-Zero Same-Identifier Difference Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-03 00:16 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus wrapper 下 sign-crossing exact-zero product 的 same-identifier difference 从 unary/additive-zero identity wrapper 推进到 right-zero subtract identity wrapper：扩展 parser target `zr_vm_selfdep_neg_zero_product_unary_same_diff_test` 与 LSP target `zr_vm_lsp_selfdep_neg_zero_product_unary_same_diff_test`，覆盖 `span * ((factor - zero) - factor)`、`span * (factor - (factor - zero))`、`((factor - zero) - factor) * span`、`(factor - (factor - zero)) * span` 的 direct exact-zero 与 positive/negative delta replay。普通表达式推断和 prefix exact-zero validation 现在会在 same-identifier exact-zero 比较前剥离 unary `+`、可证明 `[0,0]` 的 binary `+` identity wrapper，以及 binary `-` 的 RHS exact-zero identity wrapper；positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，negative-delta replay 给 target/reader/mirror `INT64_MIN..5`。仍不声明 `zero - factor`、nonzero RHS、`factor - nonzero`、unary `-` over identifiers、unsupported/unbounded same-identifier differences、nonzero product/sign-crossing nonzero product、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser/LSP subtract-zero focused cases 在生产改动前失败，parser 为 12 failures，LSP 首个 subtract-zero query 仍保持旧 `5..5` / `0..0` 范围。GREEN：WSL gcc focused parser 34 tests/0 failures + LSP PASS；WSL clang focused parser 34 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 34 tests/0 failures + LSP PASS。相邻回归：WSL gcc exact-zero selfdep 子集通过 zero-inclusive-product parser 52 tests/0 failures、zero-inclusive-difference parser 8 tests/0 failures、same-diff parser 34 tests/0 failures，且三组 LSP product/difference/same-diff query 均 PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Additive-Zero Same-Identifier Difference Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 19:39 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus wrapper 下 sign-crossing exact-zero product 的 same-identifier difference 从裸标识符/unary-plus 推进到可证明 exact-zero 的 additive-zero identity wrapper：扩展 parser target `zr_vm_selfdep_neg_zero_product_unary_same_diff_test` 与 LSP target `zr_vm_lsp_selfdep_neg_zero_product_unary_same_diff_test`，覆盖 `span * ((factor + zero) - factor)`、`span * (factor - (factor + zero))`、`((zero + factor) - factor) * span`、`(factor - (zero + factor)) * span` 的 positive/negative delta replay。普通表达式推断现在会在 same-identifier exact-zero 比较前剥离 unary `+` 与已推断为 `[0,0]` 的 binary `+` identity wrapper；prefix exact-zero validation 用同一窄归一化复用既有 exact-zero 判定。positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，negative-delta replay 给 target/reader/mirror `INT64_MIN..5`。仍不声明 nonzero identity wrapper、`factor + nonzero`、binary `-` additive wrapper、unsupported/unbounded same-identifier differences、unary `-` over identifiers、nonzero multiplication、sign-crossing nonzero products、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser/LSP add-zero focused cases 在生产改动前失败，parser 为 12 failures，LSP 首个 add-zero query 仍保持旧 `5..5` / `0..0` 范围。GREEN：WSL gcc focused parser 22 tests/0 failures + LSP PASS；WSL clang focused parser 22 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 22 tests/0 failures + LSP PASS。相邻回归：WSL gcc exact-zero selfdep 子集通过 zero-inclusive-product parser 52 tests/0 failures、zero-inclusive-difference parser 8 tests/0 failures、same-diff parser 22 tests/0 failures，且三组 LSP product/difference/same-diff query 均 PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Unary-Plus Same-Identifier Difference Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 19:10 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 覆盖 zero-minus wrapper 下 sign-crossing exact-zero product 的 unary-plus same-identifier difference exact-zero factor：新增 parser target `zr_vm_selfdep_neg_zero_product_unary_same_diff_test` 与 LSP target `zr_vm_lsp_selfdep_neg_zero_product_unary_same_diff_test`，覆盖 `span * ((+factor) - factor)`、`span * (factor - (+factor))`、`((+factor) - factor) * span`、`(factor - (+factor)) * span` 的 positive/negative delta replay。普通表达式推断现在会在 same-identifier exact-zero 比较前剥离 unary `+` identity wrapper，prefix exact-zero validation 也接受同一 unary-plus same-identifier 形态；positive-delta replay 给 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，negative-delta replay 给 target/reader/mirror `INT64_MIN..5`。仍不声明 unary `-`、unsupported/unbounded same-identifier differences、nonzero factor identity、nonzero multiplication、sign-crossing nonzero products、broader exact-zero normalization beyond checked unary-plus same-identifier difference、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc 新 parser/LSP focused cases 在生产改动前失败，parser 为 8 failures，LSP 首个 query 仍保持旧范围。GREEN：WSL gcc focused parser 10 tests/0 failures + LSP PASS；WSL clang focused parser 10 tests/0 failures + LSP PASS；Windows MSVC Debug focused parser 10 tests/0 failures + LSP PASS；WSL gcc 旧 zero-inclusive-product parser 回归 52 tests/0 failures；WSL gcc selfdep 子矩阵 `SELFDEP_TARGETS=47 SELFDEP_MATRIX_RUN=47 FAIL=0`。聚合 CTest language_pipeline/language_server 未声明 GREEN：未预构建全列表可执行文件导致缺失 `zr_vm_named_arguments_test` / `zr_vm_language_server_symbol_table_test`；MSVC broad selfdep 子矩阵被既有旧 long-path parser targets 阻塞，新短目标 focused 已通过。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Same-Identifier Difference Exact-Zero Factor Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 18:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下 sign-crossing exact-zero product 的 same-identifier difference exact-zero factor：`span=-1..1; factor=2..3; other = (+narrowed) - (zero - (span * (factor - factor)))` 与 commuted `(+narrowed) - (zero - ((factor - factor) * span))`。`ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsExactZeroValue` 已在 binary `-` 两侧是同一 identifier 且该 identifier 是 supported bounded offset 时证明 `x - x` 为 exact zero；exact-zero product helper 再接受另一侧 sign-crossing operand；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 unsupported/unbounded same-identifier difference、nonzero factor identity、nonzero multiplication、sign-crossing nonzero products、broader exact-zero normalization beyond checked same-identifier difference 与 exact-zero `+`/`-` factor wrappers、nested wrappers beyond covered depth/shape、broader non-binding sign-crossing expressions beyond covered wrappers、two-negative/general zero-product algebra、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing same-identifier difference exact-zero factor product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 52 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 52 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_SAME_IDENTIFIER_DIFFERENCE_FACTOR_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Exact-Zero Factor Nested Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 17:56 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下 sign-crossing exact-zero product 的 exact-zero factor nested identity wrapper：`span=-1..1; zero=0..0; zeroFactor=0..0; other = (+narrowed) - (zero - (span * ((zeroFactor + zero) - zero)))` 与 commuted `(+narrowed) - (zero - (((zero - zeroFactor) + zero) * span))`。`ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsExactZeroValue` 已递归证明 unary `+`/`-`、exact-zero `+`/`-`、direct zero binding/literal 为 exact zero；exact-zero product helper 再接受另一侧 sign-crossing operand；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 nonzero factor identity、nonzero multiplication、sign-crossing nonzero products、broader exact-zero normalization beyond checked exact-zero `+`/`-` factor wrappers、nested wrappers beyond covered depth/shape、broader non-binding sign-crossing expressions beyond covered wrappers、two-negative/general zero-product algebra、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing exact-zero factor nested identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 48 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 48 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_ZERO_FACTOR_NESTED_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Exact-Zero Factor Subtractive Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 17:14 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下 sign-crossing exact-zero product 的 exact-zero factor subtractive identity wrapper：`span=-1..1; zero=0..0; zeroFactor=0..0; other = (+narrowed) - (zero - (span * (zeroFactor - zero)))` 与 commuted `(+narrowed) - (zero - ((zero - zeroFactor) * span))`。`ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsExactZeroValue` 已把两个 exact-zero operand 的 binary `-` 证明为 exact zero；exact-zero product helper 再接受另一侧 sign-crossing operand；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 nonzero factor identity、nonzero multiplication、sign-crossing nonzero products、broader exact-zero normalization beyond checked exact-zero `+`/`-` factor wrappers、nested factor-side wrappers beyond covered、broader non-binding sign-crossing expressions beyond covered wrappers、two-negative/general zero-product algebra、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing exact-zero factor subtractive identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 44 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 44 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_ZERO_FACTOR_SUBTRACTIVE_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Exact-Zero Factor Additive Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 16:41 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下 sign-crossing exact-zero product 的 exact-zero factor additive identity wrapper：`span=-1..1; zero=0..0; zeroFactor=0..0; other = (+narrowed) - (zero - (span * (zeroFactor + zero)))` 与 commuted `(+narrowed) - (zero - ((zero + zeroFactor) * span))`。`ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsExactZeroValue` 已把两个 exact-zero operand 的 binary `+` 证明为 exact zero；exact-zero product helper 再接受另一侧 sign-crossing operand；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 nonzero factor identity、nonzero multiplication、sign-crossing nonzero products、broader exact-zero normalization beyond checked exact-zero `+` factor wrapper、factor-side subtractive wrappers、broader non-binding sign-crossing expressions beyond covered wrappers、two-negative/general zero-product algebra、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing exact-zero factor additive identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 40 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 40 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_ZERO_FACTOR_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Unary Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 16:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下的受限 sign-crossing exact-zero product unary identity wrapper：`span=-1..1; zeroFactor=0..0; other = (+narrowed) - (zero - ((+span) * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * (+span)))`。上一阶段新增的 `prefix_offset_expression_is_sign_crossing_product_operand` 已允许乘法另一侧递归证明 exact zero 时接受 unary `+` 包裹的 direct int64 sign-crossing binding；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 unary `-`、sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus/additive zero/subtractive right-zero identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing unary identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 36 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 36 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_UNARY_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Subtractive Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 16:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下的受限 sign-crossing exact-zero product subtractive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) - (zero - ((span - zero) * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * (span - zero)))`。上一切片已让 `type_inference_loop_assignment_prefix_offset.c` 的私有 `prefix_offset_expression_is_sign_crossing_product_operand` 仅对 binary `-` 右侧 exact zero 做透明转发；supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 `zero - span`、nonzero right subtraction identity、sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus/additive zero/subtractive right-zero identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-minus sign-crossing subtractive identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 32 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 32 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_SUBTRACTIVE_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Sign-Crossing Exact-Zero Product Subtractive Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 15:57 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader subtract 下的受限 sign-crossing exact-zero product subtractive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) - ((span - zero) * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * (span - zero))`。上一切片已在 `type_inference_loop_assignment_prefix_offset.c` 的私有 `prefix_offset_expression_is_sign_crossing_product_operand` 中仅对 binary `-` 右侧 exact zero 做透明转发；prefix-reader binary `-` 再把 product exact `[0,0]` 作为 supported bounded offset/no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 `zero - span`、nonzero right subtraction identity、sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus/additive zero/subtractive right-zero identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused direct subtract sign-crossing subtractive identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 39 tests/0 failures 且 focused LSP PASS；WSL clang focused parser negative-product 39 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_SUBTRACT_SUBTRACTIVE_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Sign-Crossing Exact-Zero Product Subtractive Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 15:43 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 覆盖 direct target-reader additive 下的受限 sign-crossing exact-zero product subtractive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) + ((span - zero) * zeroFactor)` 与 commuted `(zeroFactor * (span - zero)) + (+narrowed)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的私有 `prefix_offset_expression_is_sign_crossing_product_operand`：仅当 binary `-` 右侧递归证明为 exact zero 时，透明转发左侧继续匹配 sign-crossing product operand；乘法仍要求另一侧递归证明 exact zero，因此 product 保持 exact `[0,0]`。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 `zero - span`、nonzero right subtraction identity、sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus/additive zero/subtractive right-zero identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 新增 `(span - zero) * zeroFactor` 与 `zeroFactor * (span - zero)` positive-delta cases 失败为 expected `INT64_MAX` 仍是 `5`，negative-delta cases 失败为 expected `INT64_MIN` 仍是 `5`。GREEN：WSL gcc focused parser zero-inclusive-additive 36 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-additive 36 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ADDITIVE_SUBTRACTIVE_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Sign-Crossing Exact-Zero Product Unary Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 15:18 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader subtract 下的受限 sign-crossing exact-zero product unary identity wrapper：`span=-1..1; zeroFactor=0..0; other = (+narrowed) - ((+span) * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * (+span))`。上一阶段新增的 `prefix_offset_expression_is_sign_crossing_product_operand` 已允许乘法另一侧递归证明 exact zero 时接受 unary `+` 包裹的 direct int64 sign-crossing binding；prefix-reader binary `-` 再把 RHS exact `[0,0]` 作为 supported bounded offset/no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus and additive zero-identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused direct subtract sign-crossing unary identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 35 tests/0 failures 且 focused LSP PASS；WSL clang focused parser negative-product 35 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_SUBTRACT_UNARY_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Sign-Crossing Exact-Zero Product Unary Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 15:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader additive 下的受限 sign-crossing exact-zero product unary identity wrapper：`span=-1..1; zeroFactor=0..0; other = (+narrowed) + ((+span) * zeroFactor)` 与 commuted `(zeroFactor * (+span)) + (+narrowed)`。上一阶段新增的 `prefix_offset_expression_is_sign_crossing_product_operand` 已允许乘法另一侧递归证明 exact zero 时接受 unary `+` 包裹的 direct int64 sign-crossing binding，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond unary plus and additive zero-identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-additive focused direct additive sign-crossing unary identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-additive 32 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-additive 32 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ADDITIVE_UNARY_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Sign-Crossing Exact-Zero Product Additive Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 14:51 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader subtract 下的受限 sign-crossing exact-zero product additive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) - ((span + zero) * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * (zero + span))`。上一阶段新增的 `prefix_offset_expression_is_sign_crossing_product_operand` 已允许乘法另一侧递归证明 exact zero 时接受 direct int64 sign-crossing binding、unary `+` 包裹、以及 binary `+` 中一侧 exact zero 另一侧递归仍为 sign-crossing product operand 的窄形态；prefix-reader binary `-` 再把 RHS exact `[0,0]` 作为 supported bounded offset/no-op 消费，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond additive zero-identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused direct subtract sign-crossing identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 31 tests/0 failures 且 focused LSP PASS；WSL clang focused parser negative-product 31 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_SUBTRACT_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Sign-Crossing Exact-Zero Product Additive Identity-Wrapper Positive/Negative Delta Replay | 已完成 | 2026-07-02 14:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader additive 下的受限 sign-crossing exact-zero product additive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) + ((span + zero) * zeroFactor)` 与 commuted `(zeroFactor * (zero + span)) + (+narrowed)`。上一阶段新增的 `prefix_offset_expression_is_sign_crossing_product_operand` 已允许乘法另一侧递归证明 exact zero 时接受 direct int64 sign-crossing binding、unary `+` 包裹、以及 binary `+` 中一侧 exact zero 另一侧递归仍为 sign-crossing product operand 的窄形态，因此本切片不需要新的生产代码或 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond additive zero-identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-additive focused direct additive sign-crossing identity-wrapper exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-additive 28 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-additive 28 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ADDITIVE_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55 VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Additive Identity-Wrapper Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 14:17 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 TDD RED/GREEN 把 zero-minus wrapper 下的 sign-crossing exact-zero product 从 direct binding 推进到受限 additive identity wrapper：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) - (zero - ((span + zero) * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * (zero + span)))`。实现限定在 `type_inference_loop_assignment_prefix_offset.c`：新增私有 `prefix_offset_expression_is_sign_crossing_product_operand`，只在乘法另一侧递归证明 exact zero 时接受 direct int64 sign-crossing binding、unary `+` 包裹、以及 binary `+` 中一侧 exact zero 另一侧递归仍为 sign-crossing product operand 的窄形态；不扩展 nonzero product、减法/乘法通用代数或 arbitrary offset normalizer。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片仍不声明 sign-crossing nonzero products, broader non-binding sign-crossing expressions beyond additive zero-identity wrappers, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 新增 `(span + zero) * zeroFactor` 与 `zeroFactor * (zero + span)` positive-delta cases 失败为 expected `INT64_MAX` 仍是 `5`，negative-delta cases 失败为 expected `INT64_MIN` 仍是 `5`。GREEN：WSL gcc focused parser zero-inclusive-product 28 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 28 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_IDENTITY_WRAPPER_FOCUSED build=0 parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Sign-Crossing Exact-Zero Product Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 13:45 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下的受限 sign-crossing exact-zero product offset：`zero=0..0; span=-1..1; zeroFactor=0..0; other = (+narrowed) - (zero - (span * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * span))`。现有 exact-zero product helper 已在上一切片允许乘法一侧递归证明 exact zero 时接受 direct int64 sign-crossing binding，supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 bounded offset `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing nonzero products, non-binding sign-crossing expressions, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused sign-crossing zero-minus exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 24 tests/0 failures 且 focused LSP PASS；WSL clang focused parser zero-inclusive-product 24 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ZERO_MINUS_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Sign-Crossing Exact-Zero Product Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 13:31 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct `target - exact-zero product` identity 的受限 sign-crossing bounded binding partner：`span=-1..1; zeroFactor=0..0; other = (+narrowed) - (span * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * span)`。现有 exact-zero product helper 已在上一切片允许乘法一侧递归证明 exact zero 时接受 direct int64 sign-crossing binding，prefix-reader binary `-` 再把 RHS exact `[0,0]` 作为 supported bounded offset/no-op 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing nonzero products, non-binding sign-crossing expressions, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused sign-crossing direct subtract exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 27 tests/0 failures 且 focused LSP PASS；WSL clang focused parser negative-product 27 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_SUBTRACT_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 TARGETS=55`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Sign-Crossing Exact-Zero Product Additive Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 13:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 direct target-reader additive exact-zero product identity 从已覆盖的 nonnegative/negative partner 推进到受限 sign-crossing bounded binding partner：`span=-1..1; zeroFactor=0..0; other = (+narrowed) + (span * zeroFactor)` 与 commuted `(zeroFactor * span) + (+narrowed)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c`：新增私有 sign-crossing binding 判定，只在乘法另一侧已递归证明 exact zero 时接受 direct int64 binding 且 `min<0,max>0`，把乘零项保持为 exact `[0,0]`；不扩展 general product algebra、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing nonzero products, non-binding sign-crossing expressions, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD RED：WSL gcc focused parser 新增 sign-crossing zero-product additive positive-delta cases 失败为 expected `INT64_MAX` 仍是 `5`，negative-delta cases 失败为 expected `INT64_MIN` 仍是 `5`。GREEN：WSL gcc focused parser zero-inclusive-additive 24 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 24 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_SIGN_CROSSING_ZERO_PRODUCT_ADDITIVE_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC self-dependency project build/matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Zero-Inclusive Nonnegative Exact-Zero Product Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 12:46 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下的 zero-inclusive nonnegative exact-zero product offset 边界：`zero=0..0; maybeUnit=0..1; zeroFactor=0..0; other = (+narrowed) - (zero - (maybeUnit * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * maybeUnit))`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset 且包含 zero-inclusive nonnegative range，supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 bounded offset `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused zero-inclusive-nonnegative zero-minus exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 20 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 20 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_INCLUSIVE_NONNEGATIVE_ZERO_PRODUCT_ZERO_MINUS_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Inclusive Nonnegative Exact-Zero Product Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 12:37 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct `target - exact-zero product` identity 的 zero-inclusive nonnegative partner 边界：`maybeUnit=0..1; zeroFactor=0..0; other = (+narrowed) - (maybeUnit * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * maybeUnit)`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset 且包含 zero-inclusive nonnegative range，prefix-reader binary `-` 先把 RHS 识别为 bounded offset `[0,0]`，随后复用 target-subtract/add zero-offset guard，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused zero-inclusive-nonnegative direct exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 23 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 23 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_INCLUSIVE_NONNEGATIVE_ZERO_PRODUCT_SUBTRACT_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Zero-Inclusive Nonnegative Exact-Zero Product Additive Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 12:28 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader additive exact-zero product identity 的 zero-inclusive nonnegative partner 边界：`maybeUnit=0..1; zeroFactor=0..0; other = (+narrowed) + (maybeUnit * zeroFactor)` 与 commuted `(zeroFactor * maybeUnit) + (+narrowed)`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset 且包含 zero-inclusive nonnegative range，binary `+` exact-zero identity target extraction 随后把乘零项作为 no-op reader 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-additive focused zero-inclusive-nonnegative exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-additive 20 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 20 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_INCLUSIVE_NONNEGATIVE_ZERO_PRODUCT_ADDITIVE_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Minus Bounded Nonnegative Exact-Zero Product Offset Positive/Negative Delta Replay | 已完成 | 2026-07-02 12:16 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-minus wrapper 下的 bounded nonnegative exact-zero product offset 子集：`zero=0..0; factor=2..3; zeroFactor=0..0; other = (+narrowed) - (zero - (factor * zeroFactor))` 与 commuted `(+narrowed) - (zero - (zeroFactor * factor))`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset，supported bounded offset 的 binary `-` identity path 把内层 `zero - [0,0]` 映射为 bounded offset `[0,0]`，外层 prefix-reader binary `-` 再按 zero-offset no-op 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-product focused bounded-nonnegative zero-minus exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-product 16 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 16 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_BOUNDED_NONNEGATIVE_ZERO_PRODUCT_ZERO_MINUS_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Bounded Nonnegative Exact-Zero Product Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 12:03 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct `target - exact-zero product` identity 的 bounded nonnegative partner 子集：`factor=2..3; zeroFactor=0..0; other = (+narrowed) - (factor * zeroFactor)` 与 commuted `(+narrowed) - (zeroFactor * factor)`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset，prefix-reader binary `-` 先把 RHS 识别为 bounded offset `[0,0]`，随后复用 target-subtract/add zero-offset guard，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused bounded-nonnegative direct zero-product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 19 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 19 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_BOUNDED_NONNEGATIVE_ZERO_PRODUCT_SUBTRACT_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Bounded Nonnegative Exact-Zero Product Additive Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 11:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct target-reader additive exact-zero product identity 的 bounded nonnegative partner 子集：`unit=2..3; zeroFactor=0..0; other = (+narrowed) + (unit * zeroFactor)` 与 commuted `(zeroFactor * unit) + (+narrowed)`。现有 exact-zero product helper 已允许另一侧为 supported bounded nonnegative offset，binary `+` exact-zero identity target extraction 随后把乘零项作为 no-op reader 消费，因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product offsets, sign-crossing products, two-negative/general zero-product algebra, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-additive focused bounded-nonnegative exact-zero product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-additive 16 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 16 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_BOUNDED_NONNEGATIVE_ZERO_PRODUCT_ADDITIVE_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Exact-Zero Product Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 11:29 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 direct `target - exact-zero product` identity 形态，不经过 zero-minus wrapper：`zeroFactor=0..0; maybeDown=-1..0; down=-2..-1; other = (+narrowed) - (maybeDown * zeroFactor)`、commuted `(zeroFactor * maybeDown)`、`other = (+narrowed) - (down * zeroFactor)`、commuted `(zeroFactor * down)`。现有 prefix-reader binary `-` 已先通过 `ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsSupportedBoundedOffset` 把 exact-zero product RHS 识别为 bounded offset `[0,0]`，随后分别复用 target-subtract-offset guard（positive delta）和 target-add-offset guard（negative delta zero offset），因此不需要生产代码变更、不新增 CMake target。positive-delta replay 为 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 nonzero product, sign-crossing nonzero products, two-negative/general zero-product algebra, strict-negative validator zero-factor broadening, broader exact-zero normalization, general multiplication/subtraction/additive algebra, 任意 offset normalizer, 非 identity complex prefix RHS, 写入 delta dependency 的 prefix reader, replay-resolved delta 组合, arbitrary interleavings, nested/unsupported assignments, true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP negative-product focused direct zero-product positive/negative-delta cases 在现有实现中直接通过；WSL gcc focused parser negative-product 15 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 15 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_DIRECT_ZERO_PRODUCT_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Double Zero-Minus Zero-Inclusive Difference Negative-Delta Replay / Positive-Delta Guard | 已完成 | 2026-07-02 11:15 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖此前仍列为剩余工作的 helper-level `zero - maybeDown` 组合形态：`zero=0..0; maybeDown=-1..0; other = (+narrowed) - (zero - (zero - maybeDown))`，并分别验证 `narrowed = narrowed + (choose ? step : step + bias)` 的正向 guard 与 `narrowed = narrowed - (choose ? step : step + bias)` 的负向 replay。现有私有 helper 组合已足够：supported bounded offset validator 先把内层 exact-zero-left `zero - maybeDown` 映射为 `[0,1]`；zero-inclusive negative helper 再把外层 `zero - [0,1]` 映射为 `[-1,0]`；prefix-reader binary `-` 在 negative-delta 序列中把 `target - [-1,0]` 映射为正 offset replay。该切片无生产代码变更、不新增 CMake target。negative-delta replay 为 target `INT64_MIN..5`、reader/mirror `INT64_MIN..6`；paired positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片不声明 sign-crossing/positive RHS as negative binding、general `zero - expr` normalization、general subtraction/additive/multiplication algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser/LSP zero-inclusive-difference double zero-minus positive-delta guard 与 negative-delta replay cases 在现有实现中直接通过；WSL gcc focused parser zero-inclusive-difference 8 tests/0 failures 且 focused LSP PASS；WSL clang focused parser 8 tests/0 failures 且 focused LSP PASS；WSL gcc current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_DOUBLE_ZERO_MINUS_FOCUSED parser=0 lsp=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=45 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Strict-Negative Exact-Zero Product Additive and Zero-Minus Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 10:39 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 exact-zero product identity 从 zero-inclusive negative partner 扩到受支持 bounded strict-negative partner：`down=-2..-1; zeroFactor=0..0; other = (+narrowed) + (down * zeroFactor)`、commuted `(zeroFactor * down) + (+narrowed)`，以及 zero-minus wrapper `(+narrowed) - (zero - (down * zeroFactor))` / commuted `zeroFactor * down`。实现限定在 `type_inference_loop_assignment_prefix_offset.c`：新增私有 exact-zero product operand helper，binary `*` 仍要求一侧递归证明 exact zero，另一侧只能是已支持的 bounded nonnegative offset、bounded zero-inclusive negative binding 或 bounded strict-negative binding；随后复用既有 binary `+` identity target extraction 或 zero-minus exact-zero-left offset path，不新增 CMake target。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；negative-delta exact-zero product additive 和 zero-minus forms 均按 no-op reader replay 为 target/reader/mirror `INT64_MIN..5`。该切片不声明 sign-crossing 乘零、two-negative/general zero-product algebra、strict-negative validator zero-factor broadening、general exact-zero normalization、general additive/subtraction/multiplication algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：新增 parser additive direct/commuted strict-negative zero-product positive-delta cases 先失败为 target max `5` 而非 `INT64_MAX`，negative-delta cases 先失败为 target min `5` 而非 `INT64_MIN`；focused LSP 首个 strict-negative zero-product additive positive case first returned target/observer/mirror `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused additive/product 4-target set PASS；WSL clang focused additive/product 4-target set PASS；WSL gcc current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；WSL clang current self-dependency target matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_STRICT_NEG_ZERO_PRODUCT_FOCUSED RUN=4 FAILURES=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=58 EXIT_FAILURES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Zero-Inclusive Negative Exact-Zero Product Additive Identity Positive/Negative Delta Replay | 已完成 | 2026-07-02 10:11 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一阶段尚未声明的 exact-zero product identity 补到 direct target-reader additive 消费路径：`zeroFactor=0..0; maybeDown=-1..0; other = (+narrowed) + (maybeDown * zeroFactor)` 与 commuted `(zeroFactor * maybeDown) + (+narrowed)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 `ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsExactZeroValue`：binary `*` 只有在一侧递归证明为 exact zero、另一侧由 `ZrParser_TypeInferenceLoopAssignment_PrefixZeroInclusiveNegativeExpressionIsBinding` 证明为 bounded zero-inclusive negative binding 时，才把乘积视为 exact zero；随后复用既有 binary `+` exact-zero identity target extraction，不新增 CMake target。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同一 exact-zero product identity 也把既有 `(+narrowed) - (zero - (maybeDown * zeroFactor))` 与 commuted zero-product product cases 从旧保守 guard 修正为 downward no-op replay，negative-delta target/reader/mirror 为 `INT64_MIN..5`。非零 zero-inclusive negative additive offset 的 negative-delta guard 仍保持 `5..5`/`0..0`。该切片不声明 strict-negative 或 sign-crossing 乘零、two negative operands、general zero-product algebra、general exact-zero normalization、general additive/subtraction/multiplication algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：新增 parser additive direct/commuted zero-product positive-delta cases 先失败为 target max `5` 而非 `INT64_MAX`；focused LSP 首个 zero-product additive target/observer/mirror first returned `5..5`、`0..0`、`0..0`。GREEN 过程中，exact-zero 修复还暴露既有 zero-minus zero-product negative-delta cases 应从旧 guard 期望修正为 downward identity replay，parser/LSP 预期同步为 `INT64_MIN..5`。最终验证：WSL gcc focused 16-target set PASS；WSL clang focused 16-target set PASS；WSL gcc active matrix `RUN=57 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；WSL clang active matrix `RUN=57 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_PRODUCT_ADD_FOCUSED RUN=16 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0 VSCMD_VER=17.14.34`；Windows MSVC available executable matrix `RUN=60 EXIT_FAILURES=0 FAILURE_MATCHES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Zero-Inclusive Negative Additive Offset Conditional Positive-Delta Replay / Negative-Delta Guard | 已完成 | 2026-07-02 09:41 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 direct target-reader binary `+` 消费路径推进到受限 zero-inclusive negative additive offset 子集：`maybeDown=-1..0; other = (+narrowed) + maybeDown` 与 commuted `maybeDown + (+narrowed)`；`mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_reader.c` 的 binary `+` reader 分支和 `type_inference_loop_assignment_prefix_target_guard.c/.h`：当一侧已由 `ZrParser_TypeInferenceLoopAssignment_PrefixZeroInclusiveNegativeExpressionIsBinding` 证明为 bounded zero-inclusive negative range 且 `min<0,max==0,min != INT64_MIN`，另一侧读取受支持 target，并且后续 self-dependent delta 为 upward positive/nonnegative-positive 时，新增 `PrefixReaderTargetAddZeroInclusiveNegativeOffsetIsSupported` 以 target 下界 guard 证明 `target + [min,0]` 不下溢；两种 operand order 均覆盖。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。为避免继续扩展接近 1000 行的旧 LSP expression query，新建 focused parser/LSP targets `zr_vm_numeric_loop_selfdep_neg_zero_incl_add_dataflow_test` 与 `zr_vm_lsp_selfdep_neg_zero_incl_add_query_test` 并纳入 CMake 聚合。该切片不扩展 zero-inclusive helper 本身、不声明 exact-zero product/additive identity、helper-level `zero - maybeDown` as negative binding、strict-negative validator 新形态、sign-crossing/positive RHS as negative binding、general additive/subtraction/multiplication algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：新增 parser direct/commuted positive-delta cases 先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guards 通过；focused LSP positive-delta target/observer/mirror first returned `5..5`、`0..0`、`0..0`。GREEN：WSL gcc 新 parser/LSP targets PASS；WSL gcc focused 12-target set PASS；WSL clang focused 12-target set PASS；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_INCL_NEG_ADD_FOCUSED_12 RUN=12 BUILD_FAILURES=0 EXIT_FAILURES=0 VSCMD_VER=17.14.34`。最终验证：WSL gcc active matrix `RUN=57 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；WSL clang active matrix `RUN=57 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；Windows MSVC available executable matrix `RUN=60 EXIT_FAILURES=0 FAILURE_MATCHES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Direct Target Reader Subtract Zero-Inclusive Negative Product Offset Conditional Negative-Delta Replay / Positive-Delta Guard | 已完成 | 2026-07-02 09:08 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 direct target-reader `target - zero-inclusive negative RHS` 消费路径推进到已有 zero-inclusive negative helper 能证明的 product 子集：`maybeDown=-1..0; factor=2..3; other = (+narrowed) - (maybeDown * factor)` 与 commuted `factor * maybeDown`; `mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_reader.c` 的 binary `-` reader 分支：当 RHS 已由 `ZrParser_TypeInferenceLoopAssignment_PrefixZeroInclusiveNegativeExpressionIsBinding` 证明为 bounded zero-inclusive negative range、`max==0` 且 `min != INT64_MIN` 时，把 `target - rhs` 映射为正 offset 上界 `-min`，并复用 target-add-offset guard；不扩展 zero-inclusive helper 本身或 strict-negative product validator。补充 discovery/GREEN 覆盖同一 product target 中 commuted strict-negative `factor * down` 既有路径。negative-delta direct zero-inclusive product replay 保持 target `INT64_MIN..5`、reader/mirror `INT64_MIN..8`；strict-negative product/commuted strict-negative product 保持 reader/mirror `INT64_MIN+2..11`；同形 positive-delta zero-inclusive product guards 保持 target `5..5`、reader/mirror `0..0`。该切片不声明 helper-level `zero - maybeDown` as negative binding、zero-inclusive helper 新 RHS 形态、two negative operands、sign-crossing/positive RHS as negative binding、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：新增 parser direct/commuted zero-inclusive product negative-delta cases 先失败为 `Expected -9223372036854775808 Was 5`，positive-delta guards 通过；focused LSP direct zero-inclusive product first returned target/observer/mirror `5..5`、`0..0`、`0..0`。GREEN：WSL gcc/clang focused 10-target sets PASS；Windows MSVC Visual Studio Debug focused `MSVC_ZERO_INCLUSIVE_NEG_PRODUCT_FOCUSED_PASS RUN=10`，`VSCMD_VER=17.14.34`。最终验证：WSL gcc active matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；WSL clang active matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；Windows MSVC available executable matrix `RUN=58 EXIT_FAILURES=0 FAILURE_MATCHES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Strict-Negative Product Offset Conditional Negative-Delta Replay / Positive-Delta Guard | 已完成 | 2026-07-02 08:50 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 strict-negative offset validator 扩到受限 strict-negative product offset 子集：`down=-2..-1; factor=2..3; other = (+narrowed) - (down * factor); mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。实现限定在私有 `type_inference_loop_assignment_prefix_negative_offset.c`：binary `*` 只有在一侧已证明为 bounded strict-negative offset（`max < 0`）、另一侧已证明为 supported bounded positive offset（`min > 0`）时接受，并映射为 `[negativeMin * positiveMax, negativeMax * positiveMin]`；同时 guard `negativeMin != INT64_MIN`、乘法下溢、lower bound 等于 `INT64_MIN` 和 upper bound `>=0`，commuted operand order 覆盖。negative-delta replay 保持 target `INT64_MIN..5`、reader/mirror `INT64_MIN+2..11`；同形 positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片不声明 zero-inclusive/zero-factor product in strict-negative validator、two negative operands、sign-crossing/positive RHS as negative binding、helper-level `zero - maybeDown` as negative binding、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED：新增 parser negative-delta case 先失败为 `Expected -9223372036854775808 Was 5`，positive-delta guard 通过；focused LSP first returned target/observer/mirror `5..5`、`0..0`、`0..0`。GREEN：WSL gcc/clang focused 10-target sets PASS；Windows MSVC Visual Studio Debug focused `RUN=10 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`，`VSCMD_VER=17.14.34`。最终验证：WSL gcc active matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；WSL clang active matrix `RUN=55 BUILD_FAILURES=0 EXIT_FAILURES=0 PRECISE_FAILURE_MATCHES=0 MISSING=0`；Windows MSVC available executable matrix `RUN=58 EXIT_FAILURES=0 FAILURE_MATCHES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Difference All-Zero RHS Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 08:19 +08:00 | 按阶段 2 的数值区间/语义事实路线，用 discovery/GREEN 覆盖 zero-inclusive difference 的 all-zero RHS outside product-factor 子集：`zero=0..0; maybeDown=-1..0; other = (+narrowed) - (zero - (maybeDown - zero)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。现有私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c` 已经在 binary `-` 中把 exact-zero right operand 作为身份元递归回 left zero-inclusive negative binding，因此无需生产代码变更；outer exact-zero-left `zero - (...)` 继续映射为 zero-inclusive positive bounded offset。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 zero-inclusive negative left operand 减去 exact-zero RHS 的 identity 子集；不声明 `zero - maybeDown` as a helper-level negative binding、nonzero identity/cancellation、sign-crossing/positive RHS、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD discovery/GREEN：新增 parser all-zero RHS positive-delta/negative-delta guard 用例在现有实现中直接通过，WSL gcc focused parser zero-inclusive-difference 6 tests/0 failures，focused LSP zero-inclusive-difference query PASS；WSL clang focused parser 6 tests/0 failures，focused LSP PASS；Windows MSVC Visual Studio Debug focused rebuild/run `RUN=2 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`，`VSCMD_VER=17.14.34`。最终验证：WSL gcc 当前活跃 self-dependency target matrix `RUN=53 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；WSL clang 当前活跃 self-dependency target matrix `RUN=53 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；Windows MSVC 可用 self-dependency executable run matrix `RUN=56 EXIT_FAILURES=0 FAILURE_MATCHES=0`。full repository GREEN 不声明。 |
| 阶段 2 / Prefix Strict-Negative Offset Validator Modularization Maintenance | 已完成 | 2026-07-02 07:44 +08:00 | 按阶段 2 的模块维护门槛，在继续扩展 strict-negative / zero-inclusive offset 语义前，把 strict-negative offset validator 及其私有 strict-negative addition、zero-inclusive negative plus strict-negative addition、zero-inclusive negative minus strict-positive helpers 从 `type_inference_loop_assignment_prefix_offset.c` 拆到新私有源文件 `type_inference_loop_assignment_prefix_negative_offset.c`；`type_inference_loop_assignment_prefix_offset.h` 的公开 API 不变，`type_inference_loop_assignment_prefix_offset.c` 从 974 行降到 655 行，新模块 330 行。该切片不扩展语义能力；上一条 zero-inclusive plus strict-negative sum 的 accepted/rejected forms 和 range result 保持不变，不声明 all-zero RHS outside product-factor case、helper-level `zero - maybeDown` as a negative binding、sign-crossing/positive RHS、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | 拆分前 WSL gcc focused baseline：negative-offset-expression parser 28 tests/0 failures、zero-inclusive-sum parser 4 tests/0 failures、两条 focused LSP query PASS；拆分后同组 WSL gcc focused rerun PASS。最终验证：WSL gcc 当前活跃 self-dependency target matrix `RUN=53 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；WSL clang 当前活跃 self-dependency target matrix `RUN=53 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused rebuild/run `RUN=4 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`，可用 self-dependency executable run matrix `RUN=56 EXIT_FAILURES=0 FAILURE_MATCHES=0`，`VSCMD_VER=17.14.34`。首次 WSL gcc bin-directory matrix 只暴露旧构建目录里 8 个已改名 stale executables 的 `unknown target`，已改用当前构建目标清单复核；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Plus Strict-Negative Sum Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 06:38 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive sum 的剩余 strict-negative operand 子集补到 strict-negative validator：`zero=0..0; maybeDown=-1..0; maybeLower=-2..-1; other = (+narrowed) - (zero - (maybeDown + maybeLower)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 strict-negative offset validator：binary `+` 只有在一侧已由私有 zero-inclusive negative helper 证明为 bounded `min<0,max==0`，另一侧已证明为 strict-negative supported bounded offset，且 lower-bound addition 不下溢、不产生 `INT64_MIN`、结果上界仍严格小于 0 时，才把 `maybeDown + maybeLower` 映射为 strict-negative range；两种 operand order 均覆盖。外层 exact-zero-left `zero - (...)` 再映射为 positive bounded offset。positive-delta replay 保持 target `5..INT64_MAX`，reader/mirror 因 offset 至少为 1 而保持 `0..INT64_MAX-1`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片不放宽 zero-inclusive negative helper 本身；不声明 all-zero RHS outside product-factor case、helper-level `zero - maybeDown` as a negative binding、sign-crossing/positive RHS、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser strict-negative sum positive-delta 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；focused LSP strict-negative sum target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 WSL gcc focused parser zero-inclusive-sum 4 tests/0 failures，focused LSP zero-inclusive-sum query PASS。验证矩阵：WSL gcc available self-dependency executable matrix `RUN=63 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；WSL clang available self-dependency executable matrix `RUN=65 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused build/run 后 available self-dependency executable matrix `RUN=54 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Difference Strict-Positive Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 06:22 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一条 zero-inclusive difference 的剩余 strict-positive offset difference 子集补到 strict-negative validator：`zero=0..0; maybeDown=-1..0; maybeUnit=1..2; other = (+narrowed) - (zero - (maybeDown - maybeUnit)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 strict-negative offset validator：binary `-` 只有在左侧已由私有 zero-inclusive negative helper 证明为 bounded `min<0,max==0`，右侧已证明为 strict-positive supported bounded offset，且 `negativeMin - positiveMax` 不下溢、不产生 `INT64_MIN`、结果上界仍严格小于 0 时，才把 `maybeDown - maybeUnit` 映射为 strict-negative range；外层 exact-zero-left `zero - (...)` 再映射为 positive bounded offset。positive-delta replay 保持 target `5..INT64_MAX`，reader/mirror 因 offset 至少为 1 而保持 `0..INT64_MAX-1`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片不放宽 zero-inclusive negative helper 本身的 strict-positive RHS；不声明 all-zero RHS outside product-factor case、helper-level `zero - maybeDown` as a negative binding、sign-crossing/positive RHS、general multiplication/subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser strict-positive difference positive-delta 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；focused LSP strict-positive difference target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 WSL gcc focused parser zero-inclusive-difference 4 tests/0 failures，focused LSP zero-inclusive-difference query PASS。验证矩阵：WSL gcc available self-dependency executable matrix `RUN=63 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；WSL clang available self-dependency executable matrix `RUN=65 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused build/run 后 available self-dependency executable matrix `RUN=54 BUILD_FAILURES=0 EXIT_FAILURES=0 FAILURE_MATCHES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive All-Zero Product Negative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 05:58 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive product negative-offset 从 nonzero bounded factor 子集补到 exact-zero factor 子集：`zero=0..0; maybeDown=-1..0; zeroFactor=0..0; other = (+narrowed) - (zero - (maybeDown * zeroFactor))` 与 commuted `zeroFactor * maybeDown`; `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现仍限定在私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c`：binary `*` 仍要求恰好一侧递归证明为 bounded zero-inclusive negative range、另一侧证明为 supported bounded nonnegative offset；当 nonnegative offset 的 upper bound 为 0 时直接映射为 exact `[0,0]`，不进入乘法下溢路径；非零 upper bound 继续沿用 `negativeMin * offsetMax` 的 `INT64_MIN` guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 already-supported zero-inclusive negative operand 乘以 exact-zero bounded nonnegative factor；不声明两个 negative operands、sign-crossing/positive RHS 作为 negative binding、overflow-prone products、general zero-product/multiplication algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser all-zero product positive-delta 两个用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guards 保持通过；focused LSP all-zero product positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-product 8 tests/0 failures，focused LSP zero-inclusive-product query PASS。验证矩阵：WSL gcc focused product + available self-dependency executable matrix `RUN=59 EXIT_FAILURES=0 MISSING=0`；WSL clang focused product + available self-dependency executable matrix `RUN=59 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused product build/run PASS，available self-dependency executable matrix `RUN=54 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Difference Negative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 05:29 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive negative RHS 从 product 子集继续推进到受限 difference 子集：`zero=0..0; maybeDown=-1..0; maybeUnit=0..1; other = (+narrowed) - (zero - (maybeDown - maybeUnit)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c`：binary `-` 只有在左侧递归证明为 bounded zero-inclusive negative range、右侧已证明为 supported bounded nonnegative offset，且右侧 `min==0,max>0`、`negativeMin - offsetMax` 不下溢、不产生 `INT64_MIN` 时，才映射为 `[negativeMin-offsetMax,0]`；外层 exact-zero-left `zero - (...)` 再推导 `[0,max]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 already-supported zero-inclusive negative operand 减去 zero-inclusive nonnegative offset；不声明 strict-positive right offsets、nonzero upper bounds、all-zero RHS、两个 arbitrary signed operands、underflow-prone differences、general subtraction/algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser difference positive-delta 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；新 LSP difference query 的 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-difference 2 tests/0 failures，focused LSP zero-inclusive-difference query PASS。最终验证矩阵：WSL gcc focused+adjacent 45-target matrix `RUN=45 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 45-target matrix `RUN=45 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 45-target matrix `RUN=45 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Product Negative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 05:07 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive negative RHS 从 subtractive 子集继续推进到受限 product 子集：`zero=0..0; maybeDown=-1..0; factor=2..3; other = (+narrowed) - (zero - (maybeDown * factor))` 与 commuted `factor * maybeDown`; `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c`：binary `*` 只有在一侧递归证明为 bounded zero-inclusive negative range、另一侧已证明为 supported bounded nonnegative offset，且 `negativeMin * offsetMax` 不下溢、不产生 `INT64_MIN` 时，才映射为 `[negativeMin*offsetMax,0]`；外层 exact-zero-left `zero - (...)` 再推导 `[0,max]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明一个 zero-inclusive negative operand 被 bounded nonnegative offset 缩放；不声明 all-zero products、两个 negative operands、sign-crossing/positive RHS 作为 negative binding、overflow-prone products、general multiplication/algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser product positive-delta 两个用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guards 保持通过；新 LSP product query 的 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-product 4 tests/0 failures，focused LSP zero-inclusive-product query PASS。最终验证矩阵：WSL gcc focused+adjacent 43-target matrix `RUN=43 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 43-target matrix `RUN=43 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 43-target matrix `RUN=43 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。MSVC 一次性多目标构建中途返回 `-1` 且日志未见编译错误，改为逐目标构建/运行后获得上述干净汇总。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Subtractive Negative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 04:31 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive negative RHS 从 unary-negative 继续推进到受限 subtractive 子集：`zero=0..0; maybeUnit=0..1; other = (+narrowed) - (zero - (zero - maybeUnit)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c`：binary `-` 只有在左侧 exact zero、右侧已证明为 supported bounded nonnegative offset，且 `min==0`、`max>0` 时，才把 `zero - maybeUnit` 映射为 `[-max,0]`；外层 exact-zero-left `zero - (...)` 再推导 `[0,max]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 `0 - (0..N)` 形成 zero-inclusive negative RHS；不声明 nonzero-left subtraction、all-zero RHS、strict-positive-only 右侧在此 helper 内扩权、sign-crossing/negative operands、helper-level `zero - maybeDown` 作为 negative binding、general unary/subtraction algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser subtractive positive-delta 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；新 LSP subtractive query 的 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-subtractive 2 tests/0 failures，focused LSP zero-inclusive-subtractive query PASS。Windows 验证期间发现新增 parser 目标名过长导致 MSVC `C1083` 生成对象文件失败，缩短新增 subtractive parser/LSP CMake 目标名后 focused MSVC 2-target 构建运行通过。最终验证矩阵：WSL gcc focused+adjacent 37-target matrix `RUN=37 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 37-target matrix `RUN=37 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 37-target matrix `RUN=37 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Unary-Negative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 04:04 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive negative RHS 从 identity/sum 继续推进到受限 unary-negative 子集：`zero=0..0; maybeUnit=0..1; other = (+narrowed) - (zero - (-maybeUnit)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在私有 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c`：当 unary `-` 的 operand 已证明为 supported bounded nonnegative offset，且 `min==0`、`max>0` 时，才把它映射为 `[-max,0]`，随后仍由 exact-zero-left `zero - (...)` 推导 `[0,max]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 `-(0..N)` 形成 zero-inclusive negative RHS；不声明 strict-positive unary-negative 扩权、all-zero RHS、sign-crossing/negative operands、helper-level `zero - maybeDown` 作为 negative binding、general unary/subtraction algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 CFG-wide loop dataflow。 | TDD RED/GREEN：新增 parser unary positive-delta 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；新 LSP unary query 的 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-unary 2 tests/0 failures，focused LSP zero-inclusive-unary query PASS。验证矩阵：WSL gcc focused+adjacent 35-target matrix `RUN=35 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 35-target matrix `RUN=35 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 35-target matrix `RUN=35 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Prefix Offset Zero-Inclusive Negative Predicate Modularization | 已完成 | 2026-07-02 03:11 +08:00 | 按阶段 2 的模块维护门槛，在继续扩展语义前先拆分已接近 1000 行的 prefix offset 实现：把 zero-inclusive negative binding 递归识别和安全区间合并从 `type_inference_loop_assignment_prefix_offset.c` 移到私有模块 `type_inference_loop_assignment_prefix_zero_inclusive_negative.c/.h`。`ZrParser_TypeInferenceLoopAssignment_PrefixReaderExpressionIsSupportedBoundedOffset` 现在只通过私有入口 `ZrParser_TypeInferenceLoopAssignment_PrefixZeroInclusiveNegativeExpressionIsBinding` 调用该谓词，`prefix_offset.h` 的公开 API 不变；主文件从 980 行降到 845 行，新模块 148 行。该切片不扩展语义能力，仍只声明上一条 zero-inclusive negative sum-offset 的安全边界；不声明新的 RHS 形态、general offset normalizer、strict-negative validator 放宽、arbitrary algebra、fixed point 或 CFG-wide dataflow。 | 拆分前 WSL gcc 6-target baseline `RUN=6 EXIT_FAILURES=0`；拆分后 WSL gcc focused 6-target `RUN=6 EXIT_FAILURES=0`。最终矩阵：WSL gcc focused+adjacent 33-target matrix `RUN=33 EXIT_FAILURES=0`；WSL clang focused+adjacent 33-target matrix `RUN=33 EXIT_FAILURES=0`；Windows MSVC Visual Studio Debug focused+adjacent 33-target matrix `RUN=33 BUILD_FAILURES=0 EXIT_FAILURES=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Negative Sum Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 02:38 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 zero-inclusive negative RHS 从 exact-zero identity wrapper 推进到受限 sum 子集：`zero=0..0; maybeDown=-1..0; maybeLower=-2..0; other = (+narrowed) - (zero - (maybeDown + maybeLower)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 zero-inclusive negative binding helper：binary `+` 只有在左右两侧都递归证明为 zero-inclusive negative binding，且 lower-bound 相加不下溢、不等于 `INT64_MIN` 时，才合并为 `[leftMin+rightMin,0]`；随后仍由 exact-zero-left `zero - (...)` 映射为 `[0,3]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明两个已支持 zero-inclusive negative operands 的安全加法合并；不声明 nonzero identity、all-zero RHS、helper 内的 `zero - maybeDown` 作为 negative binding、sign-crossing 或 positive RHS、strict-negative validator 放宽、general subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser positive-delta sum 用例先失败为 target max `5` 而非 `INT64_MAX`，paired negative-delta guard 保持通过；新 LSP sum query 的 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-sum 2 tests/0 failures，focused LSP zero-inclusive-sum query PASS。验证矩阵：WSL gcc focused+adjacent 33-target matrix `RUN=33 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 33-target matrix `RUN=33 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 33-target matrix `RUN=33 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Negative Exact-Zero Identity Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 02:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片 `zero - maybeDown` 的 nonnegative bounded offset 支持推进到受限 exact-zero identity RHS wrapper 子集：`zero=0..0; maybeDown=-1..0; other = (+narrowed) - (zero - (maybeDown + zero))`、`other = (+narrowed) - (zero - (zero + maybeDown))`、`other = (+narrowed) - (zero - (maybeDown - zero)); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 zero-inclusive negative binding helper：binary `+` 只有在一侧递归证明 exact zero 时透明转发另一侧 zero-inclusive negative binding，binary `-` 只有在右侧 exact zero 时透明转发左侧；随后仍由 exact-zero-left `zero - (...)` 映射为 `[0,1]` 并复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 zero-inclusive negative RHS 的 exact-zero identity wrappers；不声明 nonzero identity、negative+negative 合并、helper 内的 `zero - maybeDown` 作为 negative binding、all-zero RHS、sign-crossing 或 positive RHS、strict-negative validator 放宽、general subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增窄 parser identity 目标中三个 positive-delta wrapper 用例先失败为 target max `5` 而非 `INT64_MAX`，negative-delta guard 用例保持通过；新 LSP identity query 首个 positive-delta target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser zero-inclusive-identity 6 tests/0 failures，focused LSP zero-inclusive-identity query PASS。验证矩阵：WSL gcc focused+adjacent 31-target matrix `RUN=31 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 31-target matrix `RUN=31 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 31-target matrix `RUN=31 EXIT_FAILURES=0 MISSING=0`，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Zero-Inclusive Negative Binding Nonnegative Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 01:49 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片仍排除的 zero-inclusive negative RHS 推进到受限 nonnegative bounded offset 子集：`zero=0..0; maybeDown=-1..0; other = (+narrowed) - (zero - maybeDown); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c`：新增 zero-inclusive negative binding helper，仅接受标识符或一元 `+` 包裹的 int64 range `min<0, max==0, min != INT64_MIN`；binary `-` 仍要求左侧 exact zero，才把 `zero - maybeDown` 映射为 `[0,1]` 并复用既有 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX`，因为 offset 可为 0；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 exact-zero-left minus zero-inclusive negative binding 形成 nonnegative bounded offset；不声明 nonzero-left subtraction、all-zero RHS、sign-crossing 或 positive RHS、strict-negative validator 放宽、general subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser `zero - maybeDown` upward 用例先失败为 target max `5` 而非 `INT64_MAX`；新 LSP target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 28 tests/0 failures，focused LSP zero-inclusive negative-offset query PASS。验证矩阵：WSL gcc focused+adjacent 29-target matrix PASS；WSL clang focused+adjacent 29-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 29-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Zero-Minus Negative Binding Positive Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-02 01:16 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 previously excluded `zero - negative` 推进到受限 positive bounded offset 子集：`zero=0..0; down=-2..-1; other = (+narrowed) - (zero - down); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 supported bounded nonnegative offset validator：binary `-` 只有在左侧 exact zero、右侧已递归证明为 supported strict-negative offset、且取反不会下溢时，才映射为 `[-negativeMax, -negativeMin]`；随后继续由 prefix reader 的 `target - positive offset` path 复用 target-subtract-offset guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX-1`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 exact-zero-left minus strict-negative RHS 形成 positive bounded offset；不声明 nonzero-left subtraction、zero-inclusive 或 non-strict negative RHS、`negativeMin == INT64_MIN`、general subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser `zero - down` upward 用例先失败为 target max `5` 而非 `INT64_MAX`；LSP target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 26 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc clean run-only 28-target matrix `RUN=28 EXIT_FAILURES=0 MISSING=0`；WSL clang focused+adjacent 28-target matrix `RUN=28 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 28-target matrix `VSCMD_VER=17.14.34 RUN=28 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Strict-Negative Sum Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-07-02 00:56 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 binary `-` right strict-negative offset support 从 exact-zero identity wrappers 推进到受限 strict-negative sum 子集：`down=-2..-1; lower=-3..-2; other = (+narrowed) - (down + lower); mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 strict-negative offset validator：binary `+` 只有在左右两侧都已递归证明为 supported strict-negative offset、相加不会下溢且结果 `offsetMin != INT64_MIN` 时才合并为 `[leftMin+rightMin, leftMax+rightMax]`；随后继续由 `type_inference_loop_assignment_prefix_reader.c` 的 binary `-` path 映射为 positive offset max 并复用 target-add-offset guard。negative-delta replay 保持 target `INT64_MIN..5`、reader/mirror `INT64_MIN+3..10`；同形 positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 supported strict-negative offset operands 的安全加法合并；不声明 `zero - negative`、nonzero identity、zero-inclusive 或 non-strict negative offsets、`offsetMin == INT64_MIN`、general subtraction/additive algebra、任意 offset normalizer、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser `down + lower` downward 用例先失败为 target min `5` 而非 `INT64_MIN`，LSP target/observer/mirror 先返回 `5..5`、`0..0`、`0..0`；修复后 focused parser negative-offset-expression 24 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 28-target build/run summary 为 `RUN=28 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，clean run-only rerun `RUN=28 EXIT_FAILURES=0 MISSING=0`；WSL clang 同矩阵 build/run summary 为 `RUN=28 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`，clean run-only rerun `RUN=28 EXIT_FAILURES=0 MISSING=0`；Windows MSVC Visual Studio Debug focused+adjacent 28-target matrix PASS，`VSCMD_VER=17.14.34`，`RUN=28 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Strict-Negative Exact-Zero Identity Offset LSP Query Split and Final Matrix Evidence | 已完成 | 2026-07-02 00:23 +08:00 | 按阶段 2 的数值区间/语义事实路线，在 exact-zero identity-wrapper 语义修复完成后补齐测试维护门槛：把新增的 LSP identity 查询覆盖从 992 行的 `test_lsp_numeric_loop_self_dependency_conditional_delta_negative_offset_expression_semantic_query.c` 拆到独立 `test_lsp_numeric_loop_self_dependency_conditional_delta_negative_offset_identity_semantic_query.c`，注册新目标 `zr_vm_lsp_selfdep_conditional_delta_negative_offset_identity_query_test` 并纳入 `language_server` 聚合；原 negative-offset-expression 查询回落到 668 行，新 identity 查询 348 行。语义边界保持上一条记录：只声明 strict-negative RHS 的 exact-zero identity wrappers before downward stable self-dependent sequence，不扩展 negative+negative、`zero - negative`、nonzero identity 或 general algebra。 | Focused WSL gcc：parser negative-offset-expression 22 tests/0 failures，negative-offset-expression query PASS，negative-offset-identity query PASS。最终验证矩阵：WSL gcc focused+adjacent 28-target matrix PASS；WSL clang focused+adjacent 28-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 28-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=28 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Strict-Negative Exact-Zero Identity Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-07-01 10:58 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 binary `-` right strict-negative offset support 推进到 exact-zero identity wrapper 子集：`down=-2..-1; other = (+narrowed) - (down + zero)`、`other = (+narrowed) - (zero + down)`、`other = (+narrowed) - (down - zero)`，其中 `zero=0..0`。实现限定在 `type_inference_loop_assignment_prefix_offset.c` 的 strict-negative offset validator：binary `+` 仅在一侧递归证明 exact zero 时透明转发另一侧 strict-negative offset，binary `-` 仅在右侧 exact zero 时透明转发左侧 strict-negative offset；随后继续由 `type_inference_loop_assignment_prefix_reader.c` 的 binary `-` path 映射为 positive offset max 并复用 target-add-offset guard。negative-delta replay 保持 target `INT64_MIN..5`、reader/mirror `INT64_MIN+1..7`；同形 positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 strict-negative RHS 的 exact-zero identity wrappers before downward stable self-dependent sequence；不声明 negative+negative 合并、`zero - negative`、nonzero identity、zero-inclusive 或 non-strict negative offsets、`offsetMin == INT64_MIN`、general subtraction/additive algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：临时旧行为下新增 parser `down + zero`、`zero + down`、`down - zero` 三个 downward 用例均失败为 target min `5` 而非 `INT64_MIN`，对应 positive-delta guard 保持通过；LSP 首个 identity-wrapped target/observer/mirror 失败为 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 22 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 27-target matrix PASS；WSL clang focused+adjacent 27-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 27-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=27 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Strict-Negative Binding / Unary-Negative Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-07-01 10:30 +08:00 | 按阶段 2 的数值区间/语义事实路线，补齐上一片 binary `-` right strict-negative offset support 的 discovery/GREEN 覆盖：`down=-2..-1; other = (+narrowed) - down; mirror = other; narrowed = narrowed - (choose ? step : step + bias)`，以及 `unit=1..2; other = (+narrowed) - (-unit)`。现有实现已通过 `type_inference_loop_assignment_prefix_reader.c` 的 binary `-` right strict-negative path 支持该形态：strict-negative binding 和 unary-negative supported positive offset 都由 `type_inference_loop_assignment_prefix_offset.c` 证明为严格负，再映射为 positive offset max 并复用 target-add-offset guard。negative-delta replay 保持 target `INT64_MIN..5`、reader/mirror `INT64_MIN+1..7`；同形 positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。本切片无生产代码改动；只声明 binary `-` 的 left target / right strict-negative binding 或 unary-negative positive offset before downward stable self-dependent sequence；不声明 `negative - target`、zero-inclusive 或 non-strict negative offsets、`offsetMin == INT64_MIN`、general subtraction/algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser negative-binding 与 unary-negative 两组 subtract strict-negative downward/positive guard 用例在现有实现下直接通过；focused parser negative-offset-expression 16 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 27-target matrix PASS；WSL clang focused+adjacent 27-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 27-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=27 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Subtract Strict-Negative Subtractive Offset Expression Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-07-01 09:55 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 prefix target-reader 的 binary `-` 从 `target - nonnegative offset` 推进到受限 `target - strict-negative offset` 子集：`zero=0..0; unit=1..2; other = (+narrowed) - (zero - unit); mirror = other; narrowed = narrowed - (choose ? step : step + bias)`，以及 bounded 形态 `pad=1..1; larger=2..3; other = (+narrowed) - (pad - larger)`。实现限定在 `type_inference_loop_assignment_prefix_reader.c`：当右侧能由 strict-negative offset validator 证明为严格负且 `offsetMin != INT64_MIN` 时，把减严格负偏移映射为正偏移上界 `-offsetMin`，再复用 `type_inference_loop_assignment_prefix_target_guard.c` 的 target-add-offset guard。negative-delta replay 后 target 保持 `INT64_MIN..5`，reader/mirror 保持 `INT64_MIN+1..7`；同形 positive-delta guard 保持 target `5..5`、reader/mirror `0..0`。该切片只声明 binary `-` 的 left target / right strict-negative offset expression before downward stable self-dependent sequence；不声明 `negative - target`、zero-inclusive 或 non-strict negative offsets、`offsetMin == INT64_MIN` 取反、general subtraction/algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser exact-zero 与 bounded 两个 subtract strict-negative downward 用例先失败为 target min `5` 而非 `INT64_MIN`，同形 positive-delta guard 通过；LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 12 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 27-target matrix PASS；WSL clang focused+adjacent 27-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 27-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=27 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Commuted Direct Target Reader Strict-Negative Subtractive Offset Expression Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 04:49 +08:00 | 按阶段 2 的数值区间/语义事实路线，补齐 exact-zero-left strict-negative subtractive offset expression 的 commuted discovery/GREEN 覆盖：`zero=0..0; unit=1..2; other = (zero - unit) + (+narrowed); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。现有实现已通过 binary `+` 的 left strict-negative offset / right target reader 分支支持该形态，strict-negative subtractive offset 仍由 `type_inference_loop_assignment_prefix_offset.c` 中 exact-zero-left / strict-positive-right binary `-` validator 映射为 `[-positiveMax,-positiveMin]`，再复用 `type_inference_loop_assignment_prefix_target_guard.c` target-add-negative guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX-1`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。本切片无生产代码改动；扩展 parser negative-offset-expression 覆盖和窄 LSP negative-offset-expression query。该切片只声明 commuted binary `+` 中 left exact-zero-minus-strict-positive negative offset / right target 的 upward safe prefix target-reader；不声明 nonzero-left subtraction、zero-inclusive right operand、bounded-left/right generalization 之外的新增行为、general negative expressions、sign-crossing/overflow-prone offsets、upward positive additive offsets、general subtraction/algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser commuted zero-minus positive offset positive-delta 与 negative-delta guard 用例在现有实现下直接通过；focused parser negative-offset-expression 8 tests/0 failures，focused LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 27-target matrix PASS；WSL clang focused+adjacent 27-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 27-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=27 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Commuted Direct Target Reader Bounded Strict-Negative Subtractive Offset Expression Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 04:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，补齐 commuted bounded strict-negative subtractive offset expression 的 discovery/GREEN 覆盖：`pad=1..1; larger=2..3; other = (pad - larger) + (+narrowed); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。现有实现已通过 binary `+` 的 left strict-negative offset / right target reader 分支支持该形态，strict-negative subtractive offset 仍由 `type_inference_loop_assignment_prefix_offset.c` 中 bounded binary `-` 且 `leftMax < rightMin` 的 validator 映射为 `[-2,-1]`，再复用 `type_inference_loop_assignment_prefix_target_guard.c` target-add-negative guard。positive-delta replay 保持 target `5..INT64_MAX`、reader/mirror `0..INT64_MAX-1`；同形 negative-delta guard 保持 target `5..5`、reader/mirror `0..0`。本切片无生产代码改动；新增 parser 覆盖并新增窄 LSP negative-offset-expression query 目标，避免继续扩展 906 行 target-positive query。该切片只声明 commuted binary `+` 中 left-negative/right-target 的 bounded strict-negative subtractive offset；不声明 target-side `(+narrowed) + (pad - larger)` 之外的新增行为、zero-inclusive 或 non-strict subtractive results、general negative expressions、sign-crossing/overflow-prone offsets、upward positive additive offsets、general subtraction/algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser commuted bounded strict-negative positive-delta 与 negative-delta guard 用例在现有实现下直接通过；focused parser negative-offset-expression 6 tests/0 failures，focused 新 LSP negative-offset-expression query PASS。验证矩阵：WSL gcc focused+adjacent 27-target matrix PASS；WSL clang focused+adjacent 27-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 27-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=27 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Bounded Strict-Negative Subtractive Offset Expression Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 03:52 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 strict-negative subtractive offset 从 exact-zero-left 推进到更一般但仍受限的 bounded positive-left/right 子集：`pad=1..1; larger=2..3; other = (+narrowed) + (pad - larger); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。当左右两侧都能证明为 supported bounded nonnegative offset 且 `leftMax < rightMin` 时，`pad - larger` 被安全映射为 strict-negative range `[-2,-1]`，replay 后 target 保持 `5..INT64_MAX`，bounded subtractive negative offset reader 保持 `0..INT64_MAX-1`。实现限定在 `type_inference_loop_assignment_prefix_offset.c`：strict-negative offset validator 接受 binary `-` 的 bounded-left/right form 并计算 `[leftMin-rightMax, leftMax-rightMin]`，再复用既有 target-add-negative guard；原 exact-zero-left 路径保留。该切片只声明 target-side binary `+` 中 bounded nonnegative subtraction 且结果严格负的 upward safe prefix target-reader；不声明 commuted `(pad - larger) + target`、zero-inclusive 或 non-strict subtractive results、general negative expressions、sign-crossing/overflow-prone offsets、upward positive additive offsets、general subtraction/algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。LSP target-positive query 当前 906 行，处于拆分观察区；后续继续扩展该文件前应优先拆分同职责 case。 | TDD RED/GREEN：新增 parser `(+narrowed) + (pad - larger)` upward 用例先失败为 target max `5` 而非 `INT64_MAX`，同形 negative-delta downward guard 保持 target `5..5`、observer/mirror `0..0`；LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 4 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 26-target matrix PASS；WSL clang focused+adjacent 26-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 26-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵均为 `RUN=26 BUILD_FAILURES=0 EXIT_FAILURES=0 MISSING=0`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Strict-Negative Subtractive Offset Expression Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 03:16 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 strict-negative offset 从绑定或 unary-negative supported offset 推进到受限 subtractive expression 子集：`zero=0..0; unit=1..2; other = (+narrowed) + (zero - unit); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `5..INT64_MAX`，zero-minus positive offset reader 保持 `0..INT64_MAX-1`。实现限定在新拆出的 `type_inference_loop_assignment_prefix_offset.c`：strict-negative offset validator 仅接受 binary `-` 且左侧递归证明 exact zero、右侧是 supported bounded nonnegative offset、右侧 min > 0，然后映射为 `[-positiveMax, -positiveMin]` 并复用既有 target-add-negative guard。新增窄 parser 目标 `zr_vm_numeric_loop_selfdep_conditional_delta_negative_offset_expression_dataflow_test`，避免继续扩展 909 行 target-positive parser 文件；LSP target-positive query 扩到 828 行。该切片只声明 target-side binary `+` 中 `exact-zero - strict-positive supported offset` 的 upward safe prefix target-reader；不声明 commuted `(zero - unit) + target`、nonzero-left subtraction、zero-inclusive right operand、general negative expressions、sign-crossing/overflow-prone offsets、upward positive additive offsets、general additive/unary algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser `(+narrowed) + (zero - unit)` upward 用例先失败为 target max `5` 而非 `INT64_MAX`，同形 negative-delta downward guard 先保持 target `5..5`、observer/mirror `0..0`；LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`。修复后 focused parser negative-offset-expression 2 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 26-target matrix PASS；WSL clang focused+adjacent 26-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 26-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Prefix Reader Offset Predicate Modularization | 已完成 | 2026-07-01 02:46 +08:00 | 按阶段 2 数值区间/语义事实路线的维护门槛，在继续扩展 prefix target-reader 之前，把接近 1000 行的 `type_inference_loop_assignment_prefix_reader.c` 中 exact-zero、supported bounded nonnegative offset、strict-negative offset、bitwise identity-mask offset predicate 拆到 `type_inference_loop_assignment_prefix_offset.c/.h`。`type_inference_loop_assignment_prefix_reader.c` 保留 RHS target-reader 提取与 target guard 调用，行数从 967 降到 273；新 offset 模块 688 行，继续复用既有 `type_inference_loop_assignment_prefix_target_guard.c/.h`。本阶段不新增语义覆盖，不改变已声明的 unary-minus negative-binding positive range-offset 行为边界；只是为后续 target-positive/test 扩展解除模块阈值风险。 | 拆分前 WSL gcc focused parser target-positive 12 tests/0 failures 且 focused LSP target-positive query PASS；拆分后同一 focused parser/LSP 仍 PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Unary Minus Negative Binding Positive Range-Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-07-01 02:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe positive additive offset 推进到受限 unary-minus over strict-negative binding 子集：`down=-2..-1; other = (+narrowed) + (-down); mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `INT64_MIN..5`，unary-minus negative-binding positive range-offset reader 保持 `INT64_MIN+1..7`。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c` 的 supported bounded nonnegative offset validator：unary `-` 只在 operand 已经通过 strict-negative offset validator 时，映射为 `[-negativeMax, -negativeMin]`，然后复用既有 target-add-offset guard；一般 double-negation algebra、negative operand 表达式泛化、zero-inclusive 或 sign-crossing 仍不接受。该切片复用窄 parser/LSP target-positive targets，新增 paired coverage，不新增构建目标；parser target-positive 当前 909 行，后续再扩展该测试文件前需要优先考虑拆分。该切片只声明 downward self-dependent sequence 之前的 target-side unary-minus over strict-negative binding positive offset prefix target-reader；不声明 commuted `(-negative) + target`、arbitrary unary-minus normalization、zero-inclusive/sign-crossing negation、overflow-prone offsets、upward positive additive offsets、general additive/unary algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `(+narrowed) + (-down)` downward 用例先失败为 target min `5` 而非 `INT64_MIN`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；同形 conditional positive-delta upward guard 用例保持 target `5..5`、observer/mirror `0..0`。修复后 focused parser target-positive 12 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Unary Negative Range-Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 01:39 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片仍排除的 direct unary negative offset expressions 推进到受限 `-(supported bounded nonnegative offset)` 子集：`unit=1..2; other = (+narrowed) + (-unit); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `5..INT64_MAX`，unary negative range-offset reader 保持 `0..INT64_MAX-1`。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c` 的 strict-negative offset validator：unary `-` 只在 operand 已经通过 supported bounded nonnegative offset validator 且 min > 0 时，映射为 `[-max, -min]`，然后复用既有 target-add-negative guard；`-(0..n)`、sign-crossing、overflow-prone 和一般 unary-minus algebra 仍不接受。该切片复用窄 parser/LSP target-positive targets，新增 paired coverage，不新增构建目标；prefix reader 当前 949 行，仍低于模块化阈值。该切片只声明 upward self-dependent sequence 之前的 target-side direct unary-negative safe offset prefix target-reader；不声明 commuted unary-negative `(-offset) + target`、zero-inclusive negation、negative operand double-negation、sign-crossing offsets、overflow-prone offsets、upward positive additive offsets、general additive/unary algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `(+narrowed) + (-unit)` upward 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；同形 conditional negative-delta downward guard 用例保持 target `5..5`、observer/mirror `0..0`。修复后 focused parser target-positive 10 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Commuted Target-Side Negative Range-Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 01:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片仍排除的 commuted negative `offset + target` 子集推进到受限 left-strict-negative/right-target range offset：`down=-2..-1; other = down + (+narrowed); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `5..INT64_MAX`，commuted negative range-offset reader 保持 `0..INT64_MAX-1`。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：binary `+` 在 target-left/right-negative-offset 之后也尝试 left-negative-offset/right-target，二者都复用 `type_inference_loop_assignment_prefix_target_guard.c/.h` 中既有 target-add-negative guard；仍要求 offset 是已绑定 int64 严格负 range、后续 self-dependent delta 向上、当前 target 下界加 offsetMin 不低于 int64 下界。该切片复用窄 parser/LSP target-positive targets，新增 paired coverage，不新增构建目标；prefix reader 当前 930 行，仍低于模块化阈值。该切片只声明 upward self-dependent sequence 之前的 commuted strict-negative additive range-offset prefix target-reader；不声明直接 unary negative offset 表达式、sign-crossing offsets、overflow-prone offsets、upward positive additive offsets、general additive algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `down + (+narrowed)` upward 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；同形 conditional negative-delta downward guard 用例保持 target `5..5`、observer/mirror `0..0`。修复后 focused parser target-positive 8 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Negative Range-Offset Conditional Positive Delta Replay / Downward Negative-Delta Guard | 已完成 | 2026-07-01 00:29 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片仍排除的 negative offsets 推进到受限 target-left strict-negative range offset 子集：`down=-2..-1; other = (+narrowed) + down; mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `5..INT64_MAX`，negative range-offset reader 保持 `0..INT64_MAX-1`。生产修复把 prefix reader 的 plan/target guard 与正/减偏移方向判断从 `type_inference_loop_assignment_prefix_reader.c` 抽到 `type_inference_loop_assignment_prefix_target_guard.c/.h`，原读取器降到 875 行；新增 strict-negative binding offset validator，只接受 binary `+` 的 target-left/right-negative-offset 形态，要求右侧 offset 是已绑定 int64 严格负 range、后续 self-dependent delta 向上、当前 target 下界加 offsetMin 不低于 int64 下界。该切片复用窄 parser/LSP target-positive targets，新增 paired coverage，不新增构建目标。该切片只声明 upward self-dependent sequence 之前的 target-side strict-negative additive range-offset prefix target-reader；不声明 commuted negative `offset + target`、直接 unary negative offset 表达式、sign-crossing offsets、overflow-prone offsets、upward positive additive offsets、general additive algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `(+narrowed) + down` upward 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；同形 conditional negative-delta downward guard 用例保持 target `5..5`、observer/mirror `0..0`。修复后 focused parser target-positive 6 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Commuted Target-Side Positive Range-Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-06-30 23:34 +08:00 | 按阶段 2 的数值区间/语义事实路线，把上一片仍排除的 commuted `offset + target` 子集推进到同一个受限 downward positive additive offset guard：`unit=1..2; other = unit + (+narrowed); mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `INT64_MIN..5`，commuted positive range-offset reader 保持 `INT64_MIN+1..7`。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：binary `+` 现在先尝试 target-left/right-offset，再尝试 left-offset/right-target，二者都复用既有 target-add-offset guard，仍要求 offset 是 supported bounded nonnegative range、后续 self-dependent delta lower 为负且 upper 不为正、当前 target 上界加 offsetMax 不溢出。该切片复用窄 parser/LSP target-positive targets，新增 paired coverage，不新增构建目标。该切片只声明 downward self-dependent sequence 之前的 commuted safe positive additive range-offset prefix target-reader；不声明 upward `target + positive` 或 `offset + target`、negative/sign-crossing offsets、overflow-prone offsets、general additive algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `unit + (+narrowed)` downward 用例先失败为 target min `5` 而非 `INT64_MIN`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；同形 conditional positive-delta upward guard 用例保持 target `5..5`、observer/mirror `0..0`。修复后 focused parser target-positive 4 tests/0 failures，focused LSP target-positive query PASS。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Positive Range-Offset Conditional Negative Delta Replay / Upward Positive-Delta Guard | 已完成 | 2026-06-30 23:01 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 从只支持 target-side subtractive offset 推进到受限 target-side positive additive offset 的向下 self-dependent sequence 子集：`unit=1..2; other = (+narrowed) + unit; mirror = other; narrowed = narrowed - (choose ? step : step + bias)`。该形态下 replay 后 target 保持 `INT64_MIN..5`，positive range-offset reader 保持 `INT64_MIN+1..7`，因为后续 self-dependent delta 是向下的、target 上界仍有限，`target + offsetMax` 能在 int64 上界内证明安全。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：新增 target-add-offset guard，只接受 target 在 binary `+` 左侧、右侧是 supported bounded nonnegative offset、后续 self-dependent delta lower 为负且 upper 不为正、当前 target 上界加 offsetMax 不溢出的形态；同形 conditional positive-delta upward sequence 仍保持 guarded，不会把 overflow-prone `target + positive` 当作 safe reader。新增窄 parser/LSP target-positive targets，并接入 CMake 与 language pipeline / language_server 聚合。该切片只声明 downward self-dependent sequence 之前的 safe target-side positive additive range-offset prefix target-reader；不声明 upward `target + positive`、commuted `offset + target`、negative/sign-crossing offsets、overflow-prone offsets、general additive algebra、非 identity complex prefix RHS、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新增 parser target-positive 用例先失败为 target min `5` 而非 `INT64_MIN`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser target-positive 2 tests/0 failures，focused LSP target-positive query PASS，并同时覆盖向上 positive-delta 同形表达式仍 guarded 为 target `5..5`、observer/mirror `0..0`。验证矩阵：WSL gcc focused+adjacent 25-target matrix PASS；WSL clang focused+adjacent 25-target matrix PASS；Windows MSVC Visual Studio Debug focused+adjacent 25-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Unary-Minus Exact-Zero Binding Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 22:20 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从受限 modulo same-identifier exact-zero identity operand 推进到受限 unary-minus wrapped exact-zero binding identity operand：`zero=0..0; unit=1..2; other = ((+narrowed) + (-zero)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片明确把 unary `-` exact-zero rewriting 留在未覆盖范围；本片只补一元 `-` 包住已经递归证明 exact zero 的表达式，仍不把非零负数、负 offset 或一般 unary-minus algebra 纳入 safe prefix target-reader。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 unary `+` 或 unary `-` 仅在 argument 已递归证明 exact zero 时返回 true；其余 target-name extraction、subtractive offset guard 与 replay 逻辑保持既有路径。旧 replay-resolved net-zero / net-negative target-reading LSP cases 已作为回归守卫重跑通过。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side unary-minus exact-zero binding identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 nonzero unary-minus normalization、negative/sign-crossing offsets、unary `-` over nonzero operands、nonzero identity operands、`target + positive`、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (-zero)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 11 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。回归守卫 `zr_vm_language_server_numeric_loop_self_dependency_semantic_query_test` PASS，覆盖旧 replay-resolved net-zero / net-negative target-reading cases。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Modulo Same-Identifier Exact-Zero Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 22:02 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从受限 subtractive same-identifier exact-zero identity operand 推进到受限 modulo same-identifier exact-zero identity operand：`factor=2..3; unit=1..2; other = ((+narrowed) + (factor % factor)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片只补同一标识符 `x - x => 0`；本片只补同一标识符、同 int64 range、且该标识符是 strict-positive supported bounded nonnegative offset 的 `x % x => 0` identity operand，避免 zero-inclusive divisor 进入除零风险路径。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c` 与 `type_inference.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 binary `%` 仅在左右都是同一标识符、该标识符可作为 supported bounded offset 且 min > 0 时返回 true；`ZrParser_BinaryExpressionType_Infer` 的 same-identifier exact-zero fold 只对 `-` 或 strict-positive `%` 发布 `0..0` 并重置 range segments，再落回 general numeric range。旧 replay-resolved net-zero / net-negative target-reading LSP cases 已作为回归守卫重跑通过。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side modulo same-identifier exact-zero identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 zero-inclusive divisor、different operands、`a%b` algebra、non-identifier structural equality、general modulo simplification、negative/sign-crossing non-target operands、unary `-` exact-zero rewrite、nonzero identity operands、`target + positive`、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (factor % factor)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 10 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。回归守卫 `zr_vm_language_server_numeric_loop_self_dependency_semantic_query_test` PASS，覆盖旧 replay-resolved net-zero / net-negative target-reading cases。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Subtractive Same-Identifier Exact-Zero Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 21:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从受限 bitwise-xor same-identifier exact-zero identity operand 推进到受限 subtractive same-identifier exact-zero identity operand：`factor=2..3; unit=1..2; other = ((+narrowed) + (factor - factor)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片只补 `x ^ x => 0`；本片只补同一标识符、且该标识符能通过 supported bounded nonnegative offset validator，同时 ordinary binary expression inference 能把同一 int64 标识符同范围 `x - x` 折叠为 exact `0..0` 的 identity operand。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c` 与 `type_inference.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 binary `-` 仅在左右都是同一标识符且该标识符可作为 supported bounded offset 时返回 true；`ZrParser_BinaryExpressionType_Infer` 只对 same-identifier int64 range-equal subtraction 发布 `0..0` 并重置 range segments，再落回 general numeric `-` range。旧 replay-resolved net-zero / net-negative target-reading LSP cases 已作为回归守卫重跑通过，intermediate write replay behavior 保持不收窄。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side subtractive same-identifier exact-zero identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 different operands、`a-b` algebra、non-identifier structural equality、general expression normalization、negative/sign-crossing non-target operands beyond supported bounded offset predicate、unary `-` exact-zero rewrite、nonzero identity operands、`target + positive`、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (factor - factor)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 9 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。回归守卫 `zr_vm_language_server_numeric_loop_self_dependency_semantic_query_test` PASS，覆盖旧 replay-resolved net-zero / net-negative target-reading cases。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Bitwise-Xor Same-Identifier Exact-Zero Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 19:46 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从受限 bitwise-and zero annihilator 推进到受限 bitwise-xor same-identifier exact-zero identity operand：`factor=2..3; unit=1..2; other = ((+narrowed) + (factor ^ factor)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片明确把 same-value bitwise algebra 与 `^` exact-zero claims 留在未覆盖范围；本片只补同一标识符、且该标识符能通过 supported bounded nonnegative offset validator 的 `x ^ x => 0` identity operand。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 binary `^` 仅在左右都是同一标识符且该标识符可作为 supported bounded offset 时返回 true；不做 different operands、general XOR range algebra、negative/sign-crossing operands、non-identifier structural equality 或 general bitwise expression normalization。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side bitwise-xor same-identifier exact-zero identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-` exact-zero rewriting、nonzero identity operands、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (factor ^ factor)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；初次 GREEN 构建暴露 helper 缺少前置声明，补声明后 focused parser 8 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Bitwise-And Exact-Zero Annihilator Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 19:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从受限 zero-product identity operand 推进到受限 bitwise-and exact-zero annihilator identity operand：`zero=0..0; factor=2..3; unit=1..2; other = ((+narrowed) + (zero & factor)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片明确把 bitwise zero expressions 留在未覆盖范围；本片只补 exact-zero binding 与已支持 bounded nonnegative operand 的安全 bitwise-and zero annihilator。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 binary `&` 仅在一侧递归证明 exact zero、另一侧能通过 `type_inference_loop_assignment_prefix_reader_expression_is_supported_bounded_offset` 时返回 true；不做 nonzero mask、same-value bitwise algebra、negative/sign-crossing operands 或 general bitwise expression normalization。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side bitwise-and exact-zero annihilator identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-` exact-zero rewriting、nonzero identity operands、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (zero & factor)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 7 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Exact-Zero Product Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 19:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从 additive exact-zero composition 推进到受限 zero-product identity operand：`zero=0..0; factor=2..3; unit=1..2; other = ((+narrowed) + (zero * factor)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片明确把 multiplication zero expressions 留在未覆盖范围；本片只补 exact-zero binding 因子乘以已支持 bounded nonnegative operand 的安全 identity operand。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 对 binary `*` 仅在一侧递归证明 exact zero、另一侧能通过 `type_inference_loop_assignment_prefix_reader_expression_is_supported_bounded_offset` 时返回 true；不做 nonzero multiplication cancellation、zero-product general algebra、negative/sign-crossing factors 或 general expression normalization。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side exact-zero product identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-` exact-zero rewriting、nonzero identity operands、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (zero * factor)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 6 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Additive Exact-Zero Composition Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 18:45 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero identity operand 从单个 exact-zero binding / unary-plus wrapped binding 推进到 additive exact-zero composition：`zero=0..0; pad=0..0; unit=1..2; other = ((+narrowed) + (zero + pad)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片已让 exact-zero predicate 识别 unary `+` wrapped exact-zero binding，但它仍不识别 `(zero + pad)` 这种双方都已证明为 exact zero 的 identity operand。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 只在 binary op 为 `+` 或 `-` 且左右两侧都已递归证明 exact zero 时才返回 true；unary `-`、nonzero cancellation、multiplication/bitwise zero expressions 与 general expression normalization 仍拒绝。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side additive exact-zero composition identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-` exact-zero rewriting、nonzero identity operands、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (zero + pad)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 5 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Unary-Plus Exact-Zero Binding Identity-Operand Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 18:24 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side exact-zero binding identity operand 从 bare `zero` 推进到 transparent unary-plus wrapped `(+zero)`：`zero=0..0; unit=1..2; other = ((+narrowed) + (+zero)) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。上一片已让 target-name extraction 在 target-side identity wrapper 中识别 exact-zero binding，但 exact-zero predicate 本身只认 literal zero 或 bare identifier binding，导致 `(+zero)` 未被视为 identity operand。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：`type_inference_loop_assignment_prefix_reader_expression_is_exact_zero_value` 只在 unary op 为 `+` 时递归检查 argument 是否 exact zero；unary `-` 仍拒绝。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side unary-plus exact-zero binding identity operand over unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-` exact-zero rewriting、nonzero identity operands、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + (+zero)) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 4 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Exact-Zero Binding Identity-Wrapper Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 18:05 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target-side identity wrapper 从 literal zero 推进到 type environment 中 exact `0..0` binding：`zero=0..0; unit=1..2; other = ((+narrowed) + zero) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。此前 RHS-level identity wrapper 已能用 exact-zero predicate 识别绑定零，但 `type_inference_loop_assignment_prefix_reader_supported_target_name` 的 target-name extraction 只剥 literal zero identity wrappers，导致 `((+narrowed) + zero) - unit` 未被归为 safe prefix target reader。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：把 compiler state 传入 supported target-name extraction，并在 target-side `+` / `-` identity wrapper 中复用既有 exact-zero value predicate；同时继续复用 target-side unary `+` target-name extraction 与 bounded nonnegative subtractive offset guard。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side exact-zero binding identity-wrapper over unary-plus subtractive range-offset prefix target-reader chain；不声明 nonzero identity operands、unary `-`、`target + positive`、sign-crossing/negative offsets、general expression normalization、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `((+narrowed) + zero) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 3 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader RHS Unary-Plus Wrapper Over Target-Side Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 17:40 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 RHS 从 target-side unary-plus subtractive offset 继续推进到整个 RHS 外层 unary-plus transparent wrapper：`unit=1..2; other = +((+narrowed) - unit)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。此前 target-name extraction 已能识别 `(+narrowed)`，offset-side validator 也能识别 unary `+`，但 `type_inference_loop_assignment_prefix_reader_expression_reads_supported_target` 只在 binary/target-name 层面递归，导致顶层 `+((+narrowed) - unit)` 未被归为 safe prefix target reader。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：仅当 RHS 顶层 unary op 为 `+` 时递归检查 inner expression 是否已经是 supported target-reader RHS；unary `-` 继续拒绝。该切片复用窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标，新增 paired coverage，不新增构建目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe RHS unary-plus wrapper over target-side unary-plus subtractive range-offset prefix target-reader chain；不声明 unary `-`、`target + positive`、sign-crossing/negative offsets、general unary/binary algebra、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `+((+narrowed) - unit)` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP RHS unary-plus wrapper target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 2 tests/0 failures，focused LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Target-Side Unary-Plus Identity-Wrapper Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 17:09 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 target side 从 bare identifier / zero identity wrapper 推进到 unary-plus transparent target-name wrapper：`unit=1..2; other = (+narrowed) - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。offset-side unary `+` 支持不覆盖 subtractive prefix reader 左侧的 target-name extraction；原 `type_inference_loop_assignment_prefix_reader_supported_target_name` 只识别 identifier 与 zero identity binary wrapper，导致 `(+narrowed) - unit` 未被归为 target reader。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c`：仅当 unary op 为 `+` 时递归读取 wrapped target name，然后复用既有 bounded nonnegative subtractive offset guards。为避免继续增长 859 行 parser 条件 delta 文件和 965 行 LSP 条件 delta 文件，本切片新增窄 parser target-unary dataflow 目标与窄 LSP target-unary query 目标。该切片只声明 stable supported upward self-dependent sequence 之前的 safe target-side unary-plus identity-wrapper subtractive range-offset prefix target-reader chain；不声明 unary `-`、`target + positive`、sign-crossing/negative offsets、general unary/binary algebra、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser `(+narrowed) - unit` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP target-side unary-plus target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 focused parser 1 test/0 failures，新增 LSP target-unary query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 23-target matrix PASS；WSL clang focused+adjacent 23-target matrix PASS；Windows MSVC Debug focused+adjacent 23-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=23、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Unary-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 16:20 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 modulo-plus supported nonnegative subexpression 推进到 unary-plus transparent supported nonnegative wrapper：`zero=0..0; unit=2..3; other = narrowed - (zero | (+unit))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。ordinary numeric range inference 已能保留 unary `+`，但 safe prefix target-reader bounded-offset validator 与 ordinary bitwise identity expression range publication 过去不会把 `+unit` 透明转发到已有 supported nonnegative range，导致 target 不 widened 且 observer/mirror 停在 `0..0`。生产修复限定在 `type_inference_loop_assignment_prefix_reader.c` 与 `type_inference_bitwise_identity.c`：只在 unary op 为 `+` 时递归读取已有 supported bounded/nonnegative range；不支持 unary `-`。该切片继续使用 parser bitwise arithmetic-offset 目标，并新增窄 LSP bitwise-unary-offset query 目标，避免继续增长 894 行历史 bitwise-offset query 文件。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over unary-plus range-offset prefix target-reader chain；不声明 unary `-`、sign-crossing/negative operands、general unary/binary algebra、`target + positive`、general bitwise range algebra、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (+unit)` target-reader 用例先失败为 target max `5` 而非 `INT64_MAX`，LSP nested unary-plus target/observer/mirror 先分别返回 `5..5`、`0..0`、`0..0`；修复后 parser 5 tests/0 failures，新增 LSP bitwise-unary-offset query PASS，并报告 target `5..INT64_MAX`、observer/mirror `0..INT64_MAX-2`。验证矩阵：WSL gcc focused+adjacent 22-target matrix PASS；WSL clang focused+adjacent 22-target matrix PASS；Windows MSVC Debug focused+adjacent 22-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=22、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Modulo-Plus Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 15:42 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 right-shift supported nonnegative subexpression 推进到受限 modulo-plus supported nonnegative subexpression：`zero=0..0; unit=6..8; factor=3..3; pad=1..1; other = narrowed - (zero | ((unit % factor) + pad))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 与普通 numeric range path 已能接受 safe nonnegative `%` offset；direct `%` range 是 zero-inclusive `0..2`，因此本切片用 `+ pad` 形成可观测的 positive `1..3` offset。ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `%` 子表达式，导致外层 left-zero `|` 不能发布 `((unit % factor) + pad)` 的 `1..3` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range 且 divisor min 严格大于 0 时接受 `%`，并发布 `0..min(left.max, divisor.max - 1)`；既有 `+ pad` path 再发布 `1..3`。它仍服务于 bitwise identity publication，不声明一般 arithmetic/range-set precision。该切片继续使用 parser bitwise arithmetic-offset 目标；LSP focused 目标追加 paired query。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over modulo-plus range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions beyond checked modulo-plus identity-publisher path、divisor-may-be-zero modulo、negative/sign-crossing modulo operands、exact residue/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | ((unit % factor) + pad)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-1`（`Expected 9223372036854775806 Was 9223372036854775807`），LSP nested modulo-plus target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 21-target matrix PASS；WSL clang focused+adjacent 21-target matrix PASS；Windows MSVC Debug focused+adjacent 21-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=21、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Right-Shift Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 15:13 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 left-shift supported nonnegative subexpression 推进到受限 right-shift supported nonnegative subexpression：`zero=0..0; unit=4..8; shift=1..1; other = narrowed - (zero | (unit >> shift))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 与普通 numeric range path 已能接受这个 safe nonnegative `>>` offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `>>` 子表达式，导致外层 left-zero `|` 不能发布 `(unit >> shift)` 的 `2..4` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range、shift count 非负且小于 int64 sign bit 时接受 `>>`，并按 `left.min >> right.max` 到 `left.max >> right.min` 发布 conservative shifted range；它仍服务于 bitwise identity publication，不声明一般 arithmetic/bitwise shift range publisher。该切片继续使用新拆出的 parser bitwise arithmetic-offset 目标；LSP focused 目标追加 paired query。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over right-shift range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、modulo arithmetic under bitwise identity、negative/sign-crossing shift operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (unit >> shift)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-2`，LSP nested right-shift target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。验证矩阵：WSL gcc focused+adjacent 21-target matrix PASS；WSL clang focused+adjacent 21-target matrix PASS；Windows MSVC Debug focused+adjacent 21-target matrix PASS，`VSCMD_VER=17.14.34`。首次 WSL gcc 矩阵被 unrelated active AOT build intermediate state 中的 stale `ZrCore_String_EqualNative` link error 阻断；当前源码已无该调用，单目标重建可过，修正临时脚本 pipefail 后重跑同一 21-target matrix PASS。最终三份矩阵日志均为 RUN=21、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Left-Shift Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 14:38 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 divided supported nonnegative subexpression 推进到受限 left-shift supported nonnegative subexpression：`zero=0..0; unit=1..2; shift=1..1; other = narrowed - (zero | (unit << shift))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 与普通 numeric range path 已能接受这个 safe nonnegative `<<` offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `<<` 子表达式，导致外层 left-zero `|` 不能发布 `(unit << shift)` 的 `2..4` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range、shift count 非负且小于 int64 sign bit、max 左移不溢出 int64 时接受 `<<` 并发布 checked shifted range；它仍服务于 bitwise identity publication，不声明一般 arithmetic/bitwise shift range publisher。该切片继续使用新拆出的 parser bitwise arithmetic-offset 目标，避免增长 943 行历史 parser bitwise-offset focused 文件；LSP focused 目标追加 paired query。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over left-shift range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、right-shift/modulo arithmetic under bitwise identity、negative/sign-crossing or overflow-prone shifts、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (unit << shift)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-2`，LSP nested left-shift target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。验证矩阵：WSL gcc focused+adjacent 21-target matrix PASS；WSL clang focused+adjacent 21-target matrix PASS；Windows MSVC Debug focused+adjacent 21-target matrix PASS，`VSCMD_VER=17.14.34`。首次 WSL gcc 矩阵被 unrelated active AOT source signature 中间态阻断，确认同一目标当前重建可过后重跑同一 21-target matrix PASS。最终三份矩阵日志均为 RUN=21、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Divided Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 14:02 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 multiplicative supported nonnegative subexpression 推进到受限 divided supported nonnegative subexpression：`zero=0..0; unit=6..8; factor=2..2; other = narrowed - (zero | (unit / factor))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受这个 offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `/` 子表达式，导致外层 left-zero `|` 不能发布 `(unit / factor)` 的 `3..4` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range 且 divisor min 严格大于 0 时接受 `/` 并发布 checked quotient range；它仍服务于 bitwise identity publication，不声明一般 arithmetic range publisher。为避免继续增长 943 行的历史 parser bitwise-offset focused 文件，本切片新增窄 parser bitwise arithmetic-offset 目标承接该除法 fixture；LSP focused 目标继续追加 paired query。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over divided range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、divisor-may-be-zero divisions、negative/sign-crossing operands、modulo/shift arithmetic under bitwise identity、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：新 parser nested `zero | (unit / factor)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-3`，LSP nested divided target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-3`。验证矩阵：WSL gcc focused+adjacent 21-target matrix PASS；WSL clang focused+adjacent 21-target matrix PASS；Windows MSVC Debug focused+adjacent 21-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=21、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Multiplicative Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 13:25 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 additive/subtractive supported nonnegative subexpression 推进到受限 multiplicative supported nonnegative subexpression：`zero=0..0; unit=1..2; factor=2..2; other = narrowed - (zero | (unit * factor))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受这个 offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `*` 子表达式，导致外层 left-zero `|` 不能发布 `(unit * factor)` 的 `2..4` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range 且 max product 不溢出 int64 时接受 `*` 并发布 checked product range；它仍服务于 bitwise identity publication，不声明一般 arithmetic range publisher。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over multiplicative range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、negative/sign-crossing operands、overflow-prone products、division/modulo/shift arithmetic under bitwise identity、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (unit * factor)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-2`，LSP nested multiplicative target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。验证矩阵：WSL gcc focused+adjacent 20-target matrix PASS；WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`。最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Subtractive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 12:31 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从 additive supported nonnegative subexpression 推进到受限 subtractive supported nonnegative subexpression：`zero=0..0; unit=2..3; pad=1..1; other = narrowed - (zero | (unit - pad))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受这个 offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `-` 子表达式，导致外层 left-zero `|` 不能发布 `(unit - pad)` 的 `1..2` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range 且 `leftMin >= rightMax` 时接受 `-` 并发布 checked nonnegative difference range；它仍服务于 bitwise identity publication，不声明一般 arithmetic range publisher。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over subtractive range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、negative/sign-crossing operands、underflow-prone subtraction、overflow-prone operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (unit - pad)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-1`，LSP nested subtractive target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-1`。验证矩阵：WSL gcc focused+adjacent 20-target matrix PASS；WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`。首次 WSL gcc 矩阵实际跑完 20 targets 并汇总 0 failures，但临时脚本因 CRLF 尾行返回 syntax error；改用 LF 临时脚本后同一矩阵重跑 PASS。最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Additive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 12:14 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 nested bitwise identity-zero offset RHS 从纯 bitwise identity 子表达式推进到受限 additive supported nonnegative subexpression：`zero=0..0; unit=2..3; pad=1..1; other = narrowed - (zero | (unit + pad))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受这个 offset，但 ordinary bitwise expression range publication 过去的 recursive supported range 不会读取 `+` 子表达式，导致外层 left-zero `|` 不能发布 `(unit + pad)` 的 `3..4` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在在左右都是 supported bounded nonnegative range 且 max sum 不溢出 int64 时接受 `+` 并发布 checked sum range；它仍服务于 bitwise identity publication，不声明一般 arithmetic range publisher。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over additive range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、general arithmetic subexpressions、subtractive/multiplicative/division/modulo/shift arithmetic under bitwise identity、negative/sign-crossing operands、overflow-prone sums、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (unit + pad)` target-reader 用例先失败为 observer/mirror max `INT64_MAX` 而非 `INT64_MAX-3`，LSP nested additive target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-3`。验证矩阵：WSL gcc focused+adjacent 20-target matrix PASS；WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`。初次 WSL gcc 矩阵因工具 300s 超时未写最终汇总但目标继续完成，重跑生成干净汇总；初次 MSVC Ninja 矩阵受 long-path/日志编码影响，一个 adjacent 目标构建失败，改用既有 Visual Studio generator Debug 目录后同一 20-target matrix PASS。最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Nested Commuted Bitwise Identity-Zero Over Identity-Mask Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 11:29 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 bitwise offset RHS 从单层 commuted `&` identity-mask 推进到嵌套 identity composition：`zero=0..0; mask=3..3; unit=2..3; other = narrowed - (zero | (mask & unit))`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受这个嵌套 offset，但 ordinary bitwise expression range publication 过去只识别 literal/binding 级 supported range，导致外层 left-zero `|` 不能读取内层 `(mask & unit)` 的 `2..3` range，observer/mirror 退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：`supported_nonnegative_range` 现在递归识别受限 bitwise identity 子表达式，包括 same-identifier `&`/`|` 幂等保留、same-identifier `^` exact-zero、`&` exact-zero annihilator、`&` all-ones identity-mask、以及 `|`/`^` exact-zero identity；递归仍只在 supported bounded nonnegative range 上成立。该切片只声明 stable supported upward self-dependent sequence 之前的 safe nested commuted bitwise identity-zero over identity-mask range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、non-identity/non-all-ones masks、unknown/different non-identity operands、negative/sign-crossing operands、arbitrary arithmetic subexpressions、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser nested `zero | (mask & unit)` target-reader 用例先失败为 observer/mirror max `INT64_MAX`，LSP nested target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。初次并行 WSL gcc/clang 矩阵被 unrelated active AOT source signature 中间态阻断；重试确认失败目标恢复后，按顺序重跑 WSL gcc focused+adjacent 20-target matrix PASS、WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`，首次 MSVC 矩阵仅因临时脚本无匹配计数 bug 停在第一个已通过目标，修正脚本后重跑通过。最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Commuted Bitwise-And Identity-Mask Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 10:54 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 bitwise-and identity-mask offset RHS 从右侧 mask 身份元推进到 left-mask commuted `&` composition：`mask=3..3; unit=2..3; other = narrowed - (mask & unit)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受 all-ones identity mask 在任一侧，但 ordinary bitwise expression range publication 过去默认复制左操作数，导致 left-mask `mask & unit` 的 observer/mirror 上界被当作减 `3` 而非减 `unit=2..3`。生产修复限定在 `type_inference_bitwise_identity.c`：当 `&` 两侧都是 supported bounded nonnegative range，且一侧是 exact singleton all-ones mask 覆盖另一侧 full range 时，发布被覆盖一侧的 min/max；`&` exact-zero annihilator 仍优先发布 exact `0..0`。该切片只声明 stable supported upward self-dependent sequence 之前的 safe commuted bitwise-and identity-mask range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、non-identity/non-all-ones masks、unknown/different non-identity operands、negative/sign-crossing operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser commuted `mask & unit` target-reader 用例先失败为 observer/mirror max `INT64_MAX-3`，LSP commuted AND target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX-3`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。WSL gcc focused+adjacent 20-target matrix PASS；WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`；首次 MSVC 矩阵由临时脚本状态码/产物路径问题误报失败，修正脚本后重跑通过。最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Commuted Bitwise Identity-Zero Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 10:38 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 bitwise identity-zero offset RHS 从右侧 exact-zero 身份元推进到左右可交换的 `|`/`^` exact-zero identity composition：`zero=0..0; unit=2..3; other = narrowed - (zero | unit)` 和 `other = narrowed - (zero ^ unit)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。prefix reader 已能接受 exact-zero 在任一侧，但 ordinary bitwise expression range publication 过去默认复制左操作数，导致 left-zero OR/XOR 的 observer/mirror 事实退化到 `0..INT64_MAX`。生产修复限定在 `type_inference_bitwise_identity.c`：当 `|` 或 `^` 一侧是 exact zero value、另一侧是 supported bounded nonnegative offset 时，发布非零侧的 min/max range；`&` zero-annihilator 仍发布 exact `0..0`。该切片只声明 stable supported upward self-dependent sequence 之前的 safe commuted bitwise-or/xor identity-zero range-offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、nonzero OR/XOR masks、unknown/different nonzero operands、negative/sign-crossing operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser commuted `zero | unit` / `zero ^ unit` target-reader 用例先失败为 observer/mirror max `INT64_MAX`，LSP commuted OR target/observer/mirror 中 observer/mirror 同样报告 `0..INT64_MAX`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX-2`。WSL gcc focused+adjacent 20-target matrix PASS；WSL clang 首次因并行 AOT 新文件触发 CMake glob 未重跑导致一个无关 AOT symbol link 失败，重跑后 focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`；最终三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Bitwise-And Zero-Annihilator Exact-Zero Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 10:12 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 bitwise exact-zero offset RHS 从受限 bitwise-xor same-identifier idempotent 推进到受限 bitwise-and zero-annihilator composition：`unit=2..3; zero=0..0; other = narrowed - (unit & zero); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`，其中 `unit & zero` 只在一侧是 exact zero value 且另一侧已是 supported bounded nonnegative offset 时折叠为 exact-zero offset `0..0`。生产修复分两层：`type_inference_loop_assignment_prefix_reader.c` 的 prefix reader validator 识别 `&` exact-zero annihilator 并用零 offset 做 target 下溢安全检查；`type_inference_bitwise_identity.c` 接收 compiler state，按当前 type environment 对 AST `x & 0` / `0 & x` 样式发布 `0..0`，使 observer/mirror 能随 replay 保持 `0..INT64_MAX`。该切片只声明 stable supported upward self-dependent sequence 之前的 safe bitwise-and exact-zero-annihilator offset prefix target-reader chain；不声明 `target + positive`、general bitwise range algebra、nonzero AND masks、unknown/different nonzero operands、negative/sign-crossing operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser bitwise-and zero-annihilator exact-zero offset target 先失败为 max `5`，LSP bitwise-offset target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX`。WSL gcc/clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Bitwise-Xor Idempotent Exact-Zero Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 09:44 +08:00 | 按阶段 2 的数值区间/语义事实路线，把 safe prefix target reader 的 bitwise offset RHS 从受限 bitwise-or idempotent same-identifier composition 推进到受限 bitwise-xor idempotent same-identifier exact-zero composition：`unit=2..3; other = narrowed - (unit ^ unit); mirror = other; narrowed = narrowed + (choose ? step : step + bias)`，其中 `unit ^ unit` 只在左右都是同一标识符且该标识符已是 supported bounded nonnegative offset 时折叠为 exact-zero offset `0..0`。生产修复分两层：`type_inference_loop_assignment_prefix_reader.c` 的 prefix reader validator 识别 same-identifier `^` 并用零 offset 做 target 下溢安全检查；新增 `type_inference_bitwise_identity.c` 从已经过大的 `type_inference.c` 抽出同名 bitwise identity range publication，使 AST `unit ^ unit` 查询发布 `0..0`，observer/mirror 能随 replay 保持 `0..INT64_MAX`。该切片只声明 stable supported upward self-dependent sequence 之前的 safe bitwise-xor idempotent same-identifier exact-zero offset prefix target-reader chain；不声明 `target + positive`、general XOR range algebra、different operands、negative/sign-crossing operands、exact bit-level/range-set precision、写入 delta dependency 的 prefix reader、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、true fixed point 或 general CFG-wide dataflow。 | TDD RED/GREEN：parser bitwise-xor idempotent exact-zero offset target 先失败为 max `5`，LSP bitwise-offset target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`；修复后 parser/LSP 均报告 `narrowed + 0` 为 `5..INT64_MAX`，`other + 0` / `return mirror + 0` 为 `0..INT64_MAX`。WSL gcc/clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`；三份矩阵日志均为 RUN=20、BUILD_FAILURES=0、EXIT_FAILURES=0、PRECISE_FAILURE_MATCHES=0。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Bitwise-Or Idempotent Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 09:11 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 bitwise-and same-identifier idempotent bounded offset 推进到受限 bitwise-or same-identifier idempotent bounded offset composition：`unit=2..3; other = narrowed - (unit | unit)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `|` over same identifier idempotent form，要求左右都是同一标识符且该标识符已经是 supported bounded nonnegative offset，然后把该标识符 min/max 当作组合 offset range。测试侧新增 parser bitwise-offset focused 目标，避免把 940 行的 parser offset-expression focused 文件继续推过项目拆分警戒线。一般复杂 RHS、`target + positive`、general bitwise range algebra、different operands、nonzero OR masks、negative/sign-crossing operands、exact bit-level/range-set precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive bitwise-or idempotent range-offset target 先失败为 max `5`，LSP bitwise-offset query target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser 1 PASS + LSP bitwise-offset focused PASS 与 20-target matrix PASS；WSL clang focused+adjacent 20-target matrix PASS；Windows MSVC Debug focused+adjacent 20-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Bitwise-And Idempotent Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 08:42 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 bitwise-xor identity-zero bounded offset 推进到受限 bitwise-and same-identifier idempotent bounded offset composition：`unit=2..3; other = narrowed - (unit & unit)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `&` over same identifier idempotent form，要求左右都是同一标识符且该标识符已经是 supported bounded nonnegative offset，然后把该标识符 min/max 当作组合 offset range。一般复杂 RHS、`target + positive`、general bitwise range algebra、different operands、non-identity masks、negative/sign-crossing operands、exact bit-level/range-set precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive bitwise-and idempotent range-offset target 先失败为 max `5`，LSP bitwise-offset query target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser 10 PASS + LSP bitwise-offset focused PASS 与 19-target matrix PASS；WSL clang focused+adjacent 19-target matrix PASS；Windows MSVC Debug focused+adjacent 19-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Bitwise-Xor Identity-Zero Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 07:47 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 bitwise-or identity-zero bounded offset 推进到受限 bitwise-xor identity-zero bounded offset composition：`unit=2..3; zero=0..0; other = narrowed - (unit ^ zero)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `^` over exact-zero identity form，要求一侧是 exact zero value，另一侧已经是 supported bounded nonnegative offset，然后把非零一侧 min/max 当作组合 offset range。新增 focused LSP bitwise-offset 查询目标承接此类覆盖，避免继续增长原 conditional-delta 查询文件。一般复杂 RHS、`target + positive`、general bitwise range algebra、nonzero XOR masks、negative/sign-crossing operands、exact bit-level/range-set precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive bitwise-xor identity-zero range-offset target 先失败为 max `5`，新增 LSP bitwise-offset query target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser 9 PASS + LSP bitwise-offset focused PASS 与 19-target matrix PASS；WSL clang focused+adjacent 19-target matrix PASS；Windows MSVC Debug focused+adjacent 19-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Bitwise-Or Identity-Zero Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 07:24 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 bitwise-and identity-mask bounded offset 推进到受限 bitwise-or identity-zero bounded offset composition：`unit=2..3; zero=0..0; other = narrowed - (unit | zero)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `|` over exact-zero identity form，要求一侧是 exact zero value，另一侧已经是 supported bounded nonnegative offset，然后把非零一侧 min/max 当作组合 offset range。一般复杂 RHS、`target + positive`、general bitwise range algebra、nonzero OR masks、negative/sign-crossing operands、exact bit-level/range-set precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive bitwise-or identity-zero range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser 8 PASS + LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Bitwise-And Identity-Mask Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 07:01 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 positive right-shift bounded offset 推进到受限 bitwise-and identity-mask bounded offset composition：`unit=2..3; mask=3..3; other = narrowed - (unit & mask)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `&` over already-supported bounded nonnegative offsets 的 identity-mask 子集，要求一侧是 exact nonnegative all-ones mask，另一侧 offset range 全部落在该 mask 内，然后把被 mask 保护的一侧 min/max 当作组合 offset range。一般复杂 RHS、`target + positive`、general bitwise range algebra、non-identity masks、negative/sign-crossing operands、exact bit-level/range-set precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive bitwise-and identity-mask range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Right-Shift Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 06:34 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从受限 positive left-shift bounded offset 推进到受限 positive right-shift bounded offset composition：`unit=4..8; shift=1..1; other = narrowed - (unit >> shift)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复先让 prefix-reader validator 接受 `>>` over already-supported bounded nonnegative offsets，要求 shift count 下界非负、上界小于 int64 sign-bit shift count，并按非负右移单调性计算 `left.min >> shift.max` 到 `left.max >> shift.min` 的 conservative range；随后补齐 bitwise expression dispatch 与 `type_inference_numeric_range.c` 的非负安全右移 range 发布，使 ordered prefix replay 后的 observer/mirror 事实能用 offset 下界收紧。一般复杂 RHS、`target + positive`、任意 shift forms、negative/sign-crossing shift operands、exact bit-level/range-set shift precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive right-shift range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Left-Shift Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 06:06 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从 zero-inclusive modulo bounded offset 推进到受限 positive left-shift bounded offset composition：`unit=1..2; shift=1..1; other = narrowed - (unit << shift)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复先让 prefix-reader validator 接受 `<<` over already-supported bounded nonnegative offsets，要求 shift count 下界非负、上界小于 int64 sign-bit shift count，且 `left.max << shift.max` 不会溢出；随后补齐 bitwise expression dispatch 与 `type_inference_numeric_range.c` 的非负安全左移 range 发布，使 ordered prefix replay 后的 observer/mirror 事实能用 offset 下界收紧。一般复杂 RHS、`target + positive`、right-shift/任意 shift forms、negative/sign-crossing shift operands、overflow-prone left shifts、exact bit-level/range-set shift precision、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive left-shift range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`；首次 GREEN 尝试暴露 observer/mirror 仍为 `0..INT64_MAX-1`，确认 bitwise `<<` 未进入 numeric range helper。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Zero-Inclusive Modulo Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 05:35 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从 divided bounded offset 推进到受限 zero-inclusive modulo bounded offset composition：`unit=6..8; factor=3..3; other = narrowed - (unit % factor)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `%` over already-supported bounded nonnegative offsets，要求右侧 divisor min 大于 `0`，结果保守为 `0..min(left.max, divisor.max - 1)`，且结果 max 继续进入当前 int64 target 下界减 offset 不下溢检查。一般复杂 RHS、`target + positive`、zero-divisor/divisor-may-be-zero modulo、negative/sign-crossing modulo、exact residue/range-set modulo precision、shift/arbitrary offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 zero-inclusive modulo range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Divided Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 05:19 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从 bounded multiplication 推进到受限 divided bounded offset composition：`unit=6..8; factor=2..2; other = narrowed - (unit / factor)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `/` over already-supported bounded nonnegative offsets，要求右侧 divisor min 大于 `0`，计算 `left.min / right.max` 到 `left.max / right.min` 的 quotient range，且结果 max 继续进入当前 int64 target 下界减 offset 不下溢检查。一般复杂 RHS、`target + positive`、zero-divisor/divisor-may-be-zero divisions、negative/sign-crossing divisions、modulo/shift/arbitrary offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive divided range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-3`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Multiplicative Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 05:05 +08:00 | 按阶段 2 语义推断核心路线，把 safe offset prefix reader 从 nonnegative subtractive bounded offset 推进到受限 multiplicative bounded offset composition：`unit=1..2; factor=2..2; other = narrowed - (unit * factor)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在接受 `*` over already-supported bounded nonnegative offsets，计算 min/max product，拒绝 max product 溢出 int64 的组合，且结果 max 继续进入当前 int64 target 下界减 offset 不下溢检查。一般复杂 RHS、`target + positive`、negative/sign-crossing 或 overflow-prone offset products、division/modulo/shift/arbitrary offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive multiplicative range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Subtractive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 04:48 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 从 additive bounded offset 推进到受限 subtractive bounded offset composition：`unit=2..3; pad=1..1; other = narrowed - (unit - pad)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：supported bounded offset 现在返回 min/max，`left - right` 只有在 `left.min >= right.max`、结果全程非负时才可作为 offset，且结果 max 继续进入当前 int64 target 下界减 offset 不下溢检查。parser 侧新增独立 offset-expression focused 目标承接后续同类覆盖，避免原 conditional-delta focused 文件继续跨阈值堆叠。一般复杂 RHS、`target + positive`、negative/sign-crossing 或 underflow-prone offsets、multiplicative/任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive subtractive range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 18-target matrix PASS；WSL clang focused+adjacent 18-target matrix PASS；Windows MSVC Debug focused+adjacent 18-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Exact Zero-Binding Identity-Expression Chain Conditional Delta Replay | 已完成 | 2026-06-30 04:29 +08:00 | 按阶段 2 语义推断核心路线，把 safe prefix target-reader identity expression 从字面量零推进到 exact zero int64 binding：`zero=0..0; other = narrowed + zero`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：新增带 type environment 的 exact zero value 判定，只有 int64、has range 且 `min=max=0` 的标识符可在 `target + zero`、`zero + target`、`target - zero` 这类 identity-wrapper 位置等价于零；无上下文的 supported target name 提取仍只剥字面量零。一般复杂 RHS、`target + positive`、nonzero bindings、negative/sign-crossing offsets、arbitrary offset expressions、overflow-prone offsets、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 zero-binding identity-expression target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Additive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 04:09 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 从单个 bounded offset 推进到受限 additive offset：`unit=1..2; pad=1..1; other = narrowed - (unit + pad)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 的 `+` 节点现在可在左右两侧都是 supported bounded nonnegative offset 时求 checked int64 max sum，并用该 sum 检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、negative/sign-crossing offsets、subtractive/multiplicative/任意 offset expressions、overflow-prone offset sums、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive additive range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-2`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Exact Zero-Only Identifier-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 03:54 +08:00 | 按阶段 2 语义推断核心路线，补齐 safe subtractive offset prefix reader 的 exact zero-only identifier offset 边界：`unit=0..0; other = narrowed - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 现在可为正整数字面量或 type environment 中 `min >= 0` 的 int64 有界 range 标识符，包含 exact `0..0`；target subtract guard 从 `offset > 0` 放宽到 `offset >= 0`，仍用 offset 上界检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、negative/sign-crossing offsets、任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 zero-only offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Zero-Inclusive Positive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 03:42 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 从 strictly positive bounded int64 range 绑定推进到 zero-inclusive positive bounded int64 range 绑定：`unit=0..1; other = narrowed - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 现在可为正整数字面量或 type environment 中 `min >= 0 && max > 0` 的 int64 有界 range 标识符，并用 offset 上界检查当前 int64 target 下界减 offset 不下溢；offset 侧零恒等包裹继续走递归剥离路径。一般复杂 RHS、`target + positive`、exact zero-only identifier offsets、negative/sign-crossing offsets、任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 zero-inclusive positive range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Positive Range-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 03:25 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 从 exact positive singleton 绑定推进到 positive bounded int64 range 绑定：`unit=1..2; other = narrowed - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 现在可为正整数字面量或 type environment 中 `min > 0` 的 int64 有界 range 标识符，并用 offset 上界检查当前 int64 target 下界减 offset 不下溢；offset 侧零恒等包裹继续走递归剥离路径。一般复杂 RHS、`target + positive`、zero/negative/sign-crossing offsets、任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 positive range-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Exact Positive Singleton-Offset Identity-Wrapper Chain Conditional Delta Replay | 已完成 | 2026-06-30 03:05 +08:00 | 按阶段 2 语义推断核心路线，把 exact positive singleton offset 再扩到 offset 端零恒等包裹：`unit=1..1; other = narrowed - (unit + 0)`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 现在可递归穿过 `+ 0`、`0 +`、`- 0` 这类零恒等包裹，再应用正整数字面量或 type environment 中 int64 exact positive singleton 标识符检查；仍要求该 target 后续存在非负向上 stable self-dependent sequence，并检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、non-singleton offsets、任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 singleton-offset identity-wrapper target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Exact Positive Singleton-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 02:53 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 从 literal-only 推进到 exact positive singleton 绑定：`unit=1..1; other = narrowed - unit`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：offset RHS 现在可为正整数字面量或 type environment 中 int64 exact positive singleton 标识符，并继续要求该 target 后续存在非负向上 stable self-dependent sequence；同时检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、non-singleton offsets、任意 offset expressions、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 singleton-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Constant-Offset Identity-Wrapper Chain Conditional Delta Replay | 已完成 | 2026-06-30 02:33 +08:00 | 按阶段 2 语义推断核心路线，把 safe subtractive offset prefix reader 再扩到零恒等包裹：`other = (narrowed - 1) + 0`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：在已受限的 `target - positive integer literal` 安全检查外，允许外层 `+ 0`、`0 +` 或 `- 0` 递归回到受支持 reader 表达式；仍要求该 target 后续存在非负向上 stable self-dependent sequence，并检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、任意非零偏移、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 constant-offset identity-wrapper target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Constant-Offset Chain Conditional Delta Replay | 已完成 | 2026-06-30 02:12 +08:00 | 按阶段 2 语义推断核心路线，把 pre-sequence target-reader chain 的第一跳从 `other = narrowed + 0` 扩到受限常量偏移 `other = narrowed - 1`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：新增 safe subtractive offset 追踪，只接受 identity-wrapped target 左侧再减正整数 literal，并继续要求该 target 后续存在非负向上 stable self-dependent sequence；同时检查当前 int64 target 下界减 offset 不下溢。一般复杂 RHS、`target + positive`、任意非零偏移、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 constant-offset target 先失败为 max `5`，LSP target/observer/mirror 初始为 `5..5`、`0..0`、`0..0`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。parser/LSP 最终报告 target `5..INT64_MAX`，observer/mirror `0..INT64_MAX-1`；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Identity-Expression Chain Conditional Delta Replay | 已完成 | 2026-06-30 01:53 +08:00 | 按阶段 2 语义推断核心路线，把 pre-sequence target-reader chain 的第一跳从 `other = narrowed` 扩到 `other = narrowed + 0`，后续仍为 `mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。生产修复限定在 prefix-reader validator：新增 identity-expression 追踪，只接受 `target + 0`、`0 + target`、`target - 0` 这类不改变 target range 的表达式包裹，并继续要求该 target 后续存在 stable supported self-dependent sequence；replay 仍由前一片的 widened envelope + ordered prefix replay 负责发布 reader。一般复杂 RHS、非零偏移 RHS、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 identity-expression target 先失败为 max `5`，LSP observer/mirror 初始仍为 `0..0`；LSP target query 后续改用 loop 后语句锚点避免命中 prefix RHS。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Chain Conditional Delta Replay | 已完成 | 2026-06-30 01:22 +08:00 | 按阶段 2 语义推断核心路线，把上一片 pre-sequence direct target reader 扩到一个普通 reader chain hop：`other = narrowed; mirror = other; narrowed = narrowed + (choose ? step : step + bias)`。`narrowed=5..5`、`other=0..0`、`mirror=0..0`、`step=1..3`、`bias=1..2` 时，target `narrowed + 0` 应 widen 到 `5..INT64_MAX`，prefix reader `other + 0` 与 chained reader `mirror + 0` / `return mirror + 0` 应 join 为 `0..INT64_MAX`。生产修复保持在 replay 窄路径：先发布 sequence target 的 widened/intermediate envelope，再按顺序重放 safe contiguous prefix steps，让 chained reader 读取 replay 更新后的绑定。复杂 RHS、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新 mirror 断言先失败为上界仍是 `5`；LSP 新查询 `return mirror + 0` 先返回 `0..5`。GREEN：WSL gcc focused parser/LSP PASS 与 17-target matrix PASS；WSL clang focused+adjacent 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Pre-Sequence Direct Target Reader Conditional Delta Replay | 已完成 | 2026-06-30 01:09 +08:00 | 按阶段 2 语义推断核心路线补齐 loop assignment replay 的前缀 direct target reader 组合：新增 `zr_vm_numeric_loop_selfdep_conditional_delta_dataflow_test` 与 `zr_vm_lsp_selfdep_conditional_delta_query_test`，覆盖 `other = narrowed; narrowed = narrowed + (choose ? step : step + bias)`。`narrowed=5..5`、`other=0..0`、`step=1..3`、`bias=1..2` 时，target `narrowed + 0` 应 widen 到 `5..INT64_MAX`，prefix reader `other + 0` / `return other + 0` 应 join 为 `0..INT64_MAX`。生产修复只扩展 join/replay 的窄前缀 reader 路径：同一 stable self-dependent sequence 之前的 contiguous direct target-reading ordinary assignment 可在 widened envelope 下重放；该 validator 已抽到 `type_inference_loop_assignment_prefix_reader.c/.h`，避免继续堆高 `type_inference_loop_assignment_join.c`。conditional positive delta 的范围支持本身不是本切片新增能力。复杂 RHS、prefix reader 写入 delta dependency、replay-resolved delta 组合、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | TDD RED：WSL gcc parser 新目标先失败为 target 上界仍是 `5`；LSP 新查询先返回 target `5..5` 与 observer `0..0`。GREEN：模块抽取后 WSL gcc focused parser/LSP PASS，WSL clang focused parser/LSP PASS；WSL gcc/clang self-dependency coefficient/product 相邻 17-target matrix PASS；Windows MSVC Debug focused+adjacent 17-target matrix PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Coefficient Residual Eight-Hop Ordinary Reader Chain Replay / No Fixed 4-Hop Cap Audit | 已完成 | 2026-06-29 23:54 +08:00 | 按阶段 2 语义推断核心路线继续推进 arbitrary-depth reader generalization 前的 fixed-hop-cap audit：审计现有 loop assignment replay/sequence/join 路径，确认 ordinary assignment replay 按 `plan->steps.length` 遍历，未发现固定 4-hop reader cap；随后复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=1..1` positive singleton seventh-wrapper fixture 从 `pulse = echo` 再扩展为 `phase = pulse; relay = phase; signal = relay; trace = signal`。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0`、`pulse + 0` 与 `trace + 0` / `return trace + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 tested positive singleton seventh-wrapper residual 的 eight-hop ordinary reader witness 与当前实现无固定 4-hop cap；arbitrary-depth correctness proof、其他 seventh-wrapper eight-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive singleton seventh-wrapper eight-hop ordinary reader witness 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`；构建进程已确认清空。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Coefficient Residual Four-Hop Reader Chain Replay | 已完成 | 2026-06-29 23:35 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional positive singleton reader chain 深度：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=1..1` positive singleton seventh-wrapper fixture 从 `echo = mirror` 再扩展一个 ordinary assignment hop 到 `pulse = echo`。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0`、`pulse + 0` / `return pulse + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 positive singleton seventh-wrapper residual 的 bounded four-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper four-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive singleton seventh-wrapper four-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`；构建进程已确认清空。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Only Scale-Product Exact No-Op Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 23:18 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader chain 的 zero-only exact no-op 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..0` exact-zero seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=0..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..5`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `0..5`。生产实现无需变更；该切片只声明 zero-only seventh-wrapper exact no-op 的 bounded three-hop reader chain；zero-only residual expansion、arbitrary zero-product algebra、arbitrary-depth reader generalization、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-only seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。一次 Windows adjacent matrix 命令被打断后清理遗留 MSBuild 并重跑通过；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Sign-Crossing Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 22:39 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..1` sign-crossing seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=-1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 sign-crossing seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP sign-crossing seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Negative Non-Singleton Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 22:19 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-2..-1` strict-negative seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=-2..-1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `-1..INT64_MAX`。生产实现无需变更；该切片只声明 strict-negative seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP strict-negative seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Negative Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 21:58 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..0` zero-inclusive negative seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=-1..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive negative seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive negative seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Positive Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 21:32 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..1` zero-inclusive positive seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=0..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive positive seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive positive seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Non-Singleton Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 21:10 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=2..3` strict-positive non-singleton seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=2..3`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `-4..INT64_MAX`。生产实现无需变更；该切片只声明 strict-positive non-singleton seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP strict-positive non-singleton seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Coefficient Residual Three-Hop Reader Chain Replay | 已完成 | 2026-06-29 20:52 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional reader chain 边界：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=1..1` positive singleton seventh-wrapper fixture 从 `mirror = other` 再扩展一个 ordinary assignment hop 到 `echo = mirror`。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0`、`mirror + 0`、`echo + 0` / `return echo + 0` 均为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 positive singleton seventh-wrapper residual 的 bounded three-hop reader chain；arbitrary-depth reader generalization、其他 seventh-wrapper three-hop coverage、arbitrary interleavings、nested/unsupported assignments、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive singleton seventh-wrapper three-hop reader chain coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Only Scale-Product Exact No-Op Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 20:42 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-only exact no-op two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..0` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=0..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..5`，`other + 0` 为 `0..5`，`mirror + 0` / `return mirror + 0` 也为 `0..5`。生产实现无需变更；该切片只声明 zero-only seventh-wrapper exact no-op 的一跳额外 ordinary reader propagation；zero-only residual expansion、arbitrary zero-product algebra、broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-only seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Sign-Crossing Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 20:31 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 sign-crossing two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..1` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=-1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `0..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 sign-crossing seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP sign-crossing seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Negative Non-Singleton Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 20:21 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 strict-negative two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-2..-1` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=-2..-1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `-1..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `-1..INT64_MAX`。生产实现无需变更；该切片只声明 strict-negative seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP strict-negative seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Negative Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 20:12 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-inclusive negative two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..0` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=-1..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `0..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive negative seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive negative seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Positive Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 20:01 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-inclusive positive two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..1` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=0..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `0..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive positive seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive positive seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Non-Singleton Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 19:44 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 strict-positive non-singleton two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=2..3` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=2..3`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `-4..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `-4..INT64_MAX`。生产实现无需变更；该切片只声明 strict-positive non-singleton seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP strict-positive non-singleton seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。并行 MSVC focused 构建曾因同 build dir `ZERO_CHECK` 文件锁失败，串行重跑通过；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Coefficient Residual Two-Hop Reader Propagation Replay | 已完成 | 2026-06-29 19:33 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 positive singleton two-hop case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=1..1` seventh-wrapper fixture 扩展为同时断言 target、immediate reader 与 `mirror = other` 的额外普通赋值 hop。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，target `narrowed + 0` 保持 `5..INT64_MAX`，`other + 0` 为 `0..INT64_MAX`，`mirror + 0` / `return mirror + 0` 也为 `0..INT64_MAX`。生产实现无需变更；该切片只声明 positive singleton seventh-wrapper residual 的一跳额外 ordinary reader propagation；broader reader chains beyond this one extra hop、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive singleton seventh-wrapper two-hop reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Only Scale-Product Exact No-Op Reader Propagation Replay | 已完成 | 2026-06-29 19:14 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-only exact no-op case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..0` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=0..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 精确为 `0..0`，target 保持 `5..5`，`other = narrowed` 的 observer 经 zero-or-more join 得到 `0..5`。生产实现无需变更；该切片只声明 zero-only seventh-wrapper exact no-op 的 immediate reader propagation；zero-only residual expansion、arbitrary zero-product algebra、broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-only seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 22 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 22 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Sign-Crossing Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 18:52 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 sign-crossing case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..1` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=-1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-1..1]`，tail `+step` 抵消一个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 捕获 tail 前 prefix 并经 zero-or-more join 得到 `0..INT64_MAX`。生产实现无需变更；该切片只声明 sign-crossing seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP sign-crossing seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Negative Non-Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 18:34 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 strict negative case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-2..-1` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=-2..-1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-2..2]`，tail `+(step + step)` 抵消两个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 捕获 tail 前 prefix 并经 zero-or-more join 得到 `-1..INT64_MAX`。生产实现无需变更；该切片只声明 strict negative seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP negative non-singleton seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Negative Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 18:23 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-inclusive negative case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=-1..0` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=-1..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-1..1]`，tail `+step` 抵消一个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 合并 zero-iteration `other=0` 与进入循环后的 widened prefix 得到 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive negative seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive negative seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Positive Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 18:07 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 zero-inclusive positive case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=0..1` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=0..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-1..1]`，tail `+step` 抵消一个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 合并 zero-iteration `other=0` 与进入循环后的 widened prefix 得到 `0..INT64_MAX`。生产实现无需变更；该切片只声明 zero-inclusive positive seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-inclusive positive seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Non-Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 17:51 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的 strict-positive non-singleton case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 `crown=2..3` seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=2..3`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-3..3]`，tail `+(step + step + step)` 抵消三个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 捕获 tail 前 prefix 并经 zero-or-more join 得到 `-4..INT64_MAX`。生产实现无需变更；该切片只声明 strict-positive non-singleton seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive non-singleton seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 17:31 +08:00 | 按阶段 2 语义推断核心路线补齐 seven-additional reader propagation 的首个 bounded case：复用 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，把 positive singleton seventh-wrapper fixture 扩展为同时断言 target 与 immediate reader。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 时，七层 product/coefficient 保守为 `[-1..1]`，tail `+step` 抵消一个 lower-bound exact `step` 项后 target 为 `5..INT64_MAX`，`other = narrowed` 的 observer 经 zero-or-more join 得到 `0..INT64_MAX`。生产实现无需变更；该切片只声明 positive singleton seventh-wrapper residual 的 immediate reader propagation；broader reader propagation、arbitrary interleavings、nested/unsupported assignments、arbitrary-depth reader generalization、target-dependent coefficients、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP positive singleton seventh-wrapper reader coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug focused parser/LSP PASS，coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-29 17:15 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 zero-only seventh wrapper `crown=0..0` 包住既有 nonzero-possible six-additional product。生产实现无需变更；现有 exact-zero product no-op replay 已可把七层 scale product 与 coefficient 识别为 `0..0`，并在无 tail cancellation 项时保持 target。`step=1..3`、`factor=-1..1`、`crown=0..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 product/coefficient 精确为 `0..0`；`+(step * coefficient); reader` 保持 target `5..5`。该切片只声明 zero-only seventh wrapper exact no-op over an accepted nonzero-possible six-additional product；zero-only residual expansion、arbitrary zero-product algebra、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP zero-only seventh-wrapper exact no-op coverage 在现有实现下直接通过；WSL gcc focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 7 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 7 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 16:59 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 sign-crossing seventh wrapper `crown=-1..1` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 nonzero-possible 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=-1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing seventh wrapper over an accepted nonzero-possible six-additional product；zero-only seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 sign-crossing case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run seven-additional parser 6 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 6 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 6 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 16:42 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 strict negative seventh wrapper `crown=-2..-1` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 nonzero non-crossing 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only/sign-crossing seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=-2..-1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 与 coefficient 保守为 `[-2..2]`；tail `+(step + step)` 抵消两个 lower-bound exact `step` 项后留下 `[0..4] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict negative seventh wrapper over an accepted nonzero-possible six-additional product；zero-only/sign-crossing seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 negative non-singleton case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run seven-additional parser 5 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 5 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 5 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 16:26 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 zero-inclusive negative seventh wrapper `crown=-1..0` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 positive-possible-or-zero-inclusive-negative 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only/strict-negative/sign-crossing seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=-1..0`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative seventh wrapper over an accepted nonzero-possible six-additional product；zero-only/strict-negative/sign-crossing seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 zero-inclusive negative case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run seven-additional parser 4 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 4 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 4 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 16:10 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 zero-inclusive positive seventh wrapper `crown=0..1` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 positive-possible 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only/zero-inclusive-negative/negative/sign-crossing seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=0..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive seventh wrapper over an accepted nonzero-possible six-additional product；zero-only/zero-inclusive-negative/negative/sign-crossing seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 zero-inclusive positive case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run seven-additional parser 3 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 3 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 3 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 15:44 +08:00 | 按阶段 2 语义推断核心路线继续推进 seven-additional 前沿：扩展 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 strict-positive non-singleton seventh wrapper `crown=2..3` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 strict-positive 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only/zero-inclusive/negative/sign-crossing seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=2..3`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 保守为 `[-3..3]`、coefficient 为 `[-3..3]`；tail `+(step + step + step)` 抵消三个 lower-bound exact `step` 项后留下 `[0..6] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive non-singleton seventh wrapper over an accepted nonzero-possible six-additional product；zero-only/zero-inclusive/negative/sign-crossing seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 positive non-singleton case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run seven-additional parser 2 PASS 与 LSP seven-additional PASS；WSL clang focused build/run seven-additional parser 2 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 2 PASS 与 LSP PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Seven-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-29 15:19 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 之后的 bounded arbitrary-depth 前沿：新增 `zr_vm_numeric_selfdep_coeff_cross_seven_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_seven_additional_scale_query_test`，覆盖 positive singleton seventh wrapper `crown=1..1` 包住既有 nonzero-possible six-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_extra_extra_extra_deeper_extension`，只在 seventh wrapper 为 positive singleton 且内层满足 six-additional nonzero-possible guard 时接受，不扩大到 zero-only/zero-inclusive/negative/sign-crossing seventh wrappers 或 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crown=1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的七层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive singleton seventh wrapper over an accepted nonzero-possible six-additional product；zero-only/zero-inclusive/negative/sign-crossing seventh wrappers、further arbitrary-depth products、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused seven-additional parser 新 case 失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP seven-additional query 返回 full int64。GREEN：WSL gcc focused build/run 新 parser seven-additional 1 PASS 与 LSP seven-additional PASS；WSL clang focused build/run 新 parser seven-additional 1 PASS 与 LSP seven-additional PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run 新 parser seven-additional 1 PASS 与 LSP seven-additional PASS，`VSCMD_VER=17.14.34`。相邻验证：WSL gcc/clang self-dependency coefficient/product 相关 20 targets PASS；Windows MSVC Debug coefficient/seven-additional/LSP 相邻 10 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Sign-Crossing Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 14:50 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional sign-crossing coefficient residual 的 reader propagation coverage，并先拆出新增 reader coverage 的 focused targets，避免继续扩张已到 999 行 size-watch 边界的旧 parser four-additional 文件：新增 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_reader_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_reader_query_test`，覆盖 `scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+step` sign-crossing fourth-wrapper residual fixture。新 parser/LSP reader targets 同时查询 target `narrowed + 0` 对应事实为 `5..INT64_MAX`，以及 `other = narrowed` 后的 `other + 0` / `return other + 0` observer 为 `0..INT64_MAX`。现有生产实现无需变更；sign-crossing fourth-wrapper residual replay 已能让 target 保持 upward widening，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `0..INT64_MAX`。该切片只声明 bounded sign-crossing fourth-wrapper residual 的 immediate reader propagation 与新增 reader target split；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | Discovery/GREEN：新增 parser/LSP sign-crossing reader coverage 在现有实现下直接通过；WSL gcc focused build/run 新 parser reader target 1 PASS 与 LSP reader query PASS；WSL clang focused build/run 新 parser reader target 1 PASS 与 LSP reader query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run 新 parser reader target 1 PASS 与 LSP reader query PASS，`VSCMD_VER=17.14.34`。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Negative Non-Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 14:28 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional negative non-singleton coefficient residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=-2..-1`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+(step + step)` strict-negative fourth-wrapper residual fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有生产实现无需变更；strict-negative fourth-wrapper residual replay 已能让 target 保持 upward widening `5..INT64_MAX`，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `-1..INT64_MAX`。该切片只声明 bounded negative non-singleton fourth-wrapper residual 的 immediate reader propagation；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | Discovery/GREEN：新增 parser/LSP negative non-singleton observer coverage 在现有实现下直接通过；WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build succeeded, first run used the wrong `tests\\Debug` path, then correct `bin\\Debug` parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Negative Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 14:13 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional zero-inclusive negative coefficient residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=-1..0`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+step` zero-inclusive negative fourth-wrapper residual fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有生产实现无需变更；zero-inclusive negative fourth-wrapper residual replay 已能让 target 保持 upward widening `5..INT64_MAX`，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `0..INT64_MAX`。该切片只声明 bounded zero-inclusive negative fourth-wrapper residual 的 immediate reader propagation；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | Discovery/GREEN：新增 parser/LSP zero-inclusive negative observer coverage 在现有实现下直接通过；WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Positive Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 14:01 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional zero-inclusive positive coefficient residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=0..1`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+step` zero-inclusive positive fourth-wrapper residual fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有生产实现无需变更；zero-inclusive positive fourth-wrapper residual replay 已能让 target 保持 upward widening `5..INT64_MAX`，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `0..INT64_MAX`。该切片只声明 bounded zero-inclusive positive fourth-wrapper residual 的 immediate reader propagation；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | RED/Correction/GREEN：WSL gcc focused parser initially exposed the new observer expectation should be `0..INT64_MAX` rather than `2..INT64_MAX` because zero-iteration `other=0` participates in the zero-or-more join；after correcting the coverage expectation, WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Non-Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 13:49 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional positive non-singleton coefficient residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=2..3`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+(step + step + step)` strict-positive fourth-wrapper residual fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有生产实现无需变更；strict-positive non-singleton fourth-wrapper residual replay 已能让 target 保持 upward widening `5..INT64_MAX`，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `-4..INT64_MAX`。该切片只声明 bounded positive non-singleton fourth-wrapper residual 的 immediate reader propagation；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | RED/Correction/GREEN：WSL gcc focused parser initially exposed the new observer expectation should be `-4..INT64_MAX` rather than `0..INT64_MAX`; after correcting the coverage expectation, WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Singleton Scale-Product Coefficient Residual Reader Propagation Replay | 已完成 | 2026-06-29 13:33 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional positive singleton coefficient residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1`、`factor=-1..1`、`step=1..3`、tail `+step` positive singleton fourth-wrapper residual fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有生产实现无需变更；positive singleton fourth-wrapper residual replay 已能让 target 保持 upward widening `5..INT64_MAX`，循环内 reader assignment 合并 zero-iteration `other=0` 与 loop-entry widened `narrowed`，得到 observer `0..INT64_MAX`。该切片只声明 bounded positive singleton fourth-wrapper residual 的 immediate reader propagation；不声明 broader reader propagation、arbitrary interleavings、nested/unsupported assignments、fixed point、general CFG-wide dataflow 或 broader coefficient algebra。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Subtractive Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 13:21 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional zero-only subtractive positive residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=0..0` subtractive `step - zero_product` fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；10:46 切片的 subtractive exact-zero product support 已能把被减掉的 zero product 视为 no-op 并保留 positive `step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 bounded exact-zero fourth-wrapper subtractive residual 后的 immediate reader propagation；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、broader reader propagation、general arbitrary-depth products、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Inner Zero Factor Scale-Product Same-Assignment Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 13:10 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional inner-zero same-assignment positive residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `cover=0..0` inner exact-zero factor `zero_product + step` fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；10:35 切片的 inner exact-zero factor support 已能丢弃 zero product term 并在同一 assignment 保留 `+step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 bounded inner exact-zero `cover=0..0` factor plus simple positive residual 后的 immediate reader propagation；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、broader reader propagation、general arbitrary-depth products、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Same-Assignment Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 12:58 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional zero-only same-assignment positive residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `scale=0..0` zero-product `+step` fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；10:21 切片的 exact-zero fourth-wrapper support 已能丢弃 zero product term 并在同一 assignment 保留 `+step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 exact-zero fourth-wrapper product plus simple positive residual 后的 immediate reader propagation；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、broader reader propagation、general arbitrary-depth products、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Commuted Product Self-Canceling Factor Scale-Product Same-Assignment Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 12:51 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional commuted product self-canceling factor positive residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `((cover * mask) - (mask * cover)) * gate` fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；11:23 切片的 product-term multiset equality 已能把 commuted target-independent product factor 证明为 exact zero，并在同一 assignment 保留 `+step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 target-independent commuted binary-product self-canceling factor plus simple positive residual 后的 immediate reader propagation；不声明 arbitrary zero-product algebra、distribution、target-dependent zero products、broader reader propagation、general arbitrary-depth products、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Self-Canceling Factor Scale-Product Same-Assignment Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 12:40 +08:00 | 按阶段 2 语义推断核心路线继续补 four-additional self-canceling factor positive residual 的 reader propagation coverage：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中扩展既有 `(cover - cover) * (mask * gate)` fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；11:04 切片的 target-independent `x - x` exact-zero factor support 已能在同一 assignment 保留 `+step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 target-independent self-canceling factor plus simple positive residual 后的 immediate reader propagation；不声明 arbitrary zero-product algebra、target-dependent zero products、broader reader propagation、general arbitrary-depth products、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused four-additional build/run parser 13 PASS 与 LSP four-additional query PASS；WSL clang focused build/run parser 13 PASS 与 LSP four-additional query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser 13 PASS 与 LSP four-additional query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Target Self-Canceling Product Factor Positive Residual Reader Propagation Replay | 已完成 | 2026-06-29 12:32 +08:00 | 按阶段 2 语义推断核心路线继续补 direct target self-canceling product factor 的 reader propagation coverage：在 `zr_vm_numeric_loop_self_dependency_symbolic_product_dataflow_test` 与 `zr_vm_language_server_numeric_loop_self_dependency_symbolic_product_semantic_query_test` 中扩展既有 fixture，除 target `narrowed + 0` 外新增 `other = narrowed` 后的 `other + 0` / `return other + 0` 查询。现有实现无需生产代码变更；12:07 切片的 direct `target - target` exact-zero product factor support 已能在同一 assignment 保留 `+step` residual，循环内 reader assignment 捕获从零次循环 `other=0` 与进入循环后 `narrowed` upward widening 合并出的 `0..INT64_MAX`。该切片只声明 direct target self-canceling product factor plus simple positive residual 后的 immediate reader propagation；不声明 arbitrary target-dependent zero-product algebra、broader reader propagation、distribution、general target coefficients、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：WSL gcc focused product targets rebuilt before rerun，parser product 10 PASS，LSP product query PASS；WSL clang focused build/run parser product 10 PASS 与 LSP product query PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run parser product 10 PASS 与 LSP product query PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Target Self-Canceling Product Factor Positive Residual Replay | 已完成 | 2026-06-29 12:07 +08:00 | 按阶段 2 语义推断核心路线继续收窄 target-dependent zero-product 边界：在 `zr_vm_numeric_loop_self_dependency_symbolic_product_dataflow_test` 与 `zr_vm_language_server_numeric_loop_self_dependency_symbolic_product_semantic_query_test` 中新增 direct target self-canceling product factor positive residual fixtures。`type_inference_loop_assignment_target_zero.c/.h` 新增 narrow internal support，只把 product factor 中的直接 `target - target` 当作 exact zero，并让 self-dependent delta range recognition 与 plan target-use validation 共用该边界；`type_inference_loop_assignment_sequence_symbolic_delta.c` 的 singleton coefficient path 也识别直接 target self-canceling zero factor 并沿 `*` 传播 zero singleton。`step=1..3`、`factor=-2..2` 下 `narrowed = narrowed + ((step * ((narrowed - narrowed) * factor)) + step); other = narrowed; narrowed + 0` 推断为 target `5..INT64_MAX`。该切片只声明 direct target identifier subtraction `target - target` as exact zero product factor plus simple positive identifier residual in the same assignment；不声明 arbitrary target-dependent algebra、distribution、general zero-product algebra、general target-dependent coefficients、broader symbolic simplification、fixed point 与 general CFG-wide dataflow。 | RED：WSL gcc focused product parser 旧 9 cases PASS 后新增 case 失败 `Expected 9223372036854775807 Was 5`，LSP product query 返回 `5..5`。GREEN：WSL gcc focused product parser 10 PASS，LSP product query PASS；WSL gcc/clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` build/run adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Commuted Product Self-Canceling Factor Scale-Product Same-Assignment Positive Residual Replay | 已完成 | 2026-06-29 11:23 +08:00 | 按阶段 2 语义推断核心路线继续收窄 target-independent zero-product residual 前沿：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 commuted product self-canceling factor same-assignment positive residual fixtures。现有实现无需生产代码变更；前序 singleton coefficient path 已能对 target-independent `x - x` 调用 delta expression equality，而 equality 的 product-term multiset matching 可证明 `cover * mask` 与 `mask * cover` 等价，因此 `((cover * mask) - (mask * cover)) * gate` 作为 exact-zero factor 被丢弃，同一 assignment 中的 `+ step` residual 保留，最终 `narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested target-independent commuted binary-product self-canceling factor inside the four-additional product plus simple positive identifier residual in the same assignment；不声明 arbitrary zero-product algebra、general symbolic simplification/distribution、target-dependent zero products、general zero-only residual expansion beyond this additive shape、general arbitrary-depth products、nested term product beyond supported product-term equality、sign-crossing base term、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser/LSP commuted product self-canceling factor fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 13 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` build/run adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Self-Canceling Factor Scale-Product Same-Assignment Positive Residual Replay | 已完成 | 2026-06-29 11:04 +08:00 | 按阶段 2 语义推断核心路线继续收窄 zero-product residual 前沿：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 self-canceling factor same-assignment positive residual fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_delta.c` 的 singleton coefficient path 中识别 target-independent `x - x` 为 exact zero，再把 zero singleton 沿乘法系数传播。`cover=1..3` 下 `(cover - cover) * (mask * gate)` 现在能作为 exact-zero factor，使 `narrowed = narrowed + ((step * (factor * (scale * (outer * (span * ((cover - cover) * (mask * gate))))))) + step)` 丢弃 zero product term 并保留同一 delta 表达式中的 `+ step`，最终 `narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested target-independent self-canceling subtraction factor inside the four-additional product plus simple positive identifier residual in the same assignment；不声明 arbitrary zero-product algebra、general symbolic simplification、target-dependent zero products、general zero-only residual expansion beyond this additive shape、general arbitrary-depth products、nested term product、sign-crossing base term、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | RED：WSL gcc focused four-additional parser 前 11 个 cases PASS，新增 self-canceling factor case 失败为 `Expected 5 Was -9223372036854775808`；LSP four-additional query 返回 full int64。GREEN：WSL gcc focused four-additional parser 12 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Subtractive Positive Residual Replay | 已完成 | 2026-06-29 10:46 +08:00 | 按阶段 2 语义推断核心路线继续收窄 zero-product residual 前沿：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 subtractive positive residual fixtures。现有实现无需生产代码变更；`scale=0..0` 让 `step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))` 成为 exact-zero product 时，`narrowed = narrowed + (step - zero_product)` 保留正向 `step` residual，最终 `narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested `step - exact_zero_product` same-assignment shape with exact-zero fourth wrapper；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、general zero-only residual expansion beyond this subtractive shape、general arbitrary-depth products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser/LSP four-additional zero-only subtractive positive residual fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 11 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Inner Zero Factor Scale-Product Same-Assignment Positive Residual Replay | 已完成 | 2026-06-29 10:35 +08:00 | 按阶段 2 语义推断核心路线继续收窄 zero-product residual 前沿：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 inner-zero factor same-assignment positive residual fixtures。现有实现无需生产代码变更；`cover=0..0` 位于 `scale * (outer * (span * (cover * (mask * gate))))` 内层时，整个 scale product 与 `factor * (...)` coefficient 仍被推断为 singleton `0..0`，residual collector 丢弃 zero product term 并保留同一 delta 表达式中的 `+ step`，最终 `narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested inner exact-zero `cover=0..0` factor inside the four-additional product plus simple positive identifier residual in the same assignment；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、general zero-only residual expansion beyond this additive shape、general arbitrary-depth products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser/LSP four-additional inner-zero same-assignment positive residual fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 10 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Same-Assignment Positive Residual Replay | 已完成 | 2026-06-29 10:21 +08:00 | 按阶段 2 语义推断核心路线继续收窄 zero-only residual expansion 前沿：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 same-assignment positive residual fixtures。现有实现无需生产代码变更；`scale=0..0` 包住 accepted nonzero-possible three-additional product 时，`step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))` 的 coefficient 被推断为 singleton `0..0` 并作为 exact-zero term 由 residual collector 丢弃，同一 delta 表达式中的 `+ step` 保留下来，最终 `narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested `zero_product + step` same-assignment shape with exact-zero fourth wrapper；不声明 arbitrary zero-product algebra、non-singleton zero-equivalent products、general zero-only residual expansion beyond this additive shape、general arbitrary-depth products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser/LSP four-additional zero-only same-assignment positive residual fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 9 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Then Positive Delta Replay | 已完成 | 2026-06-29 10:08 +08:00 | 按阶段 2 语义推断核心路线补齐 four-additional zero-only prefix + positive-delta 相邻边界：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 zero-only fourth-wrapper then-positive-delta fixtures。现有推断无需生产代码变更；`scale=0..0` 包住 accepted nonzero-possible three-additional product (`outer=1..1`, `span=1..1`, `cover=1..1`, `mask=-1..1`, `gate=1..1`) 时，whole scale product/coefficient 仍为 exact `0..0`，循环内 target-reading assignment 与 `other = narrowed` 只作为 no-op prefix，后续独立 `narrowed = narrowed + step` 复用既有 positive self-dependent delta widening，`narrowed + 0` 推断为 `5..INT64_MAX`。该切片只声明 tested exact-zero fourth wrapper prefix followed by a separate positive identifier delta；不声明 general zero-only residual expansion、zero-product symbolic residual inside the product、arbitrary zero-product algebra、general arbitrary-depth products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow。 | Discovery/GREEN：新增 parser/LSP four-additional zero-only-prefix then-positive-delta fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 8 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang adjacent self-dependency 20 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，adjacent self-dependency 20 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-29 09:52 +08:00 | 按阶段 2 语义推断核心路线补齐 four-additional arbitrary-depth 前沿的 zero-only no-op 边界：在 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test` 中新增 zero-only fourth-wrapper fixtures。现有 no-op replay 路径无需生产代码变更；`scale=0..0` 包住 accepted nonzero-possible three-additional product (`outer=1..1`, `span=1..1`, `cover=1..1`, `mask=-1..1`, `gate=1..1`) 时，whole scale product/coefficient 均为 exact `0..0`，循环内 target-reading assignment 不改变 `narrowed`，后续 `narrowed + 0` 保持 `5..5`。该切片只声明 tested zero-only fourth wrapper exact no-op over an accepted nonzero-possible three-additional product；zero-only residual expansion、arbitrary zero-product algebra、general arbitrary-depth products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP four-additional zero-only fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused four-additional parser 7 PASS，LSP four-additional query PASS；WSL gcc 与 WSL clang self-dependency 相关 26 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 17:08 +08:00 | 按阶段 2 语义推断核心路线补齐 deeper chained scale-product family 的 zero-only no-op 边界：在 `zr_vm_numeric_selfdep_coeff_cross_deeper_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_deeper_scale_query_test` 中新增 chained zero-only fixtures。现有 no-op replay 路径无需生产代码变更；`scale=0..0` 包住 `mask=1..1`、`gate=1..1` 的 accepted two-factor chained product 时，whole scale product/coefficient 均为 exact `0..0`，循环内 target-reading assignment 不改变 `narrowed`，后续 `narrowed + 0` 保持 `5..5`。该切片只声明 tested chained zero-only exact no-op over an accepted two-factor product；zero-only residual expansion、arbitrary zero-product algebra、arbitrary deeper scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP chained zero-only fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused deeper parser 28 PASS，LSP deeper query PASS；WSL gcc 与 WSL clang self-dependency 相关 26 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:53 +08:00 | 按阶段 2 语义推断核心路线补齐 deeper chained scale-product family 的 sign-crossing 相邻边界：在 `zr_vm_numeric_selfdep_coeff_cross_deeper_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_deeper_scale_query_test` 中新增 chained sign-crossing fixtures。现有 bounded scale-product guard 已可接受 `scale=-1..1` 包住 `mask=1..1`、`gate=1..1` 的 accepted two-factor chained product，无需生产代码变更；`step=1..3`、`factor=-1..1` 时 whole coefficient 保守为 `[-1..1]`，tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 tested chained sign-crossing one-additional scale-product extension over an accepted two-factor product；zero-only chained no-op/residual、arbitrary deeper scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 parser/LSP chained sign-crossing fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused deeper parser 27 PASS，LSP deeper query PASS；WSL gcc 与 WSL clang self-dependency 相关 26 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 16:35 +08:00 | 按阶段 2 语义推断核心路线补齐 six-additional 前沿的 zero-only no-op 边界：在 six-additional parser/LSP focused targets 中新增 zero-only sixth-wrapper fixtures。现有 no-op replay 逻辑无需生产代码变更；`crest=0..0` 包住 accepted nonzero-possible five-additional product 时整体 scale product 与 coefficient 确定为 `0..0`，循环内 `narrowed = narrowed + (step * (factor * (crest * (shell * (scale * (outer * (span * (cover * (mask * gate))))))))); other = narrowed;` 不改变 target，后续 `narrowed + 0` 保持 `5..5`。该切片只声明 tested zero-only sixth wrapper exact no-op over an accepted nonzero-possible five-additional product；zero-only residual expansion、arbitrary zero-product algebra、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 zero-only parser/LSP fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused six-additional parser 7 PASS，LSP six-additional query PASS；WSL gcc 与 WSL clang self-dependency 相关 26 executables PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:19 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 前沿的下一条相邻边界：在 six-additional parser/LSP focused targets 中新增 sign-crossing sixth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive、zero-inclusive negative、strict-negative 或 sign-crossing sixth wrapper 包住已接受的 nonzero-possible five-additional product，但仍拒绝 zero-only sixth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing sixth wrapper over an accepted nonzero-possible five-additional product；zero-only sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional target 保持前五个 cases PASS，但新增 sign-crossing case 在旧 nonzero-non-crossing guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 6 PASS，LSP six-additional query PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:07 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 前沿的下一条相邻边界：在 six-additional parser/LSP focused targets 中新增 negative non-singleton sixth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive、zero-inclusive negative 或 strict-negative sixth wrapper 包住已接受的 nonzero-possible five-additional product，但仍拒绝 zero-only/sign-crossing sixth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crest=-2..-1`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-2..2]`；tail `+(step + step)` 抵消两个 lower-bound exact `step` 项后留下 `[0..4] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict-negative sixth wrapper over an accepted nonzero-possible five-additional product；zero-only/sign-crossing sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional target 保持 positive singleton、positive non-singleton、zero-inclusive positive 与 zero-inclusive negative cases PASS，但新增 negative non-singleton case 在旧 positive-possible-or-zero-inclusive-negative guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 5 PASS，LSP six-additional query PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:55 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 前沿的下一条相邻边界：在 six-additional parser/LSP focused targets 中新增 zero-inclusive negative sixth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive 或 zero-inclusive negative sixth wrapper 包住已接受的 nonzero-possible five-additional product，但仍拒绝 zero-only/strict-negative/sign-crossing sixth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crest=-1..0`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative sixth wrapper over an accepted nonzero-possible five-additional product；zero-only/strict-negative/sign-crossing sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional target 保持 positive singleton、positive non-singleton 与 zero-inclusive positive cases PASS，但新增 zero-inclusive negative case 在旧 positive-possible guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc focused/self-dependency adjacent 矩阵通过；focused six-additional parser 4 PASS，LSP six-additional query PASS，WSL gcc self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。WSL clang 本轮被无关 AOT `backend_aot_c_scalar_locals.c` signature/argument mismatch 阻断，不声明 GREEN。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:43 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 前沿的下一条相邻边界：在 six-additional parser/LSP focused targets 中新增 zero-inclusive positive sixth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_extra_extra_deeper_extension`。该 guard 允许 strict-positive 或 zero-inclusive positive sixth wrapper 包住已接受的 nonzero-possible five-additional product，但仍拒绝 zero-only/zero-inclusive negative/negative/sign-crossing sixth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crest=0..1`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive sixth wrapper over an accepted nonzero-possible five-additional product；zero-only/zero-inclusive negative/negative/sign-crossing sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional target 保持 positive singleton 与 positive non-singleton cases PASS，但新增 zero-inclusive positive case 在旧 strict-positive guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 3 PASS，LSP six-additional query PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:22 +08:00 | 按阶段 2 语义推断核心路线继续推进 six-additional 前沿的下一条相邻边界：在 six-additional parser/LSP focused targets 中新增 positive non-singleton sixth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_extra_extra_deeper_extension`。该 guard 允许 strict-positive sixth wrapper 包住已接受的 nonzero-possible five-additional product，但仍拒绝 zero-only/zero-inclusive/negative/sign-crossing sixth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`crest=2..3`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-3..3]`；tail `+(step + step + step)` 抵消三个 lower-bound exact `step` 项后留下 `[0..6] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict-positive non-singleton sixth wrapper over an accepted nonzero-possible five-additional product；zero-only/zero-inclusive/negative/sign-crossing sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional target 保持 positive singleton case PASS，但新增 positive non-singleton case 在旧 singleton guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 2 PASS，LSP six-additional query PASS，WSL gcc/clang self-dependency 相关 26 targets PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:08 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional 之后的下一条 bounded arbitrary-depth 前沿：新增 `zr_vm_numeric_selfdep_coeff_cross_six_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_six_additional_scale_query_test`，覆盖 positive singleton sixth wrapper `crest=1..1` 包住已接受的 nonzero-possible five-additional product。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_extra_extra_deeper_extension`，只在第六层 wrapper 为 positive singleton 且内层满足 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_deeper_extension` 时接受。`step=1..3`、`factor=-1..1`、`crest=1..1`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的六层 scale product 与 coefficient 保守为 `[-1..1]`；tail `+step` 抵消一个 lower-bound exact `step` 项后留下 `[0..2] * step` residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive singleton sixth wrapper over an accepted nonzero-possible five-additional product；zero-only/zero-inclusive/negative/sign-crossing sixth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser six-additional case 在旧 five-additional guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP six-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 1 PASS，LSP six-additional query PASS，WSL gcc/clang self-dependency 相关 26 targets PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 14:50 +08:00 | 按阶段 2 语义推断核心路线补齐 five-additional target-reading symbolic coefficient-cross family 的 zero-only no-op 边界：在 five-additional parser/LSP focused targets 中新增 zero-only fifth-wrapper fixtures。现有 no-op replay 逻辑无需生产代码变更；`shell=0..0` 包住 accepted nonzero-possible four-additional product 时整体 scale product 与 coefficient 确定为 `0..0`，循环内 `narrowed = narrowed + (step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); other = narrowed;` 不改变 target，后续 `narrowed + 0` 保持 `5..5`。该切片只声明 tested zero-only fifth wrapper exact no-op over an accepted nonzero-possible four-additional product；zero-only residual expansion、arbitrary zero-product algebra、further arbitrary depth、nested term products、sign-crossing base terms、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery/GREEN：新增 zero-only parser/LSP fixtures 在现有实现下直接通过，无生产代码 RED 缺陷；WSL gcc focused five-additional parser 7 PASS，LSP five-additional query PASS；WSL gcc 与 WSL clang self-dependency 相关 24 targets PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:36 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional target-reading symbolic coefficient-cross residual 的下一条相邻边界：新增 five-additional parser/LSP sign-crossing fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_possible_extra_extra_scale_identifier`，将 extra-extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive、zero-inclusive negative、strict negative 或 sign-crossing fifth wrapper 包住已接受的 nonzero-possible four-additional product，但仍拒绝 zero-only fifth wrapper 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing fifth wrapper over an accepted nonzero-possible four-additional product；zero-only fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser five-additional target 保持 positive singleton、positive non-singleton、zero-inclusive positive、zero-inclusive negative 与 negative non-singleton cases 通过，但新增 sign-crossing case 在旧 guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP five-additional query 返回 full int64。GREEN：WSL gcc 与 WSL clang focused/self-dependency adjacent 矩阵通过；focused five-additional parser 6 PASS，LSP five-additional query PASS，WSL gcc/clang self-dependency 相关 24 targets PASS。Windows MSVC Debug 在 `build-msvc` focused 与实际存在的 self-dependency 18-target 矩阵通过；`build-msvc-lsp-selfdep` normal focused build 被无关 AOT `backend_aot_c_function_body.c` C2198/C2143 阻断，未作为本切片失败归因。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:17 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional target-reading symbolic coefficient-cross residual 的下一条相邻边界：新增 five-additional parser/LSP negative non-singleton fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_non_crossing_scale_identifier`，将 extra-extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive、zero-inclusive negative 或 strict negative fifth wrapper 包住已接受的 nonzero-possible four-additional product，但仍拒绝 zero-only/sign-crossing fifth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`shell=-2..-1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +(step + step)` 得到 scale product `[-2..2]` 与 coefficient `[-2..2]`，展开成两个负向 exact `step` terms 加 `[0..4] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict negative fifth wrapper over an accepted nonzero-possible four-additional product；zero-only/sign-crossing fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser five-additional target 保持 positive singleton、positive non-singleton、zero-inclusive positive 与 zero-inclusive negative cases 通过，但新增 negative non-singleton case 在旧 guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 5 PASS，LSP five-additional query PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:01 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional target-reading symbolic coefficient-cross residual 的下一条相邻边界：新增 five-additional parser/LSP zero-inclusive negative fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_or_zero_inclusive_negative_scale_identifier`，将 extra-extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive 或 zero-inclusive negative fifth wrapper 包住已接受的 nonzero-possible four-additional product，但仍拒绝 zero-only/strict-negative/sign-crossing fifth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`shell=-1..0`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative fifth wrapper over an accepted nonzero-possible four-additional product；zero-only/strict-negative/sign-crossing fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser five-additional target 保持 positive singleton、positive non-singleton 与 zero-inclusive positive cases 通过，但新增 zero-inclusive negative case 在旧 guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 4 PASS，LSP five-additional query PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:43 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional target-reading symbolic coefficient-cross residual 的下一条相邻边界：新增 five-additional parser/LSP zero-inclusive positive fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_scale_identifier`，将 extra-extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_extra_deeper_extension`。该 guard 允许 strict-positive 或 zero-inclusive positive fifth wrapper 包住已接受的 nonzero-possible four-additional product，但仍拒绝 zero-only/zero-inclusive negative/negative/sign-crossing fifth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`shell=0..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive fifth wrapper over an accepted nonzero-possible four-additional product；zero-only/zero-inclusive negative/negative/sign-crossing fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser five-additional target 保持 positive singleton 与 positive non-singleton cases 通过，但新增 zero-inclusive positive case 在旧 guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 3 PASS，LSP five-additional query PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:23 +08:00 | 按阶段 2 语义推断核心路线继续推进 five-additional target-reading symbolic coefficient-cross residual 的下一条相邻边界：新增 five-additional parser/LSP positive non-singleton fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 strict-positive fifth-wrapper helper `loop_assignment_sequence_symbolic_coefficient_is_positive_scale_identifier`，将 extra-extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_extra_deeper_extension`。该 guard 允许 strict-positive fifth wrapper 包住已接受的 nonzero-possible four-additional product，但仍拒绝 zero-only/zero-inclusive/negative/sign-crossing fifth wrappers 与 further arbitrary depth。`step=1..3`、`factor=-1..1`、`shell=2..3`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +(step + step + step)` 得到 scale product `[-3..3]` 与 coefficient `[-3..3]`，展开成三个负向 exact `step` terms 加 `[0..6] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive non-singleton fifth wrapper over an accepted nonzero-possible four-additional product；zero-only/zero-inclusive/negative/sign-crossing fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser five-additional target 保持 positive singleton case 通过，但新增 positive non-singleton case 在旧 guard 行为下失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过。覆盖 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 6 PASS、five-additional parser coefficient-cross 2 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional/five-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:05 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 five-additional parser/LSP positive singleton fifth-wrapper fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_extra_deeper_extension`，只允许 positive singleton fifth wrapper 包住已接受的 nonzero-possible four-additional product。`step=1..3`、`factor=-1..1`、`shell=1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (shell * (scale * (outer * (span * (cover * (mask * gate)))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive singleton fifth wrapper over an accepted nonzero-possible four-additional product；zero-only/zero-inclusive/negative/sign-crossing fifth wrappers、further arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc focused parser/LSP five-additional cases 在旧 guard 行为下分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过。覆盖 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 6 PASS、five-additional parser coefficient-cross 1 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional/five-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 04:06 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 four-additional parser/LSP sign-crossing fourth-wrapper fixtures，并将 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 extra-deeper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_deeper_extension`，复用既有 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_possible_scale_extension_identifier`。该 guard 允许任何 nonzero-possible fourth wrapper 包住已接受的 nonzero-possible three-additional product，但仍拒绝 zero-only fourth wrapper 与 general arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing fourth wrapper over an accepted nonzero-possible three-additional product；zero-only fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc normal focused build 仍受无关 AOT typed-bool/direct-call 签名不一致影响，因此使用构建系统生成命令手动重编/重链接 focused parser/LSP targets；新增 parser four-additional sign-crossing case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 先返回 full int64。GREEN：WSL gcc/clang 使用构建系统生成命令手动更新 parser shared library 与 four-additional focused targets 后，相邻矩阵通过；Windows MSVC Debug normal focused build 与相邻矩阵通过。覆盖 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 6 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。MSVC build 仍输出无关 AOT value-semir `fprintf` 格式警告；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 03:45 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 four-additional parser/LSP strict negative fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_non_crossing_scale_identifier`，将 fourth-wrapper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_deeper_extension`。该 guard 允许 strict-positive、zero-inclusive positive、zero-inclusive negative 或 strict negative fourth wrapper 包住既有 nonzero-possible three-additional product。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +(step + step)` 得到 scale product `[-2..2]` 与 coefficient `[-2..2]`，展开成两个负向 exact `step` terms 加 `[0..4] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict negative fourth wrapper over an accepted nonzero-possible three-additional product；zero-only/sign-crossing fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：WSL gcc normal build 先被无关 AOT typed-bool/direct-call 签名不一致阻断，随后用已构建库手动重编/重链接 focused parser/LSP targets；新增 parser four-additional strict-negative case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 先返回 full int64。GREEN：WSL gcc/clang 使用构建系统生成命令手动更新 parser shared library 与 four-additional focused targets 后，相邻矩阵通过；Windows MSVC Debug normal focused build 与相邻矩阵通过。覆盖 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 5 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 03:17 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 four-additional parser/LSP zero-inclusive negative fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_or_zero_inclusive_negative_scale_identifier`，将 fourth-wrapper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_deeper_extension`。该 guard 只允许 strict-positive、zero-inclusive positive 或 zero-inclusive negative fourth wrapper 包住既有 nonzero-possible three-additional product。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative fourth wrapper over an accepted nonzero-possible three-additional product；zero-only/strict-negative/sign-crossing fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser four-additional zero-inclusive negative scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 4 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 02:47 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 four-additional parser/LSP zero-inclusive positive fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_scale_identifier`，将 fourth-wrapper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_deeper_extension`。该 guard 只允许 strict-positive 或 zero-inclusive positive fourth wrapper 包住既有 nonzero-possible three-additional product。`step=1..3`、`factor=-1..1`、`scale=0..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive fourth wrapper over an accepted nonzero-possible three-additional product；zero-only/zero-inclusive negative/non-positive fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser four-additional zero-inclusive positive scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 通过已生成对象和现有库重链接后先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 3 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 02:18 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：新增 four-additional parser/LSP strict-positive non-singleton fixtures，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_scale_identifier`，将 fourth-wrapper guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_deeper_extension`。该 guard 只允许 `min > 0` 的 fourth wrapper 包住既有 nonzero-possible three-additional product。`step=1..3`、`factor=-1..1`、`scale=2..3`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +(step + step + step)` 得到 scale product `[-3..3]` 与 coefficient `[-3..3]`，展开成三个负向 exact `step` terms 加 `[0..6] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict-positive non-singleton fourth wrapper over an accepted nonzero-possible three-additional product；zero-only/zero-inclusive/non-positive fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser four-additional positive non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 2 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 01:43 +08:00 | 按阶段 2 语义推断核心路线继续推进 deeper target-reading symbolic coefficient-cross residual 的下一条 bounded arbitrary-depth 前沿：先把 parser three-additional cases 从 `test_numeric_loop_self_dependency_symbolic_coeff_cross_deeper_scale_dataflow.c` 拆到 `test_numeric_loop_self_dependency_symbolic_coeff_cross_three_additional_scale_dataflow.c` / `zr_vm_numeric_selfdep_coeff_cross_three_additional_scale_test`，再补 three-additional zero-only no-op parser/LSP 回归，确认 exact zero wrapper 仍保持 `5..5`。新增 four-additional parser/LSP focused targets，并在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 增加 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_deeper_extension`，只允许 positive singleton fourth wrapper 包住既有 nonzero-possible three-additional product。`step=1..3`、`factor=-1..1`、`scale=1..1`、`outer=1..1`、`span=1..1`、`cover=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (cover * (mask * gate))))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive singleton fourth wrapper over an accepted nonzero-possible three-additional product；zero-only/non-positive fourth wrappers、general arbitrary depth、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Split baseline：WSL gcc 旧 parser deeper target 26 PASS，新 parser three-additional target 6 PASS。Discovery：three-additional zero-only no-op parser/LSP case 在生产代码变更前已通过。RED：新增 parser four-additional positive singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP four-additional target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 26 PASS、three-additional parser coefficient-cross 7 PASS、four-additional parser coefficient-cross 1 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional/four-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 01:08 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 将 third-wrapper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_deeper_extension`，并使用 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_possible_deeper_scale_extension_identifier`，允许 sign-crossing/nonzero-possible outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 zero-only third wrapper 或 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing third wrapper over an accepted nonzero-possible deeper product；zero-only third wrappers、arbitrary deeper products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper three-additional sign-crossing scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 32 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 10:37 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 将 third-wrapper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_deeper_extension`，新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_non_crossing_deeper_scale_extension_identifier`，只允许 positive-possible、zero-inclusive negative 或 strict negative non-crossing outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 zero-only/sign-crossing third wrapper 或 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +(step + step)` 得到 scale product `[-2..2]` 与 coefficient `[-2..2]`，展开成两个负向 exact `step` terms 加 `[0..4] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict negative third wrapper over an accepted nonzero-possible deeper product；zero-only/sign-crossing third wrappers、arbitrary deeper products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper three-additional negative non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 31 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 10:12 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual，并先把 three-additional LSP query cases 从 deep-scale LSP 文件拆到 `test_lsp_numeric_loop_self_dependency_symbolic_coeff_cross_three_additional_scale_semantic_query.c` / `zr_vm_lsp_selfdep_coeff_cross_three_additional_scale_query_test`，避免继续越过 1000 行阈值。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 zero-inclusive-possible deeper scale extension helper，并把 third-wrapper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_zero_inclusive_possible_deeper_extension`，只允许 positive-possible 或 `min < 0 && max == 0` 的 zero-inclusive negative range outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 zero-only、strict negative、sign-crossing third wrappers 或 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative third wrapper over an accepted nonzero-possible deeper product；zero-only/strict-negative/sign-crossing third wrappers、arbitrary deeper products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Split baseline：WSL gcc 旧 deep-scale LSP 目标 PASS，新 three-additional LSP 目标 PASS。RED：新增 parser deeper three-additional zero-inclusive negative scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 30 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 09:32 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 positive-possible scale extension helper，并把 third-wrapper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_deeper_extension`，只允许 `min > 0` 或 `min == 0 && max > 0` 的 zero-inclusive positive range outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 zero-only、negative/sign-crossing third wrappers 或 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=0..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive third wrapper over an accepted nonzero-possible deeper product；zero-only/non-positive third wrappers、arbitrary deeper products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper three-additional zero-inclusive positive scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 29 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 09:16 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 strict positive scale extension helper，并把 third-wrapper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_deeper_extension`，只允许 `min > 0` 的 positive range outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=2..3`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +(step + step + step)` 得到 scale product `[-3..3]` 与 coefficient `[-3..3]`，展开成三个负向 exact `step` terms 加 `[0..6] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 strict positive non-singleton third wrapper over an accepted nonzero-possible deeper product；zero-inclusive/non-positive third wrappers、arbitrary deeper products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper three-additional positive non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 28 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:49 +08:00 | 按阶段 2 语义推断核心路线继续补 deeper target-reading symbolic coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 positive singleton deeper extension helper，只允许一个 positive singleton outer wrapper 包住既有 nonzero-possible two-additional product，不扩大到 arbitrary depth。`step=1..3`、`factor=-1..1`、`scale=1..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (span * (mask * gate)))))); reader; +step` 得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，tail 抵消 exact 部分后 target 从 `5..5` 扩为 `5..INT64_MAX`。同时补 two-additional zero-only scale-product exact no-op parser/LSP 回归，确认现有路径保持 target `5..5`。该切片只声明 positive singleton third wrapper over an accepted nonzero-possible deeper product；zero-only residual expansion、arbitrary deeper/non-positive third wrappers、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Discovery：新增 two-additional zero-only no-op parser/LSP case 在生产代码变更前已通过并保留为回归。RED：新增 parser deeper three-additional positive singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 27 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:25 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level sign-crossing scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 strict sign-crossing second-level wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=-1..1`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +step` 因而得到 scale product `[-1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 sign-crossing second-level wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level sign-crossing fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Baseline：WSL gcc focused parser deeper target 24 PASS、LSP deeper target PASS。Support refactor：LSP deeper target 抽出 shared assignment-range query setup 到 `lsp_numeric_range_query_test_support.c/.h` 后仍 PASS。RED：新增 parser deeper two-additional-level sign-crossing scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 25 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:01 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level negative non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 negative non-singleton second-level wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +(step + step)` 因而得到 scale product `[-2..-1]` 与 coefficient `[-2..2]`，展开成两个负向 exact `step` terms 加 `[0..4] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 negative non-singleton second-level wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level negative non-singleton fixture、sign-crossing second-level wrappers without future tests、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Baseline：WSL gcc focused parser deeper target 23 PASS、LSP deeper target PASS。RED：新增 parser deeper two-additional-level negative non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 24 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:45 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level zero-inclusive negative scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 zero-inclusive negative second-level wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +step` 因而得到 scale product `[-1..0]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive negative second-level wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level zero-inclusive negative fixture、negative non-singleton/sign-crossing second-level wrappers without future tests、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Baseline：WSL gcc focused parser deeper target 22 PASS、LSP deeper target PASS。RED：新增 parser deeper two-additional-level zero-inclusive negative scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 23 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:25 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level zero-inclusive positive scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 zero-inclusive positive second-level wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=0..1`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +step` 因而得到 scale product `[0..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 zero-inclusive positive second-level wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level zero-inclusive positive fixture、zero-inclusive negative/negative/sign-crossing second-level wrappers without future tests、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Baseline：WSL gcc focused parser deeper target 21 PASS、LSP deeper target PASS。RED：新增 parser deeper two-additional-level zero-inclusive positive scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 22 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:08 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level positive non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 positive range second-level wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=2..3`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +(step + step + step)` 因而得到 scale product `[2..3]` 与 coefficient `[-3..3]`，展开成三个负向 exact `step` terms 加 `[0..6] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive range second-level wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level positive fixture、zero-inclusive/negative/sign-crossing second-level wrappers without future tests、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Baseline：WSL gcc focused parser deeper target 20 PASS、LSP deeper target PASS。RED：新增 parser deeper two-additional-level positive non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 21 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:51 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained two-additional-level positive singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个 positive singleton outer wrapper，再包住 already accepted one-additional-level scale product。`step=1..3`、`factor=-1..1`、`scale=1..1`、`outer=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (outer * (mask * gate))))); reader; +step` 因而得到 scale product `[1..1]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 positive singleton outer wrapper over the existing bounded one-level scale-product classifier；zero-only、arbitrary deeper scale products beyond this two-additional-level positive-singleton fixture、non-positive/zero-inclusive/negative/sign-crossing second-level wrappers without future tests、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper two-additional-level positive singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 20 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:26 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained zero-inclusive negative scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个带 zero-inclusive negative outer factor 的 nested scale product，且 inner scale product 已属于当前 accepted scale-product set。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (mask * gate)))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one additional zero-inclusive negative scale-product extension over an already accepted two-factor product within the existing residual term budget；zero-only、arbitrary deeper scale products beyond this one-additional-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper chained zero-inclusive negative scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 19 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:06 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained zero-inclusive positive scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个带 zero-inclusive positive outer factor 的 nested scale product，且 inner scale product 已属于当前 accepted scale-product set。`step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (mask * gate)))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。parser deeper target 先抽出 direct/chained scale-product helper，避免继续在 near-threshold 测试文件复制 setup；该切片只声明 one additional zero-inclusive positive scale-product extension over an already accepted two-factor product within the existing residual term budget；zero-only、zero-inclusive negative outer scale extension、arbitrary deeper scale products beyond this one-additional-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | Refactor baseline：WSL gcc focused parser deeper target 17 PASS、LSP deeper target PASS；支撑抽取后 parser deeper target 17 PASS。RED：新增 parser deeper chained zero-inclusive positive scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 18 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:41 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained negative non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个带 negative non-singleton outer factor 的 nested scale product，且 inner scale product 已属于当前 accepted scale-product set。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (mask * gate)))); reader; +(step + step)` 因而得到 coefficient `[-2..2]`，展开成两个负向 exact `step` terms 加 `[0..4] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one additional negative non-singleton scale-product extension over an already accepted two-factor product within the existing residual term budget；zero-only、zero-inclusive outer scale extensions、arbitrary deeper scale products beyond this signed extension fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper chained negative non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 17 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 17 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:25 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained positive non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个带 positive non-singleton outer factor 的 nested scale product，且 inner scale product 已属于当前 accepted scale-product set。`step=1..3`、`factor=-1..1`、`scale=2..3`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (mask * gate)))); reader; +(step + step + step)` 因而得到 coefficient `[-3..3]`，展开成三个负向 exact `step` terms 加 `[0..6] * step` residual，后续 tail 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one additional positive non-singleton scale-product extension over an already accepted two-factor product within the existing residual term budget；zero-only、zero-inclusive/nonpositive outer scale extensions beyond the accepted negative non-singleton fixture、arbitrary deeper scale products beyond this one-additional-level signed extension fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper chained positive non-singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 16 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 16 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:07 +08:00 | 按阶段 2 语义推断核心路线补 deeper chained positive singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是 crossing identifier 乘以一个带 positive singleton outer factor 的 nested scale product，且 inner scale product 已属于当前 accepted positive bounded scale-product set。`step=1..3`、`factor=-1..1`、`scale=1..1`、`mask=1..1`、`gate=1..1` 的 `+(step * (factor * (scale * (mask * gate)))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one additional positive-singleton scale-product extension over an already accepted two-factor product within the existing residual term budget；zero-only、arbitrary deeper scale products beyond this positive-singleton extension fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper chained positive singleton scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 15 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 15 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Positive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:46 +08:00 | 按阶段 2 语义推断核心路线补 deeper mixed zero-inclusive positive/negative scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 mixed scale product 的组合，其中两个 product factors 分别是 zero-inclusive positive identifier range 与 zero-inclusive negative identifier range，并复用现有 range inference 计算 whole coefficient range。`step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=-1..0` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 scale product `[-1..0]` 与 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with mixed zero-inclusive positive/negative scale product within the existing residual term budget；zero-only、broader sign-crossing products beyond this accepted two-factor fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper mixed zero-inclusive positive/negative scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 14 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 14 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Negative Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:29 +08:00 | 按阶段 2 语义推断核心路线补 deeper mixed zero-inclusive negative plus strict sign-crossing scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 mixed scale product 的组合，其中两个 product factors 分别是 zero-inclusive negative identifier range 与 strict sign-crossing identifier range，并复用现有 range inference 计算 whole coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=-1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with mixed zero-inclusive negative/sign-crossing scale product within the existing residual term budget；zero-only、mixed zero-inclusive positive/negative products、broader sign-crossing products beyond this accepted two-factor fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper mixed zero-inclusive negative/sign-crossing scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 13 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 13 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Positive Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:14 +08:00 | 按阶段 2 语义推断核心路线补 deeper mixed zero-inclusive positive plus strict sign-crossing scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 mixed scale product 的组合，其中两个 product factors 分别是 zero-inclusive positive identifier range 与 strict sign-crossing identifier range，并复用现有 range inference 计算 whole coefficient range。`step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=-1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with mixed zero-inclusive positive/sign-crossing scale product within the existing residual term budget；zero-only、mixed zero-inclusive negative/sign-crossing products、mixed zero-inclusive positive/negative products、broader sign-crossing products beyond this accepted two-factor fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper mixed zero-inclusive positive/sign-crossing scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc 与 WSL clang focused build/run 通过，新 parser deeper target 12 PASS，新 LSP deeper target PASS；WSL gcc/clang 相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 12 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。Windows MSVC Debug focused build 被当前工作树中无关 AOT `backend_aot_c_zrp_metadata_prune.c` 参数数量不匹配 C2198 阻塞，未到达本切片测试目标；Windows GREEN 与 full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 03:56 +08:00 | 按阶段 2 语义推断核心路线补 deeper double strict sign-crossing scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 double strict sign-crossing scale product 的组合，并复用现有 range inference 计算 whole coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..1`、`mask=-1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double strict sign-crossing scale product within the existing residual term budget；zero-only、mixed zero-inclusive/sign-crossing product combinations、broader sign-crossing scale products beyond the accepted two strict-crossing factors、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper double sign-crossing scale-product coefficient-cross residual target case 在旧 guard 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 11 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 11 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 03:33 +08:00 | 按阶段 2 语义推断核心路线补 deeper double negative non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 same-sign negative scale product 的组合，即两个 product factors 都是 strictly negative non-singleton identifier ranges，并复用现有 range inference 计算 whole coefficient range。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=-2..-1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step + step + step)` 因而得到 scale product `[1..4]` 与 coefficient `[-4..4]`；collector 在加减子树收集后即时抵消已出现的 exact `step` 项并回收 residual term budget，最终四个 lower-bound exact 项被 tail 抵消，只留下 `[0..8] * step` upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with same-sign double negative non-singleton scale product within the existing residual term budget；strict sign-crossing scale products without a positive singleton factor、zero-only/mixed zero-inclusive product combinations、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper double negative non-singleton scale-product coefficient-cross residual target case 在旧 guard/term-budget 行为下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused build/run 通过，新 parser deeper target 10 PASS，新 LSP deeper target PASS；三套相邻矩阵通过 parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 10 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base LSP coefficient-cross PASS、deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Coefficient-Cross Test Split | 已完成 | 2026-06-26 09:31 +08:00 | 按阶段 2 语义推断核心的持续验收要求，拆分 coefficient-cross target-reading symbolic residual 回归覆盖：原 parser 文件保留 one-layer coefficient-cross 8 个基础 widening 用例，新 parser target `zr_vm_numeric_selfdep_coeff_cross_deeper_scale_test` 承担 9 个 deeper scale-product 用例；LSP 原 target 保留基础 8 个查询，新 target `zr_vm_lsp_selfdep_coeff_cross_deeper_scale_query_test` 承担 deeper scale-product 9 个查询。该切片不改变 semantic facts、numeric range 或 residual collector 行为，只让基础 residual 与 deeper scale-product residual 独立验证并退出近 1000 行累积风险。 | Baseline：拆分前 WSL gcc/clang 旧 parser coefficient-cross 17 PASS 与 LSP coefficient-cross 17 PASS/checks 通过。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug 重新配置/构建/运行四个目标通过：旧 parser 8 PASS、new parser deeper scale 9 PASS、旧 LSP PASS、new LSP deeper scale PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Negative Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 09:09 +08:00 | 按阶段 2 语义推断核心路线补 deeper double negative singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 same-sign negative scale product 的组合，且该 scale product 至少一个 factor 是 negative singleton，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=-1..-1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)` 因而得到 coefficient `[-2..2]`，展开成两个负向 exact `step` term 加 `[0..4] * step` residual，后续 `+(step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-2`、`mask = -1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double negative scale product containing a negative singleton factor within the existing residual term budget；double negative non-singleton scale products without singleton factor、strict sign-crossing scale products without a positive singleton factor、zero-only/mixed zero-inclusive product combinations、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser deeper double negative singleton scale-product coefficient-cross residual target case 在旧 double zero-inclusive guard 下经 WSL gcc touched-object/manual relink 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP target query 先返回 full int64。普通 WSL gcc target build 和 `build-msvc-lsp-selfdep` MSVC build 均被既有 AOT `backend_aot_c_zrp_metadata_prune.c` 参数不匹配阻塞。GREEN：WSL gcc/clang touched-object/manual relink focused run 与 Windows MSVC `build-msvc` manual relink focused run 通过，三套均通过 parser coefficient-cross 17 PASS、LSP coefficient-cross 17 PASS/checks；WSL gcc/clang 与 Windows MSVC manual relink 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑/不声明，full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:51 +08:00 | 按阶段 2 语义推断核心路线补 deeper double zero-inclusive negative scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 double zero-inclusive negative scale product 的组合，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=-1..0` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-1`、`mask = (seed % 2)-1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double zero-inclusive negative scale product within the existing residual term budget；zero-only scale products、negative non-singleton / strict sign-crossing scale products without a positive singleton factor、mixed-sign zero-inclusive product combinations、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding double zero-inclusive product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 double zero-inclusive positive-only scale-product guard 下新增 parser deeper double zero-inclusive negative scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper double zero-inclusive negative scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 16 PASS、LSP coefficient-cross 16 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片已确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:36 +08:00 | 按阶段 2 语义推断核心路线补 deeper double zero-inclusive positive scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 double zero-inclusive positive scale product 的组合，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=0..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = seed % 2`、`mask = seed % 2` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double zero-inclusive positive scale product within the existing residual term budget；zero-only scale products、double zero-inclusive negative / negative non-singleton / strict sign-crossing scale products without a positive singleton factor、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding double zero-inclusive product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 positive-singleton-or-one-zero-inclusive scale-product guard 下新增 parser deeper double zero-inclusive positive scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper double zero-inclusive positive scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 15 PASS、LSP coefficient-cross 15 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:22 +08:00 | 按阶段 2 语义推断核心路线补 deeper sign-crossing scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 strict sign-crossing scale product 的组合，且该 scale product 保留一个 positive singleton factor，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 3)-1`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with strict sign-crossing scale product sharing a positive singleton factor within the existing residual term budget；zero-only scale products、scale products without a positive singleton factor、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding sign-crossing scale/product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 same-sign/zero-bounded scale-product guard 下新增 parser deeper sign-crossing scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper sign-crossing scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 14 PASS、LSP coefficient-cross 14 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:13 +08:00 | 按阶段 2 语义推断核心路线补 deeper negative non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 same-sign negative scale product 的组合，且该 scale product 保留一个 positive singleton factor，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)` 因而得到 coefficient `[-2..2]`，展开成两个负向 exact `step` term 加 `[0..4] * step` residual，后续 `+(step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-2`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with same-sign negative scale product sharing a positive singleton factor within the existing residual term budget；zero-only scale products、strict sign-crossing scale products、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative scale/product combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 zero-bounded-only scale-product guard 下新增 parser deeper negative non-singleton scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper negative non-singleton scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 13 PASS、LSP coefficient-cross 13 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:58 +08:00 | 按阶段 2 语义推断核心路线补 deeper zero-inclusive negative scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 zero-inclusive negative scale product 的组合，且该 scale product 保留一个 positive singleton factor，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-1`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with zero-inclusive negative scale product sharing a positive singleton factor within the existing residual term budget；zero-only scale products、negative-only/sign-crossing scale products beyond this zero-inclusive negative fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 zero-inclusive positive-only scale-product guard 下新增 parser deeper zero-inclusive negative scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper zero-inclusive negative scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 12 PASS、LSP coefficient-cross 12 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为上一切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:42 +08:00 | 按阶段 2 语义推断核心路线补 deeper zero-inclusive positive scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 zero-inclusive positive scale product 的组合，且该 scale product 保留一个 positive singleton factor，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = seed % 2`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with zero-inclusive positive scale product sharing a positive singleton factor within the existing residual term budget；zero-only scale products、negative-only/sign-crossing scale products beyond the zero-inclusive negative fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 positive scale-product guard 下新增 parser deeper zero-inclusive positive scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper zero-inclusive positive scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 11 PASS、LSP coefficient-cross 11 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。WSL gcc product 相邻目标构建仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:14 +08:00 | 按阶段 2 语义推断核心路线补 deeper positive non-singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一个 positive scale product 的组合，且该 scale product 至少保留一个 positive singleton factor，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=2..3`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step + step)` 因而得到 coefficient `[-3..3]`，展开成三个负向 exact `step` term 加 `[0..6] * step` residual，后续 `+(step + step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)+2`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with positive non-singleton scale product sharing a positive singleton factor within the existing residual term budget；deeper scale products、double-non-singleton scale products、zero/negative/sign-crossing scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 positive singleton scale-product guard 下新增 parser deeper positive non-singleton scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper positive non-singleton scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 10 PASS、LSP coefficient-cross 10 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。WSL gcc 全相邻矩阵包含 product 目标时构建被既有 AOT `backend_aot_c_zrp_metadata_prune.c` 参数不匹配阻塞；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:46 +08:00 | 按阶段 2 语义推断核心路线补 deeper positive singleton scale-product 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 crossing identifier 与一层 positive singleton scale product 的组合，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=2..2`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)` 因而得到 coefficient `[-2..2]`，展开成两个负向 exact `step` term 加 `[0..4] * step` residual，后续 `+(step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1`、`scale = 2`、`mask = 1` 覆盖同一边界。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with positive singleton scale product within the existing residual term budget；deeper scale products、non-singleton/zero/negative/sign-crossing scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 guard 下新增 parser deeper positive singleton scale-product coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP deeper positive singleton scale-product coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 9 PASS、LSP coefficient-cross 9 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Zero-Inclusive Negative Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:23 +08:00 | 按阶段 2 语义推断核心路线补 zero-inclusive negative scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * zero_inclusive_negative_identifier_range` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..0` 的 `+(step * (factor * scale)); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1` 和 `(seed % 2)-1` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with zero-inclusive negative identifier scale within the existing residual term budget；zero-only scale、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 guard 下新增 parser zero-inclusive negative scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP zero-inclusive negative scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 8 PASS、LSP coefficient-cross 8 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Zero-Inclusive Positive Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:02 +08:00 | 按阶段 2 语义推断核心路线补 zero-inclusive positive scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * zero_inclusive_positive_identifier_range` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=0..1` 的 `+(step * (factor * scale)); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1` 和 `seed % 2` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with zero-inclusive positive identifier scale within the existing residual term budget；zero-only scale、one-sided zero-inclusive negative scale、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 guard 下新增 parser zero-inclusive positive scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP zero-inclusive positive scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 7 PASS、LSP coefficient-cross 7 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Sign-Crossing Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:44 +08:00 | 按阶段 2 语义推断核心路线补 sign-crossing scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * strict_sign_crossing_identifier_range` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-1..1` 的 `+(step * (factor * scale)); reader; +step` 因而得到 coefficient `[-1..1]`，展开成一个负向 exact `step` term 加 `[0..2] * step` residual，后续 `+step` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1` 和 `(seed % 3)-1` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with strict sign-crossing identifier scale within the existing residual term budget；zero-only/one-sided zero-inclusive scale factors、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：旧 guard 下新增 parser sign-crossing scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP sign-crossing scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 6 PASS、LSP coefficient-cross 6 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Non-Singleton Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:29 +08:00 | 按阶段 2 语义推断核心路线补 negative non-singleton scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * same_sign_nonzero_identifier_range` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..1`、`scale=-2..-1` 的 `+(step * (factor * scale)); reader; +(step + step)` 因而得到 coefficient `[-2..2]`，展开成两个负向 exact `step` term 加 `[0..4] * step` residual，后续 `+(step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 3)-1` 和 `(seed % 2)-2` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with negative non-singleton identifier scale within the existing residual term budget；zero/sign-crossing scale factors、deeper nested products、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative scale combinations、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：在旧 negative-singleton guard 下，新增 parser negative non-singleton scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP negative non-singleton scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 5 PASS、LSP coefficient-cross 5 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:01 +08:00 | 按阶段 2 语义推断核心路线补 negative singleton scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * positive_identifier_range`、`crossing_identifier * negative_singleton_identifier` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..3`、`scale=-1..-1` 的 `+(step * (factor * scale)); reader; +(step + step + step)` 因而得到 coefficient `[-3..1]`，展开成三个负向 exact `step` term 加 `[0..4] * step` residual，后续 `+(step + step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 5)-1` 和 `scale = -1` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with negative singleton identifier scale；negative non-singleton scale、zero/sign-crossing scale factors、deeper nested products、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser negative scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP negative scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 4 PASS、LSP coefficient-cross 4 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Non-Singleton Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 04:31 +08:00 | 按阶段 2 语义推断核心路线补 non-singleton positive scale 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * positive_identifier_range` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..3`、`scale=2..3` 的 `+(step * (factor * scale)); reader; +(step + step + step)` 因而得到 coefficient `[-3..9]`，展开成三个负向 exact `step` term 加 `[0..12] * step` residual，后续 `+(step + step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 5)-1` 和 `(seed % 2)+2` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with positive identifier range scale；deeper nested products、zero/negative/sign-crossing scale factors、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser non-singleton scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP non-singleton scale coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 3 PASS、LSP coefficient-cross 3 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Nested Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 04:14 +08:00 | 按阶段 2 语义推断核心路线补 nested multiplicative coefficient 的跨零 residual 窄边界：same-target symbolic residual collector 对 simple nonnegative identifier term，可接受 coefficient 是一层 `crossing_identifier * positive_singleton_identifier` 或反向顺序的 product，并复用现有 range inference 计算整个 coefficient range。`step=1..3`、`factor=-1..3`、`scale=2..2` 的 `+(step * (factor * scale)); reader; +(step + step)` 因而得到 coefficient `[-2..6]`，展开成两个负向 exact `step` term 加 `[0..8] * step` residual，后续 `+(step + step)` 抵消 exact 部分并留下 upward residual，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 用 `(seed % 3)+1`、`(seed % 5)-1` 和 `scale = 2` 覆盖同一边界。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with positive singleton identifier scale；deeper nested products、non-singleton scale factors、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser nested coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP nested coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 2 PASS、LSP coefficient-cross 2 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 03:54 +08:00 | 按阶段 2 语义推断核心路线补 range coefficient residual 的跨零窄边界：same-target symbolic residual collector 对 simple identifier term / simple identifier coefficient product 可接受 non-target sign-crossing int64 coefficient range，前提是 term 也由当前类型环境证明为非负 identifier range。collector 将 `[min..max] * term` 展开为负下界 exact signed terms 加 `[0..max-min] * term` 非负残差，使后续 `+(step + step)` 能抵消 `factor=-1` 下界部分，并留下正向 residual。`step=1..3`、`factor=-1..3` 的 `+(step * factor); reader; +(step + step)` 因而提交 upward sequence delta，target 从 `5..5` 扩为 `5..INT64_MAX`；LSP 通过 `step = (seed % 3) + 1` 与 `factor = (seed % 5) - 1` 覆盖同一边界。range-coefficient symbolic range 只有在缩窄当前 raw range 或把 sign-crossing 改善为 one-sided/zero 时覆盖，避免破坏 observer/product exact-cancel 精度；跨零 expansion 只覆盖 simple identifier term/coefficient。该切片只声明 non-target sign-crossing identifier coefficient residual widening with nonnegative identifier term；nested multiplicative coefficient decomposition、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP coefficient-cross target query 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 1 PASS、LSP coefficient-cross 1 PASS/check、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Subtractive Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 03:10 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 checked integer-literal subtractive factor 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 integer-literal-only `-` factor 通过 checked int64 subtraction 规约为 literal product value，再复用 checked literal-product folding 与 non-literal multiset matching。`step=-1..1`、`factor=-2..2` 的 `+(step * (factor * (5 - 2))); reader; -((3 * factor) * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `-1..11`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 subtractive constant product factor。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal subtractive factor folding；arbitrary subtraction algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、subtraction overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser subtractive constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP subtractive constant product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 9 PASS、LSP product 9 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Additive Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:52 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 checked integer-literal additive factor 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 integer-literal-only `+` factor 通过 checked int64 addition 规约为 literal product value，再复用 checked literal-product folding 与 non-literal multiset matching。`step=-1..1`、`factor=-2..2` 的 `+(step * (factor * (1 + 2))); reader; -((3 * factor) * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `-1..11`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 additive constant product factor。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal additive factor folding；arbitrary addition algebra、subtractive factor folding、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、addition overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser additive constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP additive constant product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 8 PASS、LSP product 8 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Modulo Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:35 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 checked integer-literal modulo factor 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 integer-literal-only `%` factor 通过 checked int64 modulo 规约为 literal product value，再复用 checked literal-product folding 与 non-literal multiset matching。`step=-1..1`、`factor=-2..2` 的 `+(step * (factor * (7 % 4))); reader; -((3 * factor) * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `-1..11`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 modulo constant product factor。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal modulo factor folding；arbitrary modulo algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、modulo-by-zero/overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser modulo constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP modulo constant product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 7 PASS、LSP product 7 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Divided Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:19 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 checked integer-literal division factor 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 integer-literal-only `/` factor 通过 checked int64 division 规约为 literal product value，再复用 checked literal-product folding 与 non-literal multiset matching。`step=-1..1`、`factor=-2..2` 的 `+(step * (factor * (6 / 2))); reader; -((3 * factor) * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `-1..11`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 divided constant product factor。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal division factor folding；arbitrary division algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、division-by-zero/overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser divided constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP divided constant product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 6 PASS、LSP product 6 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Double-Negative Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:01 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 bounded nested unary sign-chain 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把一元 `+`/`-` 链规约为 literal sign product 与底层 non-literal term，再复用 checked integer-literal product folding 与 multiset matching。`step=-1..1`、`factor=-2..2` 的 `+((-(-step)) * factor); reader; -(factor * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..7`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 double-negative product factor。该切片只声明 bounded supported binary `*` product-term equality 内的一元正负号链归一；arbitrary unary operators、general sign algebra、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser double-negative product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP double-negative product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 5 PASS、LSP product 5 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Positive Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 01:46 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的一元正号因子归一边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 unary `+term` 归一为底层 non-literal term，再复用 checked integer-literal product folding 与 multiset matching。`step=-1..1`、`factor=-2..2` 的 `+((+step) * factor); reader; -(factor * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..7`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 unary-positive product factor。该切片只声明 bounded supported binary `*` product-term equality 内的一元正号因子归一；nested unary normalization、general sign algebra、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser unary-positive product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP unary-positive product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 4 PASS、LSP product 4 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Negative Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 01:33 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的一元负号因子归一边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，会把 unary `-term` 归一为 literal product factor `-1` 和底层 non-literal term，再复用 checked integer-literal product folding 与 multiset matching。`step=-1..1`、`factor=-2..2` 的 `+((-step) * factor); reader; -((-1 * factor) * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..7`；LSP 用 `(seed % 3)-1` 和 `(seed % 5)-2` 覆盖同一 unary-negative product factor。该切片只声明 bounded supported binary `*` product-term equality 内的一元负号因子归一；general sign algebra、double-negation normalization、unary-plus normalization、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser unary-negative product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP unary-negative product-factor case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 3 PASS、LSP product 3 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Constant Product Exact-Cancel Replay | 已完成 | 2026-06-26 01:10 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 checked literal-product exact-cancel 边界：delta expression product-term equality 在 bounded supported binary `*` tree flattening 后，把 integer literal factors 通过 checked int64 multiplication 归一为一个 product value，再匹配剩余非 literal terms。`step=-1..1`、`factor=-1..1` 的 `+(step * (factor * (2 * 3))); reader; -((6 * step) * factor)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `-1..11`；LSP 用 `(seed % 3)-1` 两个 sign-crossing factors 覆盖同一 folded constant product。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal product folding；distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser folded constant-product exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP folded constant-product case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 2 PASS、LSP product 2 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Associative-Commutative Product Exact-Cancel Replay | 已完成 | 2026-06-26 00:51 +08:00 | 按阶段 2 语义推断核心路线补 product expression equality 的 bounded associative/commutative exact-cancel 边界：delta expression equality 在既有 supported-expression 检查后，把 supported binary `*` tree 展平成有限 product term list，并用 multiset 递归等价匹配。`step=-1..1`、`factor=-2..2`、`scale=-1..1` 的 `+(step * (factor * scale)); reader; -((scale * step) * factor)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..7`；LSP 用 `(seed % 3)-1`、`(seed % 5)-2`、`(seed % 3)-1` 覆盖同一 sign-crossing nested product。该切片只声明 bounded supported binary `*` product-term multiset equality for exact same-target cancellation；distribution、constant folding、coefficient solving、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新 LSP product target 初始缺 common include registration，先编译失败于 `semantic/lsp_local_semantic_query.h`；修正 CMake 注册后，新增 parser product case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`，新增 LSP product case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser product 1 PASS、LSP product 1 PASS/check、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Sign-Crossing Product Exact-Cancel Replay | 已完成 | 2026-06-26 00:28 +08:00 | 按阶段 2 语义推断核心路线补 sign-crossing product 的精确抵消边界：delta expression equality 在既有 supported-expression 检查后，对 binary `*` 支持左右 operand 交换递归等价。`step=-1..1`、`factor=-2..2` 的 `+(step * factor); reader; -(factor * step)` 因而不会进入 range coefficient residual widening，而是把同一 target-reading product delta 精确抵消，target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..7`；LSP 用 `step = (seed % 3) - 1`、`factor = (seed % 5) - 2` 覆盖同一 sign-crossing product。该切片只声明 supported binary `*` swapped-operand equality for exact same-target cancellation；arbitrary multiplication algebra、distribution、coefficient solving、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。 | RED：新增 parser sign-crossing product exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Range-Coefficient Residual Exact-Cancellation Replay | 已完成 | 2026-06-26 00:11 +08:00 | 按阶段 2 语义推断核心路线补 range residual 的同系数精确抵消：symbolic residual term 现在记录 coefficient expression，cancellation 只有在 base term、coefficient range、coefficient expression 均等价且符号相反时才发生，避免只因数值 range 相同就抵消不同 coefficient。`step=1..3`、`factor=-1..1` 的 `+(step * factor); reader; -(factor * step)` 因此可抵消同一 `step` coefficient 下的 `[0..2] * factor` residual，让 target 保持 `5..5`，observer 覆盖零次循环和首轮读取为 `0..8`；LSP 用 `step = seed + 1`、`factor = seed - 128` 覆盖更宽 range，target 为 `5..5`、observer 为 `-32763..32517`。该切片只声明 same coefficient expression residual exact cancellation；sign-crossing coefficient、target-dependent coefficient、exact residue/range-set analysis、arbitrary coefficient algebra、broader correlated solver、member/call/unsupported delta、fixed point 与 general CFG-wide dataflow 仍不声明完成。附带补 replay-resolved net-positive parser/LSP coverage，旧实现已通过。 | RED：新增 parser exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP exact-cancel case 先返回 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 15 PASS、LSP coefficient 14 PASS/checks、parser self-dependency 17 PASS、LSP self-dependency 17 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Positive Range-Coefficient Residual Replay | 已完成 | 2026-06-25 23:36 +08:00 | 按阶段 2 语义推断核心路线补 commuted range coefficient 的窄正残差推断：product coefficient collector 现在在乘法两侧都可作为 non-target same-sign range coefficient 时比较规范化 lower bound，选择 lower bound 更大的 candidate 作为 coefficient side，以保留后续 exact lower-bound term cancellation。`factor=2..4` 与 `step=1..3` 的 `+(factor * step); reader; -(step + step)` 因此不再误把 `step` 当作 coefficient，而是把 `factor` 展开为两个精确 `step` 加 `[0..2] * step` residual，得到 full sequence delta `0..6` 并把 target widening 为 `5..INT64_MAX`。该切片只声明 commuted product 中 non-target same-sign range coefficient candidate selection；不声明 sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、correlated coefficient/term solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser commuted positive range-coefficient case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；新增 LSP coefficient query 使用 `step = seed + 1`、`factor = seed + 2` 和 `factor * step` 后失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 14 PASS、新 LSP coefficient 13 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Range-Coefficient Residual Replay | 已完成 | 2026-06-24 21:11 +08:00 | 按阶段 2 语义推断核心路线补 non-singleton coefficient 的全负残差推断：product coefficient slot 现在可接受不读取 loop target、且 range inference 证明为全负 int64 range 的 coefficient expression。collector 把全负系数 range 取绝对值并翻转 residual sign，再沿用 positive range coefficient 的 lower-bound exact expansion 与 `[0..max-min] * term` residual projection。因此 `step=1..3, factor=-4..-2` 的 `+(step * factor); reader; +(step + step)` 不再退化为 full int64，而是得到 full sequence delta `-6..0` 并把 target widening 为 `INT64_MIN..5`。该切片只声明 non-target、same-sign negative range coefficient residual replay；不声明 sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、correlated coefficient/term solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser negative range-coefficient case 先失败为 target upper bound `Expected 5 Was 9223372036854775807`；新增 LSP coefficient query 使用 `step = seed + 1`、`factor = -2 - seed` 后失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 13 PASS、新 LSP coefficient 12 PASS/checks。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Positive Range-Coefficient Residual Replay | 已完成 | 2026-06-24 20:58 +08:00 | 按阶段 2 语义推断核心路线补 non-singleton coefficient 的窄正残差推断：same-target symbolic residual collector 现在可接受 product coefficient slot 中不读取 loop target、且 range inference 证明为非负 int64 range 的 coefficient expression。`[min..max] * term` 会保留 `min` 个精确 term 用于后续抵消，同时把剩余 `[0..max-min] * term` 作为 range-coefficient residual term；当前 residual projection 用 checked int64 range multiplication 计算残差范围。因此 `step=1..3, factor=2..4` 的 `+(step * factor); reader; -(step + step)` 不再退化为 sign-crossing full int64，而是得到 full sequence delta `0..6` 并把 target widening 为 `5..INT64_MAX`。该切片只声明 non-target、nonnegative range coefficient residual replay；不声明 negative/sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、correlated coefficient/term solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser positive range-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`；新增 LSP coefficient query 使用 `step = seed + 1`、`factor = seed + 2` 后失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 12 PASS、新 LSP coefficient 11 PASS/checks。新 source 触发 CMake glob reconfigure；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Coefficient Nested-If Loop-Write Invalidation Guard | 已完成 | 2026-06-24 20:14 +08:00 | 按阶段 2 语义推断核心路线补 nested-if coefficient dependency invalidation：same-target symbolic singleton-coefficient replay 现在会在同一 loop sequence 中识别后续 nested if/else assignment 写入已经累计的 delta RHS dependency。`+(step * factor); if (inner) { factor = 3; } else { factor = 4; }; reader; -(step + step)` 不再使用 pre-loop `factor=2..2` 保持 target 精确，而是把 target 与 reader/observer 降级到 full int64；尾部 same-target stable delta 继续从当前保守 binding replay，避免计划因普通 full-int64 RHS 算术溢出风险而提前放弃。该切片只声明 ordinary assignment 与 plan-visible nested-if write 的 singleton coefficient replay invalidation；不声明 broader/chained invalidation beyond ordinary and nested-if writes、non-singleton coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser nested-if invalidated singleton-coefficient case 先失败为 expected full int64 target `Expected -9223372036854775808 Was 5`，新增 LSP coefficient query 失败为 target `min=5 max=5`、observer `min=0 max=0`。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 11 PASS、新 LSP coefficient 10 PASS/checks。WSL/Clang 与 MSVC rebuild 期间触发无关 AOT/library 重编和既有 warnings；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Coefficient Loop-Write Invalidation Guard | 已完成 | 2026-06-24 15:24 +08:00 | 按阶段 2 语义推断核心路线补 loop recurrence soundness 边界：same-target symbolic coefficient replay 现在会在 loop sequence 中识别后续 ordinary assignment 写入已经累计的 delta RHS 依赖名，并把这种 coefficient cancellation 降级为保守 sign-crossing replay，而不是沿用循环前 singleton coefficient 做精确抵消。`+(step * factor); factor = 3; reader; -(step + step)` 不再使用 pre-loop `factor=2..2` 保持 target 精确，而是让 target 与 reader/observer 回到 full int64。该切片只声明 ordinary loop write invalidates prior accumulated coefficient dependency；不声明非单点 coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、任意 multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser invalidated singleton-coefficient case 先失败为 expected full int64 target `Expected -9223372036854775808 Was 5`，新 LSP case 失败为 target `min=5 max=5`、observer `min=0 max=0`。GREEN：WSL gcc/clang static focused touched object rebuild + CMake-generated relink 通过；Windows MSVC static focused Ninja build/run 通过。三套运行均通过 parser coefficient 10 PASS、新 LSP coefficient 9 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-Xor Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:51 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中递归折叠 integer-literal-only `^` 表达式，使用与 CFG constant folding 一致的 checked int64 bitwise-xor guard（非负 left、非负 right），再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (1 ^ 3)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal bitwise-xor coefficient folding；通用 multiplication/coefficient algebra、非 literal coefficient 的新能力、target-dependent coefficient、exact residue/range-set analysis、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser bitwise-xor literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc/clang static focused 通过 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；Windows MSVC static focused 通过 Ninja build/run。三套均通过 parser coefficient 9 PASS、新 LSP coefficient 8 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-Or Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:39 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中递归折叠 integer-literal-only `|` 表达式，使用与 CFG constant folding 一致的 checked int64 bitwise-or guard（非负 left、非负 right），再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (1 | 2)); other = narrowed; narrowed = narrowed - (step + step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-4..14`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-379..386`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal bitwise-or coefficient folding；bitwise-xor coefficient folding、通用 multiplication/coefficient algebra、非 literal coefficient 的新能力、target-dependent coefficient、exact residue/range-set analysis、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser bitwise-or literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc/clang static focused 通过 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；Windows MSVC static focused 通过 Ninja build/run。三套均通过 parser coefficient 8 PASS、新 LSP coefficient 7 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-And Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:28 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中递归折叠 integer-literal-only `&` 表达式，使用与 CFG constant folding 一致的 checked int64 bitwise-and guard（非负 left、非负 right），再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (3 & 2)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal bitwise-and coefficient folding；bitwise-or/xor coefficient folding、通用 multiplication/coefficient algebra、非 literal coefficient 的新能力、target-dependent coefficient、exact residue/range-set analysis、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser bitwise-and literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc/clang static focused 通过 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；Windows MSVC static focused 通过 Ninja build/run。三套均通过 parser coefficient 7 PASS、新 LSP coefficient 6 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Right-Shift Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:11 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中递归折叠 integer-literal-only `>>` 表达式，使用与 CFG constant folding 一致的 checked int64 right shift guard（非负 left、非负 shift、shift < 63），再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (4 >> 1)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal right-shift coefficient folding；其他 bitwise coefficient folding、通用 multiplication/coefficient algebra、非 literal coefficient 的新能力、target-dependent coefficient、exact residue/range-set analysis、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser right-shifted literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc/clang static focused 通过 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；Windows MSVC static focused 通过 Ninja build/run。三套均通过 parser coefficient 6 PASS、新 LSP coefficient 5 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Left-Shift Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 13:39 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中递归折叠 integer-literal-only `<<` 表达式，使用与 CFG constant folding 一致的 checked int64 left shift guard（非负 left、非负 shift、shift < 63、无溢出），再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (1 << 1)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal left-shift coefficient folding；right-shift/bitwise coefficient folding、通用 multiplication/coefficient algebra、非 literal coefficient 的新能力、target-dependent coefficient、exact residue/range-set analysis、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser shifted literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc static focused、WSL clang static focused 与 Windows MSVC static focused 通过普通 focused build/run；三套均通过 parser coefficient 5 PASS、新 LSP coefficient 4 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Range Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 13:19 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 non-literal singleton coefficient 窄边界：same-target sequence tracker 的 symbolic residual collector 现在会在 product coefficient slot 中尝试把非 target expression 推断成 int64 range，且只在 `min==max` 的单点区间下把该值作为 signed coefficient 展开。`while (flag) { narrowed = narrowed + (step * factor); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 在 parser 中以 `factor=2..2` 返回 `narrowed=5..5`、`other=-1..11`；LSP `factor: int = 2` / `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明不读取 target 的 int64 singleton-range coefficient expression；非单点 coefficient、target-dependent coefficient、exact coefficient/range-set analysis、任意乘法 algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser singleton coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc static focused 通过 object rearchive/relink 隔离无关 AOT value-semir 构建阻断，WSL clang static focused 与 Windows MSVC static focused 通过普通 focused build/run；三套均通过 parser coefficient 4 PASS、新 LSP coefficient 3 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Modulo Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 12:36 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在会在 product coefficient 位置递归折叠 integer-literal-only `%` 表达式，使用受检 int64 取模并拒绝除零与 `INT64_MIN % -1` 溢出，再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (5 % 3)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal modulo folding；通用 multiplication/coefficient algebra、非 literal coefficient、exact residue-set analysis、overflow symbolic algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser modulo literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc static focused、WSL clang static focused 与 Windows MSVC static focused 通过 parser coefficient 3 PASS、新 LSP coefficient 2 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Divided Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 11:48 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在会在 product coefficient 位置递归折叠 integer-literal-only `/` 表达式，使用受检 int64 除法并拒绝除零与 `INT64_MIN / -1` 溢出，再把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (6 / 3)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal division folding；modulo、通用 multiplication/coefficient algebra、非 literal coefficient、overflow symbolic algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。LSP 覆盖拆到新的 coefficient semantic-query target，避免继续扩大已接近千行的 symbolic LSP fixture。 | RED：新增 parser divided literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，新增 LSP coefficient local query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过新 parser coefficient 2 PASS、新 LSP coefficient 1 PASS/check、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multiplicative Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 11:15 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 product coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在会在 product coefficient 位置递归折叠 integer-literal-only `*` 表达式，并把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (2 * 1)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal multiplication folding；通用 multiplication/coefficient algebra、非 literal coefficient、division/modulo、overflow symbolic algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。Parser 覆盖拆到新的 coefficient target，避免继续扩大已接近千行的 symbolic parser fixture。 | RED：新增 parser multiplicative literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过新 parser coefficient 1 PASS、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 10:20 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 folded literal coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在会在 product coefficient 位置递归折叠 integer-literal-only `+`/`-` 表达式，并把折叠后的 signed coefficient 交给既有 bounded term expansion。`while (flag) { narrowed = narrowed + (step * (1 + 1)); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 supported residual product coefficient slot 内 checked integer-literal additive expression folding；通用 multiplication/coefficient algebra、非 literal coefficient、division/modulo、overflow symbolic algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser folded literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 16 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 09:13 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 negative literal coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在可识别 integer literal coefficient 外层 unary `+` / `-`，负系数会翻转 residual term sign 后在现有 term limit 内按绝对值展开。`while (flag) { narrowed = narrowed + (step * -2); other = narrowed; narrowed = narrowed + (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-249..261`。该切片只声明 unary-negative integer literal multiplier expansion inside the supported residual accumulator；通用 multiplication/coefficient algebra、overflow symbolic algebra、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser negative literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 15 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 15 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 09:01 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 positive literal coefficient 窄边界：same-target sequence tracker 的 symbolic residual term collector 现在会在现有 term limit 内把 `term * N` 或 `N * term` 形式的 positive integer literal product 展开为重复 signed-additive terms，再复用既有 term cancellation。`while (flag) { narrowed = narrowed + (step * 2); other = narrowed; narrowed = narrowed - (step + step); } narrowed + 0; other + 0` 因此在 parser 中返回 `narrowed=5..5`、`other=-1..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-251..259`。该切片只声明 bounded positive integer literal multiplier expansion inside the supported residual accumulator；通用 multiplication algebra、coefficient solver、zero/negative multiplier rewrite、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser literal-coefficient case 先失败为 target `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 14 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 14 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multi-Residual Prefix Range Replay | 已完成 | 2026-06-24 08:43 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic current residual range 窄边界：same-target sequence tracker 现在在 residual terms 尚未全消时，也能把剩余 signed-additive terms 推断成当前 residual 区间并覆盖 prefix `deltaMin/deltaMax`。因此 `while (flag) { narrowed = narrowed + step; narrowed = narrowed - (step + bias); other = narrowed; narrowed = narrowed + (bias + 1); } narrowed + 0; other + 0` 的 ordinary target-reading `other` 读取点按 `-bias` 回放，而不是按未相关化的 `step - (step + bias)` envelope 回放。Parser 中 `other=100`、`step=-3..3`、`bias=2..4` 返回 `narrowed=5..INT64_MAX`、`other=1..INT64_MAX`；LSP `step = seed - 128` / `bias = seed + 3` fixture 返回 `narrowed=5..INT64_MAX`、`other=-253..INT64_MAX`。该切片只声明 supported signed-additive residual term list 的 current-range projection；通用 symbolic solver、跨变量相关性、系数合并、乘法/除法、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser prefix case 先失败为 `Expected 1 Was -5`，LSP local semantic query observer 先返回 `min=-508 max=INT64_MAX`。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 13 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 13 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multi-Residual Net-Positive Delta Replay | 已完成 | 2026-06-24 08:24 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的多项 symbolic residual accumulator 窄边界：`type_inference_loop_assignment_sequence.c/.h` 的 same-target sequence tracker 现在维护有界 signed-additive residual term list 和 checked integer literal sum，每个 supported self-dependent delta expression 都追加 signed terms、抵消相反且递归等价的非 literal terms；只有 residual terms 为空时才把 full sequence delta 覆盖为精确常量。因此 `while (flag) { narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + bias); narrowed = narrowed + (bias + 1); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..INT64_MAX`、`other=0..INT64_MAX`；LSP `step = seed - 128` / `bias = seed + 3` fixture 返回 `narrowed=5..INT64_MAX`、`other=-123..INT64_MAX`。该切片只声明 supported signed-additive `+`/`-` delta expression 在同一 target-reading sequence 内的有界 term accumulator 与 checked constant residual；任意代数化、乘法/除法、系数合并、member/call/unsupported delta、target-reading sign-crossing residual、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 LSP local semantic query 在旧实现下对 target/observer 返回 full int64；parser corrected expectation 前也暴露旧 tracker 不能提交多项 residual，目标下界退化为 full lower bound。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 12 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 12 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；`git diff --check` 仅 LF/CRLF warning，尾随空白扫描 clean；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Residual Net-Negative Delta Replay | 已完成 | 2026-06-24 08:03 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic residual net-negative 窄边界：`type_inference_loop_assignment_delta_expression.c` 新增 signed-additive constant residual helper，可把两个 supported self-dependent delta expression 乘以各自 sign 后收集 signed terms，抵消相反且递归等价的非 literal terms，并在剩余 terms 为空时返回 checked literal residual。Sequence replay 在 target-reading stable same-target sequence 中把该 residual 作为 full sequence delta，用于 net-negative final target widening 与 reader intermediate envelope。因此 `while (flag) { narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + 1); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=INT64_MIN..5`、`other=INT64_MIN..8`；LSP `step = seed - 128` fixture 返回 `narrowed=INT64_MIN..5`、`other=INT64_MIN..132`。该切片只声明两个 supported binary `+`/`-` signed-additive delta expression 的非 literal 全抵消 + checked constant residual；一般非抵消 symbolic recurrence、positive/sign-crossing target-reading recurrence、乘法/除法、系数合并、多项 residual accumulator、member/call/unsupported delta、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser residual symbolic net-negative target 先失败为 target max `Expected 5 Was 9223372036854775807`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 11 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 11 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Positive Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:39 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 unary-positive symbolic expression 窄边界：self-dependent delta expression lookup 现在在三条使用路径中统一归一化外层 unary `+`，把 `(+step)` 变成 normalized delta node `step` 和保留 sign `+1`，再参与 expression equality、delta sign 读取和 self-dependent delta range 计算。因此 `while (flag) { narrowed = narrowed + (+step); other = narrowed; narrowed = narrowed - step; } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..8`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported self-dependent delta expression 外层 unary `+` 的 identity sign normalization；任意代数化、乘法/除法、系数合并、member/call/unsupported delta、target-reading non-zero recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser unary-positive symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 10 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 10 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Negative Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:26 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 unary-negative symbolic expression 窄边界：self-dependent delta expression lookup 现在在三条使用路径中统一归一化外层 unary `-`，把 `(-step)` 变成 normalized delta node `step` 和 sign `-1`，再参与 expression equality、delta sign 读取和 self-dependent delta range 计算。因此 `while (flag) { narrowed = narrowed + (-step); other = narrowed; narrowed = narrowed + step; } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..8`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported self-dependent delta expression 外层 unary `-` 的符号归一化；任意代数化、乘法/除法、系数合并、member/call/unsupported delta、target-reading non-zero recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser unary-negative symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 9 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 9 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Same-Side Term Cancellation Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:07 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 same-side symbolic term cancellation 窄边界：supported binary `+`/`-` delta expression 的 signed additive path 现在会先消去同一表达式内符号相反且递归等价的非 literal terms，再比较剩余 signed multiset 并为 self-dependent delta range 计算剩余项区间。因此 `+((step + bias) - bias)` 与 `-step` 可被判定为同一 supported stable delta 的相反符号，同时 first-iteration reader 只看到 `source + step`。`while (flag) { narrowed = narrowed + ((step + bias) - bias); other = narrowed; narrowed = narrowed - step; } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..8`；LSP `step = seed - 128` / `bias = seed + 3` fixture 返回 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported binary `+`/`-` 中同侧一正一负的等价非 literal term cancellation；跨表达式任意代数化、乘法/除法、系数合并、溢出的 literal sum、member/call/unsupported delta、target-reading non-zero recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser same-side term cancellation symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 8 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 8 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Signed-Additive Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 06:46 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 signed additive symbolic expression 窄边界：delta expression equality 已拆到 `type_inference_loop_assignment_delta_expression.c`，并在 supported binary `+`/`-` tree 上收集 signed additive signature。Integer literal terms 使用 checked int64 signed sum 归一化，非 literal terms 保持 signed multiset + 既有递归表达式等价匹配，所以 `+((step + 5) - 2)` 与 `-(step + 3)` 可被判定为同一 supported stable delta 的相反符号。`while (flag) { narrowed = narrowed + ((step + 5) - 2); other = narrowed; narrowed = narrowed - (step + 3); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported binary `+`/`-` delta expression 的 signed additive integer-literal normalization；非 literal 同侧抵消、乘法/除法代数化、溢出的 literal sum、member/call/unsupported delta、target-reading non-zero recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。主 self-dependency 文件 1030 -> 493 行，新 delta-expression 文件 545 行。 | RED：新增 parser signed-additive symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused、WSL clang focused 与 Windows MSVC focused 通过 parser symbolic 7 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 7 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / LSP Numeric Loop Self-Dependency Symbolic Query Target Split | 已完成 | 2026-06-24 06:13 +08:00 | 阶段 2 后续 bounded loop recurrence/symbolic correlation work 先执行测试边界维护：把 LSP self-dependency 文件中的 6 个 target-reading symbolic query cases 抽到 `tests/language_server/test_lsp_numeric_loop_self_dependency_symbolic_semantic_query.c`，旧 general self-dependency query target 保留非 symbolic recurrence/interleave/replay-resolved cases，新 `zr_vm_language_server_numeric_loop_self_dependency_symbolic_semantic_query_test` 专门覆盖 direct/additive-zero/same-shape/commuted/associative/folded-integer-sum symbolic exact-net-zero LSP query。旧文件从 993 行降到 723 行，新文件 460 行；本条不新增 parser/type-inference 语义，只为后续阶段 2 切片保持可维护测试边界。 | WSL gcc focused 构建并运行通过 parser symbolic 6 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、新 LSP symbolic 6 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS/checks。Windows MSVC focused 构建与运行通过同一组目标。WSL clang 构建被当前未归属本 LSP 切片的 AOT dirty files 阻塞，错误为 `backend_aot_write_c_direct_neg_signed` 在 `backend_aot_c_lowering_typed_arithmetic.c` 与 `backend_aot_c_emitter.h` 中类型冲突；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Additive-Zero Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:54 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic additive-zero 窄边界：delta expression equality 现在在 supported binary `+` tree flatten path 中接受一侧不是 binary `+`、另一侧是 `+ 0` plus tree 的 signature matching，所以 `+step` 与 `-(step + 0)` 可被判定为同一 supported stable delta 的相反符号。`while (flag) { narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + 0); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..8`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported integer `+` delta expression 的 additive-zero signature matching；减法/乘法代数化、任意常量折叠、member/call/unsupported delta、target-reading positive/sign-crossing 非抵消 recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser additive-zero symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过 parser symbolic 6 PASS、parser self-dependency 16 PASS、LSP self-dependency 22 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Integer-Sum Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic expression integer-sum 窄边界：delta expression equality 现在对 supported binary `+` tree flatten 后的 integer literal terms 做 checked int64 sum normalization，再匹配剩余 expression terms，所以 `+((step + 1) + 2)` 与 `-(step + 3)` 可被判定为同一 supported stable delta 的相反符号。`while (flag) { narrowed = narrowed + ((step + 1) + 2); other = narrowed; narrowed = narrowed - (step + 3); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported integer `+` delta expression 的 integer-literal sum normalization；任意常量折叠、减法/乘法代数化、溢出的 literal sum、member/call/unsupported delta、target-reading positive/sign-crossing 非抵消 recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser folded integer-sum symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过 parser symbolic 5 PASS、parser self-dependency 16 PASS、LSP self-dependency 21 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Associative Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:18 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic expression 关联律窄边界：delta expression equality 现在对 supported binary `+` tree 做有界 term flatten + multiset 匹配，所以 `+((step + 1) + 2)` 与 `-(step + (1 + 2))` 可被判定为同一 supported stable delta 的相反符号。`while (flag) { narrowed = narrowed + ((step + 1) + 2); other = narrowed; narrowed = narrowed - (step + (1 + 2)); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..11`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported integer `+` delta expression 的 associative exact-term matching；常量折叠式等价、减法/乘法代数化、member/call/unsupported delta、target-reading positive/sign-crossing 非抵消 recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser symbolic associative expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过 parser symbolic 4 PASS、parser self-dependency 16 PASS、LSP self-dependency 20 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:41 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic expression 交换律窄边界：delta expression equality 现在对 binary `+` 做左右交换匹配，所以 `+(step + 1)` 与 `-(1 + step)` 可被判定为同一 supported stable delta 的相反符号。`while (flag) { narrowed = narrowed + (step + 1); other = narrowed; narrowed = narrowed - (1 + step); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..9`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported integer `+` delta expression 的 commuted exact-zero 抵消；减法/乘法代数化、常量折叠式等价、member/call/unsupported delta、target-reading positive/sign-crossing 非抵消 recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser symbolic commuted expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过 parser symbolic 3 PASS、parser self-dependency 16 PASS、LSP self-dependency 19 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:27 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic expression 相关性边界：sequence replay 逻辑已从 replay 执行文件拆到 `type_inference_loop_assignment_sequence.c/.h`，避免继续扩大原 987 行文件；symbolic exact-zero tracker 现在保存 delta expression AST，并用受支持表达式的结构等价判断同一 delta 是否以相反符号出现。`while (flag) { narrowed = narrowed + (step + 1); other = narrowed; narrowed = narrowed - (step + 1); } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..9`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported stable expression shape 的结构等价 exact-zero 抵消；非结构等价代数化、member/call/unsupported delta、target-reading positive/sign-crossing 非抵消 recurrence、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：新增 parser symbolic expression target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过新 parser symbolic 2 PASS、parser self-dependency 16 PASS、LSP self-dependency 18 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Net-Zero Delta Replay | 已完成 | 2026-06-24 03:50 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 symbolic net-zero target-reading 边界：stable self-dependent direct identifier delta 现在在 replay 中记录 delta identifier 与正负 balance，`+step; reader; -step` 这类同一符号正负抵消的 sequence 会把 full sequence delta 修正为 `0..0`，同时 reader 仍读取 `source + prefix delta` 的中间 range。`while (flag) { narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step; } narrowed + 0; other + 0` 在 parser 中返回 `narrowed=5..5`、`other=0..8`；LSP `step = seed - 128` fixture 返回 `narrowed=5..5`、`other=-123..132`。该切片只声明 stable direct identifier delta 的 symbolic exact-zero 抵消；非抵消 target-reading positive/sign-crossing recurrence、replay-resolved positive/sign-crossing、member/call/unsupported delta、unsupported assignment、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser symbolic target 先失败为 `Expected 5 Was -9223372036854775808`，LSP local semantic query 对 target/observer 均返回 full int64。GREEN：WSL gcc focused 通过新 parser symbolic 1 PASS、parser self-dependency 16 PASS、LSP self-dependency 17 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Negative Delta Replay | 已完成 | 2026-06-24 03:20 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 replay-resolved target-reading non-zero 边界：loop replay 预扫现在可在 replay scope 中解析 `+step` 与 `-(step + 1)` 这类 earlier assignment 提供的 delta，并在 full sequence delta 为 supported net-negative 时允许 `other = narrowed` 读取 recurrence 中间值 envelope。为避免误用未模拟的中间写入，预扫会拒绝在后续 replay-resolved delta 之前写入该 delta expression dependency 的普通 interleave。`while (flag) { step = 1; narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + 1); } narrowed + 0; other + 0` 在 parser 与 LSP local semantic query 中分别返回 `narrowed=-9223372036854775808..5`、`other=-9223372036854775808..6`。该切片只声明 earlier supported replay assignment 提供的 replay-resolved delta、普通 target-reading interleave、完整 same-target sequence net-negative 且无 prior interleave 写入 delta dependency；replay-resolved target-reading positive/sign-crossing recurrence、member/call/unsupported delta、任意 interleave mutation、unsupported assignment、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 target-reading replay-resolved net-negative case 先失败为 `Expected 5 Was 9223372036854775805`，LSP local semantic query 返回 target `-9223372036854775808..9223372036854775805` 和 observer `0..9223372036854775807`。GREEN：WSL gcc focused 通过新 parser 16 PASS、新 LSP 16 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Zero Delta Replay | 已完成 | 2026-06-24 02:52 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 target-reading + replay-resolved delta 组合边界：loop replay 现在允许 replay-resolved self-dependent step 作为 sequence 起点，并在 sequence 预扫中利用已回放的 earlier assignment 解析后续 delta。`step = 1; narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step` 这类 shape 会先解析两个 `step` delta，确认总 delta 为 `0..0` 后才让 `other = narrowed` 读取 `source + accumulated delta` 的中间 range。`while (flag) { step = 1; narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step; } narrowed + 0; other + 0` 在 parser 与 LSP local semantic query 中分别返回 `narrowed=5..5`、`other=0..6`。该切片只声明 earlier supported replay assignment 提供的 replay-resolved delta、普通 target-reading interleave、完整 same-target sequence exact net-zero；replay-resolved target-reading non-zero/sign-crossing recurrence、member/call/unsupported delta、unsupported assignment、fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 target-reading replay-resolved net-zero case 先失败为 `Expected 5 Was 9223372036854775806`，LSP local semantic query 返回 target `5..9223372036854775806` 和 observer `0..9223372036854775807`。GREEN：WSL gcc focused 通过新 parser 15 PASS、新 LSP 15 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Multiple Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:27 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 bounded loop recurrence 的同 target 多写边界：plan collection 现在允许同 target sequence 中已经标记为 replay-resolved 的 self-dependent step 作为后续同 target replay-resolved step 的前缀，因此 `step = 2; narrowed = narrowed + step; narrowed = narrowed + step` 这类连续读取更早 same-loop assignment 的多次 delta 写入会在 replay scope 中逐步解析并累计后再 widen。`while (flag) { step = 2; narrowed = narrowed + step; narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明无 target-reading interleave、delta 依赖均来自更早 supported replay assignment 的同 target 多个 replay-resolved same-loop-written steps；target-reading 与 replay-resolved delta 组合、更复杂链式 same-loop delta 依赖、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 multiple same-loop-written delta case 先失败为 `Expected 9223372036854775807 Was 5`，LSP local semantic query 返回 `5..5`。GREEN：WSL gcc focused 通过新 parser 14 PASS、新 LSP 14 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sequence Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:06 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的同 target 多写窄边界：plan collection 现在允许已经延续同 target stable self-dependent sequence 的目标，再接一个需要 replay-time 解析、且 delta 只依赖更早 supported same-loop assignment 的 self-dependent step；replay 在执行中间 `step = 2` 后解析该延迟 delta，并把 `+1` 与 `+step` 合成后从 sequence source 做一次 widen。`while (flag) { step = 2; narrowed = narrowed + 1; narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明无 target-reading interleave 的 same-target stable-prefix + replay-resolved same-loop-written delta sequence；target-reading interleave 与 replay-resolved delta 的组合、链式/多重 same-loop delta dependencies、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 sequence same-loop-written delta case 先失败为 `Expected 9223372036854775807 Was 5`。GREEN：WSL gcc focused 通过新 parser 13 PASS、新 LSP 13 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Negative Multi-Write Delta Replay | 已完成 | 2026-06-24 01:39 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 target-reading non-zero 边界：plan collection 仍拒绝一般 target-reading interleave，但当中间普通 assignment 读取当前 sequence target，且 previous same-target stable delta 与当前 stable delta 合成后可作为 supported self-dependent recurrence delta 时，可把当前 step 标记为延续更早 sequence。replay 对读取者不只绑定第一轮 `source + accumulated delta`，还把后续轮次的 `widen(source, totalDelta) + accumulated delta` 合入 envelope。`while (flag) { narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 2; } narrowed + 0; other + 0` 在 parser 中分别返回 `-9223372036854775808..5` 与 `-9223372036854775808..6`，LSP local semantic query 对同一形状返回相同 range。该切片只声明 target-reading ordinary assignment interleave 的 net-negative stable replay；target-reading positive/sign-crossing fixture、变量相关泛化、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 target-reading interleaved net-negative case 先失败为 `Expected -9223372036854775808 Was 4`；LSP 初次实现验证暴露查询点和 numeric fact kind 测试辅助边界。GREEN：WSL gcc focused 通过新 parser 12 PASS、新 LSP 12 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 01:10 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 target-reading interleave 边界：plan collection 仍拒绝一般 target-reading interleave，但当中间普通 assignment 读取当前 sequence target，且 previous same-target stable delta 与当前 stable delta 精确合成 `0..0` 时，可把当前 step 标记为延续更早 sequence。replay 先预扫 sequence，只有净 delta 为零时才穿过读取 target 的 interleave；执行读取者前临时把 target 绑定为 source range 加已累计 delta。`while (flag) { narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 1; } narrowed + 0; other + 0` 在 parser 中分别返回 `5..5` 与 `0..6`，LSP local semantic query 对同一形状返回相同 range。该切片只声明 target-reading ordinary assignment interleave 的 exact net-zero replay；target-reading non-zero recurrence、变量相关相关性泛化、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 target-reading interleaved net-zero case 先失败为 `Expected 5 Was 9223372036854775806`，LSP query 对 final target 返回 `5..9223372036854775806`、对 observer 返回 `0..9223372036854775807`。GREEN：WSL gcc focused 通过新 parser 11 PASS、新 LSP 11 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:49 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 interleaved 同 target 多写边界：plan collection 从当前 assignment 向前扫描时允许跳过 target-independent 普通 assignment，只有中间 step 不写当前 target、不读取当前 target、且不是 self-dependent/deferred replay 时，才把当前 step 标记为延续更早的同 target stable self-dependent sequence；replay 会执行这些中间 assignment，同时把同 target stable deltas 合成后从序列前 source binding 做一次 widening。`while (flag) { narrowed = narrowed + 1; other = 10; narrowed = narrowed - 1; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..5`。该切片只声明 target-independent ordinary assignment interleave 下的 net-delta 合成；中间 assignment 读取该 target、非零多写 recurrence 精度、变量相关相关性精化、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 interleaved positive-then-negative net-zero case 先失败为 `Expected 5 Was 9223372036854775806`，LSP query 返回 `min=5 max=9223372036854775806`。GREEN：WSL gcc focused 通过新 parser 10 PASS、新 LSP 10 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Adjacent Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:31 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的相邻同 target 多写边界：plan collection 允许当前 assignment 在立即前一个 step 是同 target stable self-dependent delta 时继续标记为 self-dependent sequence；replay 将连续同 target stable self-dependent steps 的 signed delta range 合并后，再基于序列前的 source binding 做一次 widening。`while (flag) { narrowed = narrowed + 1; narrowed = narrowed - 1; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..5`，不再先把 `+1` widening 后让 `-1` 落在 `5..9223372036854775806`。该切片只声明相邻同 target stable self-dependent steps 的 net-delta 合成；非相邻/穿插其他 target 的 arbitrary multi-write recurrence、变量相关相关性精化、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 positive-then-negative net-zero case 先失败为 `Expected 5 Was 9223372036854775806`，LSP query 返回 `min=5 max=9223372036854775806`。GREEN：WSL gcc focused 通过新 parser 9 PASS、新 LSP 9 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Prefix Positive Delta Widening | 已完成 | 2026-06-24 00:02 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的同 target 多写窄边界：plan collection 新增 zero-only preserving prefix 判断，只有当前 target 的更早写入全是 `0..0` self-dependent no-op 且没有 nested write 时，后续同 target assignment 才继续允许 self-dependent delta 检测。`while (flag) { narrowed = narrowed + 0; narrowed = narrowed + 1; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`，不再停在一轮普通 RHS 的 `5..6`。同时把 replay result store、普通 assignment replay、self-dependent replay 与 nested replay step 执行抽到 `type_inference_loop_assignment_replay.c`，主 join helper 降到 815 行。该切片只声明 zero-only preserving prefix 后的 positive self-dependent delta；任意多写/非 no-op 前缀、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 zero-only-prefix positive case 先失败为 `Expected 9223372036854775807 Was 6`，LSP query 返回 `min=5 max=6`。GREEN：WSL gcc focused 通过新 parser 8 PASS、新 LSP 8 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Delta No-Op Replay | 已完成 | 2026-06-23 23:28 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的 no-op 边界：self-dependent delta helper 接受 `min == 0 && max == 0` 的 no-op delta，`WidenSelfDependentType` 对该形状直接复制 source type，不触发上下界 widen，也不让 loop plan validation/replay 失败。`while (flag) { narrowed = narrowed + 0; other = 10; } other + 0` 在 parser 与 LSP local semantic query 中返回 `0..10`，证明 no-op self-dependent step 保留原 target 绑定，同时不阻断其他 supported assignment replay。该切片只声明 first-write zero-only no-op delta preservation；链式/多写同循环 delta、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 zero-only no-op case 先失败为 `Expected 10 Was 0`。GREEN：WSL gcc focused 通过新 parser 7 PASS、新 LSP 7 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Expression Delta Widening | 已完成 | 2026-06-23 23:17 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：same-loop-written delta 检测从 direct identifier 扩展到 supported delta expression。plan collection 先判断 self-dependent delta expression 是否读取同一 replay plan 的 target，且这些 target 都已经由更早 step 写入；如果是，则延迟到 replay 时解析。loop replay 先执行 `step = 2`，再解析 `narrowed = narrowed + (step + 1)` 的 signed delta range 并 widen 到 `5..INT64_MAX`。`while (flag) { step = 2; narrowed = narrowed + (step + 1); } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 earlier same-loop-written supported expression delta widening；zero-only no-op、链式/多写同循环 delta、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 same-loop-written expression delta case 先失败为 `Expected 9223372036854775807 Was 5`。GREEN：WSL gcc focused 通过新 parser 6 PASS、新 LSP 6 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Stable Delta Widening | 已完成 | 2026-06-23 22:54 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：first-write self-dependent delta 检测现在可以延迟解析一个由同一 replay plan 更早 supported assignment 写入的 direct identifier delta。loop replay 先执行 `step = 2` 并把结果写入临时 type scope，再解析 `narrowed = narrowed + step` 的 signed delta range 并 widen 到 `5..INT64_MAX`。`while (flag) { step = 2; narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 earlier same-loop-written direct identifier positive singleton delta widening；zero-only no-op、复杂同循环 delta 依赖链、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 same-loop-written delta case 先失败为 `Expected 9223372036854775807 Was 5`。GREEN：WSL gcc focused 通过新 parser 5 PASS、新 LSP 5 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sign-Crossing Delta Widening | 已完成 | 2026-06-23 22:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：self-dependent delta range 检测现在接受稳定 int64 `min < 0 && max > 0` 的跨符号 delta 表达式，并把 one-or-more replay 直接放宽到完整 int64 区间。`step: -3..3` / `u8 - 128` 下 `while (flag) { narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `-9223372036854775808..9223372036854775807`。该切片只声明 first-write self-dependent stable sign-crossing delta widening；zero-only no-op、same-loop-written delta dependency、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 sign-crossing case 先失败为 `Expected -9223372036854775808 Was 5`，LSP query 返回 `min=5 max=5`。GREEN：WSL gcc focused 通过新 parser 4 PASS、新 LSP 4 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Negative Delta Widening | 已完成 | 2026-06-23 22:25 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：self-dependent delta range 检测除 `min >= 0 && max > 0` 正向可能 delta 外，现在也接受稳定 int64 `min < 0 && max <= 0` 的非正负向可能 delta 表达式。`step: -3..0` / `0 - u8` 下 `while (flag) { narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `-9223372036854775808..5`。该切片只声明 first-write self-dependent zero-inclusive negative-possible delta widening；zero-only no-op、sign-crossing delta、same-loop-written delta dependency、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 zero-inclusive negative case 先失败为 `Expected -9223372036854775808 Was 5`，LSP query 返回 `min=5 max=5`。GREEN：WSL gcc focused 通过新 parser 3 PASS、新 LSP 3 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Positive Delta Widening | 已完成 | 2026-06-23 22:14 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：self-dependent loop plan 保存 signed delta 区间而不是单个严格正下界；helper 接受不引用当前 target/同一 replay plan 写入目标、可由现有推断证明为 int64、`min >= 0` 且 `max > 0` 的 delta 表达式。`step: 0..3` / `u8` 下 `while (flag) { narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 first-write self-dependent zero-inclusive positive-possible delta widening；zero-only no-op、negative/sign-crossing delta、same-loop-written delta dependency、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标中 zero-inclusive case 先失败为 `Expected 9223372036854775807 Was 5`，LSP query 返回 `min=5 max=5`；RED 前测试图重新生成曾被无关 AOT typed-call 拆分源文件缺失阻断，CMake 目标已改为只追加存在的可选拆分源。GREEN：WSL gcc focused 通过新 parser 2 PASS、新 LSP 2 PASS/checks、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive Expression Delta Widening | 已完成 | 2026-06-23 21:51 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：`type_inference_loop_assignment_self_dependency.c` 把 delta 从 direct identifier/literal 扩展为受限表达式，先拒绝引用当前 target 或含 member/call 等 unsupported 形状的 delta，再复用 `ZrParser_ExpressionType_Infer` 取得 int64 正下界；`type_inference_loop_assignment_join.c` 通过 self-dependency helper 递归检测 delta 表达式是否读取同一 loop replay plan 的写入目标。`step: 1..3` 下 `while (flag) { narrowed = narrowed + (step + 1); } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 first-write self-dependent positive expression delta widening；zero/negative delta、same-loop-written delta dependency、member/call/unsupported delta、multiplicative/conditional recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc 新 parser self-dependency 目标先失败为 `Expected 9223372036854775807 Was 5`，LSP query 返回 `min=5 max=5`。GREEN：WSL gcc focused 通过新 parser 1 PASS、新 LSP 1 PASS、loop assignment 16 PASS、LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive-Range Delta Widening | 已完成 | 2026-06-23 21:05 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 bounded loop recurrence 的下一层边界：`type_inference_loop_assignment_self_dependency.c` 的 positive delta 检测接受非 target 标识符的 int64 正下界区间，不再只接受 singleton；validator 仍拒绝同一 loop replay plan 内会被写入的 delta 标识符。`step: 1..3` 下 `while (flag) { narrowed = narrowed + step; } narrowed + 0` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 first-write self-dependent identifier delta 的 positive-lower-bound widening；delta 可能为 0/负、同循环写入的 delta、复杂表达式 delta、乘法/条件 recurrence、nested loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser positive-range delta 用例先失败为 `Expected 9223372036854775807 Was 5`，LSP query 返回 `min=5 max=5`。GREEN：WSL gcc focused 通过 loop assignment 16 PASS 与 LSP branch-assignment 25 PASS；WSL clang focused 与 Windows MSVC focused 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True Step Known-True-If Break-Branch At-Least-Once Range Join | 已完成 | 2026-06-23 20:47 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true condition 的 step-before-break deterministic-if 边界：`type_inference_loop_assignment_entry.c` 复用 literal/logical/constant bool 判断，能证明顶层 `if (true)` 只进入 then 分支并以 plain `break;` 跳过 step；`type_inference_loop_assignment_nested_if.c` 允许 missing `else` 作为无赋值目标路径；`type_inference_branch_assignment_join.c` 在 `if` condition 已知时只提交可达分支 assignment result。`if (true) { narrowed = 10; break; }` 这类 body 后续表达式在 parser 与 LSP local semantic query 中返回 `11..11`。该切片只声明顶层 deterministic-true `if` then 分支内 supported assignment + plain `break;` 的 step-before-break；symbolic/unknown if condition、else-less unknown branches、branch without break、continue、break with expression、无 break 的 known-true loop、复杂 nested control flow、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser known-true-if break-branch 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 16 PASS 与 LSP constant-condition 16 checks；WSL gcc key adjacent 通过 loop assignment 15 PASS 与 LSP branch-assignment 24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+key adjacent 通过同类目标；MSVC 构建仍输出既有 AOT warning，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step Nested-If Break-Branches At-Least-Once Range Join | 已完成 | 2026-06-23 20:24 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 step-before-break nested-if 边界：loop syntax helper 新增 plain-break guarantee 判断，entry helper 只在顶层 `if/else` 的 then/else 都保证以 plain `break;` 退出时，允许带 step 的 known-true/omitted loop 走 at-least-once body replay；body plan collector 会在收集该 nested-if assignment 后停止后续 replay。`if (flag) { narrowed = 10; break; } else { narrowed = 12; break; }` 这类 body 后续表达式在 parser 与 LSP local semantic query 中返回 `11..13`。该切片只声明顶层 if/else 双分支 plain-break、supported nested-if assignment replay 与 step-before-break；missing else、branch without break、continue、break with expression、无 break 的 known-true loop、复杂 nested control flow、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser nested-if break-branches 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 15 PASS 与 LSP constant-condition 15 checks；WSL gcc key adjacent 通过 loop assignment 15 PASS 与 LSP branch-assignment 24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+key adjacent 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init-Step Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:59 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 init+step/header-local+step break-prefix 边界：带 supported assignment init 或 direct identifier variable-declaration init、同时带 step 的 `for`，在 body 顶层 plain `break;` 证明 step 被跳过时，先应用 init scope，再用 at-least-once body replay 生成后继事实。assignment-init+step true/omitted 与 var-init+step true/omitted 四类 fixture 在 parser 与 LSP local semantic query 中均返回 `11..11`。该切片只声明 supported init/header-local + step 被顶层 plain break 跳过的形状；nested break、continue、break with expression、无 break 的 known-true loop、复杂 body control flow、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 4 个 init+step/header-local+step 用例先失败为 `Expected 11 Was 6`，LSP 4 个 query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 14 PASS 与 LSP constant-condition 14 checks；WSL gcc key adjacent 通过 loop assignment 15 PASS 与 LSP branch-assignment 24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+key adjacent 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 step-before-break 边界：plain-break 判断下沉到 loop syntax helper，`ZrParser_TypeInference_TryJoinForNumericAssignments` 对 no-init、condition known true 或省略、带 step 且 body 顶层 plain `break;` 的 `for`，允许在 step 不执行的退出路径上调用 at-least-once body replay。`for (; true; narrowed = 20) { narrowed = 10; break; } narrowed + 1` 与 omitted-condition step 变体在 parser 与 LSP local semantic query 中返回 `11..11`。该切片只声明 no-init、top-level plain break 在 step 前退出、supported numeric body assignment；init+step/header-local+step、nested break、continue、break with expression、无 break 的 known-true loop、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 两个 step 用例先失败为 `Expected 11 Was 6`，LSP 两个 query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 10 PASS 与 LSP constant-condition 10 checks；WSL gcc key adjacent 通过 loop assignment 15 PASS 与 LSP branch-assignment 24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+key adjacent 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 19:10 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 init/header-local break-prefix 边界：`ZrParser_TypeInference_TryJoinForNumericAssignments` 对 condition 为 known true 或省略 condition、带 supported assignment init 或 direct identifier variable-declaration init、且无 step 的 `for`，先在临时 init scope 中应用 header 初始化，再用 `JoinBodyAtLeastOnce` 重放 plain `break;` 前支持的 body assignment；提交 scope 时排除 header-local init binding。assignment-init true/omitted 与 var-init true/omitted 四类 fixture 在 parser 与 LSP local semantic query 中均返回 `11..11`。该切片只声明 no-step、supported assignment/direct var init、plain break 前缀；assignment step、combined var-init+step、destructuring/no-range header binding、false-condition init side effects、continue/复杂 break-sensitive CFG、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 4 个 init/header-local true/omitted 用例先失败为 `Expected 11 Was 6`，LSP 4 个 query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 8 PASS 与 LSP constant-condition 8 checks；WSL gcc 相邻通过 loop assignment 15 PASS、for-header 1 PASS、nested-loop 1 PASS、foreach cardinality 2 PASS、LSP for-header/nested/foreach/branch targets 1/1/2/24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+adjacent 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Condition Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 18:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 no-init/no-step break-prefix 边界：`ZrParser_TypeInference_TryJoinForNumericAssignments` 对 condition 为 known true 或省略 condition 且无 init/step 的 `for` 调用 `JoinBodyAtLeastOnce`；loop body plan 遇到 plain `break;` 时停止采集，保留 break 前 supported assignment。`for (; true; ) { narrowed = 10; break; } narrowed + 1` 与 `for (;;) { narrowed = 10; break; } narrowed + 1` 在 parser 与 LSP local semantic query 中返回 `11..11`。该切片只声明 no-init/no-step、plain break 前缀、supported numeric assignment；init/step/header-local binding 下的 known-true/omitted conditions、continue/复杂 break-sensitive CFG、fixed point、unsupported assignment expressions 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser true/omitted 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 constant-condition parser 4 PASS 与 LSP constant-condition 4 checks；WSL gcc 相邻通过 loop assignment 15 PASS、for-header 1 PASS、nested-loop 1 PASS、foreach cardinality 2 PASS、LSP for-header/nested/foreach/branch targets 1/1/2/24 PASS；WSL clang focused+key adjacent 与 Windows MSVC focused+adjacent 通过同类目标；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Var-Init Scoped Binding | 已完成 | 2026-06-23 18:17 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` variable-declaration init 的 false-condition 作用域边界：LSP `ZR_AST_FOR_LOOP` typecheck 现在在临时 runtime type binding scope 内检查 init/condition/step/body，退出 scope 后才调用 numeric assignment join，因此 header-local `step` 可供 loop 内部检查但不会泄漏到 loop 后表达式。`for (var step: int = 10; false; ) { narrowed = step; } step + 1` 在 parser 与 LSP local semantic query 中均没有后续 `step + 1` numeric fact；assignment init known-false 保持 `2..2`，unknown-condition direct var-init body replay 保持 `6..11`。该切片只声明 direct variable-declaration init scope isolation；destructuring/no-range 声明、initializer side effects、known-true/omitted conditions、loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser no-leak coverage 首跑已通过，LSP no-leak query 先失败并返回 `11..11`，证明 analyzer typecheck 泄漏 header binding。GREEN：WSL gcc/clang focused 与相邻 loop/LSP 目标通过；Windows MSVC 通过 constant-condition parser 2 PASS、LSP constant-condition 2 checks、for-header 1 PASS、branch-assignment 24 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Nonempty Cardinality Range Join | 已完成 | 2026-06-23 17:52 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 `foreach` cardinality 边界：direct identifier item binding 现在复用 iterable type 推断，并在数组 size constraint 可证明非空时走 at-least-once loop body join，不再保留 zero-iteration pre-loop binding。`items: int[2]; narrowed=5; foreach (item in items) { narrowed = 10; } narrowed + 1` 在 parser 与 LSP local semantic query 中返回 `11..11`；未知长度 `int[]` 仍返回 `6..11`。该切片只声明数组非空 cardinality、direct item binding 和 bounded assignment replay；destructuring、非数组 iterable、动态长度证明、true fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED-GATE：focused parser/LSP 用例已先写好，但实现前 RED 执行被 gcc build 目录占用和 clean build 依赖编译超时阻断，不记录失败输出。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric foreach cardinality 2 PASS 与 LSP numeric foreach cardinality query 2 PASS；WSL gcc/clang 相邻回归通过 numeric loop assignment 15 PASS、numeric nested loop assignment 1 PASS、numeric loop constant-condition 1 PASS、numeric for header assignment 1 PASS、LSP nested loop / for header / constant-condition query 各 1 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Loop Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 17:15 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内嵌套 loop 赋值合流：unknown-condition 外层 loop 的 replay plan 现在支持 nested `while`/`for`/`foreach` step，先在外层 replay scope 内调用内层 loop 的现有 numeric assignment join，再记录外层 plan target binding，并与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { while (inner) { narrowed = 10; } } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。新增 `type_inference_loop_assignment_nested_loop.*` 承担 nested loop target collection 与 join 分派，并把 replay scope helper 下沉到 `type_inference_loop_assignment_scope.c`，主 join helper 保持 982 行。该切片只声明 supported nested loop assignment replay；true fixed point、known-true/omitted loop condition、unsupported statements/assignments、复杂 recurrence 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 新目标先失败为 `Expected 11 Was 6`，LSP 新目标返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric nested loop assignment 1 PASS 与 LSP numeric nested loop assignment query 1 PASS；WSL gcc 相邻回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、numeric for header assignment 1 PASS、LSP numeric loop constant-condition query 1 PASS、LSP numeric for header assignment query 1 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Combined Init-Step Assignment Coverage | 已完成 | 2026-06-23 16:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for header 组合覆盖：显式 unknown condition 的 assignment init + assignment step 组合已由既有 bounded replay 支持，init 先写入临时 scope，body+step replay 再从该 scope 继续执行。`narrowed=5; for (narrowed = 1; flag; narrowed = 10) { flag; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `2..11`。该切片是覆盖确认，未改生产代码；known-true/omitted condition、false-condition variable-declaration init、嵌套 loop、true fixed point、step side-effect 精化与 general CFG-wide numeric dataflow 仍不声明完成。 | COVERAGE-GATE：WSL gcc 新 parser/LSP 目标首跑通过，证明现有实现已覆盖 combined init+step。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric for header assignment 1 PASS 与 LSP numeric for header assignment query 1 PASS；WSL gcc 相邻回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；`git diff --check` 通过，只有既有 LF/CRLF warning；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Constant-False Condition Range Application | 已完成 | 2026-06-23 16:06 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for constant-false condition：known-false init-assignment shortcut 现在不只识别 bool literal `false`，还会读取 condition logical fact，并回退到 AST constant boolean evaluation。`narrowed=5; for (narrowed = 1; 1 == 2; ) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `2..2`。该切片只声明 supported assignment init、可确定为 false 的 condition 和 no-body/no-step execution；known-true/omitted condition、variable-declaration init under false condition、combined init+step、nested loop、true fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 11`，LSP 新目标返回 `2..11`。GREEN：WSL gcc/clang focused 与 Windows MSVC focused 通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Non-Assignment Step Zero-Or-More Range Join | 已完成 | 2026-06-23 15:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for non-assignment step：显式 unknown condition 的 `for (; flag; flag)` 现在不会因为 step 不是 assignment expression 而跳过 body assignment replay，而是回落到 body-only zero-or-more join。`narrowed=5; for (; flag; flag) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。该切片只声明 non-assignment step 不阻断已有 bounded body assignment join；step side-effect 精化、nested loop、true fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 15 PASS、LSP numeric branch assignment query 24 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 15/24 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch refinement query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Init Assignment Range Application | 已完成 | 2026-06-23 15:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for known-false condition 的 init assignment 保留：`ZrParser_TypeInference_TryJoinForNumericAssignments` 在 condition fact 为 known false 时应用 supported assignment-expression init，然后停止 body/step replay。`narrowed=5; for (narrowed = 1; false; ) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `2..2`。该切片只声明 existing numeric-range target、numeric-range assignment RHS、显式 known-false condition 和 no-body/no-step execution；known-true/omitted condition、variable-declaration init leakage、combined init+step、嵌套 loop、true fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 14 PASS、LSP numeric branch assignment query 23 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 14/23 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch refinement query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Var-Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 15:05 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for variable-declaration init：`type_inference_loop_assignment_entry.c` 现在在临时 replay scope 内把直接 identifier `var step: int = 10` 注册为 loop-local numeric binding，后续 body/step replay 可读取它；joined result 提交回外层环境时排除该 loop-local 名称，避免 header 局部变量泄漏。`narrowed=5; for (var step: int = 10; flag; ) { narrowed = step; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。该切片只声明 direct identifier `var` init、可推断 numeric initializer、显式 unknown condition 和当前 bounded assignment replay；destructuring/no-range 声明、combined init+step 精化、嵌套 loop、true fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 13 PASS、LSP numeric branch assignment query 22 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 13/22 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch refinement query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Item-Dependent Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 14:43 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach item-dependent assignment：`ZrParser_TypeInference_TryJoinForeachNumericAssignments` 现在会在临时 replay scope 内为直接 identifier item 绑定 iterable element type，再复用已有 loop body join，并在提交回外层环境时排除 item binding。`items` element range 为 `2..4` 时，`for (var item in items) { narrowed = item; } narrowed + 1;` 在 parser 中返回 `3..6`；LSP `items: u8[]` 对应 fixture 返回 `1..256`。该切片只声明直接 item binding、可推断 iterable element numeric range、bounded simple assignment replay 与 zero-or-more join；destructuring pattern、显式 foreach type annotation 专用路径、cardinality 精化、嵌套 loop、真正 fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 3 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 12 PASS、LSP numeric branch assignment query 21 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 12/21 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch refinement query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Singleton Delta Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 14:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内自依赖赋值边界：unknown-condition `while` replay plan 现在支持 first-write `target = target + step`，其中 `step` 是非 target、非 loop 写入目标、int64 正单点区间标识符。`narrowed=5; step=1; while (flag) { narrowed = narrowed + step; } narrowed + 0;` 在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。实现把 self-dependent delta 解析从纯字面量扩展到查询当前 `typeEnv` singleton binding，并新增 plan-target guard，避免把同一轮 loop 内会被写入的标识符当作稳定 delta。该切片只声明 first-write int64 target、正向 singleton identifier delta；non-singleton/non-identifier delta、复杂 recurrence、嵌套 loop、true fixed point、for/foreach 扩展与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 9223372036854775807 Was 5`，LSP query 返回 `min=5 max=5`。GREEN：WSL gcc focused 通过 numeric loop assignment 11 PASS、LSP numeric branch assignment query 20 PASS；WSL clang AOT-off focused 与 Windows MSVC focused 通过同两目标 11/20 PASS；WSL gcc 相邻回归通过 numeric branch assignment 9 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP numeric branch refinement query 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；AOT-on WSL clang rebuild 被无关 dirty-tree AOT `typeTableIndex` 编译错误阻断，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Step Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 13:35 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for step dataflow：无 init、显式 unknown condition 的 `for (; flag; target = value)` 现在把 body replay 和 assignment step 作为同一轮 loop replay 的顺序步骤，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; for (; flag; narrowed = 10) { flag; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。实现新增 `ZrParser_TypeInferenceLoopAssignment_JoinBodyWithStep`，并把当前 scope binding 写入 helper 拆到 `type_inference_loop_assignment_scope.c`，主 loop join helper 保持 952 行。该切片只声明 no-init assignment step、existing numeric-range target、numeric-range assignment RHS 和显式 unknown condition；variable-declaration init、combined init+step 覆盖、omitted/constant condition、嵌套 loop、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：numeric loop assignment 10 PASS、LSP numeric branch assignment query 19 PASS；WSL gcc 相邻回归通过：numeric branch assignment 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 12:53 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for init dataflow：`ZrParser_TypeInference_TryJoinForNumericAssignments` 现在对无 step、显式 unknown condition 的 `for (target = value; flag; )` 先在临时 scope 内应用 assignment init numeric range，再调用共享 loop-body zero-or-more replay/join，最后只把成功 join 后的 init/body 合流结果提交回外层环境。因此 `narrowed=5; for (narrowed = 1; flag; ) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `2..11`。该切片只声明 existing numeric-range target、numeric-range assignment RHS、无 step、显式 unknown condition 的 assignment init；variable-declaration init、step、omitted/constant condition、嵌套 loop、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 9 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 18 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach loop assignment replay：`ZrParser_TypeInference_TryJoinForeachNumericAssignments` 现在复用共享 bounded loop assignment body，parser `ZR_AST_FOREACH_LOOP` 推断和 LSP foreach typecheck 在 foreach binding scope 弹出后接入该 helper。因此 `narrowed=5; for (var item in items) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。实现同时把 while/for/foreach 入口过滤拆到 `type_inference_loop_assignment_entry.c`，由内部 `type_inference_loop_assignment_join_internal.h` 暴露 body join，主 helper 降到 802 行。该切片只声明不依赖 foreach item 的 bounded simple numeric assignment replay；item-dependent RHS、iterable cardinality 精化、for init/step/constant-condition、嵌套 loop、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser foreach 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 8 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 17 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:14 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内赋值合流：bounded loop assignment helper 现在从 while 专用入口拆出共享 body，并新增传统 `for (; flag; )` 入口。`ZrParser_TypeInference_TryJoinForNumericAssignments` 只接受无 init、显式 unknown condition、无 step 的 `ZR_AST_FOR_LOOP`，因此 `narrowed=5; for (; flag; ) { narrowed = 10; } narrowed + 1;` 在 parser 与 LSP local semantic query 中返回 `6..11`。同时把 parser loop assignment 覆盖拆到 `test_numeric_loop_assignment_dataflow.c`，原 branch assignment 测试回到 655 行。该切片只声明 init-free/no-step/unknown-condition traditional for；for init/step dataflow、无条件或常量条件 for、foreach、嵌套 loop body、非字面量 recurrence、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser loop target 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 7 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 16 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Decrement Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 05:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内自依赖赋值边界：unknown-condition `while` replay plan 现在识别 target 在当前 plan 中尚未被前置写入的 `target = target - positive_integer_literal`，并由 `type_inference_loop_assignment_self_dependency.*` 把 signed delta 的负向一次或多次执行结果 widened 为 `[INT64_MIN, preMax - literal]`，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed - 1; } narrowed + 0;` 现在在 parser 与 LSP local semantic query 中返回 `-9223372036854775808..5`。该切片只声明 int64 正整数字面量自减；非字面量 delta、`literal - target`、乘法/条件 recurrence、后续 RHS 因边界失去 range 的情况、嵌套 loop、for/foreach、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：Windows MSVC parser 先失败为 `Expected -9223372036854775808 Was 5`；LSP 新用例首次运行暴露 query fixture occurrence 参数错误，修正后用于 GREEN 覆盖。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 15 PASS、LSP numeric branch assignment query 15 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Increment Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 04:31 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内自依赖赋值边界：unknown-condition `while` replay plan 现在识别 target 在当前 plan 中尚未被前置写入的 `target = target + positive_integer_literal`，并由 `type_inference_loop_assignment_self_dependency.*` 计算一次或多次执行的 widening range，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed + 1; } narrowed + 0;` 现在在 parser 与 LSP local semantic query 中返回 `5..9223372036854775807`。该切片只声明 int64 正整数字面量自增；自减、非字面量 delta、乘法/条件 recurrence、后续 RHS 因上界溢出而失去 range 的情况、嵌套 loop、for/foreach、真正 loop fixed point 与 general CFG-wide numeric dataflow 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 9223372036854775807 Was 5`，LSP assignment query 返回 `min=5 max=5`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 14 PASS、LSP numeric branch assignment query 14 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；新增源文件触发 CMake glob 重新配置，构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Nested If Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 04:01 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内受限嵌套分支赋值合流：unknown-condition `while` replay plan 现在支持把块内 nested simple `if/else` 作为 replay step，在临时 loop replay scope 中复用既有 branch assignment join，再把当前 plan target binding 存入 loop result 并与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { if (inner) { narrowed = 1; } else { narrowed = 10; } } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中返回 `2..11`。同时把 loop assignment 语法拆包与 nested-if target collection 拆到 `type_inference_loop_assignment_syntax.*` / `type_inference_loop_assignment_nested_if.*`，主 helper 回到 878 行。general CFG-wide numeric dataflow、self-dependent RHS、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 13 PASS、LSP numeric branch assignment query 13 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Statement Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:29 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内赋值合流：`type_inference_loop_assignment_join.c` 的 plan collection 现在允许 unknown-condition `while` 块内在 simple numeric assignment 前后出现普通 expression statement，并只把 assignment 纳入 replay plan。`narrowed=5; while (flag) { flag; narrowed = 10; flag; } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中返回 `6..11`。general CFG-wide numeric dataflow、self-dependent RHS、嵌套 loop/if loop body、真正 loop fixed point、for/foreach、unsupported statements/assignments 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 12 PASS、LSP numeric branch assignment query 12 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Target Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:08 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内跨目标赋值合流：`type_inference_loop_assignment_join.c` 现在对 unknown-condition `while` 的 simple assignment block 做顺序 replay，先在临时 scope 中写入 loop-body target range，再逐个 target 与 pre-loop range 做 zero-or-more join。`low=5, high=20; while (flag) { low = 1; high = low + 10; } high + low;` 现在在 parser 与 LSP local semantic query 中返回 `12..25`。general CFG-wide numeric dataflow、self-dependent RHS、真正 loop fixed point、for/foreach、unsupported statements/assignments 仍不声明完成。 | RED：新增 LSP assignment query 先返回 `min=25 max=25`；parser 对应 RED 覆盖在共享 checkout 中曾被无关 AOT 编译错误打断。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 11 PASS、LSP numeric branch assignment query 11 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 02:42 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：新增 focused while helper，对 unknown-condition `while` 中块内唯一 simple integer assignment，把进入循环前 target range 与循环体 RHS range 作为零次或多次执行结果合流。`var narrowed: int = 5; while (flag) { narrowed = 10; } return narrowed + 1;` 现在在 parser 与 LSP local semantic query 中返回 `6..11`。general CFG-wide numeric dataflow、self-dependent RHS、loop fixed point、for/foreach、多目标循环和 unsupported assignment 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 10 PASS、LSP numeric branch assignment query 10 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、CFG reachability 29 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Branch Assignment Dataflow | 已完成 | 2026-06-23 01:00 +08:00 | 在 split assignment join helper 上补 bounded nested simple `if/else` 赋值 replay：plan 分析会递归发现嵌套 `ZR_AST_IF_EXPRESSION` 内的 simple assignment target；branch replay 会先在临时 branch scope 中对嵌套 if 调用同一 assignment join helper，再把当前 plan target binding 存回外层 branch result。`if (flag) { if (inner) { narrowed = 1; } else { narrowed = 2; } } else { if (inner) { narrowed = 10; } else { narrowed = 20; } } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `2..21`。此切片只声明 simple branch blocks 内的嵌套 simple if/else assignment dataflow；loop/fixed-point、任意嵌套控制流、unsupported assignments、任意 symbolic range-set/hole-set 与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc parser 新用例先失败为 `Expected 2 Was 1`，LSP assignment query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：numeric branch assignment 9 PASS、LSP numeric branch assignment query 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Segment-Preserving Branch Assignment Join | 已完成 | 2026-06-23 00:42 +08:00 | 在 split assignment join helper 后新增 `type_inference_branch_assignment_segments.c/.h`，让 branch assignment join 不只写回 min/max envelope，还能把两侧单点或已有 segment payload 规范化、排序、合并，并在仍不连续时写入 join 后 binding。`if (flag) { narrowed = 1; } else { narrowed = 10; } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 envelope `2..11` 与 segments `2..2` / `11..11`。同时把 LSP assignment query 用例拆到 `test_lsp_numeric_branch_assignment_semantic_query.c`，原 branch refinement query 文件回到只覆盖 refinement。此切片只声明 simple branch-assignment join 的 segment-preserving 合流；loop/fixed-point、嵌套控制流 dataflow、任意 symbolic range-set/hole-set 与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc parser 新用例先失败为 `Expected 2 Was 0`，新 LSP assignment query 目标返回 `exprSegments=0` / `numeric segments=0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：numeric branch assignment 8 PASS、LSP numeric branch assignment query 8 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Target Branch Assignment Dataflow | 已完成 | 2026-06-23 00:09 +08:00 | 在 split assignment join helper 上把分支赋值合流从单 target 推进到同一 simple if/else 分支块内的多 target 顺序赋值：helper 收集两侧分支中出现的 simple assignment target，在 branch-scoped replay environment 中按语句顺序推断 RHS 并记录每个 target 的最终类型，随后逐个 target 对 then/else 可达路径 range 做 min/max envelope join 并写回当前 type environment。因此 `if (flag) { low = 1; high = low + 1; } else { low = 10; high = low + 10; } high + low;` 现在在 parser 与 LSP local semantic query 中报告 `3..30`。此切片只声明 simple if/else 分支块内多 target、顺序、数值 assignment 的局部 replay/join；loop/fixed-point、segment-preserving assignment join、跨嵌套控制流与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected 3 Was 0`，LSP query 返回 `min=0 max=0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 7 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 23 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric RHS-Dependent Sequential Branch Assignment Range Join | 已完成 | 2026-06-22 23:44 +08:00 | 在 split assignment join helper 上把分支块内赋值从“唯一 simple assignment”扩展为“同一 target 的顺序 simple assignments”：helper 在 branch-scoped replay environment 中按顺序推断 RHS 并更新临时 binding，因此后一条 `narrowed = narrowed + 1` 能读取前一条分支内写入范围，两侧最终范围再合并为 min/max envelope。`if (flag) { narrowed = 1; narrowed = narrowed + 1; } else { narrowed = 10; narrowed = narrowed + 1; } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `3..12`。此切片只声明同一 target 的顺序 simple numeric assignments；多 target/多变量 branch dataflow、loop/fixed-point、segment-preserving assignment join 与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected 3 Was 1`，LSP query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 6 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 22 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nonterminal Multi-Statement Branch Assignment Range Join | 已完成 | 2026-06-22 23:21 +08:00 | 在 split assignment join helper 上把分支块识别从“块末尾 simple assignment”扩展并收紧为“块内唯一 simple assignment”：当前覆盖 then/else 两侧都有后置普通表达式、块中间对同一数值变量赋值的形态，并继续把两侧 RHS range 合并为 min/max envelope。`if (flag) { narrowed = 1; flag; } else { narrowed = 10; flag; } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `2..11`。此切片只声明分支块内唯一 simple assignment；多次写、RHS 依赖前置写、loop/fixed-point、segment-preserving assignment join 与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected 2 Was 1`，LSP query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 5 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 21 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Statement Terminal Branch Assignment Range Join | 已完成 | 2026-06-22 23:04 +08:00 | 在 split assignment join helper 上把分支块识别从“块内唯一表达式”推进到“块末尾 simple assignment”：当前覆盖 then/else 两侧都有前置普通表达式、末尾对同一数值变量赋值的形态，并继续把两侧 RHS range 合并为 min/max envelope。`if (flag) { flag; narrowed = 1; } else { flag; narrowed = 10; } narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `2..11`。此切片只声明 terminal simple assignment；assignment 不在末尾、多次写、RHS 依赖前置写、loop/fixed-point、segment-preserving assignment join 与 general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected 2 Was 1`，LSP query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 4 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 20 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Else-Only Branch Assignment Range Join | 已完成 | 2026-06-22 22:44 +08:00 | 在已有 assignment join helper 上补 explicit else-only 覆盖：当 then branch 没有同名赋值、else branch 对同一数值变量做 simple assignment 时，true path 复用进入 if 前的 binding range，false path 使用 else RHS range，两者合流回当前 type environment。`var narrowed: int = 5; if (flag) { flag; } else { narrowed = 10; } return narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `6..11`。此切片只声明 simple else-only assignment + retained true-path numeric range；multi-statement、loop/fixed-point、segment-preserving assignment join、general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected 11 Was 6`，LSP else-only query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 3 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 19 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Numeric One-Sided Branch Assignment Range Join | 已完成 | 2026-06-22 22:26 +08:00 | 在已有 if/else 同名赋值合流 helper 上扩展 simple if true-branch 单侧赋值：无 else 时，false path 复用进入 if 前在 false-branch scope 中保留的数值 binding range，再与 then RHS range 合流。同步补 `ZrParser_TypeInference_TryApplyInitializerNumericRange`，让显式类型注解的数值变量声明（如 `var narrowed: int = 5`）仍保留 initializer 单点范围供后续分支合流使用；parser lambda return binding 与 LSP typecheck binding 共享该规则。`var narrowed: int = 5; if (flag) { narrowed = 10; } return narrowed + 1;` 现在在 parser 与 LSP local semantic query 中报告 `6..11`。此切片只声明 simple one-sided true-branch assignment + retained pre-branch numeric range；multi-statement、else-only、loop/fixed-point、segment-preserving assignment join、general CFG-wide numeric dataflow 仍待后续。 | RED：WSL gcc 新 parser 用例先失败为 `Expected TRUE Was FALSE`，LSP if/then query 返回 `hasRange=0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 2 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 18 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 2 / Definite Assignment Declaration Related Information | 已完成 | 2026-06-22 21:36 +08:00 | 结构化诊断新增 owned `relatedInformation` 数组和 add/free 支持；definite-assignment read diagnostics 现在在 declarationRange 附加 `Variable declaration is here`；LSP `Diagnostic_FromStructured` 保留 related 信息并通过现有 LSP diagnostic 转换发布。LSP semantic query diagnostics 在有 AST 时改用 CFG-backed definite assignment resolver，分支 join 例子 `var seed: int; if (flag) { seed = 1; } return seed;` 现在发布 `possibly_uninitialized_read` 并指向声明。此切片只声明未初始化/可能未初始化 read 的声明 relatedInformation；fix-it、descriptorId、诊断码注册表、所有权 related 和 compiler 外部/二进制序列化仍待后续。 | RED：parser/LSP 新断言先缺 related 信息，LSP branch fixture 起初 `diagnostics=0`；GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 11 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_numeric_branch_assignment_dataflow_test` 1 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 17 PASS。`zr_vm_compiler_semantic_query_diagnostics_test` 本轮未作为 GREEN 证据：它在 typed metadata export 阶段因无效 `exportedVar->name` segfault，发生在当前 LSP relatedInformation 路径之前；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Cross-Block Branch Assignment Range Join | 已完成 | 2026-06-22 21:03 +08:00 | 新增 `type_inference_branch_assignment_join.c`，parser `ZR_AST_IF_EXPRESSION` 推断和 LSP `semantic_analyzer_typecheck.c` 共用同一 simple if/else assignment join helper：仅当 then/else 两侧各有一个同名数值赋值、目标 binding 已是数值类型、两侧 RHS 均有 numeric range 时，把两侧范围 join 为 min/max envelope 并写回当前 type environment。`if (flag) { narrowed = 1; } else { narrowed = 10; } narrowed + 1` 现在在 parser 与 LSP local semantic query 中都报告 `2..11`。新增 `zr_vm_numeric_branch_assignment_dataflow_test`，并扩展 `zr_vm_language_server_numeric_branch_semantic_query_test` 到 17 项。此切片只声明 simple if/else same-variable assignment range join；one-sided branch、多语句分支、loop/fixed-point、segment-aware assignment join、general CFG-wide numeric dataflow 仍待后续阶段。 | TDD RED 先让 WSL gcc 新 parser 用例失败为 `Expected 2 Was 1`，LSP 新用例返回无 range fact。修复后 WSL gcc、WSL clang、Windows MSVC focused 目标均通过：`zr_vm_numeric_branch_assignment_dataflow_test` 1 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 17 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 10 PASS。WSL clang 初次 local query 异常定位为 stale test object / semantic fact ABI mismatch，强制重建后通过；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Min-Only Negative Interval Diagnostics | 已完成 | 2026-06-22 19:46 +08:00 | min-only / no finite-upper-bound 数组现在会对“索引区间可能为负”的已知整数索引发布 warning 级 semantic query diagnostic `array_index_may_be_out_of_bounds`，但不会伪造不存在的上界越界判断。新增 `int[1 ..]` fixture：`index: int` 的 `values[index]` 发布 1 条 “may be negative” warning，`index: u8` 的正向无界索引保持静默；无有限上界场景的 warning message 不再输出误导性的 `array max size: 0`。此切片只声明已知整数 range 的负向可能性策略；truly unknown/no-inferable-range 索引、运行时 bounds check、跨块 numeric dataflow、二进制/外部诊断序列化和旧 `ZrParser_ArrayIndexBounds_Check` 替换仍不在本轮范围内。 | TDD RED 先让 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 16 tests / 1 failure：`Expected 1 Was 0`，并让 `zr_vm_language_server_semantic_query_diagnostics_test` 新用例返回 `Expected one min only array negative interval diagnostic; diagnostics=0`。修复后 WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug focused/adjacent 目标通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 10 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 16 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 10 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / CFG Folded Constant Relational Conditions | 已完成 | 2026-06-22 19:15 +08:00 | `cfg.c` 的 shared condition folder 现在在 `cfg_node_constant` 折叠出两边 `SZrParserCfgConstant` 后，统一比较 `==`/`!=` 以及 same-kind integer/float/char 的 `<`/`>`/`<=`/`>=`；因此 checked integer/finite float arithmetic/unary folding 产出的常量也能继续驱动 CFG 分支剪枝。新增 `(1 + 2) > 2` 与 `(1.5 + 2.25) > 3.0` 覆盖，分别剪枝不可达 else branch。此切片只声明 same-kind folded constant relational comparison；mixed-kind relational、string/bool relational、数值区间、符号值和更复杂组合表达式仍保持 unknown/待完成。 | TDD RED 先让 WSL gcc `zr_vm_cfg_constant_conditions_test` 新用例失败为 1/22 `Expected Non-NULL`。修复后 WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug focused/adjacent 目标通过：`zr_vm_cfg_constant_conditions_test` 22 PASS、`zr_vm_cfg_float_arithmetic_conditions_test` 12 PASS、`zr_vm_cfg_integer_bitwise_conditions_test` 5 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 6 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 10 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / LSP Numeric Segment Display Cap And Six-Segment Stress | 已完成 | 2026-06-22 18:32 +08:00 | LSP numeric range 展示层现在统一走 `lsp_numeric_range_text.c/.h`：hover、rich hover、inlay hint label、completion detail 和 signature parameter documentation 共享同一个 formatter，按 `ZR_LSP_NUMERIC_RANGE_SEGMENT_DISPLAY_LIMIT` 只展示前 4 个整数 segment，并用 `... +N more` 汇总剩余段；内部 `SZrInferredType` / `SZrSemanticNumericFact` 仍保留完整 `rangeSegmentCount` 与动态 `rangeExtraSegments`，不为 UI 文本裁剪事实本体。新增 6 段 same-identifier `\|\|` fixture：`seed == 1 \|\| seed == 3 \|\| seed == 5 \|\| seed == 7 \|\| seed == 9 \|\| seed == 11` 经 `seed + 1` 产生完整段集 `2..2` / `4..4` / `6..6` / `8..8` / `10..10` / `12..12`，LSP 文本展示为 `2..12 (segments 2..2, 4..4, 6..6, 8..8, ... +2 more)`。同时把 local semantic hover fact 文本拼接拆到 `lsp_local_semantic_hover_text.c/.h`，让 `lsp_local_semantic_query.c` 回落到 720 行并继续只负责 query/fact selection。此切片只声明已测试 6 段压力 fixture 和 LSP 展示 cap policy；不声明任意布尔公式、无界 segment 压力、CFG-wide numeric dataflow、completion resolve UI 扩展或全仓库 GREEN。 | TDD RED 先让独立 WSL gcc Debug 构建 `build/codex-lsp-segment-display-wsl-gcc-debug` 中的 hover/inlay surface 测试失败：底层 query 已保留 6 段，但 hover 与 inlay/completion/signature 仍输出全部 6 段而不是裁剪文本；parser 6 段事实覆盖作为 lower-layer gate 通过。修复后 WSL gcc、WSL clang、Windows MSVC 独立 Debug focused build/run 均通过：`zr_vm_numeric_branch_segment_sets_test` 3 PASS、`zr_vm_language_server_expression_fact_hover_test` 5 PASS、`zr_vm_language_server_inlay_semantic_facts_test` 9 PASS。共享 `build-wsl-gcc` 当时被其他 AOT 会话占用 CMake regenerate，因此本轮使用独立构建目录；WSL clang 首次 configure/build 超时后重跑通过；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Dynamic Segment-Set And Interior Hole Range Refinement | 已完成 | 2026-06-22 16:43 +08:00 | 数值范围 segment payload 从固定两段扩展为“前两段内联 + 额外段动态数组”：`SZrInferredType` 与 `SZrSemanticNumericFact` 新增 `rangeExtraSegments`，并通过 `ZrParser_NumericRangeSegments_*` / `ZrParser_InferredType_*RangeSegment*` 统一 reset/free/copy/access；共享实现拆到 `type_system_numeric_segments.c`，避免继续把 `type_system.c` 撑大。branch refinement 现在对受支持的同 identifier segment array 做 normalize/intersect/union，true-branch interior `seed != 10` 可把 `0..20` 分成 hole-set，`seed + 1` 报告 envelope `1..21` + segments `1..10` / `12..21`；`seed < 5 \|\| seed == 10 \|\| seed > 20` 经 `+ 1` 报告三段 `1..5` / `11..11` / `22..256`。LSP hover、inlay、completion、signature 和 local query 不再按固定 capacity 截断 segment 输出。此切片只声明当前测试覆盖的 interior `!=` 空洞与 same-identifier 三段 `\|\|` 子集；不声明任意布尔公式、任意规模/压力策略、CFG-wide numeric dataflow、completion resolve UI 扩展或全仓库 GREEN。 | TDD RED 先让 WSL gcc 新增 `zr_vm_numeric_branch_segment_sets_test` 失败：interior `!=` 与三段 `\|\|` 两个用例均为 `Expected TRUE Was FALSE`。修复后 WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug 同一组 focused/adjacent 目标通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_numeric_branch_segment_sets_test` 2 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 10 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 16 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_expression_fact_hover_test` 4 PASS、`zr_vm_language_server_inlay_semantic_facts_test` 9 PASS。MSVC 使用 Visual Studio 17.14 环境；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Else-If And Nested Logical Combination Range Refinement | 已完成 | 2026-06-22 15:58 +08:00 | `type_inference_branch_refinement.c` 现在可以把同一整数 identifier 上的受支持布尔子条件先规约为 refined type，再对 true-branch `&&` 做交集、对 true-branch `\|\|` 做 union/固定两段 segment 表示；因此 `if ((seed > 2 && seed < 10) \|\| seed == 20) { seed + 1; }` 在 `seed: 0..255` 下报告 envelope `4..21`，segments `4..10` / `21..21`。同时补 `else if` 链覆盖：外层 false scope 与内层 true scope 叠加后，`else if (seed < 20) { seed + 1; }` 报告 `11..20`，LSP production path 的 local semantic query 也能消费该事实。此切片只声明 same-identifier、固定两段 payload 的嵌套 `&&`/`\|\|` 可表示子集，以及 `else if` 链收窄覆盖确认；不声明任意布尔公式、任意 segment-set/hole-set、跨块 numeric dataflow 或全仓库 GREEN。 | TDD RED 先让 WSL gcc `zr_vm_numeric_branch_refinement_test` 的 nested `&&`/`\|\|` 用例失败为 `Expected TRUE Was FALSE`，并让 `zr_vm_language_server_numeric_branch_semantic_query_test` 返回旧宽范围 `1..256`；`else if` LSP 用例作为 coverage gate 直接通过，证明 production path 已能叠加外层 false 与内层 true scope。修复后 WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug 同一组 focused/adjacent 目标通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 15 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 10 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 15 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS。构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / LSP Segmented Numeric Range Inlay Completion Signature Display | 已完成 | 2026-06-22 15:35 +08:00 | LSP inlay hint label、completion detail、signature parameter documentation 现在会在整数 numeric fact 的 `range min..max` 文本后追加固定两段 segment payload，例如 `range 1..256 (segments 1..10, 22..256)`。`tests/language_server/test_lsp_inlay_semantic_facts.c` 新增同一 fixture 覆盖三条 surface：`var segmented = seed + 1` 的 inlay hint、`segmented` completion detail，以及 `pick(seed + 1)` 的 signature argument docs。修复还让 `ZrParser_SemanticFacts_FindNumericByNode` 在同一 AST node 存在多条 numeric fact 时选择更丰富的事实（exact、segmented、range 更窄、带 unsigned payload 的事实优先），避免 LSP symbol surfaces 被较早 materialize 的宽泛 initializer fact 截住。此切片只声明 fixed two-segment numeric fact payload 在 inlay/completion/signature 三处显示，并声明 same-node numeric fact lookup 的 richer-fact preference；不声明任意 segment-set/hole-set、跨块 numeric dataflow、completion resolve UI 扩展或全仓库 GREEN。 | TDD RED 先让 WSL gcc `zr_vm_language_server_inlay_semantic_facts_test` 新用例失败为 `Expected inlay hint to include segmented numeric range; hintCount=1`；添加 formatter 后仍失败且诊断显示首个 label 为 `: int, range 1..256, unsigned 1..256`，证明问题在 lower/shared fact selection 而不只是 formatter。随后 `zr_vm_semantic_facts_test` 新增 same-node broad/segmented numeric fact 选择覆盖，修复后 WSL gcc focused 两个目标通过：semantic facts 10 PASS、inlay semantic facts 9 PASS。最终 WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug 同一组 focused/adjacent 目标通过：`zr_vm_semantic_facts_test` 10 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 13 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 13 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_expression_fact_hover_test` 4 PASS、`zr_vm_language_server_inlay_semantic_facts_test` 9 PASS。构建仍输出既有 `%type` format warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / LSP Segmented Numeric Range Hover Rendering | 已完成 | 2026-06-22 15:01 +08:00 | LSP local numeric hover 现在会消费 `SZrSemanticNumericFact.rangeSegmentCount/rangeSegments`，在已有 `Numeric range: min..max` 行内追加固定分段展示，例如 `Numeric range: 1..256 (segments 1..10, 22..256)`；plain local hover、public hover 和 rich hover 的 `numericRange` section 都共享同一 parser-owned numeric fact。`tests/language_server/test_lsp_expression_fact_hover.c` 新增 `seed < 10 \|\| seed > 20` 的分段范围 hover 覆盖，确认 query payload、plain/public hover 文本和 rich-hover role 一致。同时补 `tests/parser/test_numeric_branch_refinement.c` 与 `tests/language_server/test_lsp_numeric_branch_semantic_query.c` 的 false-branch `&&` disjoint coverage；该覆盖在实现前直接通过，证明上一阶段的 logical-union helper 已支持 `!(seed > 10 && seed < 20)` 的两段 complement，此轮没有为该路径追加生产代码。此切片只声明 LSP hover 消费固定两段 numeric fact payload 和 false-branch `&&` 覆盖确认；不声明 completion/inlay/signature 分段展示、任意 segment-set/hole-set、完整布尔公式或跨块 numeric dataflow。 | TDD RED 先让 WSL gcc `zr_vm_language_server_expression_fact_hover_test` 新用例失败：底层 `numericFact` 已有 envelope `1..256`、segments `1..10` / `22..256`，但 hover 仍只显示 `Numeric range: 1..256`。修复后 WSL gcc focused hover 通过 4 PASS；WSL gcc、WSL clang、Windows MSVC `build/codex-semantic-*` Debug 同一组 focused/adjacent 目标通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 13 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 13 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_expression_fact_hover_test` 4 PASS。额外 WSL gcc 相邻 `zr_vm_language_server_local_semantic_hover_test` 仍在既有 loop jump exit cause 用例失败，失败路径不经过 numeric segment formatter；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Logical-Or Disjoint Segment Range Refinement | 已完成 | 2026-06-22 14:35 +08:00 | `SZrInferredType` / `SZrSemanticNumericFact` 现在在既有 min/max envelope 之外携带固定 2 段 numeric range payload（`rangeSegmentCount` / `rangeSegments`）。`type_inference_branch_refinement.c` 能保留 simple same-identifier logical union 的不相交分段，因此 `if (seed < 10 \|\| seed > 20) { seed + 1; }` 在 `seed: 0..255` 下会把 then-scope `seed` 表示为 envelope `0..255` + segments `0..9` / `21..255`；`type_inference_numeric_range.c` 会把这些 segments 通过 scalar `+` / `-` 平移，所以 `seed + 1` 的 parser numeric fact 与 LSP local semantic query 都报告 envelope `1..256` + segments `1..10` / `22..256`。既有 oversized copy/init 文件只补 segment payload 传播，新分支语义仍留在 split branch helper。此切片只声明固定 2 segment payload 与 simple same-identifier true-branch `\|\|` disjoint 覆盖；不声明任意 segment-set/hole-set、完整布尔公式或 De Morgan、segment-aware hover 渲染、symbolic arithmetic、CFG dataflow，或未加测试的 false-branch `&&` disjoint 覆盖。 | TDD RED 先让 WSL gcc/clang focused build 编译失败，因为 `SZrInferredType` 与 `SZrSemanticNumericFact` 尚无 `rangeSegmentCount` / `rangeSegments` 成员，证明 range-set payload 不存在。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 12 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 12 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Unary-Not Branch Polarity Interval Refinement | 已完成 | 2026-06-22 14:09 +08:00 | `type_inference_branch_refinement.c` 现在识别 branch condition 外层 unary `!`，并以相反 branch polarity 复用已有 numeric range scope。比如 `if (!(seed < 10)) { seed + 1; }` 会按 false branch of `seed < 10` 处理，因此 `seed: 0..255` 在 then scope 内收窄为 `10..255`，`seed + 1` 的 parser numeric fact 与 LSP local semantic query 都报告 `11..256`。该路径复用既有 simple comparison、true-branch `&&`、false-branch `\|\|`、同向可表示 union/complement union 等受支持子集；不声明完整 De Morgan 展开、任意嵌套布尔公式、disjoint union/hole-set/range-set、`else if` 链、CFG branch pruning、bounds/overflow 诊断或跨块 numeric dataflow。 | TDD RED 先让 WSL gcc `zr_vm_numeric_branch_refinement_test` unary-not 新用例失败为 `Expected TRUE Was FALSE`，并让 `zr_vm_language_server_numeric_branch_semantic_query_test` 返回旧范围 `1..256`，证明外层 `!` 尚未反转 branch polarity。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 11 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 11 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Logical-Or Same-Direction Interval Refinement | 已完成 | 2026-06-22 13:57 +08:00 | `type_inference_branch_refinement.c` 现在支持 simple logical OR 的 true branch 可表示同向 union 子集：当两侧 simple comparison 都作用于同一个整数 identifier，且各自得到的 true-branch 区间相交或相邻时，将两侧结果合并成单一连续区间。比如 `if (seed < 10 \|\| seed < 20) { seed + 1; }` 中 true branch 可合并为 `seed < 20`，因此 `seed: 0..255` 在 then scope 内收窄为 `0..19`，`seed + 1` 的 parser numeric fact 与 LSP local semantic query 都报告 `1..20`。实现把前一轮 false-branch logical AND 路径泛化为按 operator 与是否反向 bound 参数化的 logical-union helper，并保留 false-branch `&&` 的既有行为。此切片不声明 disjoint union/hole-set/range-set、完整 true-branch logical OR、任意嵌套布尔公式、`else if` 链、CFG branch pruning、bounds/overflow 诊断或跨块 numeric dataflow。 | TDD RED 先让 WSL gcc `zr_vm_numeric_branch_refinement_test` 新用例失败为 `Expected TRUE Was FALSE`，并让 `zr_vm_language_server_numeric_branch_semantic_query_test` 返回旧范围 `1..256`，证明 true branch 尚未消费 simple logical OR 的可表示同向 union。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 10 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 10 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出既有 warning/noise，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric False-Branch Logical-And Same-Direction Interval Refinement | 已完成 | 2026-06-22 13:39 +08:00 | `type_inference_branch_refinement.c` 现在支持 simple logical `&&` 的 false branch 可表示同向边界子集：当两侧反向后的比较都作用于同一个整数 identifier 且结果区间相交或相邻时，将 `!(left && right)` 的两侧反向结果合并成单一连续区间。比如 `if (seed < 10 && seed < 20) { ... } else { seed + 1; }` 中 false branch 等价于 `(seed >= 10) || (seed >= 20)`，可合并为 `seed >= 10`，因此 `seed: 0..255` 在 else scope 内收窄为 `10..255`，`seed + 1` 的 parser numeric fact 与 LSP local semantic query 都报告 `11..256`。实现拆出 refined-type 与 temporary branch scope helper，避免继续复制 simple comparison scope 创建逻辑；不相交的 false-branch `&&` complements 仍保持未收窄，避免用单一区间错误表示 hole/disjoint union。此切片不声明 disjoint union/hole-set、true-branch `||`、任意嵌套布尔公式、`else if` 链、CFG branch pruning、bounds/overflow 诊断或跨块 numeric dataflow。 | TDD RED 先让 WSL gcc `zr_vm_numeric_branch_refinement_test` 新用例失败为 `Expected TRUE Was FALSE`，并让 `zr_vm_language_server_numeric_branch_semantic_query_test` 返回旧范围 `1..256`，证明 false branch 尚未消费 simple `&&` 的可表示同向互补。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 9 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 9 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Non-Integer Index Diagnostics | 已完成 | 2026-06-22 13:17 +08:00 | 数组 computed member 推断现在会在索引表达式类型已知且非整数时写入 error 级 semantic query diagnostic `array_index_type_mismatch`；例如 `var values = [1, 2]; return values["name"];` 会通过 compiler `queryDiagnostics` 和 LSP diagnostics 发布该 code。判定保留 integer、`UNKNOWN`、以及无 typeName 的 `OBJECT` 作为不诊断路径，避免把 truly unknown / no-inferable-range 索引误报；已知 string/bool/float/array/typed-object 等非整数索引才走 mismatch。`ZrParser_SemanticQuery_Diagnostics` 对该 code 使用类型不匹配的 cause/suggestion，既有 `array_index_out_of_bounds` 与 `array_index_may_be_out_of_bounds` code/severity 不变。此切片不声明 unknown/no-range 索引诊断、min-only/no finite-upper-bound 正索引策略、运行时 bounds check、二进制/外部诊断序列化或旧 `ZrParser_ArrayIndexBounds_Check` 替换。 | TDD RED 先让 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 `Expected 1 Was 0`，并让 `zr_vm_language_server_semantic_query_diagnostics_test` 新用例返回 `Expected one non integer array index diagnostic; diagnostics=0`，证明已知非整数索引尚未通过 semantic query diagnostics 发布。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 15 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 9 PASS；构建仍输出既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Primitive Integer And Min-Max Diagnostics | 已完成 | 2026-06-22 12:49 +08:00 | 固定数组 bounds 语义查询诊断现在锁定两类增量：`index: int` 这类原生整数索引会使用类型转换阶段已有的完整 primitive range，继续对 `[1, 2]` 发布 warning 级 `array_index_may_be_out_of_bounds` 而不是确定错误；`int[1 .. 3]` 这类有限最大长度约束现在会把 `arrayMaxSize` 作为 computed index 上界，常量 `values[3]` 发布 error 级 `array_index_out_of_bounds`，`index: u8` 的 `values[index]` 发布 warning 级 `array_index_may_be_out_of_bounds`。固定大小、`min==max` 与有限 `max` 共用同一 upper-bound 判定；只有 min-only / 无有限上界数组不会对正索引声明越界。此切片不声明 truly unknown/non-integer/no-inferable-range 索引诊断、运行时 bounds check、跨块 numeric dataflow、二进制/外部诊断序列化或旧 `ZrParser_ArrayIndexBounds_Check` 替换。 | TDD RED：primitive `index:int` 回归加入后 WSL gcc compiler/LSP diagnostics 直接通过，证明已有 primitive full-range 行为；array min/max RED 先因 `int[1..3]` 语法无空格而暴露当前 range 语法需要 `int[1 .. 3]`，修正后 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 失败为 `Expected 1 Was 0`，证明有限 max-size bounds 未发布 query diagnostic。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 14 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 8 PASS；构建仍输出既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Possible Interval Diagnostics | 已完成 | 2026-06-22 12:18 +08:00 | 固定大小数组的 computed member 推断现在会在非常量整数索引区间与合法 bounds 部分重叠、但仍包含越界值时写入 warning 级 semantic query diagnostic；例如 `index: u8` 下 `var values = [1, 2]; return values[index];` 的索引区间为 `0..255`，对 2 元素数组发布 `array_index_may_be_out_of_bounds`，而确定全越界的 `values[index + 2]` 仍发布 error 级 `array_index_out_of_bounds`。expression diagnostic fact 新增 `diagnosticCode` 与 `diagnosticSeverity` 载荷，semantic query 按 fact 载荷映射 code/severity；数组 bounds diagnostic fact 生成从已经偏大的 `type_inference_semantic_facts.c` 拆到 `type_inference_array_diagnostics.c`。此切片只声明固定大小数组 + 已知整数区间的部分/可能越界 warning；不声明 unknown/no-range 索引诊断、数组最小/最大尺寸约束、运行时 bounds check、跨块 numeric dataflow、二进制/外部诊断序列化或旧 `ZrParser_ArrayIndexBounds_Check` 错误路径替换。 | TDD RED 先让 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 `Expected 1 Was 0`，并让 `zr_vm_language_server_semantic_query_diagnostics_test` 新用例返回 `Expected one possible interval array bounds diagnostic; diagnostics=0`，证明部分重叠索引区间尚未通过 query diagnostics 发布。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 12 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 6 PASS；新增 helper 文件后对 WSL gcc/clang 与 Windows MSVC 都执行 CMake regenerate，使 source glob 纳入新文件；构建仍输出既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Interval Diagnostics | 已完成 | 2026-06-22 11:48 +08:00 | 固定大小数组的 computed member 推断现在会在索引表达式已有确定整数区间且整个区间必然越界时写入 parser-owned expression diagnostic fact；例如 `index: u8` 下 `var values = [1, 2]; var maybe = values[index]; return values[index + 2];` 只为 `index + 2` 的 `2..257` 区间发布 error 级 `array_index_out_of_bounds`，不会把部分重叠的 `values[index]` 误报为确定越界。compiler frontend 和 LSP diagnostics 继续通过同一 semantic query diagnostics consumer 发布该诊断。此切片只声明固定大小数组 + 确定全越界整数区间索引；不声明部分/可能越界诊断、数组最小/最大尺寸约束、运行时 bounds check、跨块 numeric dataflow、二进制/外部诊断序列化或旧 `ZrParser_ArrayIndexBounds_Check` 错误路径替换。 | TDD RED 先让 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 `Expected 0 to be greater than 0`，并让 `zr_vm_language_server_semantic_query_diagnostics_test` 新用例返回 `diagnostics=0`，证明变量区间索引越界还没有通过 query diagnostics 发布；同一 fixture 同时覆盖 `values[index]` 部分重叠索引不应误报。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 11 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 5 PASS；构建仍输出既有 warning / WSL clang glob 提示；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Array Bounds Semantic Query Diagnostics | 已完成 | 2026-06-22 11:24 +08:00 | 固定大小数组的 computed member 推断现在会在索引是可求值整数常量且越界时写入 parser-owned expression diagnostic fact；`ZrParser_SemanticQuery_Diagnostics` 将该 fact 映射为 error 级结构化诊断 `array_index_out_of_bounds`。compiler frontend 会把 `var values = [1, 2]; return values[2];` 这类固定数组常量越界带入 `SZrSemanticContext.queryDiagnostics`，LSP diagnostics 路径也从同一 query consumer 发布 `array_index_out_of_bounds`。此切片只声明固定大小数组 + 可求值整数索引（含负索引）的语义查询诊断；不声明变量索引区间越界、数组最小/最大尺寸约束、运行时 bounds check、跨块 numeric dataflow、二进制/外部诊断序列化或旧 `ZrParser_ArrayIndexBounds_Check` 的错误路径替换。 | TDD RED 先让 WSL gcc `zr_vm_semantic_query_test` 新用例失败为 `Expected Non-NULL`，让 `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 `Expected 0 to be greater than 0`，并让 LSP diagnostic 新用例返回 `diagnostics=0`，证明数组越界 fact 尚未通过 query diagnostics 发布。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 14 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 10 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 4 PASS。额外 `zr_vm_type_inference_test` 在 WSL clang 与 Windows MSVC 通过；WSL gcc 完整 `type_inference` 运行在本切片相关 computed-access 用例通过后，于后续 FFI 指针辅助测试处 segfault，未作为本轮 GREEN 证据；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Overflow Semantic Query Diagnostics | 已完成 | 2026-06-22 10:53 +08:00 | `ZrParser_SemanticQuery_Diagnostics` 现在把 scope 内 `mayOverflow=true` 的 numeric facts 映射为 warning 级结构化诊断 `numeric_overflow`，message/cause/suggestion 统一说明目标整数类型可能溢出以及可用更宽类型或 guard。compiler frontend 发布 semantic query diagnostics 时会把 `return 9223372036854775807 + 1;` 这类已有 parser/type-inference 溢出事实带入 `SZrSemanticContext.queryDiagnostics`；LSP diagnostics 路径也能从同一 query consumer 发布 `numeric_overflow`。此切片只声明消费既有 `mayOverflow` numeric facts；不新增溢出推断、数组越界/bounds 诊断、符号算术/SMT、CFG-wide numeric dataflow 或二进制/外部诊断序列化。 | TDD RED 先让 WSL gcc `zr_vm_semantic_query_test` 新用例失败为 `Expected Non-NULL`，证明 `mayOverflow` numeric facts 尚未作为 query diagnostics 发布。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 13 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 9 PASS、`zr_vm_language_server_local_semantic_query_test` 19 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 3 PASS；构建仍输出既有 `%type` format warning；tracked-file `git diff --check` 仅有 LF/CRLF 提示，未跟踪 diagnostic tests/session 文件尾随空白扫描通过；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Interval Logical Branch Reachability Diagnostics | 已完成 | 2026-06-22 10:31 +08:00 | `type_inference_logical_fact_known_bool_value` 现在把 parser/type-inference 已写入的 exact `ALWAYS_TRUE` / `ALWAYS_FALSE` logical fact 作为条件真值来源，`ZrParser_ConditionalType_Infer` 因此能把 `seed < 300 ? 1 : 2` 这类由区间比较证明恒真的条件表达式记录为常量分支 reachability，并通过 compiler semantic query diagnostics 发布 `unreachable_code`。LSP 的 constant-condition evaluator 也会读取同类 logical fact；`semantic_analyzer_typecheck.c` 在完成 `if` 条件 typecheck 后先 materialize 条件表达式 facts，再记录 constant branch reachability，使 `if (seed < 300) ... else ...` 的 unreachable else 同时保留直接 `unreachable_branch` 诊断和 parser semantic query 的 `unreachable_code` 诊断。`semantic_analyzer_query_diagnostics.c` 的重复诊断过滤改为 range+code 匹配，避免同范围的 `unreachable_branch` 抑制 semantic query `unreachable_code`。此切片只声明 exact logical facts 驱动 conditional/if 不可达分支诊断；不声明新的 union/hole-set、true-branch `||` / false-branch `&&` 建模、bounds/overflow 诊断、符号 SMT、跨块 numeric dataflow 或通用 CFG 常量剪枝。 | TDD RED 先让 WSL gcc `zr_vm_compiler_semantic_query_diagnostics_test` 新用例失败为 `Expected 0 to be greater than 0`，证明 interval logical fact 尚未驱动 compiler query diagnostics；LSP 新用例失败为 `diagnostics=0`，随后又暴露同 range 直接诊断会让 `unreachable_code` 被去重过滤。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 8 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 8 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 2 PASS；`git diff --check` 仅有 LF/CRLF 提示；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric False-Branch Inequality Equality Interval Refinement | 已完成 | 2026-06-22 09:53 +08:00 | `type_inference_branch_refinement.c` 现在支持 false branch 上的 `!=` 反向单点收窄：当条件形如 `seed != 10` 且已有 `seed` range 为 `0..255`，else scope 会把条件反向成 `seed == 10`，把分支内 `seed` 精确收窄为 `10..10`，因此 `seed + 1` 写入 numeric fact range `11..11`。这不同于 true-branch `!=` 的 edge-only complement：false-branch `!=` 的互补是可由单一区间表达的 equality，即使排除点位于原区间内部也可精确表示。本轮还把分支收窄 parser 覆盖从 `tests/parser/test_numeric_range_inference.c` 拆到 `tests/parser/test_numeric_branch_refinement.c` / `zr_vm_numeric_branch_refinement_test`，把 LSP 分支查询覆盖从 `tests/language_server/test_lsp_numeric_semantic_query.c` 拆到 `tests/language_server/test_lsp_numeric_branch_semantic_query.c` / `zr_vm_language_server_numeric_branch_semantic_query_test`，避免继续扩大已接近 1000 行的通用 numeric 测试文件。此切片只声明 false-branch simple `!=` 的 equality interval refinement 和测试目标拆分；不声明 true-branch interior `!=` hole-set、true-branch `||`、false-branch `&&` 的并集建模、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc 新 parser branch target 失败为 `Expected TRUE Was FALSE`，并让新 LSP numeric branch query 返回旧范围 `1..256`，证明 false branch `!=` 没有反向成 equality bound。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_numeric_branch_refinement_test` 8 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_numeric_branch_semantic_query_test` 8 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Edge Inequality Interval Refinement | 已完成 | 2026-06-22 09:21 +08:00 | `type_inference_branch_refinement.c` 现在支持 true branch 上可用单一区间表达的 inequality complement：当条件形如 `seed != 0` 且已有 `seed` range 为 `0..255`，then scope 会把排除的边界点转换成 edge complement，把分支内 `seed` 收窄为 `1..255`，因此 `seed + 1` 写入 numeric fact range `2..256`。反向 literal-on-left 形式沿用同一 `!=` edge bound。该 helper 只在排除点正好等于当前 interval min 或 max 时应用 `!=` 边界，否则保持未细化，避免用单一区间错误表示中间点补集。此切片只声明 true-branch `!=` 的 edge complement 区间收窄，不声明 interior inequality complement、false-branch `!=`、true-branch `||`、false-branch `&&` 的并集建模、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc parser 用例失败为 `Expected TRUE Was FALSE`，并让 LSP numeric query 新用例仍返回旧范围 `1..256`，证明 true branch inequality 没有可表示的 edge complement 收窄。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 17 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 11 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric False-Branch Edge Equality Interval Refinement | 已完成 | 2026-06-22 09:12 +08:00 | `type_inference_branch_refinement.c` 现在支持 false branch 上可用单一区间表达的 equality complement：当条件形如 `seed == 0` 且已有 `seed` range 为 `0..255`，else scope 会把排除的边界点转换成 `seed != 0` 的 edge complement，把分支内 `seed` 收窄为 `1..255`，因此 `seed + 1` 写入 numeric fact range `2..256`。该 helper 只在排除点正好等于当前 interval min 或 max 时应用 `!=` 边界，否则保持未细化，避免用单一区间错误表示中间点补集。此切片只声明 false-branch `==` 的 edge complement 区间收窄，不声明 interior equality complement、general `!=`、true-branch `||`、false-branch `&&` 的并集建模、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc parser 用例失败为 `Expected TRUE Was FALSE`，并让 LSP numeric query 新用例仍返回旧范围 `1..256`，证明 false branch equality 没有可表示的 edge complement 收窄。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 16 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 10 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Equality Interval Refinement | 已完成 | 2026-06-22 08:58 +08:00 | `type_inference_branch_refinement.c` 现在支持 true branch 上的 simple equality comparison：当条件形如 `seed == 10`，branch scope 会把已有 `seed` integer range 与单点 `[10, 10]` 求交，分支内 `seed + 1` 写入 numeric fact range `11..11`。反向 literal-on-left 形式也沿用同一 equality bound。parser branch scope API 与 LSP semantic typecheck 继续共用同一入口。此切片只声明 true-branch simple `==` 中 identifier-vs-integer-literal 的单点区间收窄；false-branch `==` / `!=` 的补集、true-branch `||`、false-branch `&&` 的并集建模、跨块 numeric dataflow、bounds/overflow 诊断和 CFG 常量剪枝仍不在本轮范围内。 | TDD RED 先让 WSL gcc parser 用例失败为 `Expected TRUE Was FALSE`，并让 LSP numeric query 新用例仍返回旧范围 `1..256`，证明 equality 条件没有进入 branch scope。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 15 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 9 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / Numeric False-Branch Logical-Or Interval Refinement | 已完成 | 2026-06-22 08:47 +08:00 | `type_inference_branch_refinement.c` 现在支持 false branch 上的 simple logical `||` 互补交集：当条件形如 `seed <= 2 || seed >= 10`，false scope 会把左、右比较分别反向成 `seed > 2` 与 `seed < 10`，并在同一临时 environment 链上依次应用，分支内 `seed` 从 `0..255` 收窄为 `3..9`，因此 `seed + 1` 写入 numeric fact range `4..10`。这复用上一切片的 full branch environment 链清理逻辑，parser branch scope API 与 LSP semantic typecheck 继续共用同一入口。此切片只声明 false-branch simple `||` 中 identifier-vs-integer-literal 比较的互补区间交集，不声明 true-branch `||`、false-branch `&&` 的并集建模、`else if` 链、嵌套任意布尔表达式、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc parser 用例失败为 `Expected TRUE Was FALSE`，并让 LSP numeric query 新用例仍返回旧范围 `1..256`，证明 false branch 没有对 `||` 两侧取反并交集。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 14 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 8 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Logical-And Interval Refinement | 已完成 | 2026-06-22 08:38 +08:00 | `type_inference_branch_refinement.c` 现在支持 true branch 上的 simple logical `&&` 条件交集：当条件形如 `seed > 2 && seed < 10`，branch scope 会先应用左侧比较，再在同一临时 environment 链上应用右侧比较，分支内 `seed` 从 `0..255` 收窄为 `3..9`，因此 `seed + 1` 写入 numeric fact range `4..10`。`ZrParser_TypeInference_PopBranchScope` 也扩展为可释放整条临时 branch environment 链，避免组合条件堆叠 scope 后泄漏或只弹出最内层。parser branch scope API 与 LSP semantic typecheck 继续共用同一入口。此切片只声明 true-branch simple `&&` 中 identifier-vs-integer-literal 比较的区间交集，不声明 false-branch `||` 德摩根收窄、`else if` 链、嵌套任意布尔表达式、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc focused parser 用例失败为 `Expected TRUE Was FALSE`，并让 LSP numeric query 新用例仍返回旧范围 `1..256`，证明组合条件没有被分支 scope 消费。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 13 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 7 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric False-Branch Less-Than Interval Refinement | 已完成 | 2026-06-22 08:29 +08:00 | `type_inference_branch_refinement.c` 现在复用同一套分支区间 scope helper 处理 true/false branch，并在 public `type_inference.h` 暴露 `ZrParser_TypeInference_PushFalseBranchNumericRangeScope`。false branch 会把 simple identifier-vs-integer-literal 比较反向成互补边界：`<` 变为 `>=`、`<=` 变为 `>`、`>` 变为 `<=`、`>=` 变为 `<`；parser `ZR_AST_IF_EXPRESSION` 推断和 LSP semantic typecheck 现在都会在 else 分支进入 false-branch 临时 type environment。`if (seed < 10) ... else { seed + 1; }` 会把已有 `seed` range `0..255` 在 else 内收窄为 `10..255`，因此 `seed + 1` 写入 numeric fact range `11..256`，同时保留原 symbol/type/declaration payload。此切片只声明 false-branch simple integer comparison (`<`/`<=`/`>`/`>=`) 的区间收窄，不声明 `else if` 链、`&&`/`||` 组合条件、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc focused build 因缺少 `ZrParser_TypeInference_PushFalseBranchNumericRangeScope` 编译/链接失败，证明测试锁定了新的 false-branch scope API。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 12 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 6 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric True-Branch Less-Than Interval Refinement | 已完成 | 2026-06-22 07:59 +08:00 | 新增 `zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_branch_refinement.c`，并在 public `type_inference.h` 暴露 `SZrTypeInferenceBranchScope`、`ZrParser_TypeInference_PushTrueBranchNumericRangeScope` 和 `ZrParser_TypeInference_PopBranchScope`。`ZR_AST_IF_EXPRESSION` 推断与 LSP semantic typecheck 现在会在 then 分支进入临时 type environment，把 simple identifier-vs-integer-literal 条件的 true branch 区间收窄：`seed < 10` 会把已有 `0..255` 收窄为 `0..9`，分支内 `seed + 1` 因此写入 numeric fact range `1..10`；helper 同时保留原 symbol/type/declaration payload，避免分支临时 binding 破坏引用语义。此切片只声明 true-branch simple integer comparison (`<`/`<=`/`>`/`>=`) 的区间收窄，不声明 false-branch narrowing、`&&`/`||` 组合条件、跨块 numeric dataflow、bounds/overflow 诊断或 CFG 常量剪枝。 | TDD RED 先让 WSL gcc focused build 因缺少 `SZrTypeInferenceBranchScope` 与 push/pop API 编译失败，证明测试在锁定新分支收窄入口。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 11 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 5 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Integer Modulo Interval Range Inference | 已完成 | 2026-06-22 07:22 +08:00 | 新增 `zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_numeric_range.c/.h`，把 primitive/binary/unary numeric interval helper 和 checked int64 arithmetic 从 `type_inference_semantic_facts.c` 拆出，避免语义事实发射文件继续扩大。`type_inference_apply_binary_numeric_range` 现在支持 integer `%` 的保守区间传播：除数区间不含 0 且不覆盖 `INT64_MIN % -1` 时，按除数绝对值上界计算余数范围，非负被除数得 `[0, abs(maxDivisor)-1]`，非正被除数得负向范围，跨 0 被除数得对称范围；除数跨 0 或 `INT64_MIN/-1` 边界时清除 result range 并保留 numeric promotion fact。`tests/parser/test_numeric_range_inference.c` 扩展为 10 项，覆盖 `seed: int64` 范围 `8..12` 下 `seed % 5` 写入 `0..4` range 与 unsigned payload，`denom: int64` 范围 `-1..1` 时 `seed % denom` 无 range，以及 `INT64_MIN % -1` 边界无 range。`tests/language_server/test_lsp_numeric_semantic_query.c` 扩展 `seed: u8` 的 `seed % 5` local semantic query，确认 LSP 能读到 parser-owned numeric fact `0..4`。此切片只声明 integer modulo interval range inference 与 numeric range helper 拆分，不声明 exact residue-set 分析、divide/modulo-by-zero 诊断、symbolic overflow 诊断、分支内区间收窄、CFG 常量分支剪枝或完整数值数据流。 | TDD RED 先让 WSL gcc 新增 `%` 覆盖失败：`zr_vm_numeric_range_inference_test` 9 项中 2 项失败，分别为 `Expected 0 Was 8` 和 `Expected FALSE Was TRUE`，证明 `%` 仍沿用旧区间且除数跨 0 未清 range；随后补 `INT64_MIN % -1` 边界，旧实现失败为 10 项中 1 项 `Expected FALSE Was TRUE`。修复并拆分 helper 后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 10 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 4 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Integer Division Interval Range Inference | 已完成 | 2026-06-22 06:53 +08:00 | `type_inference_apply_binary_numeric_range` 现在支持 integer `/` 的区间传播：新增 checked `type_inference_int64_div` 与 `type_inference_int64_div_range`，只在除数区间不含 0 时计算四个端点商并取 min/max，同时复用 `INT64_MIN / -1` 溢出保护；除数区间跨 0 或端点除法不可安全建模时清除 result range。`tests/parser/test_numeric_range_inference.c` 扩展为 7 项，覆盖 `seed: int64` 范围 `8..12` 下 `seed / 2` 写入 `ZR_SEMANTIC_NUMERIC_FACT_PROMOTION` range `4..6` 与 unsigned payload，以及 `denom: int64` 范围 `-1..1` 时 `seed / denom` 保持无 range。`tests/language_server/test_lsp_numeric_semantic_query.c` 扩展 `seed: u8` 的 `seed / 2` local semantic query，确认 LSP 能读到 parser-owned numeric fact `0..127`。此切片只声明 integer division interval range inference，不声明 modulo interval、divide-by-zero 诊断、symbolic overflow 诊断、分支内区间收窄、CFG 常量分支剪枝或完整数值数据流。 | TDD RED 先让 WSL gcc 新增覆盖失败：`zr_vm_numeric_range_inference_test` 7 项中 2 项失败，分别为 `Expected 4 Was 8` 和 `Expected FALSE Was TRUE`，证明 `/` 仍沿用旧区间且除数跨 0 未清 range；`zr_vm_language_server_numeric_semantic_query_test` 新增除法查询失败为 `hasRange=0`。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 7 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 3 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Interval Comparison Logical Facts | 已完成 | 2026-06-22 06:25 +08:00 | 新增 `zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_interval_logic.c`，把 integer interval 比较证明从 1000+ 行的 `type_inference_semantic_facts.c` 拆成窄 helper，并在 `type_inference_record_expression_and_numeric_facts` 中接入。该 helper 只消费左右子表达式已有 `SZrSemanticNumericFact` 的整数 range；当 `<` / `>` / `<=` / `>=` / `==` / `!=` 能由区间关系证明恒真或恒假时，写入 exact `ZR_SEMANTIC_LOGICAL_FACT_ALWAYS_TRUE` / `ALWAYS_FALSE`。`tests/parser/test_numeric_range_inference.c` 扩展为 5 项，覆盖 `seed: int64` 范围 `2..4` 下 `seed < 10` 恒真、`seed > 10` 恒假，以及 `seed < 3` 区间重叠时保持 unknown。`tests/language_server/test_lsp_logical_semantic_query.c` 新增 `seed: u8` 的 `seed < 300` local semantic query，证明区间 logical fact 能透出到 LSP 查询。此切片只声明 integer interval comparison logical facts，不声明分支内区间收窄、bounds/overflow 诊断、float interval 比较、CFG 常量分支剪枝或完整数值数据流。 | TDD RED 先让 WSL gcc `zr_vm_numeric_range_inference_test` 失败为 5 项中 2 项 `Expected Non-NULL`，证明区间比较尚未写 logical fact；重叠区间 unknown 用例在 RED 阶段已通过。修复后 WSL gcc、WSL clang、Windows MSVC `build\agent-msvc-tests` Debug 同一组 focused/adjacent 目标均通过：`zr_vm_numeric_range_inference_test` 5 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_logical_fact_emission_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 2 PASS、`zr_vm_language_server_logical_semantic_query_test` 3 PASS；构建仍输出当前脏树既有 warning，不声明全仓库 GREEN。 |
| 阶段 1 / Numeric Unary Minus Interval Range Inference | 已完成 | 2026-06-22 05:50 +08:00 | 新增并注册 `tests/parser/test_numeric_range_inference.c` / `zr_vm_numeric_range_inference_test`，避免继续扩大 1370+ 行的 `tests/parser/test_expression_fact_emission.c`。`type_inference_apply_unary_numeric_range` 现在对带 integer 范围约束的 operand 使用 checked 端点翻转，把 `[lo, hi]` 推断为 `[-hi, -lo]`；端点取负溢出时清除 range 约束，而不是沿用 operand 的旧区间。`-seed` 在 `seed: int64` 范围为 `2..4` 时会写入 result range 与 `ZR_SEMANTIC_NUMERIC_FACT_RANGE` `-4..-2`；`INT64_MIN..INT64_MIN` 这类无法安全取负的 operand 会保留 inferred int64 类型、清除 result range，并写入无 range 的 `ZR_SEMANTIC_NUMERIC_FACT_PROMOTION`。同时扩展 `tests/language_server/test_lsp_numeric_semantic_query.c`，锁定 `seed: u8` 的 `seed + 3` 可通过 LSP local semantic query 返回 signed/unsigned `3..258` 区间事实。此切片只声明一元负号 interval 翻转、溢出清除和既有 interval addition 的 LSP 回归，不声明比较分支收窄、符号约束、array bounds 诊断或完整数值数据流。 | TDD RED 先让 WSL gcc focused build/run 失败：`zr_vm_numeric_range_inference_test` 1 项中新增 unary-minus interval 用例失败，为 `Expected -4 Was 2`；加入 overflow-boundary 覆盖后又暴露 `Expected Non-NULL`，因为无 range 的 unary numeric result 未写出 numeric fact。修复后 WSL gcc focused build/run 通过 2 PASS，并且相邻 `zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_numeric_semantic_query_test` 2 PASS。WSL clang 同一组 focused/adjacent 目标通过 2/28/9/12/2 PASS；Windows MSVC `build\agent-msvc-tests` Debug 同一组目标通过 2/28/9/12/2 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Nonnegative Integer Right Shift Constant Condition Folding | 已完成 | 2026-06-22 05:10 +08:00 | 扩展 `tests/parser/test_cfg_integer_bitwise_conditions.c` / `zr_vm_cfg_integer_bitwise_conditions_test`，新增 `if ((12 >> 2) == 3)` 覆盖。`cfg_constants.c` 现在把两个已知非负 integer literal 的 binary `>>` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 right-shift 条件会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。此切片只声明 nonnegative integer `>>` 折叠，并拒绝负操作数与无效 shift count；隐式转换、数值区间、符号值或混合类型算术仍保持 unknown。 | TDD RED 先让 WSL gcc focused build/run 失败：`zr_vm_cfg_integer_bitwise_conditions_test` 5 项中新增 right-shift 用例 1 项失败，为 `Expected Non-NULL`；修复后 WSL gcc 与 clang focused build/run 均通过 5 PASS，Windows MSVC focused build/run 通过 5 PASS。WSL gcc/clang 与 Windows MSVC 相邻 CFG/dataflow/semantic facts 集合直接运行通过：constant conditions 21 PASS、integer bitwise conditions 5 PASS、float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Nonnegative Integer Left Shift Constant Condition Folding | 已完成 | 2026-06-22 04:48 +08:00 | 扩展 `tests/parser/test_cfg_integer_bitwise_conditions.c` / `zr_vm_cfg_integer_bitwise_conditions_test`，新增 `if ((3 << 2) == 12)` 覆盖。`cfg_constants.c` 现在把两个已知非负 integer literal 的 binary `<<` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 left-shift 条件会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。此切片只声明 nonnegative integer `<<` 折叠，并拒绝负操作数、无效 shift count 与 int64 溢出结果；右移、隐式转换、数值区间、符号值或混合类型算术仍保持 unknown。 | TDD RED 先让 WSL gcc focused build/run 失败：`zr_vm_cfg_integer_bitwise_conditions_test` 4 项中新增 left-shift 用例 1 项失败，为 `Expected Non-NULL`；修复后 WSL gcc 与 clang focused build/run 均通过 4 PASS，Windows MSVC focused build/run 通过 4 PASS。WSL gcc/clang 与 Windows MSVC 相邻 CFG/dataflow/semantic facts 集合直接运行通过：constant conditions 21 PASS、integer bitwise conditions 4 PASS、float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Nonnegative Integer Bitwise XOR Constant Condition Folding | 已完成 | 2026-06-22 04:40 +08:00 | 扩展 `tests/parser/test_cfg_integer_bitwise_conditions.c` / `zr_vm_cfg_integer_bitwise_conditions_test`，新增 `if ((6 ^ 3) == 5)` 覆盖。`cfg_constants.c` 现在把两个已知非负 integer literal 的 binary `^` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 XOR 条件会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。此切片只声明 nonnegative integer `^` 折叠；带负操作数的 bitwise、shift、隐式转换、数值区间、符号值或混合类型算术仍保持 unknown。 | TDD RED 先让 WSL gcc focused build/run 失败：`zr_vm_cfg_integer_bitwise_conditions_test` 3 项中新增 XOR 用例 1 项失败，为 `Expected Non-NULL`；修复后 WSL gcc 与 clang focused build/run 均通过 3 PASS，Windows MSVC focused build/run 通过 3 PASS。WSL gcc/clang 与 Windows MSVC 相邻 CFG/dataflow/semantic facts 集合直接运行通过：constant conditions 21 PASS、integer bitwise conditions 3 PASS、float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Nonnegative Integer Bitwise OR Constant Condition Folding | 已完成 | 2026-06-22 04:31 +08:00 | 扩展 `tests/parser/test_cfg_integer_bitwise_conditions.c` / `zr_vm_cfg_integer_bitwise_conditions_test`，新增 `if ((4 | 1) == 5)` 覆盖。`cfg_constants.c` 现在把两个已知非负 integer literal 的 binary `|` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 OR 条件会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。此切片只声明 nonnegative integer `|` 折叠；带负操作数的 bitwise、`^`/shift、隐式转换、数值区间、符号值或混合类型算术仍保持 unknown。 | TDD RED 先让 WSL gcc focused build/run 失败：`zr_vm_cfg_integer_bitwise_conditions_test` 2 项中新增 OR 用例 1 项失败，为 `Expected Non-NULL`；修复后 WSL gcc 与 clang focused build/run 均通过 2 PASS，Windows MSVC focused build/run 通过 2 PASS。WSL gcc/clang 与 Windows MSVC 相邻 CFG/dataflow/semantic facts 集合直接运行通过：constant conditions 21 PASS、integer bitwise conditions 2 PASS、float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；首次 WSL 相邻 loop 变量命令未计入证据，已用显式 binary path 重跑；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Nonnegative Integer Bitwise AND Constant Condition Folding | 已完成 | 2026-06-22 04:18 +08:00 | 新增并注册 `tests/parser/test_cfg_integer_bitwise_conditions.c` / `zr_vm_cfg_integer_bitwise_conditions_test`，避免继续扩大已进入 900 行级别的 `tests/parser/test_cfg_constant_conditions.c`。`cfg_constants.c` 现在把两个已知非负 integer literal 的 binary `&` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((6 & 3) == 2)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。此切片只声明 nonnegative integer `&` 折叠；带负操作数的 bitwise、`|`/`^`/shift、隐式转换、数值区间、符号值或混合类型算术仍保持 unknown。 | TDD RED 先让 WSL gcc focused build/run 失败：新增 `zr_vm_cfg_integer_bitwise_conditions_test` 1/1 失败，为 `Expected Non-NULL`；修复后 WSL gcc 与 clang focused build/run 均通过 1 PASS，Windows MSVC focused build/run 通过 1 PASS。WSL gcc/clang 与 Windows MSVC 相邻 CFG/dataflow/semantic facts 集合直接运行通过：constant conditions 21 PASS、integer bitwise conditions 1 PASS、float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Integer Bitwise Not Constant Condition Folding | 已完成 | 2026-06-22 04:05 +08:00 | `cfg_constants.c` 现在把已知 integer literal 的 unary `~` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if (~0 == -1)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。扩展 `tests/parser/test_cfg_constant_conditions.c`，新增 integer bitwise-not equality 覆盖。实现用 modeled int64 常量域里的恒等式 `~x == -1 - x` 和既有 checked integer helper 计算，避免依赖 C signed bit representation。此切片只声明 integer unary `~` 折叠，不声明 float bitwise、隐式转换、数值区间、符号值或混合类型算术。 | TDD RED 先用 WSL gcc 手工编译/链接新增测试到修复前 parser 共享库，`zr_vm_cfg_constant_conditions_test` 21 项中新增 bitwise-not 用例 1 项失败，为 `Expected Non-NULL`；修复后 WSL gcc/clang focused build/run 均通过 constant conditions 21 PASS，Windows MSVC focused build/run 通过 constant conditions 21 PASS。WSL gcc/clang 现有相邻 CFG/dataflow/semantic facts 二进制在新 parser 共享库下直接运行通过：float arithmetic conditions 11 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Unary Plus Constant Condition Folding | 已完成 | 2026-06-22 03:47 +08:00 | `cfg_constants.c` 现在把已知 integer literal 与 finite float literal 的 unary `+` 表达式折叠成原常量，并继续交给共享条件比较折叠入口处理；因此 `if (+3 == 3)` 与 `if (+1.5 == 1.5)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。扩展 `tests/parser/test_cfg_constant_conditions.c` 与 `tests/parser/test_cfg_float_arithmetic_conditions.c`，新增 integer unary plus equality 与 finite float unary plus equality 覆盖。此切片只声明 unary `+` 保值折叠，不声明 bitwise `~`、隐式转换、数值区间、符号值或混合类型算术。 | TDD RED 先让 Windows MSVC focused build/run 失败：`zr_vm_cfg_constant_conditions_test` 新增 integer unary plus 用例 1/20 失败，`zr_vm_cfg_float_arithmetic_conditions_test` 新增 float unary plus 用例 1/11 失败，均为 `Expected Non-NULL`；修复后 Windows MSVC focused build/run 通过 constant conditions 20 PASS、float arithmetic conditions 11 PASS。WSL clang focused rebuild/run 通过同两目标 20 PASS/11 PASS；WSL gcc focused rebuild 在完成前两个目标后，后续相邻目标重建被当前脏树中无关 AOT `backend_aot_c_lowering_generic_power.c` 声明冲突阻断，但已重建的 gcc focused 二进制通过 20 PASS/11 PASS。GCC/Clang 现有相邻 CFG/dataflow/semantic facts 二进制在新 parser 共享库下直接运行通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Unary Minus Constant Condition Folding | 已完成 | 2026-06-22 03:35 +08:00 | `cfg_constants.c` 现在把已知 integer literal 与 finite float literal 的 unary `-` 表达式折叠成常量，并继续交给共享条件比较折叠入口处理；因此 `if (-3 == -3)` 与 `if (-1.5 == -1.5)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。扩展 `tests/parser/test_cfg_constant_conditions.c` 与 `tests/parser/test_cfg_float_arithmetic_conditions.c`，新增 integer unary minus equality、`INT64_MIN` unary minus overflow unknown、finite float unary minus equality 覆盖。此切片只声明 unary `-` 折叠，不声明 unary `+`、bitwise `~`、NaN/Infinity 折叠、隐式转换、数值区间、符号值或混合类型算术。 | TDD RED 先让 Windows MSVC focused build/run 失败：`zr_vm_cfg_constant_conditions_test` 新增 integer unary minus 用例 1/18 失败，`zr_vm_cfg_float_arithmetic_conditions_test` 新增 float unary minus 用例 1/10 失败，均为 `Expected Non-NULL`；修复后 Windows MSVC focused build/run 通过 constant conditions 18 PASS、float arithmetic conditions 10 PASS。加入 `INT64_MIN` overflow unknown 边界后 Windows MSVC focused build/run 通过 constant conditions 19 PASS、float arithmetic conditions 10 PASS。WSL gcc 与 clang 均逐目标重建并通过相邻 CFG/dataflow/semantic facts 集合：constant conditions 19 PASS、float arithmetic conditions 10 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。最新 Windows 相邻重跑被当前脏树中无关 AOT `backend_aot_c_function_body.c` 编译错误阻断；之前 float-division 后的 Windows 相邻集合已通过，本切片不声明全仓库 GREEN。 |
| 阶段 1 / CFG Float Division Constant Condition Folding | 已完成 | 2026-06-22 03:07 +08:00 | `cfg_constants.c` 现在把两个已知 finite float literal 的 `/` 表达式折叠成 float 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((7.5 / 2.5) == 3.0)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。扩展 `tests/parser/test_cfg_float_arithmetic_conditions.c`，新增 finite float division equality、`7.5 / 0.0` 除零 unknown、`DBL_MAX / 0.5` 非 finite 结果 unknown 边界。此切片只声明 finite float `/` 折叠，不声明 NaN/Infinity 折叠、隐式转换、数值区间、符号值或混合类型算术。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_float_arithmetic_conditions_test` 新增 float division 用例失败 1/7，为 `Expected Non-NULL`，因为 `(7.5 / 2.5) == 3.0` 未折叠；修复后 focused build/run 7 PASS。加入除零与溢出 unknown 边界后 Windows MSVC focused build/run 9 PASS。相邻 Windows 回归通过：constant conditions 17 PASS、typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused 与相邻目标均通过：constant conditions 17 PASS、float arithmetic conditions 9 PASS，且同一组 CFG/dataflow/semantic facts 目标通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Float Multiplication Constant Condition Folding | 已完成 | 2026-06-22 02:47 +08:00 | `cfg_constants.c` 现在把两个已知 finite float literal 的 `*` 表达式折叠成 float 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((1.5 * 2.5) == 3.75)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增并注册 `tests/parser/test_cfg_float_arithmetic_conditions.c` / `zr_vm_cfg_float_arithmetic_conditions_test`，把 finite float `+`/`-`/`*` equality 与非 finite 结果 unknown 边界集中到浮点算术条件专用测试中；`tests/parser/test_cfg_constant_conditions.c` 回落到 781 行，避免继续堆叠。此切片只声明 finite float `*` 折叠，不声明 float `/`、NaN/Infinity 折叠、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` 新增 float multiplication 用例失败 1/22，为 `Expected Non-NULL`，因为 `(1.5 * 2.5) == 3.75` 未折叠；修复后 focused build/run 22 PASS。随后拆出 `zr_vm_cfg_float_arithmetic_conditions_test` 并加入 `DBL_MAX * 2.0` overflow-unknown 边界，Windows MSVC focused build/run 通过：constant conditions 17 PASS、float arithmetic conditions 6 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused 与相邻目标均通过：constant conditions 17 PASS、float arithmetic conditions 6 PASS，且同一组 CFG/dataflow/semantic facts 目标通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Float Subtraction Constant Condition Folding | 已完成 | 2026-06-22 02:28 +08:00 | `cfg_constants.c` 现在把两个已知 finite float literal 的 `-` 表达式折叠成 float 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((5.5 - 2.25) == 3.25)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` float subtraction equality 用例，并新增 `-DBL_MAX - DBL_MAX` 溢出边界用例确认非 finite 结果保持 unknown、两侧分支都不剪枝。此切片只声明 finite float `-` 折叠，不声明 float `*`/`/`、NaN/Infinity 折叠、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` float subtraction 新增用例失败为 `Expected Non-NULL`，因为 `(5.5 - 2.25) == 3.25` 未折叠；修复后 focused build/run 20 PASS，加入溢出 unknown 边界后 Windows MSVC focused build/run 21 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 21 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；WSL gcc adjacent 首次多目标变量形式命令不被 CMake 接受后用显式目标列表重跑成功；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Float Addition Constant Condition Folding | 已完成 | 2026-06-22 02:07 +08:00 | `cfg_constants.c` 现在把两个已知 finite float literal 的 `+` 表达式折叠成 float 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((1.5 + 2.25) == 3.75)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` float addition equality 用例，并新增 `DBL_MAX + DBL_MAX` 溢出边界用例确认非 finite 结果保持 unknown、两侧分支都不剪枝。此切片只声明 finite float `+` 折叠，不声明 float `-`/`*`/`/`、NaN/Infinity 折叠、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` float addition 新增用例失败为 `Expected Non-NULL`，因为 `(1.5 + 2.25) == 3.75` 未折叠；修复后 focused build/run 18 PASS，加入溢出 unknown 边界后 Windows MSVC focused build/run 19 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 19 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；初次并行 WSL focused build 超时后按 gcc/clang 顺序重跑成功；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Integer Division/Modulo Constant Condition Folding | 已完成 | 2026-06-22 01:43 +08:00 | `cfg_constants.c` 现在把已知 integer literal 的 `/` 与 `%` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((9 / 3) == 3)` 与 `if ((10 % 4) == 2)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` integer division/modulo equality 用例；此切片只声明带除零和 `INT64_MIN / -1` / `INT64_MIN % -1` overflow/UB 检查的已知整数除法/取模折叠，不声明浮点或字符串算术、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` division 新增用例失败为 `Expected Non-NULL`，因为 `(9 / 3) == 3` 未折叠；修复后 focused build/run 16 PASS。随后 modulo 新增用例先失败为 `Expected Non-NULL`，因为 `(10 % 4) == 2` 未折叠；修复后 Windows MSVC focused build/run 17 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 17 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Integer Multiplication Constant Condition Folding | 已完成 | 2026-06-22 01:23 +08:00 | `cfg_constants.c` 现在把已知 integer literal 的 `*` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((2 * 3) == 6)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` integer multiplication equality 用例；此切片只声明带 int64 overflow 检查的已知整数乘法折叠，不声明除法/取模、浮点或字符串算术、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` 新增用例失败为 `Expected Non-NULL`，因为 `(2 * 3) == 6` 未折叠；修复后 Windows MSVC focused build/run 15 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 15 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Integer Subtraction Constant Condition Folding | 已完成 | 2026-06-22 01:11 +08:00 | `cfg_constants.c` 现在把已知 integer literal 的 `-` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((3 - 1) == 2)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` integer subtraction equality 用例；此切片只声明带 int64 overflow 检查的已知整数减法折叠，不声明乘除/取模、浮点或字符串算术、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` 新增用例失败为 `Expected Non-NULL`，因为 `(3 - 1) == 2` 未折叠；修复后 Windows MSVC focused build/run 14 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 14 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Integer Addition Constant Condition Folding | 已完成 | 2026-06-22 00:51 +08:00 | `cfg_constants.c` 现在能把已知 integer literal 的 `+` 表达式折叠成 integer 常量，并继续交给共享条件比较折叠入口处理；因此 `if ((1 + 1) == 2)` 会被证明为 true，else 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，then 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` integer addition equality 用例；此切片只声明带 int64 overflow 检查的已知整数加法折叠，不声明减法/乘除/取模、浮点或字符串算术、隐式转换、数值区间或符号值。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` 新增用例失败为 `Expected Non-NULL`，因为 `(1 + 1) == 2` 未折叠；修复后 Windows MSVC focused build/run 13 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 13 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；不声明全仓库 GREEN。 |
| 阶段 1 / CFG Mixed-Kind Constant Condition Folding | 已完成 | 2026-06-22 00:36 +08:00 | `if`/loop 共享布尔条件折叠入口复用 `cfg_node_constant`、`cfg_constants_can_compare` 和 `cfg_constants_equal` 处理 known constant `==`/`!=`，因此 `if (1 == "1")` 会被证明为 false，then 分支写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，else 分支保持可达。新增 `tests/parser/test_cfg_constant_conditions.c` mixed-kind equality 用例；此切片只声明已知常量相等/不等的 mixed-kind deterministic false/true，不声明 mixed-kind `<`/`>`、隐式转换、数值区间、符号值或算术常量折叠。 | TDD RED 先让 Windows MSVC `zr_vm_cfg_constant_conditions_test` 新增用例失败为 `Expected Non-NULL`，因为 `1 == "1"` 未折叠；修复后 Windows MSVC focused build/run 12 PASS。相邻 Windows 回归通过：typed-catch branch 2 PASS、typed-catch flow 8 PASS、loop 6 PASS、switch 5 PASS、try-catch 11 PASS、reachability 29 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang focused build/run 均通过：constant conditions 12 PASS，并直接运行同一组相邻 CFG/dataflow/semantic facts 目标均通过；WSL 构建仍输出既有 project const-qualifier warnings，不声明全仓库 GREEN。 |
| 阶段 1 / CFG Direct Lambda IIFE Throw Effects | 已完成 | 2026-06-22 00:10 +08:00 | 直接 lambda IIFE 的 throws-effect 首版落地：新增 `tests/parser/test_cfg_throw_effects.c` / `zr_vm_cfg_throw_effects_test`；`cfg_throw_profile.c` 识别 `primary(lambda)` 后接单个 `FUNCTION_CALL` 成员的直接 IIFE，`may-enter-catch` 与 known throw kind profile 只采集 call args/generic args 和 lambda body。非抛出 IIFE 后的 `catch(e)` 会被标为不可达；`throw "boom"` 的 IIFE 只保留 `string` typed catch，剪枝 `int` catch 和后续 catch-all。普通函数调用、import、construct 仍保持保守可抛；此切片不声明命名函数解析、成员调用 effect、闭包 fixed point、异步 lambda 或用户/继承异常类型。 | TDD RED 先让新增 Windows MSVC `zr_vm_cfg_throw_effects_test` 2/2 失败；修复后 Windows MSVC focused build/run 2 PASS，相邻 CFG/dataflow/semantic facts 回归通过：throw effects 2 PASS、try-catch 11 PASS、typed-catch flow 8 PASS、branch 2 PASS、loop 6 PASS、switch 5 PASS、reachability 29 PASS、constant conditions 11 PASS、switch constants 4 PASS、finally 7 PASS、dataflow 4 PASS、semantic facts 9 PASS。WSL gcc/clang 分段 focused build/run 覆盖同一组目标均通过；WSL 构建仍输出既有 project const-qualifier warnings，不声明全仓库 GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 16:17 +08:00 | 新增 `cfg.h` / `type_inference/cfg.c`，建立 entry/statement/exit block graph，先覆盖顺序语句与 `return`/`throw`/`break`/`continue` 终结后的不可达传播；不可达 block 会向 `SZrSemanticReachabilityFact` 写入 cause 与 causeNode。新增 `tests/parser/test_cfg_reachability.c`，并把 `zr_vm_cfg_reachability_test` 注册到 parser semantic test 区和 language_pipeline 聚合清单。 | TDD RED 先因 CFG 类型/API 缺失编译失败；修复后 Windows MSVC `zr_vm_cfg_reachability_test` 2 PASS，`zr_vm_semantic_facts_test` 4 PASS，`zr_vm_expression_fact_emission_test` 28 PASS。最新 WSL 扫描仍有无关 stuck `find` 任务，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 16:33 +08:00 | `ZR_AST_IF_EXPRESSION` statement 进入 CFG：建立条件 statement block、then/else 分支体和 join block；bool literal 条件剪掉不可达分支，并为被剪枝分支内语句写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，causeNode 指向条件表达式。 | TDD RED 先失败为 `if (true) {A} else {B}` 的 else 语句无不可达 fact；修复后 Windows MSVC `zr_vm_cfg_reachability_test` 3 PASS，`zr_vm_semantic_facts_test` 4 PASS，`zr_vm_expression_fact_emission_test` 28 PASS。最新 WSL 扫描仍有无关 stuck `find` 与 `cc1` 任务，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 16:39 +08:00 | `ZR_AST_WHILE_LOOP` statement 进入 CFG：建立条件 statement block、循环体子图、back edge 和 join block；`while(false)` 循环体不连入 entry 可达路径，并为循环体内语句写入 `ZR_SEMANTIC_REACHABILITY_CONDITION_FALSE`，causeNode 指向 while 条件表达式。 | TDD RED 先失败为 `while(false) {A}` 的循环体语句无不可达 fact；修复后 Windows MSVC `zr_vm_cfg_reachability_test` 4 PASS，`zr_vm_semantic_facts_test` 4 PASS，`zr_vm_expression_fact_emission_test` 28 PASS。最新 WSL 扫描仍有无关 stuck `find` 与 `cc1` 任务，未声明 WSL GREEN。 |
| 阶段 1 / Dataflow | 进行中 | 2026-06-20 16:57 +08:00 | 新增 `dataflow.h/.c` 通用引擎骨架，支持 forward/backward 工作队列、per-block in/out state、entry 初始化、join/transfer 回调和 unreachable block 过滤；CFG 同步补上 `return`/`throw` 到 exit 的退出边，供后向分析从 exit 找到函数退出语句。 | TDD RED 先因 `dataflow.h` 缺失编译失败；补实现后后向测试先失败为 unreachable statement 被遍历，修复 entry-reachability 过滤后 Windows MSVC `zr_vm_dataflow_engine_test` 2 PASS。CFG 退出边测试先失败为 successorCount `0`，修复后 `zr_vm_cfg_reachability_test` 5 PASS；`zr_vm_semantic_facts_test` 4 PASS、`zr_vm_expression_fact_emission_test` 28 PASS。最新 WSL 扫描仍有无关 stuck `find` / AOT `cc1` 任务，未声明 WSL GREEN。 |
| 阶段 1 / Semantic Query | 进行中 | 2026-06-20 17:23 +08:00 | 新增 `semantic_query.h` / `semantic/semantic_query.c` 公共查询骨架，支持 module/node scope、`TypeAt` 类型复制、`ReferencesOf` 按 symbolId 收集 borrowed reference fact 指针、`DefinitionOf` 按 reference fact 的 symbolId/declarationRange 找 declaration、`FactsAt` 聚合六类 facts、`Diagnostics` 返回当前空列表骨架。 | TDD RED 先因 `semantic_query.h` 缺失编译失败；补实现后 `ReferencesOf` 复用输出数组的边界用例先失败为旧结果污染下一次查询，修复后 Windows MSVC `zr_vm_semantic_query_test` 7 PASS，覆盖最窄 expression type、事实聚合、definition 解析、references-of scope 过滤、复用输出清空、node scope 过滤和空 diagnostics；相邻 `zr_vm_reference_fact_emission_test` 5 PASS、`zr_vm_semantic_facts_test` 4 PASS。WSL focused retry 在 CMake regenerate/build 阶段 180 秒超时且未产出测试结果，已终止本次自启动 CMake/Ninja 进程；后续扫描仍有无关 AOT/core 编译/链接任务，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 17:46 +08:00 | `ZR_AST_FOR_LOOP` statement 进入 CFG：建立可选 init、condition statement block、循环体子图、可选 step、back edge 和 join block；`for(...; false; ...)` 循环体不连入 entry 可达路径，并为循环体内语句写入 `ZR_SEMANTIC_REACHABILITY_CONDITION_FALSE`，causeNode 指向 for 条件表达式。 | TDD RED 先失败为 `for(false) {A}` 的循环体语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 6 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 当前被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断在 `zr_vm_parser_shared` 编译阶段，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 17:53 +08:00 | `ZR_AST_FOREACH_LOOP` statement 进入 CFG：建立 foreach entry/iteration block、循环体子图、back edge 和 join block；循环体会进入 CFG，因此体内 `break`/`continue`/`return`/`throw` 后续语句可以写入对应 reachability fact。 | TDD RED 先失败为 `foreach { break; A }` 的 `break` 后语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 7 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 仍被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:03 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` statement 进入第一版 CFG：建立 try statement block、try body 子图和 join block；try body 会进入 CFG，因此体内 `return`/`throw`/`break`/`continue` 后续语句可以写入对应 reachability fact。此切片尚未建模 catch/finally 异常边。 | TDD RED 先失败为 `try { return; A }` 的 `return` 后语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 8 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 仍被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:09 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 的 catch clauses 进入第一版 CFG：从 try statement block 进入 catch body 子图，并在可 fallthrough 时接回 join；catch body 内 `return`/`throw`/`break`/`continue` 后续语句可以写入对应 reachability fact。此切片尚未建模精确异常匹配和 finally 执行边。 | TDD RED 先失败为 `try { A } catch { return; B }` 的 catch body 内 `return` 后语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 9 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 仍被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:14 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 的 finally block 进入第一版 CFG：从 try/catch join 进入 finally body 子图，并在可 fallthrough 时接回 final join；finally body 内 `return`/`throw`/`break`/`continue` 后续语句可以写入对应 reachability fact。此切片尚未建模所有 abrupt-completion 下 finally 必执行的精确边。 | TDD RED 先失败为 `try { A } finally { return; B }` 的 finally body 内 `return` 后语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 10 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 仍被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:20 +08:00 | `ZR_AST_SWITCH_EXPRESSION` statement 进入第一版 CFG：建立 selector block、case candidate chain、case body 子图和 join block；case body 内 `return`/`throw`/`break`/`continue` 后续语句可以写入对应 reachability fact。此切片尚未做 case 值匹配、default 剪枝或 union 穷尽性证明。 | TDD RED 先失败为 `switch { case { return; A } }` 的 case body 内 `return` 后语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 11 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 仍被无关 dirty-tree `compiler_quickening.c` 的 `compiler_quickening_opcode_is_false_branch` 重定义错误阻断，未声明普通 target GREEN；WSL 未补跑，未声明 WSL GREEN。`cfg.c` 已到 936 行，下一步继续 CFG 扩展前需要先拆分构图模块边界。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:26 +08:00 | 完成当前 CFG 构图文件拆分：`cfg.c` 保留共享 block helpers、straight-line/loop 构图、top-level build 和 reachability fact emission；新增私有 `cfg_internal.h` 与 `cfg_control_flow.c` 承载 switch 和 try/catch/finally 构图，避免继续向接近 1000 行的单文件堆叠控制结构逻辑。 | 拆分后手工聚焦 Windows MSVC CFG harness 11 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS；`cfg.c` 从 936 行降到 726 行，`cfg_control_flow.c` 为 211 行。普通 Windows MSVC CMake target 和 WSL 仍未声明 GREEN，原因同上。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 18:36 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 补入 bool default 剪枝：当 selector 是 bool literal 且 cases 覆盖 `true` 与 `false` 两个 bool literal 时，default 分支不连入可达路径，但仍构建 default block 并写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，causeNode 指向 switch selector。 | TDD RED 先失败为 `switch(true) { case true; case false; default; }` 的 default 无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 12 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 与 WSL 仍未声明 GREEN，原因同上。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 19:14 +08:00 | `ZR_AST_WHILE_LOOP`、`ZR_AST_FOR_LOOP`、`ZR_AST_FOREACH_LOOP` 补入基础 loop-control target：body 内 `break` 会连接到当前 loop join；`continue` 在 while/foreach 中回到 loop header/iteration block，在传统 for 中连接到 step-entry join（无 step 时回 condition）。同时新增 `cfg_loops.c` 承载 while/for/foreach 构图，`cfg.c` 从再次超过 900 行回落到共享 helper、if/straight-line、top-level build 和 fact emission。 | TDD RED 依次失败为 while break、while continue、for break/continue、foreach break/continue 的 terminator successorCount 为 0；修复后手工聚焦 Windows MSVC CFG harness 18 PASS。拆分 loop 构图后同一 harness 仍 18 PASS；手工聚焦 Windows MSVC dataflow harness 2 PASS；普通 Windows MSVC CMake target 180 秒无结果并已停止自启动进程；WSL 旧 build 目录缺少新 target，独立 focused WSL build 300 秒超时且有无关 AOT 临时任务，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 19:31 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 补入 bool case matching：当 selector 是 bool literal 且 case value 是相反 bool literal 时，该 case 不连入可达路径，但仍构建 case block/body 并写入 `ZR_SEMANTIC_REACHABILITY_CONSTANT_BRANCH`，causeNode 指向 switch selector。 | TDD RED 先失败为 `switch(true) { case false { A } }` 的 case 无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 19 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 与 WSL 未声明 GREEN，沿用上一条记录的构建边界。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 19:50 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 补入 integer case/default matching：当 selector 与 case value 都是 integer literal 时，非匹配 case 不连入可达路径，匹配 case 会消耗常量 selector，使后续 default 不连入可达路径；若匹配 case 以 `return` 等终结且无 default，switch 不再保留未命中 fallthrough。 | TDD RED 先失败为 integer 非匹配 case/default 无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 21 PASS。随后新增匹配 integer case `return` 后 switch 之后语句不可达覆盖，RED 失败为该语句无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 22 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 与 WSL 未声明 GREEN，沿用上一条记录的构建边界。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 20:05 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 补入 string/char case/default matching：当 selector 与 case value 都是 string literal 或 char literal 时，非匹配 case 不连入可达路径，匹配 case 会消耗常量 selector，使后续 default / 未命中路径不连入可达路径。string literal 使用 `ZrCore_String_Equal` 比较，带解析错误或缺失 value 的 string/char literal 不参与常量匹配。 | TDD RED 先失败为 string 非匹配 case/default 无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 24 PASS。随后新增 char 非匹配 case/default 覆盖，RED 同样失败为无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 26 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。普通 Windows MSVC CMake target 与 WSL 未声明 GREEN，沿用上一条记录的构建边界。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 20:12 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 补入 float case/default matching：当 selector 与 case value 都是 float literal 时，使用 literal 解析后的 `TZrDouble` 值做精确比较；非匹配 case 不连入可达路径，匹配 case 会消耗常量 selector，使后续 default / 未命中路径不连入可达路径。 | TDD RED 先失败为 float 非匹配 case/default 无不可达 fact；修复后手工聚焦 Windows MSVC CFG harness 28 PASS，手工聚焦 Windows MSVC dataflow harness 2 PASS。`test_cfg_reachability.c` 当前 957 行，仍是单一 CFG reachability 用例集；本切片不做人工拆分，下一次继续增加 switch 矩阵时应优先抽出 switch literal reachability 测试边界。普通 Windows MSVC CMake target 与 WSL 未声明 GREEN，沿用上一条记录的构建边界。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 20:39 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 补入 return/throw abrupt-completion 到 finally entry 的可达性边：新增 `tests/parser/test_cfg_finally_abrupt.c`，验证 `try { return; } finally { A }` 的 finally body 不再被标为不可达，同时 `try { return; } finally { A } B` 仍保持 `B` 不可达。实现上只扫描 try/catch 受保护范围内的 `return`/`throw` terminator；break/continue 穿过 finally 的目标重写继续留待后续。同步确认 switch-local break 不是当前编译器已支持语义：`SZrBreakContinueStatement` 只有 `isBreak/expr`，编译器 break/continue 只读取 `loopLabelStack`，switch 编译路径没有 switch break label。 | TDD RED 先失败为 finally body 仍有不可达 fact（`Expected NULL`）；修复后手工聚焦 Windows MSVC finally-abrupt harness 2 PASS，既有手工聚焦 Windows MSVC CFG reachability harness 28 PASS、dataflow harness 2 PASS；正式 CMake target `zr_vm_cfg_finally_abrupt_test` 在 `build\agent-msvc-tests` Debug 构建并运行通过 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 20:51 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 补入 break/continue abrupt-completion 到 finally entry 的可达性边：`tests/parser/test_cfg_finally_abrupt.c` 新增 `try { break; } finally { A }` 与 `try { continue; } finally { A }`，验证 finally body 不再被标为不可达；实现上将 try/catch 受保护范围扫描扩展到 `ZR_AST_BREAK_CONTINUE_STATEMENT` terminator。此切片只补可达性边，break/continue 经 finally 后再跳往原 loop target 的精确目标重写仍未实现。 | TDD RED 先后失败为 break/continue 形态的 finally body 仍有不可达 fact（`Expected NULL`）；修复后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_cfg_finally_abrupt_test` 4 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_dataflow_engine_test` 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 21:14 +08:00 | `ZR_AST_IF_EXPRESSION`、`ZR_AST_WHILE_LOOP`、`ZR_AST_FOR_LOOP` 的常量布尔条件折叠扩展到非字面量 unary/logical 形态：新增 `tests/parser/test_cfg_constant_conditions.c` 与 `zr_vm_cfg_constant_conditions_test`，覆盖 `if (!false)`、`while (!true)`、`if (true && false)`、`while (false || false)`、`if (false && flag)`、`if (true || flag)`；`cfg_node_bool_constant` 现在是 `cfg.c` / `cfg_loops.c` 共享 helper，统一支持 bool literal、unary `!`、两侧均可折叠时的 logical `&&`/`||`，以及 `false && unknown` / `true || unknown` 这类短路决定项。 | TDD RED 先让 unary 用例失败为 `Expected Non-NULL`；补 unary `!` 后 Windows MSVC CMake focused targets 通过 constant-conditions 2 PASS、CFG reachability 28 PASS、finally-abrupt 4 PASS、dataflow 2 PASS。随后 logical `&&`/`||` 双常量用例先失败为 `Expected Non-NULL`；补 logical folding 后 constant-conditions 4 PASS。最后短路右侧未知用例先失败为 `Expected Non-NULL`；补 decisive-operand folding 后 `build\agent-msvc-tests` Debug 通过：`zr_vm_cfg_constant_conditions_test` 6 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_finally_abrupt_test` 4 PASS、`zr_vm_dataflow_engine_test` 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 21:21 +08:00 | `ZR_AST_SWITCH_EXPRESSION` 的常量匹配复用非字面量布尔折叠：新增 `tests/parser/test_cfg_switch_constants.c` 与 `zr_vm_cfg_switch_constants_test`，覆盖 `switch(!false) case true`、`switch(true) case !false`、`switch(false && flag) case true`；`cfg_control_flow.c` 的 switch constant extraction 现在先通过共享 `cfg_node_bool_constant` 提取 folded bool，再处理 integer/string/char/float literal。 | TDD RED 先让新增 switch-constants 3 个用例均失败为 `Expected Non-NULL`；补复用 bool folder 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_constant_conditions_test` 6 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_finally_abrupt_test` 4 PASS、`zr_vm_dataflow_engine_test` 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 21:28 +08:00 | `cfg_node_bool_constant` 补入整数 literal binary comparison 常量折叠：`ZR_AST_IF_EXPRESSION`、`ZR_AST_WHILE_LOOP`、`ZR_AST_FOR_LOOP` 的条件现在可识别 `==`、`!=`、`<`、`>`、`<=`、`>=` 两侧都是 integer literal 的恒真/恒假结果；新增 `if (1 == 2) { A } else { B }` 与 `while (1 < 0) { A }` 覆盖，分别验证 then/body 不再连入可达路径并写入 `CONSTANT_BRANCH` / `CONDITION_FALSE` reachability fact。 | TDD RED 先让 integer comparison 用例失败为 `Expected Non-NULL`；补整数 literal comparison folding 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_cfg_constant_conditions_test` 8 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_finally_abrupt_test` 4 PASS、`zr_vm_dataflow_engine_test` 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 21:36 +08:00 | `cfg_node_bool_constant` 补入 string/char/float literal binary comparison 常量折叠：string 支持 `==`/`!=`，char 与 float 支持 `==`、`!=`、`<`、`>`、`<=`、`>=`；新增 `if ("red" == "blue") { A } else { B }`、`while ('a' != 'a') { A }`、`if (1.5 >= 2.5) { A } else { B }` 覆盖，验证 then/body 不再连入可达路径并写入对应 reachability fact。string 使用 `ZrCore_String_Equal`，string/char literal 有 parse error 或缺失 value 时不参与折叠。 | TDD RED 先让 string/char/float 三个新增用例失败为 `Expected Non-NULL`；补 literal comparison helpers 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_finally_abrupt_test` 4 PASS、`zr_vm_dataflow_engine_test` 2 PASS。WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 21:48 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 补入单一 loop-control abrupt path 经 finally 后回到原 loop target 的目标边重写：新增 CFG 结构断言覆盖 `while { try { break; } finally { A } }` 与 `while { try { continue; } finally { A } }`，要求 break/continue terminator 不再直接连到 loop join/header，而是由 finally body fallthrough 连到对应原目标。实现上 try/catch body 在存在 finally 时先屏蔽直接 loop-control target，protected range 扫描记录 break/continue completion，再把 finally body fallthrough 接到原 loop target。 | TDD RED 使用新增用例手工重链旧 CFG 库时，break/continue 两个目标重写断言先失败为 `Expected FALSE Was TRUE`；补 target rewrite 后手工 MSVC current-source harness 通过：`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_finally_abrupt_test` 6 PASS、`zr_vm_dataflow_engine_test` 2 PASS。正式 CMake target 本次未声明 GREEN：`zr_vm_parser_shared` 被工作区内无关 AOT 编译错误 `backend_aot_c_function_body.c(1612) C2198` 阻断；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG | 进行中 | 2026-06-20 22:09 +08:00 | `ZR_AST_TRY_CATCH_FINALLY_STATEMENT` 补入 mixed normal/break/continue completion 共用 finally body 的路径敏感目标区分：新增 `while { try { if (x) break; if (y) continue; A } finally { F } B }` 覆盖，要求 normal 路径执行 finally 后回到 try 后续语句，break 路径执行 finally 后去 loop join，continue 路径执行 finally 后回 loop header，且同一个 finally statement 不再由同一个 CFG block 同时承载 break 与 continue 两个目标。实现上 finally 入口拆为 normal、function-exit、break、continue 四类路径，按 abrupt completion 类型构建 finally body 克隆并接回对应目标。 | TDD RED 先在旧 CFG 目标上让新增 mixed 用例失败为 `Expected TRUE Was FALSE`；补路径拆分后手工 current-source MSVC harness 通过：`zr_vm_cfg_reachability_test` 28 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 2 PASS。正式 CMake target 本次未声明 GREEN：`zr_vm_parser_shared` 被工作区内无关 AOT 编译错误 `backend_aot_c_scalar_locals.c(625/627) C2065` 阻断；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / Semantic Query | 进行中 | 2026-06-20 23:33 +08:00 | 接通不可达诊断查询桥：`ZrParser_SemanticQuery_Diagnostics` 现在从 scope 内 `ZR_SEMANTIC_REACHABILITY_UNREACHABLE` facts 生成结构化 warning，code 为 `unreachable_code`，message 为 `Unreachable code`，cause/suggestion 按 reachability cause 映射；`SZrSemanticContext` 新增 context-owned `queryDiagnostics` cache，查询前清空并返回 borrowed slice。 | TDD RED 先让新增 semantic-query diagnostics 用例失败为 `Expected Non-NULL`；修复后 Windows MSVC CMake target `zr_vm_semantic_query_test` 在 `build\agent-msvc-tests` Debug 通过 8 PASS。相邻验证：`zr_vm_semantic_facts_test` 4 PASS、`zr_vm_expression_fact_emission_test` 28 PASS、`zr_vm_reference_fact_emission_test` 5 PASS、`zr_vm_dataflow_engine_test` 2 PASS；直接运行当前 rebuilt parser DLL 下的 CFG focused binaries：reachability 28 PASS、constant-conditions 11 PASS、switch-constants 3 PASS、finally-abrupt 7 PASS。CFG 全 target 重新构建曾遇到 MSBuild tlog/lastbuildstate 锁和超时，未把本轮 CFG CMake rebuild 声明为 GREEN；WSL 未补跑，未声明 WSL GREEN。 |
| 阶段 1 / Reaching Definitions | 进行中 | 2026-06-20 23:55 +08:00 | 补入 reaching-defs 第一版事实载荷：`SZrSemanticReferenceFact` 新增 `definitionRange` / `hasDefinitionRange`，变量 declaration fact 指向声明 token，identifier write fact 指向写入 token；`ZrParser_SemanticFacts_ResolveLinearReachingDefinitions` 以当前 reference fact 顺序为直线代码 read fact 解析同 symbol 的最近 declaration/write。 | TDD RED 先让 `zr_vm_reference_fact_emission_test` 编译失败，缺少 `ZrParser_SemanticFacts_ResolveLinearReachingDefinitions` 与 reference fact definition 字段；修复后 Windows MSVC CMake target `zr_vm_reference_fact_emission_test` 在 `build\agent-msvc-tests` Debug 通过 6 PASS。此切片只声明直线 reference fact reaching-defs，不声明 CFG join、loop fixed point 或 definite-assignment 诊断。 |
| 阶段 1 / Semantic Query + Reaching Definitions | 进行中 | 2026-06-21 00:10 +08:00 | `ZrParser_SemanticQuery_DefinitionOf` 开始消费 read fact 的 `definitionRange`：若同 symbol 的 declaration/write fact range 匹配到达定义，则优先返回该 fact；否则保留既有 declaration fallback。新增覆盖 read 在 `seed = 3` 之后跳到 write token，同时 returned fact 仍保留原 declarationRange。 | TDD RED 先失败为 `Expected 3 Was 1`，证明旧查询仍返回 declaration；修复后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_semantic_query_test` 9 PASS、`zr_vm_reference_fact_emission_test` 6 PASS。相邻复测同轮通过：`zr_vm_semantic_facts_test` 4 PASS、`zr_vm_dataflow_engine_test` 2 PASS、`zr_vm_expression_fact_emission_test` 28 PASS；`git diff --check` 无 whitespace error，仅输出既有 LF/CRLF 提示。此切片只声明 query 消费直线 reaching-defs，不声明 CFG join、loop fixed point 或 definite-assignment 诊断。 |
| 阶段 1 / Definite Assignment + Dataflow | 进行中 | 2026-06-21 00:29 +08:00 | 新增 `dataflow_definite_assignment.h/.c` 第一版 definite-assignment 状态格，提供 `UNINIT` / `INIT` / `MAYBE_INIT` state size/init/get/set/join helper；dataflow 引擎修正首次到达 block 的传播语义，首次 predecessor/successor copy state，后续路径才执行 join，避免直线路径 assignment 被零初始化目的状态错误折叠成 `MAYBE_INIT`。新增覆盖直线 assignment 到 exit 为 `INIT`，以及 `if (condition) { assignment }` 无 `else` 在 exit join 为 `MAYBE_INIT`。 | TDD RED 先因 `dataflow_definite_assignment.h` 缺失编译失败；补 helper 后追加直线路径用例，旧引擎失败为 `Expected 1 Was 2`；修复首次到达 copy 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_query_test` 9 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_semantic_facts_test` 4 PASS、`zr_vm_expression_fact_emission_test` 28 PASS。此切片只声明状态格 helper 和 dataflow 首次到达语义，不声明 symbol mapping、read diagnostics、loop fixed-point 或 LSP 发布。 |
| 阶段 1 / Semantic Query + Definite Assignment | 进行中 | 2026-06-21 00:40 +08:00 | `SZrSemanticReferenceFact` 新增 optional definite-assignment 载荷：`hasDefiniteAssignmentState` / `definiteAssignmentState`，状态为 `UNKNOWN` / `UNINIT` / `INIT` / `MAYBE_INIT`。`ZrParser_SemanticQuery_Diagnostics` 现在能把 read fact 上的 `UNINIT` 映射为 error `uninitialized_read`，把 `MAYBE_INIT` 映射为 warning `possibly_uninitialized_read`，并按 query scope 过滤，忽略 `INIT` 和 out-of-scope read。 | TDD RED 先因缺少 `EZrSemanticDefiniteAssignmentState` 与 reference fact 状态字段编译失败；补 facts payload 与 diagnostics bridge 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_semantic_query_test` 10 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_semantic_facts_test` 4 PASS、`zr_vm_expression_fact_emission_test` 28 PASS。此切片只声明 query/fact 诊断桥，不声明真实 CFG-backed definite-assignment 状态生产或 LSP 发布。 |
| 阶段 1 / Semantic Facts + Definite Assignment | 进行中 | 2026-06-21 01:07 +08:00 | `ZrParser_SemanticFacts_ResolveLinearDefiniteAssignments` 新增直线 reference-order definite-assignment 事实解析：显式带状态的 declaration fact 作为初始 source，write fact 视为 `INIT`，read fact 继承同 symbol 最近 declaration/write 的状态；未知 declaration 不制造误报，read 保持 `UNKNOWN` / no-state。identifier write reference fact emission 现在也预填 `INIT` 状态。`semantic_query` 新增覆盖，确认 resolver 产出的 `UNINIT` read 会进入 `uninitialized_read` 诊断，而后续 write 后 read 为 `INIT` 不报诊断。 | TDD RED 先让 `zr_vm_semantic_facts_test` 编译失败，缺少 `ZrParser_SemanticFacts_ResolveLinearDefiniteAssignments`；修复后 Windows MSVC `zr_vm_semantic_facts_test` 5 PASS。新增 query 消费覆盖后 Windows MSVC `zr_vm_semantic_query_test` 11 PASS。新增 write fact 状态覆盖先失败为 `Expected TRUE Was FALSE`，补写入 fact 预填 `INIT` 后 Windows MSVC `zr_vm_reference_fact_emission_test` 6 PASS。此切片只声明直线 facts 生产、write fact 状态生产与 query 消费，不声明 CFG-backed symbol mapping、branch/loop definite-assignment 或 LSP 发布。 |
| 阶段 1 / LSP Diagnostics + Semantic Query | 进行中 | 2026-06-21 01:22 +08:00 | LSP semantic analyzer 现在在常规 type checking 后消费 parser semantic query diagnostics：新增 `semantic_analyzer_query_diagnostics.c`，先调用直线 definite-assignment resolver，再以 module scope 查询 `ZrParser_SemanticQuery_Diagnostics`，把结构化诊断转换成 `SZrDiagnostic` 并追加到 analyzer diagnostics；为避免重复，只补充同 range 尚未被旧 analyzer 诊断覆盖的位置。新增 LSP 回归覆盖 `return true ? 1 : 2` 的 semantic query `unreachable_code` 会出现在 `ZrLanguageServer_Lsp_GetDiagnostics` 结果中。 | TDD RED 先让 `zr_vm_language_server_semantic_query_diagnostics_test` 失败为 `Expected semantic query diagnostic code unreachable_code; diagnostics=0`；接入消费模块后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reachability_semantic_query_test` 9 PASS、`zr_vm_semantic_query_test` 11 PASS、`zr_vm_semantic_facts_test` 5 PASS、`zr_vm_reference_fact_emission_test` 6 PASS。此切片只声明 LSP diagnostics 发布 semantic query 诊断，不声明 CFG-backed definite-assignment、编译器前端发布或 reaching-definition definition 跳转序列化。 |
| 阶段 1 / LSP Navigation + Reaching Definitions | 进行中 | 2026-06-21 01:48 +08:00 | LSP definition 跳转开始消费 parser semantic query 的直线 reaching-definition 结果：新增 `lsp_semantic_definition_query.c/.h`，`ZrLanguageServer_LspSemanticQuery_AppendDefinitions` 在本地 symbol 目标上先运行直线 reaching-defs resolver，再调用 `ZrParser_SemanticQuery_DefinitionOf`；若 read fact 指向最近 write，则返回该 write token 的 LSP location，失败时保留旧 declaration fallback。新增 LSP 回归覆盖 `var seed; seed = 3; return seed;` 的 `return seed` 跳到写入 token 而不是声明 token。 | TDD RED 先让 `zr_vm_language_server_reaching_definition_navigation_test` 失败为返回 declaration range；修复后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_language_server_reaching_definition_navigation_test` 1 PASS、`zr_vm_language_server_lsp_interface_test` 全部完成、`zr_vm_semantic_query_test` 11 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS。此切片只声明 LSP definition 消费直线 reaching-defs，不声明 CFG-backed branch/loop definition join 或编译器前端发布。 |
| 阶段 1 / Compiler Diagnostics + Semantic Query | 进行中 | 2026-06-21 02:08 +08:00 | 编译器前端开始发布 parser semantic query diagnostics：新增 `compiler_semantic_query_diagnostics.c`，`compile_script` 在脚本语句编译和 typed metadata 构建成功后调用 `ZrParser_Compiler_PublishSemanticQueryDiagnostics`，先运行直线 definite-assignment resolver，再以 module scope 查询 `ZrParser_SemanticQuery_Diagnostics`，把结果留在 `SZrSemanticContext.queryDiagnostics` cache 中；warning 级 query diagnostic 不再走 `ZrParser_Compiler_Error`，不会把编译器 `hasError` / `hasStructuredError` 置位。新增 parser 回归覆盖 `return true ? 1 : 2` 编译后 cache 中存在 `unreachable_code` warning。 | TDD RED 先让 `zr_vm_compiler_semantic_query_diagnostics_test` 失败为 `queryDiagnostics.length` 仍是 0；接入 compiler bridge 后 Windows MSVC CMake targets 在 `build\agent-msvc-tests` Debug 通过：`zr_vm_compiler_semantic_query_diagnostics_test` 1 PASS、`zr_vm_semantic_query_test` 11 PASS、`zr_vm_semantic_facts_test` 5 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 1 PASS。此切片只声明编译器前端 context cache 发布 semantic query diagnostics，不声明 CFG-backed branch/loop definite-assignment、二进制/外部诊断序列化或编译器侧 reaching-definition definition 跳转消费。 |
| 阶段 1 / CFG-backed Definite Assignment + Compiler Diagnostics | 进行中 | 2026-06-21 02:49 +08:00 | 新增 `type_inference/semantic_definite_assignment.c`，以 CFG/dataflow definite-assignment 状态格为真实 source-level read 生产 `UNINIT` / `INIT` / `MAYBE_INIT` 状态，覆盖脚本、函数、test 与嵌套函数体；编译器 diagnostic bridge 现在先运行直线 resolver，再运行 CFG-backed resolver，随后查询 module-scope semantic query diagnostics。编译器 assignment lowering 也补齐 identifier write reference fact，避免 `seed = 1` 的左值被误记录成 read。新增 parser 回归覆盖 `var seed; if (flag) { seed = 1; } return seed;` 在编译后发布 `possibly_uninitialized_read` warning。 | TDD RED 先让 compiler semantic-query diagnostics 测试新增分支汇合用例失败为 `queryDiagnostics.length == 0`；接入 CFG resolver 后先暴露诊断误报为 `uninitialized_read`，再定位到编译器 assignment 左值产生假 read 且缺 write fact；修复后 Windows MSVC `zr_vm_compiler_semantic_query_diagnostics_test` 2 PASS。聚焦回归通过：`zr_vm_semantic_query_test` 11 PASS、`zr_vm_semantic_facts_test` 5 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 1 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG-backed Reaching Definitions + LSP Navigation | 进行中 | 2026-06-21 03:17 +08:00 | 新增 `type_inference/semantic_reaching_definitions.c`，以 CFG/dataflow 前向分析维护每个 resolved symbol 的单一定义状态；当分支汇合后所有可达路径保留同一 declaration/write 时，read fact 继续携带该 `definitionRange`，当不同分支写入不同 token 时将 read fact 的 `hasDefinitionRange` 清掉，让 `DefinitionOf` / LSP definition 保守回退到 declaration。LSP local-symbol definition 路径在直线 reaching-defs resolver 后，如果 analyzer AST 可用，会追加运行 CFG-backed reaching-defs resolver。 | TDD RED 先让 compiler semantic-query diagnostics 测试新增 divergent branch writes 用例编译失败，缺少 `ZrParser_SemanticFacts_ResolveControlFlowReachingDefinitions`；补实现后 parser 覆盖证明旧直线 resolver 会把 `return seed` 错指向最后一个 else write，而 CFG-backed resolver 会清掉该 read 的 single-definition payload。LSP 回归新增 `if/else` 双分支写入后 `return seed` 跳回 declaration 且不跳任一 branch write。Windows MSVC 聚焦构建通过，运行通过：`zr_vm_compiler_semantic_query_diagnostics_test` 3 PASS、`zr_vm_semantic_query_test` 11 PASS、`zr_vm_semantic_facts_test` 5 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG-backed Definite Assignment Precision | 进行中 | 2026-06-21 03:27 +08:00 | 修正变量声明初始化器的 definite-assignment 迁移顺序：`var seed = seed` 这类 self-initializer 中，declaration fact 先把 symbol 置为 `UNINIT`，initializer 内 read 在声明完成前读到 `UNINIT`，语句末尾仅在存在 initializer 时再把 symbol 置为 `INIT`。这避免 CFG-backed resolver 把带 initializer 的 declaration fact 直接视为已初始化而掩盖初始化器内部读取。 | TDD RED 先在 `zr_vm_semantic_facts_test` 新增 AST+fact 级覆盖，失败为 self-initializer read 状态 `INIT` 而非 `UNINIT`；补 `semantic_da_declaration_fact_state` 后 Windows MSVC 通过：`zr_vm_semantic_facts_test` 6 PASS，且相邻 `zr_vm_compiler_semantic_query_diagnostics_test` 3 PASS。此切片只声明 CFG-backed definite-assignment producer 的声明初始化器顺序修正，不声明编译器自引用初始化诊断发布；当前编译器会先走既有编译错误路径。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG-backed Definite Assignment Precision | 进行中 | 2026-06-21 03:50 +08:00 | 收紧 finally 多路径下的 definite-assignment 事实写回：同一个 source `finally` block 被 normal / function-exit 等 completion path 克隆为多个 CFG statement block 时，read fact 先按 fact index 累积所有到达状态，CFG 求解结束后统一写回 join 结果。`semantic_da_fact_in_statement` 同步排除 `try`/`catch`/`block`/`switch default` 这类结构化父节点的整段 range 匹配，避免父 statement 提前吞掉子 body 内的 write/read fact。 | TDD RED 先在 `zr_vm_semantic_facts_test` 新增 `try { if (flag) { seed = 1; return seed; } } finally { return seed; }` AST+fact 覆盖，旧实现把 finally read 误标为 `INIT`；定位后发现 try 父节点先匹配内部 write/read 并污染正常路径。修复后 Windows MSVC `zr_vm_semantic_facts_test` 7 PASS。此切片只声明 CFG-backed definite-assignment producer 的 finally cloned-path read join 与结构化父节点匹配收窄，不声明 loop 定点、局部重算或剩余 finally 诊断发布。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG-backed Definite Assignment Precision | 进行中 | 2026-06-21 04:26 +08:00 | 收紧常量真循环和无条件 `for` 的 CFG 出口：`cfg_loops.c` 仅在条件未知或可为 false 时连接 loop-header -> join 的零次执行边；`while(true)`、`for(...; true; ...)` 和 `for(;;)` 不再自动拥有虚假零次执行路径，仍通过真实 `break` terminator 离开循环。该 CFG 修正让 CFG-backed definite-assignment 在 write-before-break 场景中看到唯一真实到达路径。 | TDD RED 先在 `zr_vm_semantic_facts_test` 新增 AST+fact 级 `var seed; while (true) { seed = 1; break; } return seed;` 覆盖，失败为 final read `MAYBE_INIT`；同源 compiler 覆盖失败为仍发布 `possibly_uninitialized_read`。修复 CFG 零次出口并补无条件 `for` CFG 回归后 Windows MSVC focused targets 通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 6 PASS。并行构建曾遇到 MSBuild tlog 锁，随后串行重建通过；WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / Compiler + Reaching Definitions | 进行中 | 2026-06-21 04:43 +08:00 | 编译器侧 definition query 开始消费 reaching-definition payload：`ZrParser_Compiler_PublishSemanticQueryDiagnostics` 现在除 definite-assignment resolver 外，还运行直线与 CFG-backed reaching-defs resolver；`compile_script` 成功后保留在 `SZrSemanticContext` 的 reference facts 已可直接让 `ZrParser_SemanticQuery_DefinitionOf` 把唯一到达的 read 定位到 write token。 | TDD RED 在重新构建 focused target 后先失败为 `Expected Non-NULL`，旧 compiler bridge 只发布 diagnostics、不生产 reaching-definition definition payload；修复后 Windows MSVC `zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。相邻回归同轮通过：`zr_vm_semantic_query_test` 11 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS；`git diff --check` 仅有既有 LF/CRLF 提示；无残留 `build\agent-msvc-tests` 非 shell 进程。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / Reaching Definitions Multi-Definition | 进行中 | 2026-06-21 05:01 +08:00 | 首版多定义 surface 接到 parser query 与 LSP definition：reference fact 新增 `definitionRanges`，CFG-backed reaching-defs 在 ambiguous join 上保留多个到达定义 range；`ZrParser_SemanticQuery_DefinitionsOf` 返回匹配的 declaration/write fact 列表，LSP definition 路径使用它输出多个 location。`DefinitionOf` 的单定义兼容行为保留，供只需要唯一位置的 consumer 使用。 | TDD RED 先把 LSP divergent-branch 回归从 declaration fallback 改成两个 branch write location，旧实现失败为 `definitions=1`；修复后 Windows MSVC focused targets 通过：`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。相邻回归同轮通过：`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS；`git diff --check` 仅有既有 LF/CRLF 提示；无残留 `build\agent-msvc-tests` 非 shell 进程。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / Reaching Definitions Ranking | 进行中 | 2026-06-21 05:10 +08:00 | 多定义 query 输出补齐首版 source-order ranking：`DefinitionsOf` 将同源 declaration/write fact 按 `sourceRange` 起止位置排序后返回，保证 LSP 多 definition location 不受 `definitionRanges` 写入顺序影响。 | TDD RED 先反转 semantic query 多定义测试中的 `definitionRanges` 顺序，旧实现失败为 `Expected 10 Was 20`；修复后 Windows MSVC focused build 通过，回归通过：`zr_vm_semantic_query_test` 12 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS；`git diff --check` 仅有既有 LF/CRLF 提示；无残留 `build\agent-msvc-tests` 非 shell 进程。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Catch Filtering | 进行中 | 2026-06-21 05:26 +08:00 | 首版 catch 过滤边接入 CFG：`cfg_control_flow.c` 会在构建 catch body 前扫描受保护 try body 是否包含显式 `throw`；无显式 `throw` 时 catch body 仍构建但不连入 entry 可达路径，因此 `try { A } catch { B }` 的 `B` 会得到 unreachable fact；存在显式 `throw` 时保留 catch 可达路径。新增独立 `tests/parser/test_cfg_try_catch_edges.c`，避免继续扩张已超过 1000 行的 `test_cfg_reachability.c`。 | TDD RED 先让 `zr_vm_cfg_try_catch_edges_test` 失败为 `Expected Non-NULL`，证明无显式 `throw` 的 catch body 仍被误连可达；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_try_catch_edges_test` 2 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Catch Throwability | 进行中 | 2026-06-21 05:34 +08:00 | catch entry 判断扩展为保守可抛表达式扫描：`cfg_control_flow.c` 将 helper 改为 `cfg_node_may_enter_catch`，递归检查常见表达式/语句子节点，并把函数调用、构造表达式、import 表达式视为可能进入 catch；`try { call(); } catch { B }` 不再被误判为 catch body unreachable。 | TDD RED 先让新增调用覆盖失败为 `Expected NULL`；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_try_catch_edges_test` 3 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Catch Ordering | 进行中 | 2026-06-21 05:42 +08:00 | catch-all 顺序消费接入 CFG：`cfg_build_try_statement` 现在按 catch clause 顺序建图；一个可达的 catch-all（无 pattern 或空 pattern）出现后，后续 catch body 仍构建但不再接入 entry 可达路径，并以 `CONSTANT_BRANCH` 标记为不可达。新增 `try { throw; } catch { A } catch { B }` 覆盖，锁定第二个 catch 被前一个 catch-all 消耗。 | TDD RED 先让新增覆盖失败为 `Expected TRUE Was FALSE`；常规 CMake target 当前被无关 `zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c` 的 `execution_copy_value_fast` 未声明/重定义错误阻断，因此本轮用 no-deps MSBuild 刷新 parser/test。修复后 focused 回归通过：`zr_vm_cfg_try_catch_edges_test` 4 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Catch Untyped Parameter | 进行中 | 2026-06-21 05:52 +08:00 | 未类型化 catch 参数纳入 catch-all 判定：`cfg_catch_clause_is_catch_all` 现在把单个 `ZR_AST_PARAMETER` 且 `typeInfo == NULL` 的 `catch (e)` 当作全捕获，保持带类型注解或多参数 catch 不消费后续 catch。新增 `try { throw; } catch (e) { A } catch { B }` 覆盖。 | TDD RED 先让新增覆盖失败为 `Expected TRUE Was FALSE`；修复后 no-deps MSBuild 刷新 parser/test 均 0 warning / 0 error，focused 回归通过：`zr_vm_cfg_try_catch_edges_test` 5 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。参考 QuickJS `catch (e)` 全捕获绑定与 CPython typed `except X as e` 过滤模式；本切片只声明未类型化参数全捕获，不声明 typed catch 类型匹配完成。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Typed Catch Literal Matching | 进行中 | 2026-06-21 06:17 +08:00 | 首版 typed catch 字面量匹配接入 CFG：新增 `cfg_catch.c` 承载 catch 可抛性扫描、catch-all 判定和单一已知 literal throw kind 到 simple typed catch 参数的匹配；当 protected try body 只有一个已知 literal throw kind 且无未知可抛源时，`catch(e: string)` 不消费 `throw 1`，`catch(e: int)` 会消费 `throw 1` 并剪掉后续 catch。 | TDD RED 先让两个 typed catch 覆盖失败；修复后重新生成 `build\agent-msvc-tests` 工程以纳入 `cfg_catch.c`，no-deps MSBuild 刷新 parser/test 均 0 warning / 0 error，focused 回归通过：`zr_vm_cfg_try_catch_edges_test` 7 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS。此切片只声明单一已知 literal throw kind 的 simple typed catch 匹配，不声明完整异常类型系统、throw 表达式类型推断或函数 effect。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Typed Catch Multi-Kind Matching | 进行中 | 2026-06-21 06:34 +08:00 | typed catch 匹配从单一已知 literal kind 扩展为已知 kind 集合：`cfg_catch.c` 的 throw profile 保留 literal kind bitmask，`cfg_control_flow.c` 在 catch 顺序中维护 remaining known kind mask；try block 只连接一个 catch dispatch join，后续 dispatch join 串联多个可达 catch，避免固定 2 successor 限制。`try` 体可能抛 int/string 时，非相交 `catch(e: bool)` 会剪枝，`catch(e: int)` 与 `catch(e: string)` 分别可达并消费对应 kind。 | TDD RED 先让多类型 typed catch 用例失败，暴露旧 profile 把多个已知 kind 降级为 unknown，且多个可达 catch 会超过 try block successor 上限；修复后 Windows MSVC focused 回归通过：`zr_vm_cfg_try_catch_edges_test` 8 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Typed Catch Cast Matching | 进行中 | 2026-06-21 06:48 +08:00 | typed catch 匹配扩展到显式类型转换 throw 表达式：`cfg_catch.c` 新增统一 throw-kind 抽取，先识别 literal kind，再读取 `ZR_AST_TYPE_CAST_EXPRESSION.targetType` 的简单类型名；`throw (value as string)` 现在可按 string kind 进入 catch dispatch，剪枝 `catch(e: int)`、保留并消费 `catch(e: string)`，最终 catch-all 在 known kind 被消费后保持不可达。转换内部表达式仍会先递归扫描潜在可抛源，保留 `throw (call() as string)` 这类路径的 unknown 保守性。 | TDD RED 先让显式 cast 覆盖失败为第一个 typed catch 无 unreachable fact；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_try_catch_edges_test` 9 PASS。相邻回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL 本轮未补跑，未声明 WSL GREEN。 |
| 阶段 1 / CFG Typed Catch Local Binding | 进行中 | 2026-06-21 07:06 +08:00 | typed catch 匹配扩展到顺序局部声明类型绑定：新增 `cfg_throw_profile.c`，把 catch 可抛性扫描、throw kind profile、literal/cast/identifier kind 抽取和局部 `var name: BuiltinType` 绑定从 `cfg_catch.c` 拆出；`cfg_catch.c` 只保留 catch-all 与 typed catch 参数匹配。顺序块中 `var value: string = "x"; throw value;` 现在会把 `value` 映射为 string kind，进而剪枝 `catch(e: int)`、保留 `catch(e: string)` 并消费 catch-all。 | TDD RED 先让局部变量类型覆盖失败为第一个 typed catch 无 unreachable fact；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_try_catch_edges_test` 10 PASS。相邻回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL focused 尝试未产出 GREEN：现有 `build/codex-wsl-gcc-debug` 与 `build-wsl-gcc` 均缺少该测试 target，随后在 `build/codex-wsl-gcc-debug` 重新配置/构建/运行的尝试 244 秒超时。 |
| 阶段 1 / CFG Typed Catch Assignment Binding | 进行中 | 2026-06-21 07:30 +08:00 | typed catch 匹配扩展到同一顺序块内的简单赋值类型绑定：`cfg_throw_profile.c` 在 `ZR_AST_EXPRESSION_STATEMENT` 包裹的 `ZR_AST_ASSIGNMENT_EXPRESSION` 中识别 identifier 左值；普通 `=` 且右值可映射为已知 throw kind 时更新后续绑定，非简单赋值或未知右值则用 unknown binding 遮蔽旧绑定。`var value: int = 1; value = "x"; throw value;` 现在会按最新赋值的 string kind 匹配 typed catch。 | TDD RED 先让赋值覆盖用例失败为 int catch 无 unreachable fact；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL focused 未产出 GREEN：`build/codex-wsl-gcc-debug` 能看到该 target，但低并发 build/run 184 秒超时；进程扫描未发现本轮目标仍在运行。 |
| 阶段 1 / CFG Typed Catch Branch Assignment Binding | 进行中 | 2026-06-21 07:52 +08:00 | typed catch 匹配扩展到简单同名 if/else 分支赋值合并：新增 `tests/parser/test_cfg_typed_catch_flow.c` 和 `zr_vm_cfg_typed_catch_flow_test`，避免继续扩大 catch-edge harness；`cfg_throw_profile.c` 会在 then/else 两侧都产生同一 identifier 的已知绑定时合并 kind mask。`var value: int = 1; if (flag) { value = "x"; } else { value = 'c'; } throw value;` 现在按 string/char kind 集合匹配 typed catch，剪枝过期 int catch 和最终 catch-all。 | TDD RED 先让新增分支赋值覆盖失败为 int catch 无 unreachable fact；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 1 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL 重新配置 `build/codex-wsl-gcc-debug` 后 focused build/run 通过：`zr_vm_cfg_typed_catch_flow_test` 1 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；`ninja` 末尾提示 recovered build log warning，但测试均已通过。 |
| 阶段 1 / CFG Typed Catch Partial Branch Assignment Binding | 进行中 | 2026-06-21 08:13 +08:00 | typed catch 匹配扩展到单边分支赋值与进入分支前的已知类型合并：`cfg_throw_profile.c` 在 `if` 只有 then 或 else 一侧产生同名已知绑定时，会与 incoming binding 合并 kind mask；未知赋值仍用 unknown binding 遮蔽旧类型。同步把 builtin throw kind mask、simple type-name mapping 和 string helper 抽到 `cfg_throw_types.c`，让 `cfg_throw_profile.c` 回落到 profile/binding 流。`var value: int = 1; if (flag) { value = "x"; } throw value;` 现在按 int/string kind 集合匹配 typed catch，剪枝 bool catch 和最终 catch-all。 | TDD RED 先让新增单边分支用例失败为 string catch 仍有 unreachable fact；修复并拆分后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 2 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL 重新配置 `build/codex-wsl-gcc-debug` 后 focused build/run 通过：`zr_vm_cfg_typed_catch_flow_test` 2 PASS；相邻 `zr_vm_cfg_try_catch_edges_test` WSL build 被无关 dirty-tree `compiler_quickening.c` duplicate case 阻断，未声明 GREEN。 |
| 阶段 1 / CFG Typed Catch Multi-Variable Branch Binding | 进行中 | 2026-06-21 13:45 +08:00 | typed catch 匹配扩展到简单多变量分支结果绑定：`cfg_throw_bindings.c` 承载变量声明、简单赋值、分支结果绑定数组和 if 结果绑定合并，`cfg_throw_profile.c` 回落到 catch throwability/profile 扫描与 literal/cast/identifier kind 抽取。`var first: int = 1; var second: int = 2; if (flag) { first = "x"; second = "y"; } else { first = 'c'; second = 3; } throw first;` 现在按 first 的 string/char kind 集合匹配 typed catch，剪枝过期 int catch 和最终 catch-all。 | TDD RED 先让多变量分支覆盖失败为 int catch 无 unreachable fact；修复并拆分后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 3 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang 重新配置后 focused 均通过：`zr_vm_cfg_typed_catch_flow_test` 3 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。 |
| 阶段 1 / CFG Typed Catch While Assignment Binding | 进行中 | 2026-06-21 14:07 +08:00 | typed catch 匹配扩展到 unknown 条件 `while` body 的简单赋值结果绑定：`cfg_throw_bindings.c` 对非常量条件 `ZR_AST_WHILE_LOOP` 收集 body 最终绑定，并与进入循环前的 known binding 合并以保留零次迭代路径。`var value: int = 1; while (flag) { value = "x"; } throw value;` 现在按 int/string kind 集合匹配 typed catch，剪枝 bool catch 和最终 catch-all；常量 false/true 循环和更广泛 fixed point 不在本切片声明范围内。 | TDD RED 先让 unknown while 覆盖失败为 string catch 仍有 unreachable fact；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang focused 均通过：`zr_vm_cfg_typed_catch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；WSL 构建仍重编无关 AOT/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch For Assignment Binding | 进行中 | 2026-06-21 14:23 +08:00 | typed catch 匹配扩展到 unknown 条件传统 `for` body 的简单赋值结果绑定：`cfg_throw_bindings.c` 复用循环 body 合并 helper，对带显式非常量 condition 的 `ZR_AST_FOR_LOOP` 收集 body 最终绑定，并与进入循环前的 known binding 合并以保留零次迭代路径。`var value: int = 1; for (; flag; ) { value = "x"; } throw value;` 现在按 int/string kind 集合匹配 typed catch，剪枝 bool catch 和最终 catch-all；省略 condition 的 `for(;;)`、常量条件、init/step 绑定和更广泛 fixed point 不在本切片声明范围内。 | TDD RED 先让 unknown for 覆盖失败为 `Expected NULL`，因为 string catch 仍被剪枝；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang focused 均通过：`zr_vm_cfg_typed_catch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；WSL 构建仍重编无关 AOT/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch For Step Binding | 进行中 | 2026-06-21 14:42 +08:00 | typed catch 匹配扩展到 unknown 条件传统 `for` step 的简单赋值结果绑定：`cfg_throw_bindings.c` 把 step 作为 completed iteration 的后续语句收集，先让 body 绑定进入 step，再由 step 覆盖同名 final binding，最后与进入循环前的 known binding 合并。`var value: int = 1; for (; flag; value = 'c') { value = "x"; } throw value;` 现在按 int/char kind 集合匹配 typed catch，剪枝循环体内 string-only catch 和最终 catch-all；break/continue、init 绑定、常量/无条件循环和完整 loop fixed point 不在本切片声明范围内。 | TDD RED 先让 for-step 覆盖失败为 `Expected Non-NULL`，因为 string catch 仍被错误保留；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 6 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang focused 直接运行均通过：`zr_vm_cfg_typed_catch_flow_test` 6 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；后续 focused build 对 gcc/clang 均报告 no work。初次并行 WSL build 因共享 AOT 构建负载超时，但目标进程随后结束且二进制验证通过。 |
| 阶段 1 / CFG Typed Catch For Init Binding | 进行中 | 2026-06-21 15:05 +08:00 | typed catch 匹配扩展到 unknown 条件传统 `for` init 的简单赋值结果绑定：`cfg_throw_bindings.c` 先收集 init 简单赋值，再把 init 后绑定作为后续 condition/body/step 的 incoming binding；常量 false 条件只收集 init，常量 true/无条件循环继续不声明 post-loop 结果。`var value: int = 1; for (value = "x"; flag; ) { flag; } throw value;` 现在按 init 的 string kind 匹配 typed catch，剪枝过期 int catch、保留 string catch 并剪枝最终 catch-all；break/continue-sensitive exits、复杂 init、常量 true/无条件循环和完整 loop fixed point 不在本切片声明范围内。 | TDD RED 先让 for-init 覆盖失败为 `Expected Non-NULL`，因为 int catch 仍可达；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 7 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang focused build/run 均通过：`zr_vm_cfg_typed_catch_flow_test` 7 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；WSL 构建仍重编无关 AOT/parser/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch Loop Break Binding Pruning | 进行中 | 2026-06-21 15:36 +08:00 | typed catch 匹配补入直线终结语句后的结果绑定剪枝：`cfg_throw_bindings.c` 在顺序结果绑定收集时遇到 `return`/`throw`/`break`/`continue` 后停止扫描同级后续语句，避免 loop body 中不可达赋值覆盖 post-loop throw kind。`var value: int = 1; while (flag) { break; value = "x"; } throw value;` 现在只保留进入循环前的 int kind，剪枝 bool/string catch 和最终 catch-all；条件 break、break 前赋值出环、跨块循环 fixed point 等更广泛 break/continue-sensitive exits 不在本切片声明范围内。 | TDD RED 先让 break-after-assignment 覆盖失败为 `Expected Non-NULL`，因为 string catch 被错误保留；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 首次构建超时后重跑 build/run 通过，WSL clang build/run 通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；WSL 构建仍重编无关 AOT/parser/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch Foreach Assignment Binding | 进行中 | 2026-06-21 19:27 +08:00 | typed catch `foreach` body 简单赋值流落地：`cfg_throw_bindings.c` 复用循环体结果绑定合并 helper，把 `ZR_AST_FOREACH_LOOP` body 的简单结果绑定与进入循环前的 known binding 合并，保留零次迭代路径。因此 `var value: int = 1; foreach (...) { value = "x"; } throw value;` 会按 int/string kind 集合匹配 typed catch，剪枝 bool catch 和最终 catch-all。iterator/item binding state、复杂 foreach body 和更广泛 loop fixed point 不在本切片声明范围内。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_loop_flow_test` foreach 覆盖失败为 `Expected NULL`，因为 string catch 被错误剪枝；修复后 Windows MSVC focused build/test 通过：`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。WSL gcc 与 clang 重新配置后 focused 均通过：`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS；WSL 构建仍重编无关 AOT/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch Switch Assignment Binding | 进行中 | 2026-06-21 19:51 +08:00 | typed catch switch case/default 简单赋值流落地：新增 `cfg_throw_switch_bindings.c` 和 `tests/parser/test_cfg_typed_catch_switch_flow.c` / `zr_vm_cfg_typed_catch_switch_flow_test`，把 `ZR_AST_SWITCH_EXPRESSION` 的 case/default body 结果绑定合并到后续 `throw value` 的 known kind set。`var value: int = 1; switch (tag) { case 1 { value = "x"; } default { value = 'c'; } } throw value;` 现在按 string/char kind 集合匹配 typed catch，剪枝 bool/int catch 和最终 catch-all。常量 selector 精确路径、case 终结语句精度、无 default 零匹配路径之外的复杂 switch/union exhaustiveness 不在本切片声明范围内。 | TDD RED 先让 switch 覆盖失败为 `Expected Non-NULL`，因为 stale int catch 仍可达；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_switch_flow_test` 1 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS、`zr_vm_compiler_semantic_query_diagnostics_test` 7 PASS、`zr_vm_semantic_query_test` 12 PASS、`zr_vm_reference_fact_emission_test` 6 PASS、`zr_vm_language_server_semantic_query_diagnostics_test` 1 PASS、`zr_vm_language_server_reaching_definition_navigation_test` 2 PASS。`ctest -R ^zr_vm_cfg_typed_catch_switch_flow_test$` 在当前 build 目录未发现测试，因此按既有 focused harness 方式直接运行 exe。WSL gcc/clang 重新配置后 focused build/run 均通过：`zr_vm_cfg_typed_catch_switch_flow_test` 1 PASS；相邻 `zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS 在两种 WSL toolchain 也通过；WSL 构建仍重编无关 AOT/parser/library 依赖并输出既有 const-qualifier warnings。 |
| 阶段 1 / CFG Typed Catch Switch Constant Selector Binding | 进行中 | 2026-06-21 20:14 +08:00 | typed catch switch 常量 selector 精确匹配落地：新增 `cfg_constants.c` 共享 bool/int/string/char/float 常量抽取与比较，`cfg_control_flow.c` 与 `cfg_throw_switch_bindings.c` 复用同一 helper；当 selector 与 case value 可比较且命中时，switch 结果绑定只收集匹配 case body，跳过 unreachable default 绑定。`var value: int = 1; switch (1) { case 1 { value = "x"; } default { value = 'c'; } } throw value;` 现在只按 string kind 匹配 typed catch，剪枝 bool/int/char catch 和最终 catch-all。不可比较 case 仍保守回退到未知 selector 合并；terminated case-body 精度、复杂 selector 边界和 switch/union exhaustiveness 不在本切片声明范围内。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_switch_flow_test` 常量 selector 覆盖失败为 `Expected Non-NULL`，因为 default 的 char catch 仍被错误保留；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_switch_flow_test` 2 PASS。相邻 Windows 回归通过：`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL 初次 build 因新增 `cfg_constants.c` 未被旧 build 目录收进 glob 而链接缺 symbol，重新配置 gcc/clang 后 focused build/run 均通过：`zr_vm_cfg_typed_catch_switch_flow_test` 2 PASS；相邻 `zr_vm_cfg_switch_constants_test` 3 PASS 与 `zr_vm_cfg_reachability_test` 29 PASS 在 WSL gcc/clang 均通过。 |
| 阶段 1 / CFG Typed Catch Switch Terminating Case Binding | 进行中 | 2026-06-21 20:21 +08:00 | typed catch switch 终结 case 赋值剪枝落地：`cfg_throw_switch_bindings.c` 现在只把可 fallthrough 的 case/default body 结果绑定合并到 post-switch `throw value`，遇到 `return`/`throw`/`break`/`continue` 终结路径时不会让该 body 中的赋值污染后续 typed catch 匹配。`var value: int = 1; switch (tag) { case 1 { value = "x"; return; } default { value = 'c'; } } throw value;` 现在只按 default fallthrough 的 char kind 匹配 typed catch，剪枝 bool/int/string catch 和最终 catch-all。switch-local break 仍不声明为语言支持语义；复杂 selector 边界、case fallthrough 语法语义和 switch/union exhaustiveness 不在本切片声明范围内。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_switch_flow_test` terminating-case 覆盖失败为 `Expected Non-NULL`，因为 case 内 `return` 前的 string 赋值仍被错误保留；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_switch_flow_test` 3 PASS。相邻 Windows 回归通过：`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused build/run 均通过：`zr_vm_cfg_typed_catch_switch_flow_test` 3 PASS。 |
| 阶段 1 / CFG Typed Catch Constant Branch Binding | 已完成 | 2026-06-21 20:43 +08:00 | typed catch 常量 `if` 分支赋值剪枝落地：新增 `tests/parser/test_cfg_typed_catch_branch_flow.c` / `zr_vm_cfg_typed_catch_branch_flow_test`，并把目标注册到 parser semantic test 区和 `language_pipeline` 聚合清单。`cfg_throw_bindings.c` 在 `if` 结果绑定合并前复用 `cfg_node_bool_constant`；条件可折叠时只收集实际可达分支的结果绑定，不再把不可达分支赋值合并进后续 `throw value`。`var value: int = 1; if (true) { value = "x"; } else { value = 'c'; } throw value;` 现在只按 string kind 匹配 typed catch，剪枝 bool/int/char catch 和最终 catch-all。非常量 `if`、嵌套/递归分支流和复杂符号条件仍走既有保守合并或留待后续。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_branch_flow_test` 失败为 `Expected Non-NULL`，因为不可达 else 的 char catch 仍可达；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 3 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 3 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc 重新配置后 focused build/run 通过，WSL clang 自动重新配置后 focused build/run 通过：`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS；相邻 `zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 3 PASS 在 WSL gcc/clang 均通过。 |
| 阶段 1 / CFG Switch Constant Kind Mismatch | 已完成 | 2026-06-21 20:57 +08:00 | switch 常量 kind mismatch 精度落地：`cfg_constants.c` 现在把所有已知 literal 常量视为可比较，但不同 kind 永远不相等；`cfg_control_flow.c` 因此会把 `switch (1) { case "x" { ... } default { ... } }` 的 string case 剪枝为不可达，同时保留 default，`cfg_throw_switch_bindings.c` 也能跳过 mixed-kind 非匹配 case，继续使用后续 matching integer case 的结果绑定。新增 `zr_vm_cfg_switch_constants_test` mixed-kind reachability 覆盖，并扩展 `zr_vm_cfg_typed_catch_switch_flow_test` 覆盖 mixed-kind case 之后的 matching case typed-catch 绑定。此切片只声明已知 literal kind mismatch 的确定不相等，不声明复杂 selector、隐式转换、union exhaustiveness 或自定义类型匹配。 | TDD RED 先让 `zr_vm_cfg_switch_constants_test` mixed-kind case 覆盖失败为 `Expected Non-NULL`，同时 `zr_vm_cfg_typed_catch_switch_flow_test` mixed-kind 后续 matching case 覆盖失败为 `Expected Non-NULL`；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS。相邻 Windows 回归通过：`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 2 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused build/run 均通过：`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS。 |
| 阶段 1 / CFG Typed Catch Constant Break Branch Binding | 已完成 | 2026-06-21 21:17 +08:00 | typed catch 常量分支终结后的同级赋值剪枝落地：`cfg_throw_bindings.c` 新增复合语句级 result-flow 终结判断，能识别已折叠 `if (true)` 的选中分支在 `break`/`return`/`throw`/`continue` 处终结，随后停止收集同一顺序块后续赋值。`while (flag) { if (true) { value = "x"; break; } value = 'c'; } throw value;` 现在保留进入循环前的 int kind 与 break 出环前的 string kind，剪枝不可达的 char kind。此切片只声明常量分支可证明终结后的同级赋值剪枝，不声明符号条件、跨块 fixed point 或完整 break/continue-sensitive loop exits。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_loop_flow_test` 用例失败为 `Expected Non-NULL`，因为 `if(true){...break}` 后的 char catch 仍可达；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_loop_flow_test` 3 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused build/run 均通过：`zr_vm_cfg_typed_catch_loop_flow_test` 3 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。 |
| 阶段 1 / CFG Typed Catch Continue Path Binding | 已完成 | 2026-06-21 21:41 +08:00 | typed catch 条件 `continue` 路径赋值出环保留落地：`cfg_throw_bindings.c` 将 loop-control 结果绑定收集从 break-only 扩展为 break/continue 两类路径；`continue` 路径会作为下一轮条件后可能出环的状态合并到 post-loop `throw value`，传统 `for` 的 continue 路径会先经过 step 绑定再合并。`while (flag) { if (done) { value = "x"; continue; } value = 'c'; } throw value;` 现在保留进入循环前的 int、continue 路径的 string 和正常路径的 char kind，剪枝 bool catch 和最终 catch-all。此切片只声明简单同循环体条件 continue 路径，不声明跨块 fixed point、嵌套循环控制流或非简单赋值。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_loop_flow_test` continue 用例失败为 `Expected NULL`，因为 string catch 被错误剪枝；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_loop_flow_test` 4 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused build/run 均通过：`zr_vm_cfg_typed_catch_loop_flow_test` 4 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。 |
| 阶段 1 / CFG Typed Catch Constant Loop-Control Branch Binding | 已完成 | 2026-06-21 21:58 +08:00 | typed catch 常量 loop-control 分支转移剪枝落地：`cfg_throw_bindings.c` 的 loop-control 结果绑定收集现在复用 `cfg_node_bool_constant`；遇到可折叠的 `if` 时只进入实际选中分支收集 `break`/`continue` 转移绑定，不再把不可达分支里的赋值合并到 post-loop `throw value`。`while (flag) { if (true) { value = "x"; break; } else { value = 'c'; break; } } throw value;` 现在保留进入循环前的 int kind 与选中 break 路径的 string kind，剪枝不可达 else 的 char kind。此切片只声明简单 folded-if loop-control 转移分支，不声明嵌套循环控制流、非简单赋值或完整 fixed point。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_loop_flow_test` 用例失败为 `Expected Non-NULL`，因为不可达 else break 路径的 char catch 仍被错误保留；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused build/run 均通过：`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 4 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。 |
| 阶段 1 / CFG Typed Catch Switch Constant Branch Fallthrough Binding | 已完成 | 2026-06-21 22:49 +08:00 | typed catch switch 常量分支落空剪枝落地：`cfg_throw_switch_bindings.c` 的 switch case/default fallthrough 判断现在复用 `cfg_node_bool_constant`；遇到可折叠的 `if` 时只检查实际选中分支是否能落到 post-switch `throw value`，不再让不可达分支赋值或已终结选中分支前的赋值污染后续 typed catch 匹配。`switch (tag) { case 1 { if (true) { value = "x"; return; } else { value = "stale"; } } default { value = 'c'; } } throw value;` 现在只保留 default fallthrough 的 char kind，剪枝 bool/int/string/catch-all。此切片只声明 folded-if case/default fallthrough 证明，不声明符号分支、switch-local break、复杂 selector 或 union exhaustiveness。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_switch_flow_test` 用例失败为 1/5 `Expected Non-NULL`，因为常量选中分支 `return` 前的 string 赋值仍被错误保留；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS。相邻 Windows 回归通过：`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS。WSL gcc/clang focused 与相邻回归均通过：`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 1 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS。`git diff --check` exit 0，仅输出既有 LF/CRLF warning。 |
| 阶段 1 / CFG Typed Catch Symbolic Branch Fallthrough Binding | 已完成 | 2026-06-21 23:16 +08:00 | typed catch 符号分支 fallthrough 路径剪枝落地：新增 `cfg_throw_branch_bindings.c`，把 if/else 结果绑定合并从已超过 1000 行的 `cfg_throw_bindings.c` 拆出；`cfg_throw_bindings.c` 降至 939 行。非常量 `if` 现在只让能落到后续语句的分支参与 post-branch `throw value` 类型绑定，赋值后 `return`/`throw`/`break`/`continue` 的终结分支不会污染后续 typed catch 匹配。`if (flag) { value = "x"; return; } else { value = 'c'; } throw value;` 现在只按 else fallthrough 的 char kind 匹配 typed catch，剪枝 bool/int/string/catch-all。此切片只声明简单符号 if 终结分支 fallthrough 剪枝，不声明更深递归分支、用户类型/继承或完整路径 fixed point。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_branch_flow_test` 用例失败为 1/2 `Expected Non-NULL`，因为终结 then 分支的 string assignment 仍让 string catch 可达；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_branch_flow_test` 2 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS。WSL gcc/clang focused 与相邻回归通过：`zr_vm_cfg_typed_catch_branch_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_loop_flow_test` 5 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS。WSL gcc 首次 build 超时后目标已完成，重跑报告 no work；WSL 构建仍重编无关 AOT/parser/library 依赖并输出既有 warning。`git diff --check` exit 0，仅输出既有 LF/CRLF warning。 |
| 阶段 1 / CFG Typed Catch Loop Break Prefix Binding | 已完成 | 2026-06-21 23:46 +08:00 | typed catch 条件 `break` 前缀赋值出环保留落地：新增 `cfg_throw_loop_bindings.c`，把 while/for/foreach typed-catch loop 结果绑定与 loop-control 转移绑定从 `cfg_throw_bindings.c` 拆出，`cfg_throw_bindings.c` 降至 525 行，新增 loop 文件 484 行。loop-control 转移收集现在会把嵌套 `break`/`continue` 所在分支之前、外层顺序路径已生效的赋值状态带入转移绑定。`while (flag) { value = "x"; if (done) { break; } value = 'c'; } throw value;` 现在保留零次迭代 int、break 前 string 和正常路径 char，剪枝 bool/catch-all。此切片只声明简单同循环体内嵌套条件 break 前赋值出环保留，不声明跨块循环 fixed point、用户类型/继承或非简单赋值。 | TDD RED 先让新增 `zr_vm_cfg_typed_catch_loop_flow_test` 用例失败为 1/6 `Expected NULL`，因为嵌套条件 break 路径丢失了分支前 `value = "x"` 的 string 绑定；修复后 Windows MSVC focused build/run 通过：`zr_vm_cfg_typed_catch_loop_flow_test` 6 PASS。相邻 Windows 回归通过：`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS、`zr_vm_cfg_constant_conditions_test` 11 PASS、`zr_vm_cfg_switch_constants_test` 4 PASS、`zr_vm_cfg_finally_abrupt_test` 7 PASS、`zr_vm_dataflow_engine_test` 4 PASS、`zr_vm_semantic_facts_test` 9 PASS。WSL gcc/clang focused build/run 与相邻回归通过：`zr_vm_cfg_typed_catch_loop_flow_test` 6 PASS、`zr_vm_cfg_typed_catch_flow_test` 8 PASS、`zr_vm_cfg_typed_catch_branch_flow_test` 2 PASS、`zr_vm_cfg_typed_catch_switch_flow_test` 5 PASS、`zr_vm_cfg_try_catch_edges_test` 11 PASS、`zr_vm_cfg_reachability_test` 29 PASS。WSL 因新增文件触发 CMake glob 重新配置；`git diff --check` exit 0，仅输出既有 LF/CRLF warning。 |
| 阶段 1 / CFG + Dataflow + Query | 待完成 | - | catch body reachability 已覆盖显式/潜在可抛入口、catch-all 顺序消费剪枝、`catch (e)` 未类型化参数全捕获，以及已知 literal throw kind 集合、显式类型转换 throw 表达式、顺序局部声明类型绑定、同块简单赋值类型绑定、简单同名 if/else 分支赋值合并、单边分支赋值合并、简单多变量分支结果绑定、常量 if 分支简单赋值剪枝、常量 if 分支终结后同级赋值剪枝，以及 unknown 条件 while/传统 `for`/`foreach` body 简单赋值、switch case/default 简单赋值、switch matching constant selector 简单赋值、switch mixed-kind constant case/default 剪枝、switch terminating/folded-if case fallthrough 简单赋值、mixed-kind known constant `==`/`!=` 条件折叠、checked integer addition/subtraction/multiplication/division/modulo/unary plus/minus/bitwise not/nonnegative bitwise AND/OR/XOR/left-shift/right-shift 与 finite float addition/subtraction/multiplication/division/unary plus/minus 条件折叠、传统 `for` init 简单赋值、传统 `for` step 简单赋值、以及直线终结语句后的不可达赋值剪枝与进入循环前已知绑定合并到 simple typed catch 的多类型匹配与顺序消费；下一步补齐更完整 typed catch 类型/异常表达式匹配（嵌套/递归分支流、更复杂多变量循环/跨块流、用户类型、继承等）、更细函数/表达式 throws effect 标注（例如可证明不抛的调用）、更广泛常量条件折叠（数值区间、符号值、更复杂组合表达式等）、switch 复杂 selector 边界与 union 穷尽分支、CFG-backed reaching defs 的更广泛循环定点/更细 definition ranking、definite-assignment 更广泛 loop fixed point/剩余 finally 边界/局部重算、更完整 break/continue-sensitive typed catch loop exits（条件 break、break 前赋值出环、跨块循环 fixed point），以及编译器前端对 semantic query diagnostics 的二进制/外部诊断序列化。switch-local break 已审计为当前语言/编译器不支持：除非后续先在 parser/compiler 中引入 switch break label 语义，否则 CFG 不单独实现。`semantic_query.h` 已提供稳定查询面，可把不可达 reachability facts 和 definite-assignment read 状态映射为结构化诊断，并可对 read facts 消费直线/CFG-backed reaching-defs；LSP analyzer 已消费 semantic query diagnostics 作为补充诊断源；LSP definition 已消费直线 reaching-defs、首版 CFG-backed branch join reaching-defs 与首版多定义 surface 作为本地 symbol 跳转优先来源；compiler frontend 已把 semantic query diagnostics 发布到 `SZrSemanticContext.queryDiagnostics` cache，且已能通过 CFG-backed definite-assignment 在分支汇合处发布 `possibly_uninitialized_read`，能在常量真 write-before-break 循环后抑制虚假未初始化诊断，也会解析 reaching-defs 供编译后 semantic context 服务 `DefinitionOf` 查询；semantic facts 已有直线 definite-assignment 状态解析器，CFG-backed definite-assignment 已覆盖分支汇合、声明初始化器内 self-read 顺序、finally 克隆路径 read 状态合并，以及常量真循环 write-before-break 精度；dataflow 已有 definite-assignment `UNINIT` / `INIT` / `MAYBE_INIT` 状态格 helper，后续仍需接局部重算、更完整诊断来源、更广泛 loop/剩余 finally 赋值精度与更完整的 overload/member ranking。 | - |

---

## 1. 控制流图（CFG）—— 所有流分析的地基

### 1.1 现状

不可达只在表达式级（短路、常量分支），`SZrSemanticReachabilityFact` 的语句级原因（AFTER_RETURN/BREAK/CONTINUE）虽有枚举但缺真正的 CFG 支撑。

### 1.2 设计

在 `zr_vm_parser/src/zr_vm_parser/type_inference/` 下新增 `cfg.c` / `cfg.h`，对每个函数体构建基本块图：

```
SZrCfgBlock {
    TZrUInt32   id;
    SZrAstNode  **statements;   // 块内语句
    TZrUInt32   *successors;    // 后继块 id（条件分支两条边）
    TZrUInt32   *predecessors;  // 前驱块 id
    TZrUInt32    predecessorCount;
    EZrCfgEdgeKind edgeKind;    // NORMAL / TRUE_BRANCH / FALSE_BRANCH / LOOP_BACK / EXCEPTION
    TZrBool      visited;
}
SZrCfg { SZrCfgBlock *blocks; entry; exit; }
```

构建时机：在 `compile_statement.c` 完成函数体 AST 后、生成字节码前，跑一遍 `ZrParser_Cfg_Build(funcBody)`。语句到块的映射：顺序语句进同一块；`if/while/for/foreach/switch/try` 在 `compile_statement_flow.c` 已识别的控制点处切块并连边；`return/throw/break/continue` 终结当前块并连到对应目标（exit / loop header / loop exit / catch）。

- 不可达检测：从 entry 做可达性传播，`predecessorCount == 0` 且非 entry 的块 -> 整块不可达。对块内首语句发 `ZR_SEMANTIC_REACHABILITY_*` 事实 + 一条 warning 诊断（带「因为上游 return/throw/穷尽分支」的 cause）。
- 借鉴 `lua/cpython/Python/flowgraph.c` 的 `remove_unreachable`：标记访问 + 重算前驱，O(V+E)。

### 1.3 验收

- `return; x = 1;` 报 `x = 1` 不可达，cause 指向 return。
- `if (true) {A} else {B}` 报 B 不可达，cause = 常量条件。
- switch/union 穷尽后 default 不可达可识别（与 04 union 计划对齐）。

---

## 2. 数据流框架 —— 在 CFG 上跑定点迭代

新增 `dataflow.c` / `dataflow.h`，提供通用前向/后向数据流引擎（半格 + 工作队列定点迭代），借鉴 `lua/rust/compiler/rustc_mir_dataflow/src/framework/mod.rs`：

```
SZrDataflowAnalysis {
    EZrDataflowDirection direction;          // FORWARD / BACKWARD
    void (*initEntry)(SZrDataflowState*);    // 入口初值
    void (*transferStatement)(ctx, stmt, state); // 语句迁移函数
    TZrBool (*join)(dst, src);               // 半格 join，返回是否变化
}
```

引擎 `ZrParser_Dataflow_Run(cfg, analysis)`：工作队列驱动，块状态变化则把后继（前向）重新入队，直到收敛。下面三个分析都建立在它之上。

---

## 3. Definite assignment（确定赋值）+ 引用推断

### 3.1 目标

- 报「使用未初始化变量」。
- 强化引用关系：每个 `SZrSemanticReferenceFact` 都能回答「这个 READ 来自哪个 DECLARATION / 上一次 WRITE」。

### 3.2 设计

前向数据流，domain = 每个局部 symbol 的状态格 `{UNINIT, MAYBE_INIT, INIT}`：
- DECLARATION 无初值 -> UNINIT；有初值 / WRITE -> INIT。
- join：两路都 INIT -> INIT，否则 MAYBE_INIT。
- READ 一个 UNINIT/MAYBE_INIT 的 symbol -> 诊断（cause 指向声明，suggestion 给「在使用前赋值」并带 fix-it 草案，见 02 篇）。

引用推断增强：在迁移时维护「reaching definitions」，把每个 READ 的 `SZrSemanticReferenceFact` 关联到产生它的 WRITE/DECLARATION 的 symbol/location，供 LSP go-to-definition / find-references / rename 复用（03 篇）。

借鉴：Rust `MaybeUninitializedPlaces` / `EverInitializedPlaces`（`lua/rust/compiler/rustc_borrowck/src/dataflow.rs`）。

---

## 4. 所有权推断 —— 从「泛型约束检查」升级为「流式所有权追踪」

### 4.1 现状

所有权只在泛型约束层检查；`%unique/%shared/%weak/%borrow/%loan` 的运行时控制块 `SZrOwnershipControl`（`ownership.h`）成熟，但编译期没有 move/borrow 的流追踪。`TZrLifetimeRegionId` 已定义未用足。

### 4.2 设计

前向数据流，domain = 每个所有权值的状态：`{OWNED, MOVED, BORROWED(n), RELEASED}`，并按 `TZrLifetimeRegionId` 标注借用的有效区间。

迁移规则（产出 `SZrSemanticOwnershipFact`）：
- `%unique` 值被赋给别处 / 传入按值参数 -> MOVED；之后再 READ -> 报「use after move」，cause 指向 move 点。
- `%borrow` / `%loan` 产生借用，记录 region；离开 region 后使用借用 -> 报「borrow escapes / outlives source」。已有诊断 `BuildBorrowEscape` / `BuildLoanEscape` / `BuildOwnerToPlainEscape`（`diagnostic_builder.h:225-233`）可直接挂接。
- `%weak` upgrade 在可能已释放处 -> `BuildWeakUpgrade` 提示。
- 与 `%unique` 字段、`using` 确定性析构（using 计划）对齐：region 退出即视为 RELEASED。

借鉴：Rust borrowck 的 `explain_borrow` 给「为什么冲突 + 如何修」分层解释（`lua/rust/compiler/rustc_borrowck/src/diagnostics/explain_borrow.rs`）。

---

## 5. 数值推断（区间 + 溢出）

### 5.1 现状

只有字面量范围（`SZrSemanticNumericFact`）。

### 5.2 设计

前向数据流，domain = 每个数值 symbol 的区间 `[lo, hi]`（带「是否可能溢出目标类型」标记）：
- 字面量 -> 单点区间；`a + b` -> 区间相加（带目标整型上下界饱和检查）。
- 比较/分支细化：`if (x < 10)` 在 true 分支把 x 区间收窄到 `[lo, 9]`。
- 越界索引、明显溢出、恒真/恒假比较（喂给 Logical fact，驱动常量分支不可达）-> 诊断。

范围适度即可（区间格而非完整 SMT），目标是覆盖「数组常量越界」「无符号下溢」「恒真条件」这类高频问题。借鉴 javac 在 AST 节点上挂精确区间/位置的做法（`lua/jdk/.../javac/util/JCDiagnostic.java`）。

---

## 6. 逻辑语义推断

在数值/引用基础上产出 `SZrSemanticLogicalFact`：
- 恒真/恒假条件（`if (x != x)`、`if (1)`）-> 提示 + 驱动 CFG 常量分支不可达。
- 冗余条件 / 不可能同时成立的 `&&`（区间不相交）。
- 短路右侧恒不求值（已有 SHORT_CIRCUIT，纳入统一框架）。

---

## 7. 统一语义查询 API（被所有前端复用）

新增 `semantic_query.h`，把上述事实包成稳定查询面（供 LSP 03 / debug REPL 04）：

```
ZrParser_SemanticQuery_TypeAt(ctx, pos) -> InferredType
ZrParser_SemanticQuery_ReferencesOf(ctx, symbolId) -> Reference[]
ZrParser_SemanticQuery_DefinitionOf(ctx, pos) -> location
ZrParser_SemanticQuery_FactsAt(ctx, pos) -> {numeric, logical, ownership, reachability}
ZrParser_SemanticQuery_Diagnostics(ctx, scope) -> StructuredDiagnostic[]
```

要求：每个查询都接受一个「分析范围」（整模块 / 单函数 / 单作用域），并在内部对应「局部跑分析」（03 篇增量），失败返回空/Unknown 不抛错。

---

## 8. 落地顺序（本篇内部）

1. CFG 构建 + 不可达（先把语句级不可达做对，收益最直接）。
2. 通用 dataflow 引擎。
3. definite assignment + reaching defs（顺带强化引用，LSP 立即受益）。
4. 所有权流追踪（接已有 escape 诊断）。
5. 数值区间 + 逻辑事实。
6. `semantic_query.h` 收口。

每步都要：补 `tests/parser/` 单测 + `tests/language_server/` 对应查询测试，且保证「分析失败 -> 产出 Unknown，不崩」。
