---
doc_type: plan-detail
related_code:
  - zr_vm_parser/src/zr_vm_parser/type_inference
  - zr_vm_parser/include/zr_vm_parser/diagnostic_builder.h
  - zr_vm_language_server/src/zr_vm_language_server/interface/lsp_interface_position.c
  - zr_vm_lib_debug/src/zr_vm_lib_debug
  - zr_vm_cli/src/zr_vm_cli/repl/repl.c
  - tests/CMakeLists.txt
---

# 05 · 实施蓝图

把 01-04 的设计落成有依赖顺序、可独立验收的阶段。每阶段都「能单独合入、能单独测」。

---

## 1. 模块依赖图

```
semantic_query.h (01 §7)  <-- 所有前端的统一入口
        ^
        |  produces facts
  +-----+-----------------------------+
  | CFG(01 §1) -> dataflow(01 §2)      |
  |   -> definite-assign/refs(01 §3)   |
  |   -> ownership(01 §4)              |
  |   -> numeric/logical(01 §5,6)      |
  +-----------------------------------+
        |                 |                |
        v                 v                v
 diagnostics(02)     LSP(03)          debug+REPL(04)
```

- 02/03/04 都消费 01 的 `semantic_query.h`；02 的诊断扩展是 03/04 错误展示的前提。
- 03 的「UTF-16 行列修复」**不依赖** 01，可最先独立交付。

---

## 2. 分阶段计划

### 阶段 0：独立速赢（无依赖，最先做）
- **03 §1**：UTF-16 位置编解码层，替换 `lsp_interface_position.c` 全部转换；扩 `test_lsp_position_mapping.c`。
- **03 §2** 部分：越界防护（content[index] 边界检查）。
- 交付物：行列 bug 修复 + 不崩防护。可单独合入。

### 阶段 1：语义地基
- **01 §1** CFG 构建 + 语句级不可达。
- **01 §2** 通用 dataflow 引擎。
- **01 §7** `semantic_query.h` 骨架（先包现有推断，后续填新事实）。
- 交付物：不可达代码诊断 + 统一查询面雏形。

### 阶段 2：流分析铺开
- **01 §3** definite assignment + reaching defs（强化引用）。
- **01 §4** 所有权流追踪（接已有 escape 诊断）。
- **01 §5/§6** 数值区间 + 逻辑事实。
- **02 §1/§2** 诊断结构扩展（related/fixes/descriptorId）+ 关联位置。
- 交付物：未初始化/use-after-move/越界/恒真条件诊断，带「哪里-为什么-怎么改」。

### 阶段 3：前端体验
- **03 §3** 局部语义推断 + 声明级增量解析。
- **03 §4** hover/completion/semanticTokens 质量。
- **02 §3/§4** 类型不匹配具体化 + 诊断码注册表 + 本地化槽。
- 交付物：大文件流畅、诊断可一键修复、IDE 富 hover。

### 阶段 4：调试与交互
- **04 §1** debug 求值统一到 `semantic_query.h` + 条件断点设置期校验。
- **04 §2** REPL 常驻全局态（先过渡、再增量声明注入）。
- **04 §1.5** DAP 适配层（中期）。
- 交付物：调试器/REPL 与编辑器能力一致、REPL 真交互。

---

## 3. 新增/改动文件清单（预估）

新增：
- `zr_vm_parser/src/zr_vm_parser/type_inference/cfg.{c,h}`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow.{c,h}`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_definite_assign.c`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_ownership.c`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_numeric.c`
- `zr_vm_parser/include/zr_vm_parser/semantic_query.h` + 实现
- `zr_vm_parser/include/zr_vm_parser/diagnostic_registry.h` + 实现
- `zr_vm_language_server/src/zr_vm_language_server/interface/lsp_position_codec.{c,h}`

改动：
- `diagnostic_builder.h/.c`：扩展结构 + 新 builder。
- `compiler_diagnostics.c`：替换模板化类型错误。
- `lsp_interface_position.c`：转调 codec。
- `lsp_interface.c` / `semantic_analyzer*.c`：诊断序列化补 related/fix；查询接 `semantic_query.h`。
- `incremental_parser.c`：声明级增量。
- `debug_eval.c` / `debug_semantic_facts.c`：接统一推断。
- `repl.c`：常驻全局态。
- `tests/CMakeLists.txt` 等：注册新测试。

---

## 4. 测试策略

- **parser 单测**（`tests/parser/`）：CFG 不可达、definite-assign、所有权流、数值区间，每个分析独立用例 + 「分析失败产出 Unknown 不崩」用例。
- **LSP 测试**（`tests/language_server/`）：position 往返/边界（扩 `test_lsp_position_mapping.c`）、局部推断、诊断 related/fix 字段、hover/completion 内容。
- **debug 测试**（`tests/debug/`）：统一求值后的类型/诊断、条件断点设置期校验、watchpoint。
- **REPL 测试**：跨提交引用变量与运行期值、失败回滚。
- **fuzz**：position codec + 增量编辑，断言不崩 + 往返幂等。
- **回归**：现有验收（`tests/acceptance/`）与 reference fixtures 全绿。

---

## 5. 验收标准（DoD）

1. 含中文/emoji 的 `.zr` 文件，hover/definition/diagnostic 位置精确（UTF-16）。
2. `return` 后代码、常量分支、穷尽 union 后 default 报不可达，带 cause。
3. 未初始化使用、use-after-move、借用逃逸均报错，带 related 位置 + 修复建议。
4. 类型不匹配诊断给出「期望 vs 实得 + 原因 + 可行 cast」。
5. 任意畸形/超大输入下 LSP 不崩；推断失败降级为 Unknown。
6. 大文件编辑单函数时延迟显著低于全量。
7. debug 条件断点/表达式求值的类型与诊断和编辑器一致。
8. REPL 中先定义变量、后续表达式可引用其运行期值。

---

## 6. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| REPL 常驻全局态需编译器支持「追加声明」，改动深 | 先做「运行期值快照重放」过渡，验证收益后再上增量注入 |
| dataflow 在病态输入上发散/慢 | 迭代次数与节点数上限 + 超限降级部分结果 |
| 诊断结构扩展破坏现有序列化 | 字段只增不改，旧路径默认空 related/fix |
| `location.h` column 单位若为字节，反向映射也需改 | 阶段 0 先核对 column 语义并统一到 codec |
| DAP/AOT 调试范围大 | 列为中后期，先保证 interp/binary 路径 |

---

## 状态与产出记录

| 阶段 | 状态 | 完成时间 | 完成项目 | 验证 |
| --- | --- | --- | --- | --- |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 17:08 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线补齐 deeper chained focused targets 的 zero-only no-op 边界：新增 chained zero-only parser/LSP case，确认现有 no-op replay path 在 `scale=0..0` 包住 accepted `mask=1..1`、`gate=1..1` chained product 时把 whole scale product/coefficient 保持为精确 `0..0`，不需要新增生产 guard。该 coverage confirmation 只覆盖 `scale=0..0`、`mask=1..1`、`gate=1..1` 的 chained exact no-op shape，不声明 zero-only residual expansion、arbitrary zero-product algebra 或 arbitrary deeper products。 | Discovery/GREEN：新增 parser chained zero-only case 与 LSP query 在现有实现下直接通过；WSL gcc focused deeper parser 28 PASS、LSP deeper PASS；WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:53 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线补齐 deeper chained focused targets 的 sign-crossing 边界：新增 chained sign-crossing parser/LSP case，确认现有 bounded scale-product classifier 已能处理 `scale=-1..1` 包住 accepted `mask=1..1`、`gate=1..1` chained product，不需要新增生产 guard。该 coverage confirmation 只覆盖 `scale=-1..1`、`mask=1..1`、`gate=1..1` 的 one-additional chained sign-crossing residual shape，不声明 zero-only chained replay、arbitrary deeper products 或 broader coefficient algebra。 | Discovery/GREEN：新增 parser chained sign-crossing case 与 LSP query 在现有实现下直接通过；WSL gcc focused deeper parser 27 PASS、LSP deeper PASS；WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 16:35 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线补齐 six-additional focused targets 的 zero-only no-op 边界：新增 zero-only sixth-wrapper parser/LSP case，确认现有 no-op replay path 在 `crest=0..0` 包住 accepted nonzero-possible five-additional product 时把 whole scale product/coefficient 保持为精确 `0..0`，不需要新增生产 guard。该 coverage confirmation 只覆盖 `crest=0..0`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 exact no-op shape，不声明 zero-only residual expansion、arbitrary zero-product algebra 或 further arbitrary depth。 | Discovery/GREEN：新增 parser zero-only case 与 LSP query 在现有实现下直接通过；WSL gcc focused six-additional parser 7 PASS、LSP six-additional PASS；WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:19 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 six-additional focused targets：新增 sign-crossing sixth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 sixth-wrapper guard 从 nonzero-non-crossing 放宽到 nonzero-possible range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_extra_deeper_extension`，只覆盖 `crest=-1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only sixth wrapper 或 further arbitrary depth。 | RED：WSL gcc six-additional parser 前五个 cases 仍 PASS，新增 sign-crossing case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 6、LSP six-additional PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 16:07 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 six-additional focused targets：新增 negative non-singleton sixth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 sixth-wrapper guard 从 positive-possible-or-zero-inclusive-negative 放宽到 nonzero-non-crossing range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_extra_extra_deeper_extension`，只覆盖 `crest=-2..-1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only/sign-crossing sixth wrappers 或 further arbitrary depth。 | RED：WSL gcc six-additional parser positive singleton、positive non-singleton、zero-inclusive positive 与 zero-inclusive negative 仍 PASS，新增 negative non-singleton case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 5、LSP six-additional PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:55 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 six-additional focused targets：新增 zero-inclusive negative sixth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 sixth-wrapper guard 从 positive-possible 放宽到 positive-possible-or-zero-inclusive-negative range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_extra_extra_deeper_extension`，只覆盖 `crest=-1..0`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only/strict-negative/sign-crossing sixth wrappers 或 further arbitrary depth。 | RED：WSL gcc six-additional parser positive singleton、positive non-singleton 与 zero-inclusive positive 仍 PASS，新增 zero-inclusive negative case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc focused/self-dependency adjacent 矩阵通过；focused six-additional parser 4、LSP six-additional PASS，WSL gcc self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。WSL clang 被无关 AOT `backend_aot_c_scalar_locals.c` signature/argument mismatch 阻断，不声明 GREEN。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:43 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 six-additional focused targets：新增 zero-inclusive positive sixth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 sixth-wrapper guard 从 strict-positive 放宽到 positive-possible range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_extra_extra_deeper_extension`，只覆盖 `crest=0..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only/zero-inclusive negative/negative/sign-crossing sixth wrappers 或 further arbitrary depth。 | RED：WSL gcc six-additional parser positive singleton 与 positive non-singleton 仍 PASS，新增 zero-inclusive positive case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 3、LSP six-additional PASS，WSL gcc/clang self-dependency 相关 26 executables PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 executables PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:22 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 six-additional focused targets：新增 positive non-singleton sixth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 sixth-wrapper guard 从 positive singleton 放宽到 strict-positive range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_extra_extra_deeper_extension`，只覆盖 `crest=2..3`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only/zero-inclusive/negative/sign-crossing sixth wrappers 或 further arbitrary depth。 | RED：WSL gcc six-additional parser positive singleton 仍 PASS，新增 positive non-singleton case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 2、LSP six-additional PASS，WSL gcc/clang self-dependency 相关 26 targets PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Six-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 15:08 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进 arbitrary-depth 前沿：新增 six-additional parser/LSP focused targets，并把 symbolic coefficient classifier 增加一条 positive singleton sixth-wrapper guard。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_extra_extra_deeper_extension`，只覆盖 `crest=1..1`、`shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible five-additional product 外层，不声明 zero-only/zero-inclusive/negative/sign-crossing sixth wrappers 或 further arbitrary depth。 | RED：WSL gcc six-additional parser 新 case 失败 `Expected 5 Was -9223372036854775808`，LSP six-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused six-additional parser 1、LSP six-additional PASS，WSL gcc/clang self-dependency 相关 26 targets PASS。Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 23 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Only Scale-Product Exact No-Op Replay | 已完成 | 2026-06-28 14:50 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线补齐 five-additional focused targets 的 zero-only fifth-wrapper no-op coverage。新增 parser/LSP case 使用 `shell=0..0` 包住已接受的 nonzero-possible four-additional product；现有 no-op replay 直接把 `0..0` coefficient 写入视为保持 target，因此 `narrowed + 0` 精确保留为 `5..5`，无需生产代码变更。此切片只确认 tested zero-only fifth wrapper exact no-op，不声明 zero-only residual expansion、arbitrary zero-product algebra 或 further arbitrary depth。 | Discovery/GREEN：新增 zero-only fixtures 在现有实现下直接通过；WSL gcc focused five-additional parser 7 PASS，LSP five-additional query PASS；WSL gcc/clang self-dependency 相关 24 targets PASS；Windows MSVC Debug `build-msvc-lsp-selfdep` focused build/run PASS，self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:36 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 five-additional focused targets 中新增 sign-crossing fifth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-extra-deeper guard 从 nonzero-non-crossing 放宽到 nonzero-possible range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_possible_extra_extra_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_extra_deeper_extension`，只覆盖 `shell=-1..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible four-additional product 外层，不声明 zero-only fifth wrapper 或 further arbitrary depth。 | RED：WSL gcc five-additional parser 前五个 cases 仍 PASS，新增 sign-crossing case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc/clang focused/self-dependency adjacent 矩阵通过；focused five-additional parser 6、LSP five-additional PASS，WSL gcc/clang self-dependency 相关 24 targets PASS。Windows MSVC Debug 在 `build-msvc` focused 与实际存在的 18-target self-dependency 矩阵通过；`build-msvc-lsp-selfdep` normal focused build 被无关 AOT `backend_aot_c_function_body.c` C2198/C2143 阻断。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:17 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 five-additional focused targets 中新增 negative non-singleton fifth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-extra-deeper guard 从 positive-possible-or-zero-inclusive-negative 放宽到 nonzero-non-crossing range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_non_crossing_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_extra_deeper_extension`，只覆盖 `shell=-2..-1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible four-additional product 外层，不声明 zero-only/sign-crossing fifth wrappers 或 further arbitrary depth。 | RED：WSL gcc five-additional parser positive singleton、positive non-singleton、zero-inclusive positive 与 zero-inclusive negative cases 仍 PASS，新增 negative non-singleton case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 5、LSP five-additional PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 14:01 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 five-additional focused targets 中新增 zero-inclusive negative fifth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-extra-deeper guard 从 positive-possible 放宽到 positive-possible-or-zero-inclusive-negative range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_or_zero_inclusive_negative_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_extra_deeper_extension`，只覆盖 `shell=-1..0`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible four-additional product 外层，不声明 zero-only/strict-negative/sign-crossing fifth wrappers 或 further arbitrary depth。 | RED：WSL gcc five-additional parser positive singleton、positive non-singleton 与 zero-inclusive positive cases 仍 PASS，新增 zero-inclusive negative case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 4、LSP five-additional PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:43 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 five-additional focused targets 中新增 zero-inclusive positive fifth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-extra-deeper guard 从 strict positive 放宽到 positive-possible range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_extra_deeper_extension`，只覆盖 `shell=0..1`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible four-additional product 外层，不声明 zero-only/zero-inclusive negative/negative/sign-crossing fifth wrappers 或 further arbitrary depth。 | RED：WSL gcc five-additional parser positive singleton 与 positive non-singleton cases 仍 PASS，新增 zero-inclusive positive case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/self-dependency adjacent 矩阵通过；focused five-additional parser 3、LSP five-additional PASS，WSL gcc/clang self-dependency 相关 24 targets PASS，Windows MSVC Debug self-dependency 相关 21 targets PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:23 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 five-additional focused targets 中新增 positive non-singleton fifth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-extra-deeper guard 从 positive singleton 放宽到 strict positive range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_extra_deeper_extension`，只覆盖 `shell=2..3`、`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible four-additional product 外层，不声明 zero-only/zero-inclusive/negative/sign-crossing fifth wrappers 或 further arbitrary depth。 | RED：WSL gcc five-additional parser positive singleton case 仍 PASS，新增 positive non-singleton case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 6、five-additional parser 2、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four/five LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Five-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 13:05 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：新增 `zr_vm_numeric_selfdep_coeff_cross_five_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_five_additional_scale_query_test`，覆盖 positive singleton fifth wrapper `shell=1..1` 包住 accepted nonzero-possible four-additional product（`scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1`）。symbolic coefficient classifier 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_extra_extra_deeper_extension`，只在新 fifth wrapper 为 positive singleton 且内层满足既有 nonzero-possible extra-deeper guard 时放开，不声明 zero-only/zero-inclusive/negative/sign-crossing fifth wrappers 或 further arbitrary depth。 | RED：WSL gcc focused five-additional parser case 失败 `Expected 5 Was -9223372036854775808`，LSP five-additional query 返回 full int64。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug focused/adjacent 矩阵通过，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 6、five-additional parser 1、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four/five LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 04:06 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 four-additional focused targets 中新增 sign-crossing fourth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-deeper guard 扩为 nonzero-possible。最终 guard 为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_possible_extra_deeper_extension`，复用 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_possible_scale_extension_identifier`，只覆盖 `scale=-1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible three-additional product 外层，不声明 zero-only fourth wrapper 或 general arbitrary depth。 | RED：WSL gcc normal focused build 仍被无关 AOT typed-bool/direct-call 签名不一致影响，随后用构建系统生成命令手动重编/重链接 focused parser/LSP targets；four-additional parser 新 sign-crossing case 失败 `Expected 5 Was -9223372036854775808`，LSP four-additional query 返回 full int64。GREEN：WSL gcc/clang 使用构建系统生成命令手动更新 parser shared library 与 four-additional focused targets 后，相邻矩阵通过；Windows MSVC Debug normal focused build 与相邻矩阵通过。覆盖 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 6、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。MSVC build 仍输出无关 AOT value-semir `fprintf` 格式警告；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 03:45 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 four-additional focused targets 中新增 strict negative fourth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-deeper guard 从 positive-possible-or-zero-inclusive-negative 扩为 nonzero-non-crossing。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_nonzero_non_crossing_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_nonzero_non_crossing_extra_deeper_extension`，只覆盖 `scale=-2..-1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible three-additional product 外层，不声明 zero-only、sign-crossing fourth wrappers 或 general arbitrary depth。 | RED：WSL gcc normal build 先被无关 AOT typed-bool/direct-call 签名不一致阻断，随后用已构建库手动重编/重链接 focused parser/LSP targets；four-additional parser 新 case 失败 `Expected 5 Was -9223372036854775808`，LSP four-additional query 返回 full int64。GREEN：WSL gcc/clang 使用构建系统生成命令手动更新 parser shared library 与 four-additional focused targets 后，相邻矩阵通过；Windows MSVC Debug normal focused build 与相邻矩阵通过。覆盖 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 5、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 03:17 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 four-additional focused targets 中新增 zero-inclusive negative fourth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-deeper guard 从 positive-possible 扩为 positive-possible-or-zero-inclusive-negative。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_or_zero_inclusive_negative_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_or_zero_inclusive_negative_extra_deeper_extension`，只覆盖 `scale=-1..0`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible three-additional product 外层，不声明 zero-only、strict-negative、sign-crossing fourth wrappers 或 general arbitrary depth。 | RED：WSL gcc four-additional parser 新 case 先失败 `Expected 5 Was -9223372036854775808`，LSP four-additional query 返回 full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug 通过 focused/adjacent 矩阵，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 4、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 02:47 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 four-additional focused targets 中新增 zero-inclusive positive fourth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-deeper guard 从 strict positive 扩为 positive-possible。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_extra_deeper_extension`，只覆盖 `scale=0..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible three-additional product 外层，不声明 zero-only、zero-inclusive negative/non-positive fourth wrappers 或 general arbitrary depth。 | RED：WSL gcc four-additional parser 新 case 先失败 `Expected 5 Was -9223372036854775808`，LSP four-additional query 通过已生成对象和现有库重链接后返回 full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug 通过 focused/adjacent 矩阵，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 3、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 02:18 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：在现有 four-additional focused targets 中新增 strict-positive non-singleton fourth-wrapper parser/LSP case，并把 symbolic coefficient classifier 的 extra-deeper guard 从 positive singleton 放宽到 strict positive range。实现新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_scale_identifier`，并将 guard 收敛为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_extra_deeper_extension`，只覆盖 `scale=2..3`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 accepted nonzero-possible three-additional product 外层，不声明 zero-only、zero-inclusive/non-positive fourth wrappers 或 general arbitrary depth。 | RED：WSL gcc four-additional parser 新 case 先失败 `Expected 5 Was -9223372036854775808`，LSP four-additional query 返回 full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug 通过 focused/adjacent 矩阵，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 2、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。WSL clang/MSVC 只出现既有 project/AOT warnings；full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Four-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 01:43 +08:00 | 按实施蓝图阶段 2 的“数值区间 + 语义事实 + LSP 查询”验收线继续推进：先拆分 parser three-additional coefficient-cross target，新增 `zr_vm_numeric_selfdep_coeff_cross_three_additional_scale_test`；随后补 three-additional zero-only exact no-op parser/LSP 回归；最后新增 `zr_vm_numeric_selfdep_coeff_cross_four_additional_scale_test` 与 `zr_vm_lsp_selfdep_coeff_cross_four_additional_scale_query_test`，并在 symbolic coefficient classifier 中只放开 positive singleton fourth wrapper over accepted nonzero-possible three-additional product。该阶段只覆盖 `scale=1..1`、`outer/span/cover/gate=1..1`、`mask=-1..1` 的 four-additional residual replay，不声明 zero-only/non-positive fourth wrappers 或 general arbitrary depth。 | Split baseline、Discovery、RED、GREEN 均按阶段闭环：WSL gcc parser split baseline 26 PASS + 6 PASS；zero-only third-wrapper no-op 在生产变更前 parser 7 PASS/LSP PASS；four-additional RED 为 parser `Expected 5 Was -9223372036854775808`、LSP full int64；GREEN 在 WSL gcc、WSL clang、Windows MSVC Debug 通过 focused/adjacent 矩阵，含 parser symbolic 16、parser coefficient 16、base parser coefficient-cross 8、deeper parser 26、three-additional parser 7、four-additional parser 1、parser product 9、LSP symbolic 17、LSP coefficient 15、base/deeper/three/four LSP coefficient-cross、LSP product 9。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-28 01:08 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 将 third-wrapper guard 改为 nonzero-possible deeper extension，允许 sign-crossing range outer wrapper 包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper，也不开放 zero-only third wrapper 或 arbitrary depth。新增 parser/LSP three-additional target 中的 sign-crossing case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-1..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 sign-crossing third wrapper over an accepted nonzero-possible deeper product；zero-only third wrappers、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 three-additional sign-crossing parser/LSP cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 32 PASS、three-additional LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 32 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 10:37 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 将 third-wrapper guard 改为 nonzero-non-crossing deeper extension，允许 strict negative range outer wrapper 包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper，也不开放 zero-only/sign-crossing third wrapper。新增 parser/LSP three-additional target 中的 negative non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-2..-1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-2..2]`、tail `+(step + step)`，target `5..INT64_MAX`。该切片只声明这一 strict negative third wrapper over an accepted nonzero-possible deeper product；zero-only/sign-crossing third wrappers、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 three-additional negative non-singleton parser/LSP cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 31 PASS、three-additional LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 31 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 10:12 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual，并先按测试体量风险拆出 three-additional LSP query 目标：`test_lsp_numeric_loop_self_dependency_symbolic_coeff_cross_deeper_scale_semantic_query.c` 保留一般 deeper scale-product cases，新的 `test_lsp_numeric_loop_self_dependency_symbolic_coeff_cross_three_additional_scale_semantic_query.c` / `zr_vm_lsp_selfdep_coeff_cross_three_additional_scale_query_test` 专门覆盖 third-wrapper cases，两个目标共用 `lsp_numeric_range_query_test_support.c/.h`。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_zero_inclusive_possible_deeper_scale_extension_identifier`，并将 deeper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_zero_inclusive_possible_deeper_extension`，让 zero-inclusive negative range outer wrapper 可以包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper，也不开放 zero-only/strict-negative/sign-crossing third wrapper。新增 parser/LSP three-additional target 中的 zero-inclusive negative case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-1..0`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 zero-inclusive negative third wrapper over an accepted nonzero-possible deeper product；zero-only/strict-negative/sign-crossing third wrappers、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Split baseline：WSL gcc 旧 deep-scale LSP 目标 PASS，新 three-additional LSP 目标 PASS。RED：WSL gcc focused 新 three-additional zero-inclusive negative parser/LSP cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 30 PASS、three-additional LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 30 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper/three-additional LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 09:32 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_possible_scale_extension_identifier`，并将 deeper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_possible_deeper_extension`，让 zero-inclusive positive range outer wrapper 可以包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper，也不开放 zero-only/negative/sign-crossing third wrapper。新增 parser/LSP deeper split target 中的 three-additional zero-inclusive positive case，覆盖 `step=1..3`、`factor=-1..1`、`scale=0..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 zero-inclusive positive third wrapper over an accepted nonzero-possible deeper product；zero-only/non-positive third wrappers、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 three-additional zero-inclusive positive parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 29 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 29 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 09:16 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_positive_scale_extension_identifier`，并将 deeper helper 改为 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_deeper_extension`，让 strict positive range outer wrapper 可以包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper。新增 parser/LSP deeper split target 中的 three-additional positive non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=2..3`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-3..3]`、tail `+(step + step + step)`，target `5..INT64_MAX`。该切片只声明这一 strict positive third wrapper over an accepted nonzero-possible deeper product；zero-inclusive/non-positive third wrappers、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 three-additional positive non-singleton parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 28 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 28 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Three-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:49 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_positive_singleton_deeper_extension`，让 positive singleton outer wrapper 可以包住 already accepted nonzero-possible two-additional product；该路径只组合既有 residual collector 与 bounded coefficient guard，不改 delta terms helper。新增 parser/LSP deeper split target 中的 three-additional positive singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=1..1`、`outer=1..1`、`span=1..1`、`mask=-1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。同时新增 two-additional zero-only scale-product exact no-op parser/LSP regression，验证现有 no-op 路径保持 target `5..5` 而不声明 zero-only residual expansion。该切片只声明这一 positive singleton third wrapper over an accepted nonzero-possible deeper product；arbitrary deeper/non-positive third wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Discovery：two-additional zero-only no-op parser/LSP case 在生产代码变更前已通过。RED：WSL gcc focused 新 three-additional parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 27 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 27 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:25 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 把 two-additional-level helper 从 nonzero-possible non-crossing second-level wrapper 扩为 nonzero-possible second-level wrapper，改名为 `nonzero_possible_scale_extension_identifier`，只要求 `max >= min` 且 `min != 0 || max != 0`，避免开放 zero-only second-level wrapper，也不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level sign-crossing case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-1..1`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。同时将 LSP deeper target 的 shared assignment-range query setup 抽到 `lsp_numeric_range_query_test_support.c/.h` 并接入 `tests/CMakeLists.txt`，使主 LSP deeper 文件从 970 行回落到阈值以下；该抽取不改变语义断言。residual collector 与 delta terms helper 不需要新职责。该切片只声明这一 bounded two-additional-level sign-crossing scale-product extension；zero-only、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Baseline：WSL gcc focused parser deeper target 24 PASS、LSP deeper target PASS；support 抽取后 LSP deeper target PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 25 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 25 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 08:01 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 把 two-additional-level helper 从 positive-possible-or-zero-inclusive-negative second-level wrapper 扩为 nonzero-possible non-crossing second-level wrapper，改名为 `nonzero_possible_non_crossing_scale_extension_identifier` 窄判定，只接受 `min > 0`、`min == 0 && max > 0`、`min < 0 && max == 0` 或 `max < 0`，避免开放 zero-only/sign-crossing second-level wrapper，也不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level negative non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-2..-1`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-2..2]`、tail `+(step + step)`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded two-additional-level negative non-singleton scale-product extension；zero-only、arbitrary deeper products、sign-crossing second-level wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Baseline：WSL gcc focused parser deeper target 23 PASS、LSP deeper target PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 24 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 24 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:45 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 把 two-additional-level helper 从 positive-possible second-level wrapper 扩为 positive-possible-or-zero-inclusive-negative second-level wrapper，改名为 `positive_possible_or_zero_inclusive_negative_scale_extension_identifier` 窄判定，只接受 `min > 0`、`min == 0 && max > 0` 或 `min < 0 && max == 0`，避免复用会开放 negative non-singleton/sign-crossing 的宽 helper，也不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level zero-inclusive negative case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-1..0`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded two-additional-level zero-inclusive negative scale-product extension；zero-only、arbitrary deeper products、negative non-singleton/sign-crossing second-level wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Baseline：WSL gcc focused parser deeper target 22 PASS、LSP deeper target PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 23 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 23 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:25 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 把 two-additional-level helper 从 positive-only second-level wrapper 扩为 positive-possible second-level wrapper，新增/改名 `positive_possible_scale_extension_identifier` 窄判定，只接受 `min > 0` 或 `min == 0 && max > 0`，避免复用会开放 zero-inclusive negative/negative/sign-crossing 的宽 helper，也不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level zero-inclusive positive case，覆盖 `step=1..3`、`factor=-1..1`、`scale=0..1`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded two-additional-level zero-inclusive positive scale-product extension；zero-only、arbitrary deeper products、zero-inclusive negative/negative/sign-crossing second-level wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Baseline：WSL gcc focused parser deeper target 21 PASS、LSP deeper target PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 22 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 22 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 07:08 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 把 two-additional-level helper 从 positive singleton second-level wrapper 扩为 positive range second-level wrapper，新增 `positive_scale_extension_identifier` 窄判定，避免复用会开放 zero-inclusive/negative/sign-crossing 的宽 helper，也不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level positive non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=2..3`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-3..3]`、tail `+(step + step + step)`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded two-additional-level positive non-singleton scale-product extension；zero-only、arbitrary deeper products、zero-inclusive/negative/sign-crossing second-level wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Baseline：WSL gcc focused parser deeper target 20 PASS、LSP deeper target PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 21 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 21 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Two-Additional-Level Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:51 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 新增 narrow helper，只让 positive singleton outer wrapper 包住 already accepted bounded one-level scale-product classifier，不把现有 extension depth 直接放宽到 arbitrary depth。新增 parser/LSP deeper split target 中的 two-additional-level positive singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=1..1`、`outer=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded two-additional-level positive-singleton scale-product extension；zero-only、arbitrary deeper products、non-positive/zero-inclusive/negative/sign-crossing second-level wrappers、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 20 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 20 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:26 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 bounded one-level extension classifier 现在接受 strict positive、zero-inclusive positive、zero-inclusive negative、strict negative identifier outer factor，使 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_extension(..., 1)` 可额外接受 zero-inclusive negative identifier 包裹 already-supported scale product。新增 parser/LSP deeper split target 中的 chained zero-inclusive negative case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded one-additional-level zero-inclusive negative scale-product extension；zero-only、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 19 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 19 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 06:06 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：先在 `tests/parser/test_numeric_loop_self_dependency_symbolic_coeff_cross_deeper_scale_dataflow.c` 抽出 direct/chained scale-product fixture helper，把 parser deeper target 从 964 行降到 543 行，再新增 chained zero-inclusive positive outer factor case。`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 bounded one-level extension classifier 现在接受 strict positive、zero-inclusive positive、strict negative identifier outer factor，使 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_extension(..., 1)` 可额外接受 zero-inclusive positive identifier 包裹 already-supported scale product；同时删除被替代的私有 strict-positive helper，避免新增 unused warning。新增 parser/LSP deeper split target 中的 chained zero-inclusive positive case，覆盖 `step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded one-additional-level zero-inclusive positive scale-product extension；zero-only、zero-inclusive negative outer extension、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | Refactor baseline：WSL gcc focused parser deeper target 17 PASS、LSP deeper target PASS；支撑抽取后 parser deeper target 17 PASS。RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 18 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 18 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:41 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中把 bounded one-level extension wrapper 从 positive-only outer factor 扩展为 supported signed identifier outer factor，并新增/复用 supported scale extension helper，使 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_extension(..., 1)` 可额外接受 negative non-singleton identifier 包裹 already-supported scale product。新增 parser/LSP deeper split target 中的 chained negative non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=1..1`、`gate=1..1`、coefficient `[-2..2]`、tail `+(step + step)`，target `5..INT64_MAX`。parser deeper target 同步抽出 chained scale-product fixture helper，避免在 near-threshold 测试文件继续复制整套 setup；residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded one-additional-level negative non-singleton scale-product extension；zero-only、zero-inclusive outer extensions、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 17 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 17 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:25 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中新增 positive scale identifier helper，并把 bounded one-level extension wrapper 从 positive singleton outer factor 扩展为 positive identifier range outer factor，使 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_extension(..., 1)` 可额外接受 positive non-singleton identifier 包裹 already-supported scale product。新增 parser/LSP deeper split target 中的 chained positive non-singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=2..3`、`mask=1..1`、`gate=1..1`、coefficient `[-3..3]`、tail `+(step + step + step)`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded one-additional-level positive non-singleton scale-product extension；zero-only、zero-inclusive/nonpositive outer extensions beyond the accepted negative non-singleton fixture、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 16 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 16 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Chained Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 05:07 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中把 direct positive bounded scale-product guard 拆名，并新增 `loop_assignment_sequence_symbolic_coefficient_is_bounded_scale_product_with_extension(..., 1)` wrapper，使现有 guard 可额外接受一个 positive singleton identifier 包裹 already-supported scale product 的 one-level extension。新增 parser/LSP deeper split target 中的 chained positive singleton case，覆盖 `step=1..3`、`factor=-1..1`、`scale=1..1`、`mask=1..1`、`gate=1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。residual collector、delta terms helper 与 CMake target 清单不需要新职责。该切片只声明这一 bounded one-additional-level positive-singleton scale-product extension；zero-only、arbitrary deeper products、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 15 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 15 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Positive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:46 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中为 positive bounded scale-product guard 增加 mixed zero-inclusive positive/negative scale-product 分支，允许 `crossing_identifier * (zero_inclusive_positive_scale * zero_inclusive_negative_mask)` 或反向顺序进入现有 range-coefficient residual path；`type_inference_loop_assignment_sequence_symbolic_delta.c` 与 delta terms helper 继续负责 residual 收集、exact-term cancellation 与 term-budget 回收。新增 parser/LSP deeper split target 中的 mixed zero-inclusive positive/negative case，覆盖 `scale=0..1`、`mask=-1..0`、scale product `[-1..0]`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 bounded one-extra-level two-factor mixed scale-product fixture；zero-only、broader sign-crossing products、deeper scale products beyond this fixture、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 14 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 14 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Negative Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:29 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中为 positive bounded scale-product guard 增加 mixed zero-inclusive negative plus strict sign-crossing scale-product 分支，允许 `crossing_identifier * (zero_inclusive_negative_scale * strict_sign_crossing_mask)` 或反向顺序进入现有 range-coefficient residual path；`type_inference_loop_assignment_sequence_symbolic_delta.c` 与 delta terms helper 继续负责 residual 收集、exact-term cancellation 与 term-budget 回收。新增 parser/LSP deeper split target 中的 mixed zero-inclusive negative/sign-crossing case，覆盖 `scale=-1..0`、`mask=-1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 bounded one-extra-level two-factor mixed scale-product fixture；zero-only、mixed zero-inclusive positive/negative products、broader sign-crossing products、deeper scale products beyond this fixture、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 13 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 13 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Mixed Zero-Inclusive Positive Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 04:14 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中为 positive bounded scale-product guard 增加 mixed zero-inclusive positive plus strict sign-crossing scale-product 分支，允许 `crossing_identifier * (zero_inclusive_positive_scale * strict_sign_crossing_mask)` 或反向顺序进入现有 range-coefficient residual path；`type_inference_loop_assignment_sequence_symbolic_delta.c` 与 delta terms helper 继续负责 residual 收集、exact-term cancellation 与 term-budget 回收。新增 parser/LSP deeper split target 中的 mixed zero-inclusive positive/sign-crossing case，覆盖 `scale=0..1`、`mask=-1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 bounded one-extra-level two-factor mixed scale-product fixture；zero-only、mixed zero-inclusive negative/sign-crossing products、mixed zero-inclusive positive/negative products、broader sign-crossing products、deeper scale products beyond this fixture、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang focused build/run 均通过新 deeper targets（parser 12 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 12 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。Windows MSVC Debug focused build 被当前工作树中无关 AOT `backend_aot_c_zrp_metadata_prune.c` 参数数量不匹配 C2198 阻塞，未到达本切片测试目标；Windows GREEN 与 full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 03:56 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中为 positive bounded scale-product guard 增加 double strict sign-crossing scale-product 分支，允许 `crossing_identifier * (strict_sign_crossing_scale * strict_sign_crossing_mask)` 或反向顺序进入现有 range-coefficient residual path；`type_inference_loop_assignment_sequence_symbolic_delta.c` 与 delta terms helper 继续负责 residual 收集、exact-term cancellation 与 term-budget 回收。新增 parser/LSP deeper split target 中的 double sign-crossing case，覆盖 `scale=-1..1`、`mask=-1..1`、coefficient `[-1..1]`、tail `+step`，target `5..INT64_MAX`。该切片只声明这一 bounded one-extra-level two-factor strict sign-crossing scale-product fixture；zero-only、mixed zero-inclusive/sign-crossing product combinations、broader sign-crossing products、deeper scale products beyond this fixture、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 11 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 11 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-27 03:33 +08:00 | 继续按阶段 2 的数值区间/逻辑事实实施顺序推进 coefficient-cross residual：在 `type_inference_loop_assignment_sequence_symbolic_coefficient.c` 中把 positive bounded scale-product guard 扩展到 both factors strictly negative 的 same-sign scale product，不再要求其中一个 negative singleton；在新增 `type_inference_loop_assignment_sequence_symbolic_delta_terms.c/.h` 中集中处理 symbolic residual exact-term cancellation 与 term-budget 回收，`type_inference_loop_assignment_sequence_symbolic_delta.c` 在加减子树收集完后调用该 helper，避免四个 lower-bound exact term 与后续四个 `step` tail 尚未抵消前触发 8-term cap。新增 parser/LSP deeper split target 中的 double negative non-singleton case，覆盖 `scale=-2..-1`、`mask=-2..-1`、product `[1..4]`、coefficient `[-4..4]`、tail `+(step + step + step + step)`，target `5..INT64_MAX`。该切片只声明这一 bounded one-extra-level fixture；strict sign-crossing scale products without a positive singleton factor、zero-only/mixed zero-inclusive product combinations、deeper scale products beyond this fixture、nested term products、target-dependent coefficients、broader algebra、fixed point 和 CFG-wide dataflow 仍留后续阶段/切片。 | RED：WSL gcc focused 新 parser/LSP deeper cases 先分别失败为 target lower bound `Expected 5 Was -9223372036854775808` 与 LSP full int64。GREEN：WSL gcc、WSL clang、Windows MSVC Debug focused build/run 均通过新 deeper targets（parser 10 PASS、LSP PASS）和相邻矩阵：parser symbolic 16 PASS、parser coefficient 16 PASS、base parser coefficient-cross 8 PASS、deeper parser coefficient-cross 10 PASS、parser product 9 PASS、LSP symbolic 17 PASS/checks、LSP coefficient 15 PASS/checks、base/deeper LSP coefficient-cross PASS、LSP product 9 PASS/checks。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Coefficient-Cross Test Split | 已完成 | 2026-06-26 09:31 +08:00 | 按阶段 2 blueprint 的“测试可独立验收、文件不过度堆叠”要求拆分 coefficient-cross 回归目标：`tests/parser/test_numeric_loop_self_dependency_symbolic_coefficient_cross_dataflow.c` 现在只保留基础 one-layer coefficient-cross residual 8 PASS；新 `tests/parser/test_numeric_loop_self_dependency_symbolic_coeff_cross_deeper_scale_dataflow.c` 注册为 `zr_vm_numeric_selfdep_coeff_cross_deeper_scale_test`，承载 9 个 deeper scale-product residual 用例。LSP 同步拆为基础 `test_lsp_numeric_loop_self_dependency_symbolic_coefficient_cross_semantic_query.c` 与新 `test_lsp_numeric_loop_self_dependency_symbolic_coeff_cross_deeper_scale_semantic_query.c` / `zr_vm_lsp_selfdep_coeff_cross_deeper_scale_query_test`。`tests/CMakeLists.txt` 已接入新 parser target、LSP support target、language pipeline 与 language_server 聚合清单。该切片不改 production semantic inference。 | Baseline：拆分前 WSL gcc/clang 旧 parser coefficient-cross 17 PASS 与 LSP coefficient-cross 17 PASS/checks 通过。GREEN：WSL gcc、WSL clang 与 Windows MSVC Debug 重新配置/构建/运行四个目标通过：旧 parser 8 PASS、new parser deeper scale 9 PASS、旧 LSP PASS、new LSP deeper scale PASS。full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Negative Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 09:09 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper double negative singleton scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是 same-sign negative scale product when one factor is a negative singleton，因此可接受 `crossing_identifier * (negative_scale * negative_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 double negative non-singleton scale products without singleton factor、strict sign-crossing scale products without a positive singleton factor、zero-only/mixed zero-inclusive products 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=-1..-1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-2`、`mask = -1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double negative scale product containing a negative singleton factor inside the residual term budget；不声明 double negative non-singleton scale products without singleton factor、strict sign-crossing scale products without a positive singleton factor、zero-only/mixed zero-inclusive product combinations、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative product combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper double negative singleton scale-product coefficient-cross residual target case 在旧 double zero-inclusive guard 下经 WSL gcc touched-object/manual relink 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper double negative singleton scale-product coefficient-cross target query 先失败为 full int64。普通 WSL gcc target build 与 `build-msvc-lsp-selfdep` MSVC build 均被既有 AOT `backend_aot_c_zrp_metadata_prune.c` 参数不匹配阻塞。GREEN：WSL gcc/clang touched-object/manual relink focused run 与 Windows MSVC `build-msvc` manual relink focused run 通过，三套均通过 parser coefficient-cross 17 PASS、LSP coefficient-cross 17 PASS/checks；WSL gcc/clang 与 Windows MSVC manual relink 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑/不声明，full repository GREEN 不声明。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:51 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper double zero-inclusive negative scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 double zero-inclusive negative scale product，因此可接受 `crossing_identifier * (zero_inclusive_negative_scale * zero_inclusive_negative_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale products、mixed-sign zero-inclusive products、negative non-singleton / strict sign-crossing scale products without a positive singleton factor 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=-1..0` 的 `+(step * (factor * (scale * mask))); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-1`、`mask = (seed % 2)-1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double zero-inclusive negative scale product inside the residual term budget；不声明 zero-only scale products、negative non-singleton / strict sign-crossing scale products without a positive singleton factor、mixed-sign zero-inclusive product combinations、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding double zero-inclusive product combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper double zero-inclusive negative scale-product coefficient-cross residual target case 在旧 double zero-inclusive positive-only scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper double zero-inclusive negative scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 16 PASS、LSP coefficient-cross 16 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片已确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Double Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:36 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper double zero-inclusive positive scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 double zero-inclusive positive scale product，因此可接受 `crossing_identifier * (zero_inclusive_positive_scale * zero_inclusive_positive_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale products、double zero-inclusive negative / negative non-singleton / strict sign-crossing scale products without a positive singleton factor 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=0..1` 的 `+(step * (factor * (scale * mask))); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = seed % 2`、`mask = seed % 2` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with double zero-inclusive positive scale product inside the residual term budget；不声明 zero-only scale products、double zero-inclusive negative / negative non-singleton / strict sign-crossing scale products without a positive singleton factor、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding double zero-inclusive product combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper double zero-inclusive positive scale-product coefficient-cross residual target case 在旧 positive-singleton-or-one-zero-inclusive scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper double zero-inclusive positive scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 15 PASS、LSP coefficient-cross 15 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Sign-Crossing Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:22 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper sign-crossing scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 strict sign-crossing scale product，且该 product 仍保留 positive singleton factor，因此可接受 `crossing_identifier * (strict_sign_crossing_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale products、scale products without a positive singleton factor 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-1..1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 3)-1`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with strict sign-crossing scale product sharing a positive singleton factor inside the residual term budget；不声明 zero-only scale products、scale products without a positive singleton factor、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding sign-crossing scale/product combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper sign-crossing scale-product coefficient-cross residual target case 在旧 same-sign/zero-bounded scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper sign-crossing scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 14 PASS、LSP coefficient-cross 14 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Negative Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 08:13 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper negative non-singleton scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 same-sign negative scale product，且该 product 仍保留 positive singleton factor，因此可接受 `crossing_identifier * (negative_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale products、strict sign-crossing scale products 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-2..-1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-2`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with same-sign negative scale product sharing a positive singleton factor inside the residual term budget；不声明 zero-only scale products、strict sign-crossing scale products、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative scale/product combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper negative non-singleton scale-product coefficient-cross residual target case 在旧 zero-bounded-only scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper negative non-singleton scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 13 PASS、LSP coefficient-cross 13 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为前序切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Zero-Inclusive Negative Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:58 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper zero-inclusive negative scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 zero-inclusive negative scale product，且该 product 仍保留 positive singleton factor，因此可接受 `crossing_identifier * (zero_inclusive_negative_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale products、negative-only/sign-crossing scale products beyond this zero-inclusive negative fixture 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-1..0`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)-1`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with zero-inclusive negative scale product sharing a positive singleton factor inside the residual term budget；不声明 zero-only scale products、negative-only/sign-crossing scale products beyond this zero-inclusive negative fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper zero-inclusive negative scale-product coefficient-cross residual target case 在旧 zero-inclusive positive-only scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper zero-inclusive negative scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 12 PASS、LSP coefficient-cross 12 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。product adjacency 未重跑，因为上一切片刚确认 WSL gcc product 相邻目标仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Zero-Inclusive Positive Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:42 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper zero-inclusive positive scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 zero-inclusive positive scale product，且该 product 仍保留 positive singleton factor，因此可接受 `crossing_identifier * (zero_inclusive_positive_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale product、negative-only/sign-crossing scale products beyond the zero-inclusive negative fixture 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=0..1`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = seed % 2`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with zero-inclusive positive scale product sharing a positive singleton factor inside the residual term budget；不声明 zero-only scale products、negative-only/sign-crossing scale products beyond the zero-inclusive negative fixture、deeper scale products beyond this one-extra-level fixture、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper zero-inclusive positive scale-product coefficient-cross residual target case 在旧 positive scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper zero-inclusive positive scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 11 PASS、LSP coefficient-cross 11 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。WSL gcc product 相邻目标构建仍被既有 AOT 链接阻塞，`libzr_vm_parser.so` 缺少 `backend_aot_c_zrp_*` 符号；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Positive Non-Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 07:14 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper positive non-singleton scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-extra-level coefficient product guard 现在允许 scale side 是一个 positive scale product，且该 product 至少一侧仍为 positive singleton，因此可接受 `crossing_identifier * (positive_range_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝更深 scale product、double-non-singleton scale product、zero/negative/sign-crossing scale product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=2..3`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = (seed % 2)+2`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with positive non-singleton scale product sharing a positive singleton factor inside the residual term budget；不声明 deeper scale products、double-non-singleton scale products、zero/negative/sign-crossing scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper positive non-singleton scale-product coefficient-cross residual target case 在旧 positive singleton scale-product guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper positive non-singleton scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 与 Windows MSVC Debug focused build/run 通过，三套均通过 parser coefficient-cross 10 PASS、LSP coefficient-cross 10 PASS/checks；WSL gcc/clang 与 Windows MSVC Debug 的相邻 no-product 组通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks。WSL gcc 全相邻矩阵包含 product 目标时构建被既有 AOT `backend_aot_c_zrp_metadata_prune.c` 参数不匹配阻塞；本切片不声明 product adjacency 或全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Deeper Positive Singleton Scale-Product Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:46 +08:00 | 按阶段 2 loop recurrence blueprint 补 deeper positive singleton scale-product 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是一个由两个 positive singleton identifier 组成的 one-extra-level product，因此可接受 `crossing_identifier * (positive_singleton_scale * positive_singleton_mask)` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝更深 scale product、non-singleton/zero/negative/sign-crossing scale product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=2..2`、`mask=1..1` 的 `+(step * (factor * (scale * mask))); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`scale = 2`、`mask = 1` 覆盖同一 target range。该切片只声明 one-extra-level nested multiplicative sign-crossing identifier coefficient with positive singleton scale product inside the residual term budget；不声明 deeper scale products、non-singleton/zero/negative/sign-crossing scale products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser deeper positive singleton scale-product coefficient-cross residual target case 在旧 guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP deeper positive singleton scale-product coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 9 PASS、LSP coefficient-cross 9 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Zero-Inclusive Negative Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:23 +08:00 | 按阶段 2 loop recurrence blueprint 补 zero-inclusive negative scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是 lower bound 为负且 upper bound 为 0 的 identifier range，因此可接受 `crossing_identifier * zero_inclusive_negative_identifier_range` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale、deeper product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-1..0` 的 `+(step * (factor * scale)); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`(seed % 2)-1` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with zero-inclusive negative identifier scale inside the residual term budget；不声明 zero-only scale、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser zero-inclusive negative scale coefficient-cross residual target case 在旧 guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP zero-inclusive negative scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 8 PASS、LSP coefficient-cross 8 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Zero-Inclusive Positive Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 06:02 +08:00 | 按阶段 2 loop recurrence blueprint 补 zero-inclusive positive scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是 lower bound 为 0 且 upper bound 为正的 identifier range，因此可接受 `crossing_identifier * zero_inclusive_positive_identifier_range` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only scale、one-sided zero-inclusive negative scale、deeper product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=0..1` 的 `+(step * (factor * scale)); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`seed % 2` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with zero-inclusive positive identifier scale inside the residual term budget；不声明 zero-only scale、one-sided zero-inclusive negative scale、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser zero-inclusive positive scale coefficient-cross residual target case 在旧 guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP zero-inclusive positive scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 7 PASS、LSP coefficient-cross 7 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Sign-Crossing Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:44 +08:00 | 按阶段 2 loop recurrence blueprint 补 sign-crossing scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是 same-sign nonzero identifier range 或 strictly sign-crossing identifier range，因此可接受 `crossing_identifier * sign_crossing_identifier_range` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 zero-only/one-sided zero-inclusive scale、deeper product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-1..1` 的 `+(step * (factor * scale)); reader; +step`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`(seed % 3)-1` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with strict sign-crossing identifier scale inside the residual term budget；不声明 zero-only/one-sided zero-inclusive scale factors、deeper nested products、nested term product、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding coefficient combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser sign-crossing scale coefficient-cross residual target case 在旧 guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP sign-crossing scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 6 PASS、LSP coefficient-cross 6 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Non-Singleton Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:29 +08:00 | 按阶段 2 loop recurrence blueprint 补 negative non-singleton scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是 same-sign nonzero identifier range，因此可接受 `crossing_identifier * negative_identifier_range` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 deeper product、zero/sign-crossing scale 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..1`、`scale=-2..-1` 的 `+(step * (factor * scale)); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 3)-1`、`(seed % 2)-2` 覆盖同一 target range。先尝试的 `factor=-1..3, scale=-2..-1` 版本因 `[-6..2]` coefficient 要求 6 个 exact terms 加 residual，且 tail delta 在 cancellation 前超过 8 项 budget，已按 bounded residual collector 约束缩为 `[-2..2]`。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with negative non-singleton identifier scale inside the residual term budget；不声明 zero/sign-crossing scale factors、deeper nested products、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、budget-expanding negative scale combinations、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser negative non-singleton scale coefficient-cross residual target case 在旧 negative-singleton guard 下先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP negative non-singleton scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 5 PASS、LSP coefficient-cross 5 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 05:01 +08:00 | 按阶段 2 loop recurrence blueprint 补 negative singleton scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 scale side 是 positive identifier range 或 negative singleton identifier，因此可接受 `crossing_identifier * negative_singleton_identifier` 或反向顺序；`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 deeper product、negative non-singleton scale、zero/sign-crossing scale 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..3`、`scale=-1..-1` 的 `+(step * (factor * scale)); reader; +(step + step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 5)-1`、`scale = -1` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with negative singleton identifier scale；不声明 negative non-singleton scale、zero/sign-crossing scale factors、deeper nested products、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser negative scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP negative scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 4 PASS、LSP coefficient-cross 4 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Non-Singleton Scale Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 04:31 +08:00 | 按阶段 2 loop recurrence blueprint 补 non-singleton positive scale 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_coefficient.c` 的 one-layer coefficient product guard 现在允许 coefficient 是 `crossing_identifier * positive_identifier_range` 或反向顺序，`type_inference_loop_assignment_sequence_symbolic_delta.c` 继续负责 residual term 收集，同时继续要求 term 是 simple nonnegative identifier，且拒绝 deeper product、zero/negative/sign-crossing scale 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..3`、`scale=2..3` 的 `+(step * (factor * scale)); reader; +(step + step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 5)-1`、`(seed % 2)+2` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with positive identifier range scale；不声明 deeper nested products、zero/negative/sign-crossing scale factors、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser non-singleton scale coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP non-singleton scale coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 3 PASS、LSP coefficient-cross 3 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Nested Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 04:14 +08:00 | 按阶段 2 loop recurrence blueprint 补 nested multiplicative coefficient 的跨零 residual 实现边界：`type_inference_loop_assignment_sequence_symbolic_delta.c` 在 crossing coefficient guard 中新增 one-layer product 识别，允许 coefficient 是 `crossing_identifier * positive_singleton_identifier` 或反向顺序，同时继续要求 term 是 simple nonnegative identifier，且拒绝 deeper product 与 nested term product。Parser coefficient-cross fixture 新增 `step=1..3`、`factor=-1..3`、`scale=2..2` 的 `+(step * (factor * scale)); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1`、`(seed % 5)-1`、`scale = 2` 覆盖同一 target range。该切片只声明 one-layer nested multiplicative sign-crossing identifier coefficient with positive singleton identifier scale；不声明 deeper nested products、non-singleton scale factors、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser nested coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP nested coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 2 PASS、LSP coefficient-cross 2 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Sign-Crossing Range-Coefficient Residual Replay | 已完成 | 2026-06-26 03:54 +08:00 | 按阶段 2 loop recurrence blueprint 补 range coefficient residual 的跨零窄边界：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 bounded symbolic residual collector 现在会在 simple identifier term / simple identifier coefficient product 上，将 non-target sign-crossing coefficient range `[min..max]` 与非负 identifier term 拆成负下界 exact signed terms 加 `[0..max-min] * term` 非负 residual；`type_inference_loop_assignment_sequence.c` 只在 symbolic range 相对 raw range 更窄或把 sign-crossing 改善为 one-sided/zero 时采用 range-coefficient result，纯 exact-term range 仍保留直接覆盖能力。Parser coefficient-cross fixture 覆盖 `step=1..3`、`factor=-1..3` 的 `+(step * factor); reader; +(step + step)`，target `5..INT64_MAX`；LSP coefficient-cross fixture 通过 `(seed % 3)+1` 与 `(seed % 5)-1` 覆盖同一 target range。跨零 expansion 限制在 simple identifier term/coefficient，避免抢走 nested product exact-cancel path。该切片只声明 non-target sign-crossing identifier coefficient residual widening with nonnegative identifier term；不声明 nested multiplicative coefficient decomposition、sign-crossing base term、target-dependent coefficients、exact residue/range-set coefficient analysis、broader multiplication/coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser coefficient-cross residual target case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP coefficient-cross target query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient-cross 1 PASS、LSP coefficient-cross 1 PASS/check、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser product 9 PASS、LSP product 9 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Subtractive Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 03:10 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 checked integer-literal subtractive factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后通过 product-local checked subtraction folding 把 integer-literal-only `-` factor 规约成 literal product value，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+(step * (factor * (5 - 2))); reader; -((3 * factor) * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `-1..11`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `-1..11`。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal subtractive factor folding；不声明 arbitrary subtraction algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、subtraction overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser subtractive constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 9 PASS、LSP product 9 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Additive Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:52 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 checked integer-literal additive factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后通过 product-local checked addition folding 把 integer-literal-only `+` factor 规约成 literal product value，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+(step * (factor * (1 + 2))); reader; -((3 * factor) * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `-1..11`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `-1..11`。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal additive factor folding；不声明 arbitrary addition algebra、subtractive factor folding、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、addition overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser additive constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 8 PASS、LSP product 8 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Modulo Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:35 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 checked integer-literal modulo factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后通过 product-local checked modulo folding 把 integer-literal-only `%` factor 规约成 literal product value，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+(step * (factor * (7 % 4))); reader; -((3 * factor) * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `-1..11`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `-1..11`。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal modulo factor folding；不声明 arbitrary modulo algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、modulo-by-zero/overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser modulo constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 7 PASS、LSP product 7 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Divided Constant Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:19 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 checked integer-literal division factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后通过 product-local checked division folding 把 integer-literal-only `/` factor 规约成 literal product value，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+(step * (factor * (6 / 2))); reader; -((3 * factor) * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `-1..11`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `-1..11`。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal division factor folding；不声明 arbitrary division algebra、nested multiplicative factor expressions beyond supported `*` flattening、distribution、general constant folding、coefficient solving、division-by-zero/overflow folding、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser divided constant product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 6 PASS、LSP product 6 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Double-Negative Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 02:01 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 bounded nested unary sign-chain 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后通过 bounded unary sign normalization 把一元 `+`/`-` 链规约成 literal sign product 与底层 term，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+((-(-step)) * factor); reader; -(factor * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `0..7`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `0..7`。该切片只声明 bounded supported binary `*` product-term equality 内的一元正负号链归一；不声明 arbitrary unary operators、general sign algebra、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser double-negative product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 5 PASS、LSP product 5 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Positive Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 01:46 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 unary-positive factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后把 unary `+term` 归一为底层 term，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+((+step) * factor); reader; -(factor * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `0..7`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `0..7`。该切片只声明 bounded supported binary `*` product-term equality 内的一元正号因子归一；不声明 nested unary normalization、general sign algebra、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser unary-positive product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 4 PASS、LSP product 4 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Negative Product Factor Exact-Cancel Replay | 已完成 | 2026-06-26 01:33 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 unary-negative factor 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 product-term flattening 后把 unary `-term` 归一为 checked literal product factor `-1` 和底层 term，再进入 checked int64 literal-product folding 与 non-literal multiset matching；`type_inference_loop_assignment_delta_expression.c` 仍只负责 recursive equality orchestration。这样 `+((-step) * factor); reader; -((-1 * factor) * step)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `0..7`；LSP product fixture 覆盖 `(seed % 3)-1` 与 `(seed % 5)-2`，同样 target `5..5`、observer `0..7`。该切片只声明 bounded supported binary `*` product-term equality 内的一元负号因子归一；不声明 general sign algebra、double-negation normalization、unary-plus normalization、distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser unary-negative product-factor exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 3 PASS、LSP product 3 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Constant Product Exact-Cancel Replay | 已完成 | 2026-06-26 01:10 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 checked literal-product 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 承担 product-term flattening、checked int64 literal-product folding 和 non-literal multiset matching，`type_inference_loop_assignment_delta_expression.c` 只保留 recursive equality orchestration。这样 `+(step * (factor * (2 * 3))); reader; -((6 * step) * factor)` 可作为同一 supported product delta 精确抵消，不需要 range coefficient collector 接受 sign-crossing operands。Parser product fixture 覆盖 `step=-1..1`、`factor=-1..1`，target `5..5`、observer `-1..11`；LSP product fixture 覆盖两个 `(seed % 3)-1` sign-crossing factors，同样 target `5..5`、observer `-1..11`。该切片只声明 bounded supported binary `*` product-term equality 内的 checked integer-literal product folding；不声明 distribution、general constant folding、coefficient solving、overflowed literal products、float/non-integer product folding、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser folded constant-product exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 2 PASS、LSP product 2 PASS/checks、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Associative-Commutative Product Exact-Cancel Replay | 已完成 | 2026-06-26 00:51 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的 bounded associative/commutative 实现边界：`type_inference_loop_assignment_delta_product.c/.h` 在 exact structural match 之后、additive term fallback 之前，为 recursive delta expression equality 提供 supported binary `*` product-term flattening 和 multiset matching。这样 sign-crossing nested product 不需要被 range coefficient collector 接受，也能让 `+(step * (factor * scale)); reader; -((scale * step) * factor)` 作为同一 supported product delta 精确抵消。Parser product fixture 覆盖 `step=-1..1`、`factor=-2..2`、`scale=-1..1`，target `5..5`、observer `0..7`；LSP product fixture 覆盖 `(seed % 3)-1`、`(seed % 5)-2`、`(seed % 3)-1`，同样 target `5..5`、observer `0..7`。该切片只声明 bounded supported binary `*` product-term multiset equality in exact delta comparison；不声明 distribution、constant folding、coefficient solving、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 product parser/LSP targets 后，LSP target 初始缺 common language-server include registration 并失败于 `semantic/lsp_local_semantic_query.h`；修正 CMake registration 后，parser product exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`，LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser product 1 PASS、LSP product 1 PASS/check、parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Sign-Crossing Product Exact-Cancel Replay | 已完成 | 2026-06-26 00:28 +08:00 | 按阶段 2 loop recurrence blueprint 补 product expression equality 的窄实现边界：`type_inference_loop_assignment_delta_expression.c` 的 recursive delta expression equality 在 exact structural match 之后、additive term fallback 之前，对 binary `*` 增加 swapped-operand equality。这样 sign-crossing product 不需要被 range coefficient collector 接受，也能让 `+(step * factor); reader; -(factor * step)` 作为同一 supported product delta 精确抵消。Parser coefficient fixture 覆盖 `step=-1..1`、`factor=-2..2`，target `5..5`、observer `0..7`；LSP coefficient fixture 覆盖 `step = (seed % 3) - 1`、`factor = (seed % 5) - 2`，同样 target `5..5`、observer `0..7`。该切片只声明 supported binary `*` swapped-operand equality in exact delta comparison；不声明 arbitrary multiplication algebra、distribution、coefficient solving、sign-crossing coefficient residual widening、target-dependent coefficients、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser sign-crossing product exact-cancel case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 16 PASS、LSP coefficient 15 PASS/checks、parser symbolic 16 PASS、LSP symbolic 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Range-Coefficient Residual Exact-Cancellation Replay | 已完成 | 2026-06-26 00:11 +08:00 | 按阶段 2 loop recurrence blueprint 补 range residual cancellation 的实现边界：`SZrTypeInferenceLoopAssignmentSequenceSymbolicTerm` 增加 `coefficientNode`，range-coefficient product collector 在留下 `[0..max-min] * term` residual 时保存被选作 coefficient side 的 AST。`loop_assignment_sequence_symbolic_delta_terms_can_cancel` 只在两个 residual 的 base term 等价、coefficient range 相同、coefficient expression 等价且符号相反时取消 residual term，避免不同 coefficient 但相同数值范围的误抵消。Parser/LSP coefficient fixtures 覆盖 `+(step * factor); reader; -(factor * step)`，parser target `5..5`、observer `0..8`，LSP target `5..5`、observer `-32763..32517`。该切片只声明 same coefficient expression range residual exact cancellation；不声明 sign-crossing coefficient、target-dependent coefficient、exact residue/range-set analysis、arbitrary coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。附带新增 replay-resolved net-positive coverage，不改变生产路径。 | RED：parser exact-cancel residual case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP exact-cancel target/observer query 先失败为 full int64。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过；三套均通过 parser coefficient 15 PASS、LSP coefficient 14 PASS/checks、parser self-dependency 17 PASS、LSP self-dependency 17 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Positive Range-Coefficient Residual Replay | 已完成 | 2026-06-25 23:36 +08:00 | 按阶段 2 loop recurrence blueprint 补 product coefficient slot 的 commuted non-singleton range residual 路径：`type_inference_loop_assignment_sequence_symbolic_delta.c` 新增 range coefficient normalization helper，并让 range-coefficient product collector 同时评估左右 operand。两侧都可作为 non-target same-sign range coefficient 时，collector 选择 normalized lower bound 更大的 candidate 作为 coefficient side；正向 `factor=2..4` / `step=1..3` 的 `factor * step` 因此会产生两个 exact `step` terms 和 `[0..2] * step` residual，后续 `-(step + step)` 抵消 exact terms 后提交 upward recurrence。Parser 与 LSP coefficient fixtures 覆盖 `+(factor * step); reader; -(step + step)`，target 报告 `5..INT64_MAX`。该切片只声明 commuted product coefficient candidate selection for same-sign non-target range coefficients；不声明 sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary coefficient algebra、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser commuted positive range-coefficient residual case 先失败为 target lower bound `Expected 5 Was -9223372036854775808`；LSP case 在 `step = seed + 1`、`factor = seed + 2`、`factor * step` 下失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过。三套均通过 parser coefficient 14 PASS、新 LSP coefficient 13 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Range-Coefficient Residual Replay | 已完成 | 2026-06-24 21:11 +08:00 | 按阶段 2 loop recurrence blueprint 补 product coefficient slot 的 same-sign negative range residual 路径：`type_inference_loop_assignment_sequence_symbolic_delta.c` 的 range-coefficient collector 现在接受全负 coefficient range，拒绝跨零 sign-crossing range；全负 range 会翻转 residual sign 并用绝对值 range 进入已有 lower-bound expansion。`SequenceSymbolicDeltaCurrentRange` 继续通过 `type_inference_loop_assignment_symbolic_math.c/.h` 的 checked range multiplication 投影 residual term。Parser 与 LSP coefficient fixtures 覆盖 `+(step * factor); reader; +(step + step)` 的 negative residual，target 报告 `INT64_MIN..5`。该切片只声明 non-target、same-sign negative range coefficient residual replay；不声明 sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、correlated coefficient/term solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser negative range-coefficient residual case 先失败为 target upper bound `Expected 5 Was 9223372036854775807`；LSP case 在 `step = seed + 1`、`factor = -2 - seed` 下失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过。三套均通过 parser coefficient 13 PASS、新 LSP coefficient 12 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Positive Range-Coefficient Residual Replay | 已完成 | 2026-06-24 20:58 +08:00 | 按阶段 2 loop recurrence blueprint 补 product coefficient slot 的 non-singleton range residual 路径：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual term 现在可携带 `hasCoefficientRange/coefficientMin/coefficientMax`，collector 对不读取 sequence target 且 range inference 证明为非负 int64 range 的 coefficient expression 执行 lower-bound expansion，并为 `[0..max-min] * term` 留下 range residual。`SequenceSymbolicDeltaCurrentRange` 用 checked range multiplication 投影 residual term；ordinary exact terms 仍走原 structural cancellation。新增私有 `type_inference_loop_assignment_symbolic_math.c/.h` 承载 checked int64 scalar/range arithmetic，让 symbolic delta 文件从 1015 行降回 782 行。Parser 与 LSP coefficient fixtures 覆盖 `+(step * factor); reader; -(step + step)` 的 positive residual，target 报告 `5..INT64_MAX`。该切片只声明 non-target、nonnegative range coefficient residual replay；不声明 negative/sign-crossing coefficient、target-dependent coefficient、exact residue/range-set coefficient analysis、arbitrary multiplication/coefficient algebra、correlated coefficient/term solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser positive range-coefficient residual case 先失败为 target `Expected 5 Was -9223372036854775808`；LSP case 在 `step = seed + 1`、`factor = seed + 2` 下失败为 full int64 `min=-9223372036854775808 max=9223372036854775807`。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过。三套均通过 parser coefficient 12 PASS、新 LSP coefficient 11 PASS/checks；新增 source 触发 CMake glob reconfigure 和 focused relink；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Coefficient Nested-If Loop-Write Invalidation Guard | 已完成 | 2026-06-24 20:14 +08:00 | 按阶段 2 loop recurrence blueprint 补 nested-if coefficient write invalidation：`type_inference_loop_assignment_sequence.c/.h` 新增 exported future dependency write scan，并通过 loop assignment plan 的 target 名称与 nested-if assignment-name helpers 识别后续 nested if/else 是否写入当前 accumulated delta RHS 的 coefficient dependency。`type_inference_loop_assignment_join.c` 允许这种 future nested-if write 进入 replay guard 路径，并让尾部 same-target stable delta 从当前保守 binding replay，而不是退回 ordinary full-int64 RHS inference 后放弃 plan。Parser fixture 与 LSP coefficient fixture 覆盖 `+(step * factor); if (inner) { factor = 3; } else { factor = 4; }; reader; -(step + step)`，证明旧的 pre-loop `factor=2` net-zero 精度不再发布，target/observer 均 full int64。该切片只声明 ordinary assignment 与 plan-visible nested-if write invalidation；不声明 broader/chained invalidation beyond ordinary and nested-if writes、通用 coefficient solver、target-dependent/non-singleton/exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser nested-if invalidated singleton-coefficient case 先失败为 expected full int64 target `Expected -9223372036854775808 Was 5`，LSP invalidated case 失败为 target `5..5`、observer `0..0`。GREEN：WSL gcc/clang focused build/run 通过，Windows MSVC Debug focused build/run 通过。三套均通过 parser coefficient 11 PASS、新 LSP coefficient 10 PASS/checks；WSL/Clang 与 MSVC rebuild 期间触发无关 AOT/library 重编和既有 warnings；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Coefficient Loop-Write Invalidation Guard | 已完成 | 2026-06-24 15:24 +08:00 | 按阶段 2 loop recurrence blueprint 补 coefficient replay 的 soundness guard：`type_inference_loop_assignment_sequence.c` 现在在同一 sequence 中检查 ordinary assignment 是否写入已经累计的 same-target delta RHS dependency，或在下一个 same-target delta 前写入当前 delta RHS dependency；命中时关闭 symbolic cancellation 并提交 sign-crossing conservative replay。`type_inference_loop_assignment_join.c` 允许这种 future-written dependency 走 replay guard 路径，而不是在 plan validate 阶段直接拒绝。Parser 与 LSP fixture 覆盖 `+(step * factor); factor = 3; reader; -(step + step)`，证明旧的 pre-loop `factor=2` net-zero 精度不再发布，target/observer 均 full int64。该切片只声明 ordinary loop-write invalidation guard；不声明通用 coefficient solver、target-dependent/non-singleton/exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：parser invalidated singleton-coefficient case 先失败为 expected full int64 target `Expected -9223372036854775808 Was 5`，LSP invalidated case 失败为 target `5..5`、observer `0..0`。GREEN：WSL gcc/clang static focused touched object rebuild + CMake-generated relink 通过，Windows MSVC static focused Ninja build/run 通过。三套均通过 parser coefficient 10 PASS、新 LSP coefficient 9 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、loop assignment 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks、LSP branch-assignment 25 PASS/checks；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-Xor Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:51 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 bitwise literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `^` expression，沿用 CFG 常量求值的 checked int64 bitwise-xor guard，再把 `step * (1 ^ 3)` / `(1 ^ 3) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (1 ^ 3)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal bitwise-xor coefficient folding；不声明通用乘法 algebra、任意 coefficient solver、target-dependent coefficient、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser bitwise-xor literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang static focused 以 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；MSVC static focused 用 Ninja build/run 通过。三套均通过 parser coefficient 9 PASS、新 LSP coefficient 8 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-Or Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:39 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 bitwise literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `|` expression，沿用 CFG 常量求值的 checked int64 bitwise-or guard，再把 `step * (1 | 2)` / `(1 | 2) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (1 | 2)); reader; -(step + step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal bitwise-or coefficient folding；不声明 bitwise-xor folding、通用乘法 algebra、任意 coefficient solver、target-dependent coefficient、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser bitwise-or literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang static focused 以 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；MSVC static focused 用 Ninja build/run 通过。三套均通过 parser coefficient 8 PASS、新 LSP coefficient 7 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Bitwise-And Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:28 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 bitwise literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `&` expression，沿用 CFG 常量求值的 checked int64 bitwise-and guard，再把 `step * (3 & 2)` / `(3 & 2) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (3 & 2)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal bitwise-and coefficient folding；不声明 bitwise-or/xor folding、通用乘法 algebra、任意 coefficient solver、target-dependent coefficient、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser bitwise-and literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang static focused 以 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；MSVC static focused 用 Ninja build/run 通过。三套均通过 parser coefficient 7 PASS、新 LSP coefficient 6 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Right-Shift Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 14:11 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 shifted literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `>>` expression，沿用 CFG 常量求值的 checked int64 right-shift guard，再把 `step * (4 >> 1)` / `(4 >> 1) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (4 >> 1)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal right-shift coefficient folding；不声明其他 bitwise folding、通用乘法 algebra、任意 coefficient solver、target-dependent coefficient、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser right-shifted literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang static focused 以 touched object rebuild + CMake-generated relink 隔离无关 active AOT dirty WSL rebuild blocker；MSVC static focused 用 Ninja build/run 通过。三套均通过 parser coefficient 6 PASS、新 LSP coefficient 5 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Left-Shift Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 13:39 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 shifted literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `<<` expression，沿用 CFG 常量求值的 checked int64 left-shift guard，再把 `step * (1 << 1)` / `(1 << 1) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (1 << 1)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal left-shift coefficient folding；不声明 right-shift/bitwise folding、通用乘法 algebra、任意 coefficient solver、target-dependent coefficient、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static focused 新 parser shifted literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC static focused 普通 build/run 通过。三套均通过 parser coefficient 5 PASS、新 LSP coefficient 4 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Singleton-Range Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 13:19 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 non-literal singleton coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 里先复用 checked literal coefficient folding；若失败，则调用现有表达式区间推断，只接受不读取 target 且结果为 int64 singleton range 的 coefficient expression，再把该单点 signed coefficient 展开为 residual terms。Parser 与 LSP fixture 覆盖 `factor=2` 的 `+(step * factor); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded non-target singleton-range coefficient folding；不声明非单点 coefficient、target-dependent coefficient、通用乘法 algebra、任意 coefficient solver、exact residue/range-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc static-isolated 新 parser singleton coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc static focused 在无关 AOT value-semir 脏改阻断普通 archive 重建后用当前 sequence/symbolic-delta 对象重打包并重链 focused binaries 通过；WSL clang/MSVC static focused 普通 build/run 通过。三套均通过 parser coefficient 4 PASS、新 LSP coefficient 3 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Modulo Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 12:36 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 modulo literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `%` expression，沿用受检 int64 取模的除零和 `INT64_MIN % -1` guard，再把 `step * (5 % 3)` / `(5 % 3) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (5 % 3)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal modulo coefficient folding；不声明通用乘法 algebra、任意 coefficient solver、exact residue-set analysis、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 静态隔离新 parser modulo literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC static focused 新 parser coefficient 3 PASS、新 LSP coefficient 2 PASS/checks、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Divided Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 11:48 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 divided literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `/` expression，沿用受检 int64 除法的除零和 `INT64_MIN / -1` guard，再把 `step * (6 / 3)` / `(6 / 3) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (6 / 3)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal division coefficient folding；不声明 modulo、通用乘法 algebra、任意 coefficient solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。LSP 覆盖注册为独立 `zr_vm_language_server_numeric_loop_self_dependency_symbolic_coefficient_semantic_query_test`，避免继续扩大已有 symbolic LSP target。 | RED：WSL gcc 新 parser divided literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，新 LSP coefficient target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser coefficient 2 PASS、新 LSP coefficient 1 PASS/check、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multiplicative Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 11:15 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 multiplicative literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `*` expression，再把 `step * (2 * 1)` / `(2 * 1) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (2 * 1)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal multiplication coefficient folding；不声明通用乘法 algebra、任意 coefficient solver、division/modulo、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。Parser 覆盖注册为独立 `zr_vm_numeric_loop_self_dependency_symbolic_coefficient_dataflow_test`，避免继续扩大已有 symbolic parser target。 | RED：WSL gcc 新 parser multiplicative literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`。GREEN：WSL gcc/clang/MSVC focused 新 parser coefficient 1 PASS、parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 10:20 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 folded literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 的 residual collector 现在在 product coefficient slot 递归折叠 integer-literal-only `+`/`-` expression，再把 `step * (1 + 1)` / `(1 + 1) * step` 作为折叠后的 signed integer coefficient 展开为 repeated residual terms。Parser 与 LSP fixture 覆盖 `+(step * (1 + 1)); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded integer-literal additive coefficient folding；不声明通用乘法 algebra、任意 coefficient solver、division/modulo、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser folded literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，LSP target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 16 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 16 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Negative Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 09:13 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 signed literal coefficient 窄泛化：`type_inference_loop_assignment_sequence_symbolic_delta.c/.h` 承接 symbolic residual collector，在现有 term limit 内支持 unary-negative integer literal product expansion，把 `step * -2` / `-2 * step` 识别为两个负向 signed residual terms，再复用既有 residual cancellation；`type_inference_loop_assignment_sequence.c` 保持 sequence orchestration 边界。Parser 与 LSP fixture 覆盖 `+(step * -2); reader; +(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded signed integer literal multiplier expansion；不声明通用乘法 algebra、系数 solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser negative literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，LSP target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 15 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 15 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Literal-Coefficient Net-Zero Delta Replay | 已完成 | 2026-06-24 09:01 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 literal coefficient 窄泛化：`type_inference_loop_assignment_sequence.c` 的 symbolic residual collector 在现有 term limit 内支持 positive integer literal product expansion，把 `step * 2` / `2 * step` 展开为两个 signed residual terms，再复用既有 residual cancellation。Parser 与 LSP fixture 覆盖 `+(step * 2); reader; -(step + step)`，证明 full sequence delta 保持 net-zero，reader prefix 仍由乘法表达式区间给出。该切片只声明 bounded positive integer literal multiplier expansion；不声明通用乘法 algebra、系数 solver、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser literal-coefficient case 失败为 target `Expected 5 Was -9223372036854775808`，LSP target/observer 均失败为 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 14 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 14 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multi-Residual Prefix Range Replay | 已完成 | 2026-06-24 08:43 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic current residual range 窄泛化：`type_inference_loop_assignment_sequence.c` 在 residual terms 非空时对当前剩余 signed-additive terms 做表达式区间推断和 checked signed range accumulation，用 current residual range 覆盖 prefix `deltaMin/deltaMax`。Parser 与 LSP fixture 覆盖 `+step; -(step + bias); reader; +(bias + 1)`，证明 full sequence delta 仍为 `+1..+1`，reader prefix 则按 `-bias` 回放。该切片只声明同一 target-reading sequence 内 supported signed-additive term accumulator 的 current-range projection；不声明通用 symbolic algebra、跨变量相关性、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser prefix case 失败 `Expected 1 Was -5`，LSP observer 失败为 `min=-508 max=INT64_MAX`。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 13 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 13 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Multi-Residual Net-Positive Delta Replay | 已完成 | 2026-06-24 08:24 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的多项 signed-additive symbolic residual 窄泛化：`type_inference_loop_assignment_sequence.c/.h` 把 target-reading symbolic tracker 从单一 expression/balance 扩展为 fixed-size signed residual term accumulator，逐步收集 supported `+`/`-` delta expression terms、checked 累加 integer literals、抵消相反且递归等价的非 literal terms，并在 residual terms 清空时提交 checked constant full-sequence delta。Parser fixture 中 `step=-3..3` / `bias=2..4` 的 `+step; reader; -(step + bias); +(bias + 1)` 得到 target `5..INT64_MAX`、reader `0..INT64_MAX`；LSP fixture 中 `step = seed - 128` / `bias = seed + 3` 得到 target `5..INT64_MAX`、reader `-123..INT64_MAX`。该切片只声明同一 target-reading sequence 内有界 signed-additive term accumulator；不声明通用 symbolic residual solver、target-reading sign-crossing residual、乘法/除法、系数合并、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser/LSP multi-residual fixture 在旧 tracker 下退化为 full int64（LSP target/observer 明确返回 full range；parser target 下界返回 full lower bound）。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 12 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 12 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；diff/whitespace hygiene clean；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Residual Net-Negative Delta Replay | 已完成 | 2026-06-24 08:03 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 signed-additive symbolic residual 窄泛化：`type_inference_loop_assignment_delta_expression.c` 提供常量 residual 计算，按 delta sign 收集两个 supported binary `+`/`-` expression 的 signed additive terms，抵消相反且递归等价的非 literal terms 后，只在没有剩余 terms 时返回 checked literal sum。`type_inference_loop_assignment_sequence.c/.h` 的 tracker 记录该 constant residual，并把 `+step` 与 `-(step + 1)` 的 full sequence delta 修正为 `-1..-1`，从而让 target-reading reader 使用 prefix range，final target 使用 net-negative widening。Parser fixture 中 `step=-3..3` 得到 `narrowed=INT64_MIN..5`、`other=INT64_MIN..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=INT64_MIN..5`、`other=INT64_MIN..132`。该切片只声明两个 supported signed-additive delta expression 的全符号项抵消后 constant residual；不声明一般 symbolic residual accumulator、positive/sign-crossing target-reading recurrence、乘法/除法、系数合并、member/call/unsupported delta、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser residual symbolic net-negative case 返回 full upper bound `INT64_MAX` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 11 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 11 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Positive Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:39 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 unary-positive symbolic expression 窄泛化：`type_inference_loop_assignment_self_dependency.c` 在取得 self-dependent delta expression 后统一调用 unary normalizer，把支持表达式外层 `+` 转成原 delta node 与原 sign，并复用于 equality/sign/range 三条路径。因此 `+(+step)` 与后续 `-step` 可作为同一 supported stable delta 的正/负抵消，reader prefix 按 `+step` 计算。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported self-dependent delta expression 的 unary `+` identity sign normalization；不声明任意代数化、乘法/除法、系数合并、member/call/unsupported delta、non-zero target-reading recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser unary-positive symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 10 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 10 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Unary-Negative Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:26 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 unary-negative symbolic expression 窄泛化：`type_inference_loop_assignment_self_dependency.c` 在取得 self-dependent delta expression 后统一调用 unary-negative normalizer，把支持表达式外层 `-` 转成 delta sign 翻转，并复用于 equality/sign/range 三条路径。因此 `+(-step)` 与后续 `+step` 可作为同一 supported stable delta 的负/正抵消，reader prefix 按 `-step` 计算。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported self-dependent delta expression 的 unary `-` sign normalization；不声明任意代数化、乘法/除法、系数合并、member/call/unsupported delta、non-zero target-reading recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser unary-negative symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 9 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 9 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Same-Side Term Cancellation Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 07:07 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 same-side symbolic term cancellation 窄泛化：`type_inference_loop_assignment_delta_expression.c` 中的 supported binary `+`/`-` signed additive signature 现在会消去同一侧相反符号且递归等价的非 literal terms，并把该归一化同时用于 expression equality 与 stable self-dependent delta range 计算。因此 `+((step + bias) - bias)` 与 `-step` 可作为同一 supported stable delta 的正负抵消，reader prefix range 按剩余 `step` 计算。Parser fixture 中 `step=-3..3`、`bias=2..4` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` / `seed + 3` 得到 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported binary `+`/`-` 中同侧一正一负的等价非 literal term cancellation；不声明跨表达式任意代数化、乘法/除法、系数合并、溢出的 literal sum、member/call/unsupported delta、non-zero target-reading recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser same-side term cancellation symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 8 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 8 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Signed-Additive Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 06:46 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 signed additive symbolic expression 窄泛化：先把 delta expression support/equality 从接近阈值的 `type_inference_loop_assignment_self_dependency.c` 拆到 `type_inference_loop_assignment_delta_expression.c`，使主文件 1030 -> 493 行，新 helper 文件 545 行；随后让 supported binary `+`/`-` delta expression 收集有界 signed term signature，integer literal terms 以 checked int64 signed sum 归一化，非 literal terms 仍按符号与递归 expression equality 做 multiset 匹配。因此 `+((step + 5) - 2)` 与 `-(step + 3)` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..11`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported binary `+`/`-` 的 signed additive integer-literal normalization；不声明非 literal 同侧抵消、乘法/除法代数化、溢出的 literal sum、member/call/unsupported delta、non-zero target-reading recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser signed-additive symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 7 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、LSP symbolic 7 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / LSP Numeric Loop Self-Dependency Symbolic Query Target Split | 已完成 | 2026-06-24 06:13 +08:00 | 按 blueprint 的“先保持测试/实现边界，再继续叠加能力”原则完成 LSP query 支撑拆分：`test_lsp_numeric_loop_self_dependency_semantic_query.c` 只保留 general self-dependent widening、interleave 和 replay-resolved cases；direct/additive-zero/same-shape/commuted/associative/folded-integer-sum symbolic exact-net-zero LSP cases 移入新的 `test_lsp_numeric_loop_self_dependency_symbolic_semantic_query.c`，并注册为独立 support target 和 `language_server` suite 成员。旧文件 993 -> 723 行，新文件 460 行；本条是维护性拆分，不声明新 dataflow behavior。 | WSL gcc focused 通过 parser symbolic 6 PASS、parser self-dependency 16 PASS、LSP self-dependency 16 PASS/checks、新 LSP symbolic 6 PASS/checks、adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS。Windows MSVC focused 构建与运行同组目标通过。WSL clang 当前被活跃 AOT 修改阻塞于 `backend_aot_write_c_direct_neg_signed` 声明/定义类型冲突，发生在 LSP 目标构建前；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Additive-Zero Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:54 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic additive-zero 窄泛化：delta expression equality 会在一侧是 supported expression、另一侧是 supported binary `+` tree 时复用 flattened plus signature matching，所以 `+step` 与 `-(step + 0)` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-123..132`。该切片只声明 supported binary `+` 的 additive-zero signature matching；不声明减法/乘法代数化、任意常量折叠、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser additive-zero symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 6 PASS、parser self-dependency 16 PASS、新 LSP 22 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Folded Integer-Sum Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:40 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression integer-sum 窄泛化：delta expression equality 在 supported binary `+` tree flatten 后对 integer literal terms 做 checked int64 sum normalization，所以 `+((step + 1) + 2)` 与 `-(step + 3)` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..11`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported binary `+` 的 integer-literal sum normalization；不声明任意常量折叠、减法/乘法代数化、溢出的 literal sum、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser folded integer-sum symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 5 PASS、parser self-dependency 16 PASS、新 LSP 21 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Associative Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 05:18 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression correlation 下一层窄泛化：delta expression equality 会把 supported binary `+` tree 展平成有界 term list 并按 multiset 匹配，所以 `+((step + 1) + 2)` 与 `-(step + (1 + 2))` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..11`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-120..135`。该切片只声明 supported binary `+` 的 associative exact-term matching；不声明常量折叠式等价、减法/乘法代数化、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser associative symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 4 PASS、parser self-dependency 16 PASS、新 LSP 20 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:41 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression correlation 窄泛化：delta expression equality 对 binary `+` 增加左右交换匹配，所以 `+(step + 1)` 与 `-(1 + step)` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..9`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported binary `+` 的 commuted same-shape exact-zero matching；不声明减法/乘法代数化、常量折叠式等价、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser commuted symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 3 PASS、parser self-dependency 16 PASS、新 LSP 19 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:27 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression correlation 边界，并先执行维护性拆分：same-target sequence 预扫、delta 累计、target-reading interleave 判定和 reader intermediate store 从 `type_inference_loop_assignment_replay.c` 移入 `type_inference_loop_assignment_sequence.c/.h`，replay 文件降到 456 行。随后 symbolic tracker 改为保存 delta expression AST，并用结构等价比较支持 `+(step + 1)` / `-(step + 1)` exact-zero 抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..9`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-122..133`。不声明表达式代数化、commutative 归一化、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 2 PASS、parser self-dependency 16 PASS、新 LSP 18 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Net-Zero Delta Replay | 已完成 | 2026-06-24 03:50 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic correlation 边界：replay 在 same-target stable self-dependent sequence 中跟踪 direct delta identifier 的 signed balance，允许 `narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step` 这类同一 identifier 正负抵消的 full sequence 以 `0..0` 提交 final target，同时让 reader 用 prefix delta 精确查询中间 target range。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-123..132`。不声明非抵消 target-reading positive/sign-crossing recurrence、replay-resolved positive/sign-crossing、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser symbolic target 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 1 PASS、parser self-dependency 16 PASS、新 LSP 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Negative Delta Replay | 已完成 | 2026-06-24 03:20 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading + replay-resolved net-negative 多写：replay 允许 replay-resolved same-target sequence 跨过 `other = narrowed` 这类普通 reader assignment，当预扫在 replay scope 中解析出的 full sequence delta 为 supported net-negative 时，reader 按 first-iteration `source + prefix` 与 later `widen(source,totalDelta) + prefix` envelope 回放。预扫同时拒绝在后续 replay-resolved delta 前写入该 delta dependency 的普通 interleave。`narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + 1)` 在 unknown-condition `while` 后让 `narrowed` 为 `INT64_MIN..5`，并把 `other` 精化为 `INT64_MIN..6`。不声明 replay-resolved target-reading positive/sign-crossing recurrence、任意 interleave mutation、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading replay-resolved net-negative case 返回 target `max=9223372036854775805` 而非 `5`，LSP local query 返回 target `INT64_MIN..9223372036854775805` 与 observer `0..INT64_MAX`。GREEN：WSL gcc/clang/MSVC focused 新 parser 16 PASS、新 LSP 16 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Zero Delta Replay | 已完成 | 2026-06-24 02:52 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading + replay-resolved net-zero 多写：plan collector 允许 replay-resolved same-target sequence 跨过 `other = narrowed` 这类普通 reader assignment；replay 则把 replay-resolved self-dependent step 作为 sequence 起点，预扫时在临时 scope 中解析 earlier `step = 1` 提供的 `+step` / `-step` delta，并且只有 full sequence delta 精确为 `0..0` 时才让 reader 按 `source + accumulated delta` 参与精确回放。`narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step` 在 unknown-condition `while` 后保持 `narrowed` 为 `5..5`，并把 `other` 精化为 `0..6`。不声明 replay-resolved target-reading non-zero/sign-crossing recurrence、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading replay-resolved net-zero case 返回 target `max=9223372036854775806` 而非 `5`，LSP local query 返回 target `5..9223372036854775806` 与 observer `0..9223372036854775807`。GREEN：WSL gcc/clang/MSVC focused 新 parser 15 PASS、新 LSP 15 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Multiple Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:27 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 multiple same-loop-written 多写：plan collector 现在允许同 target 前缀既可以是 stable self-dependent step，也可以是已标记 replay-resolved 的 same-loop-written self-dependent step，从而支持 `step = 2; narrowed = narrowed + step; narrowed = narrowed + step`。replay 逐个执行 replay-resolved step，并在临时 scope 中解析同一个更早 supported assignment 提供的 delta，最终把两个 `+step` 合成后 widen 为 `5..INT64_MAX`。不声明 target-reading 与 replay-resolved delta 的组合、更复杂链式 same-loop delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser multiple same-loop-written delta case 返回 `max=5` 而非 `INT64_MAX`，LSP local query 同样返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 14 PASS、新 LSP 14 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sequence Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:06 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 same-loop-written 多写：plan collector 可在一个同 target stable self-dependent sequence 后接受 replay-resolved same-loop-written delta step，只要该 delta 依赖更早 supported replay assignment；replay 在执行 `step = 2` 后解析 `narrowed = narrowed + step` 的 delta，并把前置 `+1` 与该 delta 合成后一次 widen。`narrowed = narrowed + 1; narrowed = narrowed + step` 在 unknown-condition `while` 后让 `narrowed` 为 `5..INT64_MAX`。不声明 target-reading interleave 与 replay-resolved delta 的组合、链式/多重 same-loop delta dependencies、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser sequence same-loop-written delta case 返回 `max=5` 而非 `INT64_MAX`。GREEN：WSL gcc/clang/MSVC focused 新 parser 13 PASS、新 LSP 13 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Negative Multi-Write Delta Replay | 已完成 | 2026-06-24 01:39 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading net-negative 多写：plan collector 可穿过 `other = narrowed` 这类读取 sequence target 的普通 assignment，但仅限 combined stable same-target delta 仍是 supported self-dependent recurrence；replay 对读取者写入第一轮 `source + prefix` 与后续轮次 `widen(source,totalDelta) + prefix` 的 envelope。`narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 2` 在 unknown-condition `while` 后让 `narrowed` 为 `INT64_MIN..5`，并把 `other` 精化为 `INT64_MIN..6`。不声明 target-reading positive/sign-crossing fixture、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading interleaved net-negative case 返回 `min=4` 而非 `INT64_MIN`，LSP 初次实现验证暴露查询 offset / fact kind 测试边界。GREEN：WSL gcc/clang/MSVC focused 新 parser 12 PASS、新 LSP 12 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 01:10 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading interleave net-zero 多写：plan collector 可穿过 `other = narrowed` 这类读取 sequence target 的普通 assignment，但仅限 previous stable same-target delta 加当前 delta 精确为 `0..0`；replay 对这种序列先用 source range 加已累计 delta 生成中间 target binding，再执行读取者，最后提交净零 target range。`narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `narrowed` 为 `5..5`，并把 `other` 精化为 `0..6`。不声明 target-reading non-zero recurrence、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading interleaved net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 final target `5..9223372036854775806` 与 observer `0..9223372036854775807`。GREEN：WSL gcc/clang/MSVC focused 新 parser 11 PASS、新 LSP 11 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:49 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-independent interleave net-zero 多写：plan collector 可穿过普通 `other = 10` 这类不读取 sequence target 的 assignment 来延续更早同 target stable self-dependent step；replay 在合并 same-target signed deltas 时执行这些中间 assignment。`narrowed = narrowed + 1; other = 10; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `5..5`。不声明读取 `narrowed` 的 interleave、非零多写 recurrence 精度、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser interleaved positive-then-negative net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 `5..9223372036854775806`。GREEN：WSL gcc/clang/MSVC focused 新 parser 10 PASS、新 LSP 10 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Adjacent Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:31 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的相邻同 target net-zero 多写：plan collector 支持当前 step 延续 immediately previous stable self-dependent same-target step；replay 对连续同 target stable self-dependent sequence 先合并 signed delta range，再从序列前 source binding 做一次 widen。`narrowed = narrowed + 1; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `5..5`。不声明非相邻/穿插其他 target 的 arbitrary multi-write recurrence、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser positive-then-negative net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 `5..9223372036854775806`。GREEN：WSL gcc/clang/MSVC focused 新 parser 9 PASS、新 LSP 9 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Prefix Positive Delta Widening | 已完成 | 2026-06-24 00:02 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-only prefix 后续 delta：plan collector 现在区分“已经写过 target”与“此前 target 写入全是 preserving no-op”，因此 `narrowed = narrowed + 0; narrowed = narrowed + 1` 在 unknown-condition `while` 后可 widen 为 `5..INT64_MAX`。同时拆出 `type_inference_loop_assignment_replay.c` 承担 replay 执行与 result 存储，主 join helper 降到 815 行。不声明任意同 target 多写、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-only-prefix positive case 返回 `max=6`，LSP 新目标返回 `5..6`。GREEN：WSL gcc/clang/MSVC focused 新 parser 8 PASS、新 LSP 8 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Delta No-Op Replay | 已完成 | 2026-06-23 23:28 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-only no-op delta：self-dependency helper 现在接受 `0..0` delta，widen helper 对该形状复制 source type，使 no-op self-dependent assignment 不阻断同一 replay plan 中其他 assignment 的 zero-or-more join。`narrowed = narrowed + 0; other = 10` 在 unknown-condition `while` 后让 `other + 0` 得到 `0..10`。不声明链式/多写同循环 delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-only no-op case 返回 `max=0`。GREEN：WSL gcc/clang/MSVC focused 新 parser 7 PASS、新 LSP 7 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Expression Delta Widening | 已完成 | 2026-06-23 23:17 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的同循环写入 expression delta：deferred self-dependent 检测现在抽取整个 delta expression，确认其中 loop-written target 都是 earlier assignment 后，延迟到 replay scope 中解析。`step = 2; narrowed = narrowed + (step + 1)` 在 unknown-condition `while` 后 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、链式/多写同循环 delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser same-loop-written expression delta case 返回 `max=5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 6 PASS、新 LSP 6 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Stable Delta Widening | 已完成 | 2026-06-23 22:54 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的同循环写入 direct delta：loop assignment plan 现在可标记需要 replay 时解析的 self-dependent delta，允许更早 supported assignment 写入 `step` 后再解析 `narrowed = narrowed + step`。`step = 2; narrowed = narrowed + step` 在 unknown-condition `while` 后 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、复杂同循环 delta 依赖链、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser same-loop-written delta case 返回 `max=5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 5 PASS、新 LSP 5 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sign-Crossing Delta Widening | 已完成 | 2026-06-23 22:40 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 sign-crossing delta：self-dependency helper 现在接受 signed int64 `min < 0 && max > 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `-3..3` / `u8 - 128` 时 widen 为 `INT64_MIN..INT64_MAX`。不声明 zero-only no-op、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser sign-crossing case 返回 `min=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 4 PASS、新 LSP 4 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Negative Delta Widening | 已完成 | 2026-06-23 22:25 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-inclusive negative delta：self-dependency helper 现在接受 signed int64 `min < 0 && max <= 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `-3..0` / `0 - u8` 时 widen 为 `INT64_MIN..5`。不声明 zero-only no-op、sign-crossing delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-inclusive negative case 返回 `min=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 3 PASS、新 LSP 3 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Positive Delta Widening | 已完成 | 2026-06-23 22:14 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-inclusive positive delta：self-dependency helper 现在输出 signed delta min/max，接受 int64 `min >= 0 && max > 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `0..3` / `u8` 时 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、negative/sign-crossing delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-inclusive case 返回 `max=5`，LSP 新目标返回 `5..5`；RED 前测试图重新生成被无关 AOT typed-call 可选拆分源缺失阻断，CMake 目标已改为存在时才追加这些拆分源。GREEN：WSL gcc/clang/MSVC focused 新 parser 2 PASS、新 LSP 2 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive Expression Delta Widening | 已完成 | 2026-06-23 21:51 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的保守表达式 delta：self-dependency helper 从 `target +/- delta` 中抽取 delta 表达式，拒绝 target/self plan 依赖与 member/call/unsupported 形状，并复用表达式推断取得 int64 正下界。`narrowed = narrowed + (step + 1)` 在 `step` 为 `1..3` 时 widen 为 `5..INT64_MAX`。不声明 0/负 delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser 目标返回 `max=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 1 PASS、新 LSP 1 PASS；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive-Range Delta Widening | 已完成 | 2026-06-23 21:05 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的保守正区间 delta：self-dependency helper 接受 int64 positive-lower-bound identifier delta，并继续拒绝同 loop 内写入的 delta target；`narrowed = narrowed + step` 在 `step` 为 `1..3` 时 widen 为 `5..INT64_MAX`。不声明含 0/负数 delta、复杂 delta expression、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 返回 `max=5`，LSP 返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True Step Known-True-If Break-Branch At-Least-Once Range Join | 已完成 | 2026-06-23 20:47 +08:00 | 按阶段 2 数值区间/逻辑事实路线补 traditional `for` step-before-break 的 deterministic-if 执行蓝图：在 loop entry 侧用已有 bool 常量/逻辑事实 evaluator 证明 `if (true)` then 分支必走，nested-if target collector 允许 missing `else`，branch assignment replay 对已知条件只提交可达分支。`for (; true; narrowed = 20) { if (true) { narrowed = 10; break; } } narrowed + 1` 现在得到 `11..11`。不声明 symbolic if、unknown missing-else、continue、break with expression、no-break loop、复杂 nested body、fixed point 或 general CFG-wide numeric dataflow。 | RED：WSL gcc parser 先失败 `Expected 11 Was 6`，LSP 返回 `6..6`。GREEN：WSL gcc/clang/MSVC focused constant-condition parser 16 PASS、LSP constant-condition 16 checks；key adjacent loop assignment 15 PASS、LSP branch-assignment 24 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step Nested-If Break-Branches At-Least-Once Range Join | 已完成 | 2026-06-23 20:24 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` step-before-break 的窄化 body-control 切片：loop syntax helper 新增 `ZrParser_TypeInferenceLoopAssignment_StatementGuaranteesPlainBreak`，entry helper 用它识别顶层 `if/else` then/else 都 plain `break;` 的 step-skipping 证明；body plan collector 在收集 nested-if assignment 后停止后续 replay。`for (; true; narrowed = 20) { if (flag) { narrowed = 10; break; } else { narrowed = 12; break; } } narrowed + 1` 返回 `11..13`。该切片不声明 missing else、branch without break、continue、break with expression、无 break 的 known-true loop、复杂 nested control flow、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser nested-if break-branches 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 15 PASS 与 LSP constant-condition 15 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init-Step Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:59 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` init+step/header-local+step 的 step-before-break 切片：entry helper 复用 for-init 临时 scope；当 known-true/omitted condition 且 body 顶层 plain `break;` 证明 step 被跳过时，supported assignment init 与 direct identifier `var` init 都可和 step 共存，并通过 at-least-once body replay 发布后继事实。assignment-init+step true/omitted 与 var-init+step true/omitted 四类 fixture 均返回 `11..11`。该切片不声明 nested break、continue、break with expression、无 break 的 known-true loop、复杂 body control flow、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 四个新用例先失败为 `Expected 11 Was 6`，LSP 四个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 14 PASS 与 LSP constant-condition 14 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` step-before-break 切片：`ZrParser_TypeInferenceLoopAssignment_StatementIsPlainBreak` 被抽到 loop syntax 模块，join collector 和 entry helper 共用它；entry 只在 no-init、known-true/omitted condition、body 顶层 plain `break;` 会使 step 不执行时，允许 step 存在并调用 at-least-once body replay。`for (; true; narrowed = 20) { narrowed = 10; break; }` 与 omitted 变体后续表达式返回 `11..11`。该切片不声明 init+step/header-local+step、nested break、continue、break with expression、无 break 的 known-true loop、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 两个 step fixture 先失败为 `Expected 11 Was 6`，LSP 两个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 10 PASS 与 LSP constant-condition 10 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 19:10 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 init/header-local 切片：entry helper 复用 for-init 临时 scope，`bodyRunsAtLeastOnce` 分支在无 step 时调用 `JoinBodyAtLeastOnce`，所以 supported assignment init 和 direct identifier `var` init 都能先进入 replay scope，再让 break 前 body assignment 产生精确后继事实。assignment-init true/omitted 与 var-init true/omitted 四类 fixture 均返回 `11..11`。该切片不声明 step、combined var-init+step、destructuring/no-range header binding、复杂 break/continue CFG、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 四个新用例先失败为 `Expected 11 Was 6`，LSP 四个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 8 PASS 与 LSP constant-condition 8 checks；相邻 loop/for-header/nested/foreach/LSP branch assignment 回归通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Condition Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 18:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的窄切片：无 init、无 step 时，known true 或省略 condition 的 `for` 会复用 at-least-once body replay；plain `break;` 被 loop replay collector 当作前缀终止点，因此 break 前 supported assignment 可用于后续事实。`for (; true; ) { narrowed = 10; break; } narrowed + 1` 与 `for (;;) { narrowed = 10; break; } narrowed + 1` 返回 `11..11`。该切片不声明 init/step/header-local binding 下的 known-true/omitted forms、复杂 break/continue CFG、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser true/omitted 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 4 PASS 与 LSP constant-condition 4 checks；相邻 loop/for-header/nested/foreach/LSP branch assignment 回归通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Var-Init Scoped Binding | 已完成 | 2026-06-23 18:17 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 LSP traditional `for` var-init 作用域隔离：`semantic_analyzer_typecheck.c` 对 `ZR_AST_FOR_LOOP` 的 variable-declaration init 推入临时 type binding scope，让 init/condition/step/body 可见 header-local binding，并在 pop 后再运行 parser-owned numeric assignment join。`for (var step: int = 10; false; ) { narrowed = step; } step + 1` 不再把 `step` 的 `10..10` 泄漏成后续 `11..11`。该切片不声明 destructuring/no-range 声明、initializer side effects、known-true/omitted conditions、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser coverage 首跑通过但 LSP no-leak query 失败为 `11..11`。GREEN：WSL gcc/clang focused+adjacent 通过 constant-condition parser 2 PASS、LSP constant-condition 2 checks、loop assignment 15 PASS、for-header 1 PASS、branch-assignment 24 PASS；Windows MSVC 通过对应 focused/adjacent 目标。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Nonempty Cardinality Range Join | 已完成 | 2026-06-23 17:52 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 `foreach` 非空数组 cardinality：direct identifier item binding 复用 iterable type 推断，数组 size constraint 可证明非空时走 at-least-once body join，未知长度数组继续走 zero-or-more join。`items: int[2]` 的 `foreach` body assignment 后续表达式返回 `11..11`，`int[]` 仍返回 `6..11`。该切片不声明 destructuring、非数组 iterable、动态长度证明、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED-GATE：focused parser/LSP 用例已写好，但实现前 RED 执行被 gcc build 占用和 clean build 超时阻断。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric foreach cardinality 2 PASS 与 LSP numeric foreach cardinality query 2 PASS；WSL gcc/clang 相邻语义回归通过 numeric loop assignment、nested loop、constant-condition、for header 与对应 LSP query。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Loop Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 17:15 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 nested loop replay 边界：外层 loop body 里的 nested `while`/`for`/`foreach` 会作为 replay step，复用内层 loop 的现有 bounded numeric assignment join，再把受影响 target 写回外层 replay result。`while (flag) { while (inner) { narrowed = 10; } } narrowed + 1` 返回 `6..11`。新增 nested-loop helper 并把 replay scope helper 移到 scope 模块，主 join helper 保持 982 行。该切片不声明真正 loop fixed point、known-true/omitted condition、unsupported statements/assignments、复杂 recurrence 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP 新目标返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric nested loop assignment 1 PASS 与 LSP numeric nested loop assignment query 1 PASS；WSL gcc 相邻语义回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、numeric for header assignment 1 PASS、LSP numeric loop constant-condition query 1 PASS、LSP numeric for header assignment query 1 PASS。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Combined Init-Step Assignment Coverage | 已完成 | 2026-06-23 16:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for 的 combined header replay 覆盖：显式 unknown condition 下，assignment init 先写入临时 scope，body+assignment step replay 再从该 scope 继续执行。`for (narrowed = 1; flag; narrowed = 10) { flag; } narrowed + 1` 返回 `2..11`，覆盖零次迭代 init `1` 与一次或多次迭代 step `10`。本轮是覆盖确认，未改生产代码；不声明 known-true/omitted condition、false-condition variable-declaration init、nested loops、true fixed point、step side-effect 精化或 general CFG-wide dataflow。 | COVERAGE-GATE：新建 parser/LSP focused 目标首跑通过。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric for header assignment 1 PASS 与 LSP numeric for header assignment query 1 PASS；WSL gcc 相邻语义回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；`git diff --check` 通过，只有既有 LF/CRLF warning。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Constant-False Condition Range Application | 已完成 | 2026-06-23 16:06 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 constant-false condition replay 边界：known-false init shortcut 现在消费 bool literal、condition logical fact 和 AST constant boolean fallback。`for (narrowed = 1; 1 == 2; ) { narrowed = 10; } narrowed + 1` 返回 `2..2`，只保留 init assignment，不纳入 pre-loop 或 body path。该切片不声明 known-true/omitted condition、false-condition variable-declaration init、combined init+step、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 11`，LSP 新目标返回 `2..11`。GREEN：WSL gcc/clang focused 与 Windows MSVC focused 通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Non-Assignment Step Zero-Or-More Range Join | 已完成 | 2026-06-23 15:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 non-assignment step replay 边界：显式 unknown condition 的 `for (; flag; flag)` 现在把非赋值 step 当作不写入 loop target 的普通 expression，不再阻断 body-only zero-or-more join。`for (; flag; flag) { narrowed = 10; } narrowed + 1` 返回 `6..11`。该切片不声明 step side-effect 精化、nested loops、true fixed point、unsupported non-expression statements 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 15 PASS、LSP numeric branch assignment query 24 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 15/24 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Init Assignment Range Application | 已完成 | 2026-06-23 15:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for known-false condition 的 init assignment 保留：condition fact 为 known false 时，entry helper 只应用 supported assignment-expression init 并跳过 body/step replay。`for (narrowed = 1; false; ) { narrowed = 10; } narrowed + 1` 返回 `2..2`，不再保留循环前 `5` 或进入 unreachable body 的 `10`。该切片不声明 known-true/omitted condition、variable-declaration init known-false leakage、combined init+step、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 14 PASS、LSP numeric branch assignment query 23 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 14/23 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Var-Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 15:05 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 variable-declaration init replay：`type_inference_loop_assignment_entry.c` 现在把直接 identifier `var` init 转成临时 replay scope 的 loop-local numeric binding，body/step replay 可读取该 binding，提交 joined scope 时跳过该 loop-local 名称。`for (var step: int = 10; flag; ) { narrowed = step; } narrowed + 1;` 返回 `6..11`，覆盖零次迭代的 pre-loop `5` 与一次或多次迭代读取 `step=10` 的路径。该切片不声明 destructuring var init、无 initializer/range 声明、omitted/constant condition、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 13 PASS、LSP numeric branch assignment query 22 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 13/22 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Item-Dependent Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 14:43 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach item-dependent loop assignment replay：`foreach` 入口 helper 现在先从 iterable 推断 element type，在临时 replay scope 中注册直接 identifier item，再运行已有 zero-or-more loop body join。提交 joined scope 时会跳过 item binding，只把 numeric assignment targets 写回外层环境。`for (var item in items) { narrowed = item; } narrowed + 1;` 在 `items` element range 为 `2..4` 时返回 `3..6`，LSP `u8[]` fixture 返回 `1..256`。该切片不声明 destructuring pattern、显式 foreach type annotation 专用路径、iterable cardinality、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 3 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 12 PASS、LSP numeric branch assignment query 21 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 12/21 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Singleton Delta Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 14:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS：unknown-condition `while` 的 first-write positive increment widening 现在可把稳定的 int64 singleton 标识符当作 delta。`while (flag) { narrowed = narrowed + step; }` 中 `step` 若是非 target、未被同一 loop replay plan 写入、且当前 binding 为 `1..1` 这类正单点区间，则后续 `narrowed + 0` 返回 `5..9223372036854775807`。实现只扩展 `type_inference_loop_assignment_self_dependency.*` 和 loop plan validation，不扩大到 full fixed-point dataflow；non-singleton/non-identifier delta、复杂 recurrence、nested loops、for/foreach 递推与 general CFG-wide dataflow 仍待后续。 | RED：WSL gcc parser/LSP 新 fixture 分别停在 `max=5`。GREEN：WSL gcc focused 通过 numeric loop assignment 11 PASS、LSP numeric branch assignment query 20 PASS；WSL clang AOT-off focused 与 Windows MSVC focused 通过同两目标 11/20 PASS；WSL gcc 相邻语义回归通过。AOT-on WSL clang rebuild 被无关 dirty-tree AOT `typeTableIndex` 编译错误阻断；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Step Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 13:35 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 step assignment replay：无 init、显式 unknown condition 的 `for (; flag; target = value)` 会把 body 与 step assignment 顺序放入同一个 replay plan，再复用 zero-or-more join。`for (; flag; narrowed = 10) { flag; } narrowed + 1;` 返回 `6..11`，覆盖零次迭代路径与一次或多次完成迭代后的 step 赋值路径。实现同时把当前 scope binding 写入 helper 拆到 `type_inference_loop_assignment_scope.c`，主 loop helper 保持 952 行。该切片不声明 variable-declaration init、combined init+step 覆盖、omitted/constant condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：numeric loop assignment 10 PASS、LSP numeric branch assignment query 19 PASS；WSL gcc 相邻回归通过：numeric branch assignment 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 12:53 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 init assignment replay：无 step、显式 unknown condition 的 `for (target = value; flag; )` 会先把 assignment init 的 numeric range 写入临时 scope，再复用共享 loop-body zero-or-more join。`for (narrowed = 1; flag; ) { narrowed = 10; } narrowed + 1;` 返回 `2..11`，覆盖 init 后零次迭代路径与 body 一次或多次执行路径。该切片不声明 variable-declaration init、for step、omitted/constant condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 9 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 18 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach 的受限 loop assignment replay：新增 `ZrParser_TypeInference_TryJoinForeachNumericAssignments`，parser foreach expression inference 与 LSP foreach typecheck 都在外层 type environment 上调用共享 zero-or-more join body。`for (var item in items) { narrowed = 10; } narrowed + 1;` 返回 `6..11`。实现同时把 while/for/foreach loop-kind 入口拆到 `type_inference_loop_assignment_entry.c`，主 replay/join helper 降到 802 行。该切片不声明 item-dependent RHS、iterable cardinality 精化、for init/step/constant-condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser foreach 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 8 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 17 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:14 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 loop assignment replay：`type_inference_loop_assignment_join.c` 现在把共享 replay/join body 同时供 while 和 `for (; flag; )` 使用，`ZrParser_TypeInference_TryJoinForNumericAssignments` 只接收无 init、显式 unknown condition、无 step 的 `ZR_AST_FOR_LOOP`。parser for 推断与 LSP for typecheck 接入后，`narrowed=5; for (; flag; ) { narrowed = 10; } narrowed + 1;` 返回 `6..11`。实现同时把 parser loop assignment 覆盖拆到新的 `test_numeric_loop_assignment_dataflow.c`，避免继续扩张 branch assignment 测试。该切片不声明 for init/step dataflow、无条件或常量条件 for、foreach、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser loop target 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 7 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 16 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Decrement Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 05:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS 边界：unknown-condition while 的 focused helper 现在可识别首次写入 target 的 `target = target - positive_integer_literal`，把一次或多次 loop-body 结果保守 widened 到 int64 下界，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed - 1; } narrowed + 0;` 返回 `-9223372036854775808..5`。实现复用 `type_inference_loop_assignment_self_dependency.*` 的 signed delta 分支，主 loop helper 保持只接入 plan 标记与 replay 分支。该切片不声明非字面量 delta、`literal - target`、乘法/条件 recurrence、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：Windows MSVC parser 先失败为 `Expected -9223372036854775808 Was 5`；LSP 新用例首次运行暴露 query fixture occurrence 参数错误，修正后用于 GREEN 覆盖。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 15 PASS、LSP numeric branch assignment query 15 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Increment Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 04:31 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS 边界：unknown-condition while 的 focused helper 现在可识别首次写入 target 的 `target = target + positive_integer_literal`，把一次或多次 loop-body 结果保守 widened 到 int64 上界，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed + 1; } narrowed + 0;` 返回 `5..9223372036854775807`。实现新增 `type_inference_loop_assignment_self_dependency.*`，主 loop helper 只接入 plan 标记与 replay 分支。该切片不声明自减、非字面量 delta、乘法/条件 recurrence、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 9223372036854775807 Was 5`，LSP assignment query 返回 `min=5 max=5`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 14 PASS、LSP numeric branch assignment query 14 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Nested If Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 04:01 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内受限嵌套分支赋值合流：unknown-condition while 的 focused helper 现在可把 nested simple `if/else` 作为 replay step，在 loop replay scope 中复用既有 branch assignment join，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { if (inner) { narrowed = 1; } else { narrowed = 10; } } narrowed + 1;` 返回 `2..11`。同时拆出 `type_inference_loop_assignment_syntax.*` 与 `type_inference_loop_assignment_nested_if.*`，主 helper 回到 878 行。该切片不声明 self-dependent RHS、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 13 PASS、LSP numeric branch assignment query 13 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Statement Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:29 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内赋值合流：unknown-condition while 的 focused helper 现在可跳过普通 expression statement，只 replay simple numeric assignment。`narrowed=5; while (flag) { flag; narrowed = 10; flag; } narrowed + 1;` 返回 `6..11`。该切片不声明 self-dependent RHS、嵌套 loop/if loop body、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 12 PASS、LSP numeric branch assignment query 12 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Target Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:08 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内跨目标赋值合流：unknown-condition while 的 focused helper 现在可顺序 replay 多个 simple numeric assignments，并把每个目标的 loop-body replay range 与进入循环前 range 合流。`low=5, high=20; while (flag) { low = 1; high = low + 10; } high + low;` 返回 `12..25`。该切片不声明 self-dependent RHS、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：新增 LSP assignment query 先返回 `min=25 max=25`；parser 对应 RED 覆盖在共享 checkout 中曾被无关 AOT 编译错误打断。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 11 PASS、LSP numeric branch assignment query 11 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 02:42 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：parser `ZR_AST_WHILE_LOOP` 和 LSP while typecheck 现在共用 `type_inference_loop_assignment_join.c`，对 unknown-condition while 中唯一 simple integer assignment 做进入前 range 与循环体 RHS range 的 zero-or-more join。`narrowed = 5` 后 `while (flag) { narrowed = 10; } narrowed + 1` 返回 `6..11`。该切片不声明 loop fixed point、self-dependent RHS、for/foreach、多目标循环或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 10 PASS、LSP numeric branch assignment query 10 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、CFG reachability 29 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Branch Assignment Dataflow | 已完成 | 2026-06-23 01:00 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple branch assignment replay 现在能处理 branch block 内 bounded nested simple `if/else`，先在临时 branch scope 中 join 嵌套分支写入，再把当前 plan target binding 存回外层 branch result。`narrowed = 1/2` 与 `narrowed = 10/20` 的嵌套 if/else 后，`narrowed + 1` 在 parser 与 LSP local semantic query 中返回 `2..21`。general CFG-wide numeric dataflow、loop/fixed-point、任意嵌套控制流、unsupported assignments 与任意 symbolic range-set/hole-set 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 1`，LSP assignment query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 9 PASS、LSP numeric branch assignment query 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Segment-Preserving Branch Assignment Join | 已完成 | 2026-06-23 00:42 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple branch assignment join 现在会保留两侧分支写入产生的不连续 segment payload，`narrowed = 1/10` 后的 `narrowed + 1` 在 parser 与 LSP local semantic query 中返回 envelope `2..11` 与 segments `2..2` / `11..11`。LSP assignment query 覆盖已从 branch refinement 文件拆到独立 assignment 目标。general CFG-wide numeric dataflow、loop/fixed-point、跨嵌套控制流与任意 symbolic range-set/hole-set 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 0`，LSP assignment query 返回 `exprSegments=0` / `numeric segments=0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 8 PASS、LSP numeric branch assignment query 8 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Target Branch Assignment Dataflow | 已完成 | 2026-06-23 00:09 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple if/else 分支块现在允许多个 simple numeric assignment target，并在临时 branch scope 中按顺序重放 target 写入，让同一分支内的后续 target RHS 可读取前置 target range。`if (flag) { low = 1; high = low + 1; } else { low = 10; high = low + 10; } high + low;` 现在返回 `3..30`。general CFG-wide numeric dataflow、loop/fixed-point、跨嵌套控制流与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 multi-target 用例失败为 `Expected 3 Was 0`，LSP query 只返回 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 7 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 23 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric RHS-Dependent Sequential Branch Assignment Range Join | 已完成 | 2026-06-22 23:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许同一 target 的顺序 simple assignments，并在临时 branch scope 中重放前置写入，让 RHS 依赖前置写的后一条 assignment 可读取分支内 range。`if (flag) { narrowed = 1; narrowed = narrowed + 1; } else { narrowed = 10; narrowed = narrowed + 1; } narrowed + 1;` 现在返回 `3..12`。多 target/多变量 branch dataflow、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 RHS-dependent 用例失败为 `Expected 3 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 6 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 22 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nonterminal Multi-Statement Branch Assignment Range Join | 已完成 | 2026-06-22 23:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许后置普通表达式，只要块内只有一个同名数值 simple assignment，就把两侧 RHS range 合并。`if (flag) { narrowed = 1; flag; } else { narrowed = 10; flag; } narrowed + 1;` 现在返回 `2..11`。多次写、RHS 依赖前置写、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 nonterminal multi-statement 用例失败为 `Expected 2 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 5 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 21 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Statement Terminal Branch Assignment Range Join | 已完成 | 2026-06-22 23:04 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许前置普通表达式，只要块末尾是同名数值 simple assignment，就把两侧 RHS range 合并。`if (flag) { flag; narrowed = 1; } else { flag; narrowed = 10; } narrowed + 1;` 现在返回 `2..11`。assignment 不在末尾、多次写、RHS 依赖前置写、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 multi-statement 用例失败为 `Expected 2 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 4 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 20 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Else-Only Branch Assignment Range Join | 已完成 | 2026-06-22 22:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：explicit else-only simple assignment 会把 else RHS range 与 true path 保留的进入前 binding range 合并。`var narrowed: int = 5; if (flag) { flag; } else { narrowed = 10; } return narrowed + 1;` 现在返回 `6..11`。general CFG-wide numeric dataflow、multi-statement、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 else-only 用例失败为 `Expected 11 Was 6`，LSP query 只返回 `6..6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 3 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 19 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric One-Sided Branch Assignment Range Join | 已完成 | 2026-06-22 22:26 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if true-branch 单侧赋值会把 then RHS range 与 false path 保留的进入前 binding range 合并；显式类型注解的数值变量声明现在也保留 initializer 单点范围，供 parser/LSP 后续查询使用。`var narrowed: int = 5; if (flag) { narrowed = 10; } return narrowed + 1;` 现在返回 `6..11`。general CFG-wide numeric dataflow、multi-statement、else-only、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 if/then 用例失败为 `Expected TRUE Was FALSE`，LSP query 无 range。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 2 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 18 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Definite Assignment Declaration Related Information | 已完成 | 2026-06-22 21:36 +08:00 | 按阶段 2 的 definite-assignment 与 02 §1/§2 诊断 related 方向补一层最小闭环：结构化诊断拥有 `relatedInformation` 数组，未初始化/可能未初始化 read 会把 declarationRange 作为 `Variable declaration is here` 关联位置，LSP 诊断转换保留并发布该信息；LSP semantic query diagnostics 在 AST 可用时先跑 CFG-backed definite assignment，让 branch join 场景能发布 `possibly_uninitialized_read`。此切片只覆盖 DA declaration relatedInformation；fix-it、descriptorId、诊断码注册表、所有权 related 与 compiler 外部/二进制诊断通道仍不声明完成。 | RED：parser/LSP relatedInformation 断言先失败，LSP branch fixture 初始无 query diagnostic；GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：semantic query 14 PASS、LSP semantic query diagnostics 11 PASS、LSP local semantic query 19 PASS、numeric branch assignment 1 PASS、LSP numeric branch semantic query 17 PASS。compiler semantic query diagnostics 本轮因 typed metadata export 阶段 invalid `exportedVar->name` segfault 未作为 GREEN 证据；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Cross-Block Branch Assignment Range Join | 已完成 | 2026-06-22 21:03 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补一层最小跨块赋值合流：parser 和 LSP 共用 `type_inference_branch_assignment_join.c`，仅对 simple if/else 中两侧同名数值赋值做 range envelope join，并把 join 后 binding 暴露给后续表达式推断。新增 parser/LSP focused 覆盖，`narrowed = 1/10` 后的 `narrowed + 1` 变为 `2..11`。general CFG-wide numeric dataflow、loop fixed point、多语句分支和 one-sided branch 仍不声明完成。 | RED：WSL gcc parser 新用例先失败为 `Expected 2 Was 1`，LSP 新用例没有 range fact。GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：numeric branch assignment 1 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 17 PASS、LSP semantic query diagnostics 10 PASS；不声明全仓库 GREEN。 |
