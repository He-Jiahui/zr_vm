---
doc_type: plan-detail
related_code:
  - zr_vm_parser/src/zr_vm_parser/type_inference
  - zr_vm_parser/include/zr_vm_parser/diagnostic_builder.h
  - zr_vm_language_server/src/zr_vm_language_server/interface/lsp_interface_position.c
  - zr_vm_lib_debug/src/zr_vm_lib_debug
  - zr_vm_cli/src/zr_vm_cli/repl/repl.c
  - tests/CMakeLists.txt
---

# 05 · 实施蓝图

把 01-04 的设计落成有依赖顺序、可独立验收的阶段。每阶段都「能单独合入、能单独测」。

---

## 1. 模块依赖图

```
semantic_query.h (01 §7)  <-- 所有前端的统一入口
        ^
        |  produces facts
  +-----+-----------------------------+
  | CFG(01 §1) -> dataflow(01 §2)      |
  |   -> definite-assign/refs(01 §3)   |
  |   -> ownership(01 §4)              |
  |   -> numeric/logical(01 §5,6)      |
  +-----------------------------------+
        |                 |                |
        v                 v                v
 diagnostics(02)     LSP(03)          debug+REPL(04)
```

- 02/03/04 都消费 01 的 `semantic_query.h`；02 的诊断扩展是 03/04 错误展示的前提。
- 03 的「UTF-16 行列修复」**不依赖** 01，可最先独立交付。

---

## 2. 分阶段计划

### 阶段 0：独立速赢（无依赖，最先做）
- **03 §1**：UTF-16 位置编解码层，替换 `lsp_interface_position.c` 全部转换；扩 `test_lsp_position_mapping.c`。
- **03 §2** 部分：越界防护（content[index] 边界检查）。
- 交付物：行列 bug 修复 + 不崩防护。可单独合入。

### 阶段 1：语义地基
- **01 §1** CFG 构建 + 语句级不可达。
- **01 §2** 通用 dataflow 引擎。
- **01 §7** `semantic_query.h` 骨架（先包现有推断，后续填新事实）。
- 交付物：不可达代码诊断 + 统一查询面雏形。

### 阶段 2：流分析铺开
- **01 §3** definite assignment + reaching defs（强化引用）。
- **01 §4** 所有权流追踪（接已有 escape 诊断）。
- **01 §5/§6** 数值区间 + 逻辑事实。
- **02 §1/§2** 诊断结构扩展（related/fixes/descriptorId）+ 关联位置。
- 交付物：未初始化/use-after-move/越界/恒真条件诊断，带「哪里-为什么-怎么改」。

### 阶段 3：前端体验
- **03 §3** 局部语义推断 + 声明级增量解析。
- **03 §4** hover/completion/semanticTokens 质量。
- **02 §3/§4** 类型不匹配具体化 + 诊断码注册表 + 本地化槽。
- 交付物：大文件流畅、诊断可一键修复、IDE 富 hover。

### 阶段 4：调试与交互
- **04 §1** debug 求值统一到 `semantic_query.h` + 条件断点设置期校验。
- **04 §2** REPL 常驻全局态（先过渡、再增量声明注入）。
- **04 §1.5** DAP 适配层（中期）。
- 交付物：调试器/REPL 与编辑器能力一致、REPL 真交互。

---

## 3. 新增/改动文件清单（预估）

新增：
- `zr_vm_parser/src/zr_vm_parser/type_inference/cfg.{c,h}`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow.{c,h}`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_definite_assign.c`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_ownership.c`
- `zr_vm_parser/src/zr_vm_parser/type_inference/dataflow_numeric.c`
- `zr_vm_parser/include/zr_vm_parser/semantic_query.h` + 实现
- `zr_vm_parser/include/zr_vm_parser/diagnostic_registry.h` + 实现
- `zr_vm_language_server/src/zr_vm_language_server/interface/lsp_position_codec.{c,h}`

改动：
- `diagnostic_builder.h/.c`：扩展结构 + 新 builder。
- `compiler_diagnostics.c`：替换模板化类型错误。
- `lsp_interface_position.c`：转调 codec。
- `lsp_interface.c` / `semantic_analyzer*.c`：诊断序列化补 related/fix；查询接 `semantic_query.h`。
- `incremental_parser.c`：声明级增量。
- `debug_eval.c` / `debug_semantic_facts.c`：接统一推断。
- `repl.c`：常驻全局态。
- `tests/CMakeLists.txt` 等：注册新测试。

---

## 4. 测试策略

- **parser 单测**（`tests/parser/`）：CFG 不可达、definite-assign、所有权流、数值区间，每个分析独立用例 + 「分析失败产出 Unknown 不崩」用例。
- **LSP 测试**（`tests/language_server/`）：position 往返/边界（扩 `test_lsp_position_mapping.c`）、局部推断、诊断 related/fix 字段、hover/completion 内容。
- **debug 测试**（`tests/debug/`）：统一求值后的类型/诊断、条件断点设置期校验、watchpoint。
- **REPL 测试**：跨提交引用变量与运行期值、失败回滚。
- **fuzz**：position codec + 增量编辑，断言不崩 + 往返幂等。
- **回归**：现有验收（`tests/acceptance/`）与 reference fixtures 全绿。

---

## 5. 验收标准（DoD）

1. 含中文/emoji 的 `.zr` 文件，hover/definition/diagnostic 位置精确（UTF-16）。
2. `return` 后代码、常量分支、穷尽 union 后 default 报不可达，带 cause。
3. 未初始化使用、use-after-move、借用逃逸均报错，带 related 位置 + 修复建议。
4. 类型不匹配诊断给出「期望 vs 实得 + 原因 + 可行 cast」。
5. 任意畸形/超大输入下 LSP 不崩；推断失败降级为 Unknown。
6. 大文件编辑单函数时延迟显著低于全量。
7. debug 条件断点/表达式求值的类型与诊断和编辑器一致。
8. REPL 中先定义变量、后续表达式可引用其运行期值。

---

## 6. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| REPL 常驻全局态需编译器支持「追加声明」，改动深 | 先做「运行期值快照重放」过渡，验证收益后再上增量注入 |
| dataflow 在病态输入上发散/慢 | 迭代次数与节点数上限 + 超限降级部分结果 |
| 诊断结构扩展破坏现有序列化 | 字段只增不改，旧路径默认空 related/fix |
| `location.h` column 单位若为字节，反向映射也需改 | 阶段 0 先核对 column 语义并统一到 codec |
| DAP/AOT 调试范围大 | 列为中后期，先保证 interp/binary 路径 |

---

## 状态与产出记录

| 阶段 | 状态 | 完成时间 | 完成项目 | 验证 |
| --- | --- | --- | --- | --- |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Commuted Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:41 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression correlation 窄泛化：delta expression equality 对 binary `+` 增加左右交换匹配，所以 `+(step + 1)` 与 `-(1 + step)` 可作为同一 supported stable delta 的正负抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..9`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-122..133`。该切片只声明 supported binary `+` 的 commuted same-shape exact-zero matching；不声明减法/乘法代数化、常量折叠式等价、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser commuted symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 3 PASS、parser self-dependency 16 PASS、新 LSP 19 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Expression Net-Zero Delta Replay | 已完成 | 2026-06-24 04:27 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic expression correlation 边界，并先执行维护性拆分：same-target sequence 预扫、delta 累计、target-reading interleave 判定和 reader intermediate store 从 `type_inference_loop_assignment_replay.c` 移入 `type_inference_loop_assignment_sequence.c/.h`，replay 文件降到 456 行。随后 symbolic tracker 改为保存 delta expression AST，并用结构等价比较支持 `+(step + 1)` / `-(step + 1)` exact-zero 抵消。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..9`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-122..133`。不声明表达式代数化、commutative 归一化、member/call/unsupported delta、non-zero target-reading positive/sign-crossing recurrence、fixed point 或 general CFG-wide dataflow。 | RED：新增 parser symbolic expression case 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 2 PASS、parser self-dependency 16 PASS、新 LSP 18 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Symbolic Net-Zero Delta Replay | 已完成 | 2026-06-24 03:50 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 symbolic correlation 边界：replay 在 same-target stable self-dependent sequence 中跟踪 direct delta identifier 的 signed balance，允许 `narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step` 这类同一 identifier 正负抵消的 full sequence 以 `0..0` 提交 final target，同时让 reader 用 prefix delta 精确查询中间 target range。Parser fixture 中 `step=-3..3` 得到 `narrowed=5..5`、`other=0..8`；LSP fixture 中 `seed - 128` 得到 `narrowed=5..5`、`other=-123..132`。不声明非抵消 target-reading positive/sign-crossing recurrence、replay-resolved positive/sign-crossing、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser symbolic target 返回 full lower bound `INT64_MIN` 而非 `5`，LSP local query 对 target/observer 返回 full int64。GREEN：WSL gcc/clang/MSVC focused 新 parser symbolic 1 PASS、parser self-dependency 16 PASS、新 LSP 17 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Negative Delta Replay | 已完成 | 2026-06-24 03:20 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading + replay-resolved net-negative 多写：replay 允许 replay-resolved same-target sequence 跨过 `other = narrowed` 这类普通 reader assignment，当预扫在 replay scope 中解析出的 full sequence delta 为 supported net-negative 时，reader 按 first-iteration `source + prefix` 与 later `widen(source,totalDelta) + prefix` envelope 回放。预扫同时拒绝在后续 replay-resolved delta 前写入该 delta dependency 的普通 interleave。`narrowed = narrowed + step; other = narrowed; narrowed = narrowed - (step + 1)` 在 unknown-condition `while` 后让 `narrowed` 为 `INT64_MIN..5`，并把 `other` 精化为 `INT64_MIN..6`。不声明 replay-resolved target-reading positive/sign-crossing recurrence、任意 interleave mutation、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading replay-resolved net-negative case 返回 target `max=9223372036854775805` 而非 `5`，LSP local query 返回 target `INT64_MIN..9223372036854775805` 与 observer `0..INT64_MAX`。GREEN：WSL gcc/clang/MSVC focused 新 parser 16 PASS、新 LSP 16 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Replay-Resolved Net-Zero Delta Replay | 已完成 | 2026-06-24 02:52 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading + replay-resolved net-zero 多写：plan collector 允许 replay-resolved same-target sequence 跨过 `other = narrowed` 这类普通 reader assignment；replay 则把 replay-resolved self-dependent step 作为 sequence 起点，预扫时在临时 scope 中解析 earlier `step = 1` 提供的 `+step` / `-step` delta，并且只有 full sequence delta 精确为 `0..0` 时才让 reader 按 `source + accumulated delta` 参与精确回放。`narrowed = narrowed + step; other = narrowed; narrowed = narrowed - step` 在 unknown-condition `while` 后保持 `narrowed` 为 `5..5`，并把 `other` 精化为 `0..6`。不声明 replay-resolved target-reading non-zero/sign-crossing recurrence、unsupported/member/call delta、unsupported assignment、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading replay-resolved net-zero case 返回 target `max=9223372036854775806` 而非 `5`，LSP local query 返回 target `5..9223372036854775806` 与 observer `0..9223372036854775807`。GREEN：WSL gcc/clang/MSVC focused 新 parser 15 PASS、新 LSP 15 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Multiple Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:27 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 multiple same-loop-written 多写：plan collector 现在允许同 target 前缀既可以是 stable self-dependent step，也可以是已标记 replay-resolved 的 same-loop-written self-dependent step，从而支持 `step = 2; narrowed = narrowed + step; narrowed = narrowed + step`。replay 逐个执行 replay-resolved step，并在临时 scope 中解析同一个更早 supported assignment 提供的 delta，最终把两个 `+step` 合成后 widen 为 `5..INT64_MAX`。不声明 target-reading 与 replay-resolved delta 的组合、更复杂链式 same-loop delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser multiple same-loop-written delta case 返回 `max=5` 而非 `INT64_MAX`，LSP local query 同样返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 14 PASS、新 LSP 14 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sequence Same-Loop-Written Delta Replay | 已完成 | 2026-06-24 02:06 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 same-loop-written 多写：plan collector 可在一个同 target stable self-dependent sequence 后接受 replay-resolved same-loop-written delta step，只要该 delta 依赖更早 supported replay assignment；replay 在执行 `step = 2` 后解析 `narrowed = narrowed + step` 的 delta，并把前置 `+1` 与该 delta 合成后一次 widen。`narrowed = narrowed + 1; narrowed = narrowed + step` 在 unknown-condition `while` 后让 `narrowed` 为 `5..INT64_MAX`。不声明 target-reading interleave 与 replay-resolved delta 的组合、链式/多重 same-loop delta dependencies、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser sequence same-loop-written delta case 返回 `max=5` 而非 `INT64_MAX`。GREEN：WSL gcc/clang/MSVC focused 新 parser 13 PASS、新 LSP 13 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Negative Multi-Write Delta Replay | 已完成 | 2026-06-24 01:39 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading net-negative 多写：plan collector 可穿过 `other = narrowed` 这类读取 sequence target 的普通 assignment，但仅限 combined stable same-target delta 仍是 supported self-dependent recurrence；replay 对读取者写入第一轮 `source + prefix` 与后续轮次 `widen(source,totalDelta) + prefix` 的 envelope。`narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 2` 在 unknown-condition `while` 后让 `narrowed` 为 `INT64_MIN..5`，并把 `other` 精化为 `INT64_MIN..6`。不声明 target-reading positive/sign-crossing fixture、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading interleaved net-negative case 返回 `min=4` 而非 `INT64_MIN`，LSP 初次实现验证暴露查询 offset / fact kind 测试边界。GREEN：WSL gcc/clang/MSVC focused 新 parser 12 PASS、新 LSP 12 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Target-Reading Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 01:10 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-reading interleave net-zero 多写：plan collector 可穿过 `other = narrowed` 这类读取 sequence target 的普通 assignment，但仅限 previous stable same-target delta 加当前 delta 精确为 `0..0`；replay 对这种序列先用 source range 加已累计 delta 生成中间 target binding，再执行读取者，最后提交净零 target range。`narrowed = narrowed + 1; other = narrowed; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `narrowed` 为 `5..5`，并把 `other` 精化为 `0..6`。不声明 target-reading non-zero recurrence、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser target-reading interleaved net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 final target `5..9223372036854775806` 与 observer `0..9223372036854775807`。GREEN：WSL gcc/clang/MSVC focused 新 parser 11 PASS、新 LSP 11 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Interleaved Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:49 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 target-independent interleave net-zero 多写：plan collector 可穿过普通 `other = 10` 这类不读取 sequence target 的 assignment 来延续更早同 target stable self-dependent step；replay 在合并 same-target signed deltas 时执行这些中间 assignment。`narrowed = narrowed + 1; other = 10; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `5..5`。不声明读取 `narrowed` 的 interleave、非零多写 recurrence 精度、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser interleaved positive-then-negative net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 `5..9223372036854775806`。GREEN：WSL gcc/clang/MSVC focused 新 parser 10 PASS、新 LSP 10 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Adjacent Net-Zero Multi-Write Delta Replay | 已完成 | 2026-06-24 00:31 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的相邻同 target net-zero 多写：plan collector 支持当前 step 延续 immediately previous stable self-dependent same-target step；replay 对连续同 target stable self-dependent sequence 先合并 signed delta range，再从序列前 source binding 做一次 widen。`narrowed = narrowed + 1; narrowed = narrowed - 1` 在 unknown-condition `while` 后保持 `5..5`。不声明非相邻/穿插其他 target 的 arbitrary multi-write recurrence、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser positive-then-negative net-zero case 返回 `max=9223372036854775806`，LSP 新目标返回 `5..9223372036854775806`。GREEN：WSL gcc/clang/MSVC focused 新 parser 9 PASS、新 LSP 9 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Prefix Positive Delta Widening | 已完成 | 2026-06-24 00:02 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-only prefix 后续 delta：plan collector 现在区分“已经写过 target”与“此前 target 写入全是 preserving no-op”，因此 `narrowed = narrowed + 0; narrowed = narrowed + 1` 在 unknown-condition `while` 后可 widen 为 `5..INT64_MAX`。同时拆出 `type_inference_loop_assignment_replay.c` 承担 replay 执行与 result 存储，主 join helper 降到 815 行。不声明任意同 target 多写、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-only-prefix positive case 返回 `max=6`，LSP 新目标返回 `5..6`。GREEN：WSL gcc/clang/MSVC focused 新 parser 8 PASS、新 LSP 8 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Only Delta No-Op Replay | 已完成 | 2026-06-23 23:28 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-only no-op delta：self-dependency helper 现在接受 `0..0` delta，widen helper 对该形状复制 source type，使 no-op self-dependent assignment 不阻断同一 replay plan 中其他 assignment 的 zero-or-more join。`narrowed = narrowed + 0; other = 10` 在 unknown-condition `while` 后让 `other + 0` 得到 `0..10`。不声明链式/多写同循环 delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-only no-op case 返回 `max=0`。GREEN：WSL gcc/clang/MSVC focused 新 parser 7 PASS、新 LSP 7 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Expression Delta Widening | 已完成 | 2026-06-23 23:17 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的同循环写入 expression delta：deferred self-dependent 检测现在抽取整个 delta expression，确认其中 loop-written target 都是 earlier assignment 后，延迟到 replay scope 中解析。`step = 2; narrowed = narrowed + (step + 1)` 在 unknown-condition `while` 后 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、链式/多写同循环 delta、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser same-loop-written expression delta case 返回 `max=5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 6 PASS、新 LSP 6 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Same-Loop Written Stable Delta Widening | 已完成 | 2026-06-23 22:54 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的同循环写入 direct delta：loop assignment plan 现在可标记需要 replay 时解析的 self-dependent delta，允许更早 supported assignment 写入 `step` 后再解析 `narrowed = narrowed + step`。`step = 2; narrowed = narrowed + step` 在 unknown-condition `while` 后 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、复杂同循环 delta 依赖链、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser same-loop-written delta case 返回 `max=5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 5 PASS、新 LSP 5 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Sign-Crossing Delta Widening | 已完成 | 2026-06-23 22:40 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 sign-crossing delta：self-dependency helper 现在接受 signed int64 `min < 0 && max > 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `-3..3` / `u8 - 128` 时 widen 为 `INT64_MIN..INT64_MAX`。不声明 zero-only no-op、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser sign-crossing case 返回 `min=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 4 PASS、新 LSP 4 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Negative Delta Widening | 已完成 | 2026-06-23 22:25 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-inclusive negative delta：self-dependency helper 现在接受 signed int64 `min < 0 && max <= 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `-3..0` / `0 - u8` 时 widen 为 `INT64_MIN..5`。不声明 zero-only no-op、sign-crossing delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-inclusive negative case 返回 `min=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 3 PASS、新 LSP 3 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Zero-Inclusive Positive Delta Widening | 已完成 | 2026-06-23 22:14 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的 zero-inclusive positive delta：self-dependency helper 现在输出 signed delta min/max，接受 int64 `min >= 0 && max > 0` 的稳定 delta 表达式；`narrowed = narrowed + step` 在 `step` 为 `0..3` / `u8` 时 widen 为 `5..INT64_MAX`。不声明 zero-only no-op、negative/sign-crossing delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser zero-inclusive case 返回 `max=5`，LSP 新目标返回 `5..5`；RED 前测试图重新生成被无关 AOT typed-call 可选拆分源缺失阻断，CMake 目标已改为存在时才追加这些拆分源。GREEN：WSL gcc/clang/MSVC focused 新 parser 2 PASS、新 LSP 2 PASS/checks；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive Expression Delta Widening | 已完成 | 2026-06-23 21:51 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的保守表达式 delta：self-dependency helper 从 `target +/- delta` 中抽取 delta 表达式，拒绝 target/self plan 依赖与 member/call/unsupported 形状，并复用表达式推断取得 int64 正下界。`narrowed = narrowed + (step + 1)` 在 `step` 为 `1..3` 时 widen 为 `5..INT64_MAX`。不声明 0/负 delta、同循环写入目标依赖、unsupported/member/call delta、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc 新 parser 目标返回 `max=5`，LSP 新目标返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused 新 parser 1 PASS、新 LSP 1 PASS；adjacent loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Positive-Range Delta Widening | 已完成 | 2026-06-23 21:05 +08:00 | 按阶段 2 数值区间路线补 loop recurrence blueprint 的保守正区间 delta：self-dependency helper 接受 int64 positive-lower-bound identifier delta，并继续拒绝同 loop 内写入的 delta target；`narrowed = narrowed + step` 在 `step` 为 `1..3` 时 widen 为 `5..INT64_MAX`。不声明含 0/负数 delta、复杂 delta expression、multiplicative/conditional recurrence、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 返回 `max=5`，LSP 返回 `5..5`。GREEN：WSL gcc/clang/MSVC focused loop assignment 16 PASS、LSP branch assignment 25 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True Step Known-True-If Break-Branch At-Least-Once Range Join | 已完成 | 2026-06-23 20:47 +08:00 | 按阶段 2 数值区间/逻辑事实路线补 traditional `for` step-before-break 的 deterministic-if 执行蓝图：在 loop entry 侧用已有 bool 常量/逻辑事实 evaluator 证明 `if (true)` then 分支必走，nested-if target collector 允许 missing `else`，branch assignment replay 对已知条件只提交可达分支。`for (; true; narrowed = 20) { if (true) { narrowed = 10; break; } } narrowed + 1` 现在得到 `11..11`。不声明 symbolic if、unknown missing-else、continue、break with expression、no-break loop、复杂 nested body、fixed point 或 general CFG-wide numeric dataflow。 | RED：WSL gcc parser 先失败 `Expected 11 Was 6`，LSP 返回 `6..6`。GREEN：WSL gcc/clang/MSVC focused constant-condition parser 16 PASS、LSP constant-condition 16 checks；key adjacent loop assignment 15 PASS、LSP branch-assignment 24 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step Nested-If Break-Branches At-Least-Once Range Join | 已完成 | 2026-06-23 20:24 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` step-before-break 的窄化 body-control 切片：loop syntax helper 新增 `ZrParser_TypeInferenceLoopAssignment_StatementGuaranteesPlainBreak`，entry helper 用它识别顶层 `if/else` then/else 都 plain `break;` 的 step-skipping 证明；body plan collector 在收集 nested-if assignment 后停止后续 replay。`for (; true; narrowed = 20) { if (flag) { narrowed = 10; break; } else { narrowed = 12; break; } } narrowed + 1` 返回 `11..13`。该切片不声明 missing else、branch without break、continue、break with expression、无 break 的 known-true loop、复杂 nested control flow、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser nested-if break-branches 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 15 PASS 与 LSP constant-condition 15 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init-Step Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:59 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` init+step/header-local+step 的 step-before-break 切片：entry helper 复用 for-init 临时 scope；当 known-true/omitted condition 且 body 顶层 plain `break;` 证明 step 被跳过时，supported assignment init 与 direct identifier `var` init 都可和 step 共存，并通过 at-least-once body replay 发布后继事实。assignment-init+step true/omitted 与 var-init+step true/omitted 四类 fixture 均返回 `11..11`。该切片不声明 nested break、continue、break with expression、无 break 的 known-true loop、复杂 body control flow、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 四个新用例先失败为 `Expected 11 Was 6`，LSP 四个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 14 PASS 与 LSP constant-condition 14 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Step-Before-Break At-Least-Once Range Join | 已完成 | 2026-06-23 19:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` step-before-break 切片：`ZrParser_TypeInferenceLoopAssignment_StatementIsPlainBreak` 被抽到 loop syntax 模块，join collector 和 entry helper 共用它；entry 只在 no-init、known-true/omitted condition、body 顶层 plain `break;` 会使 step 不执行时，允许 step 存在并调用 at-least-once body replay。`for (; true; narrowed = 20) { narrowed = 10; break; }` 与 omitted 变体后续表达式返回 `11..11`。该切片不声明 init+step/header-local+step、nested break、continue、break with expression、无 break 的 known-true loop、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 两个 step fixture 先失败为 `Expected 11 Was 6`，LSP 两个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 10 PASS 与 LSP constant-condition 10 checks；key adjacent loop assignment 15 PASS 与 LSP branch assignment 24 PASS 通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Init Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 19:10 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的 init/header-local 切片：entry helper 复用 for-init 临时 scope，`bodyRunsAtLeastOnce` 分支在无 step 时调用 `JoinBodyAtLeastOnce`，所以 supported assignment init 和 direct identifier `var` init 都能先进入 replay scope，再让 break 前 body assignment 产生精确后继事实。assignment-init true/omitted 与 var-init true/omitted 四类 fixture 均返回 `11..11`。该切片不声明 step、combined var-init+step、destructuring/no-range header binding、复杂 break/continue CFG、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser 四个新用例先失败为 `Expected 11 Was 6`，LSP 四个 query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 8 PASS 与 LSP constant-condition 8 checks；相邻 loop/for-header/nested/foreach/LSP branch assignment 回归通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For True/Omitted Condition Break-Prefix At-Least-Once Range Join | 已完成 | 2026-06-23 18:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional `for` known-true/omitted condition 的窄切片：无 init、无 step 时，known true 或省略 condition 的 `for` 会复用 at-least-once body replay；plain `break;` 被 loop replay collector 当作前缀终止点，因此 break 前 supported assignment 可用于后续事实。`for (; true; ) { narrowed = 10; break; } narrowed + 1` 与 `for (;;) { narrowed = 10; break; } narrowed + 1` 返回 `11..11`。该切片不声明 init/step/header-local binding 下的 known-true/omitted forms、复杂 break/continue CFG、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser true/omitted 用例先失败为 `Expected 11 Was 6`，LSP query 返回 `6..6`。GREEN：WSL gcc/clang/Windows MSVC focused 通过 constant-condition parser 4 PASS 与 LSP constant-condition 4 checks；相邻 loop/for-header/nested/foreach/LSP branch assignment 回归通过；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Var-Init Scoped Binding | 已完成 | 2026-06-23 18:17 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 LSP traditional `for` var-init 作用域隔离：`semantic_analyzer_typecheck.c` 对 `ZR_AST_FOR_LOOP` 的 variable-declaration init 推入临时 type binding scope，让 init/condition/step/body 可见 header-local binding，并在 pop 后再运行 parser-owned numeric assignment join。`for (var step: int = 10; false; ) { narrowed = step; } step + 1` 不再把 `step` 的 `10..10` 泄漏成后续 `11..11`。该切片不声明 destructuring/no-range 声明、initializer side effects、known-true/omitted conditions、fixed point 或 general CFG-wide dataflow。 | RED：WSL gcc parser coverage 首跑通过但 LSP no-leak query 失败为 `11..11`。GREEN：WSL gcc/clang focused+adjacent 通过 constant-condition parser 2 PASS、LSP constant-condition 2 checks、loop assignment 15 PASS、for-header 1 PASS、branch-assignment 24 PASS；Windows MSVC 通过对应 focused/adjacent 目标。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Nonempty Cardinality Range Join | 已完成 | 2026-06-23 17:52 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 `foreach` 非空数组 cardinality：direct identifier item binding 复用 iterable type 推断，数组 size constraint 可证明非空时走 at-least-once body join，未知长度数组继续走 zero-or-more join。`items: int[2]` 的 `foreach` body assignment 后续表达式返回 `11..11`，`int[]` 仍返回 `6..11`。该切片不声明 destructuring、非数组 iterable、动态长度证明、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED-GATE：focused parser/LSP 用例已写好，但实现前 RED 执行被 gcc build 占用和 clean build 超时阻断。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric foreach cardinality 2 PASS 与 LSP numeric foreach cardinality query 2 PASS；WSL gcc/clang 相邻语义回归通过 numeric loop assignment、nested loop、constant-condition、for header 与对应 LSP query。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Loop Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 17:15 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 nested loop replay 边界：外层 loop body 里的 nested `while`/`for`/`foreach` 会作为 replay step，复用内层 loop 的现有 bounded numeric assignment join，再把受影响 target 写回外层 replay result。`while (flag) { while (inner) { narrowed = 10; } } narrowed + 1` 返回 `6..11`。新增 nested-loop helper 并把 replay scope helper 移到 scope 模块，主 join helper 保持 982 行。该切片不声明真正 loop fixed point、known-true/omitted condition、unsupported statements/assignments、复杂 recurrence 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP 新目标返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric nested loop assignment 1 PASS 与 LSP numeric nested loop assignment query 1 PASS；WSL gcc 相邻语义回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、numeric for header assignment 1 PASS、LSP numeric loop constant-condition query 1 PASS、LSP numeric for header assignment query 1 PASS。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Combined Init-Step Assignment Coverage | 已完成 | 2026-06-23 16:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for 的 combined header replay 覆盖：显式 unknown condition 下，assignment init 先写入临时 scope，body+assignment step replay 再从该 scope 继续执行。`for (narrowed = 1; flag; narrowed = 10) { flag; } narrowed + 1` 返回 `2..11`，覆盖零次迭代 init `1` 与一次或多次迭代 step `10`。本轮是覆盖确认，未改生产代码；不声明 known-true/omitted condition、false-condition variable-declaration init、nested loops、true fixed point、step side-effect 精化或 general CFG-wide dataflow。 | COVERAGE-GATE：新建 parser/LSP focused 目标首跑通过。GREEN：WSL gcc、WSL clang、Windows MSVC focused 均通过 numeric for header assignment 1 PASS 与 LSP numeric for header assignment query 1 PASS；WSL gcc 相邻语义回归通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；`git diff --check` 通过，只有既有 LF/CRLF warning。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Constant-False Condition Range Application | 已完成 | 2026-06-23 16:06 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 constant-false condition replay 边界：known-false init shortcut 现在消费 bool literal、condition logical fact 和 AST constant boolean fallback。`for (narrowed = 1; 1 == 2; ) { narrowed = 10; } narrowed + 1` 返回 `2..2`，只保留 init assignment，不纳入 pre-loop 或 body path。该切片不声明 known-true/omitted condition、false-condition variable-declaration init、combined init+step、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 11`，LSP 新目标返回 `2..11`。GREEN：WSL gcc/clang focused 与 Windows MSVC focused 通过 numeric loop assignment 15 PASS、numeric loop constant-condition 1 PASS、LSP numeric branch assignment query 24 PASS、LSP numeric loop constant-condition query 1 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Non-Assignment Step Zero-Or-More Range Join | 已完成 | 2026-06-23 15:40 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 non-assignment step replay 边界：显式 unknown condition 的 `for (; flag; flag)` 现在把非赋值 step 当作不写入 loop target 的普通 expression，不再阻断 body-only zero-or-more join。`for (; flag; flag) { narrowed = 10; } narrowed + 1` 返回 `6..11`。该切片不声明 step side-effect 精化、nested loops、true fixed point、unsupported non-expression statements 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 15 PASS、LSP numeric branch assignment query 24 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 15/24 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For False-Condition Init Assignment Range Application | 已完成 | 2026-06-23 15:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补 traditional for known-false condition 的 init assignment 保留：condition fact 为 known false 时，entry helper 只应用 supported assignment-expression init 并跳过 body/step replay。`for (narrowed = 1; false; ) { narrowed = 10; } narrowed + 1` 返回 `2..2`，不再保留循环前 `5` 或进入 unreachable body 的 `10`。该切片不声明 known-true/omitted condition、variable-declaration init known-false leakage、combined init+step、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`。GREEN：WSL gcc focused 通过 numeric loop assignment 14 PASS、LSP numeric branch assignment query 23 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 14/23 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Var-Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 15:05 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的 variable-declaration init replay：`type_inference_loop_assignment_entry.c` 现在把直接 identifier `var` init 转成临时 replay scope 的 loop-local numeric binding，body/step replay 可读取该 binding，提交 joined scope 时跳过该 loop-local 名称。`for (var step: int = 10; flag; ) { narrowed = step; } narrowed + 1;` 返回 `6..11`，覆盖零次迭代的 pre-loop `5` 与一次或多次迭代读取 `step=10` 的路径。该切片不声明 destructuring var init、无 initializer/range 声明、omitted/constant condition、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 13 PASS、LSP numeric branch assignment query 22 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 13/22 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Item-Dependent Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 14:43 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach item-dependent loop assignment replay：`foreach` 入口 helper 现在先从 iterable 推断 element type，在临时 replay scope 中注册直接 identifier item，再运行已有 zero-or-more loop body join。提交 joined scope 时会跳过 item binding，只把 numeric assignment targets 写回外层环境。`for (var item in items) { narrowed = item; } narrowed + 1;` 在 `items` element range 为 `2..4` 时返回 `3..6`，LSP `u8[]` fixture 返回 `1..256`。该切片不声明 destructuring pattern、显式 foreach type annotation 专用路径、iterable cardinality、nested loops、true fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 3 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc focused 通过 numeric loop assignment 12 PASS、LSP numeric branch assignment query 21 PASS；WSL clang focused 与 Windows MSVC focused 通过同两目标 12/21 PASS；WSL gcc 相邻语义回归通过。不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Singleton Delta Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 14:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS：unknown-condition `while` 的 first-write positive increment widening 现在可把稳定的 int64 singleton 标识符当作 delta。`while (flag) { narrowed = narrowed + step; }` 中 `step` 若是非 target、未被同一 loop replay plan 写入、且当前 binding 为 `1..1` 这类正单点区间，则后续 `narrowed + 0` 返回 `5..9223372036854775807`。实现只扩展 `type_inference_loop_assignment_self_dependency.*` 和 loop plan validation，不扩大到 full fixed-point dataflow；non-singleton/non-identifier delta、复杂 recurrence、nested loops、for/foreach 递推与 general CFG-wide dataflow 仍待后续。 | RED：WSL gcc parser/LSP 新 fixture 分别停在 `max=5`。GREEN：WSL gcc focused 通过 numeric loop assignment 11 PASS、LSP numeric branch assignment query 20 PASS；WSL clang AOT-off focused 与 Windows MSVC focused 通过同两目标 11/20 PASS；WSL gcc 相邻语义回归通过。AOT-on WSL clang rebuild 被无关 dirty-tree AOT `typeTableIndex` 编译错误阻断；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Step Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 13:35 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 step assignment replay：无 init、显式 unknown condition 的 `for (; flag; target = value)` 会把 body 与 step assignment 顺序放入同一个 replay plan，再复用 zero-or-more join。`for (; flag; narrowed = 10) { flag; } narrowed + 1;` 返回 `6..11`，覆盖零次迭代路径与一次或多次完成迭代后的 step 赋值路径。实现同时把当前 scope binding 写入 helper 拆到 `type_inference_loop_assignment_scope.c`，主 loop helper 保持 952 行。该切片不声明 variable-declaration init、combined init+step 覆盖、omitted/constant condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：numeric loop assignment 10 PASS、LSP numeric branch assignment query 19 PASS；WSL gcc 相邻回归通过：numeric branch assignment 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Init Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 12:53 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 init assignment replay：无 step、显式 unknown condition 的 `for (target = value; flag; )` 会先把 assignment init 的 numeric range 写入临时 scope，再复用共享 loop-body zero-or-more join。`for (narrowed = 1; flag; ) { narrowed = 10; } narrowed + 1;` 返回 `2..11`，覆盖 init 后零次迭代路径与 body 一次或多次执行路径。该切片不声明 variable-declaration init、for step、omitted/constant condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 9 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 18 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Foreach Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 foreach 的受限 loop assignment replay：新增 `ZrParser_TypeInference_TryJoinForeachNumericAssignments`，parser foreach expression inference 与 LSP foreach typecheck 都在外层 type environment 上调用共享 zero-or-more join body。`for (var item in items) { narrowed = 10; } narrowed + 1;` 返回 `6..11`。实现同时把 while/for/foreach loop-kind 入口拆到 `type_inference_loop_assignment_entry.c`，主 replay/join helper 降到 802 行。该切片不声明 item-dependent RHS、iterable cardinality 精化、for init/step/constant-condition、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser foreach 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 8 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 17 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric For Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 07:14 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 traditional for 的受限 loop assignment replay：`type_inference_loop_assignment_join.c` 现在把共享 replay/join body 同时供 while 和 `for (; flag; )` 使用，`ZrParser_TypeInference_TryJoinForNumericAssignments` 只接收无 init、显式 unknown condition、无 step 的 `ZR_AST_FOR_LOOP`。parser for 推断与 LSP for typecheck 接入后，`narrowed=5; for (; flag; ) { narrowed = 10; } narrowed + 1;` 返回 `6..11`。实现同时把 parser loop assignment 覆盖拆到新的 `test_numeric_loop_assignment_dataflow.c`，避免继续扩张 branch assignment 测试。该切片不声明 for init/step dataflow、无条件或常量条件 for、foreach、嵌套 loop、非字面量 recurrence、真正 loop fixed point、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser loop target 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric loop assignment 7 PASS、numeric branch assignment 9 PASS、LSP numeric branch assignment query 16 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Decrement Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 05:37 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS 边界：unknown-condition while 的 focused helper 现在可识别首次写入 target 的 `target = target - positive_integer_literal`，把一次或多次 loop-body 结果保守 widened 到 int64 下界，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed - 1; } narrowed + 0;` 返回 `-9223372036854775808..5`。实现复用 `type_inference_loop_assignment_self_dependency.*` 的 signed delta 分支，主 loop helper 保持只接入 plan 标记与 replay 分支。该切片不声明非字面量 delta、`literal - target`、乘法/条件 recurrence、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：Windows MSVC parser 先失败为 `Expected -9223372036854775808 Was 5`；LSP 新用例首次运行暴露 query fixture occurrence 参数错误，修正后用于 GREEN 覆盖。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 15 PASS、LSP numeric branch assignment query 15 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Self-Dependent Increment Assignment Zero-Or-More Range Widening | 已完成 | 2026-06-23 04:31 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 self-dependent loop RHS 边界：unknown-condition while 的 focused helper 现在可识别首次写入 target 的 `target = target + positive_integer_literal`，把一次或多次 loop-body 结果保守 widened 到 int64 上界，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { narrowed = narrowed + 1; } narrowed + 0;` 返回 `5..9223372036854775807`。实现新增 `type_inference_loop_assignment_self_dependency.*`，主 loop helper 只接入 plan 标记与 replay 分支。该切片不声明自减、非字面量 delta、乘法/条件 recurrence、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 9223372036854775807 Was 5`，LSP assignment query 返回 `min=5 max=5`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 14 PASS、LSP numeric branch assignment query 14 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Nested If Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 04:01 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内受限嵌套分支赋值合流：unknown-condition while 的 focused helper 现在可把 nested simple `if/else` 作为 replay step，在 loop replay scope 中复用既有 branch assignment join，再与 pre-loop binding 做 zero-or-more join。`narrowed=5; while (flag) { if (inner) { narrowed = 1; } else { narrowed = 10; } } narrowed + 1;` 返回 `2..11`。同时拆出 `type_inference_loop_assignment_syntax.*` 与 `type_inference_loop_assignment_nested_if.*`，主 helper 回到 878 行。该切片不声明 self-dependent RHS、嵌套 loop、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 13 PASS、LSP numeric branch assignment query 13 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Statement Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:29 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内赋值合流：unknown-condition while 的 focused helper 现在可跳过普通 expression statement，只 replay simple numeric assignment。`narrowed=5; while (flag) { flag; narrowed = 10; flag; } narrowed + 1;` 返回 `6..11`。该切片不声明 self-dependent RHS、嵌套 loop/if loop body、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected 11 Was 6`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 12 PASS、LSP numeric branch assignment query 12 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Multi-Target Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 03:08 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补 loop 内跨目标赋值合流：unknown-condition while 的 focused helper 现在可顺序 replay 多个 simple numeric assignments，并把每个目标的 loop-body replay range 与进入循环前 range 合流。`low=5, high=20; while (flag) { low = 1; high = low + 10; } high + low;` 返回 `12..25`。该切片不声明 self-dependent RHS、真正 loop fixed point、for/foreach、unsupported statements/assignments 或 general CFG-wide dataflow。 | RED：新增 LSP assignment query 先返回 `min=25 max=25`；parser 对应 RED 覆盖在共享 checkout 中曾被无关 AOT 编译错误打断。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 11 PASS、LSP numeric branch assignment query 11 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric While Assignment Zero-Or-More Range Join | 已完成 | 2026-06-23 02:42 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：parser `ZR_AST_WHILE_LOOP` 和 LSP while typecheck 现在共用 `type_inference_loop_assignment_join.c`，对 unknown-condition while 中唯一 simple integer assignment 做进入前 range 与循环体 RHS range 的 zero-or-more join。`narrowed = 5` 后 `while (flag) { narrowed = 10; } narrowed + 1` 返回 `6..11`。该切片不声明 loop fixed point、self-dependent RHS、for/foreach、多目标循环或 general CFG-wide dataflow。 | RED：WSL gcc parser 先失败为 `Expected TRUE Was FALSE`，LSP assignment query 返回 `min=6 max=6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 10 PASS、LSP numeric branch assignment query 10 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、CFG reachability 29 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nested Branch Assignment Dataflow | 已完成 | 2026-06-23 01:00 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple branch assignment replay 现在能处理 branch block 内 bounded nested simple `if/else`，先在临时 branch scope 中 join 嵌套分支写入，再把当前 plan target binding 存回外层 branch result。`narrowed = 1/2` 与 `narrowed = 10/20` 的嵌套 if/else 后，`narrowed + 1` 在 parser 与 LSP local semantic query 中返回 `2..21`。general CFG-wide numeric dataflow、loop/fixed-point、任意嵌套控制流、unsupported assignments 与任意 symbolic range-set/hole-set 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 1`，LSP assignment query 返回 `min=1 max=1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 9 PASS、LSP numeric branch assignment query 9 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Segment-Preserving Branch Assignment Join | 已完成 | 2026-06-23 00:42 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple branch assignment join 现在会保留两侧分支写入产生的不连续 segment payload，`narrowed = 1/10` 后的 `narrowed + 1` 在 parser 与 LSP local semantic query 中返回 envelope `2..11` 与 segments `2..2` / `11..11`。LSP assignment query 覆盖已从 branch refinement 文件拆到独立 assignment 目标。general CFG-wide numeric dataflow、loop/fixed-point、跨嵌套控制流与任意 symbolic range-set/hole-set 仍不声明完成。 | RED：WSL gcc parser 先失败为 `Expected 2 Was 0`，LSP assignment query 返回 `exprSegments=0` / `numeric segments=0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 8 PASS、LSP numeric branch assignment query 8 PASS、LSP numeric branch refinement query 16 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Target Branch Assignment Dataflow | 已完成 | 2026-06-23 00:09 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补跨块赋值合流：simple if/else 分支块现在允许多个 simple numeric assignment target，并在临时 branch scope 中按顺序重放 target 写入，让同一分支内的后续 target RHS 可读取前置 target range。`if (flag) { low = 1; high = low + 1; } else { low = 10; high = low + 10; } high + low;` 现在返回 `3..30`。general CFG-wide numeric dataflow、loop/fixed-point、跨嵌套控制流与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 multi-target 用例失败为 `Expected 3 Was 0`，LSP query 只返回 `0..0`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 7 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 23 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric RHS-Dependent Sequential Branch Assignment Range Join | 已完成 | 2026-06-22 23:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许同一 target 的顺序 simple assignments，并在临时 branch scope 中重放前置写入，让 RHS 依赖前置写的后一条 assignment 可读取分支内 range。`if (flag) { narrowed = 1; narrowed = narrowed + 1; } else { narrowed = 10; narrowed = narrowed + 1; } narrowed + 1;` 现在返回 `3..12`。多 target/多变量 branch dataflow、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 RHS-dependent 用例失败为 `Expected 3 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 6 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 22 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Nonterminal Multi-Statement Branch Assignment Range Join | 已完成 | 2026-06-22 23:21 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许后置普通表达式，只要块内只有一个同名数值 simple assignment，就把两侧 RHS range 合并。`if (flag) { narrowed = 1; flag; } else { narrowed = 10; flag; } narrowed + 1;` 现在返回 `2..11`。多次写、RHS 依赖前置写、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 nonterminal multi-statement 用例失败为 `Expected 2 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 5 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 21 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Multi-Statement Terminal Branch Assignment Range Join | 已完成 | 2026-06-22 23:04 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if/else 分支块允许前置普通表达式，只要块末尾是同名数值 simple assignment，就把两侧 RHS range 合并。`if (flag) { flag; narrowed = 1; } else { flag; narrowed = 10; } narrowed + 1;` 现在返回 `2..11`。assignment 不在末尾、多次写、RHS 依赖前置写、general CFG-wide numeric dataflow、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 multi-statement 用例失败为 `Expected 2 Was 1`，LSP query 只返回 `1..1`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 4 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 20 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Else-Only Branch Assignment Range Join | 已完成 | 2026-06-22 22:44 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：explicit else-only simple assignment 会把 else RHS range 与 true path 保留的进入前 binding range 合并。`var narrowed: int = 5; if (flag) { flag; } else { narrowed = 10; } return narrowed + 1;` 现在返回 `6..11`。general CFG-wide numeric dataflow、multi-statement、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 else-only 用例失败为 `Expected 11 Was 6`，LSP query 只返回 `6..6`。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 3 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 19 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric One-Sided Branch Assignment Range Join | 已完成 | 2026-06-22 22:26 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向继续补最小跨块赋值合流：simple if true-branch 单侧赋值会把 then RHS range 与 false path 保留的进入前 binding range 合并；显式类型注解的数值变量声明现在也保留 initializer 单点范围，供 parser/LSP 后续查询使用。`var narrowed: int = 5; if (flag) { narrowed = 10; } return narrowed + 1;` 现在返回 `6..11`。general CFG-wide numeric dataflow、multi-statement、else-only、loop/fixed-point 与 segment-preserving assignment join 仍不声明完成。 | RED：WSL gcc parser 新 if/then 用例失败为 `Expected TRUE Was FALSE`，LSP query 无 range。GREEN：WSL gcc、WSL clang、Windows MSVC focused/adjacent 目标通过：numeric branch assignment 2 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 18 PASS、LSP logical query 3 PASS、LSP reachability query 9 PASS、LSP semantic query diagnostics 11 PASS；不声明全仓库 GREEN。 |
| 阶段 2 / Definite Assignment Declaration Related Information | 已完成 | 2026-06-22 21:36 +08:00 | 按阶段 2 的 definite-assignment 与 02 §1/§2 诊断 related 方向补一层最小闭环：结构化诊断拥有 `relatedInformation` 数组，未初始化/可能未初始化 read 会把 declarationRange 作为 `Variable declaration is here` 关联位置，LSP 诊断转换保留并发布该信息；LSP semantic query diagnostics 在 AST 可用时先跑 CFG-backed definite assignment，让 branch join 场景能发布 `possibly_uninitialized_read`。此切片只覆盖 DA declaration relatedInformation；fix-it、descriptorId、诊断码注册表、所有权 related 与 compiler 外部/二进制诊断通道仍不声明完成。 | RED：parser/LSP relatedInformation 断言先失败，LSP branch fixture 初始无 query diagnostic；GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：semantic query 14 PASS、LSP semantic query diagnostics 11 PASS、LSP local semantic query 19 PASS、numeric branch assignment 1 PASS、LSP numeric branch semantic query 17 PASS。compiler semantic query diagnostics 本轮因 typed metadata export 阶段 invalid `exportedVar->name` segfault 未作为 GREEN 证据；不声明全仓库 GREEN。 |
| 阶段 2 / Numeric Cross-Block Branch Assignment Range Join | 已完成 | 2026-06-22 21:03 +08:00 | 按阶段 2 的“数值区间 + 逻辑事实”方向补一层最小跨块赋值合流：parser 和 LSP 共用 `type_inference_branch_assignment_join.c`，仅对 simple if/else 中两侧同名数值赋值做 range envelope join，并把 join 后 binding 暴露给后续表达式推断。新增 parser/LSP focused 覆盖，`narrowed = 1/10` 后的 `narrowed + 1` 变为 `2..11`。general CFG-wide numeric dataflow、loop fixed point、多语句分支和 one-sided branch 仍不声明完成。 | RED：WSL gcc parser 新用例先失败为 `Expected 2 Was 1`，LSP 新用例没有 range fact。GREEN：WSL gcc、WSL clang、Windows MSVC focused 目标通过：numeric branch assignment 1 PASS、numeric range 10 PASS、numeric branch refinement 15 PASS、numeric branch segment sets 3 PASS、semantic query 14 PASS、compiler semantic query diagnostics 16 PASS、LSP local semantic query 19 PASS、LSP numeric semantic query 4 PASS、LSP numeric branch semantic query 17 PASS、LSP semantic query diagnostics 10 PASS；不声明全仓库 GREEN。 |
