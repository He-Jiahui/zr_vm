---
related_code:
  - zr_vm_common/include/zr_vm_common/zr_aot_abi.h
  - zr_vm_library/src/zr_vm_library/aot_runtime.c
  - zr_vm_aot/zr_vm_library/src/zr_vm_library/aot_runtime.c
  - zr_vm_core/include/zr_vm_core/metadata_token.h
  - zr_vm_core/include/zr_vm_core/zrp_metadata.h
  - zr_vm_core/include/zr_vm_core/metadata_runtime.h
  - zr_vm_core/src/zr_vm_core/metadata_runtime.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_generic_params.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_type_node_binding.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_binding_compatibility.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_instance.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_argument_internal.h
  - zr_vm_core/src/zr_vm_core/reflection_generic_argument_object.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method_native.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method_native_internal.h
  - zr_vm_core/src/zr_vm_core/reflection_module.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method_object.c
  - zr_vm_core/src/zr_vm_core/reflection_object_internal.c
  - zr_vm_core/src/zr_vm_core/reflection_object_internal.h
  - zr_vm_core/src/zr_vm_core/reflection_generic_type_object.c
  - zr_vm_core/src/zr_vm_core/reflection_interpreter_generic_instance.c
  - zr_vm_core/include/zr_vm_core/reflection.h
  - zr_vm_core/include/zr_vm_core/hash.h
  - zr_vm_core/include/zr_vm_core/function.h
  - zr_vm_core/include/zr_vm_core/io.h
  - zr_vm_common/include/zr_vm_common/zr_io_conf.h
  - zr_vm_core/src/zr_vm_core/function.c
  - zr_vm_core/src/zr_vm_core/function_graph.c
  - zr_vm_core/src/zr_vm_core/function_metadata_binding.c
  - zr_vm_core/src/zr_vm_core/function_metadata_query.c
  - zr_vm_core/src/zr_vm_core/reflection.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_nested.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_nested.h
  - zr_vm_core/src/zr_vm_core/reflection_field_value_primitive.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_primitive.h
  - zr_vm_core/src/zr_vm_core/reflection_token_resolve.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_instance.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_binding_compatibility.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_layout_binding.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_method_binding.c
  - zr_vm_core/src/zr_vm_core/hash.c
  - zr_vm_core/src/zr_vm_core/io.c
  - zr_vm_core/src/zr_vm_core/io_runtime.c
  - zr_vm_core/src/zr_vm_core/module/module_internal.h
  - zr_vm_core/src/zr_vm_core/module/module.c
  - zr_vm_core/src/zr_vm_core/module/module_loader.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature.h
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.h
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_manifest_export.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_manifest_export.h
  - zr_vm_core/src/zr_vm_core/module/module_prototype.c
  - zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c
  - zr_vm_library/include/zr_vm_library/native_binding.h
  - zr_vm_library/src/zr_vm_library/native_binding/native_binding_internal.h
  - zr_vm_library/src/zr_vm_library/native_binding/native_binding_metadata.c
  - zr_vm_library/include/zr_vm_library/project.h
  - zr_vm_library/include/zr_vm_library/zrm.h
  - zr_vm_library/src/zr_vm_library/project/project.c
  - zr_vm_library/src/zr_vm_library/project/project_import_provider_location.c
  - zr_vm_library/src/zr_vm_library/project/project_import_resolver.c
  - zr_vm_library/src/zr_vm_library/project/project_aot_options.c
  - zr_vm_library/src/zr_vm_library/project/project_aot_options.h
  - zr_vm_library/src/zr_vm_library/project/project_features.c
  - zr_vm_library/src/zr_vm_library/project/project_features.h
  - zr_vm_library/src/zr_vm_library/project/project_preserve.c
  - zr_vm_library/src/zr_vm_library/project/project_preserve.h
  - zr_vm_library/src/zr_vm_library/project/project_import_provider_location.c
  - zr_vm_library/src/zr_vm_library/zrm.c
  - zr_vm_language_server_extension/schemas/zrp.schema.json
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler.c
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot.c
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.h
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.c
  - zr_vm_lib_system/include/zr_vm_lib_system/assembly.h
  - zr_vm_lib_system/src/zr_vm_lib_system/assembly/assembly.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/module.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/ffi_runtime/ffi_runtime_callback.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/ffi_runtime/ffi_runtime_internal.h
  - zr_vm_parser/include/zr_vm_parser/compiler.h
  - zr_vm_parser/src/zr_vm_parser/compiler.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_class.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_union.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_internal.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_state.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_statement.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_bindings.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_support.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_quickening.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_token.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_def.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_def.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_spec.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_spec.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_typed_metadata.c
  - zr_vm_parser/src/zr_vm_parser/compiler/module_init_analysis.c
  - zr_vm_parser/src/zr_vm_parser/type_inference.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_ffi.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_internal.h
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_import_metadata.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_member_resolution.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_native.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_types.c
  - zr_vm_parser/src/zr_vm_parser/parser/parser_types.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/semantic_analyzer_typecheck.c
  - zr_vm_parser/src/zr_vm_parser/writer.c
  - zr_vm_parser/src/zr_vm_parser/writer/writer_intermediate.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_emitter.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_prune.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_manifest_export.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_manifest_export.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_remap.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_remap.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_signature.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_signature.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_string_pool.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_string_pool.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_constant_pool.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_constant_pool.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_type_def.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_type_def.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_member_token.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_member_token.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_module_ref.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_module_ref.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_publication.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_publication.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_annotation_warnings.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_annotation_warnings.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_callable_provenance.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability_function_graph.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability_function_graph.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_bool_numeric_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_bool_numeric_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_numeric_three_arg_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_numeric_three_arg_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_method_metadata.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_method_metadata.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_bool_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_f64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_i64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_three_arg_thunks.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_three_arg_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layouts.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layouts.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layout_tokens.c
implementation_files:
  - zr_vm_core/include/zr_vm_core/metadata_token.h
  - zr_vm_core/include/zr_vm_core/zrp_metadata.h
  - zr_vm_core/include/zr_vm_core/metadata_runtime.h
  - zr_vm_core/include/zr_vm_core/reflection.h
  - zr_vm_core/include/zr_vm_core/hash.h
  - zr_vm_core/include/zr_vm_core/function.h
  - zr_vm_core/include/zr_vm_core/io.h
  - zr_vm_common/include/zr_vm_common/zr_io_conf.h
  - zr_vm_core/src/zr_vm_core/function.c
  - zr_vm_core/src/zr_vm_core/function_graph.c
  - zr_vm_core/src/zr_vm_core/function_metadata_binding.c
  - zr_vm_core/src/zr_vm_core/function_metadata_query.c
  - zr_vm_core/src/zr_vm_core/reflection.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_nested.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_nested.h
  - zr_vm_core/src/zr_vm_core/reflection_field_value_primitive.c
  - zr_vm_core/src/zr_vm_core/reflection_field_value_primitive.h
  - zr_vm_core/src/zr_vm_core/reflection_token_resolve.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method_native.c
  - zr_vm_core/src/zr_vm_core/reflection_generic_method_native_internal.h
  - zr_vm_core/src/zr_vm_core/reflection_module.c
  - zr_vm_core/src/zr_vm_core/reflection_interpreter_generic_instance.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_layout_binding.c
  - zr_vm_core/src/zr_vm_core/metadata_runtime_method_binding.c
  - zr_vm_core/src/zr_vm_core/hash.c
  - zr_vm_core/src/zr_vm_core/io.c
  - zr_vm_core/src/zr_vm_core/io_runtime.c
  - zr_vm_core/src/zr_vm_core/module/module_internal.h
  - zr_vm_core/src/zr_vm_core/module/module.c
  - zr_vm_core/src/zr_vm_core/module/module_loader.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature.h
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.h
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_manifest_export.c
  - zr_vm_core/src/zr_vm_core/module/module_import_signature_manifest_export.h
  - zr_vm_core/src/zr_vm_core/module/module_prototype.c
  - zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c
  - zr_vm_library/include/zr_vm_library/native_binding.h
  - zr_vm_library/src/zr_vm_library/native_binding/native_binding_internal.h
  - zr_vm_library/src/zr_vm_library/native_binding/native_binding_metadata.c
  - zr_vm_library/include/zr_vm_library/project.h
  - zr_vm_library/include/zr_vm_library/zrm.h
  - zr_vm_library/src/zr_vm_library/project/project.c
  - zr_vm_library/src/zr_vm_library/project/project_import_resolver.c
  - zr_vm_library/src/zr_vm_library/project/project_aot_options.c
  - zr_vm_library/src/zr_vm_library/project/project_aot_options.h
  - zr_vm_library/src/zr_vm_library/project/project_features.c
  - zr_vm_library/src/zr_vm_library/project/project_features.h
  - zr_vm_library/src/zr_vm_library/project/project_preserve.c
  - zr_vm_library/src/zr_vm_library/project/project_preserve.h
  - zr_vm_library/src/zr_vm_library/zrm.c
  - zr_vm_language_server_extension/schemas/zrp.schema.json
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler.c
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot.c
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.h
  - zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.c
  - zr_vm_lib_system/include/zr_vm_lib_system/assembly.h
  - zr_vm_lib_system/src/zr_vm_lib_system/assembly/assembly.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/module.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/ffi_runtime/ffi_runtime_callback.c
  - zr_vm_lib_ffi/src/zr_vm_lib_ffi/ffi_runtime/ffi_runtime_internal.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_class.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_union.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_statement.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_bindings.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_internal.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_support.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_quickening.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_token.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_def.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_def.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_spec.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_spec.h
  - zr_vm_parser/src/zr_vm_parser/compiler/compiler_typed_metadata.c
  - zr_vm_parser/src/zr_vm_parser/compiler/module_init_analysis.c
  - zr_vm_parser/src/zr_vm_parser/type_inference.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_core.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_ffi.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_import_metadata.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_member_resolution.c
  - zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_native.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c
  - zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_types.c
  - zr_vm_parser/src/zr_vm_parser/parser/parser_types.c
  - zr_vm_language_server/src/zr_vm_language_server/semantic/semantic_analyzer_typecheck.c
  - zr_vm_parser/src/zr_vm_parser/writer.c
  - zr_vm_parser/src/zr_vm_parser/writer/writer_intermediate.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_emitter.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_prune.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_manifest_export.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_manifest_export.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_signature.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_signature.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_constant_pool.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_constant_pool.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_type_def.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_type_def.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_module_ref.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_module_ref.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_publication.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_zrp_metadata_publication.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_annotation_warnings.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_annotation_warnings.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_callable_provenance.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability_function_graph.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_reachability_function_graph.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_bool_numeric_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_bool_numeric_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_numeric_three_arg_invokers.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_reflection_numeric_three_arg_invokers.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_method_metadata.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_method_metadata.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_bool_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_f64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_i64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_three_arg_thunks.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_typed_u64_three_arg_thunks.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layouts.c
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layouts.h
  - zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_type_layout_tokens.c
plan_sources:
  - user: 2026-03-31 实现 “M6 强类型推断完整闭环计划”
  - .codex/plans/M6 强类型推断完整闭环计划.md
  - .codex/plans/zr_vm阶段化总计划.md
  - docs/plans/using/02-using-scopes-and-plugin-guards.md
  - docs/plans/using/03-metadata-and-token-model.md
  - docs/plans/using/07-implementation-blueprint.md
  - docs/plans/aot/11-metadata.md
  - docs/plans/aot/12-code-stripping.md
  - docs/plans/aot/08-generic-sharing.md
  - docs/plans/aot/10-reflection.md
  - user: 2026-07-18 按 AOT 07~12 计划持续优化代码生成并逐阶段记录状态与产出
  - user: 2026-04-06 struct 值类型与 native wrapper 分层方案
  - user: 2026-04-06 新的 source-level wrapper decorator surface 和具体 handle_id lowering runtime完善
tests:
  - tests/parser/test_type_inference.c
  - tests/parser/test_compiler_regressions.c
  - tests/parser/test_compiler_call_lowering_focus_main.c
  - tests/parser/test_compiler_features.c
  - tests/parser/test_compiler_w2_performance_quickening.c
  - tests/parser/test_prototype.c
  - tests/parser/test_instruction_execution.c
  - tests/ffi/test_ffi_module.c
  - tests/language_server/test_semantic_analyzer.c
  - tests/scripts/test_artifact_golden.c
  - tests/cmake/run_projects_suite.cmake
  - tests/fixtures/projects/import_binary/fixtures/greet_binary_source.zr
  - tests/fixtures/projects/import_binary/bin/greet.zro
  - tests/module/test_metadata_token_model.c
  - tests/module/test_metadata_type_ref_binding.c
  - tests/module/test_metadata_runtime_query.c
  - tests/module/test_metadata_runtime_manifest_exports.c
  - tests/module/test_metadata_runtime_binding_compatibility.c
  - tests/module/test_reflection_dynamic_generic_instance.c
  - tests/module/test_reflection_dynamic_generic_cross_module.h
  - tests/module/test_reflection_dynamic_generic_method_context.h
  - tests/module/test_reflection_dynamic_generic_method_make.h
  - tests/acceptance/2026-07-19-aot-08-s6t-10-s4z41-11-s6j-bound-provider-generic-typespec-identity.md
  - tests/acceptance/2026-07-19-aot-08-s6u-10-s4z42-11-s2e-methodspec-method-token-vm-function-resolution.md
  - tests/acceptance/2026-07-19-aot-08-s6v-10-s4z43-11-s5a-generic-method-definition-object.md
  - tests/acceptance/2026-07-19-aot-08-s6w-10-s4z44-11-s5b-methodspec-request-resolution.md
  - tests/acceptance/2026-07-19-aot-08-s6x-10-s4z45-11-s5c-constructed-generic-method-object.md
  - tests/acceptance/2026-07-19-aot-08-s6y-10-s4z46-11-s5d-make-generic-method-object.md
  - tests/acceptance/2026-07-19-aot-08-s6z-10-s4z47-11-s5e-generic-method-argument-object-decoding.md
  - tests/acceptance/2026-07-19-aot-08-s6aa-10-s4z48-11-s5f-generic-method-native-entry.md
  - tests/acceptance/2026-07-19-aot-08-s6ab-10-s4z49-11-s5g-runtime-bound-reflection-module.md
  - tests/acceptance/2026-07-02-aot-12-s7zzq-runtime-export-member-token-publication.md
  - tests/acceptance/2026-07-02-aot-11-s7z-zrp-manifest-export-declarations.md
  - tests/acceptance/2026-07-02-aot-11-s7za-export-declaration-writer-options.md
  - tests/acceptance/2026-07-02-aot-11-s7zi-provider-manifest-export-binding-gate.md
  - tests/acceptance/2026-07-02-aot-11-s7zj-provider-import-location-discovery.md
  - tests/acceptance/2026-07-02-aot-11-s7zk-provider-aot-load-request.md
  - tests/acceptance/2026-07-02-aot-11-s7zl-provider-aot-runtime-load-request.md
  - tests/acceptance/2026-07-02-aot-11-s7zm-provider-aot-dynamic-library-success.md
  - tests/acceptance/2026-07-02-aot-11-s7zn-provider-version-selection-range-guard.md
  - tests/acceptance/2026-07-02-aot-11-s7zsc-12-s7zzt-zrp-manifest-export-pruning.md
  - tests/acceptance/2026-07-03-aot-11-s7zsd-12-s7zzu-zrp-manifest-export-declaration-publication.md
  - tests/acceptance/2026-07-03-aot-11-s7zsg-12-s7zzx-zrp-compacted-metadata-publication.md
  - tests/acceptance/2026-07-03-aot-11-s7zsk-12-s7zzzd-fielddef-token-retention.md
  - tests/acceptance/2026-07-03-aot-11-s7zsl-12-s7zzze-typespec-fielddef-retention.md
  - tests/acceptance/2026-07-03-aot-11-s7zsm-12-s7zzzf-method-only-member-token-retention.md
  - tests/acceptance/2026-07-03-aot-11-s7zsn-12-s7zzzg-typedef-fielddef-owner-token-retention.md
  - tests/acceptance/2026-07-03-aot-11-s7zsp-12-s7zzzi-methodspec-memberref-retention.md
  - tests/acceptance/2026-07-03-aot-11-s7zsq-12-s7zzzj-methodspec-memberref-token-record-guard.md
  - tests/acceptance/2026-07-03-aot-11-s7zsr-12-s7zzzk-methodspec-memberref-retained-token-record-guard.md
  - tests/acceptance/2026-07-03-aot-11-s7zss-12-s7zzzl-methodspec-memberref-recursive-retained-token-record-guard.md
  - tests/acceptance/2026-07-03-aot-11-s7zst-12-s7zzzm-signature-typeref-string-remap.md
  - tests/acceptance/2026-07-03-aot-11-s7zsu-12-s7zzzn-signature-module-string-remap.md
  - tests/acceptance/2026-07-03-aot-11-s7zsv-12-s7zzzo-module-ref-generic-context.md
  - tests/acceptance/2026-07-03-aot-11-s7zsw-12-s7zzzp-signature-union-string-remap.md
  - tests/module/test_metadata_runtime_method_binding.c
  - tests/module/test_reflection_token_resolve.c
  - tests/module/test_reflection_method_invoke.c
  - tests/module/test_metadata_runtime_typespec_layout.c
  - tests/module/test_metadata_module_hash_golden.c
  - tests/library/test_zrm_container.c
  - tests/library/test_project_import_resolver.c
  - tests/library/test_project_import_aot_provider_runtime.c
  - tests/library/test_project_import_provider_version_selection.c
  - tests/parser/test_aot_c_provider_shared_library_smoke.c
  - tests/library/test_project_manifest_normalization.c
  - tests/cli/test_cli_aot_writer_options.c
  - tests/cli/test_cli_args.c
  - tests/cli/test_cli_zrp_metadata_dump.c
  - tests/cli/test_cli_project_incremental.c
  - tests/system/test_system_assembly_module.c
  - tests/parser/test_project_import_canonicalization.c
  - tests/module/test_zrp_metadata_format.c
  - tests/parser/test_aot_c_zrp_metadata_pruning.c
  - tests/parser/test_aot_c_zrp_metadata_methodspec_pruning.c
  - tests/parser/test_aot_c_zrp_metadata_publication.c
  - tests/acceptance/2026-07-03-aot-11-s7zsx-12-s7zzzq-zrp-sidecar-definition-table-validation.md
  - tests/acceptance/2026-07-03-aot-11-s7zsy-12-s7zzzr-manifest-export-duplicate-target-guard.md
  - tests/acceptance/2026-07-03-aot-11-s7zsz-12-s7zzzs-project-export-duplicate-target-guard.md
  - tests/acceptance/2026-07-03-aot-11-s7zta-12-s7zzzt-project-export-schema-unique-items.md
  - tests/acceptance/2026-07-03-aot-11-s7ztb-12-s7zzzu-cli-export-duplicate-bridge-guard.md
  - tests/parser/test_aot_c_zrp_metadata_pool_pruning.c
  - tests/acceptance/2026-06-17-using-import-guard.md
  - tests/acceptance/2026-06-24-aot-11-s7e-zrp-project-manifest-preserve-rule-parsing.md
  - tests/acceptance/2026-06-25-aot-11-s7f-zrp-project-manifest-aot-mode.md
  - tests/acceptance/2026-06-25-aot-11-s7i-12-s4c-preserve-method-root-binding.md
  - tests/acceptance/2026-06-25-aot-11-s7j-12-s4d-dotted-type-method-preserve-roots.md
  - tests/acceptance/2026-06-25-aot-11-s7k-12-s4e-preserve-feature-condition-model.md
  - tests/acceptance/2026-06-25-aot-11-s7l-12-s4f-feature-switch-preserve-root-gating.md
  - tests/acceptance/2026-06-25-aot-11-s7m-12-s4g-generic-preserve-argument-model.md
  - tests/acceptance/2026-06-25-aot-11-s7n-12-s4h-manifest-generic-preserve-writer-roots.md
  - tests/acceptance/2026-06-25-aot-11-s7o-12-s4i-manifest-generic-preserve-typespec-binding.md
  - tests/acceptance/2026-06-25-aot-11-s7p-12-s8h-full-aot-generic-preserve-typespec-closure-gate.md
  - tests/acceptance/2026-06-25-aot-11-s7q-12-s3a-manifest-generic-preserve-instantiation-root.md
  - tests/acceptance/2026-06-25-aot-11-s7r-12-s8i-full-aot-generic-instantiation-closure-gate.md
  - tests/acceptance/2026-06-25-aot-11-s7s-12-s3c-generic-instantiation-open-base-token.md
  - tests/acceptance/2026-06-25-aot-11-s7t-12-s3d-generic-instantiation-typedef-base-token.md
  - tests/acceptance/2026-06-25-aot-11-s7u-12-s3e-manifest-generic-synthesized-typespec.md
  - tests/acceptance/2026-06-25-aot-11-s7v-12-s3f-manifest-generic-methodspec-binding.md
  - tests/acceptance/2026-06-25-aot-11-s4j-typespec-layout-binding-view.md
  - tests/acceptance/2026-06-25-aot-11-s4k-type-token-layout-cache.md
  - tests/acceptance/2026-06-25-aot-11-s4l-layout-id-token-reverse-cache.md
  - tests/acceptance/2026-06-25-aot-11-s4m-multi-entry-type-layout-cache.md
  - tests/acceptance/2026-06-25-aot-11-s4n-ctype-id-token-resolver.md
  - tests/acceptance/2026-06-25-aot-11-s4o-type-layout-token-carrier.md
  - tests/acceptance/2026-06-26-aot-11-s4p-generated-type-layout-token-population.md
  - tests/acceptance/2026-06-26-aot-11-s4q-generated-typespec-type-layout-token-population.md
  - tests/acceptance/2026-06-26-aot-11-s4r-generated-ownership-offset-table.md
  - tests/acceptance/2026-06-30-aot-11-s2d-method-token-binding-view.md
  - tests/acceptance/2026-06-30-aot-10-s3d-method-binding-reflection-carrier.md
  - tests/acceptance/2026-06-30-aot-10-s3e-methodspec-underlying-method-binding-carrier.md
  - tests/acceptance/2026-06-30-aot-10-s2b-token-driven-method-invoke-dispatcher.md
  - tests/acceptance/2026-06-30-aot-10-s2c-method-invoke-signature-arity-guard.md
  - tests/acceptance/2026-06-30-aot-10-s2d-method-invoke-signature-shape-guard.md
  - tests/acceptance/2026-06-30-aot-10-s2e-method-invoke-parameter-base-type-guard.md
  - tests/acceptance/2026-06-30-aot-10-s2f-method-invoke-return-base-type-guard.md
  - tests/acceptance/2026-06-30-aot-10-s2g-method-invoke-return-slot-reset.md
  - tests/acceptance/2026-06-30-aot-10-s2h-method-invoke-void-return-slot.md
  - tests/acceptance/2026-06-30-aot-10-s2i-method-invoke-int64-no-arg-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2j-method-invoke-uint64-no-arg-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2k-method-invoke-bool-no-arg-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2l-method-invoke-f64-no-arg-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2m-method-invoke-int64-one-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2n-method-invoke-uint64-one-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2-maint-reflection-invoker-emitter-split.md
  - tests/acceptance/2026-06-30-aot-10-s2o-method-invoke-bool-one-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2p-method-invoke-f64-one-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2q-method-invoke-int64-two-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2r-method-invoke-uint64-two-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2s-method-invoke-bool-two-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2t-method-invoke-f64-two-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2u-method-invoke-bool-numeric-two-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2v-method-invoke-int64-three-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2w-method-invoke-uint64-three-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2x-method-invoke-f64-three-arg-unbox-return-boxing.md
  - tests/acceptance/2026-06-30-aot-10-s2y-method-invoke-bool-three-arg-unbox-return-boxing.md
  - tests/parser/test_aot_reachability.c
  - tests/parser/test_aot_c_reflection_annotation_preserve.c
  - tests/acceptance/2026-06-30-aot-10-s5a-reflectable-annotation-function-root.md
  - tests/acceptance/2026-06-30-aot-10-s5b-requires-unreferenced-code-warning.md
  - tests/acceptance/2026-06-30-aot-10-s5c-requires-unreferenced-code-reason.md
  - tests/acceptance/2026-06-30-aot-10-s5d-dynamic-dependency-function-root.md
  - tests/acceptance/2026-06-30-aot-10-s5f-dynamic-dependency-method-token-root.md
  - tests/acceptance/2026-06-30-aot-10-s5g-dynamic-dependency-method-name-root.md
  - tests/acceptance/2026-06-30-aot-10-s5h-dynamic-dependency-method-name-signature-hash-root.md
  - tests/acceptance/2026-06-30-aot-10-s5i-dynamic-dependency-non-exported-method-token-root.md
  - tests/acceptance/2026-06-30-aot-10-s5j-dynamic-dependency-type-layout-root.md
  - tests/acceptance/2026-06-30-aot-10-s5k-dynamic-dependency-type-token-root.md
  - tests/acceptance/2026-06-30-aot-10-s5l-dynamic-dependency-field-token-layout-roots.md
  - tests/acceptance/2026-06-30-aot-10-s5m-dynamic-dependency-bound-typeref-token-layout-root.md
  - tests/acceptance/2026-06-30-aot-10-s5n-runtime-bound-typeref-token-layout-resolver.md
  - tests/acceptance/2026-06-30-aot-10-s4f-fielddef-token-fieldinfo-object.md
  - tests/acceptance/2026-06-30-aot-10-s4g-fieldinfo-declaring-type-object.md
  - tests/acceptance/2026-06-30-aot-10-s4h-fieldinfo-owner-link.md
  - tests/acceptance/2026-06-30-aot-10-s4i-fieldinfo-module-name.md
  - tests/acceptance/2026-06-30-aot-10-s4j-fieldinfo-fielddef-flags.md
  - tests/acceptance/2026-06-30-aot-10-s4k-fieldinfo-fielddef-signature-blob.md
  - tests/acceptance/2026-06-30-aot-10-s4l-fieldinfo-field-signature-header.md
  - tests/acceptance/2026-06-30-aot-10-s4m-fieldinfo-field-signature-type-node.md
  - tests/acceptance/2026-06-30-aot-10-s4n-fieldinfo-field-signature-primitive-type.md
  - tests/acceptance/2026-06-30-aot-10-s4o-fieldinfo-field-signature-type-object.md
  - tests/acceptance/2026-06-30-aot-10-s4p-fieldinfo-module-reflection-link.md
  - tests/acceptance/2026-06-30-aot-10-s4q-fieldinfo-signature-typedef-carrier.md
  - tests/acceptance/2026-07-01-aot-10-s4r-fieldinfo-signature-typedef-type-object.md
  - tests/acceptance/2026-07-01-aot-10-s4s-fieldinfo-signature-typeref-carrier.md
  - tests/acceptance/2026-07-01-aot-10-s4t-fieldinfo-signature-layout-consistency.md
  - tests/acceptance/2026-07-01-aot-10-s4u-fieldinfo-signature-type-node-object.md
  - tests/acceptance/2026-07-01-aot-10-s4v-fieldinfo-signature-base-type-node-object.md
  - tests/acceptance/2026-07-01-aot-10-s4w-fieldinfo-signature-child-node-objects.md
  - tests/acceptance/2026-07-01-aot-10-s4x-fieldinfo-signature-primitive-child-node-semantic.md
  - tests/acceptance/2026-07-01-aot-10-s4y-fieldinfo-signature-typedef-child-node-semantic.md
  - tests/acceptance/2026-07-01-aot-10-s4z2-fieldinfo-signature-typeref-child-node-semantic.md
  - tests/acceptance/2026-07-01-aot-10-s4z3-fieldinfo-signature-node-type-literal.md
  - tests/acceptance/2026-07-01-aot-10-s4z4-fieldinfo-value-slot-read.md
  - tests/acceptance/2026-07-01-aot-10-s4z5-fieldinfo-value-slot-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z6-fieldinfo-primitive-pod-read-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z7-fieldinfo-primitive-pod-matrix.md
  - tests/acceptance/2026-07-01-aot-10-s4z8-fieldinfo-primitive-pod-width-matrix.md
  - tests/acceptance/2026-07-01-aot-10-s4z9-fieldinfo-primitive-pod-integer-range-guard.md
  - tests/acceptance/2026-07-01-aot-10-s4z10-fieldinfo-primitive-pod-float32-range-guard.md
  - tests/acceptance/2026-07-01-aot-10-s4z11-fieldinfo-primitive-pod-float32-nan-guard.md
  - tests/acceptance/2026-07-01-aot-10-s4z12-fieldinfo-primitive-pod-float32-precision-guard.md
  - tests/acceptance/2026-07-01-aot-10-s4z13-fieldinfo-metadata-runtime-carrier.md
  - tests/acceptance/2026-07-01-aot-10-s4z14-fieldinfo-object-value-read.md
  - tests/acceptance/2026-07-01-aot-10-s4z15-fieldinfo-object-value-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z16-fieldinfo-object-primitive-pod.md
  - tests/acceptance/2026-07-01-aot-10-s4z17-fieldinfo-inline-struct-borrowed-view.md
  - tests/acceptance/2026-07-01-aot-10-s4z18-fieldinfo-inline-aggregate-borrowed-source-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z19-fieldinfo-inline-aggregate-field-copy-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z20-fieldinfo-inline-aggregate-replacement-drop-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z21-fieldinfo-nested-value-slot-read.md
  - tests/acceptance/2026-07-01-aot-10-s4z22-fieldinfo-nested-value-slot-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z23-fieldinfo-nested-value-slot-path-read.md
  - tests/acceptance/2026-07-01-aot-10-s4z24-fieldinfo-nested-value-slot-path-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z25-fieldinfo-nested-primitive-pod-path-read-write.md
  - tests/acceptance/2026-07-01-aot-10-s4z26-fieldinfo-nested-primitive-pod-leaf-layout-guard.md
  - tests/acceptance/2026-07-01-aot-10-s4z27-fieldinfo-nested-primitive-pod-path-matrix.md
  - tests/acceptance/2026-07-01-aot-10-s4z28-fieldinfo-nested-primitive-pod-width-matrix.md
  - tests/acceptance/2026-07-01-aot-12-s7zza-typedef-trailing-orphan-sweep.md
  - tests/acceptance/2026-07-01-aot-12-s7zzb-typedef-rid-compaction.md
  - tests/acceptance/2026-07-01-aot-12-s7zzc-signature-typedef-token-rewrite.md
  - tests/acceptance/2026-07-01-aot-12-s7zzd-member-token-remap-abi-publication.md
  - tests/acceptance/2026-07-01-aot-12-s7zze-member-token-remap-entry-validation.md
  - tests/acceptance/2026-07-01-aot-12-s7zzf-member-token-remap-duplicate-validation.md
  - tests/acceptance/2026-07-01-aot-12-s7zzg-member-token-remap-sidecar-source-duplicate-guard.md
  - tests/acceptance/2026-07-01-aot-12-s7zzh-member-token-remap-sidecar-token-shape-guard.md
  - tests/acceptance/2026-07-01-aot-12-s7zzi-member-token-remap-sidecar-retained-count-guard.md
  - tests/acceptance/2026-07-01-aot-12-s7zzj-signature-token-rid-compaction.md
  - tests/acceptance/2026-07-01-aot-12-s7zzk-signature-token-orphan-rejection.md
  - tests/acceptance/2026-07-01-aot-12-s7zzl-generic-param-constraint-typespec-remap.md
  - tests/acceptance/2026-07-01-aot-12-s7zz-module-ref-orphan-sweep.md
  - tests/acceptance/2026-07-01-aot-12-s7zv-field-default-constant-pool-remap.md
  - tests/acceptance/2026-06-30-aot-12-s7zu-annotation-warning-suppression.md
  - tests/acceptance/2026-06-26-aot-12-s7k-zrp-metadata-section-byte-statistics.md
  - tests/parser/test_aot_c_code_stripping.c
  - tests/parser/test_aot_c_zrp_metadata_typedef_pruning.c
  - tests/parser/test_aot_c_generic_call_typed.c
  - tests/parser/test_aot_c_value_type_shared_library_smoke.c
  - tests/fixtures/projects/using_feature_matrix
  - tests/fixtures/projects/using_edge_matrix
  - tests/fixtures/projects/lsp_language_feature_matrix
  - tests/module/test_module_system.c
doc_type: module-detail
---

# Typed Module Metadata

## Purpose

M6 的目标不是把局部推断再补几条特判，而是把“编译期知道的类型”真正落成模块产物的一部分，让三条链路用同一份事实源工作：

1. 编译器推断和 overload 选择
2. import 时的 compile-time signature / member inference
3. `.zro` 持久化单模块 typed metadata / token 中间产物，`.zrm` 作为独立程序集容器聚合可发布模块库与资源，`.zri` 保留 debug / intermediate 镜像给后续运行时与 AOT 验证使用

metadata/token 本体仍直接升级 `.zro` schema，不保留旧格式双读兼容；程序集级分发、第三方模块库和压缩资源不再继续扩张 `.zro`，而是进入 `.zrm` 容器边界。

## Related Files

- runtime function metadata
  - `SZrFunction` 在 [zr_vm_core/include/zr_vm_core/function.h](../../zr_vm_core/include/zr_vm_core/function.h) 新增 `typedLocalBindings` 和 `typedExportedSymbols`
  - `function.c` 负责这些数组的生命周期
- binary IO schema
  - `ZrCore_Hash_CreateStable64WithPrefix` 在 [zr_vm_core/include/zr_vm_core/hash.h](../../zr_vm_core/include/zr_vm_core/hash.h) 提供不依赖进程随机 seed 的稳定 `XXH3_64bits` hashing，用于 metadata signature fingerprint
  - `TZrMetadataToken`、`EZrMetadataTableTag`、`SZrMetadataTokenRecord` 在 [zr_vm_core/include/zr_vm_core/metadata_token.h](../../zr_vm_core/include/zr_vm_core/metadata_token.h) 定义
  - `SZrIoFunctionTypedTypeRef`、`SZrIoFunctionTypedLocalBinding`、`SZrIoFunctionTypedExportSymbol` 在 [zr_vm_core/include/zr_vm_core/io.h](../../zr_vm_core/include/zr_vm_core/io.h) 定义
  - `io.c` 负责 `.zro` 读写
- compile-time metadata construction
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_typed_metadata.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_typed_metadata.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_token.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_token.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.h](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_record.h)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.h](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_module_hash.h)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.h](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_signature.h)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_ref.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_ref.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_ref.h](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_metadata_type_ref.h)
- import normalization
  - [zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_import_metadata.c](../../zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_import_metadata.c)
  - [zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_member_resolution.c](../../zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_member_resolution.c)
  - [zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_native.c](../../zr_vm_parser/src/zr_vm_parser/type_inference/type_inference_native.c)
- runtime import signature verification
  - [zr_vm_core/src/zr_vm_core/module/module_import_signature.c](../../zr_vm_core/src/zr_vm_core/module/module_import_signature.c)
  - [zr_vm_core/src/zr_vm_core/module/module_import_signature.h](../../zr_vm_core/src/zr_vm_core/module/module_import_signature.h)
  - [zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.c](../../zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.c)
  - [zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.h](../../zr_vm_core/src/zr_vm_core/module/module_import_signature_binding.h)
  - [zr_vm_core/src/zr_vm_core/module/module_loader.c](../../zr_vm_core/src/zr_vm_core/module/module_loader.c)
- opcode selection and conversions
  - [zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c)
  - [zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_types.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compile_expression_types.c)
  - [zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c](../../zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c)
- artifact writers
  - [zr_vm_parser/src/zr_vm_parser/writer.c](../../zr_vm_parser/src/zr_vm_parser/writer.c)
  - [zr_vm_parser/src/zr_vm_parser/writer/writer_intermediate.c](../../zr_vm_parser/src/zr_vm_parser/writer/writer_intermediate.c)
- assembly container and packaging
  - [zr_vm_library/include/zr_vm_library/zrm.h](../../zr_vm_library/include/zr_vm_library/zrm.h) and [zr_vm_library/src/zr_vm_library/zrm.c](../../zr_vm_library/src/zr_vm_library/zrm.c) define the `.zrm` archive API, manifest entry, module entries, resource entries, and safe logical-name rules.
  - [zr_vm_library/src/zr_vm_library/project/project.c](../../zr_vm_library/src/zr_vm_library/project/project.c), [zr_vm_library/src/zr_vm_library/project/project_import_resolver.c](../../zr_vm_library/src/zr_vm_library/project/project_import_resolver.c), and [zr_vm_library/src/zr_vm_library/project/project_import_provider_location.c](../../zr_vm_library/src/zr_vm_library/project/project_import_provider_location.c) parse `assembly.output`, `resources`, and `.zrm` references, resolve modules from referenced assemblies, and expose provider-location/load-request discovery for later AOT metadata binding.
  - [zr_vm_library/src/zr_vm_library/project/project_features.c](../../zr_vm_library/src/zr_vm_library/project/project_features.c) parses top-level `.zrp` `features` boolean switch values into `SZrLibrary_ProjectFeatureSwitch` entries.
    - [zr_vm_library/src/zr_vm_library/project/project_preserve.c](../../zr_vm_library/src/zr_vm_library/project/project_preserve.c) parses declaration-level `.zrp` `preserve` rules into `SZrLibrary_ProjectPreserveRule` entries, including optional `feature` / `featureValue` condition declarations and non-empty `arguments` arrays for generic preserve rules. The current CLI AOT C path binds same-module `method` targets to top-level callable function flat indices, expands `type` rules with `members: "methods"` / `"all"` to same-prefix callable roots, applies matching feature switches before injecting roots, forwards generic preserve target/arguments to AOT writer options, binds those generic roots to existing current-module `TYPE_SPEC` records when the metadata signature matches, can synthesize current-function TypeSpec/signature bindings from matching same-module open `TYPE_REF` or `TYPE_DEF` metadata when the TypeSpec is missing, materializes the bound type roots as generic instantiation identities that prefer matching same-module open base tokens while falling back to TypeSpec tokens when no matching open-base record exists, and binds generic method roots to existing current-module MethodSpec-shaped `GENERIC_INST(MEMBER_REF methodToken, args...)` signatures. Full-AOT writer generation rejects generic type preserve roots that are text-only, TypeSpec-unbound, or TypeSpec-only without generic instantiation identity, while accepting MethodSpec-bound generic method roots. Cross-module generic target binding remains future work.
  - [zr_vm_library/src/zr_vm_library/project/project_exports.c](../../zr_vm_library/src/zr_vm_library/project/project_exports.c) parses declaration-level `.zrp` `exports` entries into `SZrLibrary_ProjectExportDeclaration` records for later cross-module metadata export publication and rejects duplicate export `kind + target` keys before they reach the writer bridge.
  - [zr_vm_library/src/zr_vm_library/project/project_aot_options.c](../../zr_vm_library/src/zr_vm_library/project/project_aot_options.c) parses declaration-level `.zrp` `aotMode` into `SZrLibrary_Project.aotMode`, which the CLI/compiler bridge maps to `SZrAotWriterOptions.requireFullAot`.
  - [zr_vm_cli/src/zr_vm_cli/compiler/compiler.c](../../zr_vm_cli/src/zr_vm_cli/compiler/compiler.c) emits project assemblies through `--emit-zrm`.
    - [zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot.c](../../zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot.c) emits project AOT C through `--emit-aot-c`, embeds the `.zro` blob, applies project AOT mode, injects same-module method/type plus generic preserve roots into writer options, binds generic type roots to existing `TYPE_SPEC` token/signature identity when current-function metadata contains a matching `GENERIC_INST` signature, synthesizes a writer-visible current-function TypeSpec/signature pair when a matching open `TYPE_REF` or `TYPE_DEF` record exists but the closed TypeSpec is missing, materializes those bound type roots as generic instantiation identities with `baseToken`, `cInstanceId`, and `shareKind`, binds generic method roots to existing MethodSpec-shaped `GENERIC_INST(MEMBER_REF methodToken, args...)` signatures, and delegates `.zrp` export declaration writer-option injection to the export bridge helper. When a same-module `TYPE_REF` or `TYPE_DEF` record matches the generic target name and the TypeSpec base node, that open generic token becomes the instance base token; otherwise the bridge falls back to the TypeSpec token for compatibility. Full-AOT writer generation now rejects manifest generic type roots that remain unbound to TypeSpec metadata or lack generic instantiation identity, while accepting MethodSpec-bound generic method roots.
    - [zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.c](../../zr_vm_cli/src/zr_vm_cli/compiler/compiler_aot_exports.c) maps `SZrLibrary_ProjectExportDeclaration` entries into `SZrAotManifestExportDeclaration` writer options, with CLI-owned scratch storage and generated-C manifest diagnostics as the current consumer surface. Current-module type declarations are opportunistically bound to matching local `TYPE_DEF` metadata records through `hasTypeTokenBinding/typeToken`; current-module method and field declarations are opportunistically bound to existing typed exported `MEMBER_DEF` tokens through `hasMemberTokenBinding/memberToken`.
  - [zr_vm_lib_system/src/zr_vm_lib_system/assembly/assembly.c](../../zr_vm_lib_system/src/zr_vm_lib_system/assembly/assembly.c) exposes the current project assembly resource API as `zr.system.assembly`.

## Behavior Model

函数级 typed metadata 统一使用三类记录：

- `SZrFunctionTypedTypeRef`
  - 表示基础值类型、nullable、ownership、数组标记、用户类型名，以及数组元素类型
- `SZrFunctionTypedLocalBinding`
  - 记录局部名、stack slot 和推断出的类型
- `SZrFunctionTypedExportSymbol`
  - 记录导出名、stack slot、access modifier、symbol kind、值类型或返回类型，以及函数参数类型列表
  - 记录导出声明的源码行列范围，供 `.zro` 消费侧做 symbol-level definition / references / document highlight

这里没有把类型信息散落进原有运行时字段。`prototypeData` 仍负责 prototype/member 布局，typed metadata 负责“导出的值和局部绑定在编译期是什么类型”。当前 union 前端切片也遵守这个分工：typed metadata 的 signature blob 表达 `Option<int>` 这类导出签名，`prototypeData` 则记录 `ZR_OBJECT_PROTOTYPE_TYPE_UNION`、每个 `ZR_AST_UNION_VARIANT` 的 tag/kind/field-count、payload 字段声明名、类型名、运行时 storage name、位置、passing mode、ownership qualifier，以及 tag/payload byte layout metadata；运行时的 typed union local materialization 直接消费这份 metadata，把构造器 carrier 写成 inline `[tag][payload]` bytes，并由 type layout 在 active variant 上执行 value/owner payload copy、drop 和 GC visitor。

## Metadata Token Model

Typed metadata 现在有一层稳定的 token / signature 表示，用来给后续跨模块签名匹配提供落盘身份：

- `TZrMetadataToken` 使用 `table_tag << 24 | rid` 的 32-bit 编码，table tag 定义在 [metadata_token.h](../../zr_vm_core/include/zr_vm_core/metadata_token.h)。
- 每个 `SZrFunctionTypedExportSymbol` 可携带 `metadataToken`、`signatureToken`、`signatureBlobOffset`、`signatureBlobLength`。
- 每个 `SZrFunctionTypedExportSymbol` 和 `SZrMetadataTokenRecord` 还携带 `signatureHash`，当前由 `zr.md.sig.v1\0` 前缀 + signature blob 的稳定 `XXH3_64bits` 计算。
- 每个 `SZrMetadataTokenRecord` 还携带 `ownerToken`，用于表达 ref 层级和配对签名归属。
- import effect 和 `SZrMetadataTokenRecord` 还携带 target signature identity：`targetMetadataToken`、`targetSignatureToken`、`targetSignatureHash` 用来保存 ref 期望绑定到的 provider def/signature；`targetModuleSignatureHash` 保存编译期看到的 provider module ABI fingerprint；`requestedModuleVersion`、`minModuleVersionInclusive`、`maxModuleVersionExclusive` 保存 `AssemblyRef` 的 semantic version range。module effect 额外携带 `assemblyName`：源码和 loader 仍使用 canonical module key（例如 `$mathLocal@2.1.0/ops/sum`）定位 provider，但 `ASSEMBLY_REF` signature 写 `.zrp references` 声明的真实程序集名（例如 `zr.math`）。
- Source module-init summary 刷新必须以最终 typed export symbol 为准：`pub var name = () => ...` 这类 prescan 阶段先登记为 `VARIABLE`/`FIELD_SIG` 的 callable export，在 type inference 生成最终 `FUNCTION`/`METHOD_SIG` 后要同步 `exportKind`、`symbolKind`、metadata token、signature token/hash 与 signature blob 区间。这样 consumer 的 `MEMBER_REF` target signature 与 provider typed export 保持同一 blob 身份，避免 `import_basic` 这类 exported callable fixture 在 runtime import verification 中出现 expected FIELD_SIG、actual METHOD_SIG 的假 mismatch。
- Provider token stream 含真实 `MODULE` RID 1 记录和 paired `SIGNATURE` record；`MODULE` signature blob 使用 `ZR_METADATA_SIGNATURE_NODE_MODULE`，编码 heap-indexed entry name 与 module version。
- `SZrFunction` / `SZrIoFunction` 持有函数级 `metadataTokenRecords`、entry-function 级 `moduleMetadataTokenRecords`、`signatureBlobHeap` 与 `metadataStringHeap`。
- `SZrFunction` / `SZrIoFunction` 还持有 `moduleVersion` 和 `moduleSignatureHash`，当前由 `compiler_metadata_module_hash.c` 以 `zr.md.mod.v2\0` 输入域计算稳定 module ABI fingerprint：provider module version、public typed exports 的导出身份/`signatureHash`/canonical signature blob bytes，以及本模块 `TYPE_DEF` / `TYPE_SPEC` 实体 record identity 都会参与。
- 运行期 `SZrFunction` 还持有 `moduleMetadataBindings`，这是成功 import verification 后产生的 ref→def binding result sidecar；它记录 caller `MEMBER_REF` token 到 provider `MEMBER_DEF` / `SIGNATURE` token、hash 和 module hash 的解析结果，也记录 caller `ASSEMBLY_REF` token 到 provider `MODULE` RID 1 及其 paired `SIGNATURE` token/hash 的解析结果，还记录 matching `TYPE_SPEC` / `TYPE_DEF` binding 的 expected/resolved layout identity，并能记录已经携带 stable provider TypeDef target identity 的 caller `TYPE_REF -> provider TYPE_DEF / SIGNATURE` binding。`ZrCore_Function_FindModuleMetadataBinding()` 可按 caller ref token 只读查询该 sidecar；`ZrCore_Function_FindMetadataTokenRecord()` / `ZrCore_Function_FindMetadataSignatureRecord()` 查询函数级 record stream，`ZrCore_Function_FindModuleMetadataTokenRecord()` / `ZrCore_Function_FindModuleMetadataSignatureRecord()` 查询 entry-function 级 module ref table。signature 查询要求 entity `relatedToken` 指向 `SIGNATURE`，且 signature record 的 `relatedToken` / `ownerToken` 都回指 entity token。`.zro` patch 28 会通过 `SZrIoFunction.moduleMetadataBindings` 持久化基础 binding，patch 32 会额外持久化 binding-level layout fields 并在 runtime load 时恢复；record 查询 API 不新增 schema patch。
- TypeSpec 和 TypeRef status helpers 会在 definition/layout drift 时拒绝 partial binding，并分别通过 `type_spec_mismatch` / `type_ref_mismatch` module-load diagnostic 暴露，不改变成功 import verification 后 sidecar 的 best-effort 语义。
- `.zro` patch 20 在 typed export symbol 后追加 token 字段，并在 typed export 区之后、static import 区之前写入函数级 metadata token block；patch 21 继续追加 `signatureHash`；patch 22 继续追加 metadata record 的 `ownerToken`；patch 23 继续追加 metadata record 的 `targetSignatureHash` 以及 module effect 的 `targetMetadataToken` / `targetSignatureToken` / `targetSignatureHash`；patch 24 继续追加 metadata record 的 `targetMetadataToken` / `targetSignatureToken`；patch 25 继续追加 entry-function 级 `moduleMetadataTokenRecords` import ref 聚合表；patch 26 继续追加 `moduleSignatureHash`；patch 27 继续追加 metadata record 与 module effect 的 `targetModuleSignatureHash`；patch 28 继续追加 runtime ref→def binding result list；patch 29 继续追加 provider `moduleVersion` 以及 metadata record / module effect 的 AssemblyRef requested/min/max version 字段；patch 30 继续追加 metadata string heap；patch 31 继续追加 record-level `layoutVersion` / `layoutHash`；patch 32 继续追加 binding-level expected/resolved `layoutVersion` / `layoutHash`；patch 33 在 module effect 的 `moduleName` 后追加 `assemblyName`。reader 对旧 patch 缺失字段兼容置 0、null 或空表；future patch 会在 IO header 阶段以 actual/supported patch 诊断拒绝。

第一阶段锚定本模块导出符号，并为 import/module effects 建立函数级 ref 前置记录和 entry-function 级 module ref 聚合表：编译末期为 provider 写入 `MODULE` RID 1 和对应 `SIGNATURE` token，为每个 typed export 分配 `MEMBER_DEF` token 和对应 `SIGNATURE` token。`MODULE` signature blob 使用 `MODULE <entryName> <moduleVersion>`，不包含 `moduleSignatureHash`；typed export 签名 blob 使用 canonical `METHOD_SIG` / `FIELD_SIG`，类型子节点当前覆盖 `PRIMITIVE`、`TYPE_REF`、`ARRAY`、`OWNERSHIP`、`UNION`、`NULLABLE`、`GENERIC_INST`。`compiler_metadata_signature.c` 负责这些 method/field/type signature size/write、`UNION` 节点识别、非 union 泛型闭型 `GENERIC_INST` 编码和 `zr.md.sig.v1\0` stable hash；字符串引用写为 `metadataStringHeap` 的固定 `u32` content-stable index。`compiler_metadata_module_record.c` 负责 provider `MODULE` record/signature；`compiler_metadata_token.c` 负责 RID、record pair、owner-chain、import effect 编排和 metadata string heap 收集；`compiler_metadata_module_hash.c` 负责 `zr.md.mod.v2\0` module ABI hash；`compiler_metadata_type_spec.c` 负责从导出 typed signature 扫描闭型/复合类型并生成基础 `TYPE_SPEC` record；`compiler_metadata_type_def.c` 负责本地 union `TYPE_DEF` record 与逻辑契约 signature；`compiler_metadata_type_def_layout.c` 负责 record-level union layout identity；`compiler_metadata_ref.c` 负责从函数级 import refs 派生 `moduleMetadataTokenRecords`。`UNION` 节点会在当前脚本 AST 中识别已知 union 类型，并编码 union 基名与递归泛型实参，例如 `Option<int>`；普通泛型闭型如 `Box<int>` 编码为 `GENERIC_INST(TYPE_REF("Box"), int)`。导出签名中出现的 nullable、ownership、array 和带泛型实参的闭型会生成 `TYPE_SPEC` 实体 record 与配对 `SIGNATURE` record，复用同一 `signatureBlobHeap` 和 metadata string heap。导出签名中引用的本地 union 会生成 `TYPE_DEF` + paired `SIGNATURE`，signature blob 包含 heap-indexed type name、generic arity、variant name/kind/default flag/field count、payload field name/passing mode/TypeSig；物理布局不进入该 blob。`TYPE_DEF` record 的 `layoutVersion` / `layoutHash` 单独保存 inline value ABI 布局 identity，当前输入为 tag size、payload offset/size/align、整体 size/align 和各 variant payload field 的 offset/size/align；未知/泛型 payload slot 与标量 fallback alignment 在 metadata fingerprint 中规范化为 64-bit reference size / max-8 alignment，避免宿主 `ZR_ALIGN_SIZE` 差异导致 module ABI hash 漂移。`moduleSignatureHash` 按 provider `moduleVersion`、public typed export name 排序后的 export identity/`signatureHash`/canonical signature blob bytes，以及排序后的 `TYPE_DEF` / `TYPE_SPEC` 实体 record identity 计算 module ABI fingerprint；同签名同版本重建保持稳定，返回类型、module version、TypeSpec identity 或本地 TypeDef contract/layout 等 ABI 身份变化会造成 hash 漂移。

`moduleEntryEffects` / exported callable summary effects 里可识别的 import read/call/ref 会生成函数级 `ASSEMBLY_REF`、`TYPE_REF`、`MEMBER_REF` token 与各自的 signature record。实体记录通过 `ownerToken` 串成 `AssemblyRef <- TypeRef <- MemberRef`；配对 `SIGNATURE.ownerToken` 指回被签名实体。`MEMBER_REF` signature blob 以 `ZR_METADATA_SIGNATURE_NODE_MEMBER_REF` 开头，并写入 canonical module name、symbol name 和 effect kind 的 string heap index；当 provider export 可解析时，blob 后续追加目标 `METHOD_SIG` / `FIELD_SIG` 子签名。typed export def 与 import ref target 复用同一套 encoder；当前 `METHOD_SIG` v1 写入 signature version、call convention、generic arity、return type、parameter count，以及每个参数的 mode 和 type；`FIELD_SIG` v1 写入 signature version 和字段 type。record 的 `targetMetadataToken` / `targetSignatureToken` / `targetSignatureHash` 保存期望 provider def、signature token 和签名 hash；`AssemblyRef` record 与 import effect 的 `targetModuleSignatureHash` 保存期望 provider module ABI hash，`requestedModuleVersion` / `minModuleVersionInclusive` / `maxModuleVersionExclusive` 保存默认 semver range。当 source summary 尚未提供 provider hash 时，会从追加的目标子签名计算稳定 hash。每对实体/`SIGNATURE` 共享同一个 `signatureHash`，便于后续 loader 在做完整 ref→def 绑定前先进行快速 fingerprint 判断。`layoutHash` 不参与 `signatureHash`，按值共享布局检查需显式读取 record-level layout identity。`moduleMetadataTokenRecords` 复用同一 signature heap，并按 table、owner、hash、target token/hash/module hash/version range 和 signature blob 字节去重重复 entry/callable import refs。source/binary import 的 compile-time member view 会保留 provider module hash、provider token/hash、provider version 和同名函数候选；member call 通过 `type_inference_member_resolution.c` 按实参类型选择 exact/compatible 候选并拒绝不匹配签名，module-init import-call effect 同样按实参类型选择 source/binary export identity。source summary 里的 primitive annotation 会规范化成 `PRIMITIVE` signature 节点，避免 `bool` / `int` 被编码为命名 `TYPE_REF` 而与 provider typed export hash 漂移。native import 的 module-level members 现在会由 `type_inference_native.c` 分配 synthetic `MEMBER_DEF` / `SIGNATURE` token 和稳定 signature hash，不改变 native descriptor ABI；native module 没有 source/binary summary 时，`module_init_analysis.c` 会走 native compile-info fallback，把这些身份字段写入 guarded import effect；registered native provider 运行期也复用同一 verifier，因此 target hash drift 会让 guard 进入 `else`。现有 import owner-chain 的 `TYPE_REF` 仍主要是 module-member owner 占位；provider summary backed target signature 类型现在会由 `compiler_metadata_type_ref.c` 额外生成 stable provider `TYPE_REF` records，覆盖返回值、参数和嵌套 generic argument 中可解析到 provider `TYPE_DEF` 的类型。module-qualified typed local annotations such as `provider.Option<int>` and destructured/unqualified alias annotations such as `Option<int>` backed by `typeValueAliases` use the same producer and can create an explicit `ASSEMBLY_REF` even when there are no import member effects. `ZrCore_Function_BindMatchingTypeRefMetadata()` 会把这些 caller `TYPE_REF` 绑定到 provider `TYPE_DEF` / paired `SIGNATURE` 和 module/layout identity。

`ZrCore_Function_BindMatchingTypeRefMetadataWithStatus()` 会在 stable provider TypeRef binding 失败时报告 unmatched、definition mismatch 和 layout mismatch 计数及首个 expected/actual token、signature hash、layout version/hash context；runtime 成功 import 后会把这些 drift 作为 `type_ref_mismatch` module-load diagnostic 暴露且不写 partial binding。Module-qualified typed local annotations and destructured/unqualified alias annotations reuse the same producer/binder/status path.

`io_runtime.c` 在 `.zro` 加载到 runtime function 时复制 token records、module ref table、签名堆、metadata string heap、hash、module version、module ABI hash、owner chain、AssemblyRef version range、record-level layout identity 和 target signature identity；exported callable summary effects 会在 module init finalize 后附着到对应 child function；`module_import_signature.c` 的 import verification 会先从 `moduleMetadataTokenRecords` 中匹配 caller 的 `MEMBER_REF` record，用它补齐缺失的 effect version range、target module hash、target token 和 target signature hash，再检查 provider `moduleVersion` 是否落入 AssemblyRef range。通过版本检查后，verifier 在 expected `targetModuleSignatureHash` 非零时与 provider entry function `moduleSignatureHash` 比对，随后在 provider 同名 typed exports 中按 target hash、target signature blob 和 target token 选择匹配候选，而不是按名字取第一个导出。verifier 还会直接遍历 `moduleMetadataTokenRecords`，把匹配当前 import module 的 `MEMBER_REF` record 解码为临时 effect 并校验 `AssemblyRef <- TypeRef <- MemberRef` owner-chain；patch 30+ 的 module/symbol/type string ref 通过 `metadataStringHeap` 解码，旧 patch 仍回退 inline string，因此 `moduleEntryEffects` 缺失或被瘦身时，module ref table 中的 target version/token/hash/bytes drift 仍会被拒绝。选中候选后，verifier 在 effect/provider 两侧 token 均非零时绑定 `targetMetadataToken` / `targetSignatureToken` 到 provider typed export `metadataToken` / `signatureToken`；若双方 token 都存在且不一致，会直接 mismatch，只有旧产物缺 token 时才继续 hash/blob fallback。随后 verifier 把实际 caller function 的 import effects 或 module ref record 的 `targetSignatureHash` 与 provider typed export `signatureHash` 比对，并在存在追加 target `METHOD_SIG` / `FIELD_SIG` 时优先从 `moduleMetadataTokenRecords` 截取 expected signature bytes，再回退函数级 `metadataTokenRecords`，最后与 provider signature blob 做长度和字节确认。版本不兼容、target module hash、token、签名 hash、签名字节不一致或 provider 不可用时，guard import 返回 `null` 并让 `using` 走 `else`；普通 required import 遇到 provider 不可用时抛出 `import_load_unavailable`，遇到版本不兼容时抛出 `assembly_version_mismatch`，遇到 target module ABI hash drift 时抛出 `assembly_signature_mismatch`，member token/hash/bytes drift 仍抛出 `import signature mismatch`。这些诊断包含 canonical module/member 和相应 expected/actual 或 min/max/actual context，避免静默接受 ABI 漂移。

当前新增的运行期 binding sidecar 会在 import verifier 成功完成 target module hash、target token、target signature hash 和 target signature bytes 校验后写入或更新 `SZrFunction.moduleMetadataBindings`。这条 sidecar 同时覆盖 `moduleEntryEffects` 校验路径和 direct `moduleMetadataTokenRecords` 解码路径：`MEMBER_REF` binding 保留原始 caller member ref token，并记录 expected 与 resolved member metadata/signature token、signature hash、module ABI hash；`ASSEMBLY_REF` binding 保留 caller assembly ref token、paired `SIGNATURE` token/hash 和 expected module ABI hash，并把 resolved metadata token 记录为 provider `ZR_METADATA_TABLE_MODULE` RID 1、resolved signature token/hash 记录为 provider `MODULE` record 的 paired `SIGNATURE`、resolved module hash 记录为 provider entry function `moduleSignatureHash`。旧 provider 产物没有 `MODULE` record 时，AssemblyRef binding 仍可兼容保留 0 resolved signature identity。TypeSpec/TypeDef sidecar 继续记录 expected/resolved layout identity。调用者可用 `ZrCore_Function_FindModuleMetadataBinding(function, refToken)` 查询成功绑定结果；`.zro` patch 28 会逐字段保存基础 binding，patch 32 会保存 layout fields，并在 `io_runtime.c` 加载时恢复。source module summary 也会在 metadata tokens 构建后回填最终 typed export token/hash/value type/参数类型，避免正常 guard 因 stale summary 产生 target identity 与 appended signature bytes 分裂。`function_metadata_binding.c` 还提供 `ZrCore_Function_UpsertModuleMetadataBinding()`、`ZrCore_Function_BindMatchingTypeSpecMetadata()`、`ZrCore_Function_BindMatchingTypeSpecMetadataWithStatus()` 和 `ZrCore_Function_BindMatchingTypeRefMetadata()`：upsert helper 集中维护 binding sidecar 容量和复用逻辑，TypeSpec helpers 按 caller/provider `TYPE_SPEC` 的 canonical signature hash 与 signature blob 字节相等记录跨模块 TypeSpec baseline binding；对于 union TypeSpec，还会定位对应 caller/provider `TYPE_DEF`，要求 TypeDef signature hash/blob 和 record-level layoutVersion/layoutHash 都一致，再写入 TypeSpec binding 与 TypeDef layout binding。TypeRef helper 只处理 caller `TYPE_REF.targetMetadataToken` 指向 `TYPE_DEF` 的记录，并要求 target TypeDef token、signature hash/token、module hash、layout identity 和 base name 匹配后写入 TypeRef binding。status helper 可报告 caller/matched/unmatched、definition mismatch、layout mismatch 计数和首个 expected/actual context。成功 member import verification 后，`module_import_signature.c` 会 opportunistically 调用 status helper；匹配项继续写入 provider `TYPE_SPEC` / paired `SIGNATURE` binding，unmatched 或 definition/layout mismatch 通过 module-load diagnostic 暴露为 `type_spec_mismatch`，但不改变 import 成功语义；definition/layout mismatch 不会留下 partial sidecar。

TypeRef binding uses the same status/diagnostic pattern: `ZrCore_Function_BindMatchingTypeRefMetadataWithStatus()` reports unmatched, definition mismatch, and layout mismatch for stable `TYPE_REF -> TYPE_DEF` records, and successful member import verification records `type_ref_mismatch` when those records drift while leaving import success best-effort.

Project manifest preserve update (2026-06-24 23:36:19 +08:00): `.zrp` project loading now parses declaration-level `preserve` arrays into `SZrLibrary_ProjectPreserveRule` records on `SZrLibrary_Project`. The parser accepts `type`, `method`, and `generic` rule kinds, a safe dotted `target`, and optional `members` values `all` or `methods`; invalid target text rejects the manifest before the project model is used by later AOT metadata or trimming stages. `zrp.schema.json` mirrors this shape. This is only the manifest declaration surface: later AOT passes must still bind targets to metadata tokens, function flat indices, generic instantiations, and writer roots. Verification covered WSL GCC/clang `zr_vm_project_manifest_normalization_test` 12/0 and `zr_vm_project_import_resolver_test` 9/0, schema JSON parsing, Windows MSVC Debug manifest normalization 12/0 and import resolver 9/0, plus CLI `hello_world`.

Project manifest export declaration update (2026-07-02 00:37:35 +08:00): `.zrp` project loading now parses declaration-level `exports` arrays into `SZrLibrary_ProjectExportDeclaration` records on `SZrLibrary_Project`. The parser accepts `type`, `method`, and `field` export kinds with the same safe dotted target policy as preserve declarations, rejects unsupported kinds and unsafe targets before later AOT stages can consume the model, and `zrp.schema.json` mirrors this shape. This is only the manifest declaration input for later persistent export manifest/table writing; it does not bind targets to metadata tokens, rewrite compacted export tokens into a file-level table, or load provider versions. Verification covered WSL GCC/clang and Windows MSVC Debug `zr_vm_project_manifest_normalization_test` direct runs at 28/0; the target is not currently registered as a CTest in these build trees.

Project manifest export writer-option update (2026-07-02 01:09:04 +08:00): the CLI AOT bridge now maps those parsed `exports` declarations into `SZrAotWriterOptions.manifestExportDeclarations`. `compiler_aot_exports.{h,c}` owns the project-to-writer kind conversion and CLI scratch storage, while `backend_aot_c_emitter.c` emits `manifest.exports` plus per-entry `manifest.export[i] kind=... target=...` diagnostics. Verification covered WSL GCC/clang/Windows MSVC Debug direct `zr_vm_cli_aot_writer_options_test` 15/0, `zr_vm_aot_c_source_contracts_test` 24/0, and `zr_vm_aot_c_code_stripping_test` 10/0; focused CTest matched registered `cli_aot_writer_options` and `aot_c_code_stripping` at 2/2 on all three toolchains. This still does not bind export targets to metadata tokens, write a persistent export table, publish compacted tokens back to a `.zrp` file, or validate provider versions.

Project manifest export member-token binding update (2026-07-02 01:43:16 +08:00): method `exports` declarations now carry a writer-visible member-token binding when the current function has a matching typed exported function symbol with a nonzero `MEMBER_DEF` token. `SZrAotManifestExportDeclaration` exposes `hasMemberTokenBinding/memberToken`, `compiler_aot_exports.c` resolves exact or current-module-prefixed method targets from `typedExportedSymbols`, and generated C emits `manifest.export[i].memberToken = 0x...` diagnostics. Verification covered WSL GCC/clang/Windows MSVC Debug direct `zr_vm_cli_aot_writer_options_test` 16/0, `zr_vm_aot_c_source_contracts_test` 24/0, and `zr_vm_aot_c_code_stripping_test` 10/0; focused CTest matched registered `cli_aot_writer_options` and `aot_c_code_stripping` at 2/2 on all three toolchains. This remains a current-module method writer-input binding only; type/field export binding, persistent export table writing, compacted-token file publication, provider version binding, and full trim analysis remain open.

Project manifest export field member-token binding update (2026-07-02 01:57:22 +08:00): field `exports` declarations now reuse the same writer-visible member-token binding when the current function has a matching typed exported variable symbol with a nonzero `MEMBER_DEF` token. `compiler_aot_exports.c` maps method declarations to function symbols and field declarations to variable symbols before applying the existing exact/current-module-prefixed target match and token-shape guard; generated C continues to emit `manifest.export[i].memberToken = 0x...` diagnostics. Verification covered WSL GCC/clang/Windows MSVC Debug direct `zr_vm_cli_aot_writer_options_test` 17/0, `zr_vm_aot_c_source_contracts_test` 24/0, and `zr_vm_aot_c_code_stripping_test` 10/0; focused CTest matched registered `cli_aot_writer_options` and `aot_c_code_stripping` at 2/2 on all three toolchains. This remains current-module method/field writer input only; type export binding, persistent export table writing, compacted-token file publication, provider version binding, and full trim analysis remain open.

Project manifest export type-token binding update (2026-07-02 02:22:47 +08:00): type `exports` declarations now carry a writer-visible type-token binding when the current function has a matching local `TYPE_DEF` metadata token record. `SZrAotManifestExportDeclaration` exposes `hasTypeTokenBinding/typeToken`, `compiler_aot_exports.c` matches exact or current-module-prefixed type targets against `TYPE_DEF(valueType,stringIndex)` signature records and the metadata string heap, and generated C emits `manifest.export[i].typeToken = 0x...` diagnostics. Verification covered WSL GCC/clang/Windows MSVC Debug direct `zr_vm_cli_aot_writer_options_test` 18/0, `zr_vm_aot_c_source_contracts_test` 24/0, and `zr_vm_aot_c_code_stripping_test` 10/0; focused CTest matched registered `cli_aot_writer_options` and `aot_c_code_stripping` at 2/2 on all three toolchains. This remains current-module writer input only; persistent export table writing, compacted-token file publication, provider version binding, and full trim analysis remain open.

Project manifest export table publication update (2026-07-02 03:24:57 +08:00): generated AOT C now publishes a real `SZrAotManifestExportEntry` table for manifest `exports`. The ABI exposes entry kind/flags/target/typeToken/memberToken plus `manifestExports/manifestExportCount` on both `ZrAotCompiledModule` and `SZrAotCodeRegistration`; `backend_aot_c_zrp_metadata_member_token.c` builds the table from writer declarations, preserves bound `TYPE_DEF` tokens, remaps bound method/field `MEMBER_DEF` tokens through the retained source-to-compacted member-token sidecar, and rejects invalid kinds, targets, token shapes, or pruned member tokens without a retained remap. Root and mirrored AOT runtime descriptor validation now require descriptor/codeRegistration manifest export table pointer/count parity and validate every entry before execution. Verification covered WSL GCC direct remap 9/0, writer options 18/0, source contracts 24/0, and WSL GCC/clang/Windows MSVC Debug focused CTest `aot_c_zrp_metadata_export_token_remap|cli_aot_writer_options` at 2/2 plus source contracts 24/0. Cross-module provider loading/version binding, standalone provider manifest consumption, complete metadata sweep/pruning, and full trim analysis remain open.

Project manifest export runtime mirror update (2026-07-02 03:57:34 +08:00): attached `SZrMetadataRuntime` now mirrors the generated-C manifest export table pointer/count from `SZrAotCodeRegistration`. `ZrCore_Module_AttachMetadataRuntime()` copies `manifestExports/manifestExportCount`, and `tests/module/test_metadata_runtime_manifest_exports.c` locks type and method export entries as readable through the runtime after attach. The mirror fields are appended at the end of `SZrMetadataRuntime` to keep existing positional initialization of older fields stable. Verification covered WSL GCC/clang/Windows MSVC Debug direct manifest export runtime mirror 1/0, metadata runtime query 25/0, binding compatibility 15/0, and focused CTest 3/3. This is attached-runtime consumption only; cross-module provider loading/version binding, standalone provider import-path manifest consumption, complete metadata sweep/pruning, and full trim analysis remain open.

Project manifest export runtime view update (2026-07-02 04:23:15 +08:00): attached metadata runtime now exposes `SZrMetadataRuntimeManifestExportView` and `ZrCore_MetadataRuntime_ReadManifestExportView()` over the generated-C manifest export table. Consumers can query one `kind + target` export and receive the stable entry pointer/index plus type/member token; duplicate target/kind entries, missing required token flags, and invalid token shapes fail closed with a cleared output view. Verification covered WSL GCC/clang/Windows MSVC Debug direct manifest export runtime view 4/0, metadata runtime query 25/0, binding compatibility 15/0, and focused CTest for those three targets. This is still a local attached-runtime reader, not cross-module provider selection or import-path version binding.

Project manifest export binding gate update (2026-07-02 04:46:47 +08:00): attached metadata runtime now exposes `ZrCore_MetadataRuntime_CheckManifestExportBindingCompatibility()` as the first local gate that joins manifest export lookup with token binding compatibility. The predicate reads one `kind + target` export view, reuses the existing token/signature/module/layout compatibility report path, and then requires the binding's resolved metadata token to match the export table's published type/member token. Missing exports return `MANIFEST_EXPORT_NOT_FOUND`, token mismatches return `MANIFEST_EXPORT_TOKEN_MISMATCH`, and version drift still reports the existing module-version mismatch. Verification covered WSL GCC/clang/Windows MSVC Debug direct manifest export runtime 7/0, binding compatibility 15/0, and focused CTest for both targets. This is still an attached-runtime predicate, not provider loading or import-path wiring.

Project manifest export provider verifier gate update (2026-07-02 05:34:27 +08:00): `module_import_signature.c` now invokes `zr_module_import_signature_verify_manifest_export_binding()` after provider typed export token/signature/hash/blob checks and before writing the module metadata binding sidecar. The new helper lives in `module_import_signature_manifest_export.c/.h`, maps import call/read/ref/type effects to manifest export method/field/type kinds, builds a binding snapshot from the caller `MEMBER_REF`, effect target identity, provider typed export identity, and provider entry module hash, then delegates to `ZrCore_MetadataRuntime_CheckManifestExportBindingCompatibility()`. Providers without an attached manifest export table keep the legacy verifier behavior; providers with a table must publish the same type/member token as the selected typed export symbol. Verification covered WSL GCC/clang/Windows MSVC Debug direct metadata TypeRef/import binding 9/0, manifest export runtime 7/0, binding compatibility 15/0, and focused CTest 3/3 for those targets. This closes only the attached provider manifest-export token identity gate; standalone provider import-path discovery/loading/version selection and full metadata sweep remain open.

Project import provider location update (2026-07-02 05:58:12 +08:00): `ZrLibrary_Project_ResolveImportProviderLocation()` now gives the AOT provider-binding path a single project-layer discovery API. It resolves a raw import specifier through the existing canonical module-key resolver, attaches AssemblyRef identity/version range data from `ZrLibrary_Project_GetDependencyImportVersionRange()`, and returns either `.zrm` archive/module entry pointers or `.zrp` source/binary/intermediate provider paths. This closes import-path location discovery for declared project and assembly-container references only; it does not load provider runtimes, choose among multiple installed versions, or validate export metadata.

Project import provider AOT load-request update (2026-07-02 06:13:21 +08:00): `ZrLibrary_Project_ResolveImportProviderAotLoadRequest()` now turns provider location discovery into a stable load-request record for the later runtime loader. The request carries backend kind, canonical `$alias@version/module`, descriptor-local module name, declared assembly/version range, `.zrp` source/binary/intermediate paths, and backend-specific `aot_c` / `aot_llvm` library path. `.zrm` providers expose archive/entry pointers and deliberately leave `libraryPath` empty so the runtime does not confuse archive entries with filesystem dynamic libraries. This closes request planning only; runtime dynamic loading, exact alias/version verification, automatic range-based candidate selection, and export metadata attach remain open.

Project import provider AOT runtime update (2026-07-02 06:41:32 +08:00): strict AOT module loading now consumes the provider load-request record for canonical `$alias@version/module` imports. `aot_runtime_prepare_record()` resolves the provider `.zrp` source/binary/library paths through `ZrLibrary_Project_ResolveImportProviderAotLoadRequest()`, validates the dynamic-library descriptor against the provider-local module name, and keeps the runtime module cache keyed by the canonical import identity. `.zrm` provider archive entries remain fail-closed at this layer instead of being treated as filesystem dynamic libraries. A follow-up success fixture covers `.zrp` generated dynamic-library loading; exact alias/version verification, automatic range-based candidate selection, and export metadata attach remain open.

Project import provider AOT dynamic-library success update (2026-07-02 07:01:13 +08:00): the provider `.zrp` success path now has a generated-library fixture. `tests/parser/test_aot_c_provider_shared_library_smoke.c` writes a root project and provider project, compiles provider `ops/sum.zr` to `.zro`, emits AOT C with provider-local `moduleName = "ops/sum"`, builds `/deps/math/bin/aot_c/lib/zrvm_aot_ops_sum.so`, and imports `$mathLocal@2.1.0/ops/sum` under strict AOT C. The fixture verifies the canonical module cache key, AOT C executed-via state, and published `seed` export. Exact alias/version verification, automatic range-based candidate selection, and cross-provider export metadata attach remain open.

Project import provider version-selection update (2026-07-02 07:25:55 +08:00): project manifest parsing and AOT load-request discovery now cover exact multi-version references for the same provider assembly. `tests/library/test_project_import_provider_version_selection.c` creates `mathV2` and `mathV3` references to `zr.math` versions `2.1.0` and `3.1.0`; `ZrLibrary_Project_ResolveImportProviderAotLoadRequest()` selects the requested alias/version exactly and returns distinct canonical keys plus provider-local source, binary, and AOT library paths. Project parsing also rejects strict `major.minor.patch` provider references whose actual version is outside the declared `[min, max)` range, including `.zrp` and `.zrm` reference paths. Automatic range-based candidate selection and cross-provider export metadata attach remain open.

Project manifest AOT mode writer-option update (2026-06-25 00:29:49 +08:00): `.zrp` `aotMode` is now consumable by the CLI/compiler AOT writer option bridge. `ZrCli_Compiler_ApplyProjectAotWriterOptions()` reads `SZrLibrary_Project.aotMode` from `SZrCliProjectContext.libraryProject` and writes `SZrAotWriterOptions.requireFullAot`, setting true for `full-aot` and false for default hybrid while preserving the rest of the writer option struct. Verification covered WSL GCC/clang and Windows MSVC Debug `zr_vm_cli_project_incremental_test` 10/0, plus Windows CLI `hello_world`. This update does not add a CLI AOT C emission mode or full-AOT closure diagnostics.

Project CLI AOT C emission update (2026-06-25 01:13:27 +08:00): project compilation now has a real CLI AOT C emission entry through `--emit-aot-c`. `SZrCliCommand.emitAotC` flows into `ZrCli_Compiler_CompileProjectWithSummary()`, which writes `.zro` first, reads that binary as the embedded module blob, sets `SZrAotWriterOptions.inputKind = ZR_AOT_INPUT_KIND_BINARY`, applies project `aotMode` through `ZrCli_Compiler_ApplyProjectAotWriterOptions()`, and emits `bin/aot_c/src/<module>.c`. The incremental manifest is now v3 and records `aot_c` paths for stale artifact cleanup. Verification covered WSL GCC/clang and Windows MSVC Debug `cli_args|cli_project_incremental`, plus a Windows CLI run that regenerated `bin/aot_c/src/main.c` after deletion. This still does not bind preserve rules to writer roots or implement default-minimal metadata policy.

Project manifest method preserve root update (2026-06-25 01:53:48 +08:00): same-module `.zrp` `preserve` rules with `kind: "method"` now bind to AOT writer manifest roots during project AOT C emission. `ZrParser_Writer_ResolveTopLevelCallableFlatIndex()` resolves an entry function top-level callable binding name to the AOT flat function index, and `ZrCli_Compiler_ApplyProjectAotPreserveRules()` injects the resolved roots into `SZrAotWriterOptions.manifestPreserveFunctionFlatIndices`. The focused test proves `main.kept` preserves an otherwise unreachable callable child under opt-in code stripping. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug focused CTest groups. Type/generic preserve targets, metadata-token resolution, cross-module method binding, and default-minimal metadata policy remain future work.

Project manifest type/method preserve root update (2026-06-25 02:09:47 +08:00): method preserve matching now treats dotted callable names such as `Widget.kept` as exact top-level callable binding names before falling back to current-module prefix stripping. Type preserve rules with `members: "methods"` or `"all"` scan top-level callable bindings and inject every `<type>.` prefixed callable as a writer manifest root, so `Widget` can preserve `Widget.used` and `Widget.kept` together. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug `cli_project_incremental|cli_aot_writer_options|aot_c_code_stripping`. Generic preserve, metadata-token resolution, cross-module targets, annotations, and feature switches remain future work.

Project manifest preserve feature-condition update (2026-06-25 02:23:14 +08:00): `.zrp` `preserve` rules now carry the manifest-side feature switch condition model. `SZrLibrary_ProjectPreserveRule` stores `feature`, `hasFeatureValue`, and `featureValue`; the parser accepts a safe dotted `feature` name only when paired with boolean `featureValue`, rejects either field on its own, and `zrp.schema.json` mirrors the same fields and dependency constraints. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug `zr_vm_project_manifest_normalization_test` 17/0, schema JSON parsing, WSL GCC `cli_aot_writer_options|aot_c_code_stripping` CTest 2/2, and `git diff --check`. This is declaration modeling only: build feature values are not yet evaluated to enable/disable roots.

Project manifest feature switch root-gating update (2026-06-25 02:40:15 +08:00): `.zrp` manifests now have a top-level `features` object for boolean switch values. `project_features.{h,c}` owns parsing and memory cleanup so the large project loader stays an orchestration boundary. `ZrCli_Compiler_ApplyProjectAotPreserveRules()` evaluates a rule's `feature` / `featureValue` before resolving method/type targets: matching switches activate the preserve rule, missing or mismatched switches skip it. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug `zr_vm_project_manifest_normalization_test` 19/0 and `zr_vm_cli_aot_writer_options_test` 5/0, WSL GCC CTest `cli_aot_writer_options|aot_c_code_stripping` 2/2, schema JSON parsing, and `git diff --check`. Generic preserve, metadata-token binding, cross-module roots, annotations, and default-minimal metadata remain future work.

Project manifest generic preserve argument update (2026-06-25 03:02:14 +08:00): `.zrp` generic preserve rules now have a concrete argument declaration surface. `SZrLibrary_ProjectPreserveRule` stores `genericArguments`, `genericArgumentCount`, and `genericArgumentCapacity`; `project_preserve.c` requires `kind: "generic"` rules to carry a non-empty `arguments` array of safe dotted type names, rejects missing/empty/non-array/invalid arguments, and rejects `arguments` on non-generic rules. `zrp.schema.json` mirrors `arguments`, `minItems: 1`, and the generic-only conditional constraint. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug `zr_vm_project_manifest_normalization_test` 25/0, schema JSON parsing, WSL GCC CTest `cli_aot_writer_options|aot_c_code_stripping` 2/2, and `git diff --check`. This is declaration modeling only; generic instantiation roots, MethodSpec/TypeSpec token binding, cross-module target binding, and default-minimal metadata remain future work.

Project manifest generic preserve writer-root update (2026-06-25 03:27:16 +08:00): `.zrp` generic preserve declarations now have a writer-visible bridge. `SZrAotWriterOptions` carries `manifestPreserveGenericRoots` and `manifestPreserveGenericRootCount`; `ZrCli_Compiler_ApplyProjectAotPreserveRules()` forwards a generic preserve rule's target and arguments after feature gating, and generated AOT C records `manifest.genericRoots` plus each target/argument as a header diagnostic. Verification covered WSL GCC, WSL clang, and Windows MSVC Debug `zr_vm_cli_aot_writer_options_test` 6/0, CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed` 3/3 in all three environments, schema JSON parsing, and `git diff --check` with only LF/CRLF warnings. This still does not materialize MethodSpec/TypeSpec tokens, generic instantiation table entries, cross-module generic targets, or default-minimal metadata output.

Project manifest generic preserve TypeSpec binding update (2026-06-25 04:00:47 +08:00): `.zrp` generic preserve roots can now bind to an already-emitted current-module `TYPE_SPEC` metadata record. `SZrAotManifestGenericRoot` carries optional TypeSpec/signature token and hash fields; the CLI AOT preserve bridge matches the root target and concrete arguments against `GENERIC_INST(TYPE_REF target, args...)` signatures in the current function metadata record stream, then generated AOT C reports the bound TypeSpec token, paired signature token, and signature hash in the manifest diagnostics. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 7/0, WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5, and `git diff --check` with only LF/CRLF warnings. This is a narrow current-module existing-TypeSpec bridge; MethodSpec parsing, TypeSpec synthesis, generic instantiation materialization, cross-module generic targets, and default-minimal metadata output remain open.

Project manifest full-AOT generic preserve TypeSpec closure update (2026-06-25 04:14:31 +08:00): full-AOT AOT C generation now consumes the TypeSpec binding status above as a closure gate. `ZrParser_Writer_WriteAotCFileWithOptions()` rejects `SZrAotWriterOptions.manifestPreserveGenericRoots` when `requireFullAot` is true and any root lacks `hasTypeSpecBinding`, so a text-only `.zrp` generic preserve declaration is not treated as a complete AOT generic closure. Hybrid generation still emits the manifest diagnostics for unbound roots. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 8/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This does not synthesize missing TypeSpecs, parse MethodSpecs, materialize generic instantiation roots, bind cross-module generic targets, or complete default-minimal metadata output.

Project manifest generic preserve instantiation update (2026-06-25 04:50:01 +08:00): TypeSpec-bound `.zrp` generic preserve roots now also carry a writer-visible generic instantiation identity. `SZrAotManifestGenericRoot` stores `hasGenericInstantiationBinding`, `genericInstantiationBaseToken`, `genericInstantiationInstanceId`, and `genericInstantiationShareKind`; the CLI AOT preserve bridge builds resolved type arguments from the manifest concrete argument text and reuses `SZrGenericInstantiationTable_GetOrAddResolved()` to assign a stable instance id/share kind. Generated AOT C reports `genericInstance.baseToken`, `genericInstance.id`, and `genericInstance.shareKind` in the manifest diagnostics. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 9/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This is TypeSpec-backed identity materialization only; MethodSpec parsing, missing TypeSpec synthesis, cross-module generic targets, and default-minimal metadata output remain open.

Project manifest full-AOT generic instantiation closure update (2026-06-25 05:08:49 +08:00): full-AOT AOT C generation now requires a manifest generic preserve root to carry both TypeSpec metadata binding and generic instantiation identity. `backend_aot_manifest_generic_roots_closed_for_full_aot()` rejects TypeSpec-only roots whose `hasGenericInstantiationBinding` is false, so a root with token/signature metadata but no `cInstanceId`/share kind is not treated as a closed full-AOT generic instance. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 10/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This is a writer-side closure gate only; MethodSpec parsing, missing TypeSpec synthesis, cross-module generic targets, reflection-driven generic construction, and default-minimal metadata output remain open.

Project manifest generic instantiation open-base update (2026-06-25 05:28:38 +08:00): TypeSpec-bound `.zrp` generic preserve roots now prefer a same-module open generic base token when materializing their generic instantiation identity. `compiler_aot.c` scans current-function metadata token records for a `TYPE_REF` signature whose name matches the generic target, passes that token to `SZrGenericInstantiationTable_GetOrAddResolved()`, and keeps the previous TypeSpec token fallback when no such record exists. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 11/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This closes only current-module TypeRef-backed open-base binding; MethodSpec parsing, TypeSpec synthesis, cross-module generic targets, reflection-driven generic construction, and default-minimal metadata output remain open.

Project manifest generic instantiation TypeDef-base update (2026-06-25 05:41:31 +08:00): the open-base resolver now also accepts current-module `TYPE_DEF` bases. `compiler_aot.c` matches `GENERIC_INST(TYPE_DEF target, args...)` TypeSpec signatures, scans same-table `TYPE_DEF` records for the open generic target name, and passes the open TypeDef token to `SZrGenericInstantiationTable_GetOrAddResolved()`; TypeRef-backed open bases and the TypeSpec fallback are unchanged. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 12/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This closes only current-module TypeDef-backed open-base binding; MethodSpec parsing, TypeSpec synthesis, cross-module generic targets, reflection-driven generic construction, and default-minimal metadata output remain open.

Project manifest generic synthesized TypeSpec update (2026-06-25 06:03:45 +08:00): `.zrp` generic preserve roots can now get a writer-visible TypeSpec/signature binding even when the current function has no pre-existing closed `TYPE_SPEC` record. `compiler_aot.c` scans current-function metadata for a same-name open `TYPE_DEF` or `TYPE_REF` record, appends a synthesized `TYPE_SPEC` plus paired `SIGNATURE` record and signature blob to the in-memory function metadata, then reuses the generic instantiation materialization path so full-AOT sees both TypeSpec binding and `baseToken`/`cInstanceId`/`shareKind`. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 13/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This closes only current-module writer-visible synthesis for AOT C emission; MethodSpec parsing, cross-module generic targets, reflection-driven generic construction, default-minimal metadata output, and full `.zro` metadata persistence policy remain open.

Project manifest generic MethodSpec update (2026-06-25 06:26:16 +08:00): `.zrp` generic preserve roots whose target names a current-module typed exported method can now bind to an existing MethodSpec-shaped signature. `SZrAotManifestGenericRoot` carries MethodSpec token, method token, and instantiation signature hash; `compiler_aot.c` matches `GENERIC_INST(MEMBER_REF methodToken, args...)` against typed exported method metadata; generated AOT C reports the MethodSpec identity, and full-AOT accepts that method root without requiring TypeSpec/generic type-instantiation identity. Verification covered WSL GCC `zr_vm_cli_aot_writer_options_test` 14/0 plus WSL GCC/clang and Windows MSVC Debug CTest `cli_aot_writer_options|aot_c_code_stripping|aot_c_generic_call_typed|generic_instantiation|metadata_token_model` 5/5. This closes only current-module writer-visible MethodSpec binding; cross-module generic method targets, generic method body closure, zrp MethodSpec table persistence, reflection-driven generic construction, and default-minimal metadata output remain open.

共享 string heap 索引化、`.zro` patch 30 roundtrip 及 project-import metadata blob 验证读取已同步；descriptor plugin safe unload/cache invalidation baseline 已接入 native registry owner refcount；loader-facing TypeSpec mismatch diagnostics 已接入 module-load diagnostic channel；AOT descriptor 更细 loader diagnostics 已接入 AOT runtime 和 core module-load diagnostic channel。当前实现先保证 token 结构、函数级 ref owner-chain、entry-function 级 module ref 聚合表、导出签名中闭型/复合类型的基础 `TYPE_SPEC` records、TypeSpec canonical signature 去重、TypeSpec canonical signature 跨模块 baseline binding、union TypeSpec→TypeDef definition/layout binding、TypeRef→TypeDef consumer-side binding、provider summary backed stable TypeRef producer（含嵌套泛型实参、module-qualified typed local annotations 和 destructured/unqualified alias annotations）、TypeSpec mismatch status 和 loader-facing diagnostic、runtime 对 module ref table 的优先消费/直连扫描/旧 records 回退、runtime ref→def binding sidecar、AssemblyRef→MODULE binding sidecar、binding 查询 API、函数级与 module ref table 的 token/signature record 查询 API 和 `.zro` patch 28 roundtrip、`.zro` patch 29 provider module version / AssemblyRef semver range roundtrip、`.zro` patch 30 metadata string heap roundtrip、`.zro` patch 31 record layout identity roundtrip、`.zro` patch 32 binding layout identity roundtrip、dependency manifest 显式 AssemblyRef min/max range、future patch actual/supported 诊断、target module hash 绑定、target token 绑定、target token mismatch diagnostics、target signature hash、def/ref canonical MethodSig/FieldSig、非 union 泛型闭型 `GENERIC_INST` 签名编码、签名 hash 和 module ABI hash 基础、native/source/binary compile-time member identity、registered native verifier 覆盖、required provider unavailable `import_load_unavailable`、project source loader/native descriptor plugin load-error detail、AOT descriptor field-level loader detail、registry owner refcount API、descriptor plugin live-owner invalidation/reload guard、同名 provider export 运行期候选绑定、二进制 roundtrip 稳定，以及 required/guard import 的运行期 version/module/token/hash-first + signature-confirmed 消费路径（含 required member-level `import signature mismatch`、required module-level `assembly_signature_mismatch`、required version-level `assembly_version_mismatch`、guard version mismatch、guard target module hash mismatch、guard target token mismatch、guard target bytes mismatch、entry effects 缺失时的 module ref table drift、provider unavailable 与 nested callable caller）。

TypeRef mismatch status and loader-facing `type_ref_mismatch` diagnostics are also covered for provider-summary backed stable TypeRefs; module-qualified explicit typed local annotations and destructured/unqualified alias mapping are covered.

TypeRef producer update (2026-06-18 20:44:02 +08:00): producer-side stable provider TypeRef extraction is now covered for import target signature return/parameter types and their nested generic arguments when the provider summary exposes matching `TYPE_DEF` records. Module-qualified typed local annotation support now reuses this helper.

TypeRef mismatch status update (2026-06-18 21:22:00 +08:00): `SZrMetadataTypeRefBindStatus` and `ZrCore_Function_BindMatchingTypeRefMetadataWithStatus()` now expose stable TypeRef binding counts plus unmatched, definition mismatch, and layout mismatch context. Successful member import verification records `type_ref_mismatch` in the module-load diagnostic channel when stable provider TypeRef binding drifts, while preserving best-effort import success and avoiding partial sidecar writes.

TypeRef explicit annotation update (2026-06-18 22:35:57 +08:00): `compiler_metadata_type_ref.c/.h` now splits top-level module-qualified type names and scans `typedLocalBindings`, so a local annotation such as `provider.Option<int>` produces a stable provider `TYPE_REF` and paired `SIGNATURE` even when the function has no import member effects. `compiler_metadata_token.c` collects those explicit modules, writes the needed metadata string heap entries, emits an explicit `ASSEMBLY_REF`, and resolves the TypeRef owner through the AssemblyRef RID resolver. Verification covered WSL GCC/clang `zr_vm_metadata_type_ref_binding_test` 6/0, `zr_vm_metadata_token_model_test` 21/0, `zr_vm_project_import_canonicalization_test` 31/0, plus MSVC Debug `zr_vm_metadata_type_ref_binding_test` 6/0.

TypeRef alias annotation update (2026-06-18 23:07:30 +08:00): `compiler_metadata_type_ref.c/.h` now resolves unqualified typed local aliases through `typeValueAliases`, so destructuring import bindings such as `Option -> provider.Option` let local annotations `Option<int>` and nested forms like `Box<Option<int>>` produce stable provider `TYPE_REF` records and explicit `ASSEMBLY_REF` owners without import member effects. The token string heap and explicit module collector reuse the same resolver. Verification covered WSL GCC/clang `zr_vm_metadata_type_ref_binding_test` 8/0, `zr_vm_metadata_token_model_test` 21/0, `zr_vm_project_import_canonicalization_test` 31/0, plus MSVC Debug `zr_vm_metadata_type_ref_binding_test` 8/0.

TypeSpec status update (2026-06-18 10:52:36 +08:00): canonical signature deduplication is implemented for exported-signature TypeSpec records, and cross-module baseline binding now records matching caller/provider `TYPE_SPEC` records in `moduleMetadataBindings` by signature hash plus blob equality. Follow-up TypeSpec mismatch status and loader-facing diagnostics are now covered; union TypeSpec→TypeDef definition/layout binding is covered by the 2026-06-18 16:11:43 update.

TypeSpec mismatch status update (2026-06-18 12:16:33 +08:00): `SZrMetadataTypeSpecBindStatus` and `ZrCore_Function_BindMatchingTypeSpecMetadataWithStatus()` now expose core-level mismatch status for caller/provider TypeSpec binding. The status reports caller, matched, and unmatched TypeSpec counts plus the first unmatched caller token/hash; the old binding wrapper keeps best-effort import behavior. Loader-facing diagnostics are now covered by the 2026-06-18 13:00:54 update.

TypeSpec loader diagnostic update (2026-06-18 13:00:54 +08:00): successful member import verification now calls the TypeSpec status helper and records a module-load diagnostic `type_spec_mismatch` when caller TypeSpec records do not match provider TypeSpec records. The diagnostic includes canonical module/member context, caller/matched/unmatched counts, and the first unmatched TypeSpec token/hash; import verification remains successful so TypeSpec sidecars stay opportunistic.

Descriptor plugin invalidation update (2026-06-18 12:49:27 +08:00): native registry invalidation and descriptor plugin reload now reject live descriptor-plugin modules before clearing module cache, closing plugin handles, or replacing descriptor records. `ZR_LIB_NATIVE_REGISTRY_ERROR_MODULE_IN_USE` reports the module/source/refcount context; tests cover a real descriptor plugin fixture with a simulated live owner ref and the ref-zero success path. AOT descriptor diagnostics are covered by the 2026-06-18 13:33:34 update.

AOT descriptor diagnostic update (2026-06-18 13:33:34 +08:00): `aot_runtime.c` validates descriptors field by field and records the first bad ABI/backend/module/entry/blob/thunk field in `lastError`, for example `abiVersion expected=2 actual=1`. `ZrLibrary_AotRuntime_ModuleLoader()` forwards that runtime detail to the core module-load diagnostic as `loader=aot-runtime ... result=descriptor-load-failed ... detail=...`, so required import/load diagnostics no longer collapse AOT descriptor problems into a generic load failure. WSL GCC verification covered `zr_vm_project_import_canonicalization_test` 31/0, `zr_vm_metadata_token_model_test` 15/0, `zr_vm_native_closure_value_test` 3/0, `zr_vm_aot_c_descriptor_diagnostics_test` 1/0, and CTest `aot_c_descriptor_diagnostics|metadata_token_model` 2/2.

TypeDef baseline update (2026-06-18 14:09:55 +08:00): local union definitions referenced by exported typed signatures now receive `TYPE_DEF` metadata token records. `compiler_metadata_type_def.c/.h` scans exported return/parameter types, resolves current-script union declarations through the shared signature helper, deduplicates by union base name and generic arity, and emits a `TYPE_DEF` + paired `SIGNATURE` record whose blob is `TYPE_DEF <metadataStringHeap name index> <genericArity>`. This reuses the existing signature blob heap, `signatureHash`, shared metadata string heap, `.zro` patch 30 persistence, and runtime copy path; no new patch was required. Verification covered WSL clang/gcc `zr_vm_metadata_token_model_test` 16/0, `zr_vm_project_import_canonicalization_test` 31/0, and metadata CTest 1/1.

TypeDef variant contract update (2026-06-18 14:35:52 +08:00): local union `TYPE_DEF` signature blobs now include the public variant and payload field contract. The blob shape is `TYPE_DEF <metadataStringHeap name index> <genericArity> <variantCount> ...`; each variant writes heap-indexed variant name, variant kind, default-using flag, and field count; each payload field writes heap-indexed field name, passing mode, and canonical TypeSig. `compiler_metadata_type_def_collect_strings()` feeds those names and referenced type names into the shared metadata string heap, so this remains a patch 30 heap-indexed encoding with no new `.zro` patch. Verification covered WSL clang/gcc `zr_vm_metadata_token_model_test` 16/0, `zr_vm_project_import_canonicalization_test` 31/0, and metadata CTest 1/1. Windows Debug smoke exposed and fixed the MSVC DLL export boundary for `compiler_build_function_metadata_tokens` by adding `ZR_PARSER_API`; after that, MSVC `zr_vm_metadata_token_model_test` also passed 16/0, and WSL clang/gcc metadata quick rebuilds remained 16/0.

TypeDef layout identity update (2026-06-18 15:28:05 +08:00): local union `TYPE_DEF` records now carry record-level `layoutVersion` / `layoutHash` for inline value ABI layout checks. The physical layout identity is not written into the `TYPE_DEF` signature blob and therefore does not change `signatureHash`; it is persisted separately in `.zro` patch 31 after each record's `signatureHash`, with older patches reading zero. `compiler_metadata_type_def_layout.c/.h` computes the fingerprint from tag size, payload offset/size/align, overall size/align, and each variant payload field's offset/size/align. Verification covered WSL clang/gcc `zr_vm_metadata_token_model_test` 17/0, `zr_vm_project_import_canonicalization_test` 31/0, metadata CTest 1/1, and MSVC Debug `zr_vm_metadata_token_model_test` 17/0. The layout helper is split out so `compiler_metadata_type_def.c` stays under the large-file threshold.

TypeSpec layout binding update (2026-06-18 16:11:43 +08:00): matching union `TYPE_SPEC` records now also bind their corresponding provider `TYPE_DEF` definition and layout identity. `SZrMetadataTokenBinding` carries expected/resolved layout version/hash fields, persisted in `.zro` patch 32. `ZrCore_Function_BindMatchingTypeSpecMetadataWithStatus()` rejects TypeDef contract drift and layout drift without writing partial TypeSpec/TypeDef bindings, and loader `type_spec_mismatch` diagnostics include definition/layout mismatch counts plus expected/actual signature/layout context. Verification covered WSL clang/gcc `zr_vm_metadata_token_model_test` 20/0, `zr_vm_project_import_canonicalization_test` 31/0, metadata CTest 1/1, and MSVC Debug `zr_vm_metadata_token_model_test` 20/0.

Module ABI TypeDef/TypeSpec identity hash update (2026-06-18 17:08:15 +08:00): `compiler_metadata_module_hash.c/.h` now owns module ABI fingerprint calculation. `moduleSignatureHash` uses `zr.md.mod.v2\0` and includes provider module version, sorted public typed export identity/signature blobs, and sorted `TYPE_DEF` / `TYPE_SPEC` entity record identity. This makes local TypeDef contract/layout or TypeSpec identity drift change the module ABI hash even when the exported function signature blob is unchanged. Verification covered WSL clang/gcc `zr_vm_metadata_token_model_test` 21/0, `zr_vm_project_import_canonicalization_test` 31/0, metadata CTest 1/1, and MSVC Debug `zr_vm_metadata_token_model_test` 21/0. No new `.zro` patch was required because patch 26 already persists `moduleSignatureHash`.

Module ABI golden update (2026-06-18 23:45:06 +08:00; refreshed 2026-06-21 12:32:00 +08:00): `tests/module/test_metadata_module_hash_golden.c` fixes module-level ABI fingerprints for both a simple public function module and a local generic-union module. The expected hashes are `0xE701BC33ECB6BF89` for `sum(i64,i64)->i64` and `0x485AE44EE06010E4` for `Option<T>` plus `choose(): Option<int>`. During this slice, `compiler_metadata_type_def_layout.c` also stopped feeding host `ZR_ALIGN_SIZE` and `sizeof(SZrTypeValue)` directly into record-level layout fingerprints for unknown/generic payload slots and scalar fallback alignment; the metadata fingerprint uses canonical 64-bit reference size and max-8 scalar alignment so GCC/Clang and MSVC produce the same module ABI hash. This does not change runtime union layout generation and does not require a `.zro` patch. Verification covered WSL GCC/clang `metadata_token_model` 21/0, `metadata_type_ref_binding` 8/0, `metadata_module_hash_golden` 2/0, direct `zr_vm_project_import_canonicalization_test` 31/0, and MSVC Debug metadata CTest 3/3.

Runtime metadata record query update (2026-06-19 00:12:05 +08:00): `function_metadata_query.c` now exposes read-only lookup helpers for function-level metadata records and entry-function module ref table records: `ZrCore_Function_FindMetadataTokenRecord()`, `ZrCore_Function_FindMetadataSignatureRecord()`, `ZrCore_Function_FindModuleMetadataTokenRecord()`, and `ZrCore_Function_FindModuleMetadataSignatureRecord()`. Signature lookup requires the entity/signature pair to mutually point through `relatedToken` plus `ownerToken`, and does not alter `.zro` schema or binding sidecars. Verification covered WSL GCC/clang `zr_vm_metadata_runtime_query_test` 3/0 and WSL GCC/clang plus MSVC Debug CTest `metadata_token_model|metadata_type_ref_binding|metadata_runtime_query|metadata_module_hash_golden` 4/4.

AOT TypeSpec runtime layout binding update (2026-06-25 21:18:46 +08:00): zrp `SZrZrpMetadataTypeSpecRow` now carries `typeLayoutId` in the former reserved slot, and the metadata runtime exposes `ZrCore_MetadataRuntime_ReadTypeSpecLayoutBindingView()` for read-only TypeSpec layout binding. The view resolves the TypeSpec token record, paired signature record, zrp TypeSpec row, and generic base-token binding, verifies row signature offset/length/hash against token identity, then resolves the row `typeLayoutId` through the code-registration layout registry. Missing registry layout data returns false even when stale `prototypeFrameTypeLayouts` exist. Verification covered WSL GCC/clang and MSVC Debug `zr_vm_metadata_runtime_typespec_layout_test` 2/0, `zr_vm_zrp_metadata_format_test` 11/0, `zr_vm_metadata_runtime_query_test` 22/0, and `zr_vm_metadata_runtime_type_layout_test` 10/0. This is runtime binding only; it does not synthesize generic layouts, persist new `.zro` schema, or complete public generic reflection.

AOT TypeDef/TypeSpec token layout cache update (2026-06-25 21:37:38 +08:00): metadata runtime now exposes `ZrCore_MetadataRuntime_ResolveTypeTokenLayout(runtime, typeToken, outTypeLayoutId)` for public `TYPE_DEF` / `TYPE_SPEC` token-to-layout lookup. The resolver delegates to the TypeDef and TypeSpec binding views, requires a code-registration registry-backed `SZrTypeLayout`, resets the output layout id on failure, and caches the latest successful token/layoutId/layout hit. It rejects non-type tokens and does not fall back to stale `prototypeFrameTypeLayouts` when registry layout data is missing. Verification covered WSL GCC/clang CTest `metadata_runtime_typespec_layout|metadata_runtime_query|metadata_runtime_type_layout|zrp_metadata_format` 4/4 and MSVC Debug focused binaries 5/0, 11/0, 22/0, and 10/0. This is a narrow token-level resolver/cache; it does not add a reverse cTypeId table, generic layout synthesis, new `.zro` schema, or public generic reflection entities.

AOT type layout token reverse-cache update (2026-06-25 21:53:56 +08:00): metadata runtime now exposes `ZrCore_MetadataRuntime_ResolveTypeLayoutToken(runtime, typeLayoutId)` for the reverse direction of the latest TypeDef/TypeSpec token layout cache. The resolver first reuses the latest token/layout cache entry, then scans zrp TypeDef rows followed by TypeSpec rows and revalidates each candidate through the existing registry-backed binding views. It returns 0 for null runtime, `ZR_FUNCTION_FRAME_TYPE_LAYOUT_ID_NONE`, missing zrp metadata, missing registry layout data, or stale prototype-cache-only layouts. Verification covered WSL GCC/clang CTest `metadata_runtime_typespec_layout|metadata_runtime_query|metadata_runtime_type_layout|zrp_metadata_format` 4/4 and MSVC Debug focused binaries 8/0, 11/0, 22/0, and 10/0. This remains a narrow latest-hit reverse lookup; it does not add a persistent cTypeId-to-token index, generic layout synthesis, new `.zro` schema, or public generic reflection entities.

AOT multi-entry type layout cache update (2026-06-25 22:13:54 +08:00): `SZrMetadataRuntime` now keeps an 8-entry bounded cache for TypeDef/TypeSpec token/layout bindings. `ZrCore_MetadataRuntime_ResolveTypeTokenLayout()` probes by token, `ZrCore_MetadataRuntime_ResolveTypeLayoutToken()` probes by layout id, and both write back successful registry-backed TypeDef/TypeSpec binding view results. The cache can retain TypeDef and TypeSpec entries concurrently, so clearing the registry after two successful lookups still leaves both cached token->layout and layoutId->token hits available. Verification covered WSL GCC/clang CTest `metadata_runtime_typespec_layout|metadata_runtime_query|metadata_runtime_type_layout|zrp_metadata_format` 4/4 and MSVC Debug focused binaries 10/0, 11/0, 22/0, and 10/0. This remains a bounded runtime cache; it does not add a persistent cTypeId-to-token index, generic layout synthesis, new `.zro` schema, or public generic reflection entities.

AOT cTypeId token resolver update (2026-06-25 22:22:13 +08:00): metadata runtime now exposes `ZrCore_MetadataRuntime_ResolveCTypeIdToken(runtime, cTypeId)`. Under the current registry invariant that `cTypeId` equals `typeLayoutId`, the resolver reuses the existing typeLayoutId-to-token reverse path, including bounded multi-entry cache hits and TypeDef/TypeSpec binding-view registry validation. It returns 0 for null runtime, `ZR_FUNCTION_FRAME_TYPE_LAYOUT_ID_NONE`, missing registry layout data, or stale prototype-cache-only layouts. Verification covered WSL GCC/clang CTest `metadata_runtime_typespec_layout|metadata_runtime_query|metadata_runtime_type_layout|zrp_metadata_format` 4/4 and MSVC Debug focused binaries 12/0, 11/0, 22/0, and 10/0. This remains a public runtime resolver facade; it does not add a persistent cTypeId-to-token index, cTypeId/typeLayoutId decoupling, generic layout synthesis, new `.zro` schema, or public generic reflection entities.

AOT method token binding view update (2026-06-30 01:38:26 +08:00): `metadata_runtime.h` now exposes `SZrMetadataRuntimeMethodBindingView` and `ZrCore_MetadataRuntime_ReadMethodBindingView()` for the code-registration method-token carrier. The implementation lives in `metadata_runtime_method_binding.c` and scans attached `methodTokens[]` for a unique local `MEMBER_DEF` token, then returns the aligned function index, `SZrAotMethodInfo`, entry thunk, and MethodInfo invoker. Missing tables, non-method tokens, duplicate tokens, stale MethodInfo function indexes, missing thunks, and missing invokers fail with a cleared output view. Verification covered WSL GCC/clang and MSVC Debug `zr_vm_metadata_runtime_method_binding_test` 2/0, plus adjacent `zr_vm_metadata_runtime_query_test` 24/0 and `zr_vm_reflection_token_resolve_test` 4/0; GCC and MSVC CTest `metadata_runtime_method_binding` also passed 1/1. This is runtime binding only; public reflection object materialization and `Method.Invoke` marshaling remain later work.

AOT method binding public reflection carrier update (2026-06-30 01:54:46 +08:00): `SZrReflectionResolvedToken` now carries optional method binding fields for public token resolution: `methodFunctionIndex`, `methodInfo`, `methodFunctionPointer`, and `methodInvoker`. `ZrCore_Reflection_ResolveToken()` fills them for ordinary MethodDef/MethodRef method tokens when the attached metadata runtime has a valid 11-S2D code-registration binding; if no AOT binding exists, the resolver still returns the method record/signature carrier and leaves the binding fields empty. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 5/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0; GCC and MSVC CTest `reflection_token_resolve|metadata_runtime_method_binding` passed 2/2. This does not implement method reflection object materialization, MethodSpec runtime binding, or `Method.Invoke` marshaling.

AOT MethodSpec underlying method binding update (2026-06-30 02:05:51 +08:00): MethodSpec public token resolution now reuses the underlying MethodDef method-token binding carrier. `ZrCore_Reflection_ResolveToken()` keeps the MethodSpec signature/hash/generic-argument identity from the MethodSpec record, then uses the MethodSpec view's `methodToken` to read the 11-S2D binding view and fill `methodFunctionIndex`, `methodInfo`, `methodFunctionPointer`, and `methodInvoker` when the underlying method has an AOT registration. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 5/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0. This still does not materialize a public generic method reflection object, MethodSpec-specific generated function slot, or `Method.Invoke` marshaling.

AOT token-driven Method.Invoke dispatcher update (2026-06-30 02:57:41 +08:00): `reflection.h` now exposes `ZrCore_Reflection_InvokeMethodToken(state, runtime, methodToken, self, args, outReturn)`. The dispatcher resolves the method token through the public reflection token carrier, requires `methodInfo`, `methodFunctionPointer`, and `methodInvoker` from the 11-S2D method binding view, then calls the registered invoker with the original state/target/method/self/args/outReturn pointers. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 6/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus GCC and MSVC CTest `reflection_token_resolve|metadata_runtime_method_binding` 2/2. This is only the token-to-invoker dispatch path; signature-aware parameter unboxing, return boxing, public `MethodInfo` objects, and AOT/interpreter equivalence remain later work.

AOT Method.Invoke signature arity guard update (2026-06-30 03:09:55 +08:00): `reflection.h` now also exposes `ZrCore_Reflection_InvokeMethodTokenWithArgCount(state, runtime, methodToken, self, args, argCount, outReturn)`. The counted dispatcher reuses the same token-to-MethodInfo/function pointer/invoker path, then checks `methodInfo->signature->parameterCount` and `hasVarArgs` before dispatching: non-varargs methods require exact arity, varargs allow `argCount >= parameterCount`, and nonzero `argCount` with null `args` is rejected without calling the invoker. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus GCC and MSVC CTest `reflection_token_resolve|metadata_runtime_method_binding` 2/2. This is still only an arity boundary; typed argument unboxing, return boxing, signature type validation, public `MethodInfo` objects, and AOT/interpreter equivalence remain later work.

AOT Method.Invoke signature shape guard update (2026-06-30 03:22:37 +08:00): the counted dispatcher now treats incomplete `SZrAotSignature` shape as non-invokable before calling the registered invoker. A signature with fixed parameters must provide `parameterTypes`, and a signature declaring a return value must provide `returnType`; both negative paths leave the invoker untouched until the missing shape carrier is supplied. The coverage lives in the new focused `tests/module/test_reflection_method_invoke.c` target so the larger reflection token resolver test does not keep absorbing Method.Invoke-specific cases. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_method_invoke_test` 1/0, `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus focused CTest `reflection_method_invoke|reflection_token_resolve|metadata_runtime_method_binding` 3/3 on all three toolchains. This is only a shape guard; typed argument compatibility, unboxing, return boxing, public `MethodInfo` object materialization, MethodSpec-specific code slots, and AOT/interpreter equivalence remain later work.

AOT Method.Invoke parameter base-type guard update (2026-06-30 03:37:13 +08:00): the counted dispatcher now checks fixed parameter base types before invoking registered AOT code. After token binding, arity, and signature shape pass, concrete non-null/non-unknown `parameterTypes[i].baseType` values must match the corresponding `SZrTypeValue.type`; out-of-range base types are rejected, while untyped/null/unknown slots and varargs extra arguments stay unforced for later marshaling work. Coverage lives in `tests/module/test_reflection_method_invoke.c`, with the older arity fixture updated to provide real runtime argument types. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_method_invoke_test` 2/0, `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus focused CTest `reflection_method_invoke|reflection_token_resolve|metadata_runtime_method_binding` 3/3 on all three toolchains. This remains a guard only; typed unboxing, numeric widening, nullable/ownership/staticCType compatibility, return boxing, and public `MethodInfo` objects remain later work.

AOT Method.Invoke return base-type guard update (2026-06-30 03:47:49 +08:00): the counted dispatcher now validates the concrete return base type after the registered invoker writes `outReturn`. When `returnType->baseType` is non-null/non-unknown and in range, `outReturn->type` must match; mismatches make the counted dispatcher return false, while no-return and untyped return slots remain unchanged. Coverage lives in `tests/module/test_reflection_method_invoke.c`, with the reflection-token arity fixture updated so its declared int64 return has a matching output slot. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_method_invoke_test` 3/0, `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus focused CTest `reflection_method_invoke|reflection_token_resolve|metadata_runtime_method_binding` 3/3 on all three toolchains. This is a post-dispatch guard only; return boxing, side-effect-free preflight, typed unboxing, numeric widening, nullable/ownership/staticCType compatibility, and public `MethodInfo` objects remain later work.

AOT Method.Invoke required return-slot reset update (2026-06-30 04:06:53 +08:00): the counted dispatcher now resets `outReturn` to null before invoking registered AOT code when the MethodInfo signature declares a required return value. This prevents a caller-provided stale `SZrTypeValue` from satisfying the return base-type post-guard when the invoker fails to write a fresh result. Coverage lives in `tests/module/test_reflection_method_invoke.c`, with `tests/module/test_reflection_token_resolve.c` changed so counted success fixtures get their declared return from the synthetic invoker instead of a prefilled output slot. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_method_invoke_test` 4/0, `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus focused CTest `reflection_method_invoke|reflection_token_resolve|metadata_runtime_method_binding|metadata_runtime_query` 4/4 on all three toolchains. This is still a guard only; return boxing, typed return register capture, parameter unboxing, numeric widening, nullable/ownership/staticCType compatibility, and public `MethodInfo` objects remain later work.

AOT Method.Invoke void return-slot canonicalization update (2026-06-30 04:17:27 +08:00): the counted dispatcher now canonicalizes no-return MethodInfo signatures by resetting `outReturn` to null after registered invoker dispatch. Even if an invoker writes an output value for a void/no-return method, the public output slot remains null. Coverage lives in `tests/module/test_reflection_method_invoke.c`, which now includes a no-return signature case where the synthetic invoker writes int64 and the dispatcher must still leave `outReturn` null. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_method_invoke_test` 5/0, `zr_vm_reflection_token_resolve_test` 7/0, `zr_vm_metadata_runtime_method_binding_test` 2/0, and `zr_vm_metadata_runtime_query_test` 24/0, plus focused CTest `reflection_method_invoke|reflection_token_resolve|metadata_runtime_method_binding|metadata_runtime_query` 4/4 on all three toolchains. This only defines the public output shape for void/no-return calls; return boxing, typed return register capture, parameter unboxing, numeric widening, nullable/ownership/staticCType compatibility, and public `MethodInfo` objects remain later work.

AOT generated Method.Invoke int64 no-arg return boxing update (2026-06-30 04:45:07 +08:00): generated AOT C now emits a narrow `zr_aot_try_invoke_i64_no_arg(...)` reflection bucket. The bucket verifies the MethodInfo signature is an int64 return with zero parameters, switches on `method->functionIndex`, calls the existing typed i64 helper `zr_aot_typed_i64_fn_<index>()`, and boxes the result through `ZrCore_Value_InitAsInt(...)`. Code-registration `functionPointers` still point at full execution thunks, whose return value is an execution success flag, so unsupported cases run the fallback thunk without writing `outReturn`. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This is only the first generated return-boxing bucket; argument unboxing, bool/u64/f64/object/inline returns, complete signature buckets, typed target ABI carriers, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke uint64 no-arg return boxing update (2026-06-30 05:06:13 +08:00): generated AOT C now emits the sibling `zr_aot_try_invoke_u64_no_arg(...)` reflection bucket. The bucket verifies the MethodInfo signature is a uint64 return with zero parameters, switches on `method->functionIndex`, calls the existing typed u64 helper `zr_aot_typed_u64_fn_<index>()`, and boxes the result through `ZrCore_Value_InitAsUInt(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `unsigned_answer(): uint` and invokes its generated reflection invoker, proving the boxed output is `ZR_VALUE_TYPE_UINT64` with value 13. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Argument unboxing, bool/f64/object/inline returns, complete signature buckets, typed target ABI carriers, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke bool no-arg return boxing update (2026-06-30 05:26:08 +08:00): generated AOT C now emits `zr_aot_try_invoke_bool_no_arg(...)` as the next reflection bucket. The bucket verifies the MethodInfo signature is a bool return with zero parameters, switches on `method->functionIndex`, calls the existing typed bool helper `zr_aot_typed_bool_fn_<index>()`, and boxes the result through `ZrCore_Value_InitAsBool(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `truth(): bool` and invokes its generated reflection invoker, proving the boxed output is `ZR_VALUE_TYPE_BOOL` with value true. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Argument unboxing, f64/object/inline returns, complete signature buckets, typed target ABI carriers, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke f64 no-arg return boxing update (2026-06-30 05:44:51 +08:00): generated AOT C now emits `zr_aot_try_invoke_f64_no_arg(...)` as the next reflection bucket. The bucket verifies the MethodInfo signature is a double return with zero parameters, switches on `method->functionIndex`, calls the existing typed f64 helper `zr_aot_typed_f64_fn_<index>()`, and boxes the result through `ZrCore_Value_InitAsFloat(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `ratio(): float` and invokes its generated reflection invoker, proving the boxed output is `ZR_VALUE_TYPE_DOUBLE` with value 2.5. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Argument unboxing, object/inline returns, complete signature buckets, typed target ABI carriers, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke int64 one-arg argument unbox update (2026-06-30 06:05:32 +08:00): generated AOT C now emits `zr_aot_try_invoke_i64_one_arg(...)` as the first reflection bucket that unboxes a runtime argument. The bucket verifies an int64 return, exactly one int64 parameter, `args[0].type == ZR_VALUE_TYPE_INT64`, and a non-null output slot; it then reads `args[0].value.nativeObject.nativeInt64`, calls the existing typed i64 one-arg helper `zr_aot_typed_i64_fn_<index>(zr_aot_arg0)`, and boxes the int64 result through `ZrCore_Value_InitAsInt(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `echo(value: int): int`, invokes its generated reflection invoker with argument 99, and verifies `ZR_VALUE_TYPE_INT64` with value 99. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Multi-argument buckets, other scalar parameters, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke uint64 one-arg argument unbox update (2026-06-30 06:25:21 +08:00): generated AOT C now emits `zr_aot_try_invoke_u64_one_arg(...)` as the unsigned sibling one-argument reflection bucket. The bucket verifies a uint64 return, exactly one uint64 parameter, `args[0].type == ZR_VALUE_TYPE_UINT64`, and a non-null output slot; it then reads `args[0].value.nativeObject.nativeUInt64`, calls the existing typed u64 one-arg helper `zr_aot_typed_u64_fn_<index>(zr_aot_arg0)`, and boxes the uint64 result through `ZrCore_Value_InitAsUInt(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `echo_unsigned(value: uint): uint`, invokes its generated reflection invoker with argument 101, and verifies `ZR_VALUE_TYPE_UINT64` with value 101. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Multi-argument buckets, other scalar parameters, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT reflection invoker emitter split update (2026-06-30 06:45:51 +08:00): generated reflection invoker emission now lives in `backend_aot_c_reflection_invokers.h/.c` instead of `backend_aot_c_method_metadata.c`. The split keeps the existing `backend_aot_write_c_reflection_invokers(...)` generated output behavior while moving current i64/u64/bool/f64 no-arg return-boxing buckets and i64/u64 one-arg unbox buckets into a focused module; `backend_aot_c_method_metadata.c` now remains scoped to MethodInfo/signature/method-token/GC-root metadata emission and is reduced from 948 to 552 lines, with the new invoker file at 397 lines. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This is a maintenance split only; it does not add a new signature bucket, generated ABI, public `MethodInfo` materialization, or cross-module token behavior.

AOT generated Method.Invoke bool one-arg argument unbox update (2026-06-30 07:05:20 +08:00): generated AOT C now emits `zr_aot_try_invoke_bool_one_arg(...)` in the split reflection invoker module. The bucket verifies a bool return, exactly one bool parameter, `args[0].type == ZR_VALUE_TYPE_BOOL`, and a non-null output slot; it then reads `args[0].value.nativeObject.nativeBool`, calls the existing typed bool one-arg helper `zr_aot_typed_bool_fn_<index>(zr_aot_arg0)`, and boxes the bool result through `ZrCore_Value_InitAsBool(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `echo_truth(value: bool): bool`, invokes its generated reflection invoker with false, and verifies `ZR_VALUE_TYPE_BOOL` with value false. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Multi-argument buckets, other scalar parameters, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke f64 one-arg argument unbox update (2026-06-30 07:21:33 +08:00): generated AOT C now emits `zr_aot_try_invoke_f64_one_arg(...)` in the split reflection invoker module. The bucket verifies a double return, exactly one double parameter, `args[0].type == ZR_VALUE_TYPE_DOUBLE`, and a non-null output slot; it then reads `args[0].value.nativeObject.nativeDouble`, calls the existing typed f64 one-arg helper `zr_aot_typed_f64_fn_<index>(zr_aot_arg0)`, and boxes the double result through `ZrCore_Value_InitAsFloat(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `echo_ratio(value: float): float`, invokes its generated reflection invoker with 1.75, and verifies `ZR_VALUE_TYPE_DOUBLE` with value 1.75. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Multi-argument buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke int64 two-arg argument unbox update (2026-06-30 07:45:38 +08:00): generated AOT C now emits `zr_aot_try_invoke_i64_two_arg(...)` in the split reflection invoker module. The bucket verifies an int64 return, exactly two int64 parameters, `args[0].type == ZR_VALUE_TYPE_INT64`, `args[1].type == ZR_VALUE_TYPE_INT64`, and a non-null output slot; it then reads both native int64 payloads and calls the existing typed i64 two-arg helper. State-free helpers use `zr_aot_typed_i64_fn_<index>(zr_aot_arg0, zr_aot_arg1)`, while divide/modulo helpers keep the `state` parameter to preserve generated divide/modulo-by-zero diagnostics. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_values(left: int, right: int): int`, invokes its generated reflection invoker with 20 and 22, and verifies `ZR_VALUE_TYPE_INT64` with value 42. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Other scalar two-arg buckets, three-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke uint64 two-arg argument unbox update (2026-06-30 08:03:24 +08:00): generated AOT C now emits `zr_aot_try_invoke_u64_two_arg(...)` in the split reflection invoker module. The bucket verifies a uint64 return, exactly two uint64 parameters, `args[0].type == ZR_VALUE_TYPE_UINT64`, `args[1].type == ZR_VALUE_TYPE_UINT64`, and a non-null output slot; it then reads both native uint64 payloads and calls the existing typed u64 two-arg helper. State-free helpers use `zr_aot_typed_u64_fn_<index>(zr_aot_arg0, zr_aot_arg1)`, while divide/modulo helpers keep the `state` parameter to preserve generated divide/modulo-by-zero diagnostics. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_unsigned(left: uint, right: uint): uint`, invokes its generated reflection invoker with 100 and 23, and verifies `ZR_VALUE_TYPE_UINT64` with value 123. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Bool/f64 two-arg buckets, three-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke bool two-arg argument unbox update (2026-06-30 08:18:43 +08:00): generated AOT C now emits `zr_aot_try_invoke_bool_two_arg(...)` in the split reflection invoker module. The bucket verifies a bool return, exactly two bool parameters, `args[0].type == ZR_VALUE_TYPE_BOOL`, `args[1].type == ZR_VALUE_TYPE_BOOL`, and a non-null output slot; it then reads both native bool payloads, calls the existing typed bool two-arg helper `zr_aot_typed_bool_fn_<index>(zr_aot_arg0, zr_aot_arg1)`, and boxes the bool result through `ZrCore_Value_InitAsBool(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `same_truth(left: bool, right: bool): bool`, invokes its generated reflection invoker with true and true, and verifies `ZR_VALUE_TYPE_BOOL` with value true. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Bool-return numeric comparison buckets, f64 two-arg buckets, three-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke f64 two-arg argument unbox update (2026-06-30 08:35:02 +08:00): generated AOT C now emits `zr_aot_try_invoke_f64_two_arg(...)` in the split reflection invoker module. The bucket verifies a double return, exactly two double parameters, `args[0].type == ZR_VALUE_TYPE_DOUBLE`, `args[1].type == ZR_VALUE_TYPE_DOUBLE`, and a non-null output slot; it then reads both native double payloads and calls the existing typed f64 two-arg helper. State-free helpers use `zr_aot_typed_f64_fn_<index>(zr_aot_arg0, zr_aot_arg1)`, while divide/modulo helpers keep the `state` parameter to preserve generated divide/modulo-by-zero diagnostics. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_ratio(left: float, right: float): float`, invokes its generated reflection invoker with 1.25 and 2.5, and verifies `ZR_VALUE_TYPE_DOUBLE` with value 3.75. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. Bool-return numeric comparison buckets, three-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke bool-return numeric comparison two-arg update (2026-06-30 09:05:37 +08:00): generated AOT C now emits `zr_aot_try_invoke_bool_i64_two_arg(...)`, `zr_aot_try_invoke_bool_u64_two_arg(...)`, and `zr_aot_try_invoke_bool_f64_two_arg(...)` from the new `backend_aot_c_reflection_bool_numeric_invokers.h/.c` module, keeping `backend_aot_c_reflection_invokers.c` as orchestration. These buckets verify bool return signatures with exactly two int64, uint64, or double parameters, validate both runtime `args` slots, unbox both native payloads, call the existing typed bool comparison helper `zr_aot_typed_bool_fn_<index>(zr_aot_arg0, zr_aot_arg1)`, and box through `ZrCore_Value_InitAsBool(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `less_values(left: int, right: int): bool`, `unsigned_after(left: uint, right: uint): bool`, and `ratio_equal(left: float, right: float): bool`, invokes them through generated reflection, and verifies boxed true results. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This closes only bool-return numeric comparison two-arg buckets; three-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke int64 three-arg argument unbox update (2026-06-30 09:32:55 +08:00): generated AOT C now emits `zr_aot_try_invoke_i64_three_arg(...)` from the new `backend_aot_c_reflection_numeric_three_arg_invokers.h/.c` module, keeping `backend_aot_c_reflection_invokers.c` as orchestration. The bucket verifies an int64 return, exactly three int64 parameters, all three runtime `args` slots, and a non-null output slot; it then unboxes `nativeInt64` payloads, calls either the state-free `zr_aot_typed_i64_fn_<index>(zr_aot_arg0, zr_aot_arg1, zr_aot_arg2)` or stateful divide/modulo-preserving helper, and boxes through `ZrCore_Value_InitAsInt(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_three(left: int, middle: int, right: int): int`, invokes it through generated reflection with 10, 20, and 12, and verifies `ZR_VALUE_TYPE_INT64` with value 42. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This closes only the int64 three-arg bucket; u64/f64/bool three-arg buckets, four-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke uint64 three-arg argument unbox update (2026-06-30 09:52:48 +08:00): generated AOT C now emits `zr_aot_try_invoke_u64_three_arg(...)` from `backend_aot_c_reflection_numeric_three_arg_invokers.h/.c`, keeping `backend_aot_c_reflection_invokers.c` as orchestration. The bucket verifies a uint64 return, exactly three uint64 parameters, all three runtime `args` slots, and a non-null output slot; it then unboxes `nativeUInt64` payloads, calls either the state-free `zr_aot_typed_u64_fn_<index>(zr_aot_arg0, zr_aot_arg1, zr_aot_arg2)` or stateful divide/modulo-preserving helper, and boxes through `ZrCore_Value_InitAsUInt(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_three_unsigned(left: uint, middle: uint, right: uint): uint`, invokes it through generated reflection with 50, 20, and 5, and verifies `ZR_VALUE_TYPE_UINT64` with value 75. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This closes only the uint64 three-arg bucket; f64/bool three-arg buckets, four-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke f64 three-arg argument unbox update (2026-06-30 10:13:18 +08:00): generated AOT C now emits `zr_aot_try_invoke_f64_three_arg(...)` from `backend_aot_c_reflection_numeric_three_arg_invokers.h/.c`, keeping `backend_aot_c_reflection_invokers.c` as orchestration. The bucket verifies a double return, exactly three double parameters, all three runtime `args` slots, and a non-null output slot; it then unboxes `nativeDouble` payloads, calls either the state-free `zr_aot_typed_f64_fn_<index>(zr_aot_arg0, zr_aot_arg1, zr_aot_arg2)` or stateful divide/modulo-preserving helper, and boxes through `ZrCore_Value_InitAsFloat(...)`. `tests/parser/test_aot_c_shared_library_smoke.c` now adds `sum_three_ratio(left: float, middle: float, right: float): float`, invokes it through generated reflection with 1.5, 2.25, and 3.25, and verifies `ZR_VALUE_TYPE_DOUBLE` with value 7.0. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This closes only the f64 three-arg bucket; bool three-arg buckets, four-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT generated Method.Invoke bool three-arg argument unbox update (2026-06-30 10:49:57 +08:00): generated AOT C now emits `zr_aot_try_invoke_bool_three_arg(...)` from `backend_aot_c_reflection_bool_three_arg_invokers.h/.c`, keeping `backend_aot_c_reflection_invokers.c` as orchestration. The bucket verifies a bool return, exactly three bool parameters, all three runtime `args` slots, and a non-null output slot; it then unboxes `nativeBool` payloads, calls `zr_aot_typed_bool_fn_<index>(zr_aot_arg0, zr_aot_arg1, zr_aot_arg2)`, and boxes through `ZrCore_Value_InitAsBool(...)`. `backend_aot_c_typed_bool_three_arg_thunks.c` now recognizes the cleanup-reset short-circuit AND shape emitted for `left && middle && right`, and `tests/parser/test_aot_c_shared_library_smoke.c` invokes `all_truth(left: bool, middle: bool, right: bool): bool` through generated reflection with all true and verifies `ZR_VALUE_TYPE_BOOL` with value true. Verification covered WSL GCC/clang/MSVC Debug frame/source/shared-library and reflection/metadata focused binaries, plus CTest `metadata_runtime_query|metadata_runtime_method_binding|reflection_token_resolve|reflection_method_invoke|aot_runtime_typed_direct_call_compatibility|aot_c_metadata_binding_loader|aot_c_method_info_signature` 7/7 on all three toolchains. This closes only the bool three-arg bucket; four-arg and wider buckets, object/inline returns, numeric widening, instance receiver handling, and public `MethodInfo` materialization remain later work.

AOT reflection annotation root update (2026-06-30 11:25:44 +08:00): opt-in AOT C code stripping now treats function decorator metadata `reflectable: true` as a reflection annotation root. `backend_aot_collect_reflection_annotation_roots(...)` scans the pre-trim function table, reachability records the root with `ZR_AOT_REACHABILITY_REASON_REFLECTION_ANNOTATION`, and the generated C header emits `code_stripping.annotationRoots` plus `code_stripping.annotationRoot[index]` markers. The focused test proves an otherwise unreachable function with that metadata remains generated, while the same function without metadata is removed. Verification covered WSL GCC/clang and Windows MSVC Debug CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, WSL GCC/clang source contracts 22/0, and `git diff --check`. This is the first `@reflectable` carrier via existing compile-time decorator metadata; `@dynamically_accessed`, `@dynamic_dependency`, `@requires_unreferenced_code`, type/member DESCRIPTION promotion, cross-module annotation roots, and unannotated-reflection warnings remain later work.

AOT requires-unreferenced-code warning update (2026-06-30 11:59:59 +08:00): opt-in AOT C code stripping now emits a separate annotation warning stream when a retained caller statically calls a callee whose function decorator metadata contains `requiresUnreferencedCode: true`. The new `backend_aot_c_annotation_warnings.{h,c}` scanner writes `trim_warnings.annotationCount` and `trim_warning.annotation[index] function=<flatIndex> instruction=<index> targetFunction=<flatIndex> reason=requires-unreferenced-code`, while keeping runtime fallback warning counts and reason masks unchanged. `backend_aot_callable_provenance.c` now resolves callable slots produced by `GET_SUB_FUNCTION`, so direct child-function calls can be diagnosed through the shared provenance path. Verification covered WSL GCC focused annotation preserve 4/0, WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, WSL GCC/clang call shared-library smoke 5/0 and dynamic deopt bridge smoke 7/0, Windows MSVC Debug same CTest 3/3 with Unix-only smoke tests ignored, and WSL GCC/clang source contracts 22/0. This is only a retained static caller warning carrier; dynamic dataflow annotations, dynamic dependency roots, warning suppression/promotion, user reason text, cross-module annotations, and complete trim analysis remain later work.

AOT requires-unreferenced-code reason text update (2026-06-30 12:22:15 +08:00): the annotation warning scanner now reads an optional `requiresUnreferencedCodeReason` string from the same function decorator metadata object. When the static-call warning is emitted and the reason is present and non-empty, the generated marker appends a quoted/escaped `message="..."` field, for example `message="uses \"name\" lookup"`. Missing, empty, or non-string reason values keep the earlier marker shape without `message=`, so existing consumers that only read `reason=requires-unreferenced-code` remain compatible. Verification covered WSL GCC focused annotation preserve 5/0, WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, WSL GCC/clang call shared-library smoke 5/0 and dynamic deopt bridge smoke 7/0, WSL GCC/clang source contracts 22/0, and Windows MSVC Debug same CTest 3/3 plus source contracts 22/0 with Unix-only smoke tests ignored. This closes only reason-text propagation for retained static-call annotation warnings; dataflow annotations, dynamic dependencies, suppression/promotion policy, cross-module annotation roots, unannotated reflection warnings, and full trim analysis remain later work.

AOT dynamic dependency function root update (2026-06-30 12:38:52 +08:00): opt-in AOT C code stripping now treats function decorator metadata `dynamicDependencyFunctionIndex` as the first `@dynamic_dependency` carrier for direct function roots. `backend_aot_collect_reflection_annotation_roots(...)` shares metadata field lookup with `reflectable`, validates that the target flat function index exists in the pre-trim function table, de-duplicates roots, and feeds the same `ZR_AOT_REACHABILITY_REASON_REFLECTION_ANNOTATION` root path used by `reflectable: true`. Generated C continues to expose the result through `code_stripping.annotationRoots` and `code_stripping.annotationRoot[index]`; the focused generated-C test proves an otherwise unreachable child with target index 2 remains emitted as `zr_aot_fn_2`, while the unannotated prune path still removes it. Verification covered WSL GCC focused annotation preserve 6/0 and reachability 8/0, WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, WSL GCC/clang call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, and source contracts 22/0, plus Windows MSVC Debug same CTest 3/3 and source contracts 22/0 with Unix-only smoke tests ignored. This is only a single flat-function-index dependency carrier; token/name member dependencies, cross-module dependency binding, `@dynamically_accessed` flow analysis, warning policy, unannotated reflection warnings, and complete metadata sweep remain later work.

AOT dynamic dependency method token root update (2026-06-30 13:22:30 +08:00): opt-in AOT C code stripping now treats function decorator metadata `dynamicDependencyMethodToken` as a token-backed `@dynamic_dependency` carrier for current-module exported methods. The annotation root collector reads a uint metadata value, rejects zero and non-`MEMBER_DEF` tokens, then resolves the token through the root function's `typedExportedSymbols` entries whose symbol kind and export kind identify function exports. A matching exported symbol maps its `callableChildIndex` back to the pre-trim function table, and the target joins the same de-duplicated `ZR_AOT_REACHABILITY_REASON_REFLECTION_ANNOTATION` root path used by `reflectable` and `dynamicDependencyFunctionIndex`. Generated C continues to expose `code_stripping.annotationRoots` and `code_stripping.annotationRoot[index]`; the focused generated-C fixture proves `dynamicDependencyMethodToken = 0x03000007` preserves the exported child as `zr_aot_fn_2`. Verification covered WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, reachability 9/0, annotation preserve 8/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, source contracts 23/0, plus Windows MSVC Debug same CTest 3/3, reachability 9/0, annotation preserve 8/0, and source contracts 23/0 with Unix-only smoke tests ignored. This is only a current-module exported MethodDef token carrier; name binding, non-exported members, field/type dependencies, cross-module annotations, dataflow analysis, warning policy, and complete metadata sweep remain later work.

AOT dynamic dependency method name root update (2026-06-30 14:01:25 +08:00): opt-in AOT C code stripping now treats function decorator metadata `dynamicDependencyMethodName` as a current-module exported method-name carrier. The annotation root collector reads a string metadata value, resolves it only against the root function's `typedExportedSymbols` function exports by exported symbol name, maps the matching `callableChildIndex` back to the pre-trim function table, and feeds the same de-duplicated `ZR_AOT_REACHABILITY_REASON_REFLECTION_ANNOTATION` root path used by reflectable and token/function dynamic dependencies. Generated C continues to expose `code_stripping.annotationRoots` and `code_stripping.annotationRoot[index]`; the focused generated-C fixture proves `dynamicDependencyMethodName = "target"` preserves the exported child as `zr_aot_fn_2`. Verification covered WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, reachability 10/0, annotation preserve 9/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, source contracts 23/0, plus Windows MSVC Debug same CTest 3/3, reachability 10/0, annotation preserve 9/0, and source contracts 23/0 with Unix-only smoke tests ignored. This is only a current-module exported method-name binding; overload/signature disambiguation, non-exported members, field/type dependencies, cross-module annotations, dataflow analysis, warning policy, and complete metadata sweep remain later work.

AOT dynamic dependency method name signature-hash update (2026-06-30 14:38:53 +08:00): opt-in AOT C code stripping now treats function decorator metadata `dynamicDependencyMethodSignatureHash` as an optional uint64 companion to `dynamicDependencyMethodName`. The annotation root collector resolves name-based dynamic dependencies through the root function's `typedExportedSymbols` by exported function name plus `SZrFunctionTypedExportSymbol.signatureHash` when the hash metadata field is present, including explicit zero hashes. Duplicate exported names without a hash are now rejected as ambiguous instead of silently binding the first matching symbol. Generated C continues to expose the retained target through `code_stripping.annotationRoot[index]`; the focused generated-C fixture proves `dynamicDependencyMethodName = "target"` plus `dynamicDependencyMethodSignatureHash = 0x2222` preserves the second overloaded exported child as `zr_aot_fn_2`, while direct reachability fixtures lock the no-hash ambiguous-name rejection and explicit zero-hash match. Verification covered WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, reachability 13/0, annotation preserve 10/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, source contracts 23/0, plus Windows MSVC Debug same CTest 3/3, reachability 13/0, annotation preserve 10/0, and source contracts 23/0. This is still only a current-module exported method-name binding; cross-module annotations, non-exported members, field/type dependencies, `@dynamically_accessed` dataflow, warning policy, and complete metadata sweep remain later work.

AOT dynamic dependency non-exported method token root update (2026-06-30 15:31:50 +08:00): opt-in AOT C code stripping now allows function decorator metadata `dynamicDependencyMethodToken` to preserve a current-module method whose typed function symbol is not a module export. The annotation root collector still reads a uint token, rejects zero and non-`MEMBER_DEF` tokens, and resolves only through the root function's `typedExportedSymbols`, but the token path now matches `symbolKind == ZR_FUNCTION_TYPED_SYMBOL_FUNCTION` plus exact `metadataToken` without requiring `exportKind == ZR_MODULE_EXPORT_KIND_FUNCTION`. A valid `callableChildIndex` maps back to the pre-trim function table and joins the same de-duplicated `ZR_AOT_REACHABILITY_REASON_REFLECTION_ANNOTATION` root path. Generated C continues to expose `code_stripping.annotationRoot[index]`; the focused generated-C fixture proves `dynamicDependencyMethodToken = 0x03000008` on a non-exported typed function symbol preserves the target as `zr_aot_fn_2`. Verification covered WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, reachability 14/0, annotation preserve 11/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, source contracts 23/0, plus Windows MSVC Debug same CTest 3/3, reachability 14/0, annotation preserve 11/0, and source contracts 23/0. This is only a current-module MethodDef token-to-function-symbol binding; cross-module annotations, field/type dependencies, non-method member tokens, `@dynamically_accessed` dataflow, warning policy, and complete metadata sweep remain later work.

AOT dynamic dependency field token layout root update (2026-06-30 17:31:53 +08:00): opt-in AOT C code stripping now allows function decorator metadata `dynamicDependencyFieldToken` to preserve current-module generated type layouts for a FieldDef's owner type and field value type through embedded zrp metadata. The token-to-layout-root scanner was split into `backend_aot_c_type_layout_metadata_roots.{h,c}`; it accepts current-module `MEMBER_DEF` FieldDef rows, verifies the owner TypeDef row and field range, and returns the owner TypeDef `typeLayoutId` plus the FieldDef `typeLayoutId` to the same type-layout root collector used by `dynamicDependencyTypeLayoutId` and `dynamicDependencyTypeToken`. Generated C now preserves both owner and field `SZrTypeLayout` descriptors, registration entries, type-layout statistics, and `code_stripping.annotationTypeLayoutRoot[]` markers when the only original frame reference lives in a trimmed function. Verification covered WSL GCC/clang focused code stripping 9/0, source contracts 24/0, type-layout contracts 1/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, WSL GCC/clang focused CTest 2/2, MSVC Debug focused code stripping 9/0, source contracts 24/0, type-layout contracts 1/0, MSVC focused CTest 2/2, and Windows smoke binaries returning OK with ignored Unix-only assertions and zero failures. This is only current-module FieldDef token-to-owner/field-layout retention; field value read/write, complete FieldInfo behavior, TypeRef/cross-module tokens, `@dynamically_accessed` flow analysis, warning policy, DESCRIPTION promotion, and complete metadata sweep remain later work.

AOT FieldDef token FieldInfo object update (2026-06-30 18:38:42 +08:00): runtime reflection can now materialize a minimum public FieldInfo object from an attached FieldDef token through `ZrCore_Reflection_BuildFieldInfoTokenObject(state, runtime, fieldToken)`. The implementation reuses the existing FieldDef token resolver carrier, reads FieldDef/owner/field-type names from the zrp string pool when rows provide offsets, keeps token/layout/offset/size facts sourced from the 11-S4 binding views, and fills a public reflection object with name, qualifiedName, kind, typeName, metadata/owner/type tokens, offset, size, layout ids, a layout sub-object, and a nested type literal. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. This is minimum FieldInfo materialization only; field value read/write, owner/module object links, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo declaring type object update (2026-06-30 18:58:39 +08:00): the minimum FieldDef token FieldInfo object now also exposes its declaring type identity. `ZrCore_Reflection_BuildFieldInfoTokenObject(state, runtime, fieldToken)` writes `ownerTypeName` and `declaringTypeName` from the owner TypeDef row's zrp string-pool name and creates a nested `declaringType` type literal reflection object. The focused test also locks the existing field `type` nested literal shape. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new assertions failed before the builder filled those fields and passed 8/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. This remains a minimum carrier; field value read/write, owner/module full reflection object links, cache policy, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo owner link update (2026-06-30 19:11:22 +08:00): the minimum FieldDef token FieldInfo object now links its public `owner` field to the same nested declaring type literal object. This keeps the owner identity visible without building module reflection objects or a separate owner cache. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new `owner` assertion failed while `owner` was still null and passed 8/0 after `ZrCore_Reflection_BuildFieldInfoTokenObject()` reused `declaringType` for `owner`. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. This remains an identity carrier only; field value read/write, module links, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo module name update (2026-06-30 19:26:41 +08:00): the minimum FieldDef token FieldInfo object now exposes `moduleName` from the attached metadata runtime module. `ZrCore_Reflection_BuildFieldInfoTokenObject()` reads `runtime->module->moduleName`, falling back to `fullPath`, and writes a stable string field without building a module reflection object or changing metadata/code-registration ABI. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new `moduleName` assertion failed while the field was missing and passed 8/0 after the builder populated it. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full module reflection links, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo FieldDef flags update (2026-06-30 19:39:14 +08:00): the minimum FieldDef token FieldInfo object now exposes raw FieldDef row flags as `metadataFlags`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` copies `resolved.fieldDefRow->flags` into the public reflection object without interpreting flag bits, without changing `isStatic` or `isConst`, and without changing metadata/code-registration ABI. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new `metadataFlags` assertion failed while the field was missing and passed 8/0 after the builder populated it from a fixture value `0xA5`. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Flag-bit semantics, static/const mapping, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo FieldDef signature blob coordinate update (2026-06-30 19:54:58 +08:00): the minimum FieldDef token FieldInfo object now exposes raw FieldDef row signature blob coordinates as `signatureBlobOffset` and `signatureBlobLength`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` copies `resolved.fieldDefRow->signatureBlobOffset` and `signatureBlobLength` into the public reflection object without validating the referenced blob slice, without parsing field signatures, and without changing metadata/code-registration ABI. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new coordinate assertions failed while the fields were missing and passed 8/0 after the builder populated them from fixture values `4/7`. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Field signature blob validation, signature semantic parsing, signature-derived field type binding, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo field signature header update (2026-06-30 20:13:27 +08:00): the minimum FieldDef token FieldInfo object now exposes the validated field signature header through `signatureRootNode`, `signatureFlags`, and `fieldTypeBlobOffset`. The FieldInfo builder reuses `ZrCore_MetadataRuntime_ReadSignatureView()` and only populates these fields when the FieldDef token's paired signature record resolves to a valid `FIELD_SIG` blob; missing, invalid, or non-field signatures keep the validated header fields at `0`. The raw FieldDef row signature blob coordinates remain separate. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Signature-derived field type binding, recursive type-node reflection objects, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo field signature type-node summary update (2026-06-30 20:32:08 +08:00): the minimum FieldDef token FieldInfo object now consumes the validated `FIELD_SIG` field type-node through `ZrCore_MetadataRuntime_ReadSignatureTypeNode()` and exposes its raw summary as `fieldTypeSignatureNode`, `fieldTypeSignatureBlobOffset`, `fieldTypeSignatureNextBlobOffset`, `fieldTypeSignaturePayload0`, `fieldTypeSignaturePayload1`, `fieldTypeSignatureBaseTypeBlobOffset`, `fieldTypeSignatureChildCount`, and `fieldTypeSignatureChildListBlobOffset`. The fixture covers `PRIMITIVE(BOOL)` at blob offset `2` with next offset `7`; missing or invalid views keep the summary fields at `0`. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. This is still a structure carrier only: signature-derived semantic field type binding, recursive type-node reflection objects, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive field signature type update (2026-06-30 20:46:10 +08:00): the minimum FieldDef token FieldInfo object now exposes primitive signature type semantics without replacing the layout-derived field type. When the validated `FIELD_SIG` field type-node is `PRIMITIVE`, `ZrCore_Reflection_BuildFieldInfoTokenObject()` writes `fieldTypeSignatureValueType` from payload0 and `fieldTypeSignatureTypeName` from the reflection built-in type-name table. The focused fixture deliberately keeps `typeName == "int"` from the FieldDef layout/token path while the signature carrier reports `ZR_VALUE_TYPE_BOOL` / `"bool"`, proving the new carrier is separate from the existing layout-derived field type object. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full signature-derived field type binding, TypeDef/TypeRef signature token binding, recursive type-node reflection objects, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive field signature type object update (2026-06-30 21:02:11 +08:00): the minimum FieldDef token FieldInfo object now exposes the primitive signature type as an independent type literal object. When the validated `FIELD_SIG` field type-node is `PRIMITIVE`, `ZrCore_Reflection_BuildFieldInfoTokenObject()` reuses `fieldTypeSignatureTypeName` to build `fieldTypeSignatureType`; the focused fixture asserts `kind == "type"`, `name == "bool"`, and `qualifiedName == "bool"` while the layout-derived `type` object remains `int`. Missing, invalid, or non-primitive signature nodes keep `fieldTypeSignatureType` null. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full signature-derived field type binding, TypeDef/TypeRef signature token binding, recursive type-node reflection objects, field value read/write, complete FieldInfo methods, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo module reflection object link update (2026-06-30 21:20:26 +08:00): the minimum FieldDef token FieldInfo object now links `module` to the attached runtime module reflection object. The focused fixture now creates a real module through `ZrCore_Module_Create()`, sets its module name to `geometry`, and asserts the resulting `FieldInfo.module` has `kind == "module"`, `name == "geometry"`, and `qualifiedName == "geometry"`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` only builds this link when `runtime->module` is a real `ZR_OBJECT_INTERNAL_TYPE_MODULE`, then reuses the existing module reflection builder/cache; otherwise the field stays null. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 8/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Field value read/write, complete FieldInfo methods, complete signature-derived field type binding, TypeDef/TypeRef signature binding, recursive type-node reflection objects, cross-module FieldRef/TypeRef, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo direct TypeDef signature carrier update (2026-06-30 21:45:03 +08:00): the minimum FieldDef token FieldInfo object now exposes direct local `TYPE_DEF` signature identity through `fieldTypeSignatureTypeToken`, `fieldTypeSignatureTypeLayoutId`, and `fieldTypeSignatureTypeSize`. The focused fixture adds `FIELD_SIG(TYPE_DEF(object, 17))` and a matching local TypeDef signature blob, then asserts the carrier resolves to `TEST_FIELD_TYPE_DEF_TOKEN`, layout id `42`, and size `16`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` matches the validated field type-node against attached metadata token records with existing signature blob readers, then reuses `ZrCore_MetadataRuntime_ResolveTypeTokenLayout()` for registry-backed layout identity. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 9/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. TypeRef/cross-module signature binding, recursive type-node reflection objects, field type consistency checks, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo direct TypeDef signature type object update (2026-07-01 00:15:36 +08:00): the minimum FieldDef token FieldInfo object now materializes direct local `TYPE_DEF` signature identity as `fieldTypeSignatureTypeName` plus the `fieldTypeSignatureType` reflection object. The focused fixture keeps `FIELD_SIG(TYPE_DEF(object, 17))`, then asserts `fieldTypeSignatureTypeName == "int"` and `fieldTypeSignatureType.kind/name/qualifiedName == type/int/int` while the existing token/layout/size carrier remains intact. `ZrCore_Reflection_BuildFieldInfoTokenObject()` reuses `ZrCore_MetadataRuntime_ReadTypeDefLayoutBindingView()` after the signature token match, reads the TypeDef row name from the zrp string pool, and feeds that name into the existing type literal object builder. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 9/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. TypeRef/cross-module signature binding, recursive wrapper/generic type-node objects, field type consistency checks, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo bound TypeRef signature carrier update (2026-07-01 00:37:26 +08:00): the minimum FieldDef token FieldInfo object now consumes a current-runtime attached `TYPE_REF` field signature node. The focused fixture adds `FIELD_SIG(TYPE_REF(object, 23))`, a matching module TypeRef token/signature pair, and a target local TypeDef layout binding; it asserts `fieldTypeSignatureTypeToken == TEST_TYPE_REF_TOKEN`, layout id `42`, size `16`, `fieldTypeSignatureTypeName == "int"`, and `fieldTypeSignatureType.kind/name/qualifiedName == type/int/int`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` keeps the public signature identity as the TypeRef token, reuses the existing TypeRef -> target TypeDef layout resolver, then reads the target TypeDef row name and feeds it into the same type literal object builder. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 10/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Cross-module TypeRef/provider loading, recursive wrapper/generic type-node objects, field type consistency checks, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo signature/layout consistency update (2026-07-01 00:55:33 +08:00): the minimum FieldDef token FieldInfo object now exposes `fieldTypeSignatureMatchesLayout`. The builder sets it only when the signature-derived layout and the FieldDef layout are both present, the signature layout id is non-none, and both layout id and registry layout pointer match. The focused fixture asserts the existing primitive `bool` signature over an `int` FieldDef layout reports false, while direct `TYPE_DEF` and bound `TYPE_REF` signature carriers resolving to layout id `42` report true. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new bool field assertions failed 3/10 while the field was missing and passed 10/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 10/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Recursive signature-derived field type binding, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo signature type-node object update (2026-07-01 01:17:21 +08:00): the minimum FieldDef token FieldInfo object now exposes `fieldTypeSignatureNodeObject`. This nested read-only object has `kind == "signatureTypeNode"`, mirrors the validated field type-node's node, blob offsets, payloads, base/child summary, and also carries the resolved signature-derived token, layout id, size, type name, and layout-match boolean. The focused fixture asserts the object shape for primitive `bool`, direct local `TYPE_DEF`, and current-runtime bound `TYPE_REF` signatures, keeping it consistent with the existing flat fields. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: object field assertions failed 3/10 while the field was missing and passed 10/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 10/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Recursive child type-node objects, generic/wrapper expansion, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo signature base type-node object update (2026-07-01 01:41:50 +08:00): the minimum FieldDef token FieldInfo object now exposes `fieldTypeSignatureNodeObject.baseTypeNodeObject` for signature nodes with a `baseTypeBlobOffset`, starting with `FIELD_SIG(GENERIC_INST(TYPE_DEF(object, 17), int64))`. The builder now passes the validated field signature blob into the signature-node object builder, which reads the base type-node from the same blob and creates a nested structural `signatureTypeNode` object with node/blob/payload summary fields. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new generic base object assertion failed 1/11 while `baseTypeNodeObject` was missing and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Generic argument child lists, semantic token/layout binding for base or arguments, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo signature child type-node object update (2026-07-01 02:00:10 +08:00): the minimum FieldDef token FieldInfo object now exposes `fieldTypeSignatureNodeObject.childNodeObjects` for signature nodes with a structural child list. The focused generic field fixture starts with `FIELD_SIG(GENERIC_INST(TYPE_DEF(object, 17), int64))` and asserts that the top-level generic node carries one child `signatureTypeNode` object for `PRIMITIVE(INT64)` at blob offset 16 while preserving the existing base `TYPE_DEF` object. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new child-list assertion failed 1/11 while `childNodeObjects` was missing and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. The object list is structural only; child semantic token/layout/name binding, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive child type-node semantic name update (2026-07-01 02:19:00 +08:00): the same generic FieldInfo signature-node fixture now verifies that a primitive generic argument child carries a semantic `typeName`. `reflection_build_signature_type_node_object_internal()` uses the existing builtin type-name mapping when a recursive signature node object has `node == PRIMITIVE` and no caller-provided name; `PRIMITIVE(INT64)` therefore appears as `childNodeObjects[0].typeName == "int"` while token/layout fields stay empty. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new primitive child `typeName` assertion failed 1/11 while the field was null and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Direct TypeDef/TypeRef child token/layout binding, recursive semantic field type materialization, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo direct TypeDef child type-node semantic update (2026-07-01 02:47:33 +08:00): the generic FieldInfo signature-node fixture now uses `FIELD_SIG(GENERIC_INST(TYPE_DEF(object, 17), int64, TYPE_DEF(object, 17)))` and verifies semantic binding on both the nested base TypeDef node and the second generic argument child node. `reflection_build_signature_type_node_object_internal()` now keeps runtime context for recursive node objects, matches direct `TYPE_DEF` nodes against existing signature token records, resolves the matched TypeDef token through the registry-backed layout resolver, and reads the TypeDef row name from the zrp string pool. Both `baseTypeNodeObject` and `childNodeObjects[1]` now carry `typeToken == TEST_FIELD_TYPE_DEF_TOKEN`, `typeLayoutId == 42`, `typeSize == 16`, and `typeName == "int"`. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new direct TypeDef nested token assertion failed 1/11 while `typeToken` was zero and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Direct TypeRef child token/layout binding, full recursive semantic field type materialization, cross-module provider loading, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo direct TypeRef child type-node semantic update (2026-07-01 03:04:36 +08:00): the generic FieldInfo signature-node fixture now uses `FIELD_SIG(GENERIC_INST(TYPE_DEF(object, 17), int64, TYPE_DEF(object, 17), TYPE_REF(object, 23)))` and verifies semantic binding on the TypeRef generic argument child node. `reflection_build_signature_type_node_object_internal()` now handles recursive `TYPE_REF` nodes through the same runtime-aware signature record path, resolves the TypeRef token through the existing target-TypeDef layout resolver, and reads the target TypeDef row name from the zrp string pool. `childNodeObjects[2]` now carries `typeToken == TEST_TYPE_REF_TOKEN`, `typeLayoutId == 42`, `typeSize == 16`, and `typeName == "int"`. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new direct TypeRef child token assertion failed 1/11 while `typeToken` was zero and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Cross-module provider loading/version compatibility, full recursive semantic field type materialization, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo recursive signature type-node type literal update (2026-07-01 03:17:49 +08:00): recursive FieldInfo signature-node objects with a semantic `typeName` now expose the public `type` type literal object. The generic fixture validates `baseTypeNodeObject.type`, primitive `childNodeObjects[0].type`, direct TypeDef `childNodeObjects[1].type`, and direct TypeRef `childNodeObjects[2].type`, all with `kind == "type"` and `name/qualifiedName == "int"`. RED/GREEN covered WSL GCC `zr_vm_reflection_token_resolve_test`: the new nested `type` object assertion failed 1/11 with `Expected Non-NULL` while the field was missing and passed 11/0 after implementation. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 11/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full recursive/signature-derived field type binding, cross-module provider loading/version compatibility, field value read/write, complete FieldInfo methods, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo value-slot read update (2026-07-01 03:49:06 +08:00): runtime reflection can now read a FieldDef token's inline `VALUE_SLOT` field value through `ZrCore_Reflection_ReadFieldInfoTokenValue(state, runtime, fieldToken, inlineStorage, inlineStorageByteSize, outValue)`. The API reuses `ZrCore_Reflection_ResolveToken()` and the existing FieldDef layout binding, matches the owner `SZrTypeLayoutField` by byte offset and type-layout id, requires `ZR_TYPE_LAYOUT_FIELD_FLAG_VALUE_SLOT`, checks the caller-provided inline storage range, and copies the stored `SZrTypeValue` with `ZrCore_Value_Copy()`. The focused fixture stores int `314159` at offset 32 and verifies null state, null runtime, non-FieldDef token, short storage, and the successful read path. RED/GREEN covered WSL GCC: the new test first failed to link because `ZrCore_Reflection_ReadFieldInfoTokenValue` did not exist, then passed 12/0 after the API and implementation were added. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 12/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Field write, raw POD/nested inline field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo value-slot write update (2026-07-01 04:12:42 +08:00): runtime reflection can now write a FieldDef token's inline `VALUE_SLOT` field value through `ZrCore_Reflection_WriteFieldInfoTokenValue(state, runtime, fieldToken, inlineStorage, inlineStorageByteSize, value)`. Read and write share the same internal owner-field resolver, which reuses `ZrCore_Reflection_ResolveToken()` and FieldDef layout binding, matches byte offset and type-layout id, requires `ZR_TYPE_LAYOUT_FIELD_FLAG_VALUE_SLOT`, and verifies the caller-provided inline storage range. The focused fixture starts with int `11`, writes int `271828`, then reads back through the read API to prove the slot changed; it also covers null state, null runtime, non-FieldDef token, null inline storage, null value, and short storage. RED/GREEN covered Windows MSVC Debug: the new test first failed to link because `ZrCore_Reflection_WriteFieldInfoTokenValue` did not exist, then passed 13/0 after the API and implementation were added. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 13/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Raw POD/nested inline field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD read/write update (2026-07-01 04:45:19 +08:00): runtime reflection can now read and write a same-runtime FieldDef token's primitive POD inline field through the existing `ZrCore_Reflection_ReadFieldInfoTokenValue()` / `ZrCore_Reflection_WriteFieldInfoTokenValue()` APIs. The value boundary now lives in `zr_vm_core/src/zr_vm_core/reflection_field_value.c`; it resolves the FieldDef owner field through the existing layout binding, keeps `VALUE_SLOT` copy behavior unchanged, and routes raw primitive fields through validated `FIELD_SIG(PRIMITIVE(...))` metadata. Raw primitive fields must have no `VALUE_SLOT`, GC value, or ownership value flags, and their layout byte size must exactly match the primitive C byte size before `memcpy` load/store is used. The focused fixture covers raw int32 storage at offset 40: read `-12345` as `INT64`, reject bool write, write int `2048`, and read back `2048`. RED/GREEN covered Windows MSVC Debug: the new test first ran 14 tests with one primitive POD read failure, then passed 14/0 after the split implementation was added. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 14/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Broader primitive fixture coverage, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD matrix update (2026-07-01 05:06:21 +08:00): the FieldInfo primitive POD raw inline value boundary now has representative matrix coverage for bool, uint32, and double in addition to the initial int32 fixture. `test_reflection_reads_and_writes_field_info_primitive_pod_matrix` uses a shared raw primitive FieldDef fixture and the same public read/write APIs. The bool case reads true, rejects int write, and writes false; the uint32 case reads `0xFEDC1234` as `UINT64`, rejects bool write, and writes `0xAABBCCDD`; the double case reads `6.25` as `DOUBLE`, rejects bool write, and writes `-12.5`. This was a coverage GREEN: Windows MSVC Debug passed 15/0 immediately after the matrix was added, so no production code changed beyond the existing generic primitive loader/store. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 15/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full primitive width/overflow coverage, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD width matrix update (2026-07-01 09:35:02 +08:00): the FieldInfo primitive POD raw inline value boundary now has full storage-width matrix coverage for the remaining primitive scalar widths used by the raw field path. `test_reflection_reads_and_writes_field_info_primitive_pod_width_matrix` reuses the same public read/write APIs and covers int8, int16, int64, uint8, uint16, uint64, and float32 raw inline storage. The signed cases read/write negative and positive values at their native widths, the unsigned cases cover 8/16/64-bit byte patterns, and the float32 case reads as `DOUBLE` then writes through `ZrCore_Value_InitAsFloat`. This was a coverage GREEN after the existing generic primitive loader/store; only the test helper needed an unreachable-code warning cleanup. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 16/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Numeric overflow/range semantics, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD integer range guard update (2026-07-01 09:49:10 +08:00): runtime reflection now rejects out-of-range integer writes to primitive POD raw inline fields before copying bytes into storage. `reflection_field_value.c` computes signed min/max by target storage width, computes unsigned max by target storage width, rejects signed-negative values for unsigned targets, and rejects unsigned values that exceed the signed target max before casting. `test_reflection_rejects_out_of_range_field_info_primitive_pod_integer_writes` covers int8 overflow/underflow, unsigned-to-int8 overflow, signed-negative-to-uint8, uint8 overflow, and unsigned-to-int64 overflow while asserting the raw bytes still decode to the original value after each failed write. RED was Windows MSVC Debug focused `reflection_token_resolve` failing 1/17 with `Expected FALSE Was TRUE`; GREEN passed 17/0 after the guard. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 17/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Float32 narrowing/finite semantics, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD float32 range guard update (2026-07-01 09:58:56 +08:00): runtime reflection now rejects out-of-range float32 writes to primitive POD raw inline fields before casting and copying bytes into storage. `reflection_field_value.c` checks `FIELD_SIG(PRIMITIVE(FLOAT))` writes against `FLT_MAX`, rejecting double source values greater than `FLT_MAX` or less than `-FLT_MAX`; failed writes leave the original raw float32 bytes intact. `test_reflection_rejects_out_of_range_field_info_primitive_pod_float32_writes` covers positive and negative out-of-range double sources and verifies the field still reads as the original `1.25f`. RED was Windows MSVC Debug focused `reflection_token_resolve` failing 1/18 with `Expected FALSE Was TRUE`; GREEN passed 18/0 after the guard. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 18/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. NaN/precision policy, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD float32 NaN guard update (2026-07-01 10:11:20 +08:00): runtime reflection now rejects NaN float32 writes to primitive POD raw inline fields before casting and copying bytes into storage. `reflection_field_value.c` rejects NaN via self-inequality before the existing `FLT_MAX` range check; failed writes leave the original raw float32 bytes intact. `test_reflection_rejects_nan_field_info_primitive_pod_float32_writes` covers a NaN double source from `strtod("nan", NULL)` and verifies the field still reads as the original `1.25f`. RED was Windows MSVC Debug focused `reflection_token_resolve` failing 1/19 with `Expected FALSE Was TRUE`; GREEN passed 19/0 after the guard. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 19/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Float32 precision policy, nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo primitive POD float32 precision guard update (2026-07-01 10:21:28 +08:00): runtime reflection now rejects precision-losing float32 writes to primitive POD raw inline fields before casting and copying bytes into storage. `reflection_field_value.c` uses `reflection_field_value_float32_can_store_losslessly()` to reject NaN, reject values outside `[-FLT_MAX, FLT_MAX]`, and require `(TZrDouble)(TZrFloat32)value == value` before the final cast. `test_reflection_rejects_precision_loss_field_info_primitive_pod_float32_writes` covers `1.0 + FLT_EPSILON / 4.0` and verifies the field still reads as the original `1.25f` after the rejected write. RED was Windows MSVC Debug focused `reflection_token_resolve` failing 1/20 with `Expected FALSE Was TRUE`; GREEN passed 20/0 after the guard. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 20/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo metadata runtime carrier update (2026-07-01 10:36:14 +08:00): the minimum FieldDef token FieldInfo object now carries `metadataRuntime` as a native pointer alongside `metadataToken`. `ZrCore_Reflection_BuildFieldInfoTokenObject()` writes the attached `SZrMetadataRuntime *`, and the focused fixture asserts the field is `ZR_VALUE_TYPE_NATIVE_POINTER` and points to the same runtime used to materialize the object. This is an object-internal same-runtime identity carrier for later object-level FieldInfo field access; it does not add metadata tables, change token ABI, or implement `FieldInfo.GetValue/SetValue`. RED was Windows MSVC Debug focused `reflection_token_resolve` failing 1/20 with the field missing; GREEN passed 20/0 after the carrier. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 20/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Nested POD/struct field marshaling, object-level FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo object value read update (2026-07-01 10:52:48 +08:00): runtime reflection now has a read-only object-level FieldInfo inline value adapter. `ZrCore_Reflection_ReadFieldInfoObjectValue()` reads `FieldInfo.metadataRuntime` as a native pointer and `FieldInfo.metadataToken` as an integer token, validates both carriers, then delegates to `ZrCore_Reflection_ReadFieldInfoTokenValue()`. The object-field reads use native-call pinning while creating string keys, so the adapter can safely bridge the public FieldInfo object back to the same token-driven value boundary. The focused fixture builds a FieldInfo object, stores int `271828` in a `VALUE_SLOT`, covers null state/null FieldInfo/short storage failure paths, and verifies successful object-level read. RED was Windows MSVC Debug build/link failing because `ZrCore_Reflection_ReadFieldInfoObjectValue` did not exist; GREEN passed `reflection_token_resolve` 21/0 after the API and adapter were added. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 21/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Object-level SetValue, nested POD/struct field marshaling, full FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo object value write update (2026-07-01 11:06:55 +08:00): runtime reflection now has an object-level FieldInfo inline value write adapter. `ZrCore_Reflection_WriteFieldInfoObjectValue()` reads `FieldInfo.metadataRuntime` as a native pointer and `FieldInfo.metadataToken` as an integer token through the shared FieldInfo identity helper, validates both carriers, then delegates to `ZrCore_Reflection_WriteFieldInfoTokenValue()`. The focused fixture builds a FieldInfo object, starts with int `11` in a `VALUE_SLOT`, covers null state/null FieldInfo/null storage/null value/short storage failure paths, verifies the rejected writes leave the original value intact, and then writes int `161803` through the object adapter. RED was Windows MSVC Debug build/link failing because `ZrCore_Reflection_WriteFieldInfoObjectValue` did not exist; GREEN passed `reflection_token_resolve` 22/0 after the API and adapter were added. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 22/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full managed `FieldInfo.SetValue`, nested POD/struct field marshaling, full FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo object primitive POD coverage update (2026-07-01 11:15:23 +08:00): object-level FieldInfo read/write adapters are now covered against primitive POD raw inline fields as well as `VALUE_SLOT` storage. The focused fixture builds a FieldInfo object for an int32 raw primitive field, reads `-12345` through `ZrCore_Reflection_ReadFieldInfoObjectValue()`, rejects a bool write through `ZrCore_Reflection_WriteFieldInfoObjectValue()` while preserving the raw bytes, then writes and reads back int `4096`. This was a coverage GREEN because the existing S4Z14/S4Z15 object adapters already delegate into the token-driven primitive POD path; no new API or metadata ABI was added. Verification covered Windows focused `reflection_token_resolve` 23/0, WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 23/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full managed `FieldInfo.SetValue`, nested POD/struct field marshaling, full FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline struct borrowed view update (2026-07-01 11:26:41 +08:00): runtime reflection now has the first read-only contract for non-primitive inline aggregate fields. `ZrCore_Reflection_ReadFieldInfoTokenValue()` and the object adapter read the field signature type node, require `FIELD_SIG(TYPE_DEF/TYPE_REF)`, require a resolved struct/union field type layout with matching byte size, and reject `VALUE_SLOT`, GC, or ownership field flags before returning `ZR_VALUE_TYPE_NATIVE_POINTER` that borrows the caller's inline storage address. `ZrCore_Reflection_WriteFieldInfoObjectValue()` still rejects that field because writes remain limited to `VALUE_SLOT` and primitive POD. The focused fixture reads a two-int inline struct view, verifies native-pointer metadata/no ownership, verifies short-storage rejection, verifies scalar write rejection preserves raw bytes, and verifies GC/ownership flagged fields are not exposed. RED was Windows focused `reflection_token_resolve` failing 1/24 with `Expected TRUE Was FALSE`; GREEN passed 24/0. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 24/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full nested field decomposition/copy/write, full managed FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate replacement/drop coverage update (2026-07-01 12:09:54 +08:00): runtime reflection now has focused coverage for replacing an existing owned nested value through the S4Z19 inline aggregate field-copy write path. The new object-level FieldInfo fixture builds a retained `FIELD_SIG(TYPE_DEF)` inline aggregate with `FIELD_COPY/FIELD_DROP` layout and a nested `SZrTypeValue` field marked `VALUE_SLOT | GC_VALUE | OWNERSHIP_VALUE`; destination starts as a unique-owned old string and the native-pointer source carries a plain new string. `ZrCore_Reflection_WriteFieldInfoObjectValue()` delegates through `ZrCore_TypeLayout_CopyInline()` and central `ZrCore_Value_Copy()`, releasing the old owner strong ref from 1 to 0 and normalizing destination ownership metadata to none while leaving the new source unowned. This was coverage GREEN: no production code or metadata ABI changed; Windows focused `reflection_token_resolve` passed 25/0 immediately after adding the test. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 25/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Recursive nested field access, full managed FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested VALUE_SLOT read update (2026-07-01 12:24:56 +08:00): runtime reflection now exposes the first layout-indexed nested inline field read boundary. `ZrCore_Reflection_ReadFieldInfoTokenNestedValue()` and `ZrCore_Reflection_ReadFieldInfoObjectNestedValue()` recover the retained FieldDef token/FieldInfo identity, validate `FIELD_SIG(TYPE_DEF/TYPE_REF)`, require a resolved struct/union field type layout, then read a nested `SZrTypeLayoutField` by index when that child is a `VALUE_SLOT`. The value is materialized with `ZrCore_Value_Copy()` into a caller `SZrTypeValue`; short storage, out-of-range child index, and outer inline aggregate fields with GC/ownership flags are rejected. RED was Windows MSVC Debug build/link failing for the missing object API; GREEN passed `reflection_token_resolve` 26/0 after adding the token/object adapters. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 26/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Multi-level recursive paths, nested writes, primitive raw child reads, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested VALUE_SLOT write update (2026-07-01 12:40:09 +08:00): runtime reflection now exposes the matching layout-indexed nested inline field write boundary for the S4Z21 read path. `ZrCore_Reflection_WriteFieldInfoTokenNestedValue()` and `ZrCore_Reflection_WriteFieldInfoObjectNestedValue()` recover the retained FieldDef token/FieldInfo identity, validate `FIELD_SIG(TYPE_DEF/TYPE_REF)`, require the same non-GC/non-ownership inline aggregate owner field, then write a nested `SZrTypeLayoutField` by index when that child is a `VALUE_SLOT`. The write uses `ZrCore_Value_Copy()`, so replacement/drop behavior is centralized: the focused fixture replaces a unique-owned old string with a plain new string, verifies the old owner strong ref drops from 1 to 0, and verifies destination ownership metadata is normalized to none. Short storage, out-of-range child index, and outer inline aggregate fields with GC/ownership flags are rejected without changing the old value. RED was Windows MSVC Debug build/link failing for the missing object API; GREEN passed `reflection_token_resolve` 27/0 after adding the token/object adapters. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 27/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Multi-level recursive paths, primitive raw child writes, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested VALUE_SLOT path read update (2026-07-01 13:00:18 +08:00): runtime reflection now exposes the first multi-level recursive nested inline field read boundary. `ZrCore_Reflection_ReadFieldInfoTokenNestedPathValue()` and `ZrCore_Reflection_ReadFieldInfoObjectNestedPathValue()` recover the retained FieldDef token/FieldInfo identity, validate `FIELD_SIG(TYPE_DEF/TYPE_REF)`, then walk `nestedFieldIndices[]` through `SZrTypeLayoutField.typeLayoutIndex` and `ZrCore_MetadataRuntime_ResolveTypeLayout()` until the leaf `VALUE_SLOT` child is copied into a caller `SZrTypeValue`. Intermediate child fields must fit their parent layout, must not carry `VALUE_SLOT`, GC, or ownership flags, must have a registered struct/union child layout, and must match that child layout's byte size. The recursive traversal helpers were split from `reflection_field_value.c` into `reflection_field_value_nested.{h,c}` after the touched implementation file crossed 1100 lines. RED was Windows MSVC Debug build/link failing for the missing object path API; GREEN passed `reflection_token_resolve` 28/0 after adding the token/object adapters and helper split. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 28/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Nested path write, primitive raw child marshaling, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested VALUE_SLOT path write update (2026-07-01 13:14:27 +08:00): runtime reflection now exposes the matching multi-level recursive nested inline field write boundary. `ZrCore_Reflection_WriteFieldInfoTokenNestedPathValue()` and `ZrCore_Reflection_WriteFieldInfoObjectNestedPathValue()` recover the retained FieldDef token/FieldInfo identity, validate `FIELD_SIG(TYPE_DEF/TYPE_REF)`, then walk `nestedFieldIndices[]` through the same `SZrTypeLayoutField.typeLayoutIndex` and `ZrCore_MetadataRuntime_ResolveTypeLayout()` chain used by path reads. Intermediate child fields must fit their parent layout, must not carry `VALUE_SLOT`, GC, or ownership flags, must have a registered struct/union child layout, and must match that child layout's byte size. The leaf must be a `VALUE_SLOT` child; writes use `ZrCore_Value_Copy()`, so replacing an existing unique-owned value drops the old owner and normalizes destination ownership metadata. RED was Windows MSVC Debug build/link failing for the missing object path write API; GREEN passed `reflection_token_resolve` 29/0 after adding the token/object adapters and path writer. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 29/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Primitive raw child marshaling, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested primitive POD path update (2026-07-01 13:37:45 +08:00): runtime reflection now exposes the first multi-level recursive nested primitive raw child read/write boundary. `ZrCore_Reflection_ReadFieldInfoTokenNestedPathPrimitiveValue()`, `ZrCore_Reflection_WriteFieldInfoTokenNestedPathPrimitiveValue()`, and the matching object adapters recover the retained FieldDef token/FieldInfo identity, validate `FIELD_SIG(TYPE_DEF/TYPE_REF)`, then walk `nestedFieldIndices[]` through `SZrTypeLayoutField.typeLayoutIndex` and `ZrCore_MetadataRuntime_ResolveTypeLayout()` until the leaf raw child is marshaled through the shared primitive POD guard. `reflection_field_value_primitive.{h,c}` now holds the shared raw primitive load/store checks, keeping top-level and nested primitive paths on the same byte-size, range, float32, NaN, precision, and type-mismatch rules. The focused fixture covers a two-level `{0,0}` INT32 raw child read/write, short storage, zero-length path, final child out-of-range, missing intermediate registered layout, intermediate VALUE_SLOT/GC/ownership flags, leaf VALUE_SLOT rejection, byte-size mismatch, bool write mismatch, and byte preservation after rejected writes. RED was Windows MSVC Debug build/link failing for the missing object primitive path APIs with C4013 and LNK2019; GREEN passed `reflection_token_resolve` 30/0 after adding token/object adapters, nested primitive traversal, and the primitive helper split. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 30/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full primitive width/signature-derived nested matrix, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested primitive POD leaf layout guard update (2026-07-01 13:51:37 +08:00): runtime reflection now rejects nested primitive leaf fields that still carry a registered child layout identity. `ZrCore_Reflection_ReadFieldInfoTokenNestedPathPrimitiveValue()` and `ZrCore_Reflection_WriteFieldInfoTokenNestedPathPrimitiveValue()` continue to use the S4Z25 path traversal and shared primitive POD guard, but `reflection_field_value_nested.c` now requires the leaf `SZrTypeLayoutField.typeLayoutIndex` to be `ZR_FUNCTION_FRAME_TYPE_LAYOUT_ID_NONE` before treating the leaf as raw primitive storage. This keeps top-level primitive FieldInfo behavior intact while preventing a nested inline aggregate child from being interpreted as a scalar raw child. RED was Windows MSVC Debug focused `reflection_token_resolve` 30 tests / 1 failure with `Expected FALSE Was TRUE`; GREEN passed `reflection_token_resolve` 30/0 after the guard. Verification covered WSL GCC/clang/MSVC Debug builds of the three focused targets and focused CTest 3/3 on all three environments. Full primitive width/signature-derived nested matrix, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested primitive POD path matrix update (2026-07-01 14:00:03 +08:00): runtime reflection coverage now extends the S4Z25 nested primitive path from INT32 to representative bool, uint32, and double raw children. The focused fixture keeps the same FieldInfo object, retained FieldDef token, two-level `{0,0}` path, and S4Z26 leaf layout identity guard, then adjusts the retained leaf/intermediate layout byte-size and alignment to prove the shared primitive POD guard handles the representative scalar families on nested paths. This was a coverage GREEN with no production code or public API change: Windows MSVC Debug `reflection_token_resolve` passed 30/0 immediately after the test extension, and WSL GCC/clang/MSVC Debug focused CTest stayed 3/3. Full primitive width/signature-derived nested matrix, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate nested primitive POD storage-width path matrix update (2026-07-01 14:05:25 +08:00): runtime reflection coverage now extends the same S4Z25/S4Z26/S4Z27 nested primitive path fixture to storage-width raw children: int8, int16, int64, uint8, uint16, uint64, and float32. The fixture factors layout byte-size/alignment changes through `configure_nested_primitive_path_field_sizes()`, keeps the same retained FieldDef token and two-level `{0,0}` object path, and continues to exercise the leaf layout identity guard plus the shared primitive POD guard. This was a coverage GREEN with no production code or public API change: Windows MSVC Debug `reflection_token_resolve` passed 30/0 immediately after the test extension, and WSL GCC/clang/MSVC Debug focused CTest stayed 3/3. Full signature-derived nested binding, managed `FieldInfo.GetValue/SetValue`, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate field-copy write update (2026-07-01 11:52:49 +08:00): runtime reflection now extends the borrowed-source write boundary from blittable raw byte copy to layout-aware copy for selected non-blittable inline aggregates. `ZrCore_Reflection_WriteFieldInfoTokenValue()` and the object adapter still require the S4Z17 inline aggregate borrowed-view predicate, a non-null native-pointer source, matching `FIELD_SIG(TYPE_DEF/TYPE_REF)` field type layout size, and no `VALUE_SLOT`, GC, or ownership field flags. Writes now delegate to `ZrCore_TypeLayout_CopyInline()`, so raw-copy layouts keep the byte-copy behavior and `copyKind == ZR_TYPE_LAYOUT_COPY_KIND_FIELD_COPY` layouts copy their fields through the central type-layout copy semantics. The focused fixture configures a two-int non-blittable field-copy layout, writes `{11, 22}` from a native pointer, and keeps null/scalar source and GC/ownership rejection coverage. RED was Windows focused `reflection_token_resolve` failing 1/24 with `Expected TRUE Was FALSE`; GREEN passed 24/0. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 24/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Destination ownership replacement/drop lifecycle, recursive nested field access, full managed FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT FieldInfo inline aggregate borrowed-source write update (2026-07-01 11:34:10 +08:00): runtime reflection now supports the second narrow inline aggregate contract: writing a blittable inline struct/union field from a native-pointer source. `ZrCore_Reflection_WriteFieldInfoTokenValue()` and the object adapter reuse the same field signature type-node reader, require `FIELD_SIG(TYPE_DEF/TYPE_REF)`, require resolved struct/union field type layout byte-size identity, reject `VALUE_SLOT`, GC, ownership, null native-pointer source, scalar source, and non-blittable layouts, then copy exactly `fieldLayout->byteSize` bytes into caller inline storage. The focused fixture now writes `{2468, -1357}` from a native pointer, verifies null/scalar source rejection preserves bytes, verifies non-blittable rejection preserves bytes, and keeps the GC/ownership exposure guard. RED was Windows focused `reflection_token_resolve` failing 1/24 with `Expected TRUE Was FALSE`; GREEN passed 24/0. Verification covered WSL GCC/clang/MSVC Debug `zr_vm_reflection_token_resolve_test` 24/0, `zr_vm_metadata_runtime_query_test` 24/0, `zr_vm_metadata_runtime_typespec_layout_test` 17/0, and focused CTest 3/3 on all three environments. Full nested field decomposition, non-blittable field-copy/drop semantics, full managed FieldInfo methods, cross-module provider loading/version compatibility, dataflow analysis, DESCRIPTION promotion, and full metadata sweep remain later work.

AOT dynamic dependency bound TypeRef token layout root update (2026-06-30 17:56:52 +08:00): opt-in AOT C code stripping now allows function decorator metadata `dynamicDependencyTypeToken` to name a current embedded zrp `TYPE_REF` token record when that record is already bound to a current blob `TYPE_DEF` through `targetMetadataToken`. `backend_aot_c_type_layout_metadata_roots.c` scans the `TOKEN_RECORDS` section, requires a unique matching `SZrMetadataTokenRecord`, requires `record->targetMetadataToken` to be a `TYPE_DEF`, then resolves that TypeDef row's `typeLayoutId` through the existing current-module TypeDef path. Generated C can therefore trim the only function that originally referenced the layout while preserving the target TypeDef `SZrTypeLayout`, registration entry, type-layout statistics, `code_stripping.annotationTypeLayoutRoot[]`, and root-only `zr_aot_type_layout_tokens[]` fallback. Verification covered WSL GCC/clang focused code stripping 10/0, source contracts 24/0, type-layout contracts 1/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, WSL GCC/clang focused CTest 2/2, MSVC Debug focused code stripping 10/0, source contracts 24/0, type-layout contracts 1/0, MSVC focused CTest 2/2, and Windows smoke binaries returning OK with ignored Unix-only assertions and zero failures. This is only an embedded-metadata bound TypeRef-to-current-TypeDef layout root; cross-module provider loading/version compatibility, runtime TypeRef-to-layout resolution, field reflection, dataflow analysis, warning policy, DESCRIPTION promotion, and complete metadata sweep remain later work.

AOT dynamic dependency type token root update (2026-06-30 16:58:17 +08:00): opt-in AOT C code stripping now allows function decorator metadata `dynamicDependencyTypeToken` to preserve a current-module generated `SZrTypeLayout` descriptor through embedded zrp TypeDef/TypeSpec rows. The C emitter passes the embedded metadata blob into the type-layout root collector before trimming, the collector accepts only `TYPE_DEF` and `TYPE_SPEC` tokens, resolves each token to a unique non-none row `typeLayoutId`, verifies an existing layout resolver function, and appends the layout id to the same root set consumed by type-layout stats, declarations, GC descriptor emission, and code registration. `backend_aot_c_type_layout_tokens.c` now also uses the metadata blob as a root-only fallback, so generated `zr_aot_type_layout_tokens[]` retains `0x02000001u` or `0x07000001u` even when no retained frame references the layout. Verification covered WSL GCC/clang focused code stripping 8/0, source contracts 24/0, type-layout contracts 1/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, WSL GCC/clang focused CTest 2/2, MSVC Debug focused code stripping 8/0, source contracts 24/0, type-layout contracts 1/0, MSVC focused CTest 2/2, and Windows smoke binaries returning OK with ignored Unix-only assertions and zero failures. This is only a current-module TypeDef/TypeSpec token-to-typeLayoutId carrier; FieldDef, TypeRef/cross-module type tokens, field dependencies, `@dynamically_accessed` flow analysis, warning policy, DESCRIPTION promotion, and complete metadata sweep remain later work.

AOT dynamic dependency type-layout root update (2026-06-30 16:25:31 +08:00): opt-in AOT C code stripping now allows function decorator metadata `dynamicDependencyTypeLayoutId` to preserve a current-module generated `SZrTypeLayout` descriptor even when the function that first referenced the layout is trimmed. The C emitter scans the pre-trim function table, de-duplicates uint32 layout ids, and passes the root set through type-layout stats, declaration emission, index-space calculation, GC descriptor table emission, and code-registration `typeLayouts[]` emission. Generated C exposes `code_stripping.annotationTypeLayoutRoots` plus indexed root markers; the focused generated-C fixture proves function 2 is removed while `ZrTypeLayout_2` and `zr_aot_type_layouts[2] = &ZrTypeLayout_2` remain. Verification covered WSL GCC/clang focused code stripping 6/0, source contracts 24/0, type-layout contracts 1/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, WSL GCC/clang focused CTest 2/2, and MSVC Debug focused code stripping 6/0, source contracts 24/0, type-layout contracts 1/0. This is only a numeric current-module typeLayoutId carrier; TypeDef/TypeSpec token dependencies, field dependencies, cross-module annotations, `@dynamically_accessed` flow analysis, warning policy, and complete metadata sweep remain later work.

AOT annotation warning suppression update (2026-06-30 12:54:26 +08:00): `SZrAotWriterOptions.suppressAnnotationWarnings` now suppresses the compile-time annotation warning stream without affecting runtime fallback diagnostics. The AOT C emitter still scans retained static callers of `requiresUnreferencedCode: true` callees, but when the option is enabled it writes `trim_warnings.annotationCount = 0`, moves the total to `trim_warnings.annotationSuppressedCount`, and omits per-warning `trim_warning.annotation[]` markers. The default path keeps the existing `reason=requires-unreferenced-code` and optional `message="..."` marker shape. Verification covered WSL GCC/clang CTest `aot_reachability|aot_c_code_stripping|aot_c_reflection_annotation_preserve` 3/3, WSL GCC/clang annotation preserve 7/0, global shared-library smoke 10/0, call shared-library smoke 5/0, dynamic deopt bridge smoke 7/0, source contracts 22/0, plus Windows MSVC Debug same CTest 3/3, annotation preserve 7/0, source contracts 22/0, and Unix-only smoke tests ignored with zero failures. This is a writer-level global suppression control, not attribute-level per-warning suppression, promotion, dataflow analysis, or complete trim analysis.

AOT type layout token carrier update (2026-06-25 23:13:20 +08:00): AOT ABI 10 adds `typeLayoutTokens/typeLayoutTokenCount` to the code-registration and compiled-module descriptors. Generated C emits a `zr_aot_type_layout_tokens[]` carrier indexed by cTypeId/typeLayoutId, runtime loading validates descriptor/codeRegistration pointer/count agreement and requires the count to match `typeLayoutCount`, and metadata runtime mirrors the count during attach. `ZrCore_MetadataRuntime_ResolveTypeLayoutToken()` and `ZrCore_MetadataRuntime_ResolveCTypeIdToken()` now try valid TypeDef/TypeSpec entries from that table before falling back to zrp row scans. Verification covered WSL GCC/clang focused executables 14/0, 19/0, 1/0, 8/0, and 2/0, plus MSVC Debug focused executables 14/0, 19/0, 1/0 and existing Unix-only smoke ignores. This carrier is not yet a persistent cTypeId-to-token index, new `.zro` schema, generic layout synthesis, or public generic reflection entity support.

AOT generated type layout token population update (2026-06-26 00:42:14 +08:00): generated `zr_aot_type_layout_tokens[]` entries now carry real `TYPE_DEF` tokens for local named struct/union layouts that can be uniquely matched to TypeDef metadata, and real `TYPE_SPEC` tokens for current-function generated generic layouts whose type name structurally matches a unique canonical TypeSpec signature blob. The token writer lives in `backend_aot_c_type_layout_tokens.c`, the type-layout emitter exposes a generated table resolver, and union layouts get runtime `SZrTypeLayout` descriptors so a local union TypeDef can bind to the generated layout registry. Missing metadata, ambiguous names, cross-module records, and unsupported signature shapes remain `0u` and still rely on zrp row-scan fallback. Verification covered WSL GCC/clang focused TypeSpec token emission through `aot_c_type_layout_contracts|aot_c_generic_call_typed` CTest 2/2 plus source contracts 19/0 and value-type smoke 3/0, and MSVC Debug covered the same CTest 2/2 plus source contracts 19/0 and value-type smoke 2/0/1 ignored. This token-population slice is not yet a cross-module token table, new `.zro` schema, generic layout synthesis, MethodSpec materialization, or public generic reflection entity support.

AOT generated ownership offset table update (2026-06-26 01:14:40 +08:00): generated `SZrTypeLayout` descriptors now write static `ZrOwnershipOffsets_<typeLayoutId>[]` arrays for struct owner fields when the offsets are present in `ownershipFieldOffsets` or derivable from managed `fields[]` entries marked as value slots with ownership. The descriptor points `.ownershipFieldOffsets` at the generated array and keeps zero-count, union, or unsafe/unsupported cases at `ZR_NULL` with an explicit failure marker for unsupported ownership offsets. Verification covered WSL GCC/clang `aot_c_type_layout_contracts` CTest 1/1, source contracts 19/0, value-type smoke 4/0, and MSVC Debug CTest 1/1 plus source contracts 19/0 and value-type smoke 3/0/1 ignored. This is not yet union ownership-offset emission, runtime generic layout construction, cross-module token-table policy, MethodSpec materialization, or public reflection entity support.

TypeRef binding update (2026-06-18 19:02:36 +08:00): `ZrCore_Function_BindMatchingTypeRefMetadata()` now records caller `TYPE_REF -> provider TYPE_DEF / SIGNATURE` binding when the caller TypeRef already carries stable provider TypeDef target identity. The matcher checks TypeDef token, paired signature hash/token, module hash, layout identity, and base name before writing the sidecar. Verification covered WSL clang/gcc `zr_vm_metadata_type_ref_binding_test` 1/0, `zr_vm_metadata_token_model_test` 21/0, `zr_vm_project_import_canonicalization_test` 31/0, plus MSVC Debug `zr_vm_metadata_type_ref_binding_test` 1/0. Current import owner-chain TypeRefs remain module-member placeholders until producer-side stable TypeRef extraction is added.

TypeRef producer update (2026-06-18 20:11:43 +08:00; nested generic refresh 2026-06-18 20:44:02 +08:00; explicit annotation refresh 2026-06-18 22:35:57 +08:00): `compiler_metadata_type_ref.c/.h` now emits stable provider `TYPE_REF` records separately from the import owner-chain placeholder. The helper scans import target signature return/parameter types and module-qualified typed local annotations, recursively enters generic arguments, resolves provider `TYPE_DEF` identity from `SZrParserModuleInitSummary.typeDefs`, writes provider TypeDef token/signature/module/layout target fields, and lets the existing TypeRef binding helper record `TYPE_REF -> TYPE_DEF` sidecars. `module_init_analysis.c/.h` refreshes source summaries with TypeDef records after metadata token construction, and `compiler_metadata_type_def.c` recursively discovers provider TypeDefs nested in exported generic signatures. Verification covered WSL GCC/clang `zr_vm_metadata_type_ref_binding_test` 6/0, `zr_vm_metadata_token_model_test` 21/0, `zr_vm_project_import_canonicalization_test` 31/0; MSVC Debug `zr_vm_metadata_type_ref_binding_test` 6/0.

Shared string heap validation update (2026-06-18 11:40:20 +08:00): `tests/parser/test_project_import_canonicalization.c` now resolves signature blob string references through `SZrFunction.metadataStringHeap` when present and keeps the legacy inline string fallback for older blobs. Recursive TypeSig skipping carries the owning `SZrFunction`, so project-import checks no longer misread shared heap indexes as inline lengths. WSL GCC verification: `zr_vm_project_import_canonicalization_test` 29/0, `zr_vm_metadata_token_model_test` 14/0, `zr_vm_gc_test` 66/0; focused compiler integration using/ownership/plugin guard cases PASS, with the full integration target still at the existing 115/23 baseline.

Shared metadata string heap update (2026-06-18 11:42:19 +08:00): `SZrMetadataStringHeapEntry` and `metadataStringHeap` are now part of the function metadata model. `.zro` patch 30 writes the heap after `signatureBlobHeap`; `io_runtime.c` copies it to runtime `SZrFunction`; signature blobs write fixed `u32` string indexes for module/type/symbol names; runtime verification decodes patch 30 heap indexes and falls back to legacy inline strings for older artifacts. WSL clang/gcc focused verification: `zr_vm_metadata_token_model_test` 14/0, `zr_vm_project_import_canonicalization_test` 29/0, metadata CTest 1/1.

## Compile-Time Metadata Build

[compiler_typed_metadata.c](../../zr_vm_parser/src/zr_vm_parser/compiler/compiler_typed_metadata.c) 把本来只存在于短生命周期 `semanticContext` / `typeEnv` 里的结果汇总回 `SZrFunction`：

- 局部变量优先从 `typeEnv` 查推断类型
- 顶层函数声明如果已经离开原始表达式路径，仍会回填为 `function` / `closure`
- 导出变量从 `typeEnv` 查值类型
- 导出函数从 AST declaration 读取返回类型和参数类型；同名 overload 会优先通过 `callableChildIndex` 和 child function 源码范围绑定到正确 declaration，避免复用首个同名函数签名
- inline struct/union frame layout id 写入运行时序列化 `prototypeData` 的 prototype index，而不是编译期临时 type prototype 数组 index；这样运行时 resolver 能在过滤掉非序列化占位 prototype 后仍命中正确 layout

结果是：script entry function 自身就能携带 import 所需的基础签名信息，而不需要保留完整 AST 或执行模块。

## Callable Alias Export Signatures

source module 的 init summary 与最终 `typedExportedSymbols` 现在都把“导出变量别名到 imported callable”的形态保留为 callable signature：

```zr
var b = %import("b");
pub var other = b.pong;
```

这类导出在 runtime 初始化顺序上仍然是普通 `VALUE` export，readiness 不会被提升成 declaration-ready function。但 typed metadata 侧会把 `symbolKind` 标记为 `FUNCTION`，并复制目标 callable 的返回类型与参数类型。

这条规则服务两个路径：

- cyclic source summary：`module_init_analysis.c` 在构建 callable catalog 后修正 public/protected imported callable alias 的签名。
- runtime/binary metadata：`compiler_typed_metadata.c` 在落 `typedExportedSymbols` 时解析一跳 `moduleAlias.member` / `%import("module").member`，避免编译后的 source import 把别名退化成 `object`/`null`。

因此 importer 对 `a.other()` 的类型判断可以继续拿到真实返回类型，同时 module init safety 仍由原来的 export kind/readiness 约束负责。

## Module Entry Effects From Using Guards

`module_init_analysis.c` also treats plugin-guard `using` bodies as scoped import alias regions:

```zr
pub func run(): i32 {
    using (var helper = %import(".helper.math")) {
        return helper.answer;
    } else {
        return 0;
    }
}
```

While scanning the guarded body, `helper` is temporarily bound as a module alias for the canonical import key, so member references such as `helper.answer` are recorded in the callable summary's `moduleEntryEffects` under the canonical project key, for example `feature/app/helper/math` instead of raw `.helper.math`. The alias scope is restored before scanning `else`, so the guard binder does not leak into fallback code. During finalize, exported callable summaries with a resolved `callableChildIndex` copy those effects into the matching child function's `moduleEntryEffects`.

Typed and no-annotation plugin guard imports can also be expressed through the union default validation variant surface:

```zr
union DynamicModule<T> where T: zr.Module {
    Unavailable;
    @Available(m: Module);
}

using (var [m]: DynamicModule<Plugins> = %import("zr.plugins")) {
    return m.answer;
} else {
    return 0;
}

using (var [m] = %import("zr.plugins")) {
    return m.answer;
} else {
    return 0;
}

using (var [m]: PluginLoad.Available = %import("zr.plugins")) {
    return m.answer;
} else {
    return 0;
}
```

`@Available` is the single default variant for `DynamicModule<T>`, so a typed `using` annotation may name only `DynamicModule<Plugins>` and destructure the default tuple payload as `var [m]`; explicitly naming `DynamicModule<Plugins>.Available` remains accepted only because `Available` is marked with `@`. If a `%import` annotation names a non-default variant, the compiler rejects it instead of treating that variant as the import success payload. For `%import` resources with tuple/object destructuring and no annotation, the compiler resolves the current script's `DynamicModule` default variant before using the same guard helper. `PluginLoad.Available` is also accepted as a built-in annotation surface for the same success payload, so it does not require a user-declared `DynamicModule` union. The bare `Module` payload type is parsed as an implicit builtin type for this declaration surface, matching the guarded import helper that binds the imported module object before the block body is scanned. Typed, no-annotation, and `PluginLoad.Available` payload bindings are also registered with the plugin escape scanner before the block is compiled, so returning `m`, passing it out, using it as a switch case expression call argument such as `(sink(m))`, storing it in aggregates or template string interpolation, closure-capturing it, assigning it inside an inner region to an outer local that later escapes, or assigning it through an expression side effect such as `if (alias = m)` / `var ok = (alias = m)` without an explicit `share()` path reports `plugin_type_escape`. The explicit escape path is implemented for guarded module handles: no-arg `m.share()` lowers to `ZR_IO_NATIVE_HELPER_OWNERSHIP_SHARE_PLAIN` / `ZrCore_Ownership_NativeSharePlain`, keeps `m` as the guard-scoped plain module binding, and returns a releasable shared owner handle. Guard success also creates a hidden shared owner for the same plain module handle and registers scope cleanup, so leaving the guard scope emits `OWN_RELEASE` without changing the visible payload. Native registry ownership tracking observes both explicit and hidden shared owners through the core ownership strong-ref observer; `ZrLibrary_NativeRegistry_GetModuleRefCount()` returns to zero after the final shared owner release.

The token builder then promotes import read/ref/call effects with canonical module and symbol names into function-level `ASSEMBLY_REF`, `TYPE_REF`, and `MEMBER_REF` records paired with `SIGNATURE` records. The record ownership chain is explicit: `AssemblyRef.ownerToken = 0`, `TypeRef.ownerToken = AssemblyRef.token`, `MemberRef.ownerToken = TypeRef.token`, and each paired signature record points back to its entity token. Exported signatures now also emit baseline `TYPE_SPEC` records for closed/composite types found in return and parameter types, with paired `SIGNATURE` records over the same canonical type signature blob. Non-union generic instances encode as `GENERIC_INST(TYPE_REF(open), args...)`, while known union instances keep the union-specific `UNION` node. The paired member signature blob starts with `ZR_METADATA_SIGNATURE_NODE_MEMBER_REF`, followed by the canonical module name, symbol name, and effect kind; if the provider export can be resolved from source or binary summary, it appends a target `METHOD_SIG` or `FIELD_SIG` and records the corresponding `targetMetadataToken`, `targetSignatureToken`, and `targetSignatureHash`. String fields in those blobs are now represented through the shared metadata string heap, and project-import verification resolves them through `SZrFunction.metadataStringHeap` with an inline-string fallback for older blobs. The effect and `AssemblyRef` record also carry `targetModuleSignatureHash` when the provider module ABI fingerprint is known, plus requested/min/max semantic module version strings. The default version range is `[compiledVersion,nextMajor)`, and dependency manifest declarations can override min/max with `minVersionInclusive` / `maxVersionExclusive` without adding `%import` parser syntax. The method sub-signature includes version, call convention, generic arity, return type, parameter count, and parameter mode/type pairs. Provider `MEMBER_DEF` signatures and import-ref target signatures now use the same encoder, so the target hash can be compared directly against the appended target bytes and provider export hash. Module-qualified typed local annotations such as `provider.Option<int>` also feed `compiler_metadata_type_ref.c`; when no import effect names that provider, token construction emits an explicit `ASSEMBLY_REF` so the stable provider `TYPE_REF` has a real owner. `compiler_metadata_ref.c` aggregates import-ref records into `moduleMetadataTokenRecords`; runtime verification searches the module ref table for a matching `MEMBER_REF`, uses that record to fill missing effect version/target identity, checks the provider module version against the AssemblyRef range, checks a nonzero target module hash against the provider entry function `moduleSignatureHash`, checks nonzero target metadata/signature tokens against the provider typed export tokens, searches the module ref table first for expected target bytes, and falls back to the function-level records only when needed for older artifacts. Guard-style `%import` lowering uses `ZrCore_Module_ImportGuardNativeEntry`, so runtime guard import now consumes those target identities before entering the block: if the provider module version, module hash, export token/hash, appended target signature bytes, or provider availability no longer matches the compiled target identity, the import guard returns null and execution falls through to `else`. Ordinary required import uses the same verification but distinguishes load, version, module, and member failures: provider unavailability raises `import_load_unavailable`, version range drift raises `assembly_version_mismatch` with min/max/actual version context, module ABI hash drift raises `assembly_signature_mismatch` with `expectedModuleHash` / `actualModuleHash`, while member token/hash/bytes drift raises `import signature mismatch` with module/member/hash context. Registered native providers share the same verifier, so target hash drift is covered for `zr.math`-style modules. Successful verification records a `moduleMetadataBindings` sidecar entry from the original caller `MEMBER_REF` token to the resolved provider `MEMBER_DEF` / `SIGNATURE` tokens and hashes. `ZrCore_Function_FindModuleMetadataBinding()` exposes that sidecar by caller ref token for runtime queries, and `.zro` patch 28 persists it across binary roundtrip. The same sidecar now also records matching caller/provider `TYPE_SPEC` records by canonical signature hash plus blob equality after successful import verification, and unmatched caller TypeSpec records are reported through the module-load diagnostic channel as `type_spec_mismatch` without failing the import. Stable provider `TYPE_REF -> TYPE_DEF` binding now follows the same best-effort pattern through `type_ref_mismatch` diagnostics. When a guarded effect belongs to an exported callable, the compile-time attach step copies it to the child function, so the same actual-caller lookup checks it; runtime does not scan every child function. Project source-loader, native descriptor-plugin load-error, and AOT descriptor validation diagnostics now flow through the core module-load diagnostic channel. Registry owner refcount integration is available through `ownerRefCount` / `ZrLibrary_NativeRegistry_GetModuleRefCount()`, and descriptor plugin invalidation/reload rejects live owner refs before unloading plugin handles or clearing cache. TypeSpec records are deduplicated locally by canonical signature and have a baseline cross-module binding plus loader-facing mismatch diagnostic path. Complete cross-region/global/async plugin type escape semantics remain separate follow-up work.

Scoped plugin release status update (2026-06-18 08:44:43 +08:00): the compiler now creates a hidden shared owner when a plain plugin guard or default `DynamicModule<T>.@Available` guard succeeds, and registers `OWN_RELEASE` scope cleanup for that hidden owner.

PluginLoad surface status update (2026-06-18 09:10:57 +08:00): `PluginLoad.Available` is now accepted as a built-in `%import` guard annotation and lowers to the same available payload path as `DynamicModule<T>.@Available`, including `.share()` and hidden scoped owner cleanup.

Registry owner refcount status update (2026-06-18 09:56:09 +08:00): the native registry now observes ownership strong-ref deltas for module objects and exposes per-module `ownerRefCount` through `ZrLibRegisteredModuleInfo` and `ZrLibrary_NativeRegistry_GetModuleRefCount()`. Descriptor safe unload/cache invalidation remains separate lifecycle work.

## Import Normalization Flow

`type_inference_import_metadata.c` 把三类 import 统一映射到同一套 compile-time prototype/type metadata：

1. native import
   - 继续从 `__zr_native_module_info` 拿原始声明
   - 再归一化成编译器自己的 prototype/member/type 视图
   - module-level members 获得 importer-local synthetic `MEMBER_DEF` / `SIGNATURE` token 与 stable signature hash，供后续 hash-first ref→def 绑定使用
2. source import
   - 走“只编译不执行”的装载路径
   - 直接读取被导入脚本的 `typedExportedSymbols`、`typedLocalBindings` 和 prototype data
3. binary import
   - 通过 `.zro` 读出 `SZrIoFunctionTyped*` 结构
   - 恢复成和 source import 一致的导出符号与 prototype/member 信息

source/binary import 的函数成员不会再按名字去重。若 provider 导出多个同名函数，import view 会保留多个 `SZrTypeMemberInfo` 候选；runtime/IO parameter metadata 复制时会保留 typed export 已经 materialize 的 `parameterTypes`，只补齐名称、default 标记和 default value。member chain 的下一段是 call 时，`type_inference_member_resolution.c` 会整理位置参数、命名参数和 default 参数，按 exact/compatible/incompatible 评分选择最合适的候选。没有 typed 候选时仍回退到既有 native callable lookup；generic callable 没有匹配时继续保留既有 generic 诊断。

统一之后，编译器不再关心模块来自 native、source 还是 binary，只看：

- 导出了哪些变量 / 函数
- 函数参数与返回类型是什么
- prototype 上有哪些字段 / 方法
- 成员调用返回什么类型

## Native FFI Wrapper Metadata

native module `types[]` entry 现在还会额外公开一组 FFI wrapper 字段，用来描述“这个 native type 在 FFI 边界该怎么 lowering”：

- `ffiLoweringKind`
- `ffiViewTypeName`
- `ffiUnderlyingTypeName`
- `ffiOwnerMode`
- `ffiReleaseHook`

这组字段在两个层面同时存在：

- module info object 的公开 metadata，供 `%type` / LSP / 调试器 / 测试读取
- runtime prototype 上的隐藏字段 `__zr_ffi*`，供 marshalling helper 直接读取

当前仓库里已经接入的 built-in wrapper 例子：

- `PointerHandle`
  - `ffiLoweringKind = "pointer"`
  - `ffiOwnerMode = "borrowed"`
- `BufferHandle`
  - `ffiLoweringKind = "pointer"`
  - `ffiOwnerMode = "owned"`

因此 typed metadata 现在不只描述“一个 type 叫什么、有哪些成员”，还会描述“它是不是一个带 native lowering 语义的 wrapper type”。

这条信息目前主要服务两条链路：

- `zr.ffi` runtime 在参数 marshalling 时判定 wrapper 是否允许直接降低为 native pointer
- parser/type inference 在 `%extern` 函数 overload 选择时，只对 FFI/native boundary 放宽 wrapper-compatible 参数检查

同一套 metadata 现在也支持 source-level wrapper class，而不是只覆盖 native registry 内建类型。

## Source Wrapper Metadata Bridge

source module 里的 wrapper class 现在可以直接声明：

```zr
#zr.ffi.lowering("handle_id")#
#zr.ffi.viewType("ModeHandleView")#
#zr.ffi.underlying("i32")#
#zr.ffi.ownerMode("borrowed")#
#zr.ffi.releaseHook("close_mode_handle")#
class ModeHandle {
    var handleId: i32;
}
```

这组 decorator 在 metadata 管道里的流向已经打通成一条闭环：

1. `compiler_class.c`
   - 把 `zr.ffi.*` wrapper decorators 编译成 type decorator metadata object
   - 同时把 decorator 名称写进 prototype decorator name list
2. `prototypeData`
   - 通过 `SZrCompiledPrototypeInfo.hasDecoratorMetadata` / `decoratorMetadataConstantIndex` 持久化
3. `%type(...)` / reflection
   - source type reflection 的 `metadata` 和 `decorators[]` 可以直接读到 `ffiLoweringKind` / `ffiViewTypeName` / `ffiUnderlyingTypeName` / `ffiOwnerMode` / `ffiReleaseHook`
4. `module_prototype.c`
   - 在 runtime materialize prototype 时，再把这组公开 metadata 桥接成隐藏字段：
     - `__zr_ffiLoweringKind`
     - `__zr_ffiViewTypeName`
     - `__zr_ffiUnderlyingTypeName`
     - `__zr_ffiOwnerMode`
     - `__zr_ffiReleaseHook`

这样 source wrapper 和 native descriptor wrapper 在 runtime 上共享同一套 hidden metadata contract。

## `handle_id` In Typed Metadata

`handle_id` 是这轮补全的第三种 lowering kind（除了已有的 `pointer` 与未来的 `value`）：

- compile-time/type inference 层：
  - 当 extern/native boundary 目标参数是整数标量，且 wrapper metadata 的
    - `ffiLoweringKind == "handle_id"`
    - `ffiUnderlyingTypeName` 与目标整数类型同名
    时，overload 选择和参数检查会把 wrapper 视为边界兼容
  - source-level wrapper decorator 当前只接受固定宽度整数名
    `i8/u8/i16/u16/i32/u32/i64/u64` 作为 `ffiUnderlyingTypeName`
  - 若声明了 `ffiViewTypeName`，source wrapper decorator 现在要求它指向同一 source file 中的
    `%extern struct`
  - 普通 zr 函数调用仍然不兼容，不会隐式转成整数
- runtime marshalling 层：
  - `ffi_runtime_callback.c` 在构造 scalar argument 时读取 prototype hidden metadata
  - 若 lowering kind 为 `handle_id` 且目标 ABI 标量类型匹配 underlying 名称，则从对象字段
    - `__zr_ffi_handleId`
    - 或 `handleId`
    取值并降为 ABI 整数

这让 typed metadata 不再只是“类型说明书”，而是 source/native wrapper 共享的 lowering contract。

## Typed Opcode Selection

M6 的 codegen 行为是“先推断，再转化，再选 opcode”。

当前已收口的路径包括：

- 二元算术
- 比较
- 赋值
- 自由函数调用
- imported module 成员调用

具体规则：

- 若两侧都是已知 `int`，生成 `ADD_INT` / 对应整数比较指令
- 若目标签名要求 `float`，先插 `TO_FLOAT`，再生成 typed call
- 若类型未知、动态成员访问、或当前路径无法给出稳定签名，退回现有 generic opcode / meta fallback

这意味着 typed path 只在“真的知道类型”时变精确，不会为了追求闭环而引入兼容分支。

native runtime struct constructor 还有一个额外边界：当 imported native metadata 把 struct prototype 标记为 `isNativeRuntime` 时，constructor lowering 会把 constructor result 作为 `$Type(...)` 和 `new Type(...)` 两条表面语法的 canonical object。这样 `container.Pair<int,string>` 这类闭型 native struct 不会在成员读取 `.first` 前被 seed receiver slot 覆盖。

### Native Member Call Fusion Boundary

`KNOWN_NATIVE_CALL` 只有在 callable slot 真的是 receiver member get 的结果时，才允许和前置 `GET_MEMBER_SLOT` 融合为 `KNOWN_NATIVE_MEMBER_CALL`。

当 compiler 已经能把 member descriptor 绑定到当前函数的 member-entry 表时，direct known member call 不再要求 `typeMember->compiledFunction` 非空。native member 本来就没有 VM closure，只要调用不是 tail-call、不是 `super` lookup、且 receiver 可以先稳定 staged 到紧邻调用窗口的目标槽，就可以直接走 known native member call 形态；VM member call 仍要求 compiled closure 无 captures。receiver staging 会显式预留目标槽，避免后续 member key 或 call window 写入未计入 `stackSlotCount` 的槽位。

static native accessor 是例外：例如 `TypeInfo.box(7)` 会通过 member slot 拿到 callable，但调用帧里没有 receiver 对象需要重写。`compile_expression_support.c` 会把这类 member entry 标成 `ZR_FUNCTION_MEMBER_ENTRY_FLAG_STATIC_ACCESSOR`，`compiler_quickening.c` 据此跳过 receiver-style native member-call fusion。

这条边界保留了实例 native member 的 fast path，也避免 static accessor 被误当成实例调用，导致 runtime 从第一个参数槽读取 receiver。

null-branch fusion 也遵守同样的 shape 边界：`RESET_STACK_NULL + LOGICAL_NOT_EQUAL` 只有在后续消费者能证明是 false 分支时才折叠为 null branch；当前接受直接 `JUMP_IF` 和 bool lowering 后的 `JUMP_IF_BOOL_FALSE`，其他分支形态继续走 generic path。

`Array<int>` hot-loop fill folding 需要同时覆盖分支融合前后的形态。早期 `SUPER_ARRAY_FILL_INT4_CONST` pass 识别 `LOGICAL_LESS_EQUAL_SIGNED + JUMP_IF` 的显式比较形态；signed branch fusion 之后会暴露 compact `SUB_SIGNED_CONST + JUMP_IF_GREATER_SIGNED + SUPER_ARRAY_ADD_INT4_CONST + ADD_SIGNED_CONST_PLAIN_DEST + JUMP` 自更新循环。因此 quickening 在 fused branch passes 和 NOP compaction 后会 late rerun fill-loop folding，把 `matrix_add_2d` 的零填充循环继续收敛到 `SUPER_ARRAY_FILL_INT4_CONST`，再交给后续 direct-result-store sweep。

## Artifact Model

LSP / compiler 侧正式消费的单模块 typed metadata 载体固定为 `.zro`。`.zro` 只保留单脚本/单模块编译中间文件职责，承载该模块的 typed metadata、token/signature、module ABI hash 和 import verification sidecar。

程序集语义链使用独立 `.zrm` 容器。`.zrm` 是可发布的 assembly/package 文件，包含 `META-INF/zrm.json` manifest、`modules/<module>.zro` 单模块 payload 和 `resources/<name>` 资源 entry；它可以作为 `.zrp references` 的单文件第三方模块库，也可以由 CLI `--emit-zrm` 从当前 project 的可达模块与 manifest resources 打包生成。资源 entry 可压缩，运行期通过 `zr.system.assembly.resourceExists/readResourceText/readResourceBytes` 从当前 project assembly 读取。详细容器格式见 [zrm-assembly-container.md](zrm-assembly-container.md)。

`.zro` 通过 `SZrIoFunctionTypedTypeRef` / `SZrIoFunctionTypedLocalBinding` / `SZrIoFunctionTypedExportSymbol` 序列化 typed metadata，
其中 `typedExportedSymbols` 额外持久化 declaration line/column span，供 binary metadata declaration resolver 直接命中导出符号，而不是只能回退到 module entry。

从 patch 20 起，`.zro` 还会持久化 typed export 的 metadata token 与 signature token，并追加函数级 signature blob heap。patch 21 在这些字段后追加 typed export 与 metadata token record 的 `signatureHash`。patch 22 在 metadata token record 中追加 `ownerToken`。patch 23 在 metadata token record 中追加 `targetSignatureHash`，并在 module effect 中追加 `targetMetadataToken`、`targetSignatureToken`、`targetSignatureHash`。patch 24 在 metadata token record 中追加 `targetMetadataToken` 和 `targetSignatureToken`，让 `MEMBER_REF` 及配对 `SIGNATURE` record 也能保留 provider def/signature token。patch 25 在 signature blob heap 后追加 `moduleMetadataTokenRecords`，保存 entry-function 级 import ref 聚合表。patch 26 在 signature blob heap 后、module ref table 前追加 `moduleSignatureHash`。patch 27 在 module effect 和 metadata token record 中追加 `targetModuleSignatureHash`。patch 28 在 module ref table 后追加 runtime `moduleMetadataBindings` binding result list。patch 29 在 metadata token model 中追加 provider `moduleVersion`，并在 module effect 与 metadata token record 中追加 AssemblyRef requested/min/max version 字符串。patch 30 在 signature blob heap 后追加 metadata string heap，signature blob 中的字符串引用改为固定 `u32` heap index。patch 31 在 metadata token record 的 `signatureHash` 后追加 record-level `layoutVersion` / `layoutHash`。patch 32 在 binding result 的 expected/resolved signature/module hash 后追加 expected/resolved `layoutVersion` / `layoutHash`。patch 33 在 module effect 的 `moduleName` 后追加 `assemblyName`，用于把 alias canonical module key 与真实 AssemblyRef identity 分离。

当 AOT C writer 嵌入的是可由 `ZrCore_ZrpMetadata_ReadHeader()` 校验的 zrp data metadata blob 时，生成 C 会输出只读 size attribution marker：总 zrp metadata 字节、token-record 字节、definition-table 字节、pool 字节，以及 tokenRecords、TypeDef/MethodDef/FieldDef、generic、TypeSpec/MethodSpec、ModuleRef 和三类 pool 的 section 明细。`.zro` 或非 zrp embedded blob 仍输出稳定 0 值 zrp marker，不改变既有 embedded module 载体语义。

`.zrp` data metadata format version 3 adds FieldDef default-value constant-pool coordinates through
`SZrZrpMetadataFieldDefRow.defaultValueConstantPoolOffset/defaultValueConstantPoolLength`. AOT C emitted metadata
pruning consumes those coordinates when rebuilding embedded metadata: retained FieldDef default-value slices are copied
to a compacted constant pool and their offsets are rewritten, while unrelated constant-pool payload remains removable.
The same emitted metadata pruning path now treats TypeSpec rows as token-record-rooted metadata:
`backend_aot_c_zrp_metadata_type_spec.{h,c}` retains a TypeSpec row only when a matching retained `TYPE_SPEC` token
record survives token-record pruning, rewrites retained row signature offsets through the compacted signature blob
pool, remaps retained TypeSpec row/token-record references to compacted TypeSpec RIDs, and lets orphan TypeSpec rows
plus their signature blob payload disappear. Retained GenericParamConstraint rows also consume that TypeSpec compaction
path: when `constraintTypeToken` is a local `TYPE_SPEC`, the emitted metadata pruner rewrites it to the compacted
TypeSpec RID after owner/range remap and direct TypeDef remap. TypeDef trailing suffix rows are pruned through
`backend_aot_c_zrp_metadata_type_def.{h,c}`: the pruner now keeps retained TypeDef rows instead of a retained prefix,
publishes compacted `TYPE_DEF` RIDs across interior orphan holes, remaps direct token-record/row-owner TypeDef
references, and lets orphan type name/namespace strings and TypeDef signature slices disappear. Retained signature blob
payloads now also receive a recursive direct `TYPE_DEF` token rewrite through
`backend_aot_c_zrp_metadata_signature.{h,c}`, so `FIELD_SIG`, `METHOD_SIG`, and standalone type-node blobs carry the
same compacted TypeDef token identity as the retained rows before signature hashes are recomputed. ModuleRef rows are now pruned through
`backend_aot_c_zrp_metadata_module_ref.{h,c}`: ModuleRef rows referenced by retained import `TYPE_REF`/`MEMBER_REF`
token records survive, and retained signature blob `ASSEMBLY_REF` payloads can also keep an otherwise token-record-orphaned
ModuleRef row plus its name/version strings alive before compacted RID publication. Retained AssemblyRef tokens are
republished with compacted RIDs, and self-only AssemblyRef plus signature records no longer retain orphan module
identity strings. This is still not persistent external export manifest/table publication. Retained
signature blob payloads that encode local `MEMBER_REF` tokens are now also rewritten through MethodDef/FieldDef
member-token compaction before signature hashes are recomputed. The retained
member-token `sourceToken -> targetToken` sidecar is now also published
through the generated AOT ABI as `SZrAotMemberTokenRemap`: generated C emits `zr_aot_member_token_remaps[]`, wires
`memberTokenRemaps/memberTokenRemapCount` into both `ZrAotCompiledModule` and `SZrAotCodeRegistration`, and lets the
runtime descriptor validator reject descriptor/codeRegistration remap-table mismatches. Runtime descriptor validation
also scans each remap entry before execution and rejects source/target tokens that are zero, not `MEMBER_DEF`, or have a
zero RID; it also rejects duplicate source-token or target-token bindings in the same table. Diagnostics include the
entry index, previous duplicate index where applicable, and source/target token values.
When a module attaches `SZrMetadataRuntime`, the runtime now mirrors `memberTokenRemapCount` and applies the same
published remap table to the loaded entry function's `typedExportedSymbols`. Existing cross-module import signature
matching therefore observes compacted retained `MEMBER_DEF` tokens from AOT-loaded providers instead of stale source
RIDs; non-member export tokens and retained tokens without a remap are preserved. This is runtime provider-publication
for the existing typed export table, not a standalone persisted export manifest writer.
Generated C now also publishes manifest `exports` as an ABI table rather than comments only: `zr_aot_manifest_exports[]`
stores each target, kind, binding flags, and bound type/member token, and `manifestExports/manifestExportCount` are wired
through both the descriptor and code registration. Method and field entries carry compacted retained `MEMBER_DEF` tokens
after pruning/remap; type entries carry bound `TYPE_DEF` tokens. Runtime descriptor validation checks pointer/count parity,
known flags, non-null targets, and token table/RID shape before accepting the module.
The persistent `.zrp` metadata format now reserves the matching file-level carrier: metadata version 4 appends a thirteenth
`manifestExports` section after the string/signature/constant pools and grows the header to 224 bytes. Each row stores the
manifest export kind, known ABI flags, target string-pool offset, and either a compacted `TYPE_DEF`/`MEMBER_DEF` token
or zero flags/tokens for an unbound declaration. Core header read/write/validate, section lookup, CLI dump/diff, and AOT
metadata size/count markers understand the section. When emitted zrp metadata pruning rebuilds a blob,
`backend_aot_c_zrp_metadata_manifest_export.c` now rewrites existing persistent manifest export rows through the compacted
string/type/member-token maps, preserves unbound rows while remapping only their target string offsets, and rejects
invalid row shape. Writer declarations bound to current-module method/field `MEMBER_DEF` tokens or type `TYPE_DEF` tokens
are appended as persistent rows during embedded metadata preparation; unbound type/method/field declarations are appended
as zero-token persistent rows. Target strings are reused or appended to the string pool, bound member/type tokens are
remapped through their retained sidecars, and the rebuilt definition tables are validated. Broader metadata sweep policy
remains separate follow-up work.
Token-record pruning and persistent manifest export row remapping now use retained-row-aware member-token wrappers for
FieldDef tokens: a FieldDef token must point at a retained FieldDef row before it is republished, and compacted target
RIDs are computed from the retained FieldDef order after retained MethodDefs rather than from the raw source field
index. This prevents a pruned FieldDef row from leaving a stale token record or causing later live fields to keep source
RIDs.
GenericParam owners and MethodSpec method tokens now treat `MEMBER_DEF` as MethodDef-only in emitted metadata pruning.
TypeDef GenericParam owners remain valid, and ordinary FieldDef token records still retain/remap live FieldDef rows, but
a FieldDef token in a method-only GenericParam owner or MethodSpec method slot is treated as a malformed row and dropped
instead of being allowed to retain a MethodSpec signature blob or fail the whole prepared metadata pass.
TypeDef token-record root checks now use retained member-token semantics without allowing FieldDef rows to reverse-retain
their own owner TypeDef. A MethodDef token in a TypeDef-rooting record must still be retained, while a FieldDef token must
resolve to a retained FieldDef row whose owner TypeDef is retained independently. This drops dead TypeDefs referenced only
by pruned FieldDef owner/target token records and keeps later live FieldDef compaction stable.
The AOT C writer can now also publish that final prepared `.zrp` blob as a sidecar file when
`SZrAotWriterOptions.compactedZrpMetadataOutputPath` is set. Publication happens only after the generated C file closes
successfully; the helper validates the final header and definition tables, writes the exact compacted bytes, and removes
a stale or partial sidecar on validation, write, or close failure while making the writer fail closed. CLI/project AOT derives that sidecar path from the generated
AOT C path, injects it only for embedded blobs that pass `.zrp` header and definition-table validation, and removes a
stale derived sidecar when the current embedded blob is not publishable compacted metadata; removal failure makes the CLI
AOT write fail closed instead of producing a fresh `.c` beside stale metadata.
Retained local `SIGNATURE` tokens are now compacted by retained signature-token-record order. If a retained row such as
MethodSpec references a local `SIGNATURE` token that no retained signature token record publishes, emitted metadata
pruning fails closed and clears the prepared embedded metadata output instead of leaking source blob/length state to the
caller.

旧 patch 产物读取时缺失区会保守置 0、null 或空表：patch 20 产物 token/blob 可用但 hash、owner、target identity、module ABI hash、module ref table、binding result list、version fields、metadata string heap、record layout identity、binding layout identity 与 effect assembly identity 为空；patch 21/22/23/24/25/26/27/28/29 按各自已存在字段读取，其后新增字段置 0/null/空；patch 30 产物 metadata string heap 可用但 record layout identity、binding layout identity 与 effect assembly identity 置 0/null；patch 31 产物 record layout identity 可用但 binding layout identity 与 effect assembly identity 置 0/null；patch 32 产物 binding layout identity 可用但 effect assembly identity 置 null。patch 33 写出的产物双写“原 typed metadata + token/signature/hash/owner/target identity + module version + AssemblyRef version range + module ABI hash + target module hash + module ref table + runtime binding result list + metadata string heap + record layout identity + binding layout identity + effect assembly identity 结构”，保证当前 import 消费路径不被迫一次性迁移。高于 `ZR_IO_SOURCE_PATCH_CURRENT` 的 future patch 会在 header 阶段被拒绝并带出 actual/supported patch 诊断。

`.zri` 继续由 [writer_intermediate.c](../../zr_vm_parser/src/zr_vm_parser/writer/writer_intermediate.c) 输出，保留：

- `TYPE_METADATA`
- `LOCAL_BINDINGS`
- `EXPORTED_SYMBOLS`
- 函数签名文本，例如 `fn add(int, int): int`

但 `.zri` 的职责已经收窄为 debug / intermediate artifact：

- 便于人读和 golden 回归
- 可承载与 `.zro` 对齐的 debug 线列范围信息
- 不再作为 language server / type inference / import metadata 的正式事实源

这条约束很重要：writer artifact 和 binary schema 可以表达相近信息，但语义入口只能有一条正式机器路径。任何 `.zro` schema 漂移都必须同步更新：

- `tests/golden/intermediate/*.zri`
- `tests/golden/binary/*.zro`
- `tests/fixtures/projects/import_binary/bin/greet.zro`

## Runtime Constraint For Typed Calls

typed opcode 只在运行时也守住调用栈边界时才成立。

本轮暴露出的底层约束是：`TO_INT` / `TO_UINT` / `TO_FLOAT` 走 meta conversion 时，scratch 区不能从 `state->stackTop` 直接复用。因为 `stackTop` 可能仍落在当前帧的 live temp 区间之前，错误复用会覆盖掉 imported callable 所在槽位。

当前修复方式与 `TO_BOOL` 保持一致：

- meta call scratch 上界使用 `callInfo->functionTop.valuePointer`
- 标记使用安全 scratch 区域

这样 `math.add(1, 2.5)` 这类 imported typed call 的 `TO_FLOAT` 不会再把 callable 自己覆盖成 `float`。

## Edge Cases And Constraints

- 没有可恢复类型的导出变量仍会降级为 `object`
- 数组类型元数据当前只记录统一元素类型视图，符合 M6 现阶段的数组元素推断需求
- `typedExportedSymbols` 只描述模块对外可见的符号，不替代 prototypeData
- 早期 M6 只保证元数据稳定落盘并可被 import / writer / runtime 验证；当前 AOT C 已消费 zrp metadata size attribution 和 FieldDef default-value constant-pool remap，typed IR 和完整 metadata sweep 仍未接入
- M6 早期 typed metadata schema 变更仍以重生成仓库内 fixture 为准；metadata token patch 20 对缺少 token 区的旧产物按空 token model 读取，patch 21 对旧 token model 产物把 `signatureHash` 置 0，patch 22 对旧 token/hash 产物把 `ownerToken` 置 0

## Test Coverage

M6 的验证不是“编译成功”级别，而是直接断言 opcode、签名和运行结果。

- [tests/parser/test_type_inference.c](../../tests/parser/test_type_inference.c)
  - source import 签名 pass / fail
  - binary import 签名 pass / fail
  - source/binary import 同名函数候选保留和调用级签名选择
  - imported member chain 返回类型
  - imported array assignment 类型拒绝
- [tests/parser/test_compiler_features.c](../../tests/parser/test_compiler_features.c)
  - mixed-type call 的 `TO_FLOAT`
  - `.zri` `TYPE_METADATA` section
  - opcode 精确选择
- [tests/parser/test_instruction_execution.c](../../tests/parser/test_instruction_execution.c)
  - source import typed call 真实执行结果
  - binary import typed call 真实执行结果
- [tests/scripts/test_artifact_golden.c](../../tests/scripts/test_artifact_golden.c)
  - `.zrs` / `.zri` / `.zro` golden 回归
- [tests/module/test_metadata_token_model.c](../../tests/module/test_metadata_token_model.c)
  - metadata token 分配
  - signature blob heap 编码
  - `signatureHash` 非零、同签名重建稳定、ABI signature 变化时 hash 变化
  - `moduleSignatureHash` 使用 public exports + `TYPE_DEF` / `TYPE_SPEC` identity；同导出函数签名但本地 union TypeDef contract/layout 漂移时 module hash 变化
  - union type signature node 编码
  - TypeSpec records for closed/composite exported signature types, local TypeSpec deduplication, baseline cross-module TypeSpec binding by canonical signature, and consumer-side TypeRef to TypeDef binding/status/diagnostics for stable TypeRef records
  - import ref 的 `ASSEMBLY_REF` / `TYPE_REF` / `MEMBER_REF` owner-chain，以及配对 `SIGNATURE` owner 指回
  - `.zro` 写入/读取 roundtrip 保留 token/hash
  - runtime loader copy 保留 token/hash、owner chain、target metadata/signature token、target signature hash identity 和 module-level import ref 聚合表
- [tests/module/test_metadata_type_ref_binding.c](../../tests/module/test_metadata_type_ref_binding.c)
  - stable provider `TYPE_REF -> TYPE_DEF` binding
  - TypeRef definition/layout mismatch status and `type_ref_mismatch` diagnostic behavior
  - provider summary backed TypeRef production from target signatures, nested generic arguments, module-qualified annotations such as `provider.Option<int>`, and destructured/unqualified alias annotations such as `Option<int>`
  - provider import signature verification rejects manifest export table token drift after the typed export candidate is selected
- [tests/module/test_metadata_runtime_query.c](../../tests/module/test_metadata_runtime_query.c)
  - function-level and entry-function module ref table token/signature record queries
  - null/zero-token handling and loose signature-pair rejection
- [tests/module/test_reflection_token_resolve.c](../../tests/module/test_reflection_token_resolve.c)
- public token resolver carriers for method/type/field/generic records, token-driven invoke guards, and minimum FieldDef token FieldInfo object materialization including nested field/declaring type literal objects, owner identity link, module name/object links, raw FieldDef flags, raw FieldDef signature blob coordinates, validated field signature header fields, field signature type-node summary fields, primitive field signature type carriers, primitive field signature type object carriers, signature type-node objects, signature base type-node objects, signature child type-node object lists, primitive child type-node semantic names, direct TypeDef base/child type-node semantic token/layout/name carriers, direct TypeRef child type-node semantic token/layout/name carriers, recursive signature type-node type literal carriers, FieldDef token metadataRuntime native-pointer carrier, FieldDef token `VALUE_SLOT` inline field read/write guards, primitive POD int32 raw inline field read/write guards, bool/uint32/double representative primitive POD raw inline field read/write guards, int8/int16/int64/uint8/uint16/uint64/float32 storage-width primitive POD raw inline field read/write guards, primitive POD integer range guards, primitive POD float32 range guards, primitive POD float32 NaN guards, and primitive POD float32 precision guards
- FieldInfo object-level `VALUE_SLOT` read adapter coverage through `ZrCore_Reflection_ReadFieldInfoObjectValue()`
- FieldInfo object-level `VALUE_SLOT` write adapter coverage through `ZrCore_Reflection_WriteFieldInfoObjectValue()`
- FieldInfo object-level primitive POD raw inline read/write coverage through the same object adapters
- FieldInfo object-level inline struct borrowed native-pointer view coverage for non-GC/non-ownership fields
- FieldInfo object-level inline aggregate borrowed-source write coverage from a non-null native-pointer source, with null source, scalar source, and non-blittable layout rejection
- FieldInfo object-level inline aggregate field-copy borrowed-source write coverage through `ZrCore_TypeLayout_CopyInline()`, while preserving null/scalar source and GC/ownership rejection
- FieldInfo object-level inline aggregate replacement/drop coverage for a nested owned `SZrTypeValue` field through `ZrCore_TypeLayout_CopyInline()` and central value-copy semantics
- FieldInfo object-level nested inline `VALUE_SLOT` read coverage through `ZrCore_Reflection_ReadFieldInfoObjectNestedValue()`, including short storage, out-of-range index, and outer GC/ownership rejection
- FieldInfo object-level nested inline `VALUE_SLOT` write coverage through `ZrCore_Reflection_WriteFieldInfoObjectNestedValue()`, including replacement/drop of an old owned value, short storage, out-of-range index, and outer GC/ownership rejection
- [tests/module/test_metadata_runtime_typespec_layout.c](../../tests/module/test_metadata_runtime_typespec_layout.c)
  - AOT runtime TypeSpec token to zrp TypeSpec row, generic base-token binding, and registry-backed layout binding
  - public TypeDef/TypeSpec token to registry-backed layout resolver and bounded multi-entry cache behavior
  - attached bound TypeRef token to target TypeDef layout resolver, cache behavior, layout identity mismatch rejection, and module identity mismatch rejection
  - registry-backed typeLayoutId to TypeDef/TypeSpec token reverse lookup and bounded multi-entry cache behavior
  - public cTypeId to TypeDef/TypeSpec token resolver under the current cTypeId/typeLayoutId registry invariant
  - code-registration typeLayout token carrier consumption, including non-type token and missing-layout rejection
  - no prototype layout cache fallback when the code-registration type layout registry is missing
- [tests/module/test_metadata_module_hash_golden.c](../../tests/module/test_metadata_module_hash_golden.c)
  - cross-toolchain stable module ABI fingerprints for a simple exported function and a local generic union export
- [tests/library/test_project_manifest_normalization.c](../../tests/library/test_project_manifest_normalization.c)
  - `.zrp` manifestVersion, assembly identity, legacy dependency/reference compatibility, publicKeyToken normalization, declaration-level `preserve` rule parsing/rejection, preserve feature condition parsing/rejection, top-level feature switch parsing/rejection, generic preserve argument parsing/rejection, declaration-level `aotMode` parsing/rejection, and export declaration parsing/rejection
  - rejects duplicate declaration-level export `kind + target` keys so ambiguous manifest export rows are blocked during project load
- [tests/cli/test_cli_aot_writer_options.c](../../tests/cli/test_cli_aot_writer_options.c)
  - `.zrp` preserve method/type/generic writer option injection, feature-conditioned root gating, generic preserve TypeSpec token binding, synthesized TypeSpec/signature binding from open metadata, TypeRef- and TypeDef-backed open-base generic instantiation identity materialization, current-module generic method MethodSpec binding, export declaration writer-option bridge, method export declaration member-token binding, full-AOT unbound generic TypeSpec rejection, full-AOT TypeSpec-only generic instantiation rejection, generated-C manifest root/export diagnostics, and opt-in code-stripping preservation/trimming behavior
  - rejects duplicate in-memory export declaration `kind + target` keys before exposing project export declarations to AOT writer options
- [tests/cli/test_cli_project_incremental.c](../../tests/cli/test_cli_project_incremental.c)
  - project-level full-AOT AOT C emission forbids shared generic missing-instance deopt while retaining the 11-S6H
    inline-struct metadata-drift guard/deopt surface
- [tests/parser/test_aot_c_code_stripping.c](../../tests/parser/test_aot_c_code_stripping.c)
  - generated AOT C zrp metadata total/token-record/definition-table/pool byte statistics and section-level size markers for valid embedded zrp metadata blobs
  - opt-in code stripping preserves dynamic-dependency type-layout roots from numeric ids, TypeDef/TypeSpec tokens, bound TypeRef token records, and FieldDef owner/field rows
  - generated AOT C method token table publishes compacted MethodDef tokens after emitted zrp metadata pruning instead of stale pre-prune RID values, and publishes the same source-to-compacted member-token remap as an ABI `SZrAotMemberTokenRemap` table
- [tests/parser/test_aot_c_descriptor_diagnostics.c](../../tests/parser/test_aot_c_descriptor_diagnostics.c)
  - AOT runtime rejects invalid member-token remap ABI entries whose source/target token is not a nonzero `MEMBER_DEF`, and reports index/sourceToken/targetToken in `lastError`
  - AOT runtime rejects duplicate member-token remap source or target bindings and reports index/previousIndex/token in `lastError`
- [tests/parser/test_aot_c_zrp_metadata_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_pruning.c)
  - emitted zrp metadata pruning drops trailing TypeDef rows with no retained token-record/member/field/type-generic roots and removes their orphan type strings while preserving source TypeDef RIDs for the retained prefix
  - retained MethodSpec row `SIGNATURE` tokens must have a matching retained signature token record; missing records make pruning fail and clear prepared output metadata
  - retained GenericParamConstraint rows that reference retained TypeSpec rows publish compacted TypeSpec RIDs after earlier TypeSpec rows are pruned
  - existing persistent manifest export rows remap compacted target string offsets and retained method/field member tokens after MethodDef pruning, including unbound rows whose token fields stay zero
  - FieldDef member tokens that reference pruned FieldDef rows are dropped, and live FieldDef rows after pruned rows publish retained-order compacted `MEMBER_DEF` RIDs
  - TypeSpec rows rooted only by token records whose `MEMBER_DEF` FieldDef owner/target rows are pruned are dropped with
    their signature blobs, while retained FieldDef-rooted TypeSpecs publish compacted `TYPE_SPEC` RIDs
  - GenericParam `MEMBER_DEF` owners and MethodSpec `methodToken` values are MethodDef-only: FieldDef tokens in those
    slots drop the malformed rows and any MethodSpec-only signature blob while preserving retained FieldDef row remap
  - TypeDef rows referenced only by token records whose FieldDef owner/target member tokens resolve to pruned FieldDef
    rows are dropped; a FieldDef token record cannot reverse-retain its own owner TypeDef
  - bound method/field manifest export declarations append persistent `.zrp` rows with compacted member tokens and target string-pool updates
  - bound type manifest export declarations append persistent `.zrp` rows with compacted TypeDef tokens and target string-pool updates
  - unbound type/method/field manifest export declarations append persistent `.zrp` rows with zero flags/tokens and target string-pool updates
- [tests/parser/test_aot_c_zrp_metadata_methodspec_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_methodspec_pruning.c)
  - focused MethodSpec pruning fixture keeps imported `MEMBER_REF` method tokens while local MethodDef rows are pruned
    and compacted, preserving the MethodSpec row, signature blob, and recomputed signature hash
- [tests/parser/test_aot_c_zrp_metadata_module_ref_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_module_ref_pruning.c)
  - focused ModuleRef pruning fixture keeps an AssemblyRef/ModuleRef row when a retained import TypeRef targets a
    TypeSpec that is retained only through a GenericParamConstraint, then rewrites the token record related token to the
    compacted AssemblyRef RID
- [tests/parser/test_aot_c_zrp_metadata_publication.c](../../tests/parser/test_aot_c_zrp_metadata_publication.c)
  - AOT C writer sidecar publication writes the final compacted `.zrp` metadata bytes after pruning, with retained
    MethodDef rows and compacted member tokens visible in the sidecar file
  - direct writer sidecar publication rejects blobs whose header is readable but whose definition tables fail validation,
    removes any stale sidecar at the requested path, and fails closed
- [tests/cli/test_cli_aot_compacted_metadata_sidecar.c](../../tests/cli/test_cli_aot_compacted_metadata_sidecar.c)
  - project/CLI AOT derives the compacted `.zrp` metadata sidecar path from the generated AOT C path
  - CLI AOT publishes the sidecar only for embedded blobs that pass `.zrp` header and definition-table validation
  - invalid definition-table blobs and ordinary non-metadata blobs do not produce compacted metadata sidecars
  - stale derived sidecars are removed when the current embedded blob is not publishable compacted metadata
  - stale-sidecar removal failure fails closed and does not write a fresh AOT C artifact
- [tests/parser/test_aot_c_zrp_metadata_typedef_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_typedef_pruning.c)
  - emitted zrp metadata pruning compacts retained TypeDef rows across interior orphan holes, publishes compacted
    `TYPE_DEF` RIDs, remaps direct token-record TypeDef fields and MethodDef owner references, and drops orphan TypeDef
    string-pool slices
  - retained signature blob payloads with direct `TYPE_DEF` type nodes are rewritten to compacted TypeDef RIDs after
    TypeDef row compaction and before signature hash recomputation
- [tests/module/test_zrp_metadata_format.c](../../tests/module/test_zrp_metadata_format.c)
  - `.zrp` v4 manifest export section header roundtrip, row payload view, string-pool target validation, unbound
    zero-token row validation, and type/member token-shape guards, plus FieldDef default-value constant-pool offset/length row validation
- [tests/parser/test_aot_c_zrp_metadata_pool_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_pool_pruning.c)
  - emitted zrp metadata pruning retains/remaps FieldDef default-value constant-pool slices and drops unrelated constant-pool payload
  - emitted zrp metadata pruning drops orphan ModuleRef rows, removes self-only AssemblyRef/signature token records,
    compacts retained AssemblyRef RIDs, remaps retained module name/version string-pool offsets, rewrites retained
    signature-blob AssemblyRef tokens, and keeps ModuleRef rows rooted only by retained signature blobs
- [tests/parser/test_aot_c_zrp_metadata_export_token_remap.c](../../tests/parser/test_aot_c_zrp_metadata_export_token_remap.c)
  - member-token remap direct helper and sidecar map retained MethodDef/FieldDef export tokens with retained FieldDef
    row context, reject pruned member tokens, reject invalid/ambiguous generated remap publication inputs, feed the
    generated-C ABI remap publication path, and build manifest export table entries with compacted member tokens while
    rejecting kind/token mismatches and duplicate manifest export kind+target declarations
- [tests/parser/test_aot_c_zrp_metadata_typespec_pruning.c](../../tests/parser/test_aot_c_zrp_metadata_typespec_pruning.c)
  - emitted zrp metadata pruning drops token-record-orphaned TypeSpec rows and removes their signature blob payload
  - retained TypeSpec row and token-record references publish compacted TypeSpec RID values after earlier TypeSpec rows are pruned
- [tests/library/test_project_import_resolver.c](../../tests/library/test_project_import_resolver.c)
  - project import aliases, dependency package/reference resolution, AssemblyRef identity/range query behavior, `.zrp` / `.zrm` references, provider-location discovery, and manifest-cycle handling
- [tests/library/test_project_import_provider_version_selection.c](../../tests/library/test_project_import_provider_version_selection.c)
  - exact `.zrp` provider alias/version AOT load-request selection for multiple versions of the same assembly, plus strict declared range rejection for out-of-range provider references
- [tests/parser/test_union.c](../../tests/parser/test_union.c)
  - union prototypeData serialization
  - variant member metadata shell
  - inline union tag/payload materialization, local copy/assignment, and owner payload release on active variant frame drop
  - typed, explicit-variant, and no-annotation `DynamicModule<T>.@Available` `%import` guard positive lowering
- [tests/parser/test_compiler_features.c](../../tests/parser/test_compiler_features.c)
  - typed and no-annotation `DynamicModule<T>.@Available` `%import` payload return escape is rejected with `plugin_type_escape`
  - no-annotation payload assignment through an inner `if`/block into an outer local remains plugin-tainted and is rejected on later return
  - plugin guard assignment expressions inside conditions and variable initializers propagate alias taint and are rejected on later return
  - switch case expressions that pass a guard handle as a call argument are rejected with `plugin_type_escape`
  - template string interpolation that stores a guard handle is rejected with `plugin_type_escape ... through field/container`
  - guarded module `.share()` emits the ownership share helper, keeps the guard module binding live, and returns a releasable shared owner
  - plugin guard scope exit emits hidden shared-owner `OWN_RELEASE` cleanup for both plain guard and default `@Available` payload paths
  - built-in `PluginLoad.Available` `%import` guard annotation lowers to the same available payload path without a user-declared `DynamicModule` union
- [tests/parser/test_project_import_canonicalization.c](../../tests/parser/test_project_import_canonicalization.c)
  - `using (var p = %import(...))` guarded body records canonical `moduleEntryEffects`
  - guarded import member effects generate function-level ref records, member-ref signature blobs, paired `signatureHash`, explicit `AssemblyRef <- TypeRef <- MemberRef` ownership, provider `targetMetadataToken` / `targetSignatureToken` / `targetSignatureHash`, and provider-derived `METHOD_SIG` parameter counts
  - guard-style `%import` uses the import-guard native helper; corrupted provider target signature hash, appended target signature blob mismatch, runtime provider unavailability, and nested callable caller hash mismatch make execution take the `else` fallback
- [tests/cmake/run_projects_suite.cmake](../../tests/cmake/run_projects_suite.cmake)
  - `hello_world`
  - `import_basic`
  - `import_binary`
  - `import_pub_function`
- [tests/cli/test_cli_import_basic_fixture.c](../../tests/cli/test_cli_import_basic_fixture.c)
  - 复制 `import_basic` source project 到临时目录，重编译并运行，断言 exported callable import 输出 `hello from import`

## Plan Sources

本实现直接对应：

- `M6 强类型推断完整闭环计划`
- `zr_vm阶段化总计划` 中关于强类型推断、import 和产物闭环的阶段目标

## Open Follow-Up

- AOT C emitted zrp metadata pruning now consumes `.zrp` v3 FieldDef default-value constant-pool coordinates and remaps
  retained default-value slices into compacted metadata, drops token-record-orphaned TypeSpec rows plus their signature
  blob payload, compacts retained TypeSpec row/token-record RID publication, requires TypeSpec-retaining FieldDef
  member-token roots to survive retained FieldDef row checks, rejects FieldDef tokens in GenericParam/MethodSpec
  MethodDef-only slots, keeps imported `MEMBER_REF` MethodSpec method tokens while local MethodDef rows are pruned,
  requires TypeDef-retaining FieldDef member-token roots to survive retained FieldDef row checks
  without self-owner reverse-retention, drops trailing orphan TypeDef suffix rows plus their type strings/signature slices, compacts retained
  TypeDef rows across interior orphan holes while remapping
  direct token-record/row-owner TypeDef references, drops orphan ModuleRef rows plus self-only AssemblyRef/signature
  token records, compacts retained AssemblyRef RID publication, rewrites retained signature-blob direct TypeDef tokens,
  compacts retained local `SIGNATURE` token publication, rejects orphan retained `SIGNATURE` references, remaps retained
  GenericParamConstraint `TYPE_SPEC` constraint tokens to compacted TypeSpec RIDs, retains TypeSpec rows/signature blobs
  rooted only by retained GenericParamConstraint `TYPE_SPEC` constraints, rewrites retained signature-blob `ASSEMBLY_REF`
  tokens through compacted ModuleRef RID publication, retains ModuleRef rows/name/version strings rooted only by retained
  signature-blob `ASSEMBLY_REF` payloads or retained import records whose TypeSpec targets are rooted by GenericParamConstraint
  context, rewrites retained signature-blob `MEMBER_REF` local member tokens
  through compacted MethodDef/FieldDef RID publication, and remaps
  published generated AOT method tokens through a retained MethodDef/FieldDef sidecar that is also emitted
  into the generated-C AOT ABI and runtime-validated for nonzero `MEMBER_DEF` source/target entry shape plus duplicate
  source/target binding rejection. Export declarations are now parsed, bound to current-module type/member tokens when
  available, and published in generated C plus persistent `.zrp` manifest export rows as compacted member/type tokens or
  zero-token unbound rows,
  and attached `SZrMetadataRuntime` now mirrors the published table pointer/count, can query exact `kind + target`
  export views, can gate those views against token/signature/module/layout binding compatibility plus export-token
  identity, and the real provider import verifier now applies that gate before writing module metadata bindings; provider
  import-path discovery/loading now reaches `.zrp` generated dynamic-library success, and exact alias/version selection
  plus strict declared range guard are covered for multi-version references. CLI/project AOT now derives a `.zrp`
  sidecar path from the generated AOT C path, while the writer-level sidecar helper publishes only blobs whose header and
  definition tables validate as real compacted `.zrp` metadata, removes stale sidecars when validation fails, and fails
  closed if the current blob is not publishable metadata. Remaining provider/metadata work is broader row-level sweep policy and full trim
  analyzer integration.
- AOT C 目前消费 zrp metadata header/section directory 做 generated-C size attribution，并消费 token records 与 TypeDef/TypeSpec/FieldDef rows 做 dynamic-dependency type-layout roots；runtime reflection 已消费 FieldDef/TypeDef string-pool rows、FieldDef raw flags、FieldDef signature blob 坐标、validated FieldDef `FIELD_SIG` header/type-node views、attached module identity 和 layout binding view 来构造最小 FieldInfo token object、declaring type carrier、owner identity link、module name/object links、metadata flags carrier、signature blob coordinate carrier、field signature header carrier、field signature type-node summary carrier、primitive field signature type carrier、primitive field signature type object carrier、signature type-node object、signature base type-node object、signature child type-node object list、primitive child type-node semantic name、direct TypeDef base/child type-node semantic token/layout/name、direct TypeRef child type-node semantic token/layout/name、recursive signature type-node type literal object、FieldInfo `metadataRuntime` native-pointer carrier、FieldDef token `VALUE_SLOT` inline field read/write boundary、primitive POD int32 raw inline field read/write boundary、bool/uint32/double representative primitive POD raw inline field read/write matrix、int8/int16/int64/uint8/uint16/uint64/float32 storage-width primitive POD raw inline field read/write matrix、primitive POD integer range guard、primitive POD float32 range guard、primitive POD float32 NaN guard、primitive POD float32 precision guard、inline struct/union borrowed native-pointer read view、blittable inline aggregate borrowed-source byte-copy write boundary、`FIELD_COPY` inline aggregate borrowed-source write boundary、nested owned value replacement/drop coverage、nested inline VALUE_SLOT child read/write boundary、multi-level nested inline VALUE_SLOT path read/write boundary、representative multi-level nested inline primitive POD raw child path read/write boundary、nested primitive leaf layout identity guard、nested primitive bool/uint32/double path matrix coverage 和 nested primitive int8/int16/int64/uint8/uint16/uint64/float32 storage-width path matrix coverage；typed IR、默认最小 metadata policy、完整 FieldInfo 行为、完整 primitive raw child matrix/signature-derived binding、完整 signature-derived field type binding 和真实 metadata sweep 仍未完整消费这批元数据
- Runtime reflection now has object-level FieldInfo value read/write adapters, a borrowed view for selected inline aggregates, restricted native-pointer source writes for blittable and selected `FIELD_COPY` inline aggregates, focused replacement/drop coverage for nested owned value slots, layout-indexed nested `VALUE_SLOT` child read/write adapters, the first multi-level nested `VALUE_SLOT` path read/write adapters, the first representative multi-level nested primitive POD raw child path read/write adapters, a nested primitive leaf layout identity guard, nested primitive bool/uint32/double path matrix coverage, and nested primitive int8/int16/int64/uint8/uint16/uint64/float32 storage-width path matrix coverage, but full managed `FieldInfo.GetValue/SetValue` method surface, complete primitive raw child matrix/signature-derived binding, complete signature-derived field type binding, and real metadata sweep remain open.
- 10-S4Z19 extends the inline aggregate borrowed-source write boundary to selected `FIELD_COPY` layouts through `ZrCore_TypeLayout_CopyInline()`. 10-S4Z20 covers destination replacement/drop for nested owned value slots on that path. 10-S4Z21 adds the first layout-indexed nested `VALUE_SLOT` child read boundary, 10-S4Z22 adds the matching child write boundary, 10-S4Z23 adds the first multi-level nested `VALUE_SLOT` path read boundary, 10-S4Z24 adds the matching path write boundary, 10-S4Z25 adds the first representative multi-level nested primitive POD raw child path read/write boundary, 10-S4Z26 rejects nested primitive leaves that still carry a child layout identity, 10-S4Z27 extends nested primitive path coverage to bool/uint32/double, and 10-S4Z28 extends the same path to int8/int16/int64/uint8/uint16/uint64/float32. Full managed `FieldInfo.GetValue/SetValue`, signature-derived primitive raw child binding, and real metadata sweep remain open.
- 更细粒度的多元素数组类型表达和 AOT/typed IR 专用消费仍未进入 `.zro` schema；当前 metadata/token 结构已经覆盖非 union 泛型闭型、union TypeDef/TypeSpec identity、stable provider TypeRef、module-qualified typed annotations 和 destructured/unqualified alias annotations
- `import_pub_function` fixture 仍带有与本轮无关的旧语法诊断噪音，当前不影响结果验证，但后续应独立清理
- 2026-07-03 11-S7ZSP / 12-S7ZZZI keeps imported `MEMBER_REF` method tokens in MethodSpec rows during compacted
  `.zrp` pruning. MethodSpec rows whose `methodToken` points at a local `MEMBER_DEF` still require retained MethodDef
  remap, while imported `MEMBER_REF` tokens remain valid method references and no longer get dropped when unrelated
  local MethodDef rows are pruned. The focused MethodSpec pruning target passes 1/0 on WSL GCC/clang and Windows MSVC
  Debug alongside source contracts 24/0, export token remap 10/0, and pruning 22/0. Broader metadata sweep policy and
  trim analyzer integration remain open.
- 2026-07-03 11-S7ZSQ / 12-S7ZZZJ adds the paired imported `MEMBER_REF` token-record guard for MethodSpec pruning.
  Imported MethodSpec method tokens must now have a matching source token record, and retained token-record remap rejects
  signature/owner/target fields that point at missing imported member records. The focused fixture drops the orphan
  MethodSpec row, `SIGNATURE` token record, and signature blob while retaining the compacted local MethodDef; WSL
  GCC/clang and Windows MSVC Debug pass source contracts 24/0, export token remap 10/0, pruning 22/0, and MethodSpec
  pruning 2/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSR / 12-S7ZZZK tightens the imported `MEMBER_REF` MethodSpec guard from token-record existence to
  retained-token-record validity. The matching imported member token record must not point at pruned local
  MethodDef/FieldDef tokens, and nested `MEMBER_REF` references in retained token-record fields run through the same
  retained-record check. The focused fixture drops the invalid imported member record, MethodSpec row, `SIGNATURE` token
  record, and signature blob; WSL GCC/clang and Windows MSVC Debug pass source contracts 24/0, export token remap 10/0,
  pruning 22/0, and MethodSpec pruning 3/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSO / 12-S7ZZZH tightens the compacted `.zrp` GenericParam publication contract: a `GenericParam.ownerToken` with `TYPE_DEF` kind is retained only when its owner TypeDef row is retained and is rewritten to the compacted TypeDef RID. GenericParam retained-row/count/range and GenericParamConstraint remap/count/range helpers now carry TypeDef/token-record/constraint context, and TypeDef retention no longer lets TypeDef-owned GenericParam rows self-root their owner TypeDef. The focused pruning fixture drops GenericParam rows owned by pruned TypeDefs; WSL GCC/clang and Windows MSVC Debug pass source contracts 24/0, export token remap 10/0, and pruning 22/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSV / 12-S7ZZZO threads GenericParam/GenericParamConstraint context through ModuleRef retained/count/compact/remap. A retained import TypeRef whose target TypeSpec is kept only by a GenericParamConstraint now keeps the related AssemblyRef/ModuleRef row, and token-record remap rewrites `relatedToken` to the compacted AssemblyRef RID. The focused ModuleRef pruning target passes 1/0 on WSL GCC/clang and Windows MSVC Debug, alongside the metadata pruning CTest matrix 5/5, TypeDef pruning 2/0, and source contracts 24/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSW / 12-S7ZZZP extends retained signature string-pool sweep/remap to `UNION(valueType, baseNameStringOffset, args...)`. Retained MethodSpec signatures now keep the union base-name string in the compacted string pool and rewrite the copied signature payload to the compacted offset. RED was WSL GCC MethodSpec pruning failing `Expected 780 Was 773` because `"Option"` was dropped; GREEN passes the metadata pruning CTest matrix 5/5 on WSL GCC/clang and Windows MSVC Debug, plus TypeDef pruning 2/0 and source contracts 24/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSX / 12-S7ZZZQ tightens writer-level compacted `.zrp` sidecar publication to validate both the final header and definition tables before writing. A direct writer request with a stale sidecar and an invalid definition-table blob now fails closed, removes the stale sidecar, and deletes the generated C artifact. RED was WSL GCC `aot_c_zrp_metadata_publication` failing `Expected FALSE Was TRUE`; GREEN passes WSL GCC/clang and Windows MSVC Debug CTest for publication, CLI sidecar, zrp metadata pruning, MethodSpec pruning, and pool pruning 5/5, plus source contracts 24/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSY / 12-S7ZZZR rejects duplicate generated manifest export `kind + target` declarations before publishing the table. `backend_aot_c_zrp_manifest_export_table_build()` now fails closed when two declarations target the same export key, matching the runtime manifest export view's ambiguity rejection. RED was WSL GCC `zr_vm_aot_c_zrp_metadata_export_token_remap_test` failing `Expected FALSE Was TRUE`; GREEN passes WSL GCC/clang and Windows MSVC Debug CTest for export-token remap, metadata-runtime manifest exports, and zrp metadata pruning 3/3, plus source contracts 24/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZSZ / 12-S7ZZZS rejects duplicate project manifest export `kind + target` declarations during `.zrp` load. `library_project_parse_export_declarations()` now scans parsed export declarations before storing the next entry, so a duplicate `method`/`Widget.run` declaration fails project creation instead of entering the CLI writer bridge. RED was WSL GCC project manifest normalization failing `Expected NULL`; GREEN passes project manifest normalization 29/0 on WSL GCC/clang and Windows MSVC Debug, plus downstream manifest export CTest 2/2 and source contracts 24/0. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZTA / 12-S7ZZZT adds schema parity for duplicate project export declarations by marking the `.zrp` `exports` array with `uniqueItems: true`. This catches fully duplicated export declaration objects in editor/schema validation while the project parser and generated table continue to enforce full `kind + target` uniqueness. RED was the schema assertion failing `AssertionError`; GREEN passes the same assertion and `python -m json.tool` for `zrp.schema.json`. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-03 11-S7ZTB / 12-S7ZZZU closes the remaining CLI writer bridge duplicate export gap for in-memory projects. `ZrCli_Compiler_ApplyProjectAotExportDeclarations()` now rejects duplicate export declaration `kind + target` keys while copying project declarations into writer options, frees the bridge scratch storage on failure, and leaves the manifest export option empty. RED was WSL GCC `test_cli_aot_writer_options_rejects_duplicate_manifest_export_declarations` failing `Expected FALSE Was TRUE`; GREEN passes CLI writer-options 19/0, project manifest normalization 29/0, manifest export/runtime view CTest 2/2, and source contracts 24/0 across WSL GCC/clang and Windows MSVC Debug. Broader metadata sweep policy and trim analyzer integration remain open.
- 2026-07-18 11-S4BO / 08-S6K / 10-S4Z32 adds complete-span signature type-node record binding for attached
  TypeDef/TypeRef and local TypeSpec records. `ZrCore_MetadataRuntime_ResolveSignatureTypeNodeRecord()` is now the
  single direct-node binding source used by TypeSpec/MethodSpec argument views and by token-only constructed-generic
  reflection objects. Nested generic, array, tuple, ownership, nullable, and union arguments materialize recursively;
  unbound nodes fail closed. The binding logic was extracted from the 1036-line metadata runtime into a 98-line focused
  module, leaving the orchestrator at 979 lines. The focused dynamic generic target moved from 15/2 RED to 15/0 GREEN,
  and the three-test GCC/Clang/MSVC matrix passes 3/3. Cross-module TypeSpec canonicalization remains open.

- 2026-07-19 11-S6J / 08-S6T / 10-S4Z41 closes the runtime-consumer side of cross-module TypeSpec canonicalization.
  `ZrCore_Function_BindMatchingTypeSpecMetadata()` remains the binding producer: it matches canonical signature hash plus
  bytes and records requester and provider token identities. Runtime compatibility now recognizes different TypeSpec RIDs
  as a legal mapping only when both metadata tokens are TypeSpec and both paired tokens are Signature; all version,
  module-signature, signature-hash, and layout checks remain in force. The reflection consumer then rereads requester and
  provider TypeSpec records and signature views, requires exact nonempty canonical signature-byte equality, and delegates
  route/layout resolution to the provider metadata runtime. This keeps provider token and layout ownership authoritative,
  rejects malformed `expected*` requester identity and stale same-hash signature bytes, and avoids a process-global TypeSpec registry. The focused matrix passes 4/4,
  GC 66/0, instruction execution 31/0, and instruction table 95/0 under GCC, Clang, and MSVC. Metadata/zrp formats are
  unchanged; automatic provider loading and broader ABI drift/deopt policy remain separate work.

- 2026-07-19 11-S2E / 08-S6U / 10-S4Z42 adds an interpreter MethodDef code-binding view without changing zrp or
  code-registration ABI. The view requires one local MethodDef row and its method record, then consumes the existing
  row `functionIndex` through `ZrCore_Function_ResolveGraphFunctionByFlatIndex()`. That resolver deliberately mirrors
  AOT function-table order: root first, then constant-referenced functions, then child functions recursively, with the
  same pointer/metadata-identity deduplication. Its visited set grows with actual graph nodes rather than the untrusted
  index, so exhausted graphs, `UINT32_MAX - 1`, allocation failure, duplicate rows, and malformed function shapes all
  fail closed. A successful view returns only an instruction-backed non-native VM function. The MethodSpec reflection
  consumer reads the existing underlying method token and delegates execution to the prior context-aware resolved
  function boundary. The focused matrix passes 5/5 and shared GC/instruction regressions pass 66/0, 31/0, and 95/0 on
  GCC, Clang, and MSVC.

- 2026-07-19 11-S5A / 08-S6V / 10-S4Z43 exposes the exact GenericParam owner range already encoded in TypeDef and
  MethodDef rows. `ZrCore_MetadataRuntime_ReadGenericOwnerView()` returns the owner token/record, the matching definition
  row, and `firstGenericParamIndex/genericParamCount`; every failure clears the view. The existing indexed GenericParam
  reader now uses this public source. The reflection consumer requires a nonempty MethodDef range and verifies every
  declared parameter against owner identity, contiguous physical row index, and logical parameter index before creating
  GC-managed method-definition and parameter objects. Names are decoded from the retained zrp string pool, while raw
  token, flags, signature coordinates, constraint range, and metadata-runtime identity remain available on the objects.
  A malformed count, owner, row ordering, token, or missing parameter fails closed. No zrp row, section, or registration
  ABI changes; matching concrete type arguments to a MethodSpec remains a later consumer.

- 2026-07-19 11-S5B / 08-S6W / 10-S4Z44 adds that MethodSpec consumer without introducing another metadata table.
  `ZrCore_Reflection_ResolveConstructedGenericMethod()` first requires the local MethodDef owner range to match the
  requested arity, then scans attached function and module token records in stable order. Every signature candidate is
  re-read through the existing MethodSpec view, checked against the same underlying method record, and matched argument
  by argument. TypeSpec and MethodSpec request resolution now share the same validation and recursive signature-node
  matcher for primitive/direct tokens and all currently encoded compound nodes. Success returns the existing MethodSpec
  and MethodDef records, signature hash, argument coordinates, and borrowed request view; mismatch or malformed metadata
  clears the output. No zrp row, MethodSpec, cache entry, or code-registration slot is synthesized. Constructed method
  object materialization, script `MakeGenericMethod`, and cross-module method binding remain later consumers.

- 2026-07-19 11-S5C / 08-S6X / 10-S4Z45 materializes that exact MethodSpec resolution as a GC-managed constructed
  generic method reflection object. The builder re-resolves and compares the complete carrier before composing the
  existing MethodSpec context and generic MethodDef definition objects. The result exposes the real method name,
  MethodSpec/MethodDef tokens, recursive arguments, signature hash, runtime identity, and a definition link while keeping
  definition/constructed flags consistent. Generic type and method object builders now share one internal field-write and
  temporary GC pinning helper; a focused full-GC assertion verifies the constructed object's argument and definition
  descendants. The three-compiler focused target passes 29/0, focused CTest passes 6/6, and shared regressions pass 66/0,
  31/0, and 95/0. Metadata schema and registration ABI are unchanged; script `MakeGenericMethod` remains a later consumer.

- 2026-07-19 11-S5D / 08-S6Y / 10-S4Z46 adds `ZrCore_Reflection_MakeGenericMethodObject()` as the direct C make
  boundary. It accepts an open MethodDef token plus concrete generic argument descriptors, delegates exact matching to
  the existing MethodSpec resolver, and delegates construction plus carrier revalidation to the S5C object builder.
  Mismatch and invalid input fail closed, and no metadata row, cache entry, or registration slot is synthesized. The
  three-compiler dynamic generic target passes 30/0 and focused metadata/reflection CTest passes 6/6. Script argument
  object decoding and `MakeGenericMethod` native dispatch remain separate work.

- 2026-07-19 11-S5E / 08-S6Z / 10-S4Z47 adds bounded reflection-object decoding before that make boundary.
  `ZrCore_Reflection_MakeGenericMethodFromObjects()` requires a trusted explicit runtime, verifies that the method
  definition carrier agrees with it, and decodes the existing primitive/token/array/tuple/ownership/nullable/union
  object schema into one exact temporary descriptor arena. Kind fields, flags, arity, scalar ranges, array continuity,
  depth 64, and total-node limit 1024 fail closed; every decoded descriptor then passes the shared metadata-aware
  validator before MethodSpec resolution. The three-compiler dynamic generic target passes 31/0, focused CTest 6/6,
  and shared regressions 66/0, 31/0, and 95/0. Native stack dispatch and module export remain separate work.

- 2026-07-19 11-S5F / 08-S6AA / 10-S4Z48 adds the runtime-bound native stack entry above that decoder.
  `ZrCore_Reflection_CreateMakeGenericMethodNativeClosure()` requires a real module-owned runtime, stores the metadata
  module object in one GC-owned closed capture owner, and leaves the direct capture pointer null, so no former stack
  address or unowned runtime pointer survives factory return. After the capture closes successfully, the factory pins
  the module with `NATIVE_HANDLE`, placing it in `OLD_PINNED`/`PINNED` so its embedded runtime and reflection-carrier
  native pointers remain address-stable.
  `ZrCore_Reflection_MakeGenericMethodNativeEntry()` accepts only a method-definition object and argument array, verifies
  its native closure identity and owner-backed module capture, derives the current runtime from that module, then
  delegates to the existing bounded decoder. Invalid capture/frame/object shapes fail closed to one null result with
  normalized stack top. A generational full-GC test verifies the capture keeps the pinned module and runtime live.
  Final dynamic generic tests pass 32/0 under GCC, Clang, and MSVC; final MSVC focused CTest passes 6/6 and shared
  regressions pass 66/0,
  31/0, and 95/0. WSL restarted and cleared the isolated directories before the post-lifetime-fix GCC/Clang wide rerun;
  the same wide matrix passed there before that fix. Metadata schema and registration ABI remain unchanged;
  `zr.reflection` module construction/export and runtime registration lifecycle remain separate work.

- 2026-07-19 08-S6AB / 10-S4Z49 / 11-S5G adds `ZrCore_Reflection_CreateModuleForRuntime()` as the first concrete
  `zr.reflection` module surface. The factory requires a module-owned metadata runtime, creates a READY service module
  whose name and full path are `zr.reflection`, and installs one public `MakeGenericMethod` native closure bound to the
  target metadata module. The service module deliberately has no attached metadata runtime, so service and target
  identities remain distinct.
  Construction temporarily ignores the target module, roots the service module/names/closure on the VM stack, reloads
  managed pointers after export installation, and applies the target `NATIVE_HANDLE` pin only after export and capture
  verification. Failure restores stack and temporary-ignore state. Dynamic generic tests pass 33/0 under GCC, Clang,
  and MSVC; final MSVC focused CTest passes 6/6 and shared regressions pass 66/0, 31/0, and 95/0. Global native
  registry/cache integration, replacement/unload policy, and module generation remain separate work.
