# AOT M2 / 04-S1-S3 Typed I64/U64/F64 Scalar C Lowering With I64 Bitwise/Shift, U64 Binary/Bit-Not Bitwise, Local Copy, Branches, Local Declarations, First Scalar Local Expressions, I64/U64/F64 Binary Direct Sync, Signed Slot Branch Written-Before No-Refresh, Conservative Signed Const Branch Fallback, And Bool Branch Local Predicate

## Scope

- Slice: `04-S1` and `04-S2` from `docs/plans/aot/04-semir-and-c-backend.md`, first typed scalar lowering sub-slice.
- Milestone: M2 scalar pure-C lowering, partial milestone completion only.
- Goal: make a focused typed `i64`, `u64`, and `f64` scalar AOT path emit C operator expressions, direct frame-slot value writes, the first typed scalar local-copy path, the first typed branch path, the first scalar C local declaration skeleton, scalar local multi-kind declarations, and the first signed `i64` arithmetic/compare/bitwise/shift/bit-not/branch plus unsigned `u64`, `f64`, numeric conversion, signed const-branch, and bool branch expressions using declared `sN` / `bN` / `uN` / `fN` locals where proven instead of `ZrCore_Stack_GetValue` plus `ZR_VALUE_FAST_SET`, including the first typed `i64` bitwise/shift, signed `i64`, unsigned `u64` binary bitwise, bit-not, and shift, and `f64` binary direct frame-slot-to-local synchronization, signed branch operand-local, signed slot/slot branch same-block written-before no-refresh predicates, conservative signed const branch fallback when the left operand is not proven written before the branch, bool branch condition-local predicate, scalar stack-copy destination-local mirror, f64 stack-copy generated-product coverage and destination temporary propagation, unsigned compare to bool-local branch generated-product lock, primitive constant scalar-local mirror, scalar destination-local fallback mirror, signed `i64` shift/bit-not destination-local, and conversion source-local slices.

## Completed Items

- Added `tests/parser/test_aot_c_typed_scalar.c`.
- Registered the focused target and CTest entry `aot_c_typed_scalar`.
- Added `backend_aot_c_scalar_semir.{h,c}` as the first SemIR-backed scalar C lowering module.
- Added `backend_aot_c_scalar_conversion.{h,c}` for the focused typed numeric conversion lowering path.
- Added `backend_aot_c_scalar_bitwise.{h,c}` for the focused typed bitwise and shift lowering path.
- Added `backend_aot_c_scalar_stack_copy.{h,c}` for the focused typed scalar `GET_STACK` / `SET_STACK` local-copy lowering path.
- Added `backend_aot_c_scalar_locals.{h,c}` for the first typed scalar C local declaration skeleton.
- Updated `backend_aot_c_lowering_control.c` so typed bool branches and fused signed branches read direct frame slots instead of branch-specific `ZrCore_Stack_GetValue` / typed-place fallback code.
- Updated `backend_aot_c_function_body.c` to emit scalar local declarations immediately after generated frame setup and before value SemIR / bytecode dispatch lowering.
- Updated `backend_aot_c_scalar_semir.c` so SemIR-backed signed `i64` binary arithmetic can use declared `zr_aot_sN` locals when destination and operands all have i64 scalar-local coverage.
- Updated the SemIR-backed signed `i64` binary scalar-local template so proven local operands now synchronize directly from `frame.slotBase[left/right].value.value.nativeObject.nativeInt64` into `zr_aot_sN`, without the intermediate `zr_aot_s_left` / `zr_aot_s_right` assignments on that proven-local path.
- Updated the SemIR-backed unsigned `u64` binary scalar-local template so proven local operands now synchronize directly from `frame.slotBase[left/right].value.value.nativeObject.nativeUInt64` into `zr_aot_uN`, without the intermediate `zr_aot_u_left` / `zr_aot_u_right` assignments on that proven-local path.
- Updated the SemIR-backed `f64` binary scalar-local template so proven local operands now synchronize directly from `frame.slotBase[left/right].value.value.nativeObject.nativeDouble` into `zr_aot_fN`, without the intermediate `zr_aot_f_left` / `zr_aot_f_right` assignments on that proven-local path.
- Updated `backend_aot_c_scalar_locals.{h,c}` with `backend_aot_c_scalar_locals_has_bool_slot` and a shared scalar-local kind query helper.
- Updated `backend_aot_c_scalar_locals.{h,c}` with `backend_aot_c_scalar_locals_has_f64_slot`.
- Updated `backend_aot_c_scalar_locals.{h,c}` with `backend_aot_c_scalar_locals_has_u64_slot`.
- Updated `backend_aot_c_scalar_semir.c` so SemIR-backed signed `i64` comparisons can use declared `zr_aot_sN` inputs and `zr_aot_bN` bool destinations when the compare destination and operands all have scalar-local coverage.
- Updated `backend_aot_c_scalar_semir.c` so SemIR-backed unsigned `u64` binary arithmetic can use declared `zr_aot_uN` locals when destination and operands all have u64 scalar-local coverage.
- Updated `backend_aot_c_scalar_semir.c` so SemIR-backed unsigned `u64` comparisons can compute from declared `zr_aot_uN` operands when both operands have scalar-local coverage, and can additionally write `zr_aot_bDst` when the destination also has bool scalar-local coverage.
- Updated `backend_aot_c_scalar_semir.c` so SemIR-backed `f64` binary arithmetic can use declared `zr_aot_fN` locals when destination and operands all have f64 scalar-local coverage.
- Updated `backend_aot_c_scalar_bitwise.{h,c}` so SemIR-backed signed `i64` binary bitwise operations can use declared `zr_aot_sN` locals when destination and operands all have i64 scalar-local coverage.
- Updated `backend_aot_c_scalar_bitwise.c` so SemIR-backed signed `i64` shift operations can compute from declared `zr_aot_sN` left/shift operands when both operands have i64 scalar-local coverage, and can additionally write `zr_aot_sDst` when the destination also has i64 scalar-local coverage.
- Updated `backend_aot_c_scalar_bitwise.c` so SemIR-backed signed `i64` bit-not can compute from a declared `zr_aot_sN` source when the source has i64 scalar-local coverage, and can additionally write `zr_aot_sDst` when the destination also has i64 scalar-local coverage.
- Updated the generic interpreter bitwise path so unsigned `BITWISE_AND`, `BITWISE_OR`, `BITWISE_XOR`, and `BITWISE_NOT` preserve `ZR_VALUE_TYPE_UINT64` results instead of returning signed `INT64` values for unsigned operands.
- Added `tests/core/test_execution_unsigned_bitwise.c` and registered `zr_vm_execution_unsigned_bitwise_test` to lock the unsigned bitwise interpreter result-type behavior below the AOT generated-product layer.
- Restored the executable typed scalar golden coverage for `var unsignedMasked: uint = unsignedSum & unsignedRight;`, requiring `zr_aot_scalar_exec_u64_bitwise` and the direct `zr_aot_u12 = zr_aot_u8 & zr_aot_u7;` generated-product shape.
- Added executable typed scalar golden coverage for `var unsignedInverted: uint = ~unsignedRight;`, requiring `zr_aot_scalar_exec_u64_bit_not` and the direct `zr_aot_u33 = ~zr_aot_u7;` current generated-product shape after the unsigned shift temporaries.
- Added executable typed scalar golden coverage for `var unsignedShifted: uint = unsignedSum << right;` and `var unsignedShiftedBack: uint = unsignedShifted >> right;`, requiring `zr_aot_scalar_exec_u64_shift`, direct `zr_aot_u13 = zr_aot_u8 << zr_aot_s1;`, and direct `zr_aot_u14 = zr_aot_u10 >> zr_aot_s1;` generated-product shapes.
- Updated `backend_aot_c_scalar_conversion.{h,c}` so focused `TO_FLOAT` conversions receive `functionIr`, query scalar locals, and compute from declared `zr_aot_sN` or `zr_aot_uN` sources when source slots have scalar-local coverage.
- Updated `backend_aot_c_scalar_conversion.c` so focused `TO_INT_FLOAT` can compute from a declared `zr_aot_fN` source when the source slot has scalar-local coverage.
- Updated `backend_aot_c_scalar_conversion.c` so focused `TO_INT_UNSIGNED` can compute from a declared `zr_aot_uN` source when the source slot has scalar-local coverage while preserving the existing `u64 -> i64` range-folding semantics.
- Updated `backend_aot_c_scalar_conversion.c` so focused `TO_UINT` can compute from a declared `zr_aot_uN` source when the source slot has scalar-local coverage and can mirror into a declared `u64` destination local when the destination is distinct and proven local.
- Updated `backend_aot_c_scalar_conversion.c` so focused `TO_UINT_SIGNED` and `TO_UINT_FLOAT` can compute from declared `zr_aot_sN` / `zr_aot_fN` sources when source slots have scalar-local coverage.
- Updated `backend_aot_c_lowering_control.c`, `backend_aot_c_emitter.h`, and `backend_aot_c_function_body.c` so fused signed branch emitters receive `functionIr`, query `i64` scalar-local coverage for both operands, synchronize frame-slot operands into `zr_aot_sN`, and emit `if (zr_aot_sL op zr_aot_sR) { goto ...; }` when both operands are proven signed scalar locals.
- Updated the fused signed slot/slot branch scalar-local template so proven local operands now synchronize directly from `zr_aot_left->value.nativeObject.nativeInt64` and `zr_aot_right->value.nativeObject.nativeInt64` into `zr_aot_sN`, without the intermediate `zr_aot_left_scalar` / `zr_aot_right_scalar` assignments on that path.
- Updated `backend_aot_c_lowering_control.c`, `backend_aot_c_emitter.h`, and `backend_aot_c_function_body.c` so `JUMP_IF_NOT_EQUAL_SIGNED_CONST` also receives `functionIr`, queries `i64` scalar-local coverage for the left operand, synchronizes the frame slot into `zr_aot_sN`, and emits `if (zr_aot_sN != zr_aot_right_literal) { goto ...; }` when the left operand is a proven signed scalar local.
- Updated `backend_aot_c_lowering_control.c`, `backend_aot_c_emitter.h`, and `backend_aot_c_function_body.c` so `JUMP_IF_BOOL_FALSE` receives `functionIr`, queries bool scalar-local coverage for the condition slot, synchronizes the frame slot into `zr_aot_bN`, and emits `if (!zr_aot_bN) { goto ...; }` when the condition is a proven bool scalar local.
- Updated the proven bool scalar-local branch template again after scalar stack-copy destination-local mirroring: it now emits `zr_aot_jump_if_bool_false_scalar_local` and branches directly on the maintained `zr_aot_bN` local instead of refreshing that proven path from `frame.slotBase`.
- Lowered SemIR-backed `i64` `ADD`, `SUB`, `MUL`, `DIV`, and `MOD` to C operators in the generated AOT C body.
- Lowered SemIR-backed signed `i64` `EQ`, `NE`, `LT`, `LE`, `GT`, and `GE` to C comparison operators in the generated AOT C body.
- Lowered SemIR-backed unsigned `u64` `ADD`, `SUB`, `MUL`, `DIV`, and `MOD` to C operators in the generated AOT C body.
- Lowered SemIR-backed unsigned `u64` `EQ`, `NE`, `LT`, `LE`, `GT`, and `GE` to C comparison operators in the generated AOT C body.
- Lowered SemIR-backed `f64` `ADD`, `SUB`, `MUL`, `DIV`, and `MOD` to C operators or `fmod` in the generated AOT C body.
- Lowered focused numeric conversions for `TO_INT`, `TO_UINT`, and `TO_FLOAT` specialized variants to direct frame-slot reads and direct scalar writes.
- Lowered focused SemIR-backed signed `i64` `BIT_NOT`, `BIT_AND`, `BIT_OR`, `BIT_XOR`, `SHL`, and `SHR` to C `~`, `&`, `|`, `^`, `<<`, and `>>` in the generated AOT C body.
- Lowered focused typed scalar local copies for bool, signed `i64`, unsigned `u64`, and `f64` to direct frame-slot scalar reads/writes before the old generic stack-copy fallback.
- Updated the focused scalar stack-copy direct path so bool, signed `i64`, unsigned `u64`, and `f64` copies also mirror into declared destination locals when scalar-local coverage proves `bN` / `sN` / `uN` / `fN` exists; the focused `branchFlag` copy now writes `zr_aot_b5 = (TZrBool)(zr_aot_b_value != 0u);` before the false branch consumes that local.
- Updated the same scalar stack-copy path so proven source locals feed the copy payload directly from `bN` / `sN` / `uN` / `fN`; the focused `branchFlag` copy now reads `zr_aot_b_value = zr_aot_b7;` instead of re-reading the source frame slot payload.
- Added focused typed scalar source coverage for `var floatCopy: float = floatLeft;`, so the executable generated product now includes f64 scalar stack-copy markers and locks both a source-local f64 read and a destination-local f64 mirror.
- Updated `backend_aot_c_scalar_locals.c` so proven scalar-local kinds propagate across `GET_STACK` / `SET_STACK` destinations; the focused f64 copy chain now declares and uses the intermediate `zr_aot_f40` local instead of re-reading the frame source payload on the second copy.
- Added generated-product assertions for the focused `if (unsignedInverted <= unsignedRight)` path. The checked product has no independent unsigned/f64 branch helper; it lowers to `zr_aot_scalar_exec_u64_compare`, writes `zr_aot_b23`, and then consumes that bool local through `zr_aot_jump_if_bool_false_scalar_local`.
- Hardened the scalar-local source contract so stack-copy destination propagation must keep the explicit `backend_aot_c_scalar_locals_instruction_is_stack_copy(` helper, read `sourceKind = slotKinds[sourceSlot];`, and record the destination with that source kind.
- Updated `backend_aot_write_c_direct_primitive_constant()` so immediate bool/i64/u64/f64 constants receive `functionIr`, query scalar-local destination coverage, and mirror supported constant payloads into `zr_aot_bN` / `zr_aot_sN` / `zr_aot_uN` / `zr_aot_fN` after writing the existing frame slot.
- Added focused generated-product assertions for primitive constant local mirrors, including `zr_aot_s0 = (TZrInt64)21;`, `zr_aot_s1 = (TZrInt64)2;`, `zr_aot_s4 = (TZrInt64)40;`, `zr_aot_s6 = (TZrInt64)9;`, `zr_aot_f19 = (TZrFloat64)1.5;`, and `zr_aot_f20 = (TZrFloat64)2;`.
- Updated scalar binary, compare, bitwise, bit-not, and shift fallback templates so when operands cannot all be consumed from locals but the destination local is proven, the already computed `zr_aot_*_result` is mirrored into `zr_aot_sN` / `zr_aot_uN` / `zr_aot_fN` / `zr_aot_bN` before the existing frame-slot result write.
- Updated `backend_aot_c_scalar_locals.{h,c}` with `backend_aot_c_scalar_locals_i64_written_before()`, a conservative same-basic-block proof that only marks `i64` locals written by earlier SemIR destinations, immediate constants, conversions, and stack-copy propagation before the current branch instruction.
- Updated `backend_aot_c_lowering_control.c`, `backend_aot_c_emitter.h`, and `backend_aot_c_function_body.c` so signed slot/slot branch emitters receive the current instruction index, use written-before proof for both operands, and on the proven path branch directly with `if (zr_aot_sL op zr_aot_sR) {` without refreshing `sN` from `zr_aot_left/right->value.nativeObject.nativeInt64` inside the branch block.
- Updated the focused signed const-branch path to use the same written-before check; because the current `product == 42` generated shape does not prove the left operand written in the same block before that branch, it deliberately falls back to `zr_aot_left_scalar != zr_aot_right_literal` instead of using a stale `zr_aot_s2` predicate.
- Lowered focused typed branch helpers for bool false and signed `i64` fused comparisons to direct frame-slot reads plus C `goto`.
- Emitted generated C local declarations for typed scalar bool, signed `i64`, unsigned `u64`, and `f64` slots. Scalar local kind tracking is now a bitmask per frame slot, so `typedLocalBindings` and SemIR destination metadata merge as per-kind flags instead of letting one source erase another when a temporary reuses the same slot across lifetimes.
- Updated `backend_aot_c_scalar_locals.c` so reused slots can declare multiple typed C mirrors such as `zr_aot_s16` and `zr_aot_f16`, or `zr_aot_s17` and `zr_aot_f17`, when typed locals and SemIR temporaries share a frame slot.
- Emitted the first signed `i64` local-expression template, for example `zr_aot_s2 = zr_aot_s0 * zr_aot_s1;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first signed `i64` binary direct frame-slot-to-`sN` synchronization template, for example `zr_aot_s0 = frame.slotBase[0].value.value.nativeObject.nativeInt64;` and `zr_aot_s1 = frame.slotBase[1].value.value.nativeObject.nativeInt64;`, before the `sN` arithmetic expression.
- Emitted the first unsigned `u64` binary direct frame-slot-to-`uN` synchronization template, for example `zr_aot_u6 = frame.slotBase[6].value.value.nativeObject.nativeUInt64;` and `zr_aot_u7 = frame.slotBase[7].value.value.nativeObject.nativeUInt64;`, before the `uN` arithmetic expression.
- Emitted the first `f64` binary direct frame-slot-to-`fN` synchronization template, for example `zr_aot_f17 = frame.slotBase[17].value.value.nativeObject.nativeDouble;` and `zr_aot_f18 = frame.slotBase[18].value.value.nativeObject.nativeDouble;`, before the `fN` arithmetic expression.
- Emitted the first signed `i64` compare local-expression template, for example `zr_aot_b25 = (TZrBool)(zr_aot_s2 > zr_aot_s4);`, while still mirroring the bool result back to the existing frame slot storage.
- Emitted the first unsigned `u64` local-expression template, for example `zr_aot_u8 = zr_aot_u6 + zr_aot_u7;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first unsigned `u64` compare local-operand template, for example `zr_aot_b12 = (TZrBool)(zr_aot_u8 > zr_aot_u7);`, while still mirroring the bool result back to the existing frame slot storage.
- Emitted the first `f64` local-expression template, for example `zr_aot_f30 = zr_aot_f17 * zr_aot_f18;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first signed `i64` binary bitwise local-expression template, for example `zr_aot_s14 = zr_aot_s10 & zr_aot_s0;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first unsigned `u64` binary bitwise local-expression template, for example `zr_aot_u12 = zr_aot_u8 & zr_aot_u7;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first unsigned `u64` bit-not local-expression template, for example `zr_aot_u33 = ~zr_aot_u7;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first signed `i64` shift destination-local expression templates, for example `zr_aot_s17 = (TZrInt64)((TZrUInt64)zr_aot_s13 << zr_aot_s1);` and `zr_aot_s18 = zr_aot_s14 >> zr_aot_s1;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first signed `i64` bit-not destination-local expression template, for example `zr_aot_s17 = ~zr_aot_s1;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first conversion local-source expression template, for example `zr_aot_f_result = (TZrFloat64)zr_aot_s2;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first float-to-int conversion local-source expression template, for example `zr_aot_s_result = (TZrInt64)zr_aot_f17;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first unsigned-to-int conversion local-source expression template, for example `zr_aot_s_result = (TZrInt64)zr_aot_u8;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first unsigned-to-uint conversion local-source expression template, for example `zr_aot_u_result = zr_aot_u6;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first signed/float-to-uint conversion local-source expression templates, for example `zr_aot_u_result = (TZrUInt64)zr_aot_s2;` and `zr_aot_u_result = (TZrUInt64)zr_aot_f17;`, while still mirroring the result back to the existing frame slot storage.
- Updated `backend_aot_c_scalar_locals.c` so focused typed numeric conversion bytecode destinations contribute scalar-local kinds from `SZrFunction::instructionsList`, allowing the reused conversion temporary slot to declare `zr_aot_s29`, `zr_aot_u29`, and `zr_aot_f29` after the focused `unsignedInverted` temporary.
- Emitted the first conversion destination-local expression templates, for example `zr_aot_f29 = (TZrFloat64)zr_aot_s2;`, `zr_aot_s29 = (TZrInt64)zr_aot_f17;`, `zr_aot_s29 = (TZrInt64)zr_aot_u8;`, `zr_aot_u29 = (TZrUInt64)zr_aot_s2;`, and `zr_aot_u29 = (TZrUInt64)zr_aot_f17;`, while still mirroring the result back to the existing frame slot storage.
- Emitted the first fused signed branch local-operand expression template, for example `if (zr_aot_s2 <= zr_aot_s4) {`, while still validating the frame-slot input values before synchronizing them into the declared `sN` locals.
- Emitted the first fused signed slot/slot branch direct `sN` synchronization template, for example `zr_aot_s2 = zr_aot_left->value.nativeObject.nativeInt64;` and `zr_aot_s4 = zr_aot_right->value.nativeObject.nativeInt64;`, before the `sN` predicate.
- Emitted the first fused signed const-branch left-local predicate template, for example `if (zr_aot_s2 != zr_aot_right_literal) {`, while still validating the frame-slot input value before synchronizing it into the declared `sN` local.
- Emitted the first bool branch condition-local predicate template, for example `if (!zr_aot_b5) {`; proven bool-local branches now consume the maintained `bN` local directly, while unproven fallback branches still validate the frame-slot bool value and use `zr_aot_condition_bool`.
- Added generated C guards for frame-slot bounds, signed-int/unsigned-int/float type checks, and divide/modulo by zero before direct C arithmetic.
- Added generated C guards for bitwise operand type checks and shift-count range checks before direct C shifts.
- Reworked frame reset, primitive constants, `RESET_STACK_NULL`, `RESET_STACK_NULL2`, integer arithmetic result writes, bool comparison result writes, float result writes, typed scalar local copies, and direct return helper emission so the typed scalar benchmark path no longer emits `ZrCore_Stack_GetValue(` or `ZR_VALUE_FAST_SET(`.

## RED / GREEN Evidence

- RED: the new focused generated-product test failed before implementation because the generated typed scalar AOT C still contained forbidden runtime token `ZrCore_Stack_GetValue(`.
- GREEN: after adding the SemIR scalar helper and direct frame-slot writes, the generated product scan found zero forbidden tokens and the generated shared library result matched the interpreter result.
- RED: after adding `product > threshold`, the comparison path exposed the old lowering again because the generated product contained forbidden runtime token `ZrCore_Stack_GetValue(`.
- GREEN: after adding SemIR-backed signed `i64` comparison lowering and direct bool result writes, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after adding `floatLeft * floatRight`, the float arithmetic path exposed the old lowering again because the generated product contained forbidden runtime token `ZrCore_Stack_GetValue(`.
- GREEN: after adding SemIR-backed `f64` binary arithmetic lowering and direct double result writes, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after adding typed `uint` locals, `unsignedLeft + unsignedRight`, and `unsignedSum > unsignedRight`, the generated product first exposed the old generic `TO_UINT` conversion lowering with `ZrCore_Stack_GetValue(` and `ZR_VALUE_FAST_SET(`.
- GREEN: after adding direct `TO_UINT`/`TO_UINT_SIGNED`/`TO_UINT_FLOAT` `u64` conversion writes plus SemIR-backed `u64` binary arithmetic/comparison lowering, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after adding `<float> product`, `<float> unsignedSum`, `<int> floatLeft`, and `<int> unsignedSum`, the generated product exposed the old typed numeric conversion lowering with `ZrCore_Stack_GetValue(` and `ZR_VALUE_FAST_SET(`.
- GREEN: after adding direct `TO_INT` and `TO_FLOAT` conversion writes alongside the existing direct `TO_UINT` path, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after extending the typed SemIR guardrail with bitwise and shift expressions, compilation failed because `BIT_NOT`, `BIT_AND`, `BIT_OR`, `BIT_XOR`, `SHL`, and `SHR` SemIR opcodes were not declared yet.
- GREEN: after adding those opcodes, SemIR mapping, and ExecIR names, the focused SemIR guardrail passed and observed typed static C metadata for the new rows.
- RED: after adding typed `int` bitwise locals to the generated-product test, the product exposed the still-open scalar local copy path through `zr_aot_direct_stack_copy` and `ZrCore_Stack_GetValue(`.
- GREEN: after narrowing the focused product case to standalone bitwise/shift expressions and adding `backend_aot_c_scalar_bitwise`, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after restoring the typed `int` bitwise/shift local chain and feeding those locals into the return value, the generated product again exposed `zr_aot_direct_stack_copy` with `ZrCore_Stack_GetValue(`.
- GREEN: after adding `backend_aot_c_scalar_stack_copy` and dispatching it before the generic stack-copy fallback, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after adding typed `if (product > threshold)` and a typed bool local branch to the generated-product test, the product exposed branch helpers that still used `ZrCore_Stack_GetValue(`.
- GREEN: after updating `backend_aot_c_lowering_control.c` to read `&frame.slotBase[slot].value` for typed bool and signed branches, the generated product scan stayed at zero forbidden tokens and the generated shared library result still matched the interpreter result.
- RED: after adding generated-product assertions for scalar C local declarations, the focused typed scalar test failed because generated C did not contain `zr_aot_scalar_locals_begin`.
- GREEN: after adding `backend_aot_c_scalar_locals.{h,c}` and calling it from the generated function body, generated C contained the scalar local declaration block and the shared library result still matched the interpreter result.
- RED: after tightening the assertions to specific typed local declarations such as `TZrUInt64 zr_aot_u6 = (TZrUInt64)0u;`, the focused typed scalar test failed because SemIR destination metadata could mark a typed local slot as conflicting.
- GREEN: after preserving `typedLocalBindings` as the first source of truth for local declarations, the generated product contained concrete `s0`, `u6`, `f16`, and `b5` declarations, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_s2 = zr_aot_s0 * zr_aot_s1;`, the focused typed scalar test failed because signed `i64` arithmetic still computed through temporary helper variables only.
- GREEN: after adding `backend_aot_c_scalar_locals_has_i64_slot` and using it in the signed `i64` binary arithmetic template, the generated product contained the `sN` expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after tightening the signed `i64` binary generated-product assertions to require direct frame-slot-to-`sN` synchronization, the focused typed scalar test failed because the proven local path still emitted `zr_aot_s0 = zr_aot_s_left;`.
- GREEN: after changing the proven local signed `i64` binary path to synchronize directly from `frame.slotBase[0/1].value.value.nativeObject.nativeInt64` into `zr_aot_s0` / `zr_aot_s1`, the generated product contains the direct sync markers, no longer contains `zr_aot_s0 = zr_aot_s_left;`, and typed scalar plus source-contract focused runs pass.
- RED: after adding a generated-product assertion that signed `i64` compare should write a bool C local expression, the focused typed scalar test failed because the compare path still computed through temporary helper variables only.
- GREEN: after adding `backend_aot_c_scalar_locals_has_bool_slot` and using it with the existing i64 local query in the signed `i64` compare template, the generated product contained the `bN` compare expression, currently `zr_aot_b25 = (TZrBool)(zr_aot_s2 > zr_aot_s4);`, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_u8 = zr_aot_u6 + zr_aot_u7;`, the focused typed scalar test failed because unsigned `u64` arithmetic still computed through temporary helper variables only.
- GREEN: after adding `backend_aot_c_scalar_locals_has_u64_slot` and using it in the unsigned `u64` binary arithmetic template, the generated product contained the `uN` expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after tightening the unsigned `u64` binary generated-product assertions to require direct frame-slot-to-`uN` synchronization, the focused typed scalar test failed because the proven local path still emitted `zr_aot_u6 = zr_aot_u_left;`.
- GREEN: after changing the proven local unsigned `u64` binary path to synchronize directly from `frame.slotBase[6/7].value.value.nativeObject.nativeUInt64` into `zr_aot_u6` / `zr_aot_u7`, the generated product contains the direct sync markers, no longer contains `zr_aot_u6 = zr_aot_u_left;`, and typed scalar plus source-contract focused runs pass.
- RED: after adding a generated-product assertion for the `f64` local expression, now `zr_aot_f29 = zr_aot_f17 * zr_aot_f18;`, the focused typed scalar test failed because `f64` arithmetic still computed through temporary helper variables only.
- GREEN: after adding `backend_aot_c_scalar_locals_has_f64_slot` and using it in the `f64` binary arithmetic template, the generated product contained the `fN` expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after tightening the `f64` binary generated-product assertions to require direct frame-slot-to-`fN` synchronization, the focused typed scalar test failed because the proven local path still emitted `zr_aot_fN = zr_aot_f_left;`.
- GREEN: after changing the proven local `f64` binary path to synchronize directly from `frame.slotBase[17/18].value.value.nativeObject.nativeDouble` into `zr_aot_f17` / `zr_aot_f18`, the generated product contains the direct sync markers, no longer contains `zr_aot_f17 = zr_aot_f_left;`, and typed scalar plus source-contract focused runs pass.
- RED: after adding a generated-product assertion for `zr_aot_s14 = zr_aot_s10 & zr_aot_s0;`, the focused typed scalar test failed because signed `i64` bitwise still computed through temporary helper variables only.
- GREEN: after passing `functionIr` into `backend_aot_c_scalar_bitwise` and using `backend_aot_c_scalar_locals_has_i64_slot` in the binary bitwise template, the generated product contained the `sN` bitwise expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_s_result = (TZrInt64)((TZrUInt64)zr_aot_s12 << zr_aot_s1);`, the focused typed scalar test failed because signed `i64` shifts still computed from helper temporaries only.
- GREEN: after passing `functionIr` into the signed shift lowering and splitting operand-local from destination-local eligibility, the generated product contained the `sN` shift operand expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_s_result = ~zr_aot_s1;`, the focused typed scalar test failed because signed `i64` bit-not still computed from the helper source variable only.
- GREEN: after splitting source-local from destination-local eligibility in the signed bit-not lowering, the generated product contained the `sN` bit-not source expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_f_result = (TZrFloat64)zr_aot_s2;`, the focused typed scalar test failed because `TO_FLOAT_SIGNED` still converted from the helper source value only.
- GREEN: after passing `functionIr` into scalar conversion and using scalar-local source queries, the generated product contained the `sN` conversion source expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_s_result = (TZrInt64)zr_aot_f17;`, the focused typed scalar test failed because `TO_INT_FLOAT` still converted from the helper source value only.
- GREEN: after adding `f64` source-local handling to the `TO_INT_FLOAT` scalar conversion path, the generated product contained the `fN` conversion source expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_s_result = (TZrInt64)zr_aot_u8;`, the focused typed scalar test failed because `TO_INT_UNSIGNED` still converted from the helper `u_source` value only.
- GREEN: after adding `u64` source-local handling to the `TO_INT_UNSIGNED` scalar conversion path, the generated product contained the `uN` unsigned-to-int conversion source expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_u_result = zr_aot_u6;`, the focused typed scalar test failed because `TO_UINT` still copied from `zr_aot_source->value.nativeObject.nativeUInt64` only.
- GREEN: after adding `u64` source-local handling to the `TO_UINT` scalar conversion path, the generated product contained the `uN` unsigned-to-uint conversion source expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding generated-product assertions for `zr_aot_u_result = (TZrUInt64)zr_aot_s2;` and `zr_aot_u_result = (TZrUInt64)zr_aot_f17;`, the focused typed scalar test failed because `TO_UINT_SIGNED` and `TO_UINT_FLOAT` still converted from frame-slot source values only.
- GREEN: after adding `i64` / `f64` source-local handling to the unsigned scalar conversion path, the generated product contained both `sN/fN -> u64` conversion source expressions, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `zr_aot_b12 = (TZrBool)(zr_aot_u8 > zr_aot_u7);`, the focused typed scalar test failed because unsigned `u64` compare still computed from helper operands only.
- GREEN: after adding `u64` operand-local handling to the unsigned compare path, the generated product contained the `uN` compare operand expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after adding a generated-product assertion for `if (zr_aot_s2 <= zr_aot_s4) {`, the focused typed scalar test failed because fused signed branch lowering still branched on `zr_aot_left_scalar <= zr_aot_right_scalar`.
- GREEN: after passing `functionIr` into fused signed branch emitters and querying `i64` scalar-local coverage for both operands, the generated product contained the `sN` branch operand expression, the forbidden-token scan stayed at zero, and the shared library result still matched the interpreter result.
- RED: after tightening the signed slot/slot branch assertions to require direct `sN` synchronization, the focused typed scalar test failed because the proven local branch path still emitted `zr_aot_s2 = zr_aot_left_scalar;` / `zr_aot_s4 = zr_aot_right_scalar;`.
- GREEN: after changing the proven local branch path to assign from `zr_aot_left->value.nativeObject.nativeInt64` and `zr_aot_right->value.nativeObject.nativeInt64` directly into `zr_aot_s2` / `zr_aot_s4`, the generated product contains the direct sync markers and no longer contains the old `sN = *_scalar` assignments; typed scalar plus source-contract focused runs pass.
- RED: after adding generated-product assertions for multi-kind scalar locals and signed `i64` shift/bit-not destination locals such as `TZrInt64 zr_aot_s17 = (TZrInt64)0;` and `zr_aot_s17 = (TZrInt64)((TZrUInt64)zr_aot_s13 << zr_aot_s1);`, the focused typed scalar test failed because reused slots still declared only one scalar local kind.
- GREEN: after switching scalar local kind tracking to per-slot bit flags and emitting every flagged declaration, the generated product contains reused `sN` / `fN` declarations plus `sN` shift/bit-not destination expressions; typed scalar and source-contract focused runs pass.
- RED: after adding generated-product assertions for conversion destination locals such as `TZrInt64 zr_aot_s28 = (TZrInt64)0;`, `TZrUInt64 zr_aot_u28 = (TZrUInt64)0u;`, `TZrFloat64 zr_aot_f28 = 0.0;`, and `zr_aot_f28 = (TZrFloat64)zr_aot_s2;`, the focused typed scalar test failed because conversion destination slots were not declared as scalar locals.
- GREEN: after scanning focused typed numeric conversion bytecode destination slots in `backend_aot_c_scalar_locals.c`, the generated product contains `s28` / `u28` / `f28` declarations and destination-local conversion expressions; typed scalar and source-contract focused runs pass.
- RED: after restoring executable `u64` bitwise to the typed scalar golden with `var unsignedMasked: uint = unsignedSum & unsignedRight;`, the lower interpreter path returned signed `INT64` for unsigned bitwise results and the focused test could not validate the generated AOT product end to end.
- GREEN: after adding `tests/core/test_execution_unsigned_bitwise.c` and fixing generic interpreter `BITWISE_AND` / `BITWISE_OR` / `BITWISE_XOR` / `BITWISE_NOT` to preserve unsigned result type, the typed scalar generated product contains `zr_aot_scalar_exec_u64_bitwise` and `zr_aot_u12 = zr_aot_u8 & zr_aot_u7;`; the focused typed scalar run passes with the patched core archive.
- RED: after adding executable `u64` bit-not coverage with `var unsignedInverted: uint = ~unsignedRight;`, the focused generated product first exposed the old generic bitwise lowering (`zr_aot_bitwise_exec_unary`) and forbidden `ZrCore_Stack_GetValue(` tokens when the temporary parser archive still carried stale scalar backend / SEMIR objects.
- GREEN: after synchronizing the current scalar AOT backend objects and `compiler_semir.c` into the temporary parser archive, the typed scalar generated product contains `zr_aot_scalar_exec_u64_bit_not` and, after the later unsigned shift temporaries, the current `zr_aot_u33 = ~zr_aot_u7;` shape; the forbidden-token scan stays at zero, and the generated shared library result still matches the interpreter result.
- RED: after adding executable `u64` shift coverage with `var unsignedShifted: uint = unsignedSum << right;` and `var unsignedShiftedBack: uint = unsignedShifted >> right;`, the focused typed scalar test first aborted in the interpreter because the compiler still emitted `SHIFT_LEFT_INT` after converting the shift count to `uint`, violating the signed-int shift-count contract.
- GREEN: after keeping typed shift operands unconverted in `compile_expression.c` and choosing `BITWISE_SHIFT_LEFT` / `BITWISE_SHIFT_RIGHT` for unsigned typed shifts, the generated product contains `zr_aot_scalar_exec_u64_shift`, `zr_aot_u13 = zr_aot_u8 << zr_aot_s1;`, and `zr_aot_u14 = zr_aot_u10 >> zr_aot_s1;`; the forbidden-token scan stays at zero and the generated shared library result still matches the interpreter result.
- RED: after adding generated-product assertions for the `if (product == 42)` source case, the focused typed scalar test failed while the direct `JUMP_IF_NOT_EQUAL_SIGNED_CONST` path still branched on `zr_aot_left_scalar != zr_aot_right_literal`.
- GREEN: after passing `functionIr` into the signed const-branch emitter and using the proven `i64` scalar-local left operand, the generated product contains `zr_aot_s2 = zr_aot_left->value.nativeObject.nativeInt64;` followed by `if (zr_aot_s2 != zr_aot_right_literal) {`; the old `zr_aot_left_scalar != zr_aot_right_literal` predicate is absent, and typed scalar plus source-contract focused runs pass.
- RED: before the bool branch local-predicate change, generated-product scanning showed the focused `branchFlag` path still emitted `if (!zr_aot_condition_bool) {` and did not emit `if (!zr_aot_b5) {`.
- GREEN: after passing `functionIr` into the bool branch emitter and using the proven bool scalar-local condition slot, the generated product contains `zr_aot_b5 = (TZrBool)(zr_aot_condition->value.nativeObject.nativeBool != 0u);` followed by `if (!zr_aot_b5) {`; the old `if (!zr_aot_condition_bool) {` predicate is absent, and typed scalar plus source-contract focused runs pass.
- RED: after tightening the scalar stack-copy mirror contract for the focused `branchFlag` path, the previous direct bool copy shape only wrote `frame.slotBase[5]` and left the declared `zr_aot_b5` local stale until a later branch refresh.
- GREEN: after passing destination-local coverage into `backend_aot_c_scalar_stack_copy.c`, bool/i64/u64/f64 scalar copies mirror the copied value into `bN` / `sN` / `uN` / `fN`; the generated product contains `zr_aot_b5 = (TZrBool)(zr_aot_b_value != 0u);`, keeps `if (!zr_aot_b5) {`, and source contracts now pass with 19 tests.
- RED: after tightening the bool branch generated-product/source contract to require a proven-local no-refresh path, the source-contract run failed on missing `zr_aot_jump_if_bool_false_scalar_local` and the generated product still refreshed `zr_aot_b5` from `zr_aot_condition->value.nativeObject.nativeBool`.
- GREEN: after routing proven bool scalar-local branches directly through the maintained `zr_aot_bN`, source contracts pass with 19 tests and the generated product contains `zr_aot_jump_if_bool_false_scalar_local` followed by `if (!zr_aot_b5) {`, while the old `zr_aot_b5 = (TZrBool)(zr_aot_condition->value.nativeObject.nativeBool != 0u);` marker is absent.
- RED: after tightening scalar stack-copy source-local coverage, the source-contract run failed on missing source-slot scalar-local queries and the focused generated product still copied bool payloads with `zr_aot_b_value = zr_aot_source->value.nativeObject.nativeBool;`.
- GREEN: after adding source-local handling to bool/i64/u64/f64 scalar stack-copy, source contracts pass with 19 tests and the generated product contains `zr_aot_b_value = zr_aot_b7;` followed by `zr_aot_b5 = (TZrBool)(zr_aot_b_value != 0u);`; the old bool source frame-payload read is absent from the focused generated-product assertion.
- RED: before the i64/u64 stack-copy generated-product coverage tightening, the executable typed scalar test only locked the bool `branchFlag` source/destination local copy shape even though source contracts covered bool/i64/u64/f64 templates.
- GREEN: after adding i64/u64 generated-product assertions, the focused typed scalar run locks `zr_aot_u_value = zr_aot_u12;` / `zr_aot_u9 = zr_aot_u_value;`, `zr_aot_u_value = zr_aot_u33;` / `zr_aot_u21 = zr_aot_u_value;`, `zr_aot_s_value = zr_aot_s16;` / `zr_aot_s13 = zr_aot_s_value;`, and `zr_aot_s_value = zr_aot_s19;` / `zr_aot_s18 = zr_aot_s_value;`, while rejecting the old i64/u64 stack-copy source frame-payload reads.
- RED: after adding a focused `zr_aot_scalar_stack_copy_f64` generated-product assertion, the typed scalar test failed because the previous source program only evaluated `floatLeft * floatRight;` and did not create an executable f64 stack-copy marker.
- GREEN: after adding `var floatCopy: float = floatLeft;`, the generated product contains `zr_aot_scalar_stack_copy_f64`, `TZrFloat64 zr_aot_f22 = 0.0;`, `zr_aot_f_value = zr_aot_f19;`, and `zr_aot_f22 = zr_aot_f_value;`; the shared library result still matches the interpreter result.
- RED: after requiring the f64 stack-copy intermediate slot to stay local, source contracts failed on missing `backend_aot_c_scalar_locals_record_stack_copy_destinations(` and the generated product lacked `TZrFloat64 zr_aot_f40 = 0.0;`.
- GREEN: after propagating scalar-local kinds across `GET_STACK` / `SET_STACK` destinations in `backend_aot_c_scalar_locals.c`, the generated product declares `zr_aot_f40`, writes `zr_aot_f40 = zr_aot_f_value;`, reads `zr_aot_f_value = zr_aot_f40;`, and rejects the old f64 source-frame payload read.
- CHECK/GREEN: inspecting the focused unsigned branch source showed `unsignedInverted <= unsignedRight` does not use a separate unsigned/f64 branch lowering entry. The generated product computes `zr_aot_b23 = (TZrBool)(zr_aot_u21 <= zr_aot_u7);`, mirrors `zr_aot_u_result = zr_aot_b23;`, and then emits `if (!zr_aot_b23) {` through the existing bool-local false-branch path. Added generated-product assertions and stack-copy propagation source-contract tightening keep this checked route GREEN.
- RED: the first generated-product lock for primitive constants assumed the focused `uint` literals would directly mirror to `zr_aot_u6 = (TZrUInt64)9;` / `zr_aot_u7 = (TZrUInt64)4;`, but the current frontend emits those literals as signed `INT64` constants before the typed unsigned path maintains `uN`.
- GREEN: after narrowing the primitive constant mirror contract to the immediate payload types actually emitted by `GET_CONSTANT`, the generated product locks signed/float mirrors such as `zr_aot_s0 = (TZrInt64)21;`, `zr_aot_s6 = (TZrInt64)9;`, `zr_aot_f19 = (TZrFloat64)1.5;`, and `zr_aot_f20 = (TZrFloat64)2;`; source contracts and typed scalar focused runs pass.
- GREEN: after adding destination-local fallback mirror source-contract markers, the source contract locks `zr_aot_s%u = zr_aot_s_result;`, `zr_aot_u%u = zr_aot_u_result;`, `zr_aot_f%u = zr_aot_f_result;`, `zr_aot_b%u = zr_aot_s_result;`, and `zr_aot_b%u = zr_aot_u_result;` template coverage. Focused object compiles and the typed scalar generated shared library remain GREEN.
- RED: after tightening signed slot/slot branch source contracts to require `backend_aot_c_scalar_locals_i64_written_before(functionIr, leftSlot, execInstructionIndex)` and the matching right-slot check, the source-contract run failed because branch local eligibility still used broad scalar-local coverage only.
- GREEN: after adding the same-block written-before helper and passing the current instruction index through signed branch emitters, source contracts pass and the focused generated product keeps `if (zr_aot_s2 <= zr_aot_s4) {` while rejecting branch-local refresh assignments from `zr_aot_left/right->value.nativeObject.nativeInt64`; the signed const branch now conservatively accepts `if (zr_aot_left_scalar != zr_aot_right_literal) {` and rejects the unproven `if (zr_aot_s2 != zr_aot_right_literal) {`.

## Tests

The WSL CMake/Ninja path is still unreliable in this workspace because build graph verification stalls in `VerifyGlobs.cmake`. Focused validation directly compiled the relevant test sources and linked against the existing static WSL libraries from:

```text
build/codex-aot-m1-deopt-wsl-gcc-debug/lib
```

Observed results:

- AOT C typed scalar `i64`/`u64`/`f64` arithmetic/comparison/conversion plus `i64` and focused `u64` bitwise/bit-not/shift, `i64` shift/local-copy/branch/scalar-local declarations, scalar stack-copy source/destination local mirror with bool/i64/u64/f64 generated-product locks and f64 intermediate local propagation, unsigned compare to bool-local branch generated-product lock, primitive constant scalar-local mirror, scalar destination-local fallback mirror, signed slot/slot branch written-before no-refresh predicates, conservative signed const branch fallback, multi-kind reused scalar slots, signed i64 local arithmetic/compare/bitwise expressions, signed i64 shift/bit-not destination locals, bool branch condition-local predicate, u64 compare/local bitwise/bit-not/shift operands, TO_UINT, TO_UINT_SIGNED, TO_UINT_FLOAT, TO_FLOAT, TO_INT_FLOAT, and TO_INT_UNSIGNED local sources, plus u64/f64 local arithmetic: 1 test / 0 failures.
- This focused typed scalar run linked the test executable and generated shared library through temporary patched `libzr_vm_parser.a` and `libzr_vm_core.a` archives containing the current scalar AOT backend, `compile_expression.c`, `compiler_semir.c`, and `execution_dispatch.c` unsigned bitwise fix; the static WSL build directory itself was not fully rebuilt.
- Generated typed scalar product scan: `ZrCore_Stack_GetValue(` + `ZR_VALUE_FAST_SET(` count = 0.
- Generated typed scalar product contains the scalar lowering markers `zr_aot_scalar_exec_i64_binary`, `zr_aot_scalar_exec_i64_compare`, `zr_aot_scalar_exec_to_i64`, `zr_aot_scalar_exec_to_u64`, `zr_aot_scalar_exec_u64_binary`, `zr_aot_scalar_exec_u64_compare`, `zr_aot_scalar_exec_u64_bitwise`, `zr_aot_scalar_exec_u64_bit_not`, `zr_aot_scalar_exec_u64_shift`, `zr_aot_scalar_exec_to_f64`, `zr_aot_scalar_exec_f64_binary`, `zr_aot_scalar_exec_i64_bit_not`, `zr_aot_scalar_exec_i64_bitwise`, `zr_aot_scalar_exec_i64_shift`, `zr_aot_scalar_stack_copy_i64`, `zr_aot_scalar_stack_copy_f64`, `zr_aot_jump_if_signed_compare`, `zr_aot_jump_if_bool_false`, `zr_aot_jump_if_bool_false_scalar_local`, and `zr_aot_scalar_locals_begin` / `zr_aot_scalar_locals_end`.
- Generated typed scalar product contains current direct local markers including `TZrUInt64 zr_aot_u12 = (TZrUInt64)0u;`, `TZrUInt64 zr_aot_u13 = (TZrUInt64)0u;`, `TZrUInt64 zr_aot_u14 = (TZrUInt64)0u;`, `TZrInt64 zr_aot_s19 = (TZrInt64)0;`, `TZrFloat64 zr_aot_f22 = 0.0;`, `TZrFloat64 zr_aot_f40 = 0.0;`, `TZrInt64 zr_aot_s31 = (TZrInt64)0;`, `TZrUInt64 zr_aot_u31 = (TZrUInt64)0u;`, `TZrFloat64 zr_aot_f31 = 0.0;`, `TZrFloat64 zr_aot_f32 = 0.0;`, `TZrBool zr_aot_b23 = ZR_FALSE;`, `zr_aot_s0 = (TZrInt64)21;`, `zr_aot_s1 = (TZrInt64)2;`, `zr_aot_s4 = (TZrInt64)40;`, `zr_aot_s6 = (TZrInt64)9;`, `zr_aot_f19 = (TZrFloat64)1.5;`, `zr_aot_f20 = (TZrFloat64)2;`, `zr_aot_b23 = (TZrBool)(zr_aot_u21 <= zr_aot_u7);`, `zr_aot_u_result = zr_aot_b23;`, `if (!zr_aot_b23) {`, `if (zr_aot_s2 <= zr_aot_s4) {`, `if (zr_aot_left_scalar != zr_aot_right_literal) {`, `zr_aot_b_value = zr_aot_b7;`, `zr_aot_b5 = (TZrBool)(zr_aot_b_value != 0u);`, `zr_aot_u_value = zr_aot_u12;`, `zr_aot_u9 = zr_aot_u_value;`, `zr_aot_u_value = zr_aot_u33;`, `zr_aot_u21 = zr_aot_u_value;`, `zr_aot_s_value = zr_aot_s16;`, `zr_aot_s13 = zr_aot_s_value;`, `zr_aot_s_value = zr_aot_s19;`, `zr_aot_s18 = zr_aot_s_value;`, `zr_aot_f_value = zr_aot_f19;`, `zr_aot_f40 = zr_aot_f_value;`, `zr_aot_f_value = zr_aot_f40;`, `zr_aot_f22 = zr_aot_f_value;`, `zr_aot_b27 = (TZrBool)(zr_aot_s2 > zr_aot_s4);`, `zr_aot_b14 = (TZrBool)(zr_aot_u8 > zr_aot_u7);`, `zr_aot_f32 = zr_aot_f19 * zr_aot_f20;`, `zr_aot_s16 = zr_aot_s12 & zr_aot_s0;`, `zr_aot_u12 = zr_aot_u8 & zr_aot_u7;`, `zr_aot_u13 = zr_aot_u8 << zr_aot_s1;`, `zr_aot_u14 = zr_aot_u10 >> zr_aot_s1;`, `zr_aot_u33 = ~zr_aot_u7;`, `zr_aot_s19 = (TZrInt64)((TZrUInt64)zr_aot_s15 << zr_aot_s1);`, `zr_aot_s20 = zr_aot_s16 >> zr_aot_s1;`, `zr_aot_s19 = ~zr_aot_s1;`, `zr_aot_f31 = (TZrFloat64)zr_aot_s2;`, `zr_aot_s31 = (TZrInt64)zr_aot_f19;`, `zr_aot_s31 = (TZrInt64)zr_aot_u8;`, `zr_aot_u31 = (TZrUInt64)zr_aot_s2;`, `zr_aot_u31 = (TZrUInt64)zr_aot_f19;`, and `zr_aot_u_result = zr_aot_u31;`.
- Generated typed scalar product still rejects the old local-sync/fallback forms checked by the focused test, including `zr_aot_s0 = zr_aot_s_left;`, `zr_aot_u6 = zr_aot_u_left;`, `zr_aot_f19 = zr_aot_f_left;`, `zr_aot_s2 = zr_aot_left_scalar;`, `zr_aot_s4 = zr_aot_right_scalar;`, the unproven signed const predicate `if (zr_aot_s2 != zr_aot_right_literal) {`, and `if (!zr_aot_condition_bool) {`.
- Generated typed scalar product also rejects the old proven bool-local branch refresh marker `zr_aot_b5 = (TZrBool)(zr_aot_condition->value.nativeObject.nativeBool != 0u);`.
- Generated typed scalar product also rejects the old focused bool copy source-frame read marker `zr_aot_b_value = zr_aot_source->value.nativeObject.nativeBool;`.
- Generated typed scalar product also rejects the old focused i64/u64 copy source-frame read markers `zr_aot_s_value = zr_aot_source->value.nativeObject.nativeInt64;` and `zr_aot_u_value = zr_aot_source->value.nativeObject.nativeUInt64;`.
- Generated typed scalar product also rejects the old focused f64 copy source-frame read marker `zr_aot_f_value = zr_aot_source->value.nativeObject.nativeDouble;`.
- Unsigned bitwise interpreter regression: `tests/core/test_execution_unsigned_bitwise.c` focused run reports 7 tests / 0 failures, including unsigned bitwise shift left/right result-type preservation.
- AOT C source contracts: 19 tests / 0 failures.
- `zr_vm_core/src/zr_vm_core/execution/execution_dispatch.c` focused object compile after unsigned bitwise fix: exit 0 with existing warnings only.
- `zr_vm_parser/src/zr_vm_parser/compiler/compile_expression.c` focused object compile after typed unsigned shift opcode selection: exit 0.
- `zr_vm_parser/src/zr_vm_parser/compiler/compiler_semir.c` focused object compile after typed bitwise static type sync: exit 0.
- `zr_vm_aot/zr_vm_parser/src/zr_vm_parser/backend_aot/backend_aot_c_scalar_bitwise.c` focused object compile after unsigned u64 bit-not generated-product coverage: exit 0.
- `backend_aot_c_scalar_semir.c` focused format compile check: 0 failures.
- `backend_aot_c_scalar_semir.c` focused format compile check after signed i64 binary direct frame-slot-to-`sN` sync wiring: 0 failures.
- `backend_aot_c_scalar_semir.c` focused format compile check after unsigned u64 binary direct frame-slot-to-`uN` sync wiring: 0 failures.
- `backend_aot_c_scalar_semir.c` focused format compile check after f64 binary direct frame-slot-to-`fN` sync wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check: 0 failures.
- `backend_aot_c_scalar_bitwise.c` focused format compile check: 0 failures.
- `backend_aot_c_scalar_stack_copy.c` focused format compile check: 0 failures.
- `backend_aot_c_scalar_stack_copy.c` focused format compile check after destination-local mirror wiring: 0 failures.
- `backend_aot_c_scalar_stack_copy.c` focused format compile check after source-local mirror wiring: 0 failures.
- `backend_aot_c_scalar_locals.c` focused format compile check: 0 failures.
- `backend_aot_c_scalar_semir.c` focused format compile check after local-expression wiring: 0 failures.
- `backend_aot_c_scalar_semir.c` focused format compile check after u64 compare local-operand wiring: 0 failures.
- `backend_aot_c_scalar_bitwise.c` focused format compile check after bitwise, shift, and bit-not local-expression wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check after TO_FLOAT local-source wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check after TO_INT_FLOAT local-source wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check after TO_INT_UNSIGNED local-source wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check after TO_UINT local-source wiring: 0 failures.
- `backend_aot_c_scalar_conversion.c` focused format compile check after TO_UINT_SIGNED/TO_UINT_FLOAT local-source wiring: 0 failures.
- `backend_aot_c_scalar_locals.c` focused format compile check after multi-kind scalar local wiring: 0 failures.
- `backend_aot_c_scalar_locals.c` focused format compile check after conversion destination-local slot scanning: 0 failures.
- `backend_aot_c_scalar_binary.c`, `backend_aot_c_scalar_semir.c`, and `backend_aot_c_scalar_bitwise.c` focused object compiles after destination-local fallback mirror wiring: 0 failures.
- `backend_aot_c_function_body.c` focused format compile check: 0 failures.
- `backend_aot_c_lowering_control.c` focused format compile check: 0 failures.
- `backend_aot_c_function_body.c` and `backend_aot_c_lowering_control.c` focused format compile checks after signed branch local-operand wiring: 0 failures.
- `backend_aot_c_function_body.c` and `backend_aot_c_lowering_control.c` focused compile checks after bool branch condition-local wiring: 0 failures.
- `backend_aot_c_lowering_control.c` focused compile check after proven bool-local no-refresh branch wiring: 0 failures.
- `backend_aot_c_scalar_locals.c`, `backend_aot_c_lowering_control.c`, and `backend_aot_c_function_body.c` focused object compiles after signed branch written-before no-refresh wiring: 0 failures.
- AOT C frame setup contracts: 1 test / 0 failures.
- AOT C guardrail contracts: 4 tests / 0 failures.
- SemIR pipeline: 10 tests / 0 failures.
- SemIR typed opcode guardrails: 1 test / 0 failures.
- SemIR static C types: 1 test / 0 failures.
- SemIR dynamic arithmetic deopt: 1 test / 0 failures.
- SemIR dynamic index deopt: 1 test / 0 failures.
- SemIR static type conflict deopt: 1 test / 0 failures.

## Status

- Status: first `04-S1` / `04-S2` typed `i64` arithmetic/comparison/bitwise/shift, `u64` arithmetic/comparison/bitwise/bit-not/shift, focused numeric conversion, `f64` arithmetic, focused typed scalar local-copy, focused typed branch, `04-S3` scalar C local declaration skeleton, scalar stack-copy source/destination local mirror with bool/i64/u64/f64 generated-product locks and stack-copy destination temporary propagation, unsigned compare to bool-local branch generated-product lock, primitive constant scalar-local mirror, scalar destination-local fallback mirror, signed slot/slot branch same-block written-before no-refresh predicates, conservative signed const branch fallback, multi-kind reused scalar slot declarations, signed `i64` local arithmetic/compare/bitwise expression, signed `i64` binary direct frame-slot-to-`sN` synchronization, unsigned `u64` binary direct frame-slot-to-`uN` synchronization, executable unsigned `u64` binary bitwise/bit-not/shift direct-sync coverage, `f64` binary direct frame-slot-to-`fN` synchronization, signed `i64` shift/bit-not destination-local expression for reused temporary slots, focused numeric conversion destination-local expression, proven bool branch condition-local no-refresh predicate expression, unsigned `u64` compare local-operand expression, `TO_UINT`, `TO_UINT_SIGNED`, `TO_UINT_FLOAT`, `TO_FLOAT`, `TO_INT_FLOAT`, and `TO_INT_UNSIGNED` local-source expression, unsigned `u64` local arithmetic, and `f64` local arithmetic sub-slices complete.
- M2 remains partially complete. Signed `i64` binary arithmetic, signed `i64` compare, signed `i64` binary bitwise, unsigned `u64` binary arithmetic/compare/bitwise/bit-not/shift, and `f64` binary arithmetic currently use `sN` / `bN` / `uN` / `fN` expressions; the signed `i64`, unsigned `u64`, and `f64` binary proven-local paths now synchronize their covered operands directly from frame slots into `sN` / `uN` / `fN` before evaluating the expression. When scalar operands are not fully proven but the destination local is proven, covered binary/compare/bitwise/shift/bit-not fallback templates mirror the computed result into that destination local before writing the frame slot. Immediate primitive constants now mirror signed/float local payloads for covered slots before later scalar operations consume them, but `uint` literals still enter the current bytecode as signed `INT64` constants and are maintained as `uN` by later typed unsigned operations. The unsigned binary bitwise, bit-not, and shift executable goldens are now covered after the interpreter unsigned bitwise/shift result type fix and the compiler typed unsigned shift opcode selection fix. Signed `i64` shift and bit-not can write reused temporary `sN` destinations when SemIR destination metadata proves `i64` and scalar-local multi-kind declarations keep those locals available. Focused typed numeric conversions now declare and use temporary `sN` / `uN` / `fN` destinations when bytecode conversion opcodes prove the destination kind. Focused scalar stack-copy now reads copied bool/i64/u64/f64 sources from declared locals when available, mirrors destinations into declared locals when available, propagates proven scalar-local kinds across stack-copy destination temporaries, and focused proven bool branches consume those locals directly. The checked unsigned compare branch path is not a separate unsigned branch emitter; it is a `u64` compare that writes `b23` and then reuses the bool-local false-branch path. Fused signed slot/slot branches now require same-block local-written-before proof for both operands and branch directly on maintained `sN` locals without branch-time payload refresh; the focused signed const branch currently lacks that proof and falls back to `zr_aot_left_scalar`. All covered paths still mirror through frame slots. Broader branch variants, cross-block/join local-state proof, resume/deopt local-state reconstruction, systematic C-local/frame-slot authoritative storage, GC root registration, broader `03-S3` block form, non-numeric/generic conversion coverage, and typed/dynamic bridge/deopt execution are still open.
