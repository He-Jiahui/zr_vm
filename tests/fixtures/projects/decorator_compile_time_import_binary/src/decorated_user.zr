module decorated_user;

let provider = import("provider");

pub fn decoratedBonusDefault(): int {
    return provider.compute(seed: 10, factor: 3);
}

pub fn decoratedBonusNamed(): int {
    return provider.compute(10, bonus: 7);
}
