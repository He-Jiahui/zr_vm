module async_native;

let math = import("zr.math");
let ffi = import("zr.ffi");

pub fn nativeScore(): int {
    var vector = init math.Vector3(1.0, 2.0, 3.0);
    var handle = ffi.BufferHandle.allocate(8);
    return handle == null ? -1 : <int> vector.x;
}
