pub comptime fn staticPlanSize(seed: int, factor: int = 2, bonus: int = 3): int {
    return seed * factor + bonus;
}

pub fn planSize(seed: int, factor: int = 2, bonus: int = 3): int {
    return seed * factor + bonus;
}
