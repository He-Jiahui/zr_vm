class CounterRegistry {
    pub static var seed: int = 40;
    pub static var bonus: int = 2;

    pub static total(): int {
        return CounterRegistry.seed + CounterRegistry.bonus;
    }
}

return CounterRegistry.seed + CounterRegistry.total();
