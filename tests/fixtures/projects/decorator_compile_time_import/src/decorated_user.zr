module decorated_user;

let decorators = import("decorators");

pub class User {
    pub var id: int = 1;
}

pub fn decoratedBonus(): int {
    return decorators.markFunction();
}

pub var verifyDecorators = fn() => {
    return decorators.markClass();
};
