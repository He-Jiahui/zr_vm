module compile_time_parameter_decorator_artifact_baseline;

comptime class MarkParameter {
    @decorate(target: typeof Parameter): zr.DecoratorPatch {
        return { metadata: { compileTimeParameter: 62 } };
    }
}

pub fn measure(#MarkParameter# value: int, other: int = 2): int {
    return value + other;
}
