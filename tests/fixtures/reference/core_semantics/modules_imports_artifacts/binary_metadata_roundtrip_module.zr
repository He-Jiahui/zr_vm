module reference.binary_meta;

const MAX_SCALE: int = 8;

comptime fn buildBias(seed: int): int {
    return seed + MAX_SCALE;
}

pub var runtimeValue: int = MAX_SCALE;

pub fn binary_meta(): int {
    return runtimeValue;
}
