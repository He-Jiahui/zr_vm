%module "reference.binary_meta";

%compileTime var MAX_SCALE: int = 8;

%compileTime buildBias(seed: int): int {
    return seed + MAX_SCALE;
}

pub var runtimeValue: int = MAX_SCALE;

%test("binary_meta") {
    return runtimeValue;
}
